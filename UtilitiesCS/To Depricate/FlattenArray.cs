using System;
//using Microsoft.VisualBasic;
//using Microsoft.VisualBasic.CompilerServices;

namespace UtilitiesCS
{
    //public static class FlattenArray
    //{
    //    public static string FlattenStringTree(object[] varBranch)
    //    {
    //        string FlattenArryRet = default;
    //        // CLEANUP: Move to a library 
    //        int i;
    //        string strTemp;

    //        strTemp = "";

    //        var loopTo = Information.UBound(varBranch);
    //        for (i = 0; i <= loopTo; i++)
    //            strTemp = varBranch[i] is Array ? strTemp + ", " + FlattenStringTree((object[])varBranch[i]) : (string)Operators.ConcatenateObject(strTemp + ", ", varBranch[i]);
    //        if (strTemp.Length != 0)
    //            strTemp = Strings.Right(strTemp, Strings.Len(strTemp) - 2);
    //        FlattenArryRet = strTemp;
    //        return FlattenArryRet;
    //    }
    //}
}