# Final QC step 1 (C#) — FORMAT-APPLY ([P6-T5])

Timestamp: 2026-08-11T00-48
Command: `./.dotnet-sdk/dotnet.exe tool run csharpier format .`
EXIT_CODE: 0

## Console output

```
Formatted 1517 files in 2079ms.
```

## Post-run working-tree check

```
$ git status --porcelain -- '*.cs'
(empty)
```

**No `*.cs` file changed.** The C# loop therefore does **not** restart at [P6-T5].

The file count is **1517**, matching the [P0-T8] baseline, because the untracked Pester byproduct
`coverage.xml` (regenerated by [P6-T3]) was removed before this run — the same tool-byproduct cleanup
recorded in `FEATURE/evidence/qa-gates/csharpier-check.2026-08-10T23-50.md`.

## Output Summary

FORMAT-APPLY, the corrected step 1 this feature documents, returns `EXIT_CODE: 0` and formats 1517
files in 2.1 s. `git status --porcelain -- '*.cs'` is empty afterwards, so no `*.cs` file was
rewritten and the toolchain loop proceeds to step 2 without restarting.
