Timestamp: 2026-07-08T01-30

Baseline coverage: P0-T12 targeted-run `PhysicalFileInfoAdapter` class aggregate = 67.61% (covered-only basis) / 74.65% (covered+partially-covered basis), 71 total coverable lines. The pre-fix `Open(FileMode, FileAccess)` line itself was line_coverage="100.00" (covered via the real `FileShare.None` open).

Post-change coverage: P3-T4 targeted-run `PhysicalFileInfoAdapter` class aggregate = 66.67% (covered-only basis) / 76.00% (covered+partially-covered basis), 75 total coverable lines (4 more total lines than baseline, matching the seam-extension edit). The post-fix `Open(FileMode, FileAccess)` line remains line_coverage="100.00" (now covered via the seam delegate and the test-owned sentinel stream, with zero real-file handle). The small movement in the covered-only aggregate (67.61% -> 66.67%) is attributable entirely to the new internal-constructor null-guard line being counted as "partially covered" rather than "fully covered" (see Changed-line coverage below for why this is not a regression) plus the enlarged denominator (71 -> 75 total lines); it is not a loss of coverage on any specific line that was previously covered.

Changed-line coverage: 100% (>= 90% per CLAUDE.md/csharp.md UT2 new-code threshold). Per-line Cobertura coverage data (dotnet-coverage merge -f cobertura on the P3-T4 targeted run's .coverage file) for the four new/changed executable lines in `UtilitiesCS/HelperClasses/FileSystem/PhysicalFileInfoAdapter.cs`:
- Line 29 (`_openByModeAndAccess = _fileInfo.Open;`, public constructor default binding): hits=1 (covered).
- Line 44-45 (`_openByModeAndAccess = openByModeAndAccess ?? throw new ArgumentNullException(nameof(openByModeAndAccess));`, internal constructor null-guard, split across two lines by CSharpier): hits=1 on both lines (covered). Note: the MS-coverage-XML function-level aggregate reports this line as "partially covered" because the `throw` branch is never exercised (consistent with the pre-existing null-guard lines for `_appendText`/`_openByMode`/`_openWrite`, which exhibit the identical partial-coverage pattern for the same reason); Cobertura's hit-count metric (executed at least once) reports it as covered (hits=1), and both metrics agree the assignment path itself is exercised.
- Line 142 (`_openByModeAndAccess(mode, access);`, the `Open(FileMode, FileAccess)` delegation body, formerly line 134): hits=1 (covered).
All four changed executable lines show hits >= 1; changed-line coverage = 100%, satisfying the >= 90% new/changed-code threshold with no exceptions.

Assembly-wide coverage: 86.02% (P3-T6, UtilitiesCS.dll module line coverage from the full `UtilitiesCS.Test` suite run: 4173/4173 tests passed). This satisfies both the `.claude/rules/general-unit-test.md` >= 85% line-coverage floor and the CLAUDE.md/csharp.md >= 80% repository-wide floor simultaneously.

Regression check: No regression on changed lines. The `Open(FileMode, FileAccess)` line is covered at 100% both before and after the change (AC4: coverage preserved, not reduced). All newly added executable lines (the seam field's two binding assignments and the null-guard) are exercised at least once. Neither threshold (changed-line >= 90%, assembly-wide >= 85%/80%) is violated. Outcome: PASS.
