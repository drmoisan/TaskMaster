# Final QC — Acceptance Criteria Verification (P6-T8)

Timestamp: 2026-06-22T15-28

AC source: `docs/features/active/2026-06-18-outlook-startup-intelconfig-deserialize-stall-207/spec.md` → `## Acceptance Criteria` (AC1–AC13). Work Mode: `full-bug` (spec.md is the sole AC source for this corrective fix).

Format note: the spec AC items are authored as prose bullets (`- AC1 (gate): ...`), NOT markdown checkboxes. Per the `acceptance-criteria-tracking` skill, prose-format AC items are NOT reformatted into checkboxes; their PASS/verification status is recorded here in this tracking artifact instead. The `issue.md` checkbox AC items (AC1–AC6, I3-AC1–I3-AC6) pertain to the prior diagnostic increments 1–3 and are already checked; they are not the AC source for this corrective fix.

| AC | Verdict | Evidence / cited artifact |
|---|---|---|
| AC1 (gate) | PASS | `AppEvents.Hook()` no longer runs the three hookups synchronously; it constructs `OutlookReadinessGate` (cheap non-throwing `IsReady()` probe — `OutlookReadinessGate.cs:55`) and a `HookReadinessCoordinator`, then arms a `DispatcherTimer` poll. Hookups run only via `PerformReadinessHookup()` when the gate reports ready. Cite P1-T1, P1-T2, P2-T2, P2-T7. |
| AC2 (pump) | PASS | Wait uses `System.Windows.Threading.DispatcherTimer` poll (`AppEvents.cs:180`); no synchronous block, no banned delay API. Analyzer build clean. Cite P2-T2, P6-T2, P2-T7. |
| AC3 (never give up) | PASS | `HookReadinessCoordinator.Tick()` returns ContinuePolling indefinitely while not-ready, runs the callback exactly once on ready (run-once guard), and a transient COMException routes to retry (subscription never silently dropped). Unit-tested across the scripted timeline incl. extended not-ready run. Cite P1-T3, P1-T5 (100% coverage), P2-T2, P2-T7. |
| AC4 (COMException retry) | PASS | `OutlookReadinessGate.IsTransientError` discriminates 0xDAC40111 / 0x8E640111; `HookReadinessCoordinator` swallows a transient COMException as ContinuePolling; `AppOlObjects.LoadInboxes` routes transient HRESULTs to retry (shared `public const` HRESULTs, no duplicated literal). Cite P1-T2, P1-T3, P1-T5, P2-T3. |
| AC5 (offload) | PASS | `ProcessMailItemAsync` extracts COM primitives on the STA (`MailItemHelper.FromMailItemAsync`, `_ = helper.Tokens`), `await Task.Yield()` releases the STA for pure compute, and marshals back only for the `SetUdf("AutoProcessed", ...)` COM write; AutoProcessed marking and engine actions preserved on both paths; return semantics unchanged. Cite P3-T1, P3-T4. |
| AC6 (behavior preservation) | PASS | `PerformReadinessHookup()` retains the three hookups in original order; `Unhook()` reverses all three unchanged; inbox catch-up outcome unchanged. The updated `AppEventsTests` asserts the synchronous lifecycle ordering preserved by the new design. Cite P2-T1, P2-T4, P2-T7. |
| AC7 (end-to-end seam test) | PASS | `HookReadinessCoordinatorTests` drives not-ready×N → transient COMException → ready → exactly-once hookup, plus never-give-up, run-once-after-complete, Unhook interaction, non-transient propagation, with `Mock<IOutlookReadinessGate>` (no live COM/timer/filesystem). 100% line coverage. COM glue exemption recorded. Cite P1-T5, P2-T5, P6-T4. |
| AC8 (file-size remediation) | PASS | `AppOlObjects.cs` reduced 523 → 424 lines via `AppOlObjects.JunkFolders.cs` (133) partial extraction; all touched/new files ≤ 500 (max is `AppEvents.cs`/`AppEventsTests.cs` at 499). Cite P4-T7, P4-T8, P6-T6. |
| AC9 (cleanup) | PASS | `RemindersProbeSchedule.cs`, `RemindersProbeScheduleTests.cs` deleted and de-referenced; `RemindersProbeDelaySeconds` removed from `.settings`/`.Designer.cs`/`app.config`; `ScheduleDeferredRemindersProbe` removed. Zero remaining references (grep exit=1). Increment-1/2 `[Startup timing]` logs retained (22 references in `AppEvents.cs`). Cite P4-T1..T6. |
| AC10 (banned-API remediation) | PASS | Pre-existing `Task.Delay(100)` in `ProcessNewInboxItemsAsync` replaced by `await NonBlockingDelay.WaitAsync(TimeSpan.FromMilliseconds(100))` (`AppEvents.cs:462`). Touched-file sweep: zero real banned-API call sites (the only matches are in explanatory comments/XML docs). Cite P1-T4, P1-T7 (100% coverage), P3-T2, P3-T3, P6-T2. |
| AC11 (toolchain/coverage) | PASS | Full loop green in order in a single pass: CSharpier (1088 files, exit 0) → analyzers (0 errors) → nullable/TWAE (0/0; restore-vendored-then-incremental method per repo convention) → MSTest 117/117 with coverage. Repo-wide aggregate 12.89% → 13.04% (no regression). New testable types ≥ 90% (both 100%). net48 honored (coordinator result is a plain enum; no positional `record struct`; no CS0518). Cite P6-T1..T5. |
| AC12 (runtime validation) | PASS (procedure documented) | Manual cold-start runtime-capture procedure documented for the maintainer; explicitly not a unit test and not in the QC loop. Cite P6-T7 (`evidence/other/ac12-runtime-validation-procedure-2026-06-21T16-30.md`). The runtime capture itself is performed by the maintainer outside this QC loop. |
| AC13 (developer-only integration harness) | PASS | `LiveOutlookHookupIntegrationTests` exists, `[TestCategory("LiveOutlook")]`, STA + live Dispatcher + live Outlook Application, logs readiness + per-hookup latency, asserts completion + STA-not-blocked; EXCLUDED from CI run (`/TestCaseFilter:"TestCategory!=LiveOutlook"` — zero LiveOutlook tests executed in P6-T4) and from the coverage denominator; documented run command. Cite P5-T1, P5-T2, P5-T3, P6-T4. |

## Summary

All AC1–AC13 verified PASS with cited artifacts. AC12 is documented-procedure PASS (the runtime capture is a maintainer activity outside this QC loop). AC13 harness is present and CI-excluded as specified.
