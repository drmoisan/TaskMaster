# folder-settings-store-model-null (Plan)

- **Issue:** #262
- **Parent:** Epic #260 (store-lockup-resilience), F2, wave 0, `depends_on: []`
- **Owner:** drmoisan
- **Last Updated:** 2026-07-07T18-00
- **Status:** Draft
- **Version:** 0.1
- **Work Mode:** full-bug
- **Feature folder:** `docs/features/active/2026-07-07-folder-settings-store-model-null-262/`

**Mode note (full-bug):** `spec.md` is the authoritative requirements source (AC1–AC7). `user-story.md` is optional and absent by default; its absence is not a blocker. Full C# QA loop obligations apply (format → analyzers → nullable/type-check → MSTest with coverage).

**Fail-closed evidence rule:** Every baseline, regression, and QA command task below writes its own evidence artifact under `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/<kind>/`. If any required artifact is missing or incomplete (missing `Timestamp:`, `Command:`, `EXIT_CODE:`, or `Output Summary:`), the corresponding checklist item MUST remain unchecked and the plan outcome MUST be reported as remediation-required, never PASS.

**Evidence location invariant:** All evidence in this plan resolves to `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/<kind>/` (canonical sub-paths: `baseline/`, `regression-testing/`, `qa-gates/`, `issue-updates/`, `other/`). No task writes to `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or any other non-canonical path. This clause is non-overridable per `evidence-and-timestamp-conventions`.

**Scope lock (production files):** Production changes are confined to three files: `TaskMaster/AppGlobals/AppOlObjects.cs` (trimmed), the new `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs`, and `TaskMaster/TaskMaster.csproj` (Compile Include wiring only). Test changes are confined to `TaskMaster.Test/AppGlobals/AppOlObjectsCoverageTests.cs`. The following files MUST NOT be modified: `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs` (owned by F1 #261), `UtilitiesCS/EmailIntelligence/IntelligenceConfig.cs`, `UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs`, and `TaskMaster.Test/AppGlobals/AppOlObjectsTests.cs` (valid-config path only, orthogonal).

**Cross-feature note (F3 #263 depends on F2):** F3 also edits `AppOlObjects.cs`. The store-loading extraction in this plan MUST produce a clean, cohesive `AppOlObjects.StoreLoading.cs` partial (following the `AppOlObjects.JunkFolders.cs` precedent) so F3 builds on the split rather than re-splitting the file. No line-level overlap with F1 (#261), which edits `StoresWrapper.cs` only.

**Fix design (binding on Phases 1 and 3, verbatim from spec §Proposed Fix):**
- New seam: `protected internal virtual StoresWrapper BuildFreshStoresWrapper() => new StoresWrapper(_globals).Init();`
- Restructured `LoadStoresAsync`: bounded `try { ... } catch (Exception e) { logger.Error(...) }`; valid-config branch (`TryGetValue` true and deserialize non-null) preserves the existing deserialize-then-`AwaitStoreRewireAsync`-then-`return` behavior exactly; both recoverable branches (config-missing, null-deserialize) log `logger.Warn` and fall through to `StoresWrapper = BuildFreshStoresWrapper();`; the catch logs `logger.Error` with the exception object attached and `StoresWrapper`-specific context; no retry inside the catch; no new dialog.
- `StoreWrapperController.cs` is unmodified; its readiness guard already reports `Ready` when the model is non-null with populated `Stores`.

---

### Phase 0 — Policy Read & Baseline Capture

- [x] [P0-T1] Read `C:\Users\DanMoisan\repos\TaskMaster-wt-2026-07-07-13-21\CLAUDE.md` in full (policy order position 1, includes the embedded General/C# Code Change and Unit Test policies). Acceptance: file read start-to-end; no section skipped.
- [x] [P0-T2] Read `C:\Users\DanMoisan\repos\TaskMaster-wt-2026-07-07-13-21\.claude\rules\general-code-change.md` (policy order position 2). Acceptance: file read start-to-end.
- [x] [P0-T3] Read `C:\Users\DanMoisan\repos\TaskMaster-wt-2026-07-07-13-21\.claude\rules\general-unit-test.md` (policy order position 3). Acceptance: file read start-to-end.
- [x] [P0-T4] Read `C:\Users\DanMoisan\repos\TaskMaster-wt-2026-07-07-13-21\.claude\rules\csharp.md` (policy order position 4, C#-specific). Acceptance: file read start-to-end.
- [x] [P0-T5] Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:` (ISO-8601 `yyyy-MM-ddTHH-mm`), `Policy Order:` (the 4-item ordered list from P0-T1..T4), and an explicit list of the 4 file paths read. Acceptance: artifact exists with all three fields populated.
- [x] [P0-T6] Confirm `docs/features/active/2026-07-07-folder-settings-store-model-null-262/spec.md` is the authoritative requirements source and contains an explicit `## Acceptance Criteria` heading with AC1–AC7 beneath it; confirm `user-story.md` is absent and record that absence as expected (not a blocker) for full-bug mode. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/ac-source-confirmation.md` recording the `## Acceptance Criteria` heading location, the AC1–AC7 count (= 7), and the `user-story.md`-absent note. Acceptance: artifact records AC1–AC7 (count 7) from spec.md and the absence note.
- [x] [P0-T7] Capture the git baseline by running `git rev-parse HEAD` and `git branch --show-current` from the repo root. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/git-baseline.md` with `Timestamp:`, `Command:` (both commands), `EXIT_CODE:`, and `Output Summary:` (branch name and short SHA). Acceptance: artifact contains both command outputs.
- [x] [P0-T8] Capture the file-size baseline for the file being split by running a line count on `TaskMaster/AppGlobals/AppOlObjects.cs`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/file-size-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (recorded line count; expected 525, confirming the pre-fix over-cap state per AC6). Acceptance: artifact records the numeric line count for `AppOlObjects.cs`.
- [x] [P0-T9] Capture the formatting baseline by running `dotnet tool run csharpier --check .` from the repo root (check-only, no mutation). Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/csharpier-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (pass, or count of unformatted files). Acceptance: artifact exists with all four fields; EXIT_CODE is not `SKIPPED`.
- [x] [P0-T10] Capture the analyzer-build baseline by running `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` from the repo root. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/analyzer-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (build result, warning/error counts). Acceptance: artifact exists with all four fields.
- [x] [P0-T11] Capture the nullable/type-check baseline by running `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` from the repo root. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/nullable-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (build result, warning/error counts). Acceptance: artifact exists with all four fields.
- [x] [P0-T12] Capture the pre-change test + coverage baseline by running `vstest.console.exe TaskMaster.Test\bin\Debug\TaskMaster.Test.dll /EnableCodeCoverage`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/baseline/test-coverage-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including the numeric pass/fail count and the overall repository line-coverage percentage, and explicitly noting that the existing test `LoadStoresAsync_LeavesStoresWrapperNullWhenConfigMissing` currently PASSES (it encodes the bug as correct). Acceptance: artifact records a numeric coverage percentage (not `UNVERIFIED`) and the note that the mis-specified test currently passes.

### Phase 1 — Structural Extraction + Seam Introduction (behavior-preserving)

Rationale: The regression tests in Phase 2 override `BuildFreshStoresWrapper()`, which must exist for the test project to compile. The store-loading concern must also be extracted so `AppOlObjects.cs` returns under the 500-line cap (AC6). Phase 1 performs both as a behavior-preserving move: `LoadStoresAsync` is NOT yet restructured, so all existing tests (including the still-buggy config-missing test) continue to pass.

- [x] [P1-T1] Create `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs` as `public partial class AppOlObjects` in `namespace TaskMaster`, with a file-header comment stating it was extracted from `AppOlObjects.cs` to bring that file under the 500-line cap (Issue #262, AC6; precedent `AppOlObjects.JunkFolders.cs`). Move these members verbatim (no behavior change) from `AppOlObjects.cs` into the new file: `public async Task LoadAsync()` (currently `:34`), the `public StoresWrapper StoresWrapper { get; set; }` property (`:244`), `protected internal virtual Task AwaitStoreRewireAsync(StoresWrapper storesWrapper)` (`:246`), and `internal async Task LoadStoresAsync()` (`:251-263`, unchanged for now). In the same file add the new seam `protected internal virtual StoresWrapper BuildFreshStoresWrapper() => new StoresWrapper(_globals).Init();` (not yet called by any code path). Remove the moved members from `AppOlObjects.cs`. Acceptance: solution compiles; the moved members exist only in `AppOlObjects.StoreLoading.cs`; `AppOlObjects.cs` still references `StoresWrapper` at its remaining call sites (`:106`, `:126`) through the shared partial; `LoadStoresAsync` body is byte-for-byte unchanged from baseline; `BuildFreshStoresWrapper()` is present but uncalled.
- [x] [P1-T2] In `TaskMaster/TaskMaster.csproj`, add `<Compile Include="AppGlobals\AppOlObjects.StoreLoading.cs" />` immediately after the existing `<Compile Include="AppGlobals\AppOlObjects.JunkFolders.cs" />` item (this project uses explicit Compile items with no glob; a new `.cs` file is not compiled until wired). Acceptance: the csproj contains exactly one `Compile Include` entry for `AppGlobals\AppOlObjects.StoreLoading.cs`; no other Compile items are removed or reordered.
- [x] [P1-T3] Confirm the file-size cap (AC6) after extraction by running a line count on both `TaskMaster/AppGlobals/AppOlObjects.cs` and `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/other/file-size-after-extraction.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (both line counts). Acceptance: artifact records `AppOlObjects.cs <= 500` and `AppOlObjects.StoreLoading.cs <= 500`.
- [x] [P1-T4] Build the solution (analyzer pass `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`) and run the full `TaskMaster.Test` assembly (`vstest.console.exe TaskMaster.Test\bin\Debug\TaskMaster.Test.dll`) to confirm the extraction is behavior-preserving. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/other/extraction-behavior-preserved.md` with `Timestamp:`, `Command:` (both commands), `EXIT_CODE:`, and `Output Summary:` (build result; pass/fail count equal to the P0-T12 baseline count; explicit note that `LoadStoresAsync_LeavesStoresWrapperNullWhenConfigMissing` still passes). Acceptance: build EXIT_CODE `0`; test pass count equals the P0-T12 baseline; no test regressed.

### Phase 2 — Regression Tests First (Red)

All tests use the existing `StubApplicationGlobals`/`StubIntelligenceConfig`/`TestableAppOlObjects` seams in `AppOlObjectsCoverageTests.cs` plus Moq for `ISmartSerializableNonTyped`. No live Outlook; no temp files. `TestableAppOlObjects` is extended to override `BuildFreshStoresWrapper()` (recording an invocation count and returning a sentinel `StoresWrapper` instance) alongside its existing `AwaitStoreRewireAsync` override (which likewise records invocation).

- [x] [P2-T1] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsCoverageTests.cs`, invert the mis-specified existing test: rename `LoadStoresAsync_LeavesStoresWrapperNullWhenConfigMissing` (`:75-87`) to `LoadStoresAsync_WhenConfigMissing_BuildsFreshStoresWrapper` and replace the assertion `sut.StoresWrapper.Should().BeNull()` with assertions that (a) `sut.StoresWrapper` is the sentinel returned by the overridden `BuildFreshStoresWrapper()`, and (b) the `BuildFreshStoresWrapper()` override was invoked exactly once. Change the `sut` construction from `new AppOlObjects(...)` to the extended `TestableAppOlObjects` that overrides `BuildFreshStoresWrapper()`. Acceptance: method compiles, is discoverable by the runner, and asserts the fresh-build fallback (Path 1) rather than a null model (AC1, AC5).
- [x] [P2-T2] [expect-fail] Add MSTest method `LoadStoresAsync_WhenConfigDeserializesToNull_BuildsFreshStoresWrapper` to `AppOlObjectsCoverageTests.cs`: arrange a `ConcurrentDictionary` that contains the `"StoresWrapper"` key, with `Mock<ISmartSerializableNonTyped>.Setup(x => x.Deserialize<StoresWrapper, SmartSerializableLoader>(It.IsAny<SmartSerializable<SmartSerializableLoader>>())).Returns((StoresWrapper)null)`; use the extended `TestableAppOlObjects` with the `BuildFreshStoresWrapper()` sentinel override and the `AwaitStoreRewireAsync` invocation recorder; act via `await sut.LoadStoresAsync()`; assert `sut.StoresWrapper` is the sentinel, that `BuildFreshStoresWrapper()` was invoked exactly once, and that `AwaitStoreRewireAsync` was NOT invoked (the fresh-build path bypasses rewire). Acceptance: method compiles and is discoverable (AC2, AC5).
- [x] [P2-T3] [expect-fail] Add MSTest method `LoadStoresAsync_WhenDeserializeThrows_AbsorbsExceptionAndLeavesStoresWrapperNull` to `AppOlObjectsCoverageTests.cs`: arrange the `"StoresWrapper"` key present, with `Mock<ISmartSerializableNonTyped>.Setup(...Deserialize...).Throws<InvalidOperationException>()` (matching the `SmartSerializableBase.cs:166-187` throw site; `ArgumentNullException` is an acceptable equivalent); use the extended `TestableAppOlObjects`; act via `Func<Task> act = async () => await sut.LoadStoresAsync();`; assert with FluentAssertions that `act` does NOT throw, that `sut.StoresWrapper` remains `null`, and that `BuildFreshStoresWrapper()` was NOT invoked (no retry after a mid-deserialize exception). Acceptance: method compiles and is discoverable (AC3, AC5).
- [x] [P2-T4] [expect-fail] Run `vstest.console.exe TaskMaster.Test\bin\Debug\TaskMaster.Test.dll /TestCaseFilter:"FullyQualifiedName~LoadStoresAsync_WhenConfigMissing_BuildsFreshStoresWrapper|FullyQualifiedName~LoadStoresAsync_WhenConfigDeserializesToNull_BuildsFreshStoresWrapper|FullyQualifiedName~LoadStoresAsync_WhenDeserializeThrows_AbsorbsExceptionAndLeavesStoresWrapperNull"` against the Phase-1 production code (fix not yet applied) and confirm all three tests FAIL: P2-T1/P2-T2 fail because `StoresWrapper` stays null and the seam is never invoked; P2-T3 fails because the un-caught exception propagates out of `LoadStoresAsync`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/regression-testing/fail-before-262.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (the three test names and each failure reason). Acceptance: artifact records 3 failed, 0 passed for the filtered run; EXIT_CODE reflects the failing run and is not `SKIPPED` (AC5).

### Phase 3 — Minimal Behavioral Fix + Coverage Completion (Green)

- [x] [P3-T1] In `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs`, restructure `LoadStoresAsync()` exactly per the binding fix design above: wrap the body in `try { ... } catch (Exception e) { logger.Error($"Failed to load StoresWrapper; store settings will remain unavailable until this is resolved. {e.Message}", e); }`; inside the try, when `_globals.IntelRes.Config.TryGetValue("StoresWrapper", out var config)` is true, deserialize into a local, and when the local is non-null assign `StoresWrapper`, `await AwaitStoreRewireAsync(StoresWrapper)`, and `return` (preserving the valid-config behavior exactly); when deserialize returns null log `logger.Warn("StoresWrapper config deserialized to null; rebuilding from live stores.")`; in the `else` (key missing) branch log `logger.Warn("StoresWrapper config not found; rebuilding from live stores.")`; after both recoverable branches fall through, assign `StoresWrapper = BuildFreshStoresWrapper();`. Add a one-line comment at the `BuildFreshStoresWrapper()` call site recording the F1 (#261) interaction (fresh build has no persisted disabled-store state to restore). Acceptance: `LoadStoresAsync` matches the fix design; no `logger.Error("StoresWrapper config not found.")` bare-string call remains; `StoresWrapper.cs`, `IntelligenceConfig.cs`, and `StoreWrapperController.cs` are unmodified (AC1, AC2, AC3).
- [x] [P3-T2] In `TaskMaster.Test/AppGlobals/AppOlObjectsCoverageTests.cs`, add a direct-coverage test `BuildFreshStoresWrapper_WhenLiveStoresAvailable_ReturnsInitializedWrapper` that invokes the real (non-overridden) `BuildFreshStoresWrapper()` via a `TestableAppOlObjects` helper method that calls `base.BuildFreshStoresWrapper()`, using a Moq `IApplicationGlobals` → `IOlObjects` → `NameSpace` → `Stores` chain mirroring the proven `CreateGlobalsWithStores` pattern in `UtilitiesCS.Test/OutlookObjects/Store/StoresWrapperTests.cs` (mocked COM enumerator, no live Outlook, no temp files); assert the returned `StoresWrapper` is non-null with populated `Stores`. Acceptance: method compiles, is discoverable, and exercises the real seam body so the new `BuildFreshStoresWrapper` method reaches its coverage target (AC7).
- [x] [P3-T3] Run `vstest.console.exe TaskMaster.Test\bin\Debug\TaskMaster.Test.dll /TestCaseFilter:"FullyQualifiedName~LoadStoresAsync_WhenConfigMissing_BuildsFreshStoresWrapper|FullyQualifiedName~LoadStoresAsync_WhenConfigDeserializesToNull_BuildsFreshStoresWrapper|FullyQualifiedName~LoadStoresAsync_WhenDeserializeThrows_AbsorbsExceptionAndLeavesStoresWrapperNull|FullyQualifiedName~BuildFreshStoresWrapper_WhenLiveStoresAvailable_ReturnsInitializedWrapper"` and confirm all four now PASS. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/regression-testing/pass-after-262.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (4 passed, 0 failed). Acceptance: artifact records 0 failed for the filtered run; EXIT_CODE `0` (AC1, AC2, AC3, AC5).
- [x] [P3-T4] Run the full `TaskMaster.Test` assembly with coverage `vstest.console.exe TaskMaster.Test\bin\Debug\TaskMaster.Test.dll /EnableCodeCoverage` and confirm no regression against the P0-T12 baseline (the existing valid-config test `LoadAsync_AssignsStoresWrapperFromConfigAndCompletes` and the orthogonal `AppOlObjectsTests` still pass). Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/regression-testing/full-suite-after-262.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (pass/fail counts and the numeric repository line-coverage percentage). Acceptance: 0 failed; pass count >= P0-T12 baseline + new tests; EXIT_CODE `0`.

### Phase 4 — Final QA Loop (Toolchain + Coverage)

**Loop rule:** Run P4-T1 → P4-T4 in order. If any step fails or auto-fixes/changes any file, restart the loop from P4-T1. Do not proceed to P4-T5 until P4-T1 through P4-T4 complete cleanly in a single pass.

- [x] [P4-T1] Run `dotnet tool run csharpier .` (formatting, repo root) and confirm exit code `0` with no residual diff on `TaskMaster/AppGlobals/AppOlObjects.cs`, `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs`, or `TaskMaster.Test/AppGlobals/AppOlObjectsCoverageTests.cs`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/qa-gates/qa-01-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE `0`; artifact records no files reformatted (else restart the loop) (AC7).
- [x] [P4-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and confirm 0 new analyzer diagnostics relative to the P0-T10 baseline. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/qa-gates/qa-02-analyzers.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE `0`; artifact records diagnostic count and confirms no increase over baseline (AC7).
- [x] [P4-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and confirm 0 warnings/errors on the touched files. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/qa-gates/qa-03-nullable.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE `0` (AC7).
- [x] [P4-T4] Run `vstest.console.exe TaskMaster.Test\bin\Debug\TaskMaster.Test.dll /EnableCodeCoverage` (post-change, full assembly) and record the numeric post-change repository line-coverage percentage and the new/changed-line coverage for the store-loading logic in `AppOlObjects.StoreLoading.cs` (restructured `LoadStoresAsync` branches and the `BuildFreshStoresWrapper` seam). Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/qa-gates/qa-04-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including both numeric values. Acceptance: EXIT_CODE `0`; new/changed-code line coverage on the store-loading logic >= 90%; repository line coverage for the testable denominator >= 80% (CLAUDE.md C# Unit Test Policy) (AC7).
- [x] [P4-T5] Compare the P0-T12 baseline coverage/pass count against the P4-T4 post-change values and confirm (a) no regression on previously-covered lines, (b) new-code coverage on the store-loading logic >= 90%, and (c) repository line coverage remains >= 80% for the testable denominator. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/qa-gates/qa-05-coverage-delta.md` with `Timestamp:`, baseline coverage value, post-change coverage value, new-code coverage value, and a pass/fail verdict for each of the three checks. Acceptance: all three checks recorded PASS; if any is not PASS, the plan outcome is remediation-required, not PASS (AC7).

### Phase 5 — Acceptance Criteria Reconciliation & Documentation

- [x] [P5-T1] Confirm the file-size cap (AC6) in the final state by re-running the line count on `TaskMaster/AppGlobals/AppOlObjects.cs` and `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/other/file-size-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (both line counts). Acceptance: both files recorded `<= 500` lines (AC6).
- [x] [P5-T2] Confirm the scope lock: run `git diff --name-only` against the P0-T7 baseline commit and verify the only changed production files are `TaskMaster/AppGlobals/AppOlObjects.cs`, `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs`, and `TaskMaster/TaskMaster.csproj`, plus the single test file `TaskMaster.Test/AppGlobals/AppOlObjectsCoverageTests.cs`; verify NO change to `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs`, `UtilitiesCS/EmailIntelligence/IntelligenceConfig.cs`, `UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs`, or `TaskMaster.Test/AppGlobals/AppOlObjectsTests.cs`. Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/other/scope-lock-confirmation.md` with `Timestamp:`, `Command:`, and the changed-file list. Acceptance: artifact lists exactly the four permitted files and confirms none of the four prohibited files changed.
- [x] [P5-T3] Confirm AC4 without modifying the controller: record that `UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs` is byte-for-byte unchanged (from P5-T2) and that on the recoverable paths `StoresWrapper` is now non-null with populated `Stores`, so `EvaluateLaunchReadiness()` returns `Ready` and `Launch()` no longer shows "not available yet". Cite the P3-T3 pass-after evidence (Path 1/Path 2 produce a populated model). Write `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/other/ac4-controller-unchanged.md` with `Timestamp:` and the reasoning + evidence references. Acceptance: artifact confirms the controller is unchanged and maps the populated-model outcome to `Ready` (AC4).
- [x] [P5-T4] Update `docs/features/active/2026-07-07-folder-settings-store-model-null-262/spec.md` (the authoritative full-bug AC source) to check AC1–AC7 under `## Acceptance Criteria`, each annotated with the evidence artifact path that satisfies it (AC1→P2-T1/P3-T1/P3-T3; AC2→P2-T2/P3-T1/P3-T3; AC3→P2-T3/P3-T1/P3-T3; AC4→P5-T3; AC5→P2-T4/P3-T3; AC6→P1-T3/P5-T1; AC7→P4-T1..P4-T5). Additionally reconcile `issue.md`'s `## Acceptance Criteria` to its own AC1–AC6 numbering (issue.md AC1–AC5 map to spec AC1–AC5; issue.md AC6 (toolchain) maps to spec AC7; the spec-only file-size criterion (spec AC6) has no issue.md counterpart and is tracked in spec.md only). Mirror the exact updated spec.md section to `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/issue-updates/issue-262.<timestamp>.md` with `Timestamp:`, the exact text, and `PostedAs:`. Acceptance: spec.md AC1–AC7 checked with evidence-path annotations; issue.md AC1–AC6 reconciled to its own numbering; the mirror shows the checked spec.md section.
- [x] [P5-T5] Write a final plan-status summary to `docs/features/active/2026-07-07-folder-settings-store-model-null-262/evidence/other/plan-status-summary.md` listing each phase's completion state and the evidence artifact path backing it. Acceptance: summary references every evidence artifact produced in P0-T5 through P5-T4 by path.

---

## Acceptance Criteria Traceability

| AC | Satisfied by |
|----|--------------|
| AC1 (config missing → fresh build, not null) | P3-T1 (fix), P2-T1 (regression), P2-T4 (fail-before), P3-T3 (pass-after) |
| AC2 (null deserialize → fresh build; rewire not invoked) | P3-T1 (fix), P2-T2 (regression), P2-T4 (fail-before), P3-T3 (pass-after) |
| AC3 (genuine failure surfaced at Error; no throw, stays null, no retry, no new dialog) | P3-T1 (fix), P2-T3 (regression), P2-T4 (fail-before), P3-T3 (pass-after) |
| AC4 (`Launch()` opens with populated model; controller unmodified) | P5-T3 (reasoning + evidence), P3-T3 (populated-model outcome) |
| AC5 (deterministic MSTest suite; fail-before/pass-after; inverted mis-specified test; Moq; no live Outlook/temp files) | P2-T1..P2-T4 (fail-before), P3-T3 (pass-after) |
| AC6 (`AppOlObjects.cs` <= 500 via new partial; both files <= 500) | P1-T1 (extraction), P1-T3, P5-T1 |
| AC7 (full toolchain in order; new-code >= 90%; repo >= 80%; net48; no regression) | P4-T1..P4-T5 |

## Preflight

`DIRECTIVE: PREFLIGHT VALIDATION ONLY`
