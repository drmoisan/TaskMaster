Timestamp: 2026-08-06T16-16 (superseding the initial no-seam assessment)
Pre-seam red proof: The deterministic static precondition check inspected `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.Lifecycle.cs` before production adjustment. It located the private static `CreateAndShowViewer` factory and its constructor-only production call, emitted `EXPECTED_UNREACHABLE_PRIVATE_FACTORY`, and exited 1. Under the preserved constraints, no test could replace the concrete factory-created viewer without a real `FilterOlFoldersViewer`, reflection, or a global hook.

Same-gate invariant: In `AppOlObjects.FolderTreeService`, the removed branch attempted to complete the same initialization object as disposed after ownership had been cleared. `Dispose` is the sole writer of `_disposed`, holds `_folderTreeServiceGate`, and clears `_folderTreeServiceInitialization` before releasing that lock. A later composition holds the same gate before comparing initialization ownership. Therefore the removed reference-equality branch was unreachable; removing its ten lines preserved behavior.

Authorized production adjustments:

- The existing public constructor retains its optional, backwards-compatible instance-local `Func<IFilterOlFoldersViewer>` parameter. `ViewerFactory_ShowFailure_DisposesCandidateAndPreservesOriginalException`, `ViewerFactory_InvokeRequiredClose_ClosesOnceAndPreservesConstructionFailure`, and `ViewerFactory_InvokeFault_IsContainedWithoutReplacingConstructionFailure` execute its body, `CreateAndShowViewer`, and both `CloseViewerAfterInitializationFailure` branches with a local fake viewer.
- `CreateFolderTreeCompatibilityView` is a protected-internal instance-local factory. `CompatibilityFactory_DisposeAfterConstruction_DiscardsCandidate` calls the production compatibility-view method and disposes at the post-construction boundary, exercising the late-disposed disposal path without a real viewer.
- `OnFolderTreeViewCandidateCreated` is a protected-internal instance-local candidate hook. `CandidateCreated_DisposeBeforeCommit_DiscardsCandidate` disposes between compatibility-view creation and `TryCommitFolderTreeView`, exercising the commit disposal branch without a global hook.
- No Outlook visibility adjustment was required. P5-T45 uses the existing pending-refresh lifecycle to reach the disposed false-authorization branch through public behavior.

Green verification: `dotnet tool run csharpier format UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.Lifecycle.cs UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.Coverage.cs`; then the analyzer build; then the focused controller fixture. The build passed with zero errors and the fixture passed 13/13. `FilterOlFoldersController.Lifecycle.cs` is 496 lines and the coverage partial is 258 lines.

Changed production line counts from `git diff --numstat`: `AppOlObjects.FolderTreeService.cs` 0 added / 10 removed; `FilterOlFoldersController.Lifecycle.cs` 24 added / 9 removed; `FilterOlFoldersController.cs` 9 added / 0 removed. The additions are exercised by the listed deterministic tests. No code was excluded, no behavior was removed other than the invariant-proven unreachable branch, and no real viewer, message loop, reflection, global dispatcher mutation, global mutable hook, live Outlook, network access, temporary file, timer, or polling loop was used.
