# Remediation Plan: Folder-tree dispatcher thread affinity

- **Issue:** #420
- **Work mode:** full-bug
- **Remediation-plan revision:** 7
- **Authoritative remediation requirements:** `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/remediation-inputs.2026-08-04T19-47.md`
- **Review inputs:** `policy-audit.2026-08-04T19-47.md`, `code-review.2026-08-04T19-47.md`, and `feature-audit.2026-08-04T19-47.md` in this feature folder
- **Evidence root:** `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/`

## Invariants and completion conditions

- Do not add `Task.Yield`, a worker-local WPF dispatcher, or caller-selected traversal fallback. Live Outlook hierarchy access, notification subscription cleanup, and `WpfDispatcherYield` remain on the captured Outlook STA dispatcher.
- Preserve the public `IOutlookFolderTreeService.GetSnapshotAsync` contract, one session-scoped service instance, request coalescing, cancellation behavior, and immutable snapshot semantics.
- Use deterministic MSTest, Moq, and FluentAssertions tests only. Do not use Outlook, network calls, temporary files, sleeps, timers, polling, retries, or a manual validation step.
- The public-entry fault-observability remediation in Phases 1 and 3 has a fixed source budget: exactly three existing production C# files (`UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs`, `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs`, and `TaskMaster/Ribbon/RibbonViewer.cs`) and two existing test C# files (`UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` and `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs`). That P1/P3 budget excludes the sole P5-T6 split test file and its sole `UtilitiesCS.Test/UtilitiesCS.Test.csproj` compile entry, required to keep test C# files within the 500-line limit. Do not add other source files, project-file changes, or global mutable test hooks for this scope.
- All evidence files must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` and must be written under the canonical feature evidence root.
- Do not mark an acceptance criterion complete until the named test or QA evidence is regenerated and independently reconciled. A missing comparable coverage measure or a new method below 90% is remediation-required, not PASS.
- Cycle 3 begins because seven later audit findings invalidate prior completion evidence for the affected service, notification, folder-filter, and ribbon paths: H1 worker-first composition can deadlock when a UI caller synchronously waits for a worker-owned initialization that requires queued UI composition; H2 a folder-filter viewer can be shown before a synchronous constructor/service-composition failure leaves it unowned; H3 a pool-thread `SnapshotChanged` event can start a fire-and-forget filter refresh that accesses `ArchiveRoot` COM or mutates viewer state off the UI dispatcher and loses faults; H4 the ribbon `async void` callback has no defined exception boundary; M1 disposal can occur after service publication but before initialization completion and expose a disposed service to waiters; M2 scheduled notification refresh faults are unobserved; M3 disposal does not cancel in-flight traversal after sink cleanup. Cycle-2 evidence, including the 165/189 (87.30%) changed-line coverage failure, is historical evidence only. Do not run Phase 7, update final documentation, or check any acceptance criterion until P5-T39 and the restarted Phase 6 cycle-3 evidence pass. Do not record a coverage exception, waiver, policy change, global test hook, live Outlook/UI test, temporary file, timer, sleep, polling loop, retry loop, `Task.Yield`, a worker-local dispatcher, or caller-selected traversal fallback for this remediation.
- Preserve the synchronous public `FolderTreeService` API while ensuring a UI-dispatcher caller never synchronously waits on a worker-owned initialization task that itself requires that dispatcher. Dedicated STA WPF tests must use controlled dispatcher work and task-completion signals, not elapsed-time assertions. A viewer created by either public entry path must be closed on every synchronous construction/service-composition failure without replacing the original exception. M3 must be remediated with deterministic cancellation evidence or explicitly dispositioned with source-backed proof that traversal cancellation is impossible at the current COM boundary and an accepted scoped follow-up; neither outcome permits an acceptance-criteria PASS without the resulting evidence.
- **Cycle-3 revision-6 planning stop:** Reopen only P5-T14, P5-T15, and P5-T17; P5-T16 remains checked historical red evidence, and all later tasks remain unchecked. P6 and P7 are hard-blocked until P5-T39 records **M3: PASS** and the controlling cycle-3/pass-3 mapping records current PASS evidence for every required AC, CR, and revision-6 finding. This revision-6 stop is superseded by the single authorized Revision-7 Cycle 4 scope below; no further remediation cycle is authorized. Four orphaned `vstest` processes that held test-output DLL copy locks were terminated by the coordinator; classify a recurrence as stale-process test infrastructure only after recording the process identity and rerunning the same command serially with no concurrent test process. Do not classify such a lock as a product-code failure or a passing skipped test.
- **Revision-7 scope:** Retain the production scope `TaskMaster/AppGlobals/AppOlObjects.FolderTreeService.cs`, `UtilitiesCS/Threading/WpfUiDispatcher.cs`, `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs`, `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.Lifecycle.cs`, `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs`, `TaskMaster/Ribbon/RibbonViewer.cs`, and `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs`. Preserve `UtilitiesCS/Threading/IUiDispatcher.cs`, `IUiDispatcher.BeginInvoke(Action): IAsyncResult`, all QuickFiler/mock signatures, and production behavior. Only after P5-T40 proves a required private path is untestable under the preserved test constraints may a deterministic test first demonstrate that gap and authorize the smallest instance-local testability seam; the seam must preserve existing behavior and public surface, must be exercised by the new test, and cannot delete behavior, exclude code, or be added solely to game coverage. Retain the authorized existing test/project scope and add only these capacity-authorized partial test files with their sole adjacent `Compile` entries: `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.Coverage.cs` in `TaskMaster.Test/TaskMaster.Test.csproj`; `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.Coverage.cs` in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`; and `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.Coverage.cs` in `UtilitiesCS.Test/UtilitiesCS.Test.csproj` after making its existing class partial. Do not add any other source, test, or project file. Every changed C# file, including each new coverage partial, must remain at or below 500 lines; only `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs` remains at or below 300 lines. The coverage partials are required because their corresponding existing files are at 490, 497, and 498 lines, respectively; preserve their completed history without inserting additional tests into them. Preserve the existing no-real-viewer, no-message-loop, no-reflection, no-global-dispatcher-mutation, no-global-mutable-hook, no-live-Outlook, no-network, no-timer, no-polling, and no-temporary-file constraints.

### Phase 0 — Remediation policy and comparable baseline

- [x] [P0-T1] Read `AGENTS.md` in its prescribed standing, general code-change, general unit-test, and C# policy order, then read `.agents/skills/policy-compliance-order/SKILL.md`, `.agents/skills/csharp/SKILL.md`, `.agents/skills/atomic-plan-contract/SKILL.md`, `.agents/skills/acceptance-criteria-tracking/SKILL.md`, and `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`; record the ordered files and non-negotiable no-fallback, deterministic-test, and coverage constraints in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/phase0-instructions-read.<timestamp>.md`.
- [x] [P0-T2] Re-read `remediation-inputs.2026-08-04T19-47.md`, the three review artifacts, `spec.md`, and the uncommitted `origin/main` diff; write a finding-to-task and acceptance-criterion-to-test mapping that identifies CR-001 through CR-007 and AC2/AC4/AC5/AC6/AC7/AC8 as unverified in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/remediation-requirements-mapping.<timestamp>.md`. Based on the feature-audit results, reconcile the current unsupported source status by changing only AC2, AC4, AC5, AC6, AC7, and AC8 in `spec.md` from `[x]` to `[ ]`; leave AC1 and AC3 unchanged. Record every changed acceptance-criterion status and its review-evidence basis in the mapping artifact.
- [x] [P0-T3] Run `dotnet tool run csharpier .`; record the result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/csharpier.<timestamp>.md`. If formatting changes files or the command fails, preserve the formatter result or correct the failure and restart Phase 0 from P0-T3.
- [x] [P0-T4] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; record diagnostics and exit status in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/analyzers.<timestamp>.md`.
- [x] [P0-T5] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; record compiler and nullable diagnostics in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/nullable.<timestamp>.md`.
- [x] [P0-T6] Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/coverage-baseline.cobertura.xml`; record numeric repository line/branch coverage and test totals in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/mstest-coverage.<timestamp>.md`.
- [x] [P0-T7] Record the baseline coverage command, `SearchRoot`, configuration, discovered test assemblies, Cobertura source-file count, and lines-valid denominator from P0-T6 in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/remediation-baseline/coverage-scope.<timestamp>.md`; later coverage comparison is valid only if these scope fields match.

### Phase 1 — Failing lifecycle and dispatcher regressions

- [x] [P1-T1] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs`, add a deterministic controlled interleaving where a worker begins first service composition and a UI-dispatcher-side `Dispose` competes for the service state; assert the worker does not hold `_folderTreeServiceGate` while waiting for UI execution and that the resulting service is either safely published once or discarded. Record the current failure in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/service-gate-dispose-fail-before.<timestamp>.md`.
- [x] [P1-T2] [expect-fail] In `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceDisposalTests.cs`, add a controlled incomplete builder test that calls `Dispose` before build completion, then releases the builder and asserts state remains `Disposed`, no snapshot is published, no refresh is scheduled, and `SnapshotChanged` is not raised. Record the current failure in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/publish-after-dispose-fail-before.<timestamp>.md`.
- [x] [P1-T3] [expect-fail] In `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceInvalidationTests.cs` and `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceDisposalTests.cs`, add dedicated-STA, recording notification-sink fakes that prove notification-driven refresh, unsubscribe, and sink disposal run on the same captured dispatcher as construction. Record the current failure in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/notification-cleanup-affinity-fail-before.<timestamp>.md`.
- [x] [P1-T4] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs`, add a controllable snapshot task and fake viewer test proving a view closed before snapshot completion is never wired, receives no service handler, and leaves no retained folder-tree handler. Record the current failure in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-close-during-load-fail-before.<timestamp>.md`.
- [x] [P1-T5] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs`, add deterministic faulted-snapshot regressions without opening a WinForms viewer or starting a message loop: (1) locate the public `CreateAsync(IApplicationGlobals, Func<IFilterOlFoldersViewer>)` optional-factory signature and pass a fake viewer factory, then assert the returned task exposes the original `InvalidOperationException`; (2) verify the public one-argument `FilterOlFoldersController(IApplicationGlobals)` and public `Readiness` contract exist, then construct through an injected-viewer delegating constructor that is required to share that constructor's initialization path and await `Readiness`, asserting the same original fault. Both cases must leave the viewer unwired with zero folder-tree handlers. Record the current failure in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-construction-fault-fail-before.<timestamp>.md`.
- [x] [P1-T6] [expect-fail] In `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs`, add a faulted controlled filter-initialization task and assert the asynchronous seam propagates it to the selected UI error boundary while the legacy `TryLoadFolderFilter` path cannot silently lose it. Record the current failure in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/ribbon-filter-fault-fail-before.<timestamp>.md`.
- [x] [P1-T7] [expect-fail] Create `UtilitiesCS.Test/Threading/WpfUiDispatcherTests.cs` and add its compile entry to `UtilitiesCS.Test/UtilitiesCS.Test.csproj`; use a dedicated STA dispatcher host to prove `WpfUiDispatcher.InvokeAsync(Func<Task<TResult>>)` returns a successful result, propagates an inner task fault, and honors cancellation before dispatch. Record the initially missing or uncovered behavior in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/wpf-ui-dispatcher-async-overload-fail-before.<timestamp>.md`.
- [x] [P1-T8] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs`, add a dedicated STA WPF dispatcher-host H1 test against the narrow instance-scoped composition-ownership seam planned in P2-T8. The test must require the seam to expose whether the captured STA can execute queued composition rather than synchronously wait on a worker-owned initialization. Until P2-T8 exists, preserve the deterministic compile-fail result as the red evidence; do not invoke the current deadlocking public getter on a real STA, use a timeout/timer/polling, or leave a blocked test thread. Record the compile-fail command, missing seam, expected assertion, assembly, and result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-ui-waiter-deadlock-fail-before.<timestamp>.md`.
- [x] [P1-T9] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs`, add deterministic H2 fake-viewer-factory regressions for synchronous controller/service-composition failure before `Readiness` exists. Invoke public `CreateAsync(IApplicationGlobals, Func<IFilterOlFoldersViewer>?)` with a fake factory and a known `InvalidOperationException`; assert the returned task exposes that exact exception, the factory-created viewer closes exactly once, and no controller or folder-tree handler is retained. Cover the equivalent legacy public-constructor factory path through the narrowest deterministic seam, or record source-backed proof that it shares the same cleanup helper. Do not use a real viewer, message loop, global hook, reflection, timing, or a substitute exception. Record evidence in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-factory-synchronous-fault-fail-before.<timestamp>.md`.
- [x] [P1-T10] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs`, add a dedicated-STA regression that requires a worker-originated `SnapshotChanged` refresh, every COM-facing access, and every viewer mutation to execute on the captured STA and requires the original controlled refresh fault to reach the defined observed error boundary. Assert the required behavior, state that it fails against the current implementation, and record the failed assertion. Use fake service/viewer and task-completion signals only; do not use live Outlook, a real viewer, timer, polling, or GC-based unobserved-task detection. Record evidence in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-worker-snapshotchanged-fail-before.<timestamp>.md`.
- [x] [P1-T11] [expect-fail] In `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs`, add a controlled folder-filter initialization fault after the ribbon `async void` callback begins and require the original exception to be observed by one deterministic log/user-error boundary. Assert the required behavior, state that it fails against the current implementation, and record the failed assertion without a real ribbon, message loop, timer, or global hook in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/ribbon-async-void-fault-boundary-fail-before.<timestamp>.md`.
- [x] [P1-T12] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs`, add an H5/M1 test against the narrow instance-scoped publication-completion interleaving hook planned in P2-T8. The test must pause deterministically after service publication and before initialization completion, dispose, release a waiter, and require that no waiter receives an already-disposed service. Until P2-T8 exists, preserve the deterministic compile-fail result as red evidence; do not race production state, use reflection, timing, a live UI/Outlook host, or a global hook. Record evidence in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-publish-dispose-race-fail-before.<timestamp>.md`.
- [x] [P1-T13] [expect-fail] In `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceInvalidationTests.cs`, add a controlled notification-scheduled refresh fault and require it to be observed by the existing service policy, with no unobserved/fire-and-forget task or unexpected retry. Assert the required behavior, state that it fails against the current implementation, and record the failed assertion. Reuse the existing notification, controlled dispatcher, and reader seams; do not use live Outlook/UI, timing, polling, or a dispatcher fallback. Record evidence in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/notification-refresh-fault-fail-before.<timestamp>.md`.
- [x] [P1-T14] [expect-fail] Create `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs`, add only its required compile entry to `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, and add a controlled in-flight traversal/disposal H7/M3 regression. Require disposal to cancel every cancellable traversal and yield boundary and terminally isolate any non-cancellable live COM segment, with no later publication, scheduling, event, or notification reattachment. Assert the required behavior, state that it fails against the current implementation, and record the failed assertion without timers, polling, live Outlook, or temporary files in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/dispose-inflight-traversal-cancellation-fail-before.<timestamp>.md`. Keep the new test file below 500 lines.

### Phase 2 — Session composition and service lifetime repair

- [x] [P2-T1] Modify `TaskMaster/AppGlobals/AppOlObjects.cs` so `FolderTreeService` represents first composition with a single awaitable/stateful initialization result created under `_folderTreeServiceGate` but awaited outside that gate; invoke the complete live composition root only through the captured `IUiDispatcher`, prevent worker fallback, and make `Dispose` safely cancel, await, or discard uncommitted initialization without a UI-dispatch/gate cycle.
- [x] [P2-T2] Update `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` so the P1-T1 interleaving passes and separately asserts one worker-first composition executes on the captured STA, concurrent callers receive one session service, and disposal never leaves a service publication after shutdown.
- [x] [P2-T3] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` to make disposal terminal under `_gate`: atomically prevent new builds/refreshes, detach notification handlers, and suppress post-dispose state changes, snapshot publication, scheduling, and `SnapshotChanged` invocation from an already-running build.
- [x] [P2-T4] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` so notification unsubscription and sink disposal dispatch through the same injected captured STA dispatcher used for construction/builds, while disposing from a caller already on that dispatcher completes without a self-wait.
- [x] [P2-T5] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceDisposalTests.cs` so P1-T2 passes and covers a completed build, an in-flight cold build, and an in-flight refresh disposed before publication, each asserting terminal state and no post-dispose event.
- [x] [P2-T6] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceInvalidationTests.cs` and `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceConcurrencyTests.cs` so P1-T3 passes and proves notification-triggered refresh, notification unsubscription, and sink disposal occur on the captured STA while concurrent requests retain single-build coalescing and current invalidation behavior.
- [x] [P2-T7] Extract only the existing folder-tree composition members from `TaskMaster/AppGlobals/AppOlObjects.cs` into the partial `AppOlObjects` implementation `TaskMaster/AppGlobals/AppOlObjects.FolderTreeService.cs`, preserving behavior and public API, then add its explicit `Compile` entry to `TaskMaster/TaskMaster.csproj`. Before further AppOlObjects edits, verify both `TaskMaster/AppGlobals/AppOlObjects.cs` and `TaskMaster/AppGlobals/AppOlObjects.FolderTreeService.cs` are at most 500 lines and record their line counts in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/appolobjects-file-size-remediation.<timestamp>.md`.
- [x] [P2-T8] In the extracted AppOlObjects folder-tree composition implementation, add only narrow instance-scoped protected/internal testability hooks for composition ownership and the publication-before-completion interleaving; the hooks must be unavailable to production callers, require no global mutable state, and let tests release controlled work without blocking a real STA or racing publication. Do not change the public `FolderTreeService` contract or add an alternate production code path.
- [x] [P2-T9] Modify the extracted AppOlObjects folder-tree composition implementation to resolve H1 with a single non-blocking composition protocol: worker-first and concurrent captured-STA callers retain one session-scoped service, the STA dispatcher never synchronously waits on a worker-owned initialization that requires queued STA work, and all live Outlook composition remains on the captured STA. Preserve disposal, exception reset/retry, one-service coalescing, and the no-fallback invariant; do not add `Task.Yield`, create a worker-local dispatcher, or weaken the synchronous public API.
- [x] [P2-T10] Update `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` so P1-T8 passes on its dedicated STA host. Assert controlled queued composition executes without a UI-waiter deadlock, both worker and STA callers receive the same service, and composition occurs on the captured STA through task/dispatcher signaling only.
- [x] [P2-T11] Modify the extracted AppOlObjects folder-tree composition implementation to resolve M1 by making service publication, initialization completion, and disposal one terminally consistent state transition. A waiter released after concurrent `Dispose` must not receive a service that has already been disposed; preserve original exception/disposal contracts and do not hold a gate while waiting for dispatcher work.
- [x] [P2-T12] Update `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` so P1-T12 passes. Assert the publication/disposal interleaving gives every waiter a non-disposed service or the expected original terminal exception, with no duplicate service or hidden retry.
- [x] [P2-T13] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` to own and propagate disposal cancellation to every cancellable traversal and yield boundary while suppressing all post-dispose state changes, publication, scheduling, events, and notification reattachment.
- [x] [P2-T14] Inspect every traversal segment and always write `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/dispose-inflight-traversal-disposition.<timestamp>.md`. Record that individual live COM property/method calls remain non-interruptible, while cancellation must reach every cancellable boundary. Retain M3 as non-PASS unless the P5-T31 through P5-T36 evidence, reconciled by P5-T39, proves that disposal during `Building` or `Refreshing` defers notification cleanup through the captured dispatcher, eliminates post-cleanup COM access, observes queued-cleanup faults, and preserves terminal isolation. A non-cancellable-boundary disposition is not a fix and cannot authorize Phase 6, Phase 7, or an acceptance-criteria PASS.
- [x] [P2-T15] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs` so P1-T14 passes. Assert cancellation reaches every cancellable traversal/yield boundary and no post-dispose result publishes, schedules, raises events, or reattaches notifications; when P2-T14 records a non-cancellable segment, assert it remains terminally isolated and retain M3 as non-PASS. Keep the file below 500 lines.

### Phase 3 — Observable cold-view initialization and ribbon fault policy

- [x] [P3-T1] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs` within the fixed three-production/two-test-file budget to preserve the one-argument public `FilterOlFoldersController(IApplicationGlobals)` constructor and its public `Readiness` property, while making both use one canonical injected-viewer initialization path. Add only the unreleased public optional-factory signature `CreateAsync(IApplicationGlobals appGlobals, Func<IFilterOlFoldersViewer>? viewerFactory = null)`: production must default a null factory to `CreateAndShowViewer`, and tests must pass the same public method a fake-viewer factory. Add an internal/protected delegating injected-viewer constructor or equivalent common path that starts the same readiness task as the legacy constructor, so its fault contract is testable without a UI message loop. The factory must await and return only after that readiness task succeeds; both paths must propagate the original snapshot fault and must not use a global mutable hook, a real viewer in tests, or a second initialization path.
- [x] [P3-T2] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs` to subscribe close/cancellation handling before the first cold snapshot await, detach it on every terminal path, and prevent viewer wiring, folder-tree event subscription, or refresh after close or failed initialization.
- [x] [P3-T3] Update `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` so P1-T4 and P1-T5 pass without reflection against non-public factory APIs or any live UI: prove the public optional-factory `CreateAsync` receives a fake viewer factory, wires only after a successful snapshot, and propagates the original snapshot exception with no retained handlers; prove the internal injected-viewer delegating constructor shares the legacy public constructor's `Readiness` initialization path and exposes the same original exception when `Readiness` is awaited; and prove close-before-completion leaves the viewer and service unwired. Record the red-before and green-after test names, assembly, and results in the P1-T4/P1-T5 evidence files.
- [x] [P3-T4] Modify `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs` and `TaskMaster/Ribbon/RibbonViewer.cs` to define one existing UI-boundary failure policy for folder-filter initialization, await that operation at the `async void` ribbon callback boundary, and remove or redirect the legacy synchronous wrapper so it cannot discard the task or its exception.
- [x] [P3-T5] Update `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs` so P1-T6 passes and asserts both successful awaited initialization and the defined fault-handling result without an unobserved task.
- [x] [P3-T6] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs` so a viewer created by the public optional factory is owned immediately by a failure-safe construction boundary. If the controller constructor or synchronous folder-tree-service composition throws before `Readiness` exists, close that exact viewer once, preserve and rethrow the original exception, and leave no controller or folder-tree subscription. Make the legacy public constructor use the same construction/cleanup path without a second initialization flow, global mutable hook, or real-viewer test dependency.
- [x] [P3-T7] Update `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` so P1-T9 passes. Assert `CreateAsync` propagates the exact synchronous service-composition exception after closing its fake factory-created viewer once and retaining no handlers; assert the legacy constructor shares that contract through the existing deterministic seam.
- [x] [P3-T8] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs` to resolve H3: marshal `SnapshotChanged` refresh work and every `ArchiveRoot`/viewer mutation to the captured UI dispatcher, await or otherwise observe refresh completion at the existing error boundary, and preserve close/disposal guards. Do not run Outlook COM access on a thread-pool continuation, introduce `Task.Yield`, a worker dispatcher, a caller-selected fallback, a real UI dependency, or a fire-and-forget fault path.
- [x] [P3-T9] Update `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` so P1-T10 passes on a dedicated STA host. Assert a worker-thread notification dispatches the full refresh to the captured STA, no view/COM-facing operation runs on the worker, and a controlled refresh failure reaches the defined observed error path while close/disposal leaves no handlers.
- [x] [P3-T10] Modify `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs` and `TaskMaster/Ribbon/RibbonViewer.cs` to resolve H4 by placing the awaited folder-filter initialization inside one defined `async void` UI error boundary that logs and reports the original exception according to existing application policy. Redirect/remove every path that can discard the initialization task, and preserve successful callback behavior without a global mutable test hook.
- [x] [P3-T11] Update `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs` so P1-T11 passes. Assert a controlled controller fault is observed once by the selected log/user-error boundary with the original exception, and assert a successful initialization does not invoke that failure policy.

### Phase 4 — Dispatcher seam completion

- [x] [P4-T1] Modify `UtilitiesCS/Threading/WpfUiDispatcher.cs` only as necessary to make `InvokeAsync(Func<Task<TResult>>)` preserve successful result, cancellation, and fault semantics while executing and unwrapping on the captured WPF dispatcher; do not alter live folder traversal to use any fallback.
- [x] [P4-T2] Update `UtilitiesCS.Test/Threading/WpfUiDispatcherTests.cs` so P1-T7 passes on its dedicated STA host, asserting result thread affinity, original exception propagation, and cancellation semantics without timers or a live UI host.
- [x] [P4-T3] Update `UtilitiesCS.Test/UtilitiesCS.Test.csproj` only if required to compile the P4-T2 test and its existing WindowsBase dependencies; do not add packages or unrelated project changes.
- [x] [P4-T4] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` to resolve M2 by observing every notification-scheduled refresh task and routing failures through the existing service logging/error policy without hiding state-transition failures, duplicate refreshes, or disposal races. Keep notification refresh and cleanup on the captured STA dispatcher and do not add a traversal fallback.
- [x] [P4-T5] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceInvalidationTests.cs` so P1-T13 passes. Assert one controlled notification refresh fault is observed by the selected policy, is not unobserved/fire-and-forget, does not publish a snapshot or schedule an unintended retry, and leaves invalidation/disposal behavior intact.

### Phase 5 — Targeted regression and acceptance reconciliation

- **Revision-6 execution clarification:** Preserve `IUiDispatcher.BeginInvoke(Action): IAsyncResult` and all existing QuickFiler seams. P5-T18/P5-T21 use one controlled dispatcher whose `BeginInvoke(Action)` and `InvokeAsync(Action)` synchronously capture the callback and return the same controlled task, which also satisfies `IAsyncResult`; P5-T19/P5-T20 change production to observe the existing typed `IUiDispatcher.InvokeAsync(Action): Task` result. P5-T34/P5-T36 place all new terminal/event cases in the capacity-approved `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs`; `OutlookFolderTreeServiceInvalidationTests.cs` may receive only minimal shared-helper changes. For M3, a pre-dispatch scheduling failure is terminally observed and never falls back off-dispatcher; once queued cleanup begins, cancellation, unsubscribe, dispatcher, sink, and stage failures cannot prevent the remaining cleanup stages from being attempted once, while the exact original stage fault is observed once and a second `Dispose` neither retries nor duplicates cleanup.

- [x] [P5-T1] Run the changed regression classes in `TaskMaster.Test` and `UtilitiesCS.Test`, including the P1-T1 through P1-T7 cases plus current worker-cold-build, forced-yield, strict-`WpfDispatcherYield`, invalidation, cancellation, and state tests. The result must name the separate public optional-factory and legacy-constructor-`Readiness` fault regressions from P1-T5 and prove they run without a real viewer or message loop. Record each assembly, class, test count, and pass result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-targeted-regressions-pass.<timestamp>.md`.
- [x] [P5-T2] Compare P5-T1 results with AC1 through AC8 and CR-001 through CR-007. Include the exact test names, assemblies, test results, and evidence that independently proves construction, build, refresh, post-yield continuation, notification cleanup, disposal, cold-view close, public optional-factory fault propagation, legacy-constructor `Readiness` fault propagation, ribbon fault policy, and no-fallback behavior. For the no-fallback proof, inspect the complete changed-production-C# diff using `git diff origin/main -- '*.cs'` and record that no changed live traversal path introduces `Task.Yield`, a worker-local WPF dispatcher, or caller-selected traversal fallback, alongside the strict-`WpfDispatcherYield` regression result. Record the reconciliation in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-acceptance-criteria-mapping.<timestamp>.md`. This task fails closed and records remediation-required if any acceptance criterion or review finding lacks independent passing evidence or if a forbidden fallback is found.
- [x] [P5-T3] In `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/spec.md`, set AC1 and AC3 from `[x]` to `[ ]` so AC1 through AC8 are all unchecked while cycle-2 evidence is incomplete. Do not otherwise revise lifecycle, implementation, scope, or completion language in this task.
- [x] [P5-T4] Update only `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` with deterministic, non-reflection test methods that cover `AppOlObjects.FolderTreeService` after `Dispose` (the `ObjectDisposedException` getter guard), a controlled `LoadFolderTreeService` exception that is propagated and clears `_folderTreeServiceInitialization` so the next access retries and publishes one service, and a test-subclass pass-through to `base.CreateFolderTreeServiceDispatcher()` that asserts the base composition factory returns `WpfUiDispatcher`. Reuse existing mock, test-subclass, and dispatcher seams; do not alter production code, add test project entries, use a live UI host, or introduce global mutable state.
- [x] [P5-T5] Update only `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceDisposalTests.cs` and, if its existing notification fake is the narrowest available seam, `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceInvalidationTests.cs` with deterministic tests that retain a notification callback captured before disposal and invoke it after disposal to prove `HandleNotification` returns without a refresh, publication, or event; and call `Dispose` twice to prove the terminal-state guard performs notification cleanup and sink disposal exactly once. Reuse existing fake notification, controlled dispatcher, and reader seams, keep each changed test file below 500 lines, and do not use reflection, live Outlook/UI, timing, polling, or production changes.
- [x] [P5-T6] Create `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs`, add only its required `Compile` entry to `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, and move no unrelated tests. Add the deterministic close-during-refresh regression in the new named split test file: complete initial asynchronous construction, raise `SnapshotChanged` through the existing fake service while the next snapshot is controlled, close the fake viewer before that refresh completes, then complete it and assert the two `_disposed` guards suppress view mutation and property notification and leave no retained service handler. Reuse the existing fake viewer and delayed folder-tree service; do not add a real viewer, message loop, reflection, global hook, timing, or production-code change. Keep both `FilterOlFoldersControllerInitializationTests.cs` and the new split test file at or below 500 lines.
- **Revision-6 sequencing and scope:** P5-T1 through P5-T13 and P5-T16 are retained as checked history. P5-T14, P5-T15, and P5-T17 are reopened because their prior evidence is historical only; no controller acceptance evidence may rely on it. The remaining work is cycle 3, pass 3; no cycle 4 may be started. `IUiDispatcher.BeginInvoke(Action): IAsyncResult` remains unchanged for public-interface compatibility. Worker-first composition must instead queue and synchronously observe the existing typed `IUiDispatcher.InvokeAsync(Action): Task` terminal result before the public getter waits. P5-T14 must complete before the corrected P5-T15 rerun and every remaining controller test. The original `FilterOlFoldersControllerRefreshDisposalTests.cs` retains all checked tests and shared private helpers, receives only P5-T15's compact ownership instrumentation and replacement H2 cases, and must finish at or below 500 lines; the lifecycle-races partial contains only P5-T16/P5-T17 lifecycle-race cases and their race-specific helpers and must finish at or below 300 lines. The capacity-approved home for every added service event-lifecycle case is `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs`; do not add those cases to the 435-line `OutlookFolderTreeServiceInvalidationTests.cs`.

- [x] [P5-T7] Before any controller lifecycle or reentrancy production edit, extract only lifecycle ownership, terminal-state, candidate-view commit, and `SnapshotChanged` subscription members from `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs` into `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.Lifecycle.cs`, and add only its required `Compile` entry to `UtilitiesCS/UtilitiesCS.csproj`. Preserve the public API and single initialization path; record both partial-file line counts in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-controller-lifecycle-extraction.<timestamp>.md` and keep every changed C# file at or below 500 lines.
- [x] [P5-T8] Update only `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs` to repair the P5-T6 test harness: make the controlled dispatcher instance-local and lazily created through the virtual factory, give its actual `IUiDispatcher.InvokeAsync<TResult>(Func<Task<TResult>>)` controller path a deterministic default completion source, reuse `DelayedFolderTreeService`, expose the captured generic async dispatch work through the controller instance, and explicitly await each captured task. Preserve the two checked P5-T6 disposal-guard cases, use no static mutable state, and record the changed test-file line count in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-p5t6-instance-dispatcher.<timestamp>.md`.
- [x] [P5-T9] Rerun the P5-T6 close/reentrancy cases from `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs` on a dedicated pumping STA using the repaired P5-T8 harness and task-completion signals only. Assert that every queued controller operation is awaited and the captured STA remains unblocked; record dispatcher/task state and the no-timeout/no-polling proof in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-p5t6-sta-hang-green.<timestamp>.md`.
- [x] [P5-T10] Modify `UtilitiesCS/Threading/WpfUiDispatcher.cs` so its public constructor preserves existing `UiThread`-backed behavior through a dispatcher-provider seam and a narrow internal constructor injects a concrete `Dispatcher` only for dedicated-STA tests. Make every member resolve through the provider or injected instance, preserve public API behavior, and do not mutate `UiThread._dispatcher`.
- [x] [P5-T11] Update the already-created `UtilitiesCS.Test/Threading/WpfUiDispatcherTests.cs` from checked P1-T7/P4-T2 to verify on a dedicated STA that the injected-dispatcher constructor preserves `Invoke`, `BeginInvoke`, and both `InvokeAsync` overload completion, fault, and cancellation behavior without reflection or global dispatcher mutation. Record fully qualified tests and changed-file line counts in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/wpf-ui-dispatcher-captured-instance-green.<timestamp>.md`; do not add a second test file or project entry.
- [x] [P5-T12] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs`, add deterministic H2 cases in which a factory-created shown viewer is disposed exactly once when null-globals validation or any synchronous pre-`Readiness` composition step fails, while the exact original exception object is rethrown and no service handler remains. Record the failed assertions in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-viewer-ownership-fail-before.<timestamp>.md`.
- [x] [P5-T13] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs`, use `DelayedFolderTreeService` to complete a delayed initial snapshot, then reentrantly close the candidate viewer from `ArchiveRoot` before compatibility-view wiring. Require no committed view, property notification, or `SnapshotChanged` handler; record the failed assertion in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-initial-archiveroot-close-fail-before.<timestamp>.md`.
- [x] [P5-T14] Recreate the partial-class capacity evidence using these exact PowerShell commands and paths: `(Get-Content 'UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs').Count`; `(Get-Content 'UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs').Count`; `@(Select-String -Path 'UtilitiesCS.Test/UtilitiesCS.Test.csproj' -SimpleMatch '<Compile Include="EmailIntelligence\FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs" />').Count`; and `git diff --check -- 'UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs' 'UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs' 'UtilitiesCS.Test/UtilitiesCS.Test.csproj'`. Record the literal commands, original/new line counts, exactly-one adjacent `Compile` entry, no second `[TestClass]`, and diff-check result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-controller-lifecycle-races-capacity.<timestamp>.md`; fail closed unless the original is <=500 lines, the lifecycle partial is <=300 lines, and each command exits successfully.
- [x] [P5-T15] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs`, rerun the corrected P5-T12 H2 contract before P5-T16 or any further controller regression. Authorize only a narrow recording fake viewer and independent `AssertionScope` observations that prove the public factory path creates then `Show`s the viewer and independently records every create/show/close/dispose mismatch; each null-globals path must assert the exact `ArgumentNullException.ParamName` with no wrapper. For the synchronous add-fault case, an instrumented `FolderTreeService` getter must record acquisition, and the viewer `_viewer.FormClosed += Viewer_FormClosed` add callback must assert that acquisition already occurred before throwing the exact known exception; the test must then expose that exact original exception while the trackable service retains zero `SnapshotChanged` handlers. The replacement red evidence must explicitly report `CloseCount=0` and `DisposeCount=0`, even when an earlier assertion fails, in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-viewer-ownership-contract-rerun-fail-before.<timestamp>.md`.
- [x] [P5-T16] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs`, use `DelayedFolderTreeService` to start refresh, complete its snapshot, then reentrantly close from `ArchiveRoot` before compatibility-view wiring. Require no refresh view mutation, property notification, or retained handler; record the failed assertion in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-refresh-archiveroot-close-fail-before.<timestamp>.md`.
- [x] [P5-T17] [expect-fail] In `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs`, replace the prior reentrant-close barriers with two deterministic task-signal interleavings and use `AssertionScope` so every view, notification, and handler assertion is evaluated and recorded while red. Candidate-view case: use an incomplete snapshot and a blocking recording-viewer `SetController` signal/release; let controller construction return, complete the snapshot so readiness reaches `SetController` on another continuation, call `Dispose` while `SetController` is paused after candidate assignment but before remaining wiring/subscription, and release in `finally`; require no view, no post-dispose refresh/application notification, and zero stored handler. Subscription case: make the fake `SnapshotChanged` add accessor actually store the delegate, call `Dispose` after storage but before add returns/subscription-state commit, then expose `RaiseSnapshotChanged` to exercise any retained handler; require no view, no post-dispose refresh/application notification, and zero stored handler. Do not use sleeps, polling, timeouts, live UI, or temporary files. Record both red interleavings in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-view-commit-and-subscription-race-fail-before.<timestamp>.md`.
- [x] [P5-T18] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.cs`, use one controlled `IUiDispatcher` whose `BeginInvoke(Action)` and `InvokeAsync(Action)` synchronously capture the callback, increment independent call counters with `Interlocked`/`Volatile`, and create one controlled dispatch-operation object per invocation so stale callbacks and retries are independently controllable; each operation returns its controlled task, which also satisfies `IAsyncResult`. Every coordination `TaskCompletionSource` must use `TaskCreationOptions.RunContinuationsAsynchronously`; provide already-terminal cancellation-token and fault-exception modes and a late-callback release. Start the getter on `Task.Run`, evaluate `winner = await Task.WhenAny(callbackCaptured, worker)` without a clock timeout, require `callbackCaptured` to win, and snapshot worker/callback/composition/load counts before cleanup. For canceled and faulted returned-task modes, record current `InvokeAsyncCallCount=0`, the exact caught cancellation token or exact fault object identity, and an incomplete worker without awaiting that incomplete worker. In `finally`, release the captured legacy callback only for cleanup, explicitly observe every test-owned canceled or faulted dispatcher task to prevent an unobserved test exception, then observe and swallow the worker result so no worker remains blocked; cleanup release is not evidence. In a separate disposal case assert the pending worker is incomplete before cleanup. In a separate late-callback case release after `Dispose`, require expected `LoadCount==0`, and record the current red actual `LoadCount==1`. Record the red outcomes and all pre-cleanup counts in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-dispatcher-task-shutdown-fail-before.<timestamp>.md`; do not use reflection, static `UiThread` mutation, live UI, sleeps, polling, timeouts, or leftover workers.
- [x] [P5-T19] Modify `TaskMaster/AppGlobals/AppOlObjects.FolderTreeService.cs` so worker-first composition calls `IUiDispatcher.InvokeAsync(Action): Task` outside `_folderTreeServiceGate` and supports both already-terminal and pending-to-terminal cancellation/fault results. A null task is an explicit invariant `InvalidOperationException` with a clear message and the same exact reset/retry behavior. Before the public getter waits, register a nonthrowing terminal observer with `TaskContinuationOptions.ExecuteSynchronously` and `TaskScheduler.Default`; classify the returned task by `IsCanceled` versus `IsFaulted` (a faulted task whose exact exception is `OperationCanceledException` remains faulted), preserve the exact cancellation token with `TrySetCanceled(token)` or exact fault object with `TrySetException(exception)`, atomically detach matching owned initialization/dispatcher/composition state under the gate, and terminally complete the owned initialization with that exact result. Add and use the narrow protected/internal instance terminal-notification hook authorized by P2-T8: it receives the terminal initialization `Task`, cannot alter production flow, and a hook failure cannot replace the terminal result. Make any callback retained by the controlled dual-method dispatcher a no-op. Final publication must recheck both `ReferenceEquals(initialization, _folderTreeServiceInitialization)` and `_disposed`; a stale callback that passed its first check before terminal detachment must discard its service and cannot clear or overwrite newer retry ownership. Preserve the public `BeginInvoke` signature, synchronous public API, one session-scoped service, captured-STA-only composition, original exception identity, and no fallback. Record the interface-compatibility assessment confirming no `IUiDispatcher`, `WpfUiDispatcher`, fake, or QuickFiler seam signature change in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-invokeasync-contract-compatibility.<timestamp>.md`.
- [x] [P5-T20] Modify `TaskMaster/AppGlobals/AppOlObjects.FolderTreeService.cs` so `Dispose` atomically detaches the matching owned initialization, dispatcher, and composition flags under `_folderTreeServiceGate`, then completes the detached initialization with `ObjectDisposedException(nameof(AppOlObjects))`. A retained callback that has not passed the ownership/composition-start gate when detachment occurs must not enter composition or `LoadFolderTreeService`. Composition that already started before disposal may finish constructing its service, but must discard/dispose that service on the captured dispatcher and cannot publish, complete with success, or clear/overwrite newer retry ownership. Final publication must recheck both `ReferenceEquals(initialization, _folderTreeServiceInitialization)` and `_disposed`. Preserve atomic publication/completion behavior and idempotent disposal.
- [x] [P5-T21] Update `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.cs` so the P5-T18 cancellation, pending-completion, and late-callback cases pass without `IsCompleted` or timing as a green oracle. Cover separate already-terminal and pending-to-canceled/pending-to-faulted `InvokeAsync` cases, including a faulted `OperationCanceledException` identity case that remains faulted rather than canceled, and a null-task invariant/reset/retry case. For each pending case, await the instance terminal-notification-hook signal, assert the exact token or exception from the terminal initialization task, release the retained callback, assert zero composition/load/publication, then safely await the worker; contain hook faults. Assert `InvokeAsync` is used and `BeginInvoke` is unused; callback not executed; retry publishes exactly one live service; `Dispose` yields the exact `ObjectDisposedException(nameof(AppOlObjects))` terminal result; late release executes zero composition/load/publication; and second `Dispose` is idempotent. Record fully qualified tests and pass counts in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-dispatcher-task-shutdown-green.<timestamp>.md`.
- [x] [P5-T22] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.cs`, use override call signals and exact known exceptions from `CreateFolderTreeServiceDispatcher()` and `IsFolderTreeServiceDispatcherThread()` only after initialization ownership is stored; after its first throw, the thread-check override must return `false`. Require dispatcher-factory and thread-check failures to be handled transactionally while ownership state is protected, with no observable window containing stale initialization and a null or invalid dispatcher. Record a nonblocking worker-exit red snapshot rather than awaiting a potentially blocked worker, then unconditionally call the already-fixed `Dispose` in `finally` and observe the worker result. Add task-signal-only M1 controls using the existing `OnFolderTreeServiceBeforeInitializationCompletion` seam: disposal before the linearization point, two coalesced callers that receive the same service, and disposal after the getter returns. Require the first access to expose the exact exception, reset pending initialization, and permit a fresh ownership retry that publishes exactly one live service; require no published-but-incomplete externally visible state. Record all failed red outcomes in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-setup-retry-linearization-fail-before.<timestamp>.md`. Do not use sleeps, polling, timeouts, live UI, reflection, or global mutation.
- [x] [P5-T23] Modify `TaskMaster/AppGlobals/AppOlObjects.FolderTreeService.cs` to catch dispatcher-factory and dispatcher-thread-check failures transactionally while the owned initialization state is protected: complete ownership with the exact exception, clear it for retry, and ensure no window exposes stale initialization with a null or invalid dispatcher. Retain the existing atomic publication/completion linearization point. Do not add a test-only publication pause or a process-global dispatcher replacement.
- [x] [P5-T24] Update existing `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` `BlockingUiDispatcher` and `QueuedStaDispatcher` tests/fakes before the combined AppOlObjects suite so their `InvokeAsync` implementations drive the signals/actions previously tied to `BeginInvoke`, preserving existing behavior assertions without hangs. Then update `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.cs` so P5-T22 passes with a direct sequential exact-exception assertion followed by fresh ownership that retries and publishes one live service; then pass the task-signal-only M1 controls for pre-linearization disposal, two coalesced callers receiving the same service, and after-getter disposal without a published-but-incomplete state. The lifecycle file must share one configurable `AppOlObjects` subclass and one `ControlledUiDispatcher`; do not duplicate STA, inline, or blocking fake infrastructure. Remove `GetFolderTreeServiceGate`, `UiDispatcherScope`, and all `UiThread._dispatcher` reflection from `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs`, replacing affected tests with the lifecycle instance fake/override seams. Record exact exception identity, one-service retry result, terminal waiter result, absence of published-but-incomplete state, removed global-state mutation, and explicit <=500-line counts for both `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` and `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.cs` in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/appolobjects-setup-retry-linearization-green.<timestamp>.md`. Do not use sleeps, polling, timeouts, live UI, reflection, or global mutation.
- [x] [P5-T25] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.Lifecycle.cs` to own each candidate viewer before validation/composition, dispose it exactly once on synchronous failure while rethrowing the original exception, and recheck terminal state after every `ArchiveRoot` and compatibility-view boundary before candidate-view commit or `SnapshotChanged` subscription.
- [x] [P5-T26] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.Lifecycle.cs` to linearize candidate-view commit and `SnapshotChanged` subscription against `Dispose`, marshal the complete initial and refresh path including `ArchiveRoot` to the captured dispatcher, and await its dispatcher task at the existing observed-error boundary. Do not use fire-and-forget dispatch, a fallback dispatcher, a global test hook, or a lock across awaited snapshot/COM work.
- [x] [P5-T27] Update `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.cs` and `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs` so P5-T9 and P5-T15 through P5-T17 pass. Assert separate STA hang resolution; independent create/show/close/dispose count observations; exact-once viewer disposal with original exception identity; exact null-globals parameter names without wrapping; the instrumented service-getter-before-`_viewer.FormClosed += Viewer_FormClosed` add-fault ordering with zero trackable-service `SnapshotChanged` handlers; initial and refresh delayed-snapshot `ArchiveRoot` close before compatibility-view wiring; and both P5-T17 task-signal interleavings. For the candidate-view signal, dispose after candidate assignment while `SetController` is blocked, release in `finally`, and prove no view, no post-dispose refresh/application notification, and zero stored handler. For the subscription signal, store the delegate before the add accessor returns, dispose before subscription-state commit, invoke `RaiseSnapshotChanged`, and prove the same three outcomes. Record the green results in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-controller-lifecycle-green.<timestamp>.md`.
- [x] [P5-T28] [expect-fail] In `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs`, enter the actual `async void` callback with an incomplete initialization `TaskCompletionSource`, assert zero reports while it is incomplete, then fault it and require exactly one report of the exact original exception. Separately require delayed success to report zero failures, the legacy wrapper to retain exception identity, and a throwing reporter to be contained without replacing the initialization fault. Record red results in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/ribbon-h4-exact-once-reporter-fail-before.<timestamp>.md`.
- [x] [P5-T29] Modify `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs` and `TaskMaster/Ribbon/RibbonViewer.cs` so the actual `async void` boundary awaits initialization, reports the original fault exactly once after task completion, preserves delayed success and legacy-wrapper exception identity, and contains a secondary reporter failure according to existing error policy.
- [x] [P5-T30] Update `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs` so P5-T28 passes. Record suspended-zero, faulted-exactly-once, delayed-success-zero, exact legacy exception identity, and throwing-reporter containment in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/ribbon-h4-exact-once-reporter-green.<timestamp>.md`.
- [x] [P5-T31] [expect-fail] In `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs`, use actual internal hierarchy-reader fake adapters, a reentrant fake `EntryID` getter that calls `Dispose`, inline synchronous `Invoke`, a drainable queued same-dispatcher `IUiDispatcher.InvokeAsync(Action)`, and a recording sink. Call `Dispose` twice before draining cleanup, fault a controlled cleanup stage, and require red proofs that no post-cleanup COM access occurs, each cleanup stage still follows through exactly once, and the exact original cleanup fault reaches the existing policy/observer once rather than an aggregate or replacement fault. Record the fail-before result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/reentrant-dispose-cleanup-stage-fault-fail-before.<timestamp>.md`.
- [x] [P5-T32] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` so disposal linearizes terminal state before queued cleanup; when a build/refresh/in-flight traversal exists, queue cleanup through the captured dispatcher with `IUiDispatcher.InvokeAsync(Action)`, and otherwise preserve safe synchronous cleanup. Attempt every cleanup stage exactly once despite cancellation, unsubscribe, dispatcher, sink, or stage faults; route the exact original stage fault once through the existing policy/instance observer; and prevent later reader COM access, publication, scheduling, event delivery, or notification reattachment. Do not claim interruption of an already executing COM call.
- [x] [P5-T33] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs` so P5-T31 passes. Assert disposal twice before drain yields one queued cleanup, each cleanup stage is attempted once after an earlier stage fault, the original task receives cancellation or `ObjectDisposedException`, post-cleanup COM access is zero, no post-dispose publication, scheduling, event delivery, or notification reattachment occurs, and the exact original cleanup fault reaches the policy/observer once. Record green results in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/reentrant-dispose-cleanup-stage-fault-green.<timestamp>.md`.
- [x] [P5-T34] [expect-fail] In the capacity-approved `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs`, add M2 event-lifecycle cases that retain a notification callback before disposal, use an actual in-flight refresh that is disposed and then faults to exercise `ScheduledRefreshFaulted` (do not invoke a copied delegate directly), and use a `SnapshotChanged` subscriber that disposes before a later counter subscriber. Require red results for no callback work/fault delivery after disposal, outside-gate callback invocation, and suppression of the later subscriber; record them in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/outlook-folder-tree-event-lifecycle-fail-before.<timestamp>.md`.
- [x] [P5-T35] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` so disposal detaches and clears `ScheduledRefreshFaulted`, retained notification and in-flight scheduled-fault paths observe terminal state before work/delivery, and `SnapshotChanged` invokes a copied subscriber list outside `_gate` with a terminal-state check before every callback. Preserve P5-T32 cleanup linearization and idempotence.
- [x] [P5-T36] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.cs` so P5-T34 passes and rerun P5-T31/P5-T33. Assert notification and in-flight-refresh-fault suppression after disposal, outside-gate reentrant `SnapshotChanged` delivery with later-subscriber suppression, zero post-cleanup COM access, no post-dispose publication, scheduling, event delivery, or notification reattachment, and unchanged exact-once cleanup stage/fault behavior. Record green evidence in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/outlook-folder-tree-event-lifecycle-green.<timestamp>.md`.
- [x] [P5-T37] Replace the nullable-context-invalid `Action? archiveRootRead = null` declaration in `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` with a declaration valid in that file's nullable context while preserving optional callback behavior. Record the exact CS8632 diagnostic before/after and changed-file line count in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-initialization-cs8632-correction.<timestamp>.md`; do not suppress the warning or alter nullable context elsewhere.
- [x] [P5-T38] Run one fresh serialized targeted MSTest command after verifying no active `vstest` process remains. Cover P5-T7 through P5-T37, P1-T1 through P1-T14, P5-T6 close-during-refresh, P5-T9 dedicated-STA rerun, P5-T11 captured-dispatcher tests, the original and lifecycle-races controller partials, AppOlObjects shutdown/setup/M1, controller ownership/reentrancy/barriers, H4, M2, and M3. Record the command, process check, assemblies, fully qualified tests, total/passed counts, no-live-Outlook/UI proof, and all changed C# line counts in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle3-pass3-targeted-regressions.<timestamp>.md`; fail closed on any failure or changed C# file above 500 lines.
- [x] [P5-T39] After P5-T38 passes, write `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle3-pass3-acceptance-criteria-mapping.<timestamp>.md` as the controlling final mapping. Map current red/green evidence for all root audit regressions: H1 per-invocation controlled dispatch operations; observed test-owned terminal tasks; already-terminal and pending-to-terminal cancellation/fault transitions; faulted-`OperationCanceledException` identity distinct from cancellation; null-task invariant/reset/retry; terminal-hook signal and hook-fault containment; exact terminal cancellation token/exception identity; atomic transactional ownership detach for dispatcher-factory/thread-check failures; retry; and stale/late-callback discard without composition, load, publication, or newer-ownership overwrite. H2 requires corrected factory/viewer ownership including independent create/show/close/dispose count assertions, null-globals parameter identity, and the instrumented service-getter-before-FormClosed-add fault with zero trackable-service `SnapshotChanged` handlers. H3 requires initial/refresh delayed-snapshot `ArchiveRoot` closure plus candidate-view and subscription task-signal barriers. Also map H4 incomplete-TCS exact reporting and throwing reporter; M1 setup reset/retry and disposal linearization; M2 retained callback/in-flight refresh fault lifecycle; and M3 cleanup linearization/exact-once staged-fault follow-through. For M3, require explicit passing proof of zero post-cleanup COM access and no post-dispose publication, scheduling, event delivery, or notification reattachment. Include P5-T14 literal-command capacity proof, updated `BlockingUiDispatcher`/`QueuedStaDispatcher` `InvokeAsync` proof, P5-T6 instance-local/STA evidence, removal of global dispatcher mutation from both AppOlObjects test files, both controller partials and their sole new compile entry, all added test/project files, every changed C# line count, AC1–AC8, and CR-001–CR-007. Inspect `git diff origin/main -- '*.cs'` and fail closed if a changed live traversal path adds `Task.Yield`, a worker-local WPF dispatcher, or caller-selected fallback. Mark this as cycle 3/pass 3/no-cycle-4 and keep P6/P7 blocked and all criteria non-PASS unless its current evidence is PASS.

#### Cycle 4 — Focused coverage remediation and predecessor reconciliation

- **Revision-7 execution gate:** Cycle-3 P6-T5 remains failed historical evidence: repository lines were 84.5796%, denominator reconciliation passed, but changed production was 759/876 (86.64%), `AppOlObjects.FolderTreeService.cs` was 268/301 (89.04%), and `FilterOlFoldersController.Lifecycle.cs` was 255/325 (78.46%). Treat the current coverage audit as an additional red input: `AppOlObjects.FolderTreeService` getter coverage has regressed from its baseline, `CreateFolderTreeServiceDispatcher` is uncovered, and `CloseViewerAfterInitializationFailure` retains an unhit `InvokeRequired` branch. This cycle may add the three declared coverage partials and compile entries, plus only the P5-T42 evidence-gated production seam. It must not reduce a coverage assertion, use a waiver, classify reconciled executable source as scope drift, or change production solely to improve coverage.
- [x] [P5-T40] [expect-fail] Reproduce and record the cycle-4 coverage-gap baseline from `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle3-coverage-and-quality-delta.2026-08-05T05-48.md` and the current Cobertura report in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-coverage-gap-fail-before.<timestamp>.md`. Identify the exact unhit changed-production lines and methods, including the `AppOlObjects.FolderTreeService` disposed-access and composition-failure paths, `CreateFolderTreeServiceDispatcher`, `CloseViewerAfterInitializationFailure` both `InvokeRequired` outcomes, controller viewer/request/refresh lifecycle branches, Outlook cleanup-observer containment, and `WpfUiDispatcher.InvokeAsync(Action)`. Record `759/876`, `268/301`, and `255/325` as failed historical measures only; the task is complete only when the report proves the pre-remediation >=90% gate fails without invoking live Outlook or a real viewer.
- [x] [P5-T41] Reconcile the currently unchecked P5-T25, P5-T26, and P5-T27 before adding coverage tests. Write `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-predecessor-reconciliation.<timestamp>.md` mapping each predecessor task's literal acceptance condition to current green evidence, exact test names, assemblies, and results. Check off a predecessor task only if that mapping independently proves it; otherwise leave it unchecked and re-execute the missing deterministic regression in its authorized existing test home before P5-T43. This task must not batch-check predecessor history or allow P6/P7 to proceed with contradictory checklist state.
- [x] [P5-T42] After P5-T40 and P5-T41, resolve only evidence-proven testability gaps before coverage-test implementation. First record deterministic red evidence that the private branch cannot be reached under the preserved no-real-viewer/no-reflection constraints. Then make only the smallest required production adjustment: remove an `AppOlObjects` branch only when a same-gate invariant proves the state impossible; add the narrowest instance-local controller viewer or compatibility-view factory seam and visibility change needed for the deterministic test; or make `OutlookFolderTreeService.TryAuthorizeBuild` internal only when direct disposed-path testing cannot otherwise reach it. Preserve public behavior and signatures, except for an optional backwards-compatible constructor or factory parameter justified by the red test. Cover every production addition, preserve all existing public callers, and prohibit global hooks, dead-code deletion, exclusions, and any behavior change unsupported by the test. Record the red proof, invariant or seam rationale, changed-line count, and green result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-testability-seam.<timestamp>.md`.
- [x] [P5-T43] Create `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.Coverage.cs` as a partial of the existing lifecycle test class and add exactly one adjacent `Compile` entry in `TaskMaster.Test/TaskMaster.Test.csproj`. Add deterministic, mock-only tests for the existing `AppOlObjects.FolderTreeService` behavior: disposed getter access; synchronous `InvokeAsync(Action)` dispatch failure; null, factory, dispatcher-predicate, and `LoadFolderTreeService` failure with initialization reset/retry; candidate service and notification-sink disposal failures; and the base dispatcher-factory and hook paths with mocked empty stores. Assert exact fault/cancellation identity, one-session publication, retry behavior, and no worker fallback. Record red/green test names, line counts, the sole compile entry, and covered/uncovered method lines in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-appolobjects-coverage.<timestamp>.md`; the new partial must remain <=500 lines.
- [x] [P5-T44] Create `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.Coverage.cs` as a partial of the existing refresh-disposal test class and add exactly one adjacent `Compile` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`. Use the P5-T42 seam only when its evidence authorizes it; otherwise use only existing or local deterministic fake viewers, dispatchers, globals, and folder-tree services to cover successful/faulted factory viewer creation and disposal, both synchronous and `InvokeRequired`-marshaled `CloseViewerAfterInitializationFailure` paths (including invoke fault containment), queued dispose-before-initialization and dispose-before-refresh, `ArchiveRoot` callbacks, tree-view getter disposal, request/view creation and commit branches, subscription attachment, and refresh-observer behavior. Assert original fault identity, exact viewer ownership counts, no retained handler, and no post-dispose view mutation. Do not use a real viewer, a message loop, reflection, or global mutable state. Record red/green test names, line counts, the sole compile entry, seam justification when applicable, and covered/uncovered method lines in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-filter-controller-coverage.<timestamp>.md`; the new partial must remain <=500 lines.
- [x] [P5-T45] Create `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.Coverage.cs` as a partial of the existing traversal-cancellation test class, adding exactly one adjacent `Compile` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`; change the existing class declaration only as needed to support that partial. Add deterministic tests for changed `OutlookFolderTreeService` authorization and cleanup-observer failure containment, preserving notification cleanup and terminal isolation. In the existing `UtilitiesCS.Test/Threading/WpfUiDispatcherTests.cs`, add the dedicated-STA `InvokeAsync(Action)` result, fault, and cancellation case. Record green results and every changed test-file line count in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-outlook-wpf-coverage.<timestamp>.md`; all changed C# files must remain <=500 lines.
- [x] [P5-T46] Run the focused cycle-4 coverage command `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-focused-coverage.cobertura.xml`, then calculate changed-production numerator/denominator and method/class/module coverage against `git diff origin/main -- '*.cs'`, including untracked authorized production files. Record the command, eight test assemblies, filters, source-root count, package inventory, `lines-valid`, `branches-valid`, denominator reconciliation, method inventory, and all targeted test results in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-focused-coverage-green.<timestamp>.md`. Before entering P6, require >=95% coverage for the changed production total and every changed `AppOlObjects`, controller, Outlook-service, and WPF-dispatcher method/class/module that P6-T5 will evaluate; this is a margin rather than a substitute for every required method. The final P6-T5 policy gates remain >=80% repository lines, >=90% changed production lines, >=90% each new or changed method/class/module, and no changed-line regression.
- [x] [P5-T47] After P5-T41 through P5-T46 pass, write `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/remediation-cycle4-acceptance-criteria-mapping.<timestamp>.md` as the cycle-4 controlling mapping. Reconcile AC1–AC8 and CR-001–CR-007 to independent current evidence; include the P5-T25–P5-T27 reconciliation, exact test names, all authorized partial/project files, capacity results, source-diff no-fallback inspection, testability-seam evidence when applicable, and coverage-gap closure. Mark an item PASS only when its evidence is current and independent; otherwise leave it unresolved and do not enter P6.

### Phase 6 — Final C# QA and coverage gate

- **Revision-7 controlling gate:** The checked cycle-3 P6-T1 through P6-T4 artifacts remain historical evidence only. Because cycle 4 adds coverage tests, restart the entire Phase 6 loop after P5-T47 records every required AC/CR PASS, the P5-T25–P5-T27 reconciliation is internally consistent, and focused coverage meets its >=95% margin. Do not enter Phase 7 until the re-run P6-T1 through P6-T6 pass.

- [x] [P6-T1] Only after P5-T47 passes, run `dotnet tool run csharpier .` and record the clean cycle-4 final-pass result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-csharpier.<timestamp>.md`; if it changes files or fails, correct the result and restart Phase 6 from P6-T1.
- [x] [P6-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and record the clean cycle-4 analyzer result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-analyzers.<timestamp>.md`; if it fails, correct the findings and restart Phase 6 from P6-T1.
- [x] [P6-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true` and record the clean cycle-4 nullable result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-nullable.<timestamp>.md`; if it fails, correct the diagnostics and restart Phase 6 from P6-T1.
- [x] [P6-T4] Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-coverage-final.cobertura.xml`; record every discovered test assembly, total/passed test counts, numeric repository line/branch coverage, filters, Cobertura source-root count, package inventory, `lines-valid`, and `branches-valid` in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-mstest-coverage.<timestamp>.md`; if it fails, correct the issue and restart Phase 6 from P6-T1.
- [x] [P6-T5] Compare P0-T6/P0-T7 and P5-T40 with the new P6-T4 report only after proving identical coverage command, `SearchRoot`, configuration, eight-test-assembly inventory, filters, Cobertura source-root count, and package inventory. Reconcile every `lines-valid` and `branches-valid` denominator delta to the reviewed `git diff origin/main -- '*.cs'` executable source diff, distinguishing reconciled executable-source additions from coverage-scope drift; a reconciled executable-source change is not scope drift. Produce `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-coverage-and-quality-delta.<timestamp>.md` with baseline/final repository coverage, changed-production-line numerator/denominator, changed-line coverage, and per-new-or-changed-method coverage for every modified or added production method. It must separately report `AppOlObjects.FolderTreeService` including its disposed and composition-failure branches and `CreateFolderTreeServiceDispatcher`; changed `FilterOlFoldersController` methods including `CloseViewerAfterInitializationFailure` both `InvokeRequired` branches; changed `OutlookFolderTreeService` authorization/cleanup-observer methods; and `WpfUiDispatcher.InvokeAsync(Action)`. Require repository line coverage >=80%, changed production lines >=90%, every new or changed method/class/module >=90%, and no changed-line regression. If any denominator cannot be reconciled or any threshold fails, record remediation-required without an exception, waiver, or policy relaxation, leave P6-T5 unchecked, and do not run P6-T6 or Phase 7.
- [x] [P6-T6] Only after P6-T5 passes, run `git diff --check origin/main` and record its exit status and output in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/remediation-cycle4-diff-check.<timestamp>.md`; if it fails, correct whitespace errors and restart Phase 6 from P6-T1.

### Phase 7 — Documentation and review re-entry

- [x] [P7-T1] Only after P5-T47, P6-T5, and P6-T6 pass, update `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/spec.md` to reconcile the cycle-4 implementation, evidence links, coverage values, feature status, and AC1 through AC8. Document the non-blocking worker-first `InvokeAsync(Action)` composition protocol without changing `BeginInvoke` compatibility; factory-created viewer cleanup with exact synchronous exceptions and counts; delayed-snapshot ArchiveRoot/view-commit/subscription linearization; ribbon H4 error handling; M2 in-flight refresh-fault lifecycle; M3 queued cleanup and terminal isolation; and the coverage closure without a waiver. Mark an AC `[x]` only when P5-T47 and final QA record it PASS; otherwise leave it `[ ]`. Remove unsupported completion, waiver, and no-scope-deviation language.
- [x] [P7-T2] Create `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/remediation-final-inventory.<timestamp>.md` listing changed production/test/project/document files, changed C# line counts, regenerated evidence, and AC1–AC8/CR-001–CR-007 status. Inventory every authorized added test/project file and its sole required compile entry, including `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceLifecycleTests.Coverage.cs` with `TaskMaster.Test/TaskMaster.Test.csproj`; `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerRefreshDisposalTests.Coverage.cs` with `UtilitiesCS.Test/UtilitiesCS.Test.csproj`; and `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceTraversalCancellationTests.Coverage.cs` with `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, together with the previously authorized partials and project entries. Prove no other source, test, or project file was introduced outside the declared scope, every changed C# file is <=500 lines, only the `FilterOlFoldersControllerRefreshDisposalTests.LifecycleRaces.cs` test partial is <=300 lines, and no real-viewer test, message loop, reflection/global dispatcher mutation, or global mutable hook was introduced.
- [x] [P7-T3] Only after P5-T47, P6-T5, and P6-T6 pass, validate this exact remediation plan and completed feature evidence with `mcp__drm-copilot__validate_orchestration_artifacts`. Record the validator result and evidence inventory in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/remediation-cycle4-precommit-validation.<timestamp>.md`. Do not begin the review, commit, or PR gate if validation fails.
- [x] [P7-T4] Only after P7-T3 passes, run `git add -A`, verify the complete staged inventory contains only approved issue-420 changes, collect current commit context with `mcp__drm-copilot__collect_commit_context`, delegate conventional-message generation to `commit-steward`, and commit the staged files. Record the commit SHA, context artifact, staged inventory, and clean post-commit status in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/remediation-cycle4-commit.<timestamp>.md`. If `git add -A` stages an unrelated change, unstage that exact file and do not commit until the staged inventory is approved.
- [x] [P7-T5] Only after P7-T4 passes, resolve the merge-base PR base, collect fresh PR context with `mcp__drm-copilot__collect_pr_context`, and delegate the committed issue-420 diff, updated specification, inventory, coverage evidence, and predecessor reconciliation to the full `feature-review` workflow. Record the review artifact paths and `REVIEW_STATUS` in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/remediation-cycle4-feature-review.<timestamp>.md`. If the review is remediation-required, stop and re-enter only the single authorized cycle-4 remediation path; do not proceed to PR or CI.
- [x] [P7-T6] Only after P7-T5 records `REVIEW_STATUS: PASS`, push the committed branch head, confirm the remote branch head SHA equals the local committed SHA, collect refreshed PR context for that pushed head, delegate the PR body to `pr-author`, then create or update the issue-420 pull request and monitor required GitHub checks for that exact PR head SHA. Record the remote head SHA, PR URL, required-check names/statuses, and the final clean worktree in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/remediation-cycle4-pr-ci.<timestamp>.md`. If a required check is absent, pending, failing, or targets a different head SHA, do not claim PR completion.
- [x] [P7-T7] Only after P7-T6 passes, rerun `mcp__drm-copilot__validate_orchestration_artifacts` for this exact plan and for `artifacts/orchestration/orchestrator-state.json` with `require_complete = true`, `require_codex_topology = true`, and `require_codex_model_routing = true`. Record both validation results, the final review artifact path, PR head SHA, green required-check evidence, and clean worktree in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/remediation-cycle4-completion-validation.<timestamp>.md`. Do not report completion if either validator, renewed review, or current-head CI gate is not passing.
