# Policy Audit — Issue #269 (quickfiler-darkmode-unread-labels-stay-blue)

- Timestamp: 2026-07-08T11-30
- Work mode: `minor-audit`
- AC source: `issue.md`, `## Acceptance Criteria` (AC1-AC5)
- Reviewer: feature-review agent (independent of the atomic-executor/orchestrator that produced the evidence)

## Executive Summary

The change adds a second, narrow `catch (System.NullReferenceException)` clause to the existing `catch (COMException)` guard in `Theme.SetQfcTheme()` and null-guards the read-state probe at its construction site in `QfcThemeHelper.cs`. Both production edits are minimal and targeted. Two new deterministic MSTest/FluentAssertions regression tests were added, with fail-before/pass-after evidence. The full C# toolchain (CSharpier, analyzers, nullable, MSTest+coverage) passed cleanly in a single pass with 4664/4664 tests. No coverage regression was found at whole-process, package, or changed-class scope. Overall policy compliance verdict: **PASS**, with one informational note on a pre-existing, change-unrelated repo-wide coverage condition (see §5) and one informational note on branch staleness (see §6).

## 1. Scope and Baseline

- Base branch: `main` (tip `5c4bf31e25210eb850827f2668c74cd72d5fa231`).
- Feature branch: `TaskMaster-wt-2026-07-07-18-37` (worktree). `git merge-base HEAD main` = `254388adeb4e189e8c3781b7c2096a7b4b208980`, which is also the branch's current `HEAD` — i.e., the branch carries no committed history ahead of the merge-base; all feature work is present as **uncommitted working-tree changes** on top of that merge-base. The true feature-vs-base diff was therefore computed as `git diff HEAD` (working tree vs. merge-base), confirmed equal in `.cs` scope to `git diff main -- '*.cs' '*.csproj' '*.sln'` (main's post-merge-base commits touch no `.cs`/`.csproj`/`.sln` files).
- Changed files (full branch diff, `git diff HEAD --numstat`):
  - `UtilitiesCS/HelperClasses/ThemeHelpers/Theme.Rendering.cs` (+12/-5) — production
  - `QuickFiler/Helper Classes/QfcThemeHelper.cs` (+1/-1) — production
  - `UtilitiesCS.Test/HelperClasses/ThemeHelpers/Theme.MailLabelThemingTests.cs` (+25/-0) — test
  - `QuickFiler.Test/Helper Classes/QfcThemeHelperTests.cs` (+26/-0) — test
  - `.claude/agent-memory/task-researcher/MEMORY.md` (+2/-0) — non-code, out of policy scope (agent memory note recording prior research; not reviewed further)
- No caller-supplied scope narrowing was detected in the task prompt for this review. The prompt explicitly instructed a full feature-vs-base audit and named all in-scope production and test files consistent with the actual diff; no attempt was made to limit review to a plan/task/phase subset. No `## Rejected Scope Narrowing` entries are recorded.
- PR context artifacts (`artifacts/pr_context.summary.txt`, `artifacts/pr_context.appendix.txt`) did not exist at review start (no PR has been opened for this branch; MCP `collect_pr_context` was unavailable in this session's toolset). Per the PR-context-artifacts skill, they were regenerated by this reviewer from the verified `git diff HEAD` data described above and written to the canonical locations before proceeding.

## 2. General Code Change Policy (`.claude/rules/general-code-change.md`, CLAUDE.md §General Code Change Policy)

| Rule | Verdict | Evidence |
|---|---|---|
| Simplicity first / no cleverness | PASS | The fix is two one-line/one-clause edits: a null-guard on the probe lambda and a second narrow `catch` clause. No indirection introduced. |
| Fail-fast / no silent broad-catch | PASS | `grep -n "catch (" UtilitiesCS/HelperClasses/ThemeHelpers/Theme.Rendering.cs` shows exactly two catch clauses: `catch (System.Runtime.InteropServices.COMException)` and `catch (System.NullReferenceException)`. No `catch (Exception)` or bare `catch` was introduced anywhere in the diff. Both clauses are narrowly typed and both set `isRead = false;` (a documented, intentional default), consistent with fail-fast (unrelated exception types still propagate). |
| Bugfix Workflow (failing regression test first, then minimal fix, then full toolchain) | PASS | `evidence/regression-testing/fail-before-theme-nre-probe.2026-07-08T09-15.md` and `evidence/regression-testing/fail-before-qfcthemehelper-null-mail.2026-07-08T09-15.md` record `EXIT_CODE: 1` pre-fix runs with the exact `NullReferenceException` stack traces at `Theme.Rendering.cs:45` and `QfcThemeHelper.cs:89`. Post-fix, `evidence/regression-testing/targeted-vstest-utilitiescs.2026-07-08T09-15.md` and `evidence/regression-testing/targeted-vstest-quickfiler.2026-07-08T09-15.md` record all targeted tests passing. The full four-stage toolchain then ran clean (§4). |
| No opportunistic refactor / minimal targeted change | PASS | `evidence/regression-testing/implementation-scope.2026-07-08T09-15.md` (`git diff --stat`) confirms exactly two production files touched, matching the plan's declared "Chosen Fix Shape." No unrelated statement in either file was modified (verified directly against the diff hunks: `Theme.Rendering.cs` diff touches only the comment block and the catch-clause list at lines 31-58; `QfcThemeHelper.cs` diff touches only line 89). |
| File size limit (<=500 lines) | PASS | `wc -l`: `Theme.Rendering.cs` = 130, `QfcThemeHelper.cs` = 365, `Theme.MailLabelThemingTests.cs` = 181, `QfcThemeHelperTests.cs` = 451. All four files, including the two test files, are under the 500-line ceiling (the 451-line test file crossed no threshold introduced by this change; it grew by 26 lines from a lower baseline). |
| I/O isolation / no temp files | PASS | New test code uses in-process WinForms controls without native handles (`new Label()`, no `.Handle` access) and a `FakeQfcItemController` test double (`QfcThemeHelperTests.cs`); no `Path.GetTempPath`/`Path.GetTempFileName`/`File.WriteAllText` appears in either new test. No live Outlook or COM object is constructed; `Microsoft.Office.Interop.Outlook` types referenced in the test file are only used through the pre-existing fake/mocked seam. |
| Comment "why, not what" | PASS | The updated comment block in `Theme.Rendering.cs` (lines 33-42) explains the rationale for extending the catch surface to `NullReferenceException`, referencing issue #269 explicitly. |

## 3. General Unit Test Policy (`.claude/rules/general-unit-test.md`, CLAUDE.md §General/§C# Unit Test Policy)

| Rule | Verdict | Evidence |
|---|---|---|
| MSTest + Moq + FluentAssertions | PASS | Both new tests use `[TestMethod]` inside existing `[TestClass]` fixtures; both use FluentAssertions (`.Should().NotThrow()`, `.Should().Be(...)`, `.Should().BeFalse()`); the QuickFiler test uses `Mock<IUiDispatcher>()` (Moq). No xUnit/NUnit introduced. |
| Independence / Isolation / Determinism | PASS | Each test constructs its own `Theme`/`Label`/`FakeQfcItemController`/`Mock` instances; no shared mutable static state; probe faults are injected via delegate/lambda, not real COM or timing. |
| Arrange-Act-Assert with documented intent | PASS | Both new tests carry `// Arrange`, `// Act`, `// Assert` comments and a docstring-style comment explaining the issue-#269 scenario being reproduced. |
| No temporary files / no external dependencies | PASS | Confirmed by direct grep of both changed test files for `Path.GetTemp`, `File.WriteAllText`, `new MailItem`, and `Microsoft.Office.Interop` construction — none found outside the pre-existing fake/mock seam. |
| Fail-before / pass-after (bugfix regression) | PASS | See §2 row above; both dossiers are stored under the canonical `evidence/regression-testing/` path per the evidence-and-timestamp-conventions skill. |
| Coverage — no regression on changed lines | PASS | See §5 (Coverage Verification). |

## 4. C# Toolchain (CLAUDE.md §C# Toolchain, `.claude/rules/csharp.md` if present)

All four stages ran in the required order (format -> analyzers -> nullable -> test+coverage), in a single clean pass, scoped to the four changed files for format and full-solution for the build/test stages:

1. Format — `evidence/qa-gates/csharpier-final.2026-07-08T09-15.md` — `EXIT_CODE: 0`, "Checked 4 files ... no files required changes."
2. Analyzers — `evidence/qa-gates/csharp-analyzers-final.2026-07-08T09-15.md` — `EXIT_CODE: 0`, "Build succeeded. 12 Warning(s). 0 Error(s)"; explicitly confirmed no warning originates from any of the four changed files (down from a 72-warning baseline due to incremental-build skips, not attributable to this change).
3. Nullable — `evidence/qa-gates/csharp-nullable-final.2026-07-08T09-15.md` — `EXIT_CODE: 0`, "Build succeeded. 0 Warning(s). 0 Error(s)."
4. Test + coverage — `evidence/qa-gates/csharp-vstest-coverage-final.2026-07-08T09-15.md` — `EXIT_CODE: 0`, "Total tests: 4664. Passed: 4664." A documented, pre-existing parallel-execution flake (unrelated to the four changed files; see `.claude/agent-memory/atomic-executor/project_utilitiescs_test_parallelism_flakiness.md`) was observed once in-session and did not recur on rerun; the authoritative, reported result is the clean 4664/4664 pass.

**Verdict: PASS.** No restart of the loop was required; no step auto-fixed or failed.

## 5. Coverage Verification (C# — the only language with changed files on this branch)

**Languages with changed files on the branch:** C# only (`.cs`). No TypeScript, Python, or PowerShell files changed on this branch (confirmed via `git diff HEAD --name-only`), so those language rows are not applicable — this is a zero-changed-files determination, not a scope narrowing of a language that has changes.

**C# coverage artifact:** No pre-existing `artifacts/csharp/coverage.xml` existed at review start. Per the mandatory Evidence Location Invariant, this repository's real C# coverage evidence lives at the feature-scoped canonical paths `evidence/baseline/coverage-baseline.cobertura.xml` and `evidence/qa-gates/coverage-final.cobertura.xml` (Cobertura format, produced by `dotnet-coverage merge -f cobertura` against the vstest `.coverage` output for the two impacted test assemblies, `UtilitiesCS.Test` + `QuickFiler.Test`). This reviewer copied the final artifact to `artifacts/csharp/coverage.xml` (a non-mutating placement of pre-existing evidence at the documented lookup path; no coverage generation was rerun) and independently re-parsed both baseline and final Cobertura files directly (the artifact is Cobertura, not JaCoCo, so the generic JaCoCo-based percentage extraction returns null; percentages below are computed manually from the Cobertura `<line hits="...">` elements).

**C# coverage verdict: PASS.** Evidence:

- Repo/run-scope whole-process line coverage (raw, including third-party/vendor DLLs loaded by the two-assembly run — QuickFiler and UtilitiesCS.Test/QuickFiler.Test only, not a full-repo suite run): baseline 65.73% (112696/171461) -> final 65.73% (112727/171496). No change at this scope; this raw figure is known (project memory `csharp-repowide-coverage-below-80`) to be a vendor-inflated denominator artifact, not the meaningful first-party number, for a full-repo comparison — it is cited here only to confirm no regression at that scope, not as the authoritative repo-wide percentage.
- First-party line coverage, computed by this reviewer via by-name exclusion of the nine known third-party packages present in this Cobertura file (`System.Interactive`, `Mono.Reflection`, `Swordfish.NET.General`, `log4net`, `System.Linq.Async`, `FluentAssertions`, `Deedle`, `FSharp.Core`, `SVGControl`): baseline 88.82% (212914/239721) -> final 88.82% (212976/239791). No regression; both readings clear the CLAUDE.md 80% repo-wide floor. (This two-assembly run's first-party package set also includes four first-party assemblies with 0% or near-0% coverage — `ToDoModel`, `Tags`, `TaskVisualization`, `TaskMaster` — because they were loaded transitively but not exercised by these two test assemblies; a true full-repo run would include their own dedicated test suites and would not show 0%. This is a pre-existing property of running only two of several test assemblies locally, not attributable to this change, and does not move the pass/fail outcome for either baseline or final.)
- Package-level (first-party, directly touched by this change): `UtilitiesCS` 88.21% -> 88.20% (-0.01pt, rounding noise); `QuickFiler` 72.51% -> 72.53% (+0.02pt). Both within the noise band documented in `evidence/qa-gates/csharp-coverage-comparison.2026-07-08T09-15.md`.
- Class-level, changed classes: `UtilitiesCS.Theme` (`Theme.Rendering.cs` partial) 54.05% -> 56.41% (+2.36pt, the new catch-clause branch now covered); `QuickFiler.QfcThemeHelper` unchanged at 96.45% (the changed probe line was already covered pre-fix by an existing non-null-`Mail` test and is now also covered by the new null-`Mail` test).
- New/changed-code coverage: the two changed production lines/blocks (`QfcThemeHelper.cs:89` and `Theme.Rendering.cs:42-53`) are each directly exercised by a dedicated new regression test asserting the fault path, in addition to pre-existing tests exercising the non-fault path — effectively 100% coverage on the diff hunks, exceeding the >=90% new/changed-code floor (CLAUDE.md) and the >=85%/>=75% modified-file floor (`.claude/rules/quality-tiers.md`, `general-unit-test.md`).
- No regression on changed lines: confirmed at every measured scope above (whole-process, first-party aggregate, package, and class level); two scopes improved, none regressed.

**Note on the modified-file 85%/75% class-level threshold vs. the CLAUDE.md 80% repo-wide floor (policy-doc conflict, pre-existing, not introduced by this change):** `.claude/rules/quality-tiers.md` and `general-unit-test.md` assert a uniform >=85% line / >=75% branch floor for all changed/modified files and repo-wide, calling out that "tier-specific lower thresholds are not used" and labeling this "Authoritative Decision #2." CLAUDE.md's C# Unit Test Policy instead states a repo-wide >=80% floor with documented COM/VSTO/WinForms exemptions and a >=90% floor only for newly added modules/classes/methods. These two policy documents conflict on the applicable numeric floor. Per the prior #178 governance-sync decision (recorded in project memory), CLAUDE.md's 80%/90% C# figures are treated as authoritative in this repository and the 85%/75% uniform-tier rule was not adopted for C#; this audit applies that resolution. Under either floor, this change's own diff-hunk coverage (~100%) and the first-party package/class figures cited above pass; the pre-existing `Theme.Rendering.cs` class-level rate (56.41%) would fail the newer 85%/75% document in isolation, but that shortfall existed before this change, is not on a changed/new file's newly-added lines, and is not this PR's responsibility to close under either policy document's own "no regression / no new files" scoping.

## 6. Evidence Location Compliance

`git diff HEAD --name-only | grep -E "^artifacts/(baselines|qa|evidence|coverage)/"` returned no matches (exit code 1, empty result) — no forbidden evidence path was written by this branch's changes. `validate_evidence_locations.py` is not present in this repository (confirmed absent; consistent with prior review-cycle findings recorded in project memory `taskmaster-validator-memories-are-cross-repo`), so the manual `git diff`-based scan above is the working substitute. All evidence for this feature is correctly located under `docs/features/active/2026-07-07-quickfiler-darkmode-unread-labels-stay-blue-269/evidence/{baseline,regression-testing,qa-gates,issue-updates}/`, matching the canonical `<FEATURE>/evidence/<kind>/` scheme. No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` entries are required.

## 7. Informational Notes (non-blocking)

1. **Branch staleness.** This worktree's `HEAD` equals its merge-base with `main`; `main` has advanced (unrelated `epic/store-lockup-resilience-planning` documentation work, `5c4bf31e`) with no `.cs`/`.csproj`/`.sln` changes in that gap. The feature diff is entirely uncommitted working-tree changes. Recommend committing and rebasing/merging onto current `main` before PR creation so CI runs against a current base; this is process hygiene, not a defect in the change itself.
2. **P2-T7 (CI check verification) is explicitly deferred.** No PR exists yet for this branch (`evidence/qa-gates/ci-check-verification.2026-07-08T10-45.md`); this is an authorized deferral per the plan's own task text, not a policy violation, but it means no CI-run confirmation of required checks exists yet. This must be completed once a PR is opened.
3. **`catch (NullReferenceException)` as a design pattern.** Catching `NullReferenceException` is generally discouraged by .NET guidance because it can mask unrelated null-dereference bugs inside the guarded block, not only the intended probe fault. The plan explicitly reasons about this trade-off (defense-in-depth alongside the root-cause null-guard at the probe construction site) and the catch block is narrowly scoped to a single statement (`isRead = MailRead();`). No analyzer diagnostic (e.g., CA1065/CA2201-family) was raised by the clean analyzer build. This is noted for completeness in the accompanying code-review artifact and is not a policy violation.

## Overall Verdict

**PASS.** No blocking (FAIL or blocking-PARTIAL) findings were identified.
