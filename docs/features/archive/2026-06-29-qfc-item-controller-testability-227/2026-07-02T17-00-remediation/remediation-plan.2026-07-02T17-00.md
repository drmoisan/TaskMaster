# Remediation Plan — Cycle 5 (Issue #227)

- **Generated:** 2026-07-02T17-00
- **Feature Folder:** `docs/features/active/2026-06-29-qfc-item-controller-testability-227/`
- **Inputs:** `2026-07-02T17-00-remediation/remediation-inputs.2026-07-02T17-00.md` (R1, R2, R3), `artifacts/research/2026-07-02T16-15-qfc-item-controller-headless-itemviewer-research.md` (design basis), `evidence/other/exemption-boundary.2026-07-02T15-05.md` (the 24-member boundary being reduced).
- **Scope:** De-exempt 5 members with no open risk: `ResolveControlGroups(ItemViewer)`, `WireControlTreeEvents()`, `WireEvents()` (R1+R3 — headless `ItemViewer` construction, no new production seam beyond a widened test technique) and `ToggleExpansionOff`/`ToggleExpansionOn` (R2 — `TlpCellSnapShot`/`IContainerControlLocal` retrofit, a small mechanical interface-widening seam). Target: residual exemption count 24 → 19.
- **Out of scope:** The remaining 19 residuals (9 orchestration/`Create*Async`/`InitializeWebViewAsync` members blocked by the unbuilt WinForms message-pump seam, `InitializeWebViewAsync`'s raw WebView2 accessor, 3 deliberate virtual test seams, 6 `async void` shells, 1 genuine `WebView2CoreInitializer` external-runtime dependency). Building a WinForms `Application.Run()`-on-background-thread pump is tracked as a separate follow-up issue.

## Design decisions locked in by this plan (read before executing)

1. **Evidence paths.** All evidence in this plan resolves to `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/<kind>/`, per `evidence-and-timestamp-conventions`. Baseline artifacts use `evidence/remediation-baseline/`; command-step QA artifacts use `evidence/qa-gates/`; the coverage-delta artifact uses `evidence/regression-testing/`; the reduced boundary artifact uses `evidence/other/`. No `artifacts/baselines/`, `artifacts/qa/`, or `artifacts/coverage/` paths are used anywhere in this plan.
2. **No new test files for R1/R3.** `ResolveControlGroups`'s test is added to the existing `QuickFiler.Test/Controllers/QfcItemController.ViewerSetupTests.cs` (production-file-to-test-file mapping convention already used throughout this feature: `ViewerSetupTests.cs` ↔ `QfcItemController.ViewerSetup.cs`). `WireControlTreeEvents`'s and `WireEvents`'s tests are added to the existing `QuickFiler.Test/Controllers/QfcItemController.EventWiringTests.cs` (↔ `QfcItemController.EventWiring.cs`). This avoids any new `<Compile Include>` csproj wiring for R1/R3.
3. **R1/R3 test technique, ground-truthed against current source.** Each new test installs and restores a `SynchronizationContext` inline (mirroring `UtilitiesCS.Test/Threading/ProgressPane_Tests.cs:59-83`'s try/finally pattern exactly, not the non-restoring `EnsureSynchronizationContext()` guard in `QfcItemController.TestSupport.cs:87-93`, because the hard constraint below requires install/restore with no cross-test leakage), constructs a real `new QuickFiler.ItemViewer()`, injects it into a `HarnessController` via `QfcItemControllerTestSupport.SetField(controller, "_itemViewer", viewer)`, and invokes the target internal method via `QfcItemControllerTestSupport.InvokeNonPublic`. Confirmed via direct read of `ItemViewer.cs:22-29` (constructor), `QfcItemController.ViewerSetup.cs:79-123` (`ResolveControlGroups` reads `TableLayoutPanels`/`Buttons`, both public properties on `QfcItemController.cs:96,262`), and `QfcItemController.EventWiring.cs:43-71` (`WireControlTreeEvents` requires `Buttons` to already be populated — i.e., `ResolveControlGroups` must run first as Arrange for the `WireControlTreeEvents`/`WireEvents` tests). `ButtonSVG : Button` (confirmed at `SVGControl/ButtonSVG.cs:12`), so the real `ItemViewer`'s six `ButtonSVG` fields (`BtnDelItem`, `BtnPopOut`, `BtnFlagTask`, `BtnForward`, `BtnReply`, `BtnReplyAll`) populate `Buttons` via the `x is Button` filter in `ResolveControlGroups`.
4. **Event-wiring-outcome assertions (ground-truthed, no framework-internal reflection).** `WireControlTreeEvents` wiring is asserted by raising `Control.OnPreviewKeyDown`/`OnKeyDown` (both `protected virtual`, stable across .NET Framework versions) via reflection on `viewer.LblAcOpen` (a `Label`, not in the `CboFolders` except-list per `QfcItemController.EventWiring.cs:57`) and verifying the injected `Mock<IQfcKeyboardHandler>`'s `KeyboardHandler_PreviewKeyDownAsync`/`KeyboardHandler_KeyDownAsync` were invoked, plus raising `Control.OnMouseEnter` on `viewer.BtnDelItem` (one of the populated `Buttons`) and asserting `BackColor` changes to the themed mouse-over color (using the existing `QfcItemControllerTestSupport.BuildColorTheme`/`BuildThemeDictionary`/`InjectThemes` helpers — `Button_MouseEnter` at `QfcItemController.EventHandlers.cs:137-140` only reads `_themes[_activeTheme].ButtonMouseOverColor`, no dispatcher involved, confirmed safe). `WireEvents`'s test reuses the `PreviewKeyDown` signal (proves `WireControlTreeEvents()` ran) plus toggling the real `viewer.ConversationMenuItem.Checked` (with `controller.SuppressEvents = true` to avoid the heavier `CollapseConversation()` branch in `CbxConversation_CheckedChanged`, `QfcItemController.EventHandlers.cs:27-40`) and asserting the `_optionConversationChecked` field updates (proves `WireIntentEvents()` ran).
5. **R2 ground-truth on the two protected `ContainerControl` members.** `IContainerControlLocal` (`UtilitiesCS/Interfaces/IWinForm/IContainerControl.cs:7-23`) declares `CurrentAutoScaleDimensions`/`PerformAutoScale()` alongside members (`ActiveControl`, `AutoScaleDimensions`, `AutoScaleMode`, `AutoValidate`, `ParentForm`, `Validate*`) that are public on `System.Windows.Forms.ContainerControl`/`ScrollableControl`/`Control` and therefore already implicitly satisfied by `ItemViewer : UserControl`. The claim that `CurrentAutoScaleDimensions`/`PerformAutoScale()` specifically are `protected` (not `public`) on `ContainerControl` is corroborated by an independent prior research artifact (`.claude/agent-memory/task-researcher/project_qfc227_headless_itemviewer_and_tlpcellsnapshot.md:38`) but has not been empirically confirmed by a compiler run in this repo. Per the calling context's explicit ground-truth directive, Phase 2 (P2-T3) requires attempting the build with `IContainerControlLocal` added to the class declaration and NO forwarders first, recording the actual compiler diagnostic, before adding the forwarders (P2-T4) — this converts an assumption into an empirically-verified build fact before production code is finalized.
6. **R2 test technique (independent of R1).** `TlpCellSnapShot.ApplyState`/`TlpCellSnapShotList.ApplyState` are tested with a bare `Control` host, a real child `TableLayoutPanel`, and a real child `Label`, using the existing public `SnapCell(TableLayoutPanel, Control)` method to build a valid snapshot, then mutating the live control's `Enabled`/`Visible`/`Text` before calling `ApplyState` against a `Mock<IItemViewer>` whose `Controls` getter returns the host's `ControlCollection` — proving `ApplyState` actually restores the snapshotted state (not merely a no-op replay). No real `ItemViewer` is constructed for this seam. A new test file, `QuickFiler.Test/Helper Classes/TlpCellSnapShotTests.cs` (namespace `QuickFiler.Test.HelperClasses`, matching the existing `ConversationResolverTests.cs` convention in the same folder), is required since no test file currently exists for `TlpCellSnapShot`; this is a legacy non-SDK `packages.config` project, so it requires an explicit `<Compile Include>` entry in `QuickFiler.Test/QuickFiler.Test.csproj`. `ToggleExpansionOff`/`ToggleExpansionOn`'s tests (private methods, reached via `QfcItemControllerTestSupport.InvokeNonPublic`) are added to the existing `QuickFiler.Test/Controllers/QfcItemController.NavigationTests.cs`.
7. **Residual-count verification.** Every count-verification task in this plan uses the bracket-anchored form: `grep -rnE "ExcludeFromCodeCoverage\]" QuickFiler/Controllers/QfcItemController*.cs UtilitiesCS/Threading/WpfUiDispatcher.cs QuickFiler/Viewers/WebView2CoreInitializer.cs QuickFiler/Interfaces/MailItemActionsAdapter.cs`. Ground-truthed at plan-authoring time: 23 matches across the 6 `QfcItemController*.cs` files + 0 (`WpfUiDispatcher.cs`) + 1 (`WebView2CoreInitializer.cs`) + 0 (`MailItemActionsAdapter.cs`) = **24**, matching the ratified-pending boundary exactly. Removing the 5 attributes in this plan (P1-T1/T2/T3, P2-T5/T6) yields **19**.
8. **File-size ledger (500-line cap).** Current line counts (confirmed by direct read at plan-authoring time) and estimated post-change counts: `QfcItemController.ViewerSetup.cs` 286→~282 (comment removal); `QfcItemController.EventWiring.cs` 398→~390; `QfcItemController.Navigation.cs` 237→~229; `TlpCellSnapShot.cs` 213→~216; `IItemViewer.cs` 121→~122; `ItemViewer.cs` 437→~450; `QfcItemController.ViewerSetupTests.cs` 368→~415; `QfcItemController.EventWiringTests.cs` 215→~330; `QfcItemController.NavigationTests.cs` 285→~375; new `TlpCellSnapShotTests.cs` ~130. All comfortably under 500; each phase includes an explicit post-edit line-count check rather than relying on these estimates.

---

### Phase 0 — Baseline capture

- [x] [P0-T1] Read, in order, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`, `docs/features/active/2026-06-29-qfc-item-controller-testability-227/2026-07-02T17-00-remediation/remediation-inputs.2026-07-02T17-00.md`, `artifacts/research/2026-07-02T16-15-qfc-item-controller-headless-itemviewer-research.md`, `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/other/exemption-boundary.2026-07-02T15-05.md`, `QuickFiler/Viewers/ItemViewer.cs`, `QuickFiler/Viewers/IItemViewer.cs`, `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs`, `QuickFiler/Controllers/QfcItemController.EventWiring.cs`, `QuickFiler/Controllers/QfcItemController.Navigation.cs`, `QuickFiler/Helper Classes/TlpCellSnapShot.cs`, `UtilitiesCS/Interfaces/IWinForm/IContainerControl.cs`, `QuickFiler.Test/Controllers/QfcItemController.TestSupport.cs`; write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/remediation-baseline/phase0-instructions-read.2026-07-02T17-00.md` with `Timestamp:`, `Policy Order:` (the list above, in order), and the explicit file list actually read. Acceptance: the artifact exists with all three required fields populated.
- [x] [P0-T2] Run `dotnet tool run csharpier . --check` (or `csharpier . --check` if installed globally) from the repo root with no file modifications; write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/remediation-baseline/baseline-csharpier.2026-07-02T17-00.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields and the recorded `EXIT_CODE` matches the actual command result.
- [x] [P0-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/remediation-baseline/baseline-analyzers.2026-07-02T17-00.md` with the four required fields. Acceptance: same as P0-T2.
- [x] [P0-T4] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/remediation-baseline/baseline-nullable.2026-07-02T17-00.md` with the four required fields. Acceptance: same as P0-T2.
- [x] [P0-T5] Run `vstest.console.exe QuickFiler.Test.dll UtilitiesCS.Test.dll /EnableCodeCoverage /InIsolation`; write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/remediation-baseline/baseline-tests-coverage.2026-07-02T17-00.md` with the four required fields, and an `Output Summary:` including the numeric pass count (expected 4442/4442, per cycle-4's exit state: `UtilitiesCS.Test.dll` 4093 + `QuickFiler.Test.dll` 349) and the repo-wide coverage percentage (expected ~63.28% per cycle-4's final measurement). Acceptance: artifact exists with a numeric coverage headline (no `UNVERIFIED` placeholder).
- [x] [P0-T6] Run `grep -rnE "ExcludeFromCodeCoverage\]" QuickFiler/Controllers/QfcItemController*.cs UtilitiesCS/Threading/WpfUiDispatcher.cs QuickFiler/Viewers/WebView2CoreInitializer.cs QuickFiler/Interfaces/MailItemActionsAdapter.cs` (bracket-anchored form) and confirm the count is 24; write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/remediation-baseline/baseline-exemption-inventory.2026-07-02T17-00.md` with the exact count and the grep command/output. Acceptance: recorded count equals 24.

---

### Phase 1 — R1+R3: Headless `ItemViewer` construction (`ResolveControlGroups`, `WireControlTreeEvents`, `WireEvents`)

- [x] [P1-T1] In `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs`, remove the 4-line residual comment block and the `[System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]` attribute immediately preceding `internal void ResolveControlGroups(ItemViewer itemViewer)` (current lines 75-78), replacing them with a single-line comment: `// De-exempted cycle-5 (R1): covered by a headless real-ItemViewer test, QfcItemController.ViewerSetupTests.cs.`. Do not change the method body. Acceptance: the method carries no `[ExcludeFromCodeCoverage]` attribute and the file still compiles.
- [x] [P1-T2] In `QuickFiler/Controllers/QfcItemController.EventWiring.cs`, remove the 3-line residual comment block and the `[System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]` attribute immediately preceding `internal void WireControlTreeEvents()` (current lines 39-42), replacing them with a single-line comment: `// De-exempted cycle-5 (R1): covered by a headless real-ItemViewer test, QfcItemController.EventWiringTests.cs.`. Do not change the method body. Acceptance: the method carries no `[ExcludeFromCodeCoverage]` attribute and the file still compiles.
- [x] [P1-T3] In `QuickFiler/Controllers/QfcItemController.EventWiring.cs`, remove the 5-line residual comment block and the `[System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]` attribute immediately preceding `internal void WireEvents()` (current lines 27-32), replacing them with a single-line comment: `// De-exempted cycle-5 (R3): both WireControlTreeEvents() and WireIntentEvents() are now non-exempt; covered by a headless real-ItemViewer test, QfcItemController.EventWiringTests.cs.`. Do not change the method body. Acceptance: the method carries no `[ExcludeFromCodeCoverage]` attribute and the file still compiles.
- [x] [P1-T4] In `QuickFiler.Test/Controllers/QfcItemController.ViewerSetupTests.cs`, add `using QuickFiler;` and `using System.Threading;` to the file's using directives, then add a new `[TestMethod]` `ResolveControlGroups_WithHeadlessItemViewer_PopulatesConcreteControlCollections` to `QfcItemController_ViewerSetupTests`: install `SynchronizationContext.SetSynchronizationContext(new SynchronizationContext())` inside a try block (saving the prior context first), construct `var viewer = new QuickFiler.ItemViewer();`, construct a `HarnessController`, inject `_itemViewer` via `QfcItemControllerTestSupport.SetField(controller, "_itemViewer", viewer)`, invoke `QfcItemControllerTestSupport.InvokeNonPublic(controller, "ResolveControlGroups", viewer)`, assert `controller.TableLayoutPanels.Should().NotBeNullOrEmpty();` and `controller.Buttons.Should().NotBeNullOrEmpty();`, and restore the prior `SynchronizationContext` in a finally block. Acceptance: the new test passes; no other test in the file is modified.
- [x] [P1-T5] In `QuickFiler.Test/Controllers/QfcItemController.EventWiringTests.cs`, add `using QuickFiler;`, `using System.Threading;`, and `using System.Drawing;` to the file's using directives, then add a new `[TestMethod]` `WireControlTreeEvents_WithHeadlessItemViewer_WiresKeyboardAndMouseHandlers` to `QfcItemController_EventWiringTests`: install/restore a `SynchronizationContext` (per P1-T4's pattern), construct `var viewer = new QuickFiler.ItemViewer();`, construct a `HarnessController`, inject `_itemViewer` (the viewer) and `_kbdHandler` (a `Mock<IQfcKeyboardHandler>().Object`) via `QfcItemControllerTestSupport.SetField`, inject `_themes`/`_activeTheme` via `QfcItemControllerTestSupport.InjectThemes(controller, QfcItemControllerTestSupport.BuildThemeDictionary("LightNormal", QfcItemControllerTestSupport.BuildColorTheme(Color.Yellow, Color.Green, Color.Gray)), "LightNormal")`, invoke `ResolveControlGroups` then `WireControlTreeEvents` via `QfcItemControllerTestSupport.InvokeNonPublic`, then assert: (a) invoking `typeof(Control).GetMethod("OnPreviewKeyDown", BindingFlags.NonPublic | BindingFlags.Instance).Invoke(viewer.LblAcOpen, new object[] { new PreviewKeyDownEventArgs(Keys.A) })` causes `mockKbd.Verify(k => k.KeyboardHandler_PreviewKeyDownAsync(viewer.LblAcOpen, It.IsAny<PreviewKeyDownEventArgs>()), Times.Once());` to pass; (b) invoking `OnKeyDown` similarly on `viewer.LblAcOpen` causes `mockKbd.Verify(k => k.KeyboardHandler_KeyDownAsync(viewer.LblAcOpen, It.IsAny<KeyEventArgs>()), Times.Once());` to pass; (c) invoking `OnMouseEnter` on `viewer.BtnDelItem` causes `viewer.BtnDelItem.BackColor.Should().Be(Color.Yellow);`. Acceptance: all three assertions pass in the new test; no other test in the file is modified.
- [x] [P1-T6] In `QuickFiler.Test/Controllers/QfcItemController.EventWiringTests.cs`, add a new `[TestMethod]` `WireEvents_WithHeadlessItemViewer_WiresBothControlTreeAndIntentEvents` to `QfcItemController_EventWiringTests`: install/restore a `SynchronizationContext`, construct a real `ItemViewer`, a `HarnessController` with `_itemViewer` and a `Mock<IQfcKeyboardHandler>()`-backed `_kbdHandler` injected, invoke `ResolveControlGroups` via `InvokeNonPublic` (Arrange prerequisite, per design-decision §3), set `controller.SuppressEvents = true;`, invoke `WireEvents` via `QfcItemControllerTestSupport.InvokeNonPublic(controller, "WireEvents")`, then assert: (a) raising `OnPreviewKeyDown` on `viewer.LblAcOpen` causes the mocked `KeyboardHandler_PreviewKeyDownAsync` to be invoked once (proves `WireControlTreeEvents()` ran); (b) setting `viewer.ConversationMenuItem.Checked = true;` causes `QfcItemControllerTestSupport.GetField(controller, "_optionConversationChecked").Should().Be(true);` (proves `WireIntentEvents()` ran). Acceptance: both assertions pass in the new test; no other test in the file is modified.
- [x] [P1-T7] Run `vstest.console.exe QuickFiler.Test.dll /Tests:ResolveControlGroups_WithHeadlessItemViewer_PopulatesConcreteControlCollections,WireControlTreeEvents_WithHeadlessItemViewer_WiresKeyboardAndMouseHandlers,WireEvents_WithHeadlessItemViewer_WiresBothControlTreeAndIntentEvents`; confirm all 3 new tests pass. Also run `grep -rnE "ExcludeFromCodeCoverage\]" QuickFiler/Controllers/QfcItemController*.cs UtilitiesCS/Threading/WpfUiDispatcher.cs QuickFiler/Viewers/WebView2CoreInitializer.cs QuickFiler/Interfaces/MailItemActionsAdapter.cs` and confirm the count is 21 (24 minus the 3 attributes removed in P1-T1/T2/T3). Confirm `QfcItemController.ViewerSetup.cs`, `QfcItemController.EventWiring.cs`, `QfcItemController.ViewerSetupTests.cs`, and `QfcItemController.EventWiringTests.cs` are each `<= 500` lines. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/p1-r1r3-verification.<TIMESTAMP>.md` recording the test command/result, the grep count, and the four line counts. Acceptance: all 3 named tests pass; exemption count equals 21; all four files `<= 500` lines.

---

### Phase 2 — R2: `TlpCellSnapShot` / `IContainerControlLocal` retrofit

- [x] [P2-T1] In `QuickFiler/Helper Classes/TlpCellSnapShot.cs`, add `using UtilitiesCS.Interfaces.IWinForm;` to the using directives, then retype `TlpCellSnapShotList.ApplyState(Control root)` (current line 61) and `TlpCellSnapShot.ApplyState(Control root)` (current line 181) to `ApplyState(IContainerControlLocal root)`. Do not change either method body. Acceptance: the file compiles once P2-T2/P2-T3/P2-T4 are applied; both `ApplyState` overloads accept `IContainerControlLocal`.
- [x] [P2-T2] In `QuickFiler/Viewers/IItemViewer.cs`, change `public interface IItemViewer : IUserControl` (current line 15) to `public interface IItemViewer : IUserControl, IContainerControlLocal`. Acceptance: the file compiles (the existing `using UtilitiesCS.Interfaces.IWinForm;` at line 11 already resolves `IContainerControlLocal`); `Mock<IItemViewer>` construction continues to succeed via Moq's interface-proxy generation.
- [x] [P2-T3] In `QuickFiler/Viewers/ItemViewer.cs`, add `using UtilitiesCS.Interfaces.IWinForm;` to the using directives and change `public partial class ItemViewer : UserControl, IItemViewer` (current line 20) to `public partial class ItemViewer : UserControl, IItemViewer, IContainerControlLocal`, with NO explicit-interface-implementation forwarders added yet. Attempt `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU"` and record the exact compiler diagnostic(s) produced (expected: CS0737-class errors naming `CurrentAutoScaleDimensions` and `PerformAutoScale` as inaccessible/not implemented, empirically confirming these two members are `protected` on `ContainerControl`, per design-decision §5's ground-truth directive). Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/other/p2-t3-containercontrol-accessibility-groundtruth.<TIMESTAMP>.md` with the exact command and full diagnostic text (or, if the build succeeds with zero errors, record that finding instead — meaning the two members are already public and P2-T4 is not needed). Acceptance: the artifact records the actual compiler output (success or the specific named errors), not an assumption.
- [x] [P2-T4] If P2-T3's recorded build failed with `CurrentAutoScaleDimensions`/`PerformAutoScale` accessibility errors: in `QuickFiler/Viewers/ItemViewer.cs`, add two explicit-interface-implementation forwarders — `SizeF IContainerControlLocal.CurrentAutoScaleDimensions => this.CurrentAutoScaleDimensions;` and `void IContainerControlLocal.PerformAutoScale() => this.PerformAutoScale();` — placed after the constructor, then rebuild with `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU"` and confirm zero errors. If P2-T3's build already succeeded with zero errors, this task is not needed; record `N/A — members already public, confirmed by P2-T3` in the same evidence artifact referenced below instead of adding forwarders. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/p2-t4-itemviewer-build-clean.<TIMESTAMP>.md` with the final build command/`EXIT_CODE:`/`Output Summary:`. Acceptance: `ItemViewer` fully satisfies `IContainerControlLocal` with a clean (0-error) build in the recorded pass.
- [x] [P2-T5] In `QuickFiler/Controllers/QfcItemController.Navigation.cs`, remove the 3-line residual comment block and the `[System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]` attribute immediately preceding `private void ToggleExpansionOff()` (current lines 207-210), and change `_tlpStates["Compressed"].ApplyState((ItemViewer)_itemViewer); // concrete-bound seam (P2-T4): control-host path, runs on real ItemViewer during init` (current line 213) to `_tlpStates["Compressed"].ApplyState(_itemViewer);`. Acceptance: `ToggleExpansionOff` carries no `[ExcludeFromCodeCoverage]` attribute, contains no `(ItemViewer)` cast, and the file compiles.
- [x] [P2-T6] In `QuickFiler/Controllers/QfcItemController.Navigation.cs`, remove the 3-line residual comment block and the `[System.Diagnostics.CodeAnalysis.ExcludeFromCodeCoverage]` attribute immediately preceding `private void ToggleExpansionOn()` (current lines 221-224), and change `_tlpStates["Expanded"].ApplyState((ItemViewer)_itemViewer); // concrete-bound seam (P2-T4): control-host path, runs on real ItemViewer during init` (current line 227) to `_tlpStates["Expanded"].ApplyState(_itemViewer);`. Acceptance: `ToggleExpansionOn` carries no `[ExcludeFromCodeCoverage]` attribute, contains no `(ItemViewer)` cast, and the file compiles.
- [x] [P2-T7] Create `QuickFiler.Test/Helper Classes/TlpCellSnapShotTests.cs` with `namespace QuickFiler.Test.HelperClasses` (matching the existing `ConversationResolverTests.cs` convention in the same folder), using directives `System`, `System.Collections.Generic`, `System.Windows.Forms`, `FluentAssertions`, `Microsoft.VisualStudio.TestTools.UnitTesting`, `Moq`, `QuickFiler`, and a `[TestClass] public class TlpCellSnapShotTests` containing two `[TestMethod]`s: (1) `ApplyState_OnInstance_RestoresSnapshottedEnabledVisibleAndAcceleratorText` — builds a `Control` host containing a `TableLayoutPanel { Name = "Compressed", ColumnCount = 1, RowCount = 1 }` containing a `Label { Name = "LblAcOpen", Text = "Original", Enabled = true, Visible = true }`, calls `snapshot.SnapCell(tlp, label)` on a `new TlpCellSnapShot()`, then mutates the live label (`Enabled = false; Visible = false; Text = "Changed";`), builds a `Mock<IItemViewer>()` with `.Setup(v => v.Controls).Returns(host.Controls)`, calls `snapshot.ApplyState(mockViewer.Object)`, and asserts `label.Enabled.Should().BeTrue(); label.Visible.Should().BeTrue(); label.Text.Should().Be("Original");`; (2) `ApplyState_OnList_AppliesEveryEntry` — repeats the same host/tlp/label setup with two distinct named controls each snapshotted into a `TlpCellSnapShotList`, mutates both live controls, calls the list's `ApplyState(mockViewer.Object)` once, and asserts both controls' `Enabled`/`Visible` are restored. Acceptance: both tests pass; the file is `<= 500` lines.
- [x] [P2-T8] In `QuickFiler.Test/QuickFiler.Test.csproj`, add `<Compile Include="Helper Classes\TlpCellSnapShotTests.cs" />` adjacent to the existing `Helper Classes\ConversationResolverTests.cs`/`Helper Classes\MailItemInfoTests.cs` entries (current lines 104-105). Acceptance: the project builds and the new test file's tests are discovered by `vstest.console.exe`.
- [x] [P2-T9] In `QuickFiler.Test/Controllers/QfcItemController.NavigationTests.cs`, add `using System.Collections.Generic;` to the using directives, then add two new `[TestMethod]`s to `QfcItemController_NavigationTests`: (1) `ToggleExpansionOff_AppliesCompressedSnapshotAndClearsExpandedFlag` — builds a `Control` host containing a `TableLayoutPanel { Name = "Compressed", ColumnCount = 1, RowCount = 1 }` containing a `Label { Name = "LblAcOpen" }`, calls `SnapCell` on a `new TlpCellSnapShot()` while the label is `Enabled = true, Visible = true`, mutates the live label to `Enabled = false, Visible = false`, builds a `Mock<IItemViewer>()` with `.Setup(v => v.Controls).Returns(host.Controls)`, constructs a `HarnessController` with `_itemViewer` set to the mock, `_expanded` set to `true`, and `_tlpStates` set to a `TlpCellStates` containing key `"Compressed"` mapped to the snapshot (via `TryAddState("Compressed", new List<TlpCellSnapShot> { snapshot })`), invokes `QfcItemControllerTestSupport.InvokeNonPublic(controller, "ToggleExpansionOff")`, and asserts `label.Enabled.Should().BeTrue(); label.Visible.Should().BeTrue();` and `QfcItemControllerTestSupport.GetField(controller, "_expanded").Should().Be(false);`; (2) `ToggleExpansionOn_AppliesExpandedSnapshotAndSetsExpandedFlag` — same pattern with `TableLayoutPanel { Name = "Expanded" }`, snapshot taken while `Enabled = false, Visible = false`, live label mutated to `Enabled = true, Visible = true`, `_tlpStates` keyed `"Expanded"`, `ItemHelper` left `null` (so the timer-creation branch is skipped), invokes `ToggleExpansionOn`, and asserts the label reverts to `Enabled = false, Visible = false` and `_expanded` becomes `true`. Acceptance: both tests pass; the file is `<= 500` lines.
- [x] [P2-T10] Run `vstest.console.exe QuickFiler.Test.dll /Tests:ApplyState_OnInstance_RestoresSnapshottedEnabledVisibleAndAcceleratorText,ApplyState_OnList_AppliesEveryEntry,ToggleExpansionOff_AppliesCompressedSnapshotAndClearsExpandedFlag,ToggleExpansionOn_AppliesExpandedSnapshotAndSetsExpandedFlag`; confirm all 4 tests pass. Also run `grep -rnE "ExcludeFromCodeCoverage\]" QuickFiler/Controllers/QfcItemController*.cs UtilitiesCS/Threading/WpfUiDispatcher.cs QuickFiler/Viewers/WebView2CoreInitializer.cs QuickFiler/Interfaces/MailItemActionsAdapter.cs` and confirm the count is 19 (21 minus the 2 attributes removed in P2-T5/T6). Confirm `TlpCellSnapShot.cs`, `IItemViewer.cs`, `ItemViewer.cs`, `QfcItemController.Navigation.cs`, `TlpCellSnapShotTests.cs`, and `QfcItemController.NavigationTests.cs` are each `<= 500` lines. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/p2-r2-verification.<TIMESTAMP>.md` recording the test command/result, the grep count, and the six line counts. Acceptance: all 4 named tests pass; exemption count equals 19; all six files `<= 500` lines.

---

### Phase 3 — Final QA loop and reduced boundary ratification

- [x] [P3-T1] Run `dotnet tool run csharpier .` (apply formatting, not just check) across the full repo; if any file changes, restart this loop from this step. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/final-csharpier.<TIMESTAMP>.md` with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:`. Acceptance: exit code 0 and zero files changed in the recorded pass.
- [x] [P3-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; if it fails or reports new diagnostics, fix and restart the loop from P3-T1. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/final-analyzers.<TIMESTAMP>.md` with the four required fields. Acceptance: exit code 0 in the recorded pass.
- [x] [P3-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`; if it fails, fix and restart the loop from P3-T1. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/final-nullable.<TIMESTAMP>.md` with the four required fields. Acceptance: exit code 0 in the recorded pass.
- [x] [P3-T4] Run `vstest.console.exe QuickFiler.Test.dll UtilitiesCS.Test.dll /EnableCodeCoverage /InIsolation`; if any test fails, fix and restart the loop from P3-T1. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/final-tests-coverage.<TIMESTAMP>.md` with the four required fields, and an `Output Summary:` including the numeric pass count (expected 4449/4449 — the P0-T5 baseline of 4442 plus the 7 new tests added across Phase 1 (3) and Phase 2 (4)) and the repo-wide coverage percentage. Acceptance: all tests pass, zero regressions; numeric coverage values recorded (no `UNVERIFIED` placeholder).
- [x] [P3-T5] Compute the coverage delta versus the Phase 0 baseline (`evidence/remediation-baseline/baseline-tests-coverage.2026-07-02T17-00.md`): repo-wide delta and confirmation that `ResolveControlGroups`, `WireControlTreeEvents`, `WireEvents` (`QfcItemController.ViewerSetup.cs`/`EventWiring.cs`), `ToggleExpansionOff`, `ToggleExpansionOn` (`QfcItemController.Navigation.cs`), and both `TlpCellSnapShot`/`TlpCellSnapShotList.ApplyState` overloads now show as covered in the post-change coverage report. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/regression-testing/coverage-delta.<TIMESTAMP>.md` with baseline coverage, post-change coverage, and the per-member coverage confirmation. Acceptance: no regression recorded; all 7 named members show as covered in the post-change report.
- [x] [P3-T6] Re-run `grep -rnE "ExcludeFromCodeCoverage\]" QuickFiler/Controllers/QfcItemController*.cs UtilitiesCS/Threading/WpfUiDispatcher.cs QuickFiler/Viewers/WebView2CoreInitializer.cs QuickFiler/Interfaces/MailItemActionsAdapter.cs` and confirm the count is 19; and confirm every file touched or added in this plan (`QfcItemController.ViewerSetup.cs`, `QfcItemController.EventWiring.cs`, `QfcItemController.Navigation.cs`, `TlpCellSnapShot.cs`, `IItemViewer.cs`, `ItemViewer.cs`, `QfcItemController.ViewerSetupTests.cs`, `QfcItemController.EventWiringTests.cs`, `QfcItemController.NavigationTests.cs`, `TlpCellSnapShotTests.cs`) is `<= 500` lines. Write `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/qa-gates/final-residual-and-file-size-verification.<TIMESTAMP>.md` recording the grep count and all ten file line counts. Acceptance: recorded exemption count equals 19; all ten files `<= 500` lines.
- [x] [P3-T7] Write the reduced exemption boundary artifact `docs/features/active/2026-06-29-qfc-item-controller-testability-227/evidence/other/exemption-boundary.<TIMESTAMP>.md`, superseding `evidence/other/exemption-boundary.2026-07-02T15-05.md`: reproduce the 19-member residual set by category — concrete control-tree orchestration tied to the retained `(ItemViewer)`-cast/no-leaf-interface invariant (12 → 9: drops `ResolveControlGroups`, `WireControlTreeEvents`, `WireEvents`; retains `Initialize` (9-arg), `Initialize(bool)`, `InitializeAsync`, `InitializeGraphicsAsync`, `InitializeSequentialAsync`, `CreateAsync`, `CreateSequentialAsync`, `InitializeWebViewAsync`, `ResolveControlGroupsAsync`); the `TlpCellSnapShot` follow-up category (2 → 0: `ToggleExpansionOff`/`ToggleExpansionOn` both resolved, category removed); deliberate virtual test seams (3, unchanged: `DoLoadConversationResolverCoreAsync`, `ToggleExpansion(Enums.ToggleState)`, `ToggleExpansionAsync(Enums.ToggleState)`); `async void` WinForms-event-signature shells (6, unchanged); genuine external-runtime dependency (1, unchanged: `WebView2CoreInitializer`). Total: 9 + 0 + 3 + 6 + 1 = 19, matching P3-T6's grep count. Submit for maintainer ratification alongside the prior 24-member boundary, per the authority-scoped coverage-exception precedent. Acceptance: the artifact's per-category arithmetic sums to 19 and matches the P3-T6 grep count exactly.

---

## Exit condition for cycle 5

`blocking_count == 0` across the re-audit (`code-review`, `feature-audit`, `policy-audit`), requiring: the residual exemption count is reduced from 24 to 19 (the 5 members above de-exempted and genuinely covered — tests exercise real behavior via a headless real `ItemViewer` for R1/R3 and a bare-`Control`-hosted `Mock<IItemViewer>` for R2, not merely construction), no changed-line regression, all ten touched/new files `<= 500` lines, the four-step toolchain green in the same recorded pass, and the reduced 19-member boundary individually justified and documented for maintainer ratification alongside the prior 24. The working tree committed before the next re-audit is an orchestration-level step outside this plan's task list.
