# Policy Audit — quickfiler-keyboard-action-contract-defects (#445)

- **Artifact:** `policy-audit.2026-08-22T11-30.md`
- **Reviewer:** feature-review agent
- **Date:** 2026-08-22T11-30
- **Branch under review:** `bug/quickfiler-keyboard-action-contract-defects-445-exec`
- **Head:** `1292b4c376c3d77839b9345a6908b7efc1bee2ec` (single commit on branch)
- **Base:** `origin/epic/quickfiler-suite-determinism-foundation-integration` (epic integration branch), merge-base `c551eabab0aa0a6b1a284252811a2e1de819634e`
- **Diff command:** `git diff origin/epic/quickfiler-suite-determinism-foundation-integration...HEAD`
- **Work mode:** `full-bug` (persisted marker in `issue.md`); AC source is `spec.md` only. No `user-story.md` exists, and its absence is correct by design for this mode.

## 1. Scope Resolution

The audit scope is the full branch diff against the resolved base: 40 files (5 C# source/test files, 3 `.claude/agent-memory/**` files, 30 evidence artifacts, `plan.2026-08-21T18-09.md`, `spec.md`). PR context artifacts were absent at review start and were regenerated by this reviewer at `artifacts/pr_context.summary.txt` (manual fallback from `git diff --numstat` in the required bullet format) and `artifacts/pr_context.appendix.txt` (full base...HEAD diff).

Changed-language enumeration from the branch diff: **C# only** (`.cs` files: `KaStringAsync.cs`, `KaChar.cs`, `KaKey.cs`, `IKbdAction.cs`, `KaStringAsyncTests.cs`). TypeScript: zero changed files on the branch. Python: zero changed files on the branch. PowerShell: zero changed files on the branch (no `.ps1`/`.psm1` in the diff; verified against `git diff --name-only`).

## Rejected Scope Narrowing

None. The caller directive requested the full branch diff against the epic integration base, which is exactly the authoritative scope. No instruction attempted to limit the audit to a plan, task, phase, or file subset, and no language with changed files was excluded from coverage verification.

## 2. Policy Compliance — General Code Change Policy

| Check | Verdict | Evidence |
|---|---|---|
| Objective clarified; plan existed and was followed | **PASS** | `plan.2026-08-21T18-09.md` (82 tasks, all checked; branch diff to the plan file is exclusively checkbox flips) |
| Bugfix Workflow: failing regression test first | **PASS** | `evidence/regression-testing/red-before-fix.2026-08-22T09-38.md`: 3 of 4 new tests Failed against unmodified production code, with captured failure messages (test (b) is a behavior pin and passes by design, disclosed in the artifact) |
| Minimal, targeted fix; no opportunistic refactor | **PASS** | Production diff is confined to the four files the spec enumerates; deeper defect (non-prefix `Substring`) filed as issue #583 instead of widening scope (verified via `gh issue view 583`: OPEN, created 2026-08-22) |
| Design principles (simplicity, separation of concerns) | **PASS** | Guard clauses at method top; no new indirection; pure value objects remain I/O-free |
| Fail fast and explicitly | **PASS** | `ArgumentNullException`/`ArgumentException` thrown from an explicit guard naming `other`; no broad catch introduced |
| File size limit (500 lines) | **PASS** | Re-derived by this reviewer with `awk`: `KaStringAsync.cs` 161, `KaChar.cs` 79, `KaKey.cs` 80, `IKbdAction.cs` 16, `KaStringAsyncTests.cs` 279. All below 500. Matches `evidence/qa-gates/file-size-audit.2026-08-22T09-54.md`. `QfcCollectionController.cs` (2,349 lines) is a pre-existing cap violation, unmodified by this change, explicitly excluded from this change's scope, and is not raised as a finding against it. |
| Public API breaking change called out | **PASS** | Spec "Public-API notice" section documents deletion of `DelegateType` (2 types) and `Update` (4 types); zero in-repo read sites re-verified by this reviewer (`grep -rn DelegateType --include=*.cs` returns 0 hits outside `.claude`) |
| Supporting documents updated | **PASS** | `spec.md` check-offs and follow-up issue #583 recorded; plan fully checked |
| No new dependencies | **PASS** | No `packages.config`, `.csproj`, or NuGet change in the diff |

## 3. Policy Compliance — C# Code Change Policy

| Check | Verdict | Evidence |
|---|---|---|
| CSharpier (manifest-pinned, via `dotnet tool run`) | **PASS** | `evidence/qa-gates/csharpier-check.2026-08-22T09-53.md`: exit 0, `Checked 1517 files in 6574ms.`, 0 needing formatting (line 16 of artifact). Matches baseline 1517/0. Caller claim confirmed. |
| Analyzer build (`/t:Rebuild`, non-vacuous) | **PASS** | `evidence/qa-gates/msbuild-analyzers.2026-08-22T09-56.md`: exit 0, 0 errors, 5 warnings (= baseline; codeless third-party System.Reactive advisories), `Skipping target "CoreCompile"` count 0, `CoreCompile:` count 100. Caller claim confirmed. |
| Nullable build (`/t:Rebuild`, no `/p:Nullable=enable`) | **PASS** | `evidence/qa-gates/msbuild-nullable.2026-08-22T09-58.md`: exit 0, 0 errors, 5 warnings, skip count 0, `CoreCompile:` count 111. The five changed files carry zero `#nullable` pragmas and add none. Caller claim confirmed. |
| vstest with `/EnableCodeCoverage` and `/InIsolation` | **PASS** | `evidence/qa-gates/vstest-final.2026-08-22T10-02.md`: 6441 passed / 0 failed / 0 skipped across 9 assemblies vs baseline 6437/0/0 (`evidence/baseline/vstest-baseline.2026-08-22T09-30.md`). Delta +4 = exactly the four new tests. `\.claude\` excluded from discovery via workspace-relative paths. Caller claim confirmed. |
| MSTest / Moq / FluentAssertions | **PASS** | New tests use `[TestClass]`/`[TestMethod]` and FluentAssertions (`ThrowExactly`, `WithParameterName`, collection `Equal`); no xUnit/NUnit introduced; no mock needed for pure value objects |
| Toolchain order (format → analyze → type-check → test), single clean pass | **PASS** | `evidence/qa-gates/final-qc-pass-attestation.2026-08-22T10-42.md` plus the four stage artifacts, timestamps in order 09-53 → 09-56 → 09-58 → 10-02 |

## 4. Policy Compliance — Unit Test Policy (General + C#)

| Check | Verdict | Evidence |
|---|---|---|
| Independence / isolation / determinism | **PASS** | New tests construct fresh `KaStringAsync` instances with captured locals; no shared state, no clock, no randomness, no temporary files, no external dependency |
| No sleep/retry/timing tolerance used to stabilize tests | **PASS** | Reviewer grep of the five changed files for `Thread.Sleep`, `Task.Delay`, `retry`, `timeout`, `DateTime.Now`, `Random.Shared`: zero hits. The coverage-run deadlock (`evidence/qa-gates/coverage-postchange.2026-08-22T10-38.md`) was resolved by re-running an unchanged command after a sibling worktree's collection cleared, not by any test modification. |
| Arrange–Act–Assert + documented intent | **PASS** | All four new tests carry explicit Intent comments and AAA sections (diff in `artifacts/pr_context.appendix.txt`) |
| Scenario completeness for changed behavior | **PASS** | Matrix covered: null probe, empty probe (both instance-state variants), branch-3 gated/ungated, latch-survival across match-then-non-match; pre-existing tests retain the remaining branches |
| Tests in test project mirroring production | **PASS** | All test changes in existing `QuickFiler.Test/Controllers/KaStringAsyncTests.cs`; no colocation |
| No pre-existing test deleted or weakened | **PASS** | Test-file diff contains exactly one method rename (body unchanged) plus four additions; per-class counts `KaCharTests` 10, `KaKeyTests` 9, `KbdActionsTests` 3, `KbdActionsRemainingBranchesTests` 10 all pass in the final run |

## 5. Coverage Verification (mandatory for every language with changed files)

Per the caller directive, `artifacts/csharp/coverage.xml` was deliberately NOT written; coverage is verified numerically from the executor's committed evidence (`evidence/baseline/coverage-baseline.2026-08-22T09-34.md`, `evidence/qa-gates/coverage-postchange.2026-08-22T10-38.md`, `evidence/qa-gates/coverage-delta.2026-08-22T10-40.md`), which record same-session Cobertura captures with an identical command and settings file before and after the change.

| Language | Changed files | Coverage gate | Measured | Verdict |
|---|---|---|---|---|
| C# | 5 | Repo-wide line coverage vs 85% floor (and CLAUDE.md 80% floor) | 70.5971% before → 70.6037% after (moved upward) | **FAIL** — pre-existing shortfall measured before any edit in this change; recorded as a pre-existing, unadjudicated exception; dispositioned non-blocking for this bugfix |
| C# | 5 | Repo-wide branch coverage vs 75% floor | 58.7406% before → 58.7469% after (moved upward) | **FAIL** — pre-existing shortfall, same disposition as the line-rate row; non-blocking for this bugfix |
| C# | 5 | New-code line coverage vs >= 90% | 12/12 = 100.00% on added executable production lines (per-line hit table in `coverage-delta.2026-08-22T10-40.md`) | **PASS** |
| C# | 5 | Changed-line no-regression | `KaStringAsync.cs` 49/49 → 60/60 (100%); `KaChar.cs` 28/33 → 28/28; `KaKey.cs` 28/33 → 28/28; zero covered-to-uncovered flips in any in-scope file | **PASS** |
| TypeScript | 0 | none — zero changed TypeScript files on the branch | — | no gate triggered |
| Python | 0 | none — zero changed Python files on the branch | — | no gate triggered |
| PowerShell | 0 | none — zero changed PowerShell files on the branch | — | no gate triggered |

**Repo-wide disposition rationale.** The shortfall (70.60% line / 58.75% branch, C#) exists on the base branch, was measured before the first edit of this change, and both rates moved marginally upward under this change. The blocking gates for a bugfix of this size are the changed-line no-regression rule and the new-code floor, both of which PASS with margin. Raising the repo-wide figure is tracked repository work (see the pre-existing 80-vs-85 threshold divergence recorded in the spec's Rollout & Follow-up), not a defect of this change.

**Arithmetic-discrepancy adjudication.** The executor disclosed that `lines-valid` reconciles exactly (+11 −5 −5 = +1) while `lines-covered` moved +6 against an in-scope expectation of +11, attributing the 5-line difference to dotnet-coverage run-to-run nondeterminism under a parallel MSTest run. This reviewer assessed the disclosure as **adequate and not masking a regression**: (a) all four in-scope files are measured per-file at 100% post-change, so any covered-to-uncovered flip is necessarily in files this change does not touch; (b) the removed members had zero read sites repository-wide, so no out-of-scope execution path depends on them; (c) the magnitude (5 of 80,550 lines, 0.006 pp) is inside the previously observed run-to-run variation band for this repository's C# coverage collection; (d) the inner test run in the instrumented session was 6441/6441, identical to the uninstrumented run, so no test outcome changed. Advisory: the 5 flipped lines were not localized to specific files; localizing them would strengthen this class of disclosure (recorded as advisory finding CR-A1 in the code review).

## 6. Evidence Location Compliance

- All 30 evidence artifacts in the branch diff live under `docs/features/active/2026-08-07-quickfiler-keyboard-action-contract-defects-445/evidence/{baseline,regression-testing,qa-gates,issue-updates,other}/` — the canonical `<FEATURE>/evidence/<kind>/` location. Zero diff entries under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/evidence/`, or `artifacts/coverage/` (verified by grep over `git diff --name-only`).
- No helper script (`.ps1`, `.py`, `.sh`) exists anywhere under `evidence/` (all 30 files are `.md`; verified by `find`).
- `validate_evidence_locations.py` does not exist in this repository (searched repo-wide; `scripts/dev_tools/` does not exist — the tree has `scripts/dev-tools/` containing only `run-actionlint.ps1`). The scripted scan is unrunnable by absence; the manual diff scan above substitutes for it.
- No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` events: no instruction supplied a non-canonical evidence path.

## 7. Scope-Constraint Verification (caller-directed, each independently re-verified)

| Constraint | Verdict | Evidence |
|---|---|---|
| `QuickFiler.Test/QuickFiler.Test.csproj` unchanged | **PASS** | Not present in `git diff --name-only` output |
| Nothing under `.claude/**` except `.claude/agent-memory/**` | **PASS** | Exactly 3 changed files under `.claude/`, all under `.claude/agent-memory/atomic-executor/` (executor memory persistence, permitted; plan task P4-T3 records why the carve-out is required). Note: spec AC18's literal text says "No file under `.claude/**`"; the approved plan and the review directive both carve out `agent-memory` — see advisory finding CR-A2. |
| Nothing under `docs/features/potential/**`, `.github/workflows/**`, `config/blast-radius.json`, `config/orchestration-routing.json` | **PASS** | grep over the changed-file list: zero matches |
| `KbdActions.cs`, `KeyboardHandler.cs`, `QfcCollectionController.cs` unmodified | **PASS** | None present in the changed-file list |
| No file over 500 lines (this change) | **PASS** | Section 2 line counts; the pre-existing `QfcCollectionController.cs` violation is out of scope by direction |
| No sleep/retry/timing tolerance stabilizing tests | **PASS** | Section 4 |
| No helper scripts under `evidence/` | **PASS** | Section 6 |

## 8. Unrunnable-by-Absence Declarations

- Any skill step naming `poetry run python -m scripts.dev_tools.*` is unrunnable by absence: no `pyproject.toml`, no `poetry.lock`, no `scripts/dev_tools/` directory exists (searched at repo root and repo-wide). No validator result is fabricated for these steps.
- The MCP `collect_pr_context` tool was unavailable; PR context artifacts were hand-authored from `git diff --numstat` / `git diff` as recorded in Section 1.

## 9. Verdict Summary

- **Blocking findings: 0.**
- FAIL rows: 2 (C# repo-wide line and branch coverage vs the uniform floors) — both pre-existing, measured before any edit of this change, both moved upward under this change, dispositioned non-blocking per the changed-line/new-code gate model for bugfixes.
- Advisory findings: 4, recorded in `code-review.2026-08-22T11-30.md` (CR-A1 through CR-A4).
- All executor numeric claims (csharpier 1517/0; analyzers 0 err/5 warn/skip 0/CoreCompile 100; nullable 0 err/5 warn/skip 0/CoreCompile 111; vstest 6441 vs 6437 baseline; coverage rates and per-file figures) were checked against the cited evidence artifacts and, where cheaply re-derivable (line counts, grep counts, diff contents, issue #583 existence), re-derived by this reviewer. No claim failed verification.

**Policy audit verdict: PASS (0 blocking findings).**
