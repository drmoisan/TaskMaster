# liveoutlook-harness-construction-scoped-skip — Remediation Plan (Issue #283)

- Plan timestamp: 2026-07-08T18-45
- Feature folder: `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/`
- Remediation inputs: `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/remediation-inputs.2026-07-08T14-34.md`
- Work Mode: minor-audit
- Scope: evidence persistence + a maintainer-ratified PowerShell coverage exemption + an orchestrator-executed CI green run. NO source-behavior change to the seam (`TaskMaster.Test/AppGlobals/LiveOutlookHarnessRunner.cs`) or any shipped fix.
- Base: `main` @ `930467f4`; prior head `143da1ed`.

## Evidence Path Note

All narrative evidence artifacts resolve to `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/<kind>/`. The two machine-verifiable coverage XML outputs are written to the permitted non-evidence canonical coverage-output paths `artifacts/csharp/coverage.xml` and `artifacts/pester/powershell-coverage.xml` (these are the gate-consumed canonical paths; they are not on the forbidden `artifacts/` evidence sub-path list — `artifacts/coverage/` is forbidden, `artifacts/csharp/` and `artifacts/pester/` are not). No non-canonical evidence path was supplied by the caller; no `EVIDENCE_LOCATION_OVERRIDE_REJECTED` entry is required.

## Findings Addressed

- R2 (Blocking, C# coverage not machine-verifiable) → Phase 1.
- R3 (Blocking, PowerShell coverage below floor + not machine-verifiable) → Phase 2.
- R1 (Blocking, modified-workflow-needs-green-run) → Phase 3 documentation task only; satisfied by the orchestrator's post-commit CI green run and re-audit, not by the executor.

### Phase 0 — Policy Read and Remediation Baseline Re-Anchor

- [x] [P0-T1] Read the policy files in required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/powershell.md` — and write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/remediation-baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists with all three fields populated.
- [x] [P0-T2] Capture the remediation baseline into `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/remediation-baseline/remediation-baseline.2026-07-08T18-45.md` recording `Timestamp:`, `Command:` (`git rev-parse HEAD`), `EXIT_CODE:`, and an `Output Summary:` that states the current branch head SHA and confirms the pre-existing baseline coverage values referenced by this plan (C# overall baseline 16.75%; PowerShell scripts baseline 77.06%). Acceptance: the artifact exists with all schema fields and both baseline coverage values recorded.

### Phase 1 — R2 C# Coverage Regeneration and Evidence Reconciliation

- [x] [P1-T1] Regenerate C# coverage to the canonical path `artifacts/csharp/coverage.xml` using the repo dotnet-coverage Cobertura flow — `scripts/vscode/Invoke-MSTestWithCoverage.ps1 -CoverageOutput artifacts/csharp/coverage.xml` (or `dotnet-coverage collect --output artifacts/csharp/coverage.xml --output-format cobertura --settings coverage.config -- <vstest.console.exe> TaskMaster.Test/bin/Debug/TaskMaster.Test.dll /Settings:scripts/vscode/TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:TestCategory!=LiveOutlook`). Record command evidence to `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/csharp-coverage-regen.2026-07-08T18-45.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (test pass count, overall line-rate). Acceptance: `artifacts/csharp/coverage.xml` exists as a machine-readable Cobertura file, `EXIT_CODE: 0`, and the LiveOutlook category remains filtered out.
- [x] [P1-T2] From `artifacts/csharp/coverage.xml` extract the line coverage for `TaskMaster.Test/AppGlobals/LiveOutlookHarnessRunner.cs` and the overall root `line-rate`; confirm the seam reports >= 90% (expected 100.0%, 30/30) and overall line coverage shows no regression versus the 16.75% baseline. Update `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/csharp-test-coverage-final.md` and `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/coverage-delta.md` to cite `artifacts/csharp/coverage.xml` as the machine-verifiable source of the reported figures. Acceptance: both dossiers reference the canonical XML path and the extracted seam figure equals the value they already report (100.0%), with a >= 90% pass and overall no-regression confirmed.
- [x] [P1-T3] Reconcile the stale seam figure in `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/ac-verification-summary.md` AC4 row (line 11), changing `seam coverage 90.0%` to `seam coverage 100.0%` so it matches `coverage-delta.md` and `csharp-test-coverage-final.md`. Acceptance: `ac-verification-summary.md` states the single confirmed seam figure 100.0% and no `90.0%` seam reference remains in that file.

### Phase 2 — R3 PowerShell Coverage Persistence and Ratified Exemption

- [x] [P2-T1] Persist a canonical Pester coverage artifact at `artifacts/pester/powershell-coverage.xml` by running a direct Pester 5.x session configured for JaCoCo/Cobertura code coverage over the two changed scripts `scripts/vscode/Invoke-MSTest.ps1` and `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, executing `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`. Record command evidence to `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/powershell-coverage-regen.2026-07-08T18-45.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` stating the numeric line coverage (expected 77.06%, commands 109 / executed 84) and the 11/11 test pass count. Acceptance: `artifacts/pester/powershell-coverage.xml` exists as a machine-readable coverage report and the numeric line coverage is recorded in the evidence artifact.
- [x] [P2-T2] Author a documented, maintainer-ratified coverage exemption at `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/powershell-coverage-exemption.md`. The document MUST state: (a) the exempted files `scripts/vscode/Invoke-MSTest.ps1` and `scripts/vscode/Invoke-MSTestWithCoverage.ps1` with the specific host-bound uncovered line ranges (vswhere resolution, `Get-ChildItem` test-assembly discovery, and the `Invoke-*Exe` executable-invocation / Cobertura post-processing blocks — the ~25 top-level script-body lines that cannot be unit-tested deterministically without invoking real external executables, prohibited by the no-external-dependency test rule); (b) the rationale that the pure logic (arg builders, runsettings resolver, wrapper seams) IS tested; (c) the finding that the 77.06% figure is pre-existing and unchanged and the appended `/TestCaseFilter` token lands on already-covered return-array lines, so there is no changed-line regression; (d) that the project maintainer (dan@danmoisan.org) authorized this exemption as part of the issue #283 full-lifecycle delivery; and (e) references to the general-unit-test coverage policy (`.claude/rules/general-unit-test.md`) and the analogous CLAUDE.md COM/VSTO host-bound exemption pattern. Acceptance: the exemption doc exists with per-file host-bound line ranges, the rationale, the changed-line no-regression finding, and the explicit maintainer ratification.

### Phase 3 — R1 CI Green-Run Handoff Note and Final Verification QC

- [x] [P3-T1] Document that R1 (modified-workflow-needs-green-run) is satisfied by the orchestrator, not the executor, by writing `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/ci-green-run-handoff.2026-07-08T18-45.md`. The note MUST state that after this remediation is committed the orchestrator will push the branch, trigger `.github/workflows/ci.yml` via `workflow_dispatch` against the new branch head, confirm `conclusion=success`, and record the run URL + head SHA at `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/ci-green-run.<timestamp>.md`; and that this executor plan does not itself run CI. Acceptance: the handoff note exists and explicitly assigns the CI green run to the orchestrator post-commit step.
- [x] [P3-T2] Run the C# formatter check `dotnet tool run csharpier --check .` to confirm no formatting drift (no source `.cs` file is modified by this evidence-only remediation) and confirm both canonical coverage XML outputs `artifacts/csharp/coverage.xml` and `artifacts/pester/powershell-coverage.xml` exist and are machine-readable. Record to `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/remediation-final-qc.2026-07-08T18-45.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: csharpier check `EXIT_CODE: 0` (no drift) and both coverage XML files confirmed present and readable.
- [x] [P3-T3] Run the final consistency gate: verify that `ac-verification-summary.md`, `coverage-delta.md`, and `csharp-test-coverage-final.md` all state the single C# seam coverage figure 100.0%, that the PowerShell coverage exemption doc is present, and that both canonical coverage artifacts exist. Record the check to `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/remediation-consistency-verify.2026-07-08T18-45.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: all three C# dossiers agree on 100.0%, the exemption doc and both coverage XML artifacts exist, and no conflicting seam coverage figure remains.

plan-path: docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/remediation-plan.2026-07-08T18-45.md
