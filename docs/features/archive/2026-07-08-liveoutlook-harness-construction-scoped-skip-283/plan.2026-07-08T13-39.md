# liveoutlook-harness-construction-scoped-skip (Plan)

- **Issue:** #283
- **Issue URL:** https://github.com/drmoisan/TaskMaster/issues/283
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-08T13-39
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** minor-audit
- **Requirements source (sole):** `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/issue.md` — `## Acceptance Criteria` (AC1–AC7) is the sole minor-audit AC source. `spec.md` / `user-story.md` / `research.md` are NOT required and MUST NOT be referenced or created.

**Fail-closed evidence rule:** This plan includes explicit baseline artifact tasks, final-QC artifact tasks, and a coverage-comparison task for BOTH in-scope languages (C# and PowerShell), which each carry mandatory coverage policy. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or incomplete, the verdict MUST be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Every evidence-producing task records its exact artifact path. Do not mark an evidence-backed task complete without the artifact present on disk with all required fields.

**Evidence location invariant (non-overridable):** All evidence artifacts resolve under `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/<kind>/`. Writing to `artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/coverage/`, or `artifacts/evidence/` is a policy violation. Each command-step artifact MUST include `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Coverage-bearing test artifacts MUST record numeric coverage headline values (never placeholders).

**net481 constraint (encode in implementation):** target is .NET Framework net481 — NO `init` accessors, NO `record`, NO `record struct` (CS0518, no `IsExternalInit` polyfill). Use a plain `readonly struct` with a constructor + get-only auto-properties. Nullable reference types stay enabled with zero new nullable warnings. Framework: MSTest + FluentAssertions. Moq is available but NOT required here: the delegate parameters (`Func<T>` construct / `Action<T>` exercise) are the injected seam, so no mock is needed — state this rationale in the seam test file. No temporary files, no live Outlook, deterministic.

---

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read policy files in the `policy-compliance-order` sequence — (1) `CLAUDE.md`, (2) `.claude/rules/general-code-change.md`, (3) `.claude/rules/general-unit-test.md`, (4) language rules `.claude/rules/csharp.md` and `.claude/rules/powershell.md` (both languages are in scope), plus `.claude/rules/ci-workflows.md` (CI `pwsh` step is edited). Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and an explicit list of every file read. Acceptance: artifact exists with all three fields populated.
- [x] [P0-T2] Record the repository baseline (branch name + HEAD commit SHA + `git status` cleanliness) to `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/repo-commit-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact records the exact branch and SHA the baseline was captured at.
- [x] [P0-T3] Capture C# formatting baseline. Command: `dotnet csharpier .` (repo-local SDK at `.dotnet-sdk`; csharpier 1.2.6 for CI parity). Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/csharp-format-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (files-reformatted count / clean status). Acceptance: artifact present with all four fields.
- [x] [P0-T4] Capture C# analyzer-build baseline. Command: `msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/csharp-analyzer-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (build result, warning/error counts). Acceptance: artifact present with all four fields.
- [x] [P0-T5] Capture C# nullable/type-check baseline. Command: `msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/csharp-nullable-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (build result, nullable-warning count). Acceptance: artifact present with all four fields.
- [x] [P0-T6] Capture C# test + coverage baseline. Command: `scripts/vscode/Invoke-MSTestWithCoverage.ps1` (dotnet-coverage cobertura) OR `vstest.console.exe <TaskMaster.Test.dll> /EnableCodeCoverage /InIsolation` (vstest at the VS18 vswhere-resolved path). Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/csharp-test-coverage-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric baseline line-coverage % and pass/fail counts. Acceptance: artifact records a numeric coverage headline (not a placeholder).
- [x] [P0-T7] Capture PowerShell formatting baseline for the QC scripts/tests in scope (`scripts/vscode/Invoke-MSTest.ps1`, `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`). Command: `mcp__drm-copilot__run_poshqc_format`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/powershell-format-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact present with all four fields.
- [x] [P0-T8] Capture PowerShell analyzer baseline. Command: `mcp__drm-copilot__run_poshqc_analyze`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/powershell-analyze-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (analyzer finding counts). Acceptance: artifact present with all four fields.
- [x] [P0-T9] Capture PowerShell Pester test + coverage baseline. Command: `mcp__drm-copilot__run_poshqc_test`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/baseline/powershell-test-coverage-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric Pester coverage % and pass/fail counts. Acceptance: artifact records a numeric coverage headline (not a placeholder).

---

### Phase 1 — Constrained Small-Path Implementation

- [x] [P1-T1] Create NEW file `TaskMaster.Test/AppGlobals/LiveOutlookHarnessRunner.cs` (same assembly as the tests). Add `#nullable enable` at the top. Declare `internal static class LiveOutlookHarnessRunner` containing: (a) nested `internal readonly struct HarnessOutcome` with constructor `(Exception? captured, string? skipReason)` and get-only auto-properties `Captured` and `SkipReason`; (b) `internal static HarnessOutcome Run<T>(Func<T> construct, Action<T> exercise)` that null-guards both delegates (`ArgumentNullException`), then in the construction phase does `try { resource = construct(); } catch (COMException comEx) { return skip-outcome whose SkipReason includes $"0x{comEx.ErrorCode:X8}" and comEx.Message; } catch (Exception ex) { return captured-outcome(ex); }` (a NON-COM construction failure is a captured failure, never a skip), then in the exercise phase does `try { exercise(resource); return new HarnessOutcome(null, null); } catch (Exception ex) { return captured-outcome(ex); }`. Include accurate XML doc comments describing the two-phase (construction vs exercise) semantics. Acceptance: file compiles clean under `/p:Nullable=enable`; construction COMException path returns non-null `SkipReason` + null `Captured`; exercise exceptions return non-null `Captured` + null `SkipReason`. (AC1, AC2, AC3)
- [x] [P1-T2] Edit `TaskMaster.Test/AppGlobals/LiveOutlookHookupIntegrationTests.cs`: REMOVE the three HRESULT constants (`RegdbEClassNotReg`, `ClassENotLicensed`, `CoEServerExecFailure`, lines ~67–76) and the `IsOutlookUnavailableHResult` method (lines ~78–85). Refactor the STA thread body so the try/catch chain (lines ~100–165) is replaced by a call to `LiveOutlookHarnessRunner.Run(() => new Outlook.Application(), app => { /* existing gate construction + coordinator + STA-pumped poll loop, still setting completed / maxTickBlockMs / readinessWaitMs / hookupLatencyMs */ })`, then assign `captured = outcome.Captured; skipReason = outcome.SkipReason;`. Do NOT weaken or remove any existing assertion (`Assert.Inconclusive` on skip, `captured.Should().BeNull(...)`, `completed.Should().BeTrue(...)`, `maxTickBlockMs.Should().BeLessThanOrEqualTo(...)`) and keep the existing nullable-oblivious style of this file. Acceptance: the whitelist constants and `IsOutlookUnavailableHResult` no longer exist in the file; the thread body invokes `LiveOutlookHarnessRunner.Run`; all four original assertions remain textually intact. (AC1, AC2, AC3)
- [x] [P1-T3] Edit `TaskMaster.Test/AppGlobals/LiveOutlookHookupIntegrationTests.cs` `<remarks>` XML doc `<para>` (currently lines ~29–38) that describes the narrow-HRESULT whitelist so it accurately describes construction-scoping: skip (`Assert.Inconclusive`) on ANY construction-phase COMException regardless of HRESULT, explicitly including `0x80010100` RPC_E_SYS_CALL_FAILED; exercise-phase exceptions (including COMExceptions) still fail. Keep the CI-filter claim (it becomes accurate after P1-T6). Acceptance: the `<para>` no longer references a three-HRESULT whitelist and states construction-scoped skip semantics. (AC6)
- [x] [P1-T4] Create NEW file `TaskMaster.Test/AppGlobals/LiveOutlookHarnessRunnerTests.cs` — `[TestClass]` WITHOUT `[TestCategory("LiveOutlook")]` (standard suite), MSTest + FluentAssertions, Arrange–Act–Assert, descriptive names, clear failure messages. Include a comment stating that no Moq mock is used because the `Func<T>`/`Action<T>` delegate parameters are the injected seam. Add deterministic tests over `LiveOutlookHarnessRunner.Run<object>`: (1) construction COMException HRESULT `0x80010100` → `SkipReason` non-null containing `"80010100"`, `Captured` null; (2) construction COMException with a different arbitrary HRESULT (`unchecked((int)0x80004005)` E_FAIL) → `SkipReason` non-null, `Captured` null (proves "regardless of HRESULT"); (3) exercise-phase `InvalidOperationException` → `Captured` is that exception, `SkipReason` null; (4) exercise-phase `COMException` → `Captured` is the COMException (strict failure retained, NOT swallowed as skip), `SkipReason` null; (5) success path → `Captured` null AND `SkipReason` null AND a side-effect flag proves the exercise delegate ran; (6) null `construct` → `ArgumentNullException`; (7) null `exercise` → `ArgumentNullException`. Acceptance: all seven scenarios present and passing; no live Outlook, no temporary files. (AC1, AC2, AC4)
- [x] [P1-T5] Edit `TaskMaster.Test/TaskMaster.Test.csproj` — add two `<Compile Include>` items inside the existing AppGlobals `<ItemGroup>` (near line ~275): `<Compile Include="AppGlobals\LiveOutlookHarnessRunner.cs" />` and `<Compile Include="AppGlobals\LiveOutlookHarnessRunnerTests.cs" />` (legacy non-SDK, `packages.config` project has no glob, so explicit compile items are required). Acceptance: both new `.cs` files appear as `<Compile Include>` entries; the project builds and both files are compiled. (AC3, AC4)
- [x] [P1-T6] Edit `.github/workflows/ci.yml` line ~140 — change the vstest invocation from `& $vstestPath $testAssemblies /EnableCodeCoverage /InIsolation /Logger:trx` to `& $vstestPath $testAssemblies /EnableCodeCoverage /InIsolation /Logger:trx /TestCaseFilter:"TestCategory!=LiveOutlook"`. Do NOT introduce a deliberately-failing nested command; the existing `if ($LASTEXITCODE -ne 0) { throw ... }` already satisfies `.claude/rules/ci-workflows.md`. Acceptance: the ci.yml vstest line includes `/TestCaseFilter:"TestCategory!=LiveOutlook"` and no new negative-path exit-code leak is introduced. (AC5)
- [x] [P1-T7] Edit `scripts/vscode/Invoke-MSTest.ps1` `Get-VsTestArgumentList` (line ~54) so the returned array ends with the filter token: `return @($TestAssembly) + @("/Settings:$RunSettingsPath", '/InIsolation', '/TestCaseFilter:TestCategory!=LiveOutlook')` (single token, no embedded quotes needed for splatting). Acceptance: `Get-VsTestArgumentList` output contains `'/TestCaseFilter:TestCategory!=LiveOutlook'` as its final element. (AC5)
- [x] [P1-T8] Edit `scripts/vscode/Invoke-MSTestWithCoverage.ps1` `Get-DotnetCoverageArgumentList` (line ~76) so the inner vstest args append `'/TestCaseFilter:TestCategory!=LiveOutlook'` after `'/InIsolation'`. Acceptance: `Get-DotnetCoverageArgumentList` output contains `'/TestCaseFilter:TestCategory!=LiveOutlook'`. (AC5)
- [x] [P1-T9] Edit `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1` — update the exact-match assertion in `Describe 'Get-VsTestArgumentList (Invoke-MSTest.ps1)'` → `It 'preserves the test assemblies and /InIsolation alongside /Settings:'` (line ~49) to include the new trailing `'/TestCaseFilter:TestCategory!=LiveOutlook'` element, and add `It` assertions that BOTH `Get-VsTestArgumentList` and `Get-DotnetCoverageArgumentList` output include `'/TestCaseFilter:TestCategory!=LiveOutlook'`. Acceptance: the Pester file asserts the filter token for both arg builders and the existing exact-match expectation is updated to match the new array shape. (AC4, AC5)

---

### Phase 2 — Final QC Loop

Run the full toolchain for each language in order and restart from the first step of that language's loop if any step fails or changes files. C# order: format → analyzers → nullable → test(coverage). PowerShell order: format → analyze → Pester(coverage). All command tasks below are unconditional; `EXIT_CODE: SKIPPED` is not a valid passing outcome.

- [x] [P2-T1] C# format. Command: `dotnet csharpier .`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/csharp-format-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 and no files left unformatted in the final pass. (AC7)
- [x] [P2-T2] C# analyzers. Command: `msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/csharp-analyzer-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (0 errors expected). Acceptance: build succeeds with no analyzer errors. (AC7)
- [x] [P2-T3] C# nullable/type-check. Command: `msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/csharp-nullable-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: build succeeds with zero new nullable warnings promoted to errors. (AC7)
- [x] [P2-T4] C# test + coverage. Command: `scripts/vscode/Invoke-MSTestWithCoverage.ps1` OR `vstest.console.exe <TaskMaster.Test.dll> /EnableCodeCoverage /InIsolation /TestCaseFilter:"TestCategory!=LiveOutlook"`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/csharp-test-coverage-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change line-coverage %, the seam file's own coverage %, and pass/fail counts (the seven new seam tests must pass). Acceptance: all tests pass and the artifact records numeric coverage headline values. (AC4, AC7)
- [x] [P2-T5] PowerShell format. Command: `mcp__drm-copilot__run_poshqc_format`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/powershell-format-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: exit 0 and no files left unformatted in the final pass. (AC7)
- [x] [P2-T6] PowerShell analyze. Command: `mcp__drm-copilot__run_poshqc_analyze`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/powershell-analyze-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (finding counts). Acceptance: no new analyzer findings on the touched scripts. (AC7)
- [x] [P2-T7] PowerShell Pester test + coverage. Command: `mcp__drm-copilot__run_poshqc_test`. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/powershell-test-coverage-final.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change Pester coverage % and pass/fail counts (the updated/added RunSettings tests must pass). Acceptance: all tests pass and the artifact records a numeric coverage headline. (AC4, AC5, AC7)
- [x] [P2-T8] Coverage delta / threshold verification. Compare baseline vs post-change coverage for both languages and report new/changed-code coverage. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/coverage-delta.md` recording: C# baseline % (from P0-T6), C# post-change % (from P2-T4), the new seam file `LiveOutlookHarnessRunner.cs` coverage % (target >= 90% for new code), PowerShell baseline % (from P0-T9) and post-change % (from P2-T7), plus a no-regression-on-changed-lines statement. Acceptance: artifact shows changed-line coverage did not regress and new-code coverage meets the >= 90% target; if any required value is unavailable, mark remediation-required (never PASS). (AC4)
- [x] [P2-T9] Acceptance-criteria verification summary. Write `docs/features/active/2026-07-08-liveoutlook-harness-construction-scoped-skip-283/evidence/qa-gates/ac-verification-summary.md` mapping AC1–AC7 (from `issue.md` `## Acceptance Criteria`) to the concrete evidence artifact(s) and task(s) that satisfy each, with a PASS/FAIL verdict per AC. Acceptance: every AC (AC1–AC7) has a linked evidence artifact and an explicit verdict; any unlinked AC forces an overall INCOMPLETE verdict. (AC1–AC7)
