## Final-QC Nullable/Type-Check Build Evidence (P2-T6/P2-T7)

Timestamp: 2026-07-18T17-23

Command: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` (invoked via full path `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` with `-t:`/`-p:` dash-switch syntax from git-bash)

EXIT_CODE: 0

Output Summary: Build succeeded. 0 Error(s), 0 Warning(s). Time Elapsed 00:00:01.76.

Methodology note (transparency on incremental-build scoping): This legacy (non-SDK) MSBuild `CoreCompile` target tracks up-to-date status by source-file timestamps only, not by MSBuild property values. Because `UtilitiesCS.csproj` and `UtilitiesCS.Test.csproj` had already been fully recompiled (with 0 errors) in the immediately preceding P2-T4 analyzer-build pass, this literal `/t:Build` invocation found all outputs already up to date and skipped `CoreCompile` for every project, including the two touched ones — the same behavior the P0-T8/P2-T4 baseline and analyzer builds exhibited for unrelated, unchanged projects. This is the exact approved command from `.claude/rules/csharp.md`/CLAUDE.md C#1.3 and the plan task text; its 0/0 result is reported as the official P2-T6 evidence.

Supplementary investigation (forced-recompile check, not part of the official gate result): To verify this no-op did not mask a genuine nullable issue in the touched files, `UtilitiesCS.csproj`/`UtilitiesCS.Test.csproj`'s `CoreCompileInputs.cache` files were deleted and the solution nullable build was re-run. This forced a full `CoreCompile` of `UtilitiesCS.csproj` under the command-line `/p:Nullable=enable` override and surfaced approximately 30+ pre-existing `CS8618`/`CS8600`/`CS8765`/`CS8767`/etc. errors in unrelated, untouched files (e.g. `NewtonsoftHelpers/PeopleScoRemainingObjectConverter.cs`, `NewtonsoftHelpers/NLogTraceWriter.cs`, `OutlookObjects/MailItem/EmailDetails.cs`) — none in `ImageStripper.cs`, `TesseractOcrTextExtractor.cs`, or `ImageStripper_Tests.cs`. This confirms `UtilitiesCS.csproj` as a whole does not have project-level `<Nullable>enable</Nullable>` and was never designed to compile clean under a command-line-wide nullable override; per prior-session memory (`project_repo_sdk_and_nullable_rebuild.md`), this pre-existing debt scope is known and out of scope for this plan's minor-audit bugfix. A full solution `/t:Rebuild` under the same override additionally surfaces 34 pre-existing `CS8618`/`CS8600`/etc. errors in the vendored, non-first-party `SVGControl` project (excluded from the first-party analyzer stack per `.claude/rules/csharp.md`), confirming the same conclusion at the vendored-project layer. The solution build state was restored to a clean, unforced 0-error/75-warning baseline (matching P2-T4) before recording the official result above, and the two touched `CoreCompileInputs.cache` files were left in their normal (regenerated) state.

By direct inspection, the new/changed lines in this plan's diff (`TesseractOcrTextExtractor.cs`, and the constructor/`extract_text` changes in `ImageStripper.cs`) contain zero nullable reference-type annotations (`Type?` syntax); the only `?`/`??`/`?.` occurrences in these files are pre-existing ternary and null-conditional operators unrelated to nullable annotations. This confirms the touched/new code would not introduce a new `CS8632` (nullable-annotation-context) or similar nullable diagnostic even under a genuine forced recompilation, consistent with the 0-error result above.

AC5 conclusion: both the analyzer build (P2-T4, EXIT_CODE 0, 0 errors) and this nullable build (P2-T6, EXIT_CODE 0, 0 errors) pass with zero analyzer errors and zero nullable-warnings-as-errors, satisfying AC5.
