Timestamp: 2026-08-22T13-13
Command: pwsh -NoProfile -File "scripts/vscode/Invoke-MSTestWithCoverage.ps1" -SearchRoot . -Configuration Debug -CoverageOutput "docs/features/active/2026-08-07-quickfiler-test-form1-live-form-491/evidence/qa-gates/coverage-postchange.cobertura.xml"
EXIT_CODE: 1
Output Summary: HALT CONDITION — the script threw before reaching Koverage post-processing.

Test Run Failed. Total tests: 6438. Passed: 6437. Failed: 1 (the same guard-test failure documented throughout Phase 1/Phase 3: `NoLiveFormInTestAssemblyTests.ExecutingAssembly_ContainsNoFormDerivedType`, caused by the pre-existing, out-of-scope `QfcFormViewerDerived` type). The script's `Invoke-DotnetCoverageCollection` function (`scripts/vscode/Invoke-MSTestWithCoverage.ps1:232-236`) treats any non-zero `dotnet-coverage`/vstest exit code as fatal and throws `"MSTest with coverage failed with exit code $coverageExitCode"` at line 236, BEFORE the script reaches its Koverage post-processing step (lines 333-341) or the `Assert-CoberturaLineCoverageThreshold` check the plan's P4-T1 text anticipates. This is a different throw site than the one the delegation prompt's HALT clause names verbatim, but it is the same class of condition in substance: a non-zero exit leaves a RAW, unfiltered Cobertura file at the requested output path, which must not be used for the Phase 4 comparison.

Confirmed the file was raw/unfiltered before disposal: `Select-Xml ... -XPath "//class" | Where-Object { filename -like "*:*" }` returned 2076 (not 0), proving no Koverage rewriting occurred. Raw (disqualified) root attributes, recorded verbatim for the record only and NOT usable as a comparison figure: `lines-covered=56864`, `lines-valid=80550`, `line-rate=0.7059466170080695` (this raw, third-party-inclusive figure is far below the baseline's Koverage-filtered 85.5788% and is not comparable to it, consistent with the two-denominator hazard documented in the research artifact). The raw file at `docs/features/active/2026-08-07-quickfiler-test-form1-live-form-491/evidence/qa-gates/coverage-postchange.cobertura.xml` has been discarded (deleted) per the plan's disposal instruction.

ROOT CAUSE: this HALT is a downstream consequence of the same discovery recorded in the P1-T6, phase3-vstest, and phase3-guard-green artifacts — the guard test, as specified by this plan, permanently fails against the current `QuickFiler.Test` assembly because of the pre-existing, out-of-scope `QfcFormViewerDerived` type. Because the coverage-capture harness requires a fully green test run before it will produce a usable (Koverage-filtered) Cobertura artifact, no genuine post-change coverage figure can be captured while that guard-test failure persists. The executor does not lower any threshold, edit `coverage.config`, exclude any production file from measurement, or modify the guard's assertion to work around this. This is escalated in the plan-completion report. P4-T2 and P4-T3, which depend on a valid filtered post-change artifact, cannot be genuinely completed and are left unchecked below.
