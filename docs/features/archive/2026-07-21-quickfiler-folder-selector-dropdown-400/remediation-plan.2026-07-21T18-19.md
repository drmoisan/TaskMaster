# QuickFiler Folder Selector Drop-Down (#400) — Atomic Remediation Plan

- **Feature folder:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400`
- **Requirements source:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/remediation-inputs.2026-07-21T18-19.md`
- **Authoritative specification:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/spec.md`
- **Work mode:** `full-bug`
- **Reviewed base:** `df5ad49c909f6b739edef45d0336151f44e827a6`
- **Reviewed head:** `b38a87751669f3522928dd01ac0f4f97b82572ed`
- **Execution route:** large C# remediation through the typed C# engineer
- **Status:** second in-place revision pending execution

## Fixed execution rules

- Execute tasks in task-ID order without replanning or widening issue #400.
- Use only automated, deterministic checks. Do not use live UI, screenshots, user interaction, wall-clock sleeps, temporary files, network access, Outlook, or externally launched acceptance processes.
- Store every new baseline, regression, and QA evidence artifact only below this feature's `evidence/baseline`, `evidence/regression-testing`, or `evidence/qa-gates` directories. Name each evidence file with the command-start time in UTC formatted `yyyy-MM-ddTHH-mm`; a rerun receives a fresh suffix.
- Every command evidence file must contain `Timestamp:`, the exact expanded `Command:`, a numeric `EXIT_CODE:`, and an `Output Summary:`. Coverage summaries must include numeric values. Expected-failure evidence must identify the failing test and the pre-fix diagnostic.
- Keep every implementation batch within three production files and three test files, route it through the typed C# engineer, and complete the CSharpier → analyzer build → nullable build → MSTest sequence required by that route before accepting the batch. The only authorized Phase 2 production scope is `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, new `QuickFiler/Viewers/BreadcrumbWebViewSurfaceFactory.cs`, and one adjacent legacy `Compile` include in `QuickFiler/QuickFiler.csproj`.
- Do not add packages or settings, alter public `IItemViewer` signatures, widen coverage exclusions, modify `coverage.config`, relocate host-neutral logic into an excluded adapter, suppress a failure, or silently skip a planned command.
- Keep every modified production, test, and reusable script file at or below 500 lines. Every new C# file, if one is demonstrably required, must have exactly one legacy project `Compile` include.
- Treat the 625-line `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` draft recorded by P0-T9 as the historical pre-Phase-2 state. Preserve the completed Phase 2 result and P4 evidence requiring the host to remain 475–485 lines and `QuickFiler/Viewers/BreadcrumbWebViewSurfaceFactory.cs` to remain approximately 105 lines, with both files at or below 500 lines.
- Preserve every accurately completed P0-T1 through P4-T4 task and its evidence. The 2026-07-21T20-38 final-pass artifacts are superseded historical evidence because complete AST-to-source accounting found `CompleteOpenAsync` at 20/29 (68.9655%), `OpenCoreAsync` at 35/40 (87.5000%), `WaitForReadinessAsync` at 6/7 (85.7143%), and `NormalizeFactory`, including its returned legacy-factory lambda, at 14/17 (82.3529%). P5 remains invalidated and unchecked until the corrective test-only batch and a fresh final pass complete.
- The only authorized post-blocker implementation scope is new `QuickFiler.Test/Viewers/BreadcrumbDropDownCoverageThresholdTests.cs` plus exactly one adjacent `<Compile Include="Viewers\BreadcrumbDropDownCoverageThresholdTests.cs" />` in `QuickFiler.Test/QuickFiler.Test.csproj`. Do not change production, another test file, packages, settings, coverage configuration, exclusions, or public signatures in this corrective batch.
- Keep `BreadcrumbDropDownCoverageThresholdTests.cs` at or below 500 lines. Preserve the exact blocker-state contents of the 500-line `BreadcrumbDropDownIntegrationTests.cs` and every existing test file, including the P0-T9 assertion-hash-protected `BreadcrumbDropDownReadinessTests.cs`, `BreadcrumbDropDownLifecycleConcurrencyTests.cs`, `BreadcrumbDropDownHostTests.cs`, and `BreadcrumbDropDownLifecycleTests.cs`.
- P4-T5 is the dependency root for the corrective batch; P4-T6 through P4-T9 depend on it, P4-T10 through P4-T13 form the required focused CSharpier → analyzer → nullable → MSTest verification loop, P4-T14 depends on that loop, and P5-T1 must not begin until P4-T5 through P4-T14 are complete and evidence-consistent.
- Leave a task unchecked whenever its required evidence is absent, incomplete, contradictory, or reports a nonzero exit code, except for a task explicitly tagged `[expect-fail]` whose named regression failure is the required result.

## Second-revision acceptance mapping

| Confirmed AC-18 gap | Corrective task coverage | Final gate |
|---|---|---|
| `CompleteOpenAsync` lines 190–198: outer open-pipeline exception recovery | P4-T7 | P5-T6 requires at least 90% complete source-member coverage |
| `OpenCoreAsync` lines 220, 230, 246, 249, and 256: current-lifecycle guards and reentrant invalidation paths | P4-T7 and P4-T8; line 230 may remain uncovered only if the complete member still reaches at least 90% without a nondeterministic race test | P5-T6 requires at least 90% complete source-member coverage |
| `WaitForReadinessAsync` line 376: cancellation wins readiness | P4-T9 | P5-T6 requires at least 90% complete source-member coverage |
| `NormalizeFactory` lines 462–464, including the returned legacy-factory lambda: null surface rejection | P4-T9 | P5-T6 requires at least 90% complete source-member coverage |
| Test and project scope | P4-T5, P4-T6, and P4-T14 | AC-17, AC-18, and AC-19 are reconciled in P6; the behavioral seams also support AC-15 and AC-16 without changing their evidence requirements |

## Second-revision executor preflight

- `DIRECTIVE: PREFLIGHT VALIDATION ONLY`
- Validate this exact plan path in place; do not create or accept a sibling remediation plan.
- Confirm that completed P0-T1 through P4-T4 tasks remain checked, P5 and P6 remain unchecked, the corrective dependency chain is executable without production or existing-test edits, the new test-file/project batch remains within the C# change budget, all evidence paths are canonical, and the final C# QA loop restarts at P5-T1.
- Return exactly `PREFLIGHT: ALL CLEAR` or `PREFLIGHT: REVISIONS REQUIRED`; a revision-required result must identify a precise delta for this same file.

### Phase 0 — Policy, requirements, and remediation baseline

- [x] [P0-T1] Read, in order, `AGENTS.md` standing instructions, the cross-language code-change section in `AGENTS.md`, the cross-language unit-test section in `AGENTS.md`, and `.agents/skills/csharp/SKILL.md`; then write `phase0-instructions-read` evidence under `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/evidence/baseline/` listing the exact files/sections in `Policy Order:` and including the required evidence fields.
- [x] [P0-T2] Read the complete `remediation-inputs.2026-07-21T18-19.md`, `spec.md`, `plan.2026-07-21T10-41.md`, `policy-audit.2026-07-21T18-19.md`, `code-review.2026-07-21T18-19.md`, `feature-audit.2026-07-21T18-19.md`, `artifacts/pr_context.summary.txt`, and `artifacts/pr_context.appendix.txt`; write `remediation-requirements-map` evidence under `evidence/baseline/` that confirms `full-bug`, records all 19 acceptance criteria, maps every remediation finding to this plan's task IDs, and fails closed if `spec.md` is absent or the recorded base/head cannot be reconciled with repository history.
- [x] [P0-T3] Run `git status --short`, `git rev-parse HEAD`, `git merge-base HEAD main`, and `git diff --check`; record the four exact commands, numeric exit codes, and outputs in a `repository-state` baseline evidence file without altering or discarding pre-existing worktree changes.
- [x] [P0-T4] Run `csharpier check .` as the non-mutating formatting baseline and write a dedicated `csharpier-baseline` evidence file. Record any pre-existing formatting delta as baseline debt; do not change source during baseline capture.
- [x] [P0-T5] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write a dedicated `analyzers-baseline` evidence file with diagnostic counts and the required fields.
- [x] [P0-T6] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true` and write a dedicated `nullable-baseline` evidence file with compiler and nullable diagnostic counts and the required fields.
- [x] [P0-T7] Compute an actual UTC-minute suffix, then run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/evidence/baseline/coverage-remediation-baseline.` followed by that concrete suffix and `.cobertura.xml`; write a separate `mstest-coverage-baseline` evidence file containing the fully expanded command, test totals, and numeric repository line coverage.
- [x] [P0-T8] Parse the Phase 0 Cobertura result and the reviewed-base coverage evidence to write `coverage-accounting-baseline` evidence under `evidence/baseline/` with numeric repository-wide coverage, changed/new selector-line coverage, per-selector-type coverage, and every measurable changed/new member; enumerate each direct WebView2/WinForms adapter surface that is nonnumeric and identify its deterministic seam tests. Treat an unavailable required number as remediation required, not as zero, pass, or skipped.
- [x] [P0-T9] Reconcile the existing `scope-and-project-baseline` evidence by writing a supplemental `scope-and-project-rebaseline` artifact under `evidence/baseline/` that records the current 625-line `BreadcrumbDropDownHost.cs` worktree draft as unverified and not delivered, records `BreadcrumbWebViewSurfaceFactory.cs` and its `QuickFiler.csproj` include as absent, records the existing host include count, authorizes only the new internal static helper plus exactly one adjacent `<Compile Include="Viewers\BreadcrumbWebViewSurfaceFactory.cs" />`, sets a 475–485-line host target and an approximately 105-line helper target with both at or below 500 lines, and captures ordered assertion inventories plus SHA-256 hashes of the assertion-bearing lines in `BreadcrumbDropDownReadinessTests.cs`, `BreadcrumbDropDownLifecycleConcurrencyTests.cs`, `BreadcrumbDropDownHostTests.cs`, and `BreadcrumbDropDownLifecycleTests.cs`; stop before Phase 2 if any further production or project file is required.

### Phase 1 — Failure-first readiness and lifecycle regressions

- [x] [P1-T1] Delegate a bounded test-only batch to the typed C# engineer to add TaskCompletionSource-controlled host/lifecycle tests proving that, while the popup surface factory/readiness task is incomplete, `PopupMessengerReady`, cached render/theme/selector `PostJson` calls, show, and focus all remain at zero; after successful completion, prove exactly one attach, one replay for each message type, one show, and one focus. Use existing focused fakes and no production changes.
- [x] [P1-T2] Extend the same bounded test batch with a TaskCompletionSource-controlled readiness-failure test proving uncommitted rollback to the original selection, partial-resource disposal, zero messenger attachment, zero stale selection publication, exactly one focus return, and no duplicate callback after the failed completion is observed.
- [x] [P1-T3] [expect-fail] Run the exact new readiness tests from `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with `vstest.console.exe` before changing production, require at least one assertion failure that identifies premature readiness exposure or missing readiness handling, and write a dedicated `readiness-fail-before` artifact under `evidence/regression-testing/`. A build failure, missing test, or unrelated failure does not satisfy this task.
- [x] [P1-T4] Delegate a second bounded test-only batch to the typed C# engineer to add a TaskCompletionSource-controlled concurrent-open test proving two pending `OpenAsync` callers share one factory invocation, create at most one surface and one `ToolStripControlHost`, raise messenger-ready once, show/focus once, and finish with one open popup.
- [x] [P1-T5] Add a TaskCompletionSource-controlled reset-before-completion test proving `Reset` invalidates the pending lifecycle; late success is disposed and cannot attach, replay, show, focus, or invoke selection callbacks, and a subsequent open starts exactly one fresh initialization that can succeed.
- [x] [P1-T6] Add TaskCompletionSource-controlled dispose-before-completion tests for both late success and late failure, proving bounded cleanup, no mutation of disposed host state, no messenger-ready event, no show/focus/cancel callback, and the expected final `IsOpen`, `PopupMessenger`, and `LastInitializationException` state.
- [x] [P1-T7] Add a TaskCompletionSource-controlled stale-failure test proving a reset lifecycle's late failure cannot overwrite `LastInitializationException` after a later lifecycle initializes successfully; also prove a current-lifecycle initialization failure remains observable.
- [x] [P1-T8] [expect-fail] Run the exact concurrent, reset, dispose, stale-failure, and fresh-open tests from `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with `vstest.console.exe` before changing production, require an assertion failure tied to duplicate creation or stale lifecycle mutation, and write a dedicated `lifecycle-fail-before` artifact under `evidence/regression-testing/`. Unrelated failures do not satisfy this task.
- [x] [P1-T9] Verify the two test-only diffs use MSTest, FluentAssertions, Moq or focused fakes, deterministic `TaskCompletionSource` completion, Arrange–Act–Assert structure, and no blocking wait, sleep, temporary file, live WebView, display, user input, network, Outlook, or external process; record the audit in `failure-first-test-policy` evidence under `evidence/regression-testing/`.

### Phase 2 — Minimal popup host readiness and lifecycle fix

- [x] [P2-T1] Delegate one bounded two-production-file plus project-include batch to the typed C# engineer: revise `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, add `QuickFiler/Viewers/BreadcrumbWebViewSurfaceFactory.cs` with internal static type `BreadcrumbWebViewSurfaceFactory`, and add exactly one `<Compile Include="Viewers\BreadcrumbWebViewSurfaceFactory.cs" />` adjacent to the existing host include in `QuickFiler/QuickFiler.csproj`. Require a 475–485-line host and an approximately 105-line helper, both at or below 500 lines; preserve exactly the two pre-remediation public host constructor contracts with any readiness-specific seam kept non-public, legacy-factory normalization, one shared pending-open task, lifecycle generation/cancellation, reset/dispose invalidation, stale-success cleanup, stale-failure suppression, readiness-gated messenger publication, popup install/show/focus/close/reset behavior, the existing method-level exclusion on `ShowOwnedPopup`, and every assertion in the four P0-T9-protected test files.
- [x] [P2-T2] In `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, add monotonic lifecycle-generation and cancellation ownership so `Reset` and `Dispose` invalidate pending initialization; require every post-await continuation to verify the captured generation and disposed state before assigning fields, attaching the messenger, replaying, showing, focusing, publishing selection, or assigning `LastInitializationException`, and dispose every stale late-success surface exactly once.
- [x] [P2-T3] Implement `BreadcrumbWebViewSurfaceFactory` as the sole owner of production factory creation and argument validation, `WebView2` construction and core initialization, the navigation-readiness `Task`, pre-navigation event subscription, `NavigationStarting`/`NavigationCompleted` correlation by `NavigationId`, `Disposed` cancellation, handler detachment on every success/failure/cancellation/disposal/synchronous-exception exit, navigation-failure translation, and partial-WebView cleanup; move the existing method-level direct-adapter `[ExcludeFromCodeCoverage]` annotation from the host with that adapter method without duplicating or widening it, and return a ready surface only after the correlated `NavigateToString` document completes successfully.
- [x] [P2-T4] In `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, preserve one current-lifecycle initialization exception in `LastInitializationException`, prevent stale failures from overwriting a later success, clear the current failure only when a fresh lifecycle begins or succeeds according to the existing contract, and preserve one ready surface across close/reopen without duplicate event subscriptions or cached replay.
- [x] [P2-T5] Run all Phase 1 readiness and lifecycle tests with `vstest.console.exe QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` and exact `/Tests:` filters, require them all to pass, and write `host-readiness-lifecycle-pass` evidence under `evidence/regression-testing/` with per-test outcomes and required fields.
- [x] [P2-T6] Add or update one deterministic closed-surface lifecycle test that exercises the same ready-surface boundary used by the closed state and proves it cannot attach or replay before readiness and does not duplicate subscriptions after reopen; make no additional production change unless the test exposes the same bounded host defect.
- [x] [P2-T7] Run the closed-surface test and the existing popup close/reopen lifecycle tests with an exact `vstest.console.exe` filter and write `closed-surface-regression` evidence under `evidence/regression-testing/`; any confirmed readiness failure must be fixed within the authorized `BreadcrumbDropDownHost.cs`/`BreadcrumbWebViewSurfaceFactory.cs` boundary before this task can pass.
- [x] [P2-T8] Review the production, project, and protected-test diff and record `host-fix-scope` evidence under `evidence/qa-gates/` proving the implementation is limited to the two authorized production files plus exactly one adjacent helper `Compile` include, the host is 475–485 lines, the helper is approximately 105 lines, both are at or below 500 lines, the direct-adapter method-level exclusion moved to the helper exactly once, `ShowOwnedPopup` retained its existing method-level exclusion, and the P0-T9 ordered assertion inventories and assertion-line SHA-256 hashes for all four protected test files are unchanged; also prove no changes were made to `ItemViewer.Breadcrumb.cs`, `BreadcrumbMessengerHub.cs`, `FolderBreadcrumb.html`, public `IItemViewer` signatures, packages, settings, or coverage configuration, and fail closed for a plan revision before any broader architectural change.

### Phase 3 — Semantic composition regressions

- [x] [P3-T1] Delegate a bounded test-only batch to the typed C# engineer to add a composed native outside/automatic-close test in `QuickFiler.Test/Viewers/BreadcrumbDropDownIntegrationTests.cs`: open a populated selector, move pending selection away from the original, trigger the native close path, and prove restoration of the original committed identity, zero `SelectionChanged` publication for the pending row, and exactly one focus return.
- [x] [P3-T2] In the same bounded batch, add an inbound-coordinator `selectorKey` Up test in `QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs`: start from a populated model, route Up through the coordinator, and prove pending-only movement, separator skipping, first-selectable clamping, no committed identity change, and one transition with no duplicate publication.
- [x] [P3-T3] Run the two new semantic composition tests with exact `vstest.console.exe` filters and write `semantic-composition` evidence under `evidence/regression-testing/` containing the required fields and per-assertion outcome summary.
- [x] [P3-T4] Run the existing focused tests for Left, Right, Enter, Escape, mouse activation, probability updates, issue #398 atomicity, invalid messages, native close, and selector navigation; write `preserved-selector-contracts` evidence under `evidence/regression-testing/` with the exact test filter and counts. Do not cite HTML source-token assertions as runtime readiness or DOM-focus proof.
- [x] [P3-T5] Audit all modified test files for independence, determinism, Arrange–Act–Assert clarity, external-boundary fakes, and the 500-line limit; split only along existing focused test-class boundaries if needed, then verify each new file has exactly one project `Compile` include and record the result in `semantic-test-policy` evidence under `evidence/qa-gates/`.

### Phase 4 — AC-18 scope reconciliation and coverage accounting

- [x] [P4-T1] Update only AC-18 in `spec.md` to preserve repository-wide line coverage of at least 80%, changed-line no-regression, and at least 90% coverage for every measurable new/changed selector type and member, while explicitly classifying only direct WebView2/WinForms adapter calls and unavoidable readiness coordination as bounded nonnumeric surfaces verified through deterministic seams. Do not lower a numeric threshold, add a waiver, widen an exclusion, or alter another acceptance criterion's meaning.
- [x] [P4-T2] Compare the complete implementation diff with `coverage.config`, project coverage attributes, and the reviewed-base exclusion set; write `coverage-exclusion-integrity` evidence under `evidence/qa-gates/` proving filters and class-level exclusions are unchanged, the existing direct-WebView2 method-level exclusion moved from `BreadcrumbDropDownHost.cs` to the corresponding adapter method in `BreadcrumbWebViewSurfaceFactory.cs` exactly once, and the existing `ShowOwnedPopup` method-level exclusion remains unchanged in the host.
- [x] [P4-T3] Inspect the excluded helper adapter method and `ShowOwnedPopup` for host-neutral state, selection, replay, popup lifecycle, or stale-completion logic; write `nonnumeric-adapter-accounting` evidence under `evidence/qa-gates/` that limits the helper exclusion to direct WebView2 construction/core initialization plus unavoidable correlated navigation-readiness coordination and cleanup, maps each nonnumeric branch to the existing deterministic factory-seam tests, and fails closed for a plan revision if excluded code contains other host-neutral behavior.
- [x] [P4-T4] Verify numeric line counts for every modified production and test file, require `BreadcrumbDropDownHost.cs` to be 475–485 lines and both it and the approximately 105-line `BreadcrumbWebViewSurfaceFactory.cs` to be at most 500 lines, and verify `QuickFiler/QuickFiler.csproj` contains exactly one helper `Compile` include immediately adjacent to the existing host include; write `file-size-and-project-includes` evidence under `evidence/qa-gates/` and fail on any duplicate, omission, non-adjacent include, or protected-test assertion change.
- [x] [P4-T5] Before any corrective edit, write `coverage-threshold-test-baseline` evidence under `evidence/regression-testing/` that cites `final-pass-superseded.2026-07-21T20-44.md`, records the four 20:38 AST-to-source member results and exact uncovered lines, confirms `BreadcrumbDropDownCoverageThresholdTests.cs` and its project include are absent, records `BreadcrumbDropDownIntegrationTests.cs` at exactly 500 lines, captures whole-file SHA-256 hashes for every existing `BreadcrumbDropDown*Tests.cs` file and `BreadcrumbSelectorCoordinatorTests.cs`, captures the current `QuickFiler.Test.csproj` breadcrumb include inventory, and captures whole-file SHA-256 hashes for both changed production files. Treat any blocker-state mismatch as a stop requiring another plan revision.
- [x] [P4-T6] Delegate one bounded zero-production/one-test-file/one-project-file batch to the typed C# engineer: add only `QuickFiler.Test/Viewers/BreadcrumbDropDownCoverageThresholdTests.cs` and exactly one `<Compile Include="Viewers\BreadcrumbDropDownCoverageThresholdTests.cs" />` adjacent to the existing breadcrumb drop-down test includes in `QuickFiler.Test/QuickFiler.Test.csproj`. Use MSTest and FluentAssertions with deterministic controls, delegates, `TaskCompletionSource` seams, and Arrange–Act–Assert structure; keep the new file at or below 500 lines and do not edit any existing test or production file.
- [x] [P4-T7] In the new test file, add a deterministic `CompleteOpenAsync` recovery test in which an initialization failure reaches rollback, the first rollback callback throws exactly once, the outer pipeline catches that exception, a second rollback completes, and the returned open task resolves `false` with the current exception and bounded cancel/focus counts; add a deterministic `PopupMessengerReady` test whose handler calls `Reset`, proving the ready-event lifecycle invalidation returns `false`, disposes the installed surface once, and performs no show or pending-focus action. These tests must exercise source lines 190–198 and line 220 without a sleep, timeout race, reflection, or production change.
- [x] [P4-T8] In the new test file, add deterministic reentrant `OpenCoreAsync` guard tests in which the injected show callback resets the host, the injected pending-focus callback resets the host, and the show callback resets then throws; prove each open resolves `false`, stale work does not show/focus/publish afterward, resources and callbacks remain bounded, and the current lifecycle is not overwritten. These tests must exercise lines 246, 249, and 256; do not add a nondeterministic concurrency test solely to force line 230.
- [x] [P4-T9] In the new test file, add a deterministic readiness-cancellation test that starts an open with a created surface and incomplete readiness task, calls `Reset`, then proves cancellation wins readiness, the open resolves `false`, the surface is rejected/disposed once, and no messenger-ready/show/focus action occurs; add a public legacy-constructor test whose legacy factory returns no surface and prove `OpenAsync` resolves `false`, `LastInitializationException` reports the no-surface contract, rollback/focus counts are bounded, and no popup surface is installed. These tests must exercise line 376 and lines 462–464, including the returned lambda.
- [x] [P4-T10] Run `csharpier format QuickFiler.Test/Viewers/BreadcrumbDropDownCoverageThresholdTests.cs` and write a dedicated `coverage-threshold-csharpier` artifact under `evidence/qa-gates/`. If formatting changes the new file, retain only formatter output and restart the focused verification loop at this task with a fresh UTC-minute evidence set; any change outside the new file is a failure.
- [x] [P4-T11] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write a dedicated `coverage-threshold-analyzers` artifact under `evidence/qa-gates/`. On failure or any file change, remediate only within the new test file or its one include and restart at P4-T10; any required production or existing-test edit requires another plan revision.
- [x] [P4-T12] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true` and write a dedicated `coverage-threshold-nullable` artifact under `evidence/qa-gates/`. On failure or any file change, remediate only within the authorized corrective scope and restart at P4-T10.
- [x] [P4-T13] Resolve `vstest.console.exe` through the repository-standard Visual Studio discovery path and run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbDropDownCoverageThresholdTests"`; require every test in the new class to be discovered and pass, and write `coverage-threshold-focused-pass` evidence under `evidence/regression-testing/` with the expanded command, test names/counts, numeric exit code, and output summary. On failure, fix only the new test file or its include and restart the P4-T10 verification loop.
- [x] [P4-T14] Write `coverage-threshold-scope-integrity` evidence under `evidence/qa-gates/` proving the corrective diff contains exactly the new at-most-500-line test file and one adjacent project include; all P4-T5 production and existing-test whole-file hashes remain unchanged; the P0-T9 protected assertion inventories/hashes remain unchanged; `BreadcrumbDropDownIntegrationTests.cs` remains exactly 500 lines; every new test maps to the source seam and AC row above; and no package, setting, coverage filter/configuration, exclusion, public signature, production file, or other test file changed. Leave this task unchecked and request a same-file plan revision on any mismatch.

### Phase 5 — Ordered final C# QA and numeric coverage

- [x] [P5-T1] After P4-T5 through P4-T14 pass, start a fresh final-pass evidence set that does not reuse the superseded 20:38 artifacts and run `csharpier format .`; write a dedicated `final-csharpier` artifact under `evidence/qa-gates/`. If formatting changes any file or the command fails, apply only formatter output within the authorized corrective scope, leave this task and all later Phase 5 tasks unchecked, and restart Phase 5 from this command with fresh UTC-minute evidence until one run exits zero and changes no file; any required edit outside the authorized corrective scope requires another plan revision.
- [x] [P5-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; write a dedicated `final-analyzers` artifact under `evidence/qa-gates/`. On failure or any file change, remediate without suppression, invalidate the current Phase 5 evidence set, and restart at P5-T1.
- [x] [P5-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write a dedicated `final-nullable` artifact under `evidence/qa-gates/`. On failure or any file change, remediate the root cause, invalidate the current Phase 5 evidence set, and restart at P5-T1.
- [x] [P5-T4] Compute an actual UTC-minute suffix and run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/evidence/qa-gates/coverage-final.` followed by that concrete suffix and `.cobertura.xml`; write a dedicated `final-mstest-coverage` artifact under `evidence/qa-gates/` with test totals and numeric repository line coverage. On failure or file change, remediate without a skip or suppression, invalidate the current Phase 5 evidence set, and restart at P5-T1.
- [x] [P5-T5] Confirm P5-T1 through P5-T4 are one uninterrupted clean format → analyzer → nullable → coverage sequence, all four command artifacts have matching run identity and complete required fields, and no stale artifact is cited; write `final-pass-integrity` evidence under `evidence/qa-gates/`.
- [x] [P5-T6] Recompute coverage against base `df5ad49c909f6b739edef45d0336151f44e827a6` using complete AST-to-source accounting rather than Cobertura `<methods>` summaries alone, and write `coverage-delta` evidence under `evidence/qa-gates/` containing numeric baseline and post-change repository coverage, changed/new-line coverage, per-type coverage, and per-measurable-member coverage for the host and every measurable member of `BreadcrumbWebViewSurfaceFactory`. Explicitly report post-change covered/valid counts and percentages for `CompleteOpenAsync`, `OpenCoreAsync`, `WaitForReadinessAsync`, and `NormalizeFactory` including its returned lambda against the 20:38 values; require repository coverage at least 80%, no changed-line regression, and every measurable new/changed selector type/member at least 90%, while identifying the moved direct-adapter method separately as nonnumeric rather than silently omitting it.
- [x] [P5-T7] In the same coverage accounting cycle, write a separate `acceptance-verification` evidence file under `evidence/qa-gates/` that enumerates the helper's moved method-level direct-WebView2 exclusion and the host's unchanged `ShowOwnedPopup` exclusion, explains why numeric execution is unavailable under the unit-test policy, maps every excluded readiness/cleanup branch to the protected readiness and lifecycle factory-seam tests, confirms the helper has no class-level exclusion or excluded host-neutral lifecycle logic, and confirms no threshold, filter, or exclusion was weakened; missing numeric or nonnumeric accounting is a failure.
- [x] [P5-T8] Run `git diff --check` and a scoped diff review for issue #400; write `final-diff-integrity` evidence under `evidence/qa-gates/` proving no whitespace error; exactly the authorized host, helper, and adjacent `QuickFiler.csproj` include comprise the Phase 2 production/project diff; exactly `BreadcrumbDropDownCoverageThresholdTests.cs` and its one adjacent `QuickFiler.Test.csproj` include comprise the post-blocker corrective diff; both production files satisfy their P4-T4 size bounds; the new test file is at most 500 lines; the helper exclusion moved exactly once while `ShowOwnedPopup` remained unchanged; all P4-T5 production/existing-test hashes and the four protected-test P0-T9 assertion inventories/hashes remain unchanged; `BreadcrumbDropDownIntegrationTests.cs` remains exactly 500 lines; and no unrelated production/test/configuration edit, policy edit, package change, public-signature change, temporary file, or evidence outside the canonical feature evidence folders is present.

### Phase 6 — Acceptance reconciliation, artifact validation, and independent re-review

- [x] [P6-T1] Reevaluate all 19 acceptance criteria in `spec.md` against the final clean-pass and coverage evidence; write an AC-by-AC result table with direct evidence paths in the final `acceptance-verification` artifact, and treat missing or contradictory evidence as FAIL.
- [x] [P6-T2] Update only the acceptance-criteria checkboxes in `spec.md`: check an item only when P6-T1 records PASS, and specifically leave AC-6, AC-7, AC-12, AC-13, AC-14, AC-15, AC-16, AC-18, and AC-19 unchecked until each corresponding audit finding is fully resolved and evidenced. Record the resulting 19-item checkbox state in `spec-checkbox-reconciliation` evidence under `evidence/qa-gates/`.
- [x] [P6-T3] Run the repository orchestration artifact validator for the executed plan and all required full-bug feature artifacts, store the exact validator invocations and numeric results in `artifact-validation` evidence under `evidence/qa-gates/`, and leave this task unchecked on any validator warning, schema error, missing artifact, or checklist/evidence contradiction.
- [x] [P6-T4] Delegate an independent full feature re-review through the repository feature-review route using the current base, current head, `spec.md`, this executed plan, the remediation inputs, and all current evidence; require new timestamped policy, code, and feature audit outputs and prohibit reuse of the 2026-07-21T18-19 audit conclusions as current proof.
- [ ] [P6-T5] Require the independent feature audit to return `PASS` with all 19 criteria supported by current evidence; if it returns remediation required or identifies any policy/code/coverage defect, preserve its findings, leave this task unchecked, and start a new remediation cycle rather than marking this plan complete.
- [ ] [P6-T6] Re-run the repository orchestration artifact validator after the independent PASS review, record the successful exact invocations in the existing `artifact-validation` evidence, and mark the remediation complete only when validators succeed, every plan task is evidence-consistent, all 19 spec criteria are checked, the final QA pass remains current for HEAD, and `git status --short` contains no unexplained change.
