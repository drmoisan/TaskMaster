# Folder-tree dispatcher thread affinity — execution plan

- **Issue:** #420
- **Work Mode:** full-bug
- **Requirements source:** `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/spec.md`
- **Research input:** `artifacts/research/2026-08-04T19-02-folder-tree-dispatcher-thread-affinity-research.md`
- **Evidence root:** `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/`

## Constraints and completion conditions

- Preserve `IOutlookFolderTreeService.GetSnapshotAsync` and all snapshot/cache contracts.
- Execute all live Outlook store, folder, and notification-subscription work on the captured Outlook STA dispatcher. `WpfDispatcherYield` remains strict; do not introduce a worker-local dispatcher or `Task.Yield` fallback.
- Use deterministic MSTest, Moq, and FluentAssertions tests only. Do not use Outlook, network access, temporary files, sleeps, timers, retries, or runtime polling.
- Maintain files below 500 lines. Split a file into focused units before adding a sixth hundredth line.
- Do not mark any evidence-backed item complete without its timestamped artifact. Every command artifact must include `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- The final coverage gate requires repository line coverage >=80%, >=90% coverage for new methods/classes/modules, and no coverage reduction in changed lines. Missing numeric coverage evidence is remediation-required, not PASS.

### Phase 0 — Policy and baseline capture

- [x] [P0-T1] Read `.agents/skills/policy-compliance-order/SKILL.md`, then read `AGENTS.md` in the required order (standing instructions, cross-language code-change policy, cross-language unit-test policy, and C# policy), followed by `.agents/skills/csharp/SKILL.md`, `.agents/skills/atomic-plan-contract/SKILL.md`, `.agents/skills/acceptance-criteria-tracking/SKILL.md`, and `.agents/skills/evidence-and-timestamp-conventions/SKILL.md`; record the ordered policy list and constraints in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/phase0-instructions-read.<timestamp>.md`.
- [x] [P0-T2] Re-read `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/issue.md`, `spec.md`, and `artifacts/research/2026-08-04T19-02-folder-tree-dispatcher-thread-affinity-research.md`; record the acceptance-criteria-to-test-surface mapping in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/requirements-mapping.<timestamp>.md`.
- [x] [P0-T3] Run `dotnet tool run csharpier .` before implementation; record its result and any formatter-induced diff in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/csharpier.<timestamp>.md`. If it changes files, commit or otherwise preserve the intended formatter result, then restart Phase 0 from P0-T3.
- [x] [P0-T4] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; record analyzer findings in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/analyzers.<timestamp>.md`.
- [x] [P0-T5] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; record compiler and nullable diagnostics in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/nullable.<timestamp>.md`.
- [x] [P0-T6] Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/coverage-baseline.cobertura.xml`; record MSTest totals and numeric Cobertura line/branch coverage headlines in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/baseline/mstest-coverage.<timestamp>.md`.

### Phase 1 — Regression tests before repair

- [x] [P1-T1] [expect-fail] In `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceConcurrencyTests.cs`, add a deterministic worker-originated cold-build regression using a dedicated in-process STA WPF dispatcher, recording every fake store/folder adapter access and the post-forced-yield continuation thread IDs; its expected result on the unmodified code is that the live build either runs outside the captured STA or reaches the dispatcher exception. Persist the run output in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/worker-cold-build-fail-before.<timestamp>.md`.
- [x] [P1-T2] [expect-fail] In `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs`, use an overriding `LoadFolderTreeService` test composition delegate and a dedicated in-process STA WPF dispatcher to prove first service composition runs on the captured STA when first accessed from a worker. Treat that delegate as the test boundary for the production composition body that creates `OutlookFolderNotificationSink`; persist the failing pre-fix result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/composition-affinity-fail-before.<timestamp>.md`.
- [x] [P1-T3] [expect-fail] In `UtilitiesCS.Test/OutlookObjects/Folder/FolderTreeSnapshotBuilderYieldTests.cs` and `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderHierarchyReaderTests.cs`, add forced-yield tests that record the dispatcher thread before and after the yield and assert the next fake adapter access stays on that dispatcher; persist the failing pre-fix result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/post-yield-affinity-fail-before.<timestamp>.md`.
- [x] [P1-T4] [expect-fail] Create `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` and add its explicit compile entry to `UtilitiesCS.Test/UtilitiesCS.Test.csproj`. Use reflection so the test compiles before the new initialization API exists: require the internal awaited initialization/factory member, arrange a controllable incomplete snapshot task, and verify that the viewer is not wired before completion and is wired only after the returned task completes. The expected pre-fix failure is the absent asynchronous member or the synchronous initialization path; do not use a timer, sleep, retry, Outlook, network, or temporary file. Persist the run output in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/filter-initialization-fail-before.<timestamp>.md`.
- [x] [P1-T5] [expect-fail] Create `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs` and add its explicit compile entry to `TaskMaster.Test/TaskMaster.Test.csproj`. Use reflection so the test compiles before the new `TryLoadFolderFilterAsync` seam exists; require that seam to return an incomplete task until its controlled filter-initialization task completes, proving that the ribbon path can await initialization without a synchronous dispatcher wait. Persist the run output in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/ribbon-filter-initialization-fail-before.<timestamp>.md`.
- [x] [P1-T6] No exception dossier is required because P1-T1 through P1-T5 each produced an executable deterministic pre-fix failure.

### Phase 2 — STA-owned composition and service dispatch

- [x] [P2-T1] Modify `TaskMaster/AppGlobals/AppOlObjects.cs` so first `FolderTreeService` construction is serialized and the complete live composition root (`NamespaceMAPI` access, `OutlookFolderHierarchyReader`, `FolderTreeSnapshotBuilder`, `OutlookFolderNotificationSink`, and service) is created on the captured `UiThread.Dispatcher`; preserve one session-scoped instance and propagate dispatch failure rather than falling back to a worker.
- [x] [P2-T2] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderTreeService.cs` to accept a narrow injected UI-dispatch execution seam and execute every cold build and notification-triggered refresh through it, while retaining in-flight coalescing, request coverage, stale/current state transitions, cancellation propagation, immutable publication, and disposal behavior.
- [x] [P2-T3] Update `TaskMaster.Test/AppGlobals/AppOlObjectsFolderTreeServiceTests.cs` and `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceConcurrencyTests.cs` so the Phase 1 worker-originated composition/build tests pass and assert only one composition and one cold build for concurrent callers.
- [x] [P2-T4] Update `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderTreeServiceInvalidationTests.cs`, `OutlookFolderTreeServiceStateTests.cs`, and `OutlookFolderTreeServiceDisposalTests.cs` to retain invalidation/coalescing, cancellation/error-state, refresh, subscription, and disposal behavior through the dispatch seam.

### Phase 3 — Preserve dispatcher continuation during live traversal

- [x] [P3-T1] Modify `UtilitiesCS/OutlookObjects/Folder/FolderTreeSnapshotBuilder.cs` only at awaits that precede or follow live traversal so the captured STA dispatcher is retained through deadline-controlled `WpfDispatcherYield`; retain context-free continuations only after immutable snapshot work is complete.
- [x] [P3-T2] Modify `UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyReader.cs` only at awaits that could move later live COM adapter work off the captured dispatcher; ensure store/folder traversal and post-yield access remain on the same STA dispatcher.
- [x] [P3-T3] Update `UtilitiesCS.Test/OutlookObjects/Folder/FolderTreeSnapshotBuilderYieldTests.cs` and `OutlookFolderHierarchyReaderTests.cs` so their forced-yield affinity tests pass on the dedicated STA dispatcher without Outlook.
- [x] [P3-T4] Update `UtilitiesCS.Test/OutlookObjects/Folder/WpfDispatcherYieldTests.cs` to assert the production adapter remains strict when invoked without a dispatcher and is not changed to select a `Task.Yield` fallback.

### Phase 4 — Nonblocking FilterOlFolders cold initialization

- [x] [P4-T1] Modify `UtilitiesCS/EmailIntelligence/OlFolderTools/FilterOlFolders/FilterOlFoldersController.cs` to expose the internal awaited initialization/factory path required by P1-T4. It must acquire the snapshot before wiring the viewer and remove the synchronous `GetAwaiter().GetResult()` cold-load wait while preserving disposal and event-subscription behavior.
- [x] [P4-T2] Modify `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs` to expose an internal testable `TryLoadFolderFilterAsync` seam that awaits the controller initialization, and modify `TaskMaster/Ribbon/RibbonViewer.cs` so `LoadFolderFilter_Click` awaits that task from its existing async event-handler boundary. Preserve the existing UI invocation behavior and do not introduce `async void` outside that event-handler boundary.
- [x] [P4-T3] Update `UtilitiesCS.Test/EmailIntelligence/FilterOlFoldersControllerInitializationTests.cs` and `TaskMaster.Test/Ribbon/TryFunctionalityInConstructionTests.cs` so the Phase 1 cold-load regressions pass, verify post-snapshot viewer wiring, and prove no synchronous UI wait is used.

### Phase 5 — Targeted regression and acceptance-criteria evidence

- [x] [P5-T1] Run the changed `UtilitiesCS.Test` and `TaskMaster.Test` regression classes in their existing test assemblies, including all Phase 1 cases; record passing outputs in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/targeted-regressions-pass.<timestamp>.md`.
- [x] [P5-T2] Compare the initial and passing regression results against every acceptance criterion in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/spec.md`; record test names, files, and the outcome for each criterion in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/regression-testing/acceptance-criteria-mapping.<timestamp>.md`.
- [x] [P5-T3] Update `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/spec.md` with the final internal dispatch design, test evidence paths, and any scope deviation; if no deviation is approved, state that none occurred.

### Phase 6 — Final C# quality gate

- [x] [P6-T1] Run `dotnet tool run csharpier .` and store the clean final-pass result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/csharpier.<timestamp>.md`; if it changes files or fails, correct the result and restart Phase 6 from P6-T1.
- [x] [P6-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and store the clean final-pass analyzer result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/analyzers.<timestamp>.md`; if it fails, correct the findings and restart Phase 6 from P6-T1.
- [x] [P6-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true` and store the clean final-pass compiler/nullable result in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/nullable.<timestamp>.md`; if it fails, correct the diagnostics and restart Phase 6 from P6-T1.
- [x] [P6-T4] Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/coverage-final.cobertura.xml`; store MSTest totals plus numeric line/branch coverage headlines in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/mstest-coverage.<timestamp>.md`; if it fails, correct the issue and restart Phase 6 from P6-T1.
- [x] [P6-T5] Compare the Phase 0 and Phase 6 evidence; record analyzer, compiler/nullable, MSTest, per-touched-file, changed-line, repository-line, and new-code coverage deltas in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/qa-gates/coverage-and-quality-delta.<timestamp>.md`. Require zero new diagnostics/failing tests, >=80% repository line coverage, >=90% new-unit coverage, and no changed-line coverage regression.

### Phase 7 — Review handoff readiness

- [x] [P7-T1] Verify the exact changed production and test file list, all files remain below 500 lines, and canonical evidence artifacts exist; record the inventory in `docs/features/active/2026-08-04-folder-tree-dispatcher-thread-affinity-420/evidence/other/final-inventory.<timestamp>.md`.
- [x] [P7-T2] Hand off the completed implementation, test evidence, coverage comparison, and updated specification to the required feature-review workflow; do not claim completion unless the review, orchestration validator, and required CI gates pass.
