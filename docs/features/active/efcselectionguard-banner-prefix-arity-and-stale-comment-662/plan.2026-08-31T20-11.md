# efcselectionguard-banner-prefix-arity-and-stale-comment (Plan)

- **Issue:** #662
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-31T20-11
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** minor-audit (bug)
- **Plan shape:** minimal-audit, three phases (Phase 0 baseline capture, Phase 1 constrained small-path implementation, Phase 2 final QC loop)

## Requirements Source

The sole requirements source for this work is `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/issue.md`, and within it only the explicit `## Acceptance Criteria` section. That section carries ten criteria: AC1, AC2, AC3, AC4, AC5, AC5b, AC6, AC7, AC8, AC9. `spec.md` and `user-story.md` do not exist for this feature and are not required by `minor-audit` mode. If either file appears in the feature folder, execution fails closed and stops.

Supporting (non-requirements) input: `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/research/2026-08-31T20-30-banner-prefix-arity-research.md`.

## Working Directory and Base Commit

Every command in this plan runs from the worktree root `C:\Users\DanMoisan\repos\TaskMaster\.claude\worktrees\agent-affe8d0920bd5c3f9` on branch `bug/efcselectionguard-banner-prefix-arity-and-stale-comment-662`. The diff anchor for AC5b and AC7 is the base commit `2b85134b42872e405602e6064e02dc9cda6c319b`.

## Fail-Closed Evidence Rules

**Fail-closed evidence rule:** Every Phase 0 baseline command step and every Phase 2 final-QC command step writes its own evidence artifact. If any required baseline artifact, final-QC artifact, or coverage-comparison artifact is missing or has an incomplete field set, the outcome is BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Each evidence-producing task below names its artifact path explicitly. Do not mark a task complete without the artifact on disk carrying every required field.

**Evidence location:** All evidence artifacts are written under `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/<kind>/`. Kinds used by this plan: `baseline/`, `regression-testing/`, `qa-gates/`, `issue-updates/`, `other/`. No `artifacts/`-rooted evidence path is permitted. `.csharpierignore:4` excludes `**/evidence/**`, so evidence artifacts do not participate in the CSharpier format check.

**Artifact field set:** Every command-step artifact carries `Timestamp:` (ISO-8601 `yyyy-MM-ddTHH-mm`), `Command:` (the exact command run), `EXIT_CODE:` (the observed integer), and `Output Summary:`. An artifact whose task expects a non-zero exit additionally carries `ExpectedExitCode:` with that integer.

**Checklist state rule:** A Phase 0 task checkbox stays unchecked while its artifact is absent or its fields incomplete.

**Commit cadence:** Commit after each phase and after each long-running command step, not only at the end. Work sitting uncommitted is lost if the run is interrupted. The final task of Phase 2 is the last commit and must run after every other artifact is on disk.

---

## The Directional Constraint (read before writing any code)

The issue's Expected Behavior reads as though the three-character and four-character banner prefixes should be unified upward to four characters. **That reading is wrong and would be a behavioural regression. This plan prohibits it.**

The mechanism, verified against the current tree in this authoring pass:

- `QuickFiler/Controllers/EfcSelectionGuard.cs:15` declares `private const string BannerPrefix = "===";` — three characters.
- `QuickFiler/Controllers/EfcFormController.cs:1146` classifies banner rows through `UtilitiesCS.OutlookObjects.Folder.BreadcrumbRowBuilder.BannerPrefix`, which is `"===="` (`UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs:19`). `IsBannerRow("===")` is therefore false.
- `EfcSelectionGuard.IsValidCreationSelection` (`EfcSelectionGuard.cs:66-77`) tests `value.Length >= MinimumCreationLength` where `MinimumCreationLength = 3` (`:22`). For the input `"==="` that comparison is `3 >= 3`, which is true, so the length rule rejects nothing.
- The three-character prefix at `:49` and `:75` is therefore the **only** mechanism rejecting a three-equals row at either EFC classification site.

Widening the guard constant to the producers' four-character value makes `IsValidFilingSelection("===")` and `IsSelectableFolder("===")` both return true and fails `QuickFiler.Test/Controllers/EfcFormControllerTests.cs:463` (`creationPath.Should().BeFalse(...)`). The sibling assertion on `:462`, which reads like the consistency guard, still passes under that edit, so the relaxation would not be caught by the assertion a reader would expect to catch it.

**Mandated direction:** the guard's constant keeps the value `"==="` exactly. Only its name and its XML documentation change. Any edit that alters that value, or that widens either `StartsWith` comparison, is out of scope and must be reverted.

## Scope Boundary

In scope — exactly four files:

| File | Change |
|---|---|
| `QuickFiler/Controllers/EfcSelectionGuard.cs` | rename the constant at `:15` to `BannerRejectionPrefix` keeping the value `"==="`; update the two `StartsWith` call sites at `:49` and `:75`; rewrite the constant's XML doc |
| `QuickFiler/Controllers/EfcFormController.cs` | replace the `SelectedFolder` comment at `:318-320`; comment only, no code change |
| `UtilitiesCS/OutlookObjects/Folder/FolderSuggestionTree.cs` | delete the `BannerPrefix` declaration at `:16`; qualify the single reader at `:197` |
| `QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs` | add one `[TestMethod]` |

Out of scope, and asserted unmodified:

- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs` (AC5b — feature #498 asserts it is unmodified; this work only adds a reader to its existing public constant).
- `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` (AC7).
- Everything under `docs/features/active/efc-controller-surface-defects-464/` — a closed-feature audit record; read-only.
- No `.csproj` edit anywhere. `QuickFiler.Test/QuickFiler.Test.csproj:63` already carries `<Compile Include="Controllers\EfcSelectionGuardTests.cs" />`, and no new file is created by this plan.
- `artifacts/orchestration/orchestrator-state.json` carries a local skip-worktree flag. Do not run `git update-index` for any reason.

## Literals This Plan Creates

The following literals do not exist in any `.cs` file in the current tree. They are quoted here verbatim, outside every command span, so that the acceptance conditions asserting them are read as instructions to the executor rather than as searches that cannot match:

- BannerRejectionPrefix
- private const string BannerRejectionPrefix = "===";
- StartsWith(BannerRejectionPrefix
- BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates
- public void BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates()
- must not be widened
- three-character rejection

## Decisions Record

- **D1 — Deletion, not aliasing, in `FolderSuggestionTree.cs`.** An aliasing declaration of the form `private const string BannerPrefix = BreadcrumbRowBuilder.BannerPrefix;` would still match AC2's declaration regex and would make AC2's expected count two instead of one. The declaration at `:16` is therefore deleted outright.
- **D2 — Relative namespace qualification, no new `using` directive.** `FolderSuggestionTree` is declared in `namespace UtilitiesCS` (`FolderSuggestionTree.cs:5`) and `BreadcrumbRowBuilder` in `namespace UtilitiesCS.OutlookObjects.Folder` (`BreadcrumbRowBuilder.cs:5`), so the reader at `:197` uses `OutlookObjects.Folder.BreadcrumbRowBuilder.BannerPrefix`. This touches one statement, adds no `using` line, and raises no ambiguity question against the two `using` directives the file already carries (`:2`, `:3`).
- **D3 — No comment in `FolderSuggestionTree.cs` may contain the token `BannerPrefix`.** AC5 asserts that file carries exactly one line matching that identifier. An explanatory comment naming the constant would make the count two and fail AC5. The rationale for the change is recorded in this plan and in the commit message instead.
- **D4 — AC6 and AC7 pass counts are read from the run's TRX `<Counters .../>` element, not from a console-line search.** `vstest.console.exe` pads its summary columns with variable whitespace, so a fixed-string search for a console figure is whitespace-sensitive and would not reliably match. The TRX `<Counters .../>` element is emitted on a single line with `passed="N"` and `failed="N"` attributes. The figures asserted are the same figures the acceptance criteria name.
- **D5 — Every occurrence assertion is scoped with the pathspec `-- '*.cs'` and anchored on the closing quote and semicolon.** The three-character literal is a prefix of the four-character literal, so an unanchored search for the shorter one also matches every longer declaration. The unscoped file count is scope-dependent and grows as this feature's own documents are authored; no unscoped figure is asserted anywhere in this plan.
- **D6 — Every count is derived twice by structurally different queries and the member sets compared,** not only the totals. A fixed-string search and a bounded-repetition regex fail differently, so agreement between them is evidence; agreement between two runs of the same query is not.
- **D7 — CSharpier print width is 100 (repository default; no `.csharpierrc` exists).** The two renamed call-site lines measure 92 characters after the rename and stay on one line, so the AC2 call-site search remains a single-line match. CSharpier does not reflow the contents of comments or string literals, so the XML-doc token and the FluentAssertions `because` token are likewise wrap-safe.
- **D8 — The mutating format pass runs only in Phase 2, after the Phase 0 read-only baseline.** A baseline captured after a formatter has repaired pre-existing drift records a repaired tree rather than the tree the work started from.
- **D9 — Long-running steps.** Each `msbuild /t:Rebuild` over the full solution and each full-suite test or coverage run takes many minutes. Launch them with a generous timeout and capture the transcript; do not abandon a run and record it as failed on a timeout alone.
- **D10 — `/InIsolation` is mandatory on every `vstest.console.exe` invocation.** Omitting it causes each assembly's `app.config` binding redirects to be ignored, producing a large number of phantom failures with empty messages and sub-millisecond durations. That is an assembly-load failure, not a regression.

## Toolchain

Commands, in this order, exactly as CLAUDE.md specifies. Do not add `/p:Nullable=enable` and do not substitute `/t:Build`; both properties are load-bearing and CLAUDE.md records why.

1. `dotnet tool run csharpier format .`, verified with `dotnet tool run csharpier check .`
2. `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`
3. `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`
4. `vstest.console.exe <assembly paths> /EnableCodeCoverage /InIsolation`

Neither `msbuild` nor `vstest.console.exe` nor `vswhere` is on `PATH` in this environment. Resolve them from the worktree root as follows, and reuse `$vswhere`, `$msbuild`, and `$vstest` in every later task of the same session:

```powershell
$vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'
$msbuild = & $vswhere -latest -requires Microsoft.Component.MSBuild -find 'MSBuild\**\Bin\MSBuild.exe' | Select-Object -First 1
$vstest  = & $vswhere -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe' | Select-Object -First 1
```

Built test-assembly paths under `Debug` / `Any CPU`: `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` and `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`.

`/Tests:` and `/TestCaseFilter:` cannot be combined in a single `vstest.console.exe` invocation. The scoped AC6 and AC7 runs use `/Tests:`; the full-assembly runs use `/TestCaseFilter:`. Exactly one `[TestCategory("LiveOutlook")]` attribute exists in the repository, at `TaskMaster.Test/AppGlobals/LiveOutlookHookupIntegrationTests.cs:72`; neither `QuickFiler.Test` nor `UtilitiesCS.Test` carries one, so the exclusion filter is a no-op for the two owned assemblies and is applied only to keep the invocation identical to repository convention.

This worktree contains no nested `.claude/worktrees/` directory, so recursive `*.Test.dll` discovery from the worktree root cannot pick up a sibling worktree's assemblies. The full-assembly runs name their assembly paths explicitly and use no discovery at all.

---

### Phase 0 — Baseline Capture

- [ ] [P0-T1] Read, in the order given, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, `.claude/rules/plan-acceptance-gates.md`, and the requirements source `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/issue.md`, then write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/phase0-instructions-read.md`. Acceptance: the artifact exists and carries a `Timestamp:` field, a `Policy Order:` field naming the reading order applied, and an explicit list of every file read, one path per line.

- [ ] [P0-T2] Confirm the diff anchor resolves in this worktree by running `git rev-parse --verify 2b85134b42872e405602e6064e02dc9cda6c319b^{commit}` and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/base-commit-resolution.md`. Acceptance: `EXIT_CODE: 0` and the artifact's `Output Summary:` records the printed 40-character object name, which must equal the anchor. If this fails, stop and report; every AC5b and AC7 gate in this plan depends on that anchor.

- [ ] [P0-T3] Provision the repo-local .NET SDK by running `pwsh -NoProfile -File .\scripts\vscode\Install-RepoDotNetSdk.ps1` from the worktree root, then write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/sdk-bootstrap.md`. Acceptance: `dotnet --version` run from the worktree root prints `8.0.205` and `dotnet --list-sdks` includes a path ending `.dotnet-sdk\sdk`; both outputs are transcribed into `Output Summary:`. Context: `global.json` pins `sdk.version` `8.0.205` with `rollForward` `latestFeature` and `paths` `[".dotnet-sdk", "$host$"]`, and this fresh worktree has no `.dotnet-sdk` directory, so without this step every `dotnet` invocation prints the `global.json` error message instead of running. The directory is git-ignored by `.gitignore:350`, so this step does not dirty the tree.

- [ ] [P0-T4] Restore the manifest-pinned CSharpier by running `dotnet tool restore` from the worktree root and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/dotnet-tool-restore.md`. Acceptance: `EXIT_CODE: 0` and `dotnet tool run csharpier --version` prints `1.2.6`, transcribed into `Output Summary:`. Context: repo-root `dotnet-tools.json` pins csharpier `1.2.6`, whose CLI requires a subcommand, so `format` and `check` are the only two invocation forms this plan uses.

- [ ] [P0-T5] Restore NuGet packages by running `pwsh -NoProfile -File .\scripts\vscode\Invoke-Restore.ps1` from the worktree root and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/nuget-restore.md`. Acceptance: `EXIT_CODE: 0` and the directory `packages\Meziantou.Analyzer.3.0.194` exists after the run, recorded in `Output Summary:`. Context: this fresh worktree has no `packages/` directory; every first-party project carries unconditional `<Analyzer Include>` items under `..\packages\`, and a missing analyzer path is compiler error CS0006, not a warning. `Invoke-Restore.ps1` resolves MSBuild through `vswhere` and runs `/t:Restore /p:RestorePackagesConfig=true /m`, which is the `packages.config`-aware form; `nuget.exe` is not present in this tree and is not on `PATH`. The `packages/` tree is git-ignored by `.gitignore:191`.

- [ ] [P0-T6] Capture the read-only CSharpier baseline by running `dotnet tool run csharpier check .` from the worktree root and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/csharpier-check-baseline.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and transcribes the command's final summary line verbatim into `Output Summary:`, together with the full list of any files the check reports as unformatted. This is the read-only subcommand and rewrites nothing; the mutating `format` pass does not run until Phase 2, so this baseline describes the tree as the work found it.

- [ ] [P0-T7] Capture the CSharpier state of the two files this work must not modify by running `dotnet tool run csharpier check UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs` and `dotnet tool run csharpier check QuickFiler.Test/Controllers/EfcFormControllerTests.cs`, and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/protected-files-format-state.md`. Acceptance: the artifact records both commands and both observed exit codes. Rationale: the Phase 2 format pass is repository-wide. If either protected file already carries formatting drift, that pass will rewrite it and the AC5b and AC7 zero-diff gates become unsatisfiable. Recording the state here makes that condition visible before any file is edited rather than after.

- [ ] [P0-T8] Capture the analyzer-gate baseline by running `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/msbuild-analyzers-baseline.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and transcribes the MSBuild summary block verbatim into `Output Summary:`, including the build-result line and the warning and error counts.

- [ ] [P0-T9] Capture the nullable/type-check baseline by running `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true` and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/msbuild-nullable-baseline.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and transcribes the MSBuild summary block verbatim into `Output Summary:`. Do not add `/p:Nullable=enable`; nullable enforcement in this repository is per-file opt-in through the `#nullable enable` directive, which `EfcSelectionGuard.cs:1` and `FolderSuggestionTree.cs:1` both carry.

- [ ] [P0-T10] Confirm both owned test assemblies were produced by the Phase 0 builds and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/test-assemblies-present.md`. Acceptance: `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` and `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll` both exist, and the artifact records each path with its file size and last-write time. Neither assembly is present in this worktree before Phase 0 runs, so a test invocation attempted before this point has nothing to run.

- [ ] [P0-T11] Capture the full `QuickFiler.Test` baseline by running the command below and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/vstest-quickfiler-baseline.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and transcribes into `Output Summary:` the single `<Counters ... />` line read from the produced TRX with `Select-String`, giving the `total`, `executed`, `passed`, and `failed` attribute values for this assembly. Those four values are the AC8 baseline figures for `QuickFiler.Test`.

```powershell
& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /EnableCodeCoverage /InIsolation "/TestCaseFilter:TestCategory!=LiveOutlook" "/Logger:trx;LogFileName=quickfiler-baseline.trx" /ResultsDirectory:docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/p0-t11
```

- [ ] [P0-T12] Capture the full `UtilitiesCS.Test` baseline by running the command below and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/vstest-utilitiescs-baseline.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and transcribes into `Output Summary:` the single `<Counters ... />` line read from the produced TRX, giving the `total`, `executed`, `passed`, and `failed` attribute values for this assembly. Those four values are the AC8 baseline figures for `UtilitiesCS.Test`.

```powershell
& $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /EnableCodeCoverage /InIsolation "/TestCaseFilter:TestCategory!=LiveOutlook" "/Logger:trx;LogFileName=utilitiescs-baseline.trx" /ResultsDirectory:docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/p0-t12
```

- [ ] [P0-T13] Capture the numeric coverage baseline. First ensure the global coverage tool is present by running `dotnet tool install --global dotnet-coverage` when `Get-Command dotnet-coverage` finds nothing; `dotnet tool restore` does not supply it because it is a global tool and is not listed in `dotnet-tools.json`. Then run `pwsh -NoProfile -File .\scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug`, copy the produced `coverage\coverage.cobertura.xml` to `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/coverage-baseline.cobertura.xml`, and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/coverage-baseline.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and its `Output Summary:` carries four numeric values read from the copied Cobertura file — the root `line-rate`, `lines-covered`, and `lines-valid` attributes, and the line percentage derived as `lines-covered / lines-valid * 100` to two decimal places — plus the `line-rate` attribute of the `<class>` node whose `filename` ends with `EfcSelectionGuard.cs` and of the node whose `filename` ends with `FolderSuggestionTree.cs`. If either `<class>` node is absent from the baseline Cobertura, record that file's figure as `NOT APPLICABLE` and name the `coverage.config` module exclusion that removes it; `coverage.config` excludes only Deedle, FSharp, Castle.Core, FluentAssertions, Moq, Microsoft.Testing, and MSTest modules, so neither owned assembly is expected to be absent. The script's own repository-wide 80% assertion throws before it rewrites the Cobertura file; the numeric values above are readable from the file either way, and this task's acceptance is the presence of those numbers, not the script's threshold verdict. The `coverage/` directory is git-ignored by `.gitignore:144`; the copy under `evidence/baseline/` is the durable artifact.

- [ ] [P0-T14] Derive the pre-change count of the three-character literal declaration twice and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/count-three-equals-prechange.md`. Primary query: `git grep -n -F -- '= "===";' -- '*.cs'`. Cross-check query, structurally different because it constrains the run length from both sides rather than matching fixed text: `git grep -nE -- '"={3}";' -- '*.cs'`. Acceptance: both queries return exactly one line, both member sets consist of the single entry `QuickFiler/Controllers/EfcSelectionGuard.cs:15`, and the artifact records both commands, both exit codes, both full member sets, and an explicit statement that the two member sets are identical element for element. The pattern is anchored on the closing quote and semicolon precisely because the three-character literal is a prefix of the four-character one and an unanchored search would cross-match.

- [ ] [P0-T15] Derive the pre-change count of the four-character literal declaration twice and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/count-four-equals-prechange.md`. Primary query: `git grep -n -F -- '= "====";' -- '*.cs'`. Cross-check query: `git grep -nE -- '"={4}";' -- '*.cs'`. Acceptance: both queries return exactly two lines, both member sets consist of `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs:19` and `UtilitiesCS/OutlookObjects/Folder/FolderSuggestionTree.cs:16`, and the artifact records both commands, both exit codes, both member sets, and an explicit statement that the sets are identical. Do not run either query without the `-- '*.cs'` pathspec: unscoped, the same text also appears in closed-feature audit records under `docs/features/active/efc-controller-surface-defects-464/` and in this feature's own documents, so the unscoped figure is scope-dependent and is asserted nowhere in this plan.

- [ ] [P0-T16] Derive the pre-change banner-prefix declaration inventory twice and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/count-declarations-prechange.md`. Primary query: `git grep -nE -- 'const +string +[A-Za-z_]*BannerPrefix' -- '*.cs'`. Cross-check query, deliberately broadened to a wider name family so that a differently named banner constant would surface rather than being missed by a narrow pattern: `git grep -nE -- 'const +string +[A-Za-z_]*[Bb]anner[A-Za-z_]*' -- '*.cs'`. Acceptance: the primary query returns exactly three lines whose member set is `QuickFiler/Controllers/EfcSelectionGuard.cs:15`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs:19`, and `UtilitiesCS/OutlookObjects/Folder/FolderSuggestionTree.cs:16`; the cross-check returns a superset; and the artifact records the cross-check's full member list partitioned into the three members whose declared name ends in `BannerPrefix` and the remaining non-members, each non-member named with its path, line, and declared identifier.

- [ ] [P0-T17] Establish the pre-change absence of any three-equals-only literal in the test file that will receive the new test, by running `git grep -c -F -- '("===")' -- QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs`, which reports no count line at all when the file contains no match, and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/baseline/absence-of-three-equals-test-prechange.md`. Acceptance: the command returns no output, the artifact records `EXIT_CODE: 1` together with `ExpectedExitCode: 1`, and `Output Summary:` records that the file's only equals-run literals are the three occurrences of the four-character banner sentinel at `:43`, `:183`, and `:245`, none of which is a bare three-equals value. This is the absence proof the Phase 1 fail-before exception dossier cites.

- [ ] [P0-T18] Commit the Phase 0 evidence. Run `git add docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662` followed by a commit whose message names issue 662 and the phase, then run `git status --porcelain -- docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662`. Acceptance: the status command returns no output. The `git add` span is what makes newly created artifact files visible to the status comparison; the pathspec confines both commands to this feature folder so that no unrelated queued change is swept into this branch.

---

### Phase 1 — Constrained Small-Path Implementation

- [ ] [P1-T1] Hand the constrained implementation to the small-path C# implementation engineer and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/other/small-path-handoff.md`. Acceptance: the artifact carries a `Timestamp:` and states, in full, the four in-scope files, the two files asserted unmodified, the prohibition on any `.csproj` edit, the prohibition on creating any new file, and the directional constraint reproduced from this plan's "The Directional Constraint" section including the sentence that the guard's constant keeps the value `"==="` exactly.

- [ ] [P1-T2] In `QuickFiler/Controllers/EfcSelectionGuard.cs`, replace the declaration at `:15` so that the constant is named BannerRejectionPrefix and its value is unchanged. Acceptance: `git grep -n -F -- 'private const string BannerRejectionPrefix = "===";' -- QuickFiler/Controllers/EfcSelectionGuard.cs` returns exactly one line, and `git grep -n -F -- '= "===";' -- QuickFiler/Controllers/EfcSelectionGuard.cs` also returns exactly one line, which is the same line. The second command is the value-preservation control: if the value were widened, it would return zero.

- [ ] [P1-T3] In the same file, update both `StartsWith` call sites — `:49` inside `IsValidFilingSelection` and `:75` inside `IsValidCreationSelection` — to read the renamed constant, leaving the `System.StringComparison.Ordinal` argument and the surrounding expression structure unchanged. Acceptance: `git grep -n -F -- 'StartsWith(BannerRejectionPrefix' -- QuickFiler/Controllers/EfcSelectionGuard.cs` returns exactly two lines. A count of one proves one call site was missed, and a count of zero proves neither was updated, so the single figure discriminates every partial outcome.

- [ ] [P1-T4] Rewrite the XML documentation comment attached to that constant so that it states three things: that the constant is deliberately a proper prefix of `BreadcrumbRowBuilder.BannerPrefix`; that it therefore rejects a strict superset of the producers' banner rows; and that it must not be widened to the producers' four-character value, naming the AC6 test as the guard against that edit. Acceptance: `git grep -n -F -- 'BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates' -- QuickFiler/Controllers/EfcSelectionGuard.cs` returns exactly one line, and the full replacement doc text is transcribed into `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/other/ac3-doc-text.md` so a reviewer can confirm all three statements without re-reading the source. CSharpier does not reflow comment contents, so the test identifier remains a single-line match after the Phase 2 format pass.

- [ ] [P1-T5] In `QuickFiler/Controllers/EfcFormController.cs`, replace the three-line `SelectedFolder` comment at `:318-320` with text describing the composition the code implements: `IsBannerRow` matching the producers' four-character prefix, combined with the guard's deliberately broader three-character rejection. Change the comment only; the `get => _router?.SelectedFolderPath;` accessor at `:321` and everything outside `:318-320` stay byte-identical. The replacement must contain the token `three-character rejection` on a single comment line. Acceptance: both of the following hold, and together they are one pass/fail outcome — `git grep -n -F -- 'IsValidSelection keeps its "====" rejection' -- QuickFiler/Controllers/EfcFormController.cs` returns no output, and `git grep -c -F -- 'three-character rejection' -- QuickFiler/Controllers/EfcFormController.cs` reports a count of 1. Record both commands, both exit codes, and the full replacement comment text in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/other/ac4-comment-replacement.md`, with `ExpectedExitCode: 1` recorded for the first command, which exits 1 on success because it is asserting an absence.

- [ ] [P1-T6] In `UtilitiesCS/OutlookObjects/Folder/FolderSuggestionTree.cs`, delete the declaration at `:16` outright and change the single reader inside `IsBanner` (`:195-198`) to reference `OutlookObjects.Folder.BreadcrumbRowBuilder.BannerPrefix`. Add no `using` directive, add no replacement constant, and add no comment containing the token `BannerPrefix` anywhere in the file. Acceptance: `git grep -n 'BannerPrefix' -- UtilitiesCS/OutlookObjects/Folder/FolderSuggestionTree.cs` returns exactly one line, and that line is inside `IsBanner` and contains `BreadcrumbRowBuilder.BannerPrefix`. Deletion rather than aliasing is required by Decisions Record D1: an aliasing declaration would still match AC2's declaration regex and would make AC2's count two instead of one.

- [ ] [P1-T7] Add exactly one new `[TestMethod]` to `QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs`, placed at the end of the existing `Folder-creation predicate` region before its `#endregion` at `:294`, named BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates. Create no new file and make no `.csproj` edit; `QuickFiler.Test/QuickFiler.Test.csproj:63` already includes this file. The method declares one local `const string because` whose value states that widening the guard to the producers' four-character prefix is the prohibited direction and contains the token `must not be widened`, then makes exactly four FluentAssertions assertions, each passing that local as the `because` argument: `IsValidFilingSelection("===")` is false, `IsValidCreationSelection("===")` is false, `IsValidFilingSelection("====")` is false, and `IsValidCreationSelection("====")` is false. Use MSTest attributes and FluentAssertions only; no Moq, no clock, no randomness, no async, no temporary file. Acceptance: `git grep -n -F -- 'public void BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates()' -- QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs` returns exactly one line. The name does not collide with any of the 25 test methods already in that file.

- [ ] [P1-T8] Verify the new test's literal shape so the AC6 assertions cannot drift to an array-driven form that no longer pins both arities explicitly. Acceptance: `git grep -c -F -- '("===")' -- QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs` reports a count of 2 and `git grep -c -F -- '("====")' -- QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs` reports a count of 2. The closing quote in each pattern is what prevents the three-character pattern from matching the four-character call, so the two counts are independent. Record both commands and both results in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/other/ac6-test-shape.md`.

- [ ] [P1-T9] Verify the new test's explanatory message names the prohibited direction. Acceptance: `git grep -c -F -- 'must not be widened' -- QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs` reports a count of 1. The single figure follows from the shape mandated in P1-T7: one local `because` constant shared by all four assertions. CSharpier does not reflow string-literal contents, so this token stays on one line through the Phase 2 format pass.

- [ ] [P1-T10] Write the fail-before exception dossier at `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/regression-testing/fail-before-exception.<TS>.md`, where `<TS>` is the ISO-8601 execution timestamp. Acceptance: the dossier carries `Timestamp:`, a `WhyFailingRunImpossible:` field of one to three sentences stating that the change is behaviour-preserving at every call site — no predicate's return value changes for any input — so the new test passes both before and after the production edit and no failing run exists to record; and an alternative proof section containing two items: the absence-of-test proof recorded by P0-T17, cited by its artifact path, showing that before this change no test in `QuickFiler.Test/Controllers/EfcSelectionGuardTests.cs` asserted a bare three-equals row against either guard predicate; and the trace showing that the test does fail under the prohibited widening edit, citing `QuickFiler.Test/Controllers/EfcFormControllerTests.cs:462-463` and naming `:463` as the assertion that fails while `:462` still passes.

- [ ] [P1-T11] Commit the Phase 1 source changes and evidence. Run `git add QuickFiler UtilitiesCS QuickFiler.Test docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662` followed by a commit whose message names issue 662 and the phase, then run `git status --porcelain -- QuickFiler UtilitiesCS QuickFiler.Test docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662`. Acceptance: the status command returns no output.

---

### Phase 2 — Final QC Loop

Run steps P2-T1 through P2-T4 in the stated order. If any of the four fails, or if the format step rewrites a file, fix the cause and restart the loop from P2-T1. The tasks below are unconditional: each states a command that must be executed and recorded, and `SKIPPED` is not a valid outcome for any of them. On a restart, write the artifacts again with a fresh `Timestamp:`; the recorded values from the final clean pass are the ones AC9 is judged against.

- [ ] [P2-T1] Run `dotnet tool run csharpier format .` from the worktree root and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/csharpier-format.md`. Acceptance: the artifact records `EXIT_CODE:`, transcribes the command's final summary line verbatim into `Output Summary:`, and records a tree observation that distinguishes a rewriting run from a non-rewriting one — the output of `git status --porcelain -- QuickFiler UtilitiesCS QuickFiler.Test` captured immediately before the command and again immediately after, with the set difference of the two listings stated explicitly. The exit code alone cannot make that distinction, because this subcommand rewrites files in place and still exits 0 after rewriting. If the set difference names any file outside the four in-scope files, stop and report before proceeding.

- [ ] [P2-T2] Run `dotnet tool run csharpier check .` from the worktree root and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/csharpier-check.md`. Acceptance: `EXIT_CODE: 0`, and the artifact transcribes the final summary line verbatim into `Output Summary:`. A non-zero exit means the tree still carries formatting drift; fix it and restart the loop from P2-T1.

- [ ] [P2-T3] Run `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/msbuild-analyzers.md`. Acceptance: `EXIT_CODE: 0`, and the artifact transcribes the MSBuild summary block verbatim into `Output Summary:`, including the build-result line and the warning and error counts. Use `/t:Rebuild`; a warm `/t:Build` skips `CoreCompile` and exits 0 without running any analyzer.

- [ ] [P2-T4] Run `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true` and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/msbuild-nullable.md`. Acceptance: `EXIT_CODE: 0`, and the artifact transcribes the MSBuild summary block verbatim into `Output Summary:`. Do not add `/p:Nullable=enable`.

- [ ] [P2-T5] Run the scoped AC6 test and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/regression-testing/ac6-scoped-run.md`. Acceptance: the TRX produced under the results directory carries a `<Counters ... />` element whose `passed` attribute is `1` and whose `failed` attribute is `0`, and the artifact records `EXIT_CODE: 0`, the full `<Counters ... />` line, and the console summary line transcribed verbatim.

```powershell
& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /Tests:BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates "/Logger:trx;LogFileName=ac6-scoped.trx" /ResultsDirectory:docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/regression-testing/p2-t5
```

- [ ] [P2-T6] Run the scoped AC7 test and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/regression-testing/ac7-scoped-run.md`. Acceptance: the TRX produced under the results directory carries a `<Counters ... />` element whose `passed` attribute is `1` and whose `failed` attribute is `0`, and the artifact records `EXIT_CODE: 0`, the full `<Counters ... />` line, and the console summary line transcribed verbatim.

```powershell
& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /Tests:IsSelectableFolder_AndIsBannerRow_ClassifyThreeAndFourEqualsRowsIdentically "/Logger:trx;LogFileName=ac7-scoped.trx" /ResultsDirectory:docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/regression-testing/p2-t6
```

- [ ] [P2-T7] Run the full `QuickFiler.Test` assembly and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/vstest-quickfiler-postchange.md`. Acceptance: the artifact records `EXIT_CODE:` and transcribes the produced TRX's `<Counters ... />` line, giving `total`, `executed`, `passed`, and `failed` for this assembly.

```powershell
& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /EnableCodeCoverage /InIsolation "/TestCaseFilter:TestCategory!=LiveOutlook" "/Logger:trx;LogFileName=quickfiler-postchange.trx" /ResultsDirectory:docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/p2-t7
```

- [ ] [P2-T8] Run the full `UtilitiesCS.Test` assembly and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/vstest-utilitiescs-postchange.md`. Acceptance: the artifact records `EXIT_CODE:` and transcribes the produced TRX's `<Counters ... />` line, giving `total`, `executed`, `passed`, and `failed` for this assembly.

```powershell
& $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /EnableCodeCoverage /InIsolation "/TestCaseFilter:TestCategory!=LiveOutlook" "/Logger:trx;LogFileName=utilitiescs-postchange.trx" /ResultsDirectory:docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/p2-t8
```

- [ ] [P2-T9] Capture post-change numeric coverage by running `pwsh -NoProfile -File .\scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug`, copy the produced `coverage\coverage.cobertura.xml` to `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/coverage-postchange.cobertura.xml`, and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/coverage-postchange.md`. Acceptance: the artifact records the observed `EXIT_CODE:` and its `Output Summary:` carries the same five numeric values P0-T13 recorded, read from the copied post-change Cobertura file — the root `line-rate`, `lines-covered`, and `lines-valid`, the derived line percentage to two decimal places, and the `line-rate` of the `<class>` node for `EfcSelectionGuard.cs` and for `FolderSuggestionTree.cs`. Copy the file to the evidence path before any later run can overwrite `coverage\coverage.cobertura.xml` in place.

- [ ] [P2-T10] Compare the two coverage captures and write `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/coverage-delta.md`. Acceptance: the artifact states the baseline line percentage from P0-T13, the post-change line percentage from P2-T9, and their signed difference in percentage points; states the baseline and post-change `line-rate` for `EfcSelectionGuard.cs` and for `FolderSuggestionTree.cs`; and records the changed-code figure, which for this change is that no new production member is added, so there is no new-code denominator, and the only changed executable statements are the two `StartsWith` return expressions in `EfcSelectionGuard.cs` and the `IsBanner` return expression in `FolderSuggestionTree.cs`. The gate passes when the post-change repository line percentage is not lower than the baseline percentage minus 0.50 percentage points, and the post-change `line-rate` for each of the two named classes is not lower than its baseline value. The expected delta is 0.00 on every figure, because the change deletes no executable production line and adds no production member; the 0.50 percentage-point allowance covers the per-class aggregation noise this repository's full-suite coverage runs exhibit. If a class node is recorded as `NOT APPLICABLE` in the baseline, record it identically here and state that a 0/0 denominator yields no comparable figure.

- [ ] [P2-T11] Verify AC1 and check it off. Run `git grep -n -F -- '= "===";' -- '*.cs'` and confirm it returns exactly one line, located in `QuickFiler/Controllers/EfcSelectionGuard.cs`; cross-check with `git grep -nE -- '"={3}";' -- '*.cs'` and confirm the same single member. Acceptance: both queries return the same single-member set, the result is recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac1-verification.md`, and the line `- [ ] AC1 —` in `issue.md` becomes `- [x] AC1 —` with the criterion text unchanged.

- [ ] [P2-T12] Verify AC2 and check it off. Run `git grep -nE -- 'const +string +[A-Za-z_]*BannerPrefix' -- '*.cs'` and confirm it returns exactly one line, located in `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs`; run `git grep -n -F -- 'StartsWith(BannerRejectionPrefix' -- QuickFiler/Controllers/EfcSelectionGuard.cs` and confirm it returns exactly two lines. The declaration count falls from three to one because the renamed guard constant no longer contains the substring the regex requires. Acceptance: both figures hold, the results are recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac2-verification.md`, and the line `- [ ] AC2 —` in `issue.md` becomes `- [x] AC2 —` with the criterion text unchanged.

- [ ] [P2-T13] Verify AC3 and check it off. Run `git grep -n -F -- 'BannerRejectionPrefix_RejectsThreeAndFourEqualsRowsOnBothPredicates' -- QuickFiler/Controllers/EfcSelectionGuard.cs` and confirm exactly one line, then read the constant's XML doc and confirm it states all three required facts. Acceptance: the command returns exactly one line, the doc text transcribed by P1-T4 is quoted in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac3-verification.md` alongside a three-item checklist marking each required statement present, and the line `- [ ] AC3 —` in `issue.md` becomes `- [x] AC3 —` with the criterion text unchanged.

- [ ] [P2-T14] Verify AC4 and check it off. Run `git grep -n -F -- 'IsValidSelection keeps its "====" rejection' -- QuickFiler/Controllers/EfcFormController.cs` and confirm it returns no output, and run `git grep -c -F -- 'three-character rejection' -- QuickFiler/Controllers/EfcFormController.cs` and confirm it reports a count of 1. Acceptance: both figures hold, the results and the full replacement comment text are recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac4-verification.md` with `ExpectedExitCode: 1` recorded for the first command, and the line `- [ ] AC4 —` in `issue.md` becomes `- [x] AC4 —` with the criterion text unchanged.

- [ ] [P2-T15] Verify AC5 and check it off. Run `git grep -n -F -- '= "====";' -- '*.cs'` and confirm it returns exactly one line, located in `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs`; run `git grep -n 'BannerPrefix' -- UtilitiesCS/OutlookObjects/Folder/FolderSuggestionTree.cs` and confirm it returns exactly one line, which is the qualified reference inside `IsBanner`. Acceptance: both figures hold, the results are recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac5-verification.md`, and the line `- [ ] AC5 —` in `issue.md` becomes `- [x] AC5 —` with the criterion text unchanged. Edit only the line whose identifier is exactly `AC5` followed by a space and an em dash; the identifier `AC5` is a prefix of `AC5b`, so an unanchored edit would alter the wrong criterion.

- [ ] [P2-T16] Verify AC5b and check it off. Run `git diff 2b85134b42872e405602e6064e02dc9cda6c319b --stat -- UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs` and confirm it prints nothing. Acceptance: the command produces empty output with `EXIT_CODE: 0`, the result is recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac5b-verification.md`, and the line `- [ ] AC5b —` in `issue.md` becomes `- [x] AC5b —` with the criterion text unchanged. The diff is anchored to the base commit rather than to the index, so it reports the file's state whether or not the work has been committed. If the repository-wide format pass in P2-T1 rewrote this file, this gate fails; the P0-T7 baseline records whether that was foreseeable.

- [ ] [P2-T17] Verify AC6 and check it off. Acceptance: the P2-T5 TRX `<Counters ... />` line carries `passed="1"` and `failed="0"`, the new test method's full source text is transcribed into `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac6-verification.md` together with that counters line, and the line `- [ ] AC6 —` in `issue.md` becomes `- [x] AC6 —` with the criterion text unchanged.

- [ ] [P2-T18] Verify AC7 and check it off. Run `git diff 2b85134b42872e405602e6064e02dc9cda6c319b --stat -- QuickFiler.Test/Controllers/EfcFormControllerTests.cs` and confirm it prints nothing, and confirm the P2-T6 TRX `<Counters ... />` line carries `passed="1"` and `failed="0"`. Acceptance: both hold, the results are recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac7-verification.md`, and the line `- [ ] AC7 —` in `issue.md` becomes `- [x] AC7 —` with the criterion text unchanged.

- [ ] [P2-T19] Verify AC8 and check it off. Acceptance: the P2-T7 TRX `<Counters ... />` line carries `failed="0"` and a `passed` value not lower than the `passed` value recorded for `QuickFiler.Test` in P0-T11; the P2-T8 TRX `<Counters ... />` line carries `failed="0"` and a `passed` value not lower than the `passed` value recorded for `UtilitiesCS.Test` in P0-T12; the four figures and their two comparisons are recorded in `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac8-verification.md`; and the line `- [ ] AC8 —` in `issue.md` becomes `- [x] AC8 —` with the criterion text unchanged. If either baseline artifact recorded a non-zero `failed` value, this gate cannot pass as written: record the discrepancy in the artifact, leave AC8 unchecked, and report `REMEDIATION-REQUIRED` naming the pre-existing failures.

- [ ] [P2-T20] Verify AC9 and check it off. Acceptance: the four artifacts `csharpier-format.md`, `csharpier-check.md`, `msbuild-analyzers.md`, and `msbuild-nullable.md` under `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/` all exist, all carry `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`, all record `EXIT_CODE: 0`, all carry timestamps from the same final loop pass, and the format artifact's `Output Summary:` carries the transcribed CSharpier summary line and the before-and-after tree observation rather than the exit code alone; a summary of those four records is written to `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/qa-gates/ac9-verification.md`; and the line `- [ ] AC9 —` in `issue.md` becomes `- [x] AC9 —` with the criterion text unchanged.

- [ ] [P2-T21] Mirror the `issue.md` acceptance-criteria update to `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/issue-updates/issue-662.<TS>.md`, where `<TS>` is the ISO-8601 execution timestamp. Acceptance: the mirror carries `Timestamp:`, the exact final text of the `## Acceptance Criteria` section as it stands in `issue.md`, and a `PostedAs:` field. Use `PostedAs: body` and record the issue URL and `IssueUpdatedAt:` if the update was posted to GitHub; otherwise use a `POSTING BLOCKED` header naming the reason, which for a local-only run is that no GitHub post was attempted at this stage.

- [ ] [P2-T22] Write the acceptance-criteria status summary to `docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662/evidence/other/ac-status-summary.md`. Acceptance: the artifact names `issue.md` as the source, states `Total AC items: 10`, states the checked-off count, states the remaining count, and lists the criterion text of every item left unchecked. The three boxes in the `## Evidence Checklist` section of `issue.md` — baseline, targeted verification, end-state — are also checked off in this task, each citing the evidence directory that satisfies it. Those three boxes are not acceptance criteria: under `minor-audit` mode only the `## Acceptance Criteria` section is the acceptance-criteria source, which is why they are handled together here while each acceptance criterion has its own check-off task.

- [ ] [P2-T23] Commit every remaining change and confirm a clean scoped tree. Run `git add QuickFiler UtilitiesCS QuickFiler.Test docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662` followed by a commit whose message names issue 662, then run `git status --porcelain -- QuickFiler UtilitiesCS QuickFiler.Test docs/features/active/efcselectionguard-banner-prefix-arity-and-stale-comment-662`. Acceptance: the status command returns no output. The `git add` span makes files created by earlier Phase 2 tasks visible to the status comparison, which enumerates tracked and untracked paths alike within the pathspec. This is the last task in the plan; write no further artifact after it.

---

## Reduced Audit Handoff (short path)

After Phase 2 completes, hand off to the reduced post-implementation audit. The audit's required inputs are: this plan file; `issue.md` with its ten acceptance criteria in their final checked state; the Phase 0 baseline artifacts under `evidence/baseline/`; the targeted verification artifacts under `evidence/regression-testing/`, including the fail-before exception dossier; and the final-QC artifacts under `evidence/qa-gates/`. The audit verdict is BLOCKED or INCOMPLETE, never PASS, if any of those artifacts is missing, if any carries an incomplete field set, or if the checklist state in `issue.md` contradicts the evidence on disk.
