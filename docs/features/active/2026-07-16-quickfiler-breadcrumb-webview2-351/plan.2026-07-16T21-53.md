# quickfiler-breadcrumb-webview2 — Atomic Implementation Plan

- **Issue:** #351
- **Epic:** folder-tree-breadcrumb-redesign (integration branch `epic/folder-tree-breadcrumb-redesign-integration`), decomposition item 3 (placeholder 9103, wave 1, C4)
- **Feature folder (`<FEATURE>`):** `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351`
- **Work Mode:** full-feature
- **Owner:** drmoisan
- **Last Updated:** 2026-07-16T21-53
- **Status:** Draft — pending atomic-executor preflight (`DIRECTIVE: PREFLIGHT VALIDATION ONLY`)
- **Requirements sources:** `<FEATURE>/issue.md`, `<FEATURE>/spec.md` (FR-1..FR-7, AC-1..AC-13), `<FEATURE>/user-story.md` (US-1..US-8), `<FEATURE>/research/2026-07-16T22-30-quickfiler-breadcrumb-webview2-research.md`, `docs/features/epics/folder-tree-breadcrumb-redesign/epic.md`
- **Workspace root:** `C:/Users/DanMoisan/repos/TaskMaster/.claude/worktrees/agent-a1d165d3cb6c7c026` (all relative paths below resolve against this root)

## Required References (policy compliance order)

1. `CLAUDE.md` (all sections, including the embedded General/C# Code Change and Unit Test policies)
2. `.claude/rules/general-code-change.md`
3. `.claude/rules/general-unit-test.md`
4. C# language policy: `.claude/rules/csharp.md` plus the `## C# Code Change Policy` and `## C# Unit Test Policy` sections of `CLAUDE.md`

All work must comply with these policies; this plan does not duplicate their content.

## Evidence Location Contract

All evidence artifacts produced by this plan resolve to `<FEATURE>/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. `<TS>` in artifact names is the ISO-8601 `yyyy-MM-ddTHH-mm` timestamp at execution time. Every command-step artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.

EVIDENCE_LOCATION_OVERRIDE_REJECTED: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/repro/` (named by spec.md FR-5/AC-6) replaced with `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/regression-testing/` (defect reproduction) and `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/` (post-fix verification). `repro/` is not a canonical evidence kind.

The JaCoCo coverage XML at `artifacts/csharp/coverage.xml` (Phase 7) is a non-evidence, tool-consumed feature-review-gate input, separate from and in addition to the canonical evidence artifacts.

## Plan Guardrails (encode spec constraints; binding on every task)

- G1: No third-party WinForms tree/list control and no WPF/`ElementHost`. Control technology is WebView2 (HTML/CSS/JS) only (AC-9).
- G2: No new NuGet packages. WebView2 SDK 1.0.3912.50 is already referenced in `QuickFiler/QuickFiler.csproj`, `UtilitiesCS/UtilitiesCS.csproj`, `QuickFiler.Test/QuickFiler.Test.csproj`, `UtilitiesCS.Test/UtilitiesCS.Test.csproj` (AC-9). `Newtonsoft.Json` is referenced only by `UtilitiesCS`; all JSON serialization code lives in `UtilitiesCS`, never in `QuickFiler`.
- G3: No change to the scoring/ranking algorithm or model output. `FolderScore`, `FolderPredictor`, `FolderRow`, and `UtilitiesCS/OutlookObjects/Folder/PercentageFormatter.cs` are consumed read-only (AC-10, US-8).
- G4: All touched projects are non-SDK net4.8.1: no `record`, no `record struct`, no `init` (no `IsExternalInit`); plain classes/readonly structs with explicit constructors. Every new `.cs` file (production and test) requires an explicit `<Compile Include>` entry in its non-SDK `.csproj`.
- G5: No production/test file may exceed 500 lines.
- G6: The 9101 provider contract is `ASSUMED-PENDING-9101-MERGE`. This plan consumes the provider behind an injectable interface and never re-implements the live Outlook query. Phase 2 reconciles the assumed contract against the merged 9101 surface before any consumer code binds to it.
- G7: The breadcrumb WebView2 reuses the existing `IWebViewCoreInitializer` seam and, where practical, the same `CoreWebView2Environment` and the same `CoreWebView2EnvironmentOptions("–incognito ")` object created in `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs:49` verbatim (the en-dash argument is explicitly out of scope to fix).
- G8: The nine dead viewer variants (`Form1`, `ItemViewerExpanded`, `QfcItemViewer`, `QFCItemViewerDarkNew`, `QfcItemViewerExpanded`, `QfcItemViewerExpandedLight`, `QFCItemViewerLightNew`, `QfcItemViewerLightSelected`, `QfcItemViewerV1`) are untouched; only the live `ItemViewer` changes (AC-1).
- G9: The runtime reproduction of the percentage-obscuring defect (Phase 1) MUST complete before any CSS/fix work (Phase 4 onward) begins (AC-6).
- G10: Selection-output contract is bit-for-bit: `GetSelectedFolder()` yields the full folder path string for suggestion rows and the verbatim non-suggestion string (including the literal `"Trash to Delete"`) for plain rows; both Path A (`AssignFolderComboBox`) and Path B (`TextBoxSearch_TextChanged`) population paths survive (AC-8, FR-7).
- G11: MSTest + Moq + FluentAssertions for all new tests; no temp files; no external processes; async provider calls tested with completed tasks; no timers.
- G12: Coverage bars: new host-neutral code >= 90% line; repository floor >= 80% on the testable denominator (CLAUDE.md COM/VSTO exemption); no coverage regression on changed lines.

## Traceability

- FR-1/AC-2/US-1: P2-T2..T4, P3-T1..T4, P4-T3, P5-T3, P5-T5
- FR-2/AC-3/US-2: P3-T1..T4, P4-T3
- FR-3/AC-4/US-3: P3-T1..T2, P4-T3
- FR-4/AC-5/US-4: P2-T1..T4, P3-T7..T8, P5-T5, P5-T10
- FR-5/AC-6/US-5: P1-T1..T2, P4-T3, P6-T1
- FR-6/AC-7/US-6: P3-T5..T8, P4-T1..T5, P5-T2, P5-T4, P5-T6, P6-T2
- FR-7/AC-8/US-7: P3-T9..T10, P5-T3, P5-T7, P6-T3
- AC-1: P5-T1..T4, P5-T10 (G8)
- AC-9/AC-10: P5-T11 (G1..G3)
- AC-11: P3-T1..T10, P4-T4..T5
- AC-12: P0-T4..T7, P7-T1..T6
- AC-13: P0-T8, P2-T1

## Implementation Plan (Atomic Tasks)

### Phase 0 — Policy Read and Baseline Capture

- [x] [P0-T1] Read the policy documents in policy-compliance order — `CLAUDE.md` (all sections), `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, and the C# Code Change / C# Unit Test policy sections of `CLAUDE.md` — and record the read evidence.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/phase0-instructions-read.md` exists containing `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- [x] [P0-T2] Read the requirements sources — `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/issue.md`, `spec.md`, `user-story.md`, `research/2026-07-16T22-30-quickfiler-breadcrumb-webview2-research.md`, and `docs/features/epics/folder-tree-breadcrumb-redesign/epic.md` — and record the context-read evidence.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/phase0-context-read.<TS>.md` exists with `Timestamp:` and the explicit list of documents read, confirming Work Mode `full-feature` and the FR-1..FR-7 / AC-1..AC-13 inventory.
- [x] [P0-T3] Record the git baseline state (current branch, HEAD SHA, `git status` cleanliness) for the workspace root `C:/Users/DanMoisan/repos/TaskMaster/.claude/worktrees/agent-a1d165d3cb6c7c026`.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/git-baseline-state.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (branch, SHA, clean/dirty).
- [x] [P0-T4] Run the baseline formatting check `dotnet tool run csharpier check .` from the repo root and record the command-step artifact under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/`.
  - Acceptance: `evidence/baseline/baseline-csharpier-check.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail and any pre-existing unformatted-file count). Baseline capture does not mutate files.
- [x] [P0-T5] Run the baseline analyzer build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and record the command-step artifact under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/`.
  - Acceptance: `evidence/baseline/baseline-analyzer-build.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error/warning counts; any pre-existing diagnostics enumerated).
- [x] [P0-T6] Run the baseline nullable/type-check build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and record the command-step artifact under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/`.
  - Acceptance: `evidence/baseline/baseline-nullable-build.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pre-existing nullable/warning state enumerated).
- [x] [P0-T7] Run the baseline test pass with coverage `vstest.console.exe QuickFiler.Test\bin\Debug\QuickFiler.Test.dll UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /EnableCodeCoverage` and record the command-step artifact under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/`.
  - Acceptance: `evidence/baseline/baseline-test-coverage.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` containing numeric total pass/fail counts AND the numeric baseline line-coverage headline percentage (overall, plus `QuickFiler.dll` and `UtilitiesCS.dll` assembly figures) derived from the produced `.coverage` report.
- [x] [P0-T8] Verify the merged 9101 provider contract state on this branch by searching `UtilitiesCS/OutlookObjects/Folder/` for the provider surface (e.g. `IFolderHierarchyProvider`, ancestor-chain/immediate-subfolder members added to `IOutlookFolderHierarchyReader.cs`) and record the finding.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/baseline/baseline-9101-contract-state.<TS>.md` exists with `Timestamp:`, the search commands/patterns used, `SearchScope:`, `SearchResult:`, and a verdict line `9101-CONTRACT: PRESENT <types found>` or `9101-CONTRACT: ABSENT (ASSUMED-PENDING-9101-MERGE unresolved)`.

### Phase 1 — Runtime Reproduction of the Percentage-Obscuring Defect

- [x] [P1-T1] (dossier-satisfied: fail-before exception dossier per authorized branch; no live Outlook host) Capture the runtime reproduction of the percentage-obscuring defect in the current `CboFolders` control (live Outlook with the add-in, QuickFiler open, suggestions populated via Path A): screenshot the dropped-down list under dark and light themes, with enough rows to force a scrollbar, at 100% and 150% display scaling, and store the capture set under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/regression-testing/`.
  - Acceptance: `evidence/regression-testing/percentage-obscuring-repro.<TS>.md` exists with `Timestamp:` and an observation log recording, for each capture: theme name, row count, display scaling, screenshot filename (PNG files in the same folder), and which hypothesis the capture confirms — hypothesis 1 (theme color contrast, `UtilitiesCS/HelperClasses/ThemeHelpers/Theme.Rendering.cs:96-98` TODO), hypothesis 2 (dropdown scrollbar overlay of the 46 px column), or hypothesis 3 (DPI/font clipping). If a live-host capture is structurally impossible in the execution environment, a schema-valid fail-before exception dossier `evidence/regression-testing/fail-before-exception.<TS>.md` (with `WhyFailingRunImpossible:` and an alternative-proof section) satisfies this task instead; one of the two artifacts MUST exist.
- [x] [P1-T2] Write the reproduction analysis that selects the confirmed hypothesis and maps it to the planned CSS fix (percentage cell `flex: 0 0 auto; margin-left: auto; flex-shrink: 0` with `ch`-based `min-width`; truncating segment container; CSS custom-property theme colors), stored under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/regression-testing/`.
  - Acceptance: `evidence/regression-testing/percentage-obscuring-analysis.<TS>.md` exists with `Timestamp:`, the confirmed (or dossier-assumed) hypothesis, and a one-paragraph statement of how the Phase 4 CSS structure eliminates it by construction. Phase 4 MUST NOT start before this artifact exists (G9).

### Phase 2 — 9101 Contract Reconciliation and Provider Consumer Seam

- [x] [P2-T1] (RECONCILIATION: DIRECT-CONSUME) Reconcile the assumed 9101 contract against the merged surface found by P0-T8 in `UtilitiesCS/OutlookObjects/Folder/`: record the actual interface/DTO names and namespace, and decide `DIRECT-CONSUME` (merged surface already matches `GetAncestorChainAsync`/`GetImmediateSubfoldersAsync` + `DisplayName`/`FolderPath`/`HasSubfolders`) or `ADAPTER-REQUIRED` (merged surface is a raw reader extension over `IOutlookFolderHierarchyReader`/`FolderTreeSnapshotNode`).
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/other/9101-contract-reconciliation.<TS>.md` exists with `Timestamp:`, the actual member signatures quoted, the decision line `RECONCILIATION: DIRECT-CONSUME` or `RECONCILIATION: ADAPTER-REQUIRED`, and the statement that the `ASSUMED-PENDING-9101-MERGE` marker is resolved (AC-13). If P0-T8 recorded `9101-CONTRACT: ABSENT`, this task records BLOCKED and the executor halts the plan and reports blocked state; it does not improvise a provider implementation (G6).
- [x] [P2-T2] (DIRECT-CONSUME: merged 9101 types used; no new files) Create the narrow QuickFiler-facing provider seam the breadcrumb codes against: `UtilitiesCS/OutlookObjects/Folder/IFolderHierarchyProvider.cs` (`Task<IReadOnlyList<FolderBreadcrumbSegment>> GetAncestorChainAsync(string leafFolderPath, CancellationToken)` and `Task<IReadOnlyList<FolderBreadcrumbSegment>> GetImmediateSubfoldersAsync(string folderPath, CancellationToken)`) and `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbSegment.cs` (net481-safe plain sealed class or readonly struct with explicit ctor; JSON-serializable members `DisplayName`, `FolderPath`, `HasSubfolders`; no COM handles), skipping whichever file the P2-T1 `DIRECT-CONSUME` decision makes redundant in favor of the merged 9101 type.
  - Acceptance: each created file compiles, is <= 500 lines, has XML doc comments on the public contract, and has an explicit `<Compile Include>` entry in `UtilitiesCS/UtilitiesCS.csproj` (G4); if `DIRECT-CONSUME`, the artifact from P2-T1 names the merged types used instead and only the missing file(s) are created.
- [x] [P2-T3] (satisfied-by-decision: DIRECT-CONSUME, authorized skip branch, no file created) Implement the thin mapping adapter `UtilitiesCS/OutlookObjects/Folder/FolderHierarchyProviderAdapter.cs` conforming the merged 9101 substrate to `IFolderHierarchyProvider` (ancestor chain root-to-leaf ordering; immediate subfolders of a segment; `HasSubfolders` derivation, e.g. from `FolderTreeSnapshotNode.ChildKeys.Count > 0`), performing no live Outlook query of its own (all I/O stays behind the injected 9101 substrate) — created only when P2-T1 decided `ADAPTER-REQUIRED`.
  - Acceptance: the adapter is a pure mapping class (no interop/COM/WinForms usage), <= 500 lines, wired via `<Compile Include>` in `UtilitiesCS/UtilitiesCS.csproj`; if `DIRECT-CONSUME` was decided, the P2-T1 artifact records this task as satisfied-by-decision with no file created (explicitly authorized skip branch).
- [x] [P2-T4] (DIRECT-CONSUME branch: contract-shape pinning tests, 8/8 pass) Create MSTest unit tests `UtilitiesCS.Test/OutlookObjects/Folder/FolderHierarchyProviderAdapterTests.cs` covering the provider seam mapping with Moq-mocked 9101 substrate and completed tasks: positive (multi-segment chain ordering; subfolder listing), negative (unknown path -> explicit failure; substrate exception propagation), edge (root-only chain; leaf without subfolders -> `HasSubfolders == false`; empty subfolder list), targeting the seam types created in Phase 2.
  - Acceptance: tests use MSTest + Moq + FluentAssertions (G11), pass locally via `vstest.console.exe UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, have an explicit `<Compile Include>` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, and exercise every public member of the Phase 2 seam types (>= 90% line on those types, verified in P7-T6); if P2-T1 decided `DIRECT-CONSUME` with zero new mapping code, the tests instead pin the merged 9101 contract shape the breadcrumb consumes.

### Phase 3 — Host-Neutral Breadcrumb Core (UtilitiesCS)

- [x] [P3-T1] Create `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs`: the host-neutral collapse/expand state machine over breadcrumb rows (per-row ancestor-chain segments from `FolderBreadcrumbSegment`; `CollapseAfter(segmentIndex)` for non-leaf double-click; `ReExpand()` restoring the full chain; leaf plus/minus expansion state gated on `HasSubfolders`; selected-row tracking; Path B plain-string rows carried without probability), mirroring the tested `UtilitiesCS/OutlookObjects/Folder/FolderTreeStateModel.cs` precedent — no WinForms, COM, WebView2, or I/O usage.
  - Acceptance: file compiles, <= 500 lines, invariants enforced with explicit argument validation (fail fast), `<Compile Include>` entry added to `UtilitiesCS/UtilitiesCS.csproj`.
- [x] [P3-T2] Create `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelTests.cs` covering positive (collapse-after-k hides all downstream segments and the leaf; plus re-expands the full chain; leaf affordance shown only when `HasSubfolders`), negative (collapse on the leaf index rejected; out-of-range segment index throws; expand on affordance-less leaf is rejected/no-op by contract), edge (single-segment chain; already-collapsed re-collapse; state after row reset), and state-transition sequences (collapse -> re-expand -> collapse; expand -> subfolder list -> collapse).
  - Acceptance: MSTest + Moq + FluentAssertions, deterministic, no temp files (G11); `<Compile Include>` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`; all tests pass.
- [x] [P3-T3] Create `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRenderProjection.cs`: a pure projection from `BreadcrumbStateModel` state to the ordered render DTO list JS consumes (segment text, arrow separators, plus/minus affordance placement, percentage text via existing `UtilitiesCS/OutlookObjects/Folder/PercentageFormatter.cs` for Path A rows, empty percentage cell for Path B rows, middle-segment truncation eligibility flags).
  - Acceptance: file compiles, <= 500 lines, no I/O/WinForms/WebView2 references, `<Compile Include>` entry in `UtilitiesCS/UtilitiesCS.csproj`; `PercentageFormatter` consumed read-only (G3).
- [x] [P3-T4] Create `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbRenderProjectionTests.cs` covering positive (full-chain row renders `Folder -> SubFolder -> Leaf` order with percentage), negative (null/invalid state rejected), edge (collapsed row renders plus + terminal segment only; leaf without subfolders renders no affordance; Path B row renders chain with empty percentage cell; 0%/100%/null score formatting parity with `PercentageFormatter`).
  - Acceptance: MSTest + FluentAssertions, `<Compile Include>` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, all tests pass.
- [ ] [P3-T5] Create `UtilitiesCS/OutlookObjects/Folder/BreadcrumbBridgeMessages.cs`: the typed JS<->.NET message protocol (message families: `segmentDoubleClick`, `affordanceToggle`, `arrowKey` (left/right), `selectionChange`, `subfolderRequest`/`subfolderResponse`, `themeChange`, `render` full-payload, `unhandledArrow` report) plus the Newtonsoft.Json-based serializer/parser (Newtonsoft.Json 13.0.4 already referenced by `UtilitiesCS/UtilitiesCS.csproj`; G2), with explicit parse failure on unknown message type or malformed JSON.
  - Acceptance: file compiles, <= 500 lines, net481-safe types (G4), `<Compile Include>` entry in `UtilitiesCS/UtilitiesCS.csproj`.
- [ ] [P3-T6] Create `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbBridgeMessagesTests.cs` covering round-trip serialization of every message family, negative parsing (malformed JSON, unknown type, missing required fields -> explicit error, not silent null), and edge payloads (empty subfolder response; maximal-length folder path; non-ASCII folder names).
  - Acceptance: MSTest + FluentAssertions, `<Compile Include>` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, all tests pass.
- [ ] [P3-T7] Create `UtilitiesCS/OutlookObjects/Folder/BreadcrumbBridgeRouter.cs`: the pure async message router (JSON string in -> typed message -> `BreadcrumbStateModel` transition and/or `IFolderHierarchyProvider` call -> JSON string(s) out), handling double-click collapse, plus/minus toggle (with `GetImmediateSubfoldersAsync` routing on expand), Left/Right arrow (returning either a state-changing response or an `unhandledArrow` report for legacy fall-through), selection change, and theme change; provider injected via constructor; fail fast on unroutable input.
  - Acceptance: file compiles, <= 500 lines, no WebView2/WinForms/COM references, `<Compile Include>` entry in `UtilitiesCS/UtilitiesCS.csproj`.
- [ ] [P3-T8] Create `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbBridgeRouterTests.cs` with Moq-mocked `IFolderHierarchyProvider` (completed tasks only) covering positive (expand routes subfolder query and returns render+response JSON; double-click collapse produces collapsed render payload; Right arrow expands when expandable), negative (provider exception surfaces as explicit error response; malformed inbound JSON -> error), edge (Right arrow with nothing to expand -> `unhandledArrow` right; Left arrow with nothing to collapse -> `unhandledArrow` left; theme change re-render), and state-transition sequences across multiple routed messages.
  - Acceptance: MSTest + Moq + FluentAssertions, string-in/string-out assertions, `<Compile Include>` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, all tests pass.
- [ ] [P3-T9] Create `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs`: the pure selection mapping from visible row/segment/subfolder identity to the `GetSelectedFolder()` output string — full folder path for Path A suggestion rows and expanded-subfolder selections, verbatim string for Path B rows including the literal `"Trash to Delete"` (G10) — plus index/item lookup helpers backing `SetFolderSelectedIndex`/`SetFolderSelectedItem`/`FolderContains`/`GetFolderItems`.
  - Acceptance: file compiles, <= 500 lines, `<Compile Include>` entry in `UtilitiesCS/UtilitiesCS.csproj`.
- [ ] [P3-T10] Create `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbSelectionMapTests.cs` covering positive (suggestion row -> full folder path; expanded subfolder -> its full path; index and item selection round-trip), negative (unknown id/index rejected explicitly), edge (`"Trash to Delete"` returned byte-identical; Path B verbatim strings with wildcard/search characters; empty item set; duplicate paths).
  - Acceptance: MSTest + FluentAssertions, `<Compile Include>` entry in `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, all tests pass.

### Phase 4 — WebView2 Messenger Seam, Breadcrumb Page, and Bridge Coordinator (QuickFiler)

- [ ] [P4-T1] Create the narrow messenger seam `QuickFiler/Viewers/IWebViewMessenger.cs` (`event EventHandler<string> MessageReceived; void PostJson(string json);` shape per spec FR-6) so all post-init WebView2 messaging is injectable and mockable.
  - Acceptance: file compiles, XML doc comments on the contract, `<Compile Include>` entry in `QuickFiler/QuickFiler.csproj` (G4).
- [ ] [P4-T2] Create the production adapter `QuickFiler/Viewers/WebView2Messenger.cs` as a thin `[ExcludeFromCodeCoverage]` 1:1 forwarder over `CoreWebView2.WebMessageReceived` (raising `MessageReceived` with `TryGetWebMessageAsString`/JSON payload) and `CoreWebView2.PostWebMessageAsJson`, matching the `QuickFiler/Viewers/WebView2CoreInitializer.cs` exempt-forwarder pattern with an in-code exemption rationale comment.
  - Acceptance: file compiles, <= 500 lines, contains no logic beyond forwarding (nothing testable is exempted), `<Compile Include>` entry in `QuickFiler/QuickFiler.csproj`.
- [ ] [P4-T3] Create the self-contained breadcrumb page `QuickFiler/Resources/FolderBreadcrumb.html` (single file, inline CSS + JS, no external fetches): flexbox row per suggestion with segment container `flex: 1 1 auto; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` and percentage cell `flex: 0 0 auto; margin-left: auto; flex-shrink: 0;` with `ch`-based `min-width` (the FR-5 CSS fix, per the P1-T2 analysis); plus/minus affordance elements; theme colors exclusively via CSS custom properties switched by the `themeChange` bridge message and `prefers-color-scheme` default; JS handlers for segment `dblclick`, affordance `click`, row selection `click`, and `keydown` Left/Right (reporting `unhandledArrow` when nothing expands/collapses), all communicating via `window.chrome.webview.postMessage` and the `message` event using the Phase 3 JSON protocol.
  - Acceptance: file added to `QuickFiler/QuickFiler.csproj` following the existing `Resources/EmailHeader.html` inclusion precedent (`QuickFiler.csproj:495`) so it is loadable as a string resource for `NavigateToString`; no third-party JS/CSS libraries (G1/G2); file <= 500 lines.
- [ ] [P4-T4] Create `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`: a host-neutral, NOT-coverage-exempt coordinator that wires `IWebViewMessenger.MessageReceived` -> `BreadcrumbBridgeRouter` (from `UtilitiesCS/OutlookObjects/Folder/BreadcrumbBridgeRouter.cs`) -> `IWebViewMessenger.PostJson`, exposes `SetSuggestions`/`SetItems`/`Clear`/selection accessors for the viewer glue, raises .NET events `SelectionChanged`, `UnhandledArrow(direction)`, and a synthetic `FolderKeyDown` (preserving the `IItemViewer.FolderKeyDown` seam for existing consumers), and awaits router responses without timers.
  - Acceptance: file compiles, <= 500 lines, no direct WebView2/WinForms/COM type usage (only the seam interfaces), `<Compile Include>` entry in `QuickFiler/QuickFiler.csproj`.
- [ ] [P4-T5] Create `QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorTests.cs` with Moq-mocked `IWebViewMessenger` and `IFolderHierarchyProvider` covering positive (inbound double-click message produces posted collapse render JSON; expand posts subfolder response; selection message raises `SelectionChanged` with the mapped path), negative (router error response posted on malformed inbound message; provider failure surfaces explicitly), edge (`unhandledArrow` right/left raise `UnhandledArrow` with correct direction; synthetic `FolderKeyDown` raised for arrow messages; `Clear` empties selection state).
  - Acceptance: MSTest + Moq + FluentAssertions, completed tasks only (G11), `<Compile Include>` entry in `QuickFiler.Test/QuickFiler.Test.csproj`, all tests pass.

### Phase 5 — ItemViewer and Controller Integration

- [ ] [P5-T1] Update `QuickFiler/Viewers/ItemViewer.Designer.cs`: replace the `_cboFolders` `ComboBox` declaration/configuration (`:189-204`, `DrawMode=OwnerDrawFixed`, `DrawItem`/`MouseDown` wiring) with a Designer-declared breadcrumb `WebView2` control (e.g. `_l0vhBreadcrumb_WebView2`) occupying the same `_l0vh_Tlp` cell with ColumnSpan 2 and `Dock = Fill`, `CreationProperties` all-null and `DefaultBackgroundColor = Transparent` matching the existing `_l0v2h2_WebView2` declaration (`:231-251`); leave the nine dead viewer variants' Designer files untouched (G8).
  - Acceptance: `QuickFiler/QuickFiler.csproj` builds; `git diff --stat` shows no changes under any dead-variant Designer file.
- [ ] [P5-T2] Create the new `[ExcludeFromCodeCoverage]` partial `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs`: exposes the breadcrumb WebView2 property, constructs the `WebView2Messenger` + `BreadcrumbBridgeCoordinator` after core init, loads `Resources/FolderBreadcrumb.html` via `NavigateToString` (mirroring `QuickFiler/Viewers/ItemViewer.WebViewThread.cs:15`), and implements the focus glue mapping `FocusFolderDropDown()`/`SetFolderDroppedDown(bool)` intent to `WebView2.Focus()` + a JS-focus/expansion-visibility bridge message, with the header comment stating that host-neutral seams own all correctness and this partial holds only WinForms glue.
  - Acceptance: file compiles, <= 500 lines, `<Compile Include>` entry in `QuickFiler/QuickFiler.csproj`, exemption rationale comment present.
- [ ] [P5-T3] Rewrite `QuickFiler/Viewers/ItemViewer.FolderSearch.cs`: delete the owner-draw machinery (`CboFolders_DrawItem` `:170-249`, `CboFolders_MouseDown` `:254-281`, `FolderPercentColumnWidth` `:163`) and the `FolderHierarchyBuilder.Build` call in `SetFolderSuggestions` (`:24-29`), and re-implement all twelve `IItemViewer` folder members (`SetFolderItems`, `SetFolderSuggestions`, `GetSelectedFolder` via `BreadcrumbSelectionMap`, `SetFolderSelectedIndex`, `SetFolderSelectedItem`, `SetFolderDroppedDown`, `ClearFolderItems`, `FocusFolderDropDown`, `FolderContains`, `GetFolderItems`, `FolderSelectionChanged`, `FolderKeyDown` per `QuickFiler/Viewers/IItemViewer.cs:80-100`) as thin delegations to the `BreadcrumbBridgeCoordinator`/state model, preserving Path A and Path B semantics and verbatim non-suggestion strings including `"Trash to Delete"` (G10).
  - Acceptance: file compiles, <= 500 lines, contains no logic beyond delegation/glue (correctness lives in the Phase 3/4 tested types); the `IItemViewer` interface file is unchanged unless a member signature is impossible to preserve, in which case the executor halts and reports (no silent public-API break).
- [ ] [P5-T4] Update `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs`: extend the `[ExcludeFromCodeCoverage]` init path (`InitializeWebViewAsync` `:36-107`) to initialize the breadcrumb WebView2 through the same injected `IWebViewCoreInitializer` and the same `CoreWebView2Environment` + `CoreWebView2EnvironmentOptions("–incognito ")` object already created for the message-body pane (G7), marshaling via `await _itemViewer.UiSyncContext;` per the existing pattern, and dispose/clear the breadcrumb resources in `Cleanup()` alongside the existing `_webViewEnvironment` clearing (`:312`).
  - Acceptance: `QuickFiler/QuickFiler.csproj` builds; no second environment with different options is created against the same user-data folder; file remains <= 500 lines (if the addition would exceed 500 lines, the breadcrumb init moves to a new `[ExcludeFromCodeCoverage]` partial `QuickFiler/Controllers/QfcItemController.BreadcrumbSetup.cs` with a `<Compile Include>` entry).
- [ ] [P5-T5] Update `QuickFiler/Controllers/QfcItemController.FolderHandling.cs` `AssignFolderComboBox` (`:161-200`): keep `SetFolderItems(_folderHandler.FolderArray)` (`:176`), route `SetFolderSuggestions(_folderHandler.FolderRowArray)` (`:183-186`) so ancestor chains come from the injected `IFolderHierarchyProvider.GetAncestorChainAsync` (via the coordinator) instead of `FolderHierarchyBuilder.Build`, and preserve the preselect logic (`:187-197`) and the `_selectedFolder = _itemViewer.GetSelectedFolder();` readback (`:198`) unchanged.
  - Acceptance: `FolderHierarchyBuilder.Build` has zero production call sites after this task (verified in P5-T10); Path A behavior per AC-8 preserved; the provider instance is constructor-injected/DI-resolved, never constructed against live Outlook inside breadcrumb code (G6).
- [ ] [P5-T6] Update `QuickFiler/Controllers/KeyboardHandler.cs`: reroute (do not remove) the folder-control Left/Right handling (`:543-583`) so the breadcrumb path is driven by `BreadcrumbBridgeCoordinator.UnhandledArrow` — Right falls through to the existing Pop Out/Enumerate Conversation dialog behavior (`:559-564`), Left falls through to the existing close/collapse behavior (`:579`) — and remove/bypass the `sender is not ComboBox -> ArgumentException` guard (`:501-505`) for the breadcrumb surface while leaving `DdOpen_KeyDownAsync`/`DdClosed_KeyDownAsync` intact for any remaining ComboBox senders.
  - Acceptance: `QuickFiler/QuickFiler.csproj` builds; both legacy fall-through behaviors are reachable from `UnhandledArrow` (asserted at runtime in P6-T2); file remains <= 500 lines or the new routing lands in a focused new file with `<Compile Include>`.
- [ ] [P5-T7] Update the controller event surface for the breadcrumb: `QuickFiler/Controllers/QfcItemController.EventWiring.cs:84` (rebind `FolderSelectionChanged` to the breadcrumb-raised event), `QuickFiler/Controllers/QfcItemController.EventHandlers.cs:164-178` (Path B: `ClearFolderItems()` / `SetFolderItems(folders)` / `SetFolderSelectedIndex(1)` / `SetFolderDroppedDown(true)` flow unchanged over the new members) and `:209-212` (selection-change refresh of `_selectedFolder`), and `QuickFiler/Controllers/QfcItemController.Navigation.cs:33-46` (`FocusFolderDropDown`/`SetFolderDroppedDown` intent calls unchanged).
  - Acceptance: `QuickFiler/QuickFiler.csproj` builds; the controller calls only `IItemViewer` members (no concrete breadcrumb type leakage beyond the existing accepted `ItemViewer` cast pattern); Path B population per AC-8 preserved.
- [ ] [P5-T8] Update `UtilitiesCS/HelperClasses/ThemeHelpers/Theme.Rendering.cs`: remove the `CboFolders` color assignments under the `TODO` (`:96-98`) and instead drive the breadcrumb theming via the existing WebView2 mechanism — `CoreWebView2.Profile.PreferredColorScheme` (pattern at `:112-116`) plus a `themeChange` bridge message that switches the page's CSS custom properties.
  - Acceptance: `UtilitiesCS/UtilitiesCS.csproj` builds; no remaining `_comboFolders`/`CboFolders` theme references for the live viewer; dark/light switching reaches the breadcrumb page (asserted at runtime in P6-T1/P6-T2).
- [ ] [P5-T9] Update `QuickFiler.Test` sources that compile against the replaced surface (at minimum `QuickFiler.Test/Form1.cs:24` harness construction and any test referencing `CboFolders`, owner-draw handlers, or removed members) so the test project builds against the new breadcrumb members without weakening any existing assertion.
  - Acceptance: `msbuild QuickFiler.Test/QuickFiler.Test.csproj /t:Build /p:Configuration=Debug` succeeds; no test deleted without a replacement covering the same behavior; any modified test still satisfies the General Unit Test Policy.
- [ ] [P5-T10] Verify decommissioning by search evidence: repo-wide grep shows (a) zero production references to `_cboFolders`/`CboFolders` outside the nine dead variant Designer files and (b) zero production call sites of `FolderHierarchyBuilder.Build` (the builder and its tests `UtilitiesCS.Test/OutlookObjects/Folder/FolderHierarchyBuilderTests.cs` remain in place, unreferenced by the live QuickFiler path), recorded with commands and matches.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/other/cbofolders-decommission-verification.<TS>.md` exists with `Timestamp:`, `Command:`, `SearchScope:`, `SearchResult:`, and an explicit pass/fail verdict for (a) and (b) (AC-5).
- [ ] [P5-T11] Verify guardrails by diff evidence: `git diff` over `QuickFiler/QuickFiler.csproj`, `UtilitiesCS/UtilitiesCS.csproj`, `QuickFiler.Test/QuickFiler.Test.csproj`, `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, and both `packages.config` files shows no new package references (G1/G2); `git diff` shows `UtilitiesCS/OutlookObjects/Folder/PercentageFormatter.cs` and the scoring/ranking sources (`FolderScore`, `FolderPredictor`, `FolderRow` types) untouched (G3); no file touched by Phases 2-5 exceeds 500 lines (G5).
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/other/guardrail-verification.<TS>.md` exists with `Timestamp:`, the commands run, and an explicit per-guardrail pass/fail line for G1, G2, G3, G5, G8 (AC-9, AC-10).

### Phase 6 — Runtime Verification Evidence

- [ ] [P6-T1] Capture post-fix runtime evidence pairing with P1-T1: with the breadcrumb control live in QuickFiler, screenshot the suggestion list in dark and light themes with a maximal-length folder path, enough rows to require scrolling, at 100% and 150% display scaling, demonstrating the percentage fully visible and unobstructed in every capture, stored under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/`.
  - Acceptance: `evidence/qa-gates/percentage-visibility-postfix.<TS>.md` exists with `Timestamp:`, per-capture theme/row-count/scaling/screenshot-filename entries (PNG files in the same folder), and an explicit statement closing the P1-T1 confirmed hypothesis (AC-6, US-5).
- [ ] [P6-T2] Verify at runtime in the live add-in, recording under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/`: (a) keyboard Left/Right on the breadcrumb — Right expands when expandable and otherwise opens the Pop Out/Enumerate dialog, Left collapses and otherwise closes the folder control (AC-7, US-6); (b) focus hand-off — `FocusFolderDropDown()`/`SetFolderDroppedDown(true)` land keyboard focus in the breadcrumb WebView2 (spec risk item); (c) double-click collapse and plus re-expand on a non-leaf segment (AC-4); (d) leaf affordance shown only when the leaf has subfolders (AC-3); (e) expanding a segment lists real immediate Outlook subfolders not present among suggestions (AC-5, US-4).
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/breadcrumb-runtime-interaction.<TS>.md` exists with `Timestamp:` and an explicit pass/fail line per item (a)-(e).
- [ ] [P6-T3] Verify at runtime and record the selection-output contract end to end: Path A (suggestions populate, preselect honored, selecting a breadcrumb row/expanded subfolder files the mail to exactly the shown full path via `DestinationOlStem` at `QuickFiler/Controllers/QfcItemController.MailActions.cs:103`), Path B (typing in search populates plain rows, `SetFolderSelectedIndex(1)` + dropdown-open behavior intact), and the `"Trash to Delete"` literal path (attachment-saving gate at `MailActions.cs:90` and `QfcCollectionController.cs:170-171` dialog-skip behave exactly as today).
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/selection-contract-runtime.<TS>.md` exists with `Timestamp:` and explicit pass/fail lines for Path A, Path B, and `"Trash to Delete"` (AC-8, US-7).
- [ ] [P6-T4] Observe and record the second-WebView2-per-pooled-viewer resource cost (memory delta and breadcrumb init latency across the `QuickFiler/Helper Classes/ItemViewerQueue.cs` pool), and record the eager-vs-lazy decision: if the cost is prohibitive, file the lazy-initialization fallback as a follow-up issue rather than widening this feature's scope.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/other/webview2-resource-observation.<TS>.md` exists with `Timestamp:`, the observed figures, and a `DECISION:` line (spec Constraints & Risks).

### Phase 7 — Final QA Loop, Coverage Gate, and Closeout

Loop behavior: P7-T1 through P7-T4 form one toolchain pass in this exact order. If any step fails or changes any file (e.g. csharpier reformats), fix forward and restart the phase from P7-T1. The phase completes only when all four steps pass in a single pass. `EXIT_CODE: SKIPPED` is not a valid outcome for any task in this phase.

- [ ] [P7-T1] Run the final formatting step `dotnet tool run csharpier .` from the repo root; if it changes files, restart the loop at P7-T1 after recording the change; record the final passing run under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/`.
  - Acceptance: `evidence/qa-gates/final-qc-csharpier.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (0 files changed on the final pass).
- [ ] [P7-T2] Run the final analyzer build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; on failure fix and restart the loop at P7-T1; record under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/`.
  - Acceptance: `evidence/qa-gates/final-qc-analyzer-build.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` (no new diagnostics vs the P0-T5 baseline).
- [ ] [P7-T3] Run the final nullable/type-check build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`; on failure fix and restart the loop at P7-T1; record under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/`.
  - Acceptance: `evidence/qa-gates/final-qc-nullable-build.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` (no new nullable warnings vs the P0-T6 baseline).
- [ ] [P7-T4] Run the final test pass with coverage `vstest.console.exe QuickFiler.Test\bin\Debug\QuickFiler.Test.dll UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /EnableCodeCoverage`; on any failure fix and restart the loop at P7-T1; record under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/`.
  - Acceptance: `evidence/qa-gates/final-qc-test-coverage.<TS>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and an `Output Summary:` containing numeric pass/fail totals, the numeric post-change line-coverage headline (overall plus `QuickFiler.dll` and `UtilitiesCS.dll`), AND the numeric new-code line-coverage figure for the Phase 2-4 host-neutral types.
- [ ] [P7-T5] Convert the final vstest coverage output to a first-party-scoped JaCoCo-format XML at `artifacts/csharp/coverage.xml` (the feature-review coverage gate parses JaCoCo `counter` elements at that exact path; scope the aggregate to first-party assemblies only), recording the conversion command and resulting `LINE`/`BRANCH` counter totals.
  - Acceptance: `artifacts/csharp/coverage.xml` exists in JaCoCo format; the conversion is summarized (command, totals) inside `evidence/qa-gates/final-qc-test-coverage.<TS>.md` or a sibling `evidence/qa-gates/coverage-conversion.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [ ] [P7-T6] Write the coverage delta/threshold verification comparing P0-T7 baseline vs P7-T4 post-change vs new/changed-code coverage: (1) new host-neutral code (`BreadcrumbStateModel`, `BreadcrumbRenderProjection`, `BreadcrumbBridgeMessages`, `BreadcrumbBridgeRouter`, `BreadcrumbSelectionMap`, `BreadcrumbBridgeCoordinator`, Phase 2 seam/adapter types) >= 90% line; (2) repository floor >= 80% on the testable denominator; (3) no coverage regression on changed lines (G12). If any required numeric value is unavailable, the verdict is REMEDIATION-REQUIRED, never PASS.
  - Acceptance: `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/qa-gates/coverage-delta-verification.<TS>.md` exists with `Timestamp:`, a three-row numeric table (baseline / post-change / new-code), a per-threshold pass/fail line, and a final verdict line (AC-12).
- [ ] [P7-T7] Update the feature documents to the delivered state: check off satisfied criteria with evidence pointers in `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/spec.md` (AC-1..AC-13 and Definition of Done) and `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/issue.md` (Acceptance Criteria), and mark completed tasks `[x]` in this plan file `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/plan.2026-07-16T21-53.md`.
  - Acceptance: every checked AC cites at least one evidence artifact path; checklist state matches evidence on disk; no AC checked without evidence.
- [ ] [P7-T8] Audit and commit the full evidence tree: verify every artifact named by this plan exists under `docs/features/active/2026-07-16-quickfiler-breadcrumb-webview2-351/evidence/` in its canonical sub-path, commit all evidence and code changes, and verify `git status` reports a clean worktree.
  - Acceptance: `evidence/qa-gates/final-evidence-audit.<TS>.md` exists with `Timestamp:`, the artifact inventory (path list with present/missing per item), `Command:`/`EXIT_CODE:`/`Output Summary:` for the final `git status`, and a clean-worktree confirmation.

## Test Plan Summary

- Unit (host-neutral, no Outlook, no WebView2): `BreadcrumbStateModel`, `BreadcrumbRenderProjection`, `BreadcrumbBridgeMessages`, `BreadcrumbBridgeRouter`, `BreadcrumbSelectionMap` in `UtilitiesCS.Test/OutlookObjects/Folder/`; `BreadcrumbBridgeCoordinator` and Phase 2 seam/adapter in `QuickFiler.Test/Viewers/` and `UtilitiesCS.Test/OutlookObjects/Folder/` — MSTest + Moq + FluentAssertions, positive/negative/edge/state-transition per task.
- Integration (mock-based): bridge message routing for double-click, keyboard, and live subfolder query via Moq'd `IWebViewMessenger` + `IFolderHierarchyProvider` (P4-T5, P3-T8).
- Runtime (host-bound, evidence-backed): defect reproduction before fix (P1-T1), post-fix percentage visibility (P6-T1), interaction/focus/fall-through (P6-T2), selection contract end-to-end (P6-T3).
- Coverage evidence: baseline `evidence/baseline/baseline-test-coverage.<TS>.md`; post-change `evidence/qa-gates/final-qc-test-coverage.<TS>.md`; comparison `evidence/qa-gates/coverage-delta-verification.<TS>.md`; review-gate input `artifacts/csharp/coverage.xml` (JaCoCo, first-party scope).

## Open Questions / Notes

- The 9101 contract reconciliation (P2-T1) is the single hard dependency gate; if 9101 is absent on the integration branch at execution time the plan halts at P2-T1 as BLOCKED (G6). No task re-implements the live Outlook query.
- No `[expect-fail]` regression-test tasks exist in this plan: the percentage-obscuring defect is host-bound/visual and cannot fail deterministically in a unit-test process; its fail-before requirement is satisfied by the P1-T1 runtime capture or, if capture is impossible, the fail-before exception dossier alternative defined in P1-T1 per `evidence-and-timestamp-conventions`.
- KeyboardHandler decommissioning decision (spec Constraints & Risks): this plan chooses rerouting over removal (P5-T6) for the smaller diff; `DdOpen_KeyDownAsync`/`DdClosed_KeyDownAsync` remain for non-breadcrumb ComboBox senders.
- Path B rows render as ancestor-split breadcrumb chains with an empty percentage cell (research §10.4 recommendation, adopted by spec FR-7).
