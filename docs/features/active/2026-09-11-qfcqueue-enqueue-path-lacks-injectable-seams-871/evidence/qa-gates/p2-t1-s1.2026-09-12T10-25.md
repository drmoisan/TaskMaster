# P2-T1 — Seam S1, the MoveMonitor property

Timestamp: 2026-09-13T15-23
Command: a line-oriented search of QuickFiler/Controllers/QfcQueue.cs for the property declaration pattern, for the move-monitor field declaration line, and for the per-owner comment, plus an ordinal case-sensitive comparison of line 41 against the recorded anchor
EXIT_CODE: 0
Output Summary: One `MoveMonitor` property declaration was added at line 56. The move-monitor field
declaration still occurs exactly once, at line 42, and the load-bearing per-owner comment still sits
at line 41 and is byte-for-byte identical to the same line at the recorded anchor.

## Recorded anchor

BASE_SHA: 8213826f695439e86e3ed34faa575de493a11ec7

## Declaration count

Searched with the anchored pattern `^\s*internal\s+IEmailMoveMonitor\s+MoveMonitor\s*$`, which
matches a property declaration line and cannot match the field, the interface name or the concrete
class name. The bare token would be unsuitable: `MoveMonitor` is a substring of both
`IEmailMoveMonitor` and `EmailMoveMonitor`.

- Matches: 1
- Line 56: `        internal IEmailMoveMonitor MoveMonitor`

## Field-retention check

Searched for the literal field declaration line.

- Matches: 1
- Line 42: `        private IEmailMoveMonitor _moveMonitor = new EmailMoveMonitor();`

The field is retained rather than converted to an auto-property because six existing test methods in
the QuickFiler test project resolve the backing field by reflection under this exact name; an
auto-property would rename it to a compiler-generated name and fail all six.

## Per-owner comment integrity

The comment at line 41 of the recorded anchor was compared against line 41 of the current file with
an ordinal case-sensitive string comparison.

- Result: `IDENTICAL=True`
- Length both sides: 384 characters
- The comment is still the line immediately preceding the field declaration.

## Seam shape

The getter returns the existing field. The setter assigns the value or throws
`ArgumentNullException` when it is null, expressed as a null-coalescing throw over
`nameof(value)`. The XML doc comment records that the default remains the per-owner instance the
field initializer creates, and records why the member is internal rather than public: the
move-monitor interface is itself internal, and a public member of an internal type is an
inconsistent-accessibility error, which is also why a constructor-parameter seam is illegal here.
