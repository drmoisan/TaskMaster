# Policy Compliance Audit — utilitiescs-nullable-extensions (Issue #363)

- Timestamp: 2026-07-19T10-03
- Reviewer: feature-review
- Branch under review: feature/utilitiescs-nullable-extensions-363
- Head: ad78aecc1dc28082b7cbb36fb0031ebf1d0c30a6
- Diff base (merge-base / integration tip): origin/epic/utilitiescs-nullable-remediation-integration (6d4da8bb)
- Diff range: origin/epic/utilitiescs-nullable-remediation-integration...HEAD
- Work mode: full-feature (from issue.md `- Work Mode: full-feature`)
- Change class: annotation-only C# (net481), 23 files under UtilitiesCS/Extensions/, plus docs and agent-memory notes

## Executive Summary

The branch is an annotation-only nullable-reference remediation implementing a per-file
`#nullable enable` opt-in across the UtilitiesCS/Extensions/ directory. Every claim material
to policy compliance was independently re-verified against `git diff` and the two Cobertura
coverage XMLs, not accepted from the executor evidence alone. All C# code-change and unit-test
policy gates pass. Three non-blocking, pre-existing conditions are recorded: the repo-wide
line-coverage figure sits below the 85% uniform floor (a pre-existing repo-wide condition
present at baseline), the canonical `artifacts/csharp/coverage.xml` aggregate artifact is
absent (coverage was verified from the feature-scope Cobertura instead), and
`ArrayExtensions.cs` remains above the 500-line file limit (pre-existing; splitting is
prohibited by the approved architecture). None of these were introduced by this feature and
none are blocking.

Overall policy verdict: PASS (0 blocking findings).

## Scope and Baseline

- Base branch resolved via the diff range supplied by the caller and re-confirmed with
  `git merge-base HEAD origin/epic/utilitiescs-nullable-remediation-integration` = 6d4da8bb.
- The audit scope is the full two-commit branch diff (feature remediation 6c591578 +
  agent-memory chore ad78aecc), not any plan/task/phase subset.
- PR context artifacts (`artifacts/pr_context.summary.txt`, `artifacts/pr_context.appendix.txt`)
  were absent in-session and were regenerated by this reviewer from `git diff --numstat` and
  the full source diff before proceeding.

## Rejected Scope Narrowing

No scope-narrowing instruction was detected in the caller prompt or in
`plan.2026-07-18T21-20.md`. The caller specified the full branch-vs-integration-tip diff and
both in-scope commits, which is the authoritative full-diff scope. The plan's note about a
repo-wide threshold conflict (documented below) is a factual flag for the maintainer, not an
instruction to skip any language or check. Nothing was narrowed; the full branch diff was
audited.

## Evidence Location Compliance

All executor evidence is written under the canonical
`docs/features/active/utilitiescs-nullable-extensions/evidence/{baseline,qa-gates,regression-testing,other}/`
tree. A branch-diff scan for files under `artifacts/baselines/`, `artifacts/qa/`,
`artifacts/evidence/`, or `artifacts/coverage/` returned no matches. No evidence-location
violation was found.

## Section 1 — General Code Change Policy

| # | Requirement | Verdict | Evidence |
|---|---|---|---|
| 1.1 | Simplicity / no behavior change / no refactor | PASS | Diff is limited to `#nullable enable` pragmas, `?` annotations, justified `!`, and `// why` comments; `git diff --shortstat` = 23 files, 167(+)/78(-); no method added/removed/renamed |
| 1.2 | Separation of concerns preserved | PASS | No structural change; no I/O boundary altered |
| 1.3 | Error handling unchanged | PASS | No new `throw`/guard statements added (spec preferred annotation + `!` over new runtime guards); existing guards unchanged |
| 1.4 | File size limit (500 lines) | FAIL (pre-existing, non-blocking) | `ArrayExtensions.cs` = 561 lines at HEAD (544 at baseline). Over-limit condition pre-dates this feature; the approved architecture explicitly prohibits splitting (a refactor). Flagged for a future issue; not a defect introduced here |
| 1.5 | Public API compatibility | PASS | All signature changes are additive nullability annotations (Section 5 below); `WinFormsExtensions.Clone<T>` overloads unchanged (downstream #374 contract) |
| 1.6 | Naming / docs | PASS | Non-obvious `!` sites carry `// why` comments (e.g., WinFormsExtensions GetAncestor, GetEventHandlerList) |

## Section 2 — C# Code Change Policy (toolchain order)

| Stage | Command / gate | Verdict | Evidence |
|---|---|---|---|
| Format (csharpier) | `csharpier format .` then `check .` exit 0 | PASS | evidence/qa-gates/final-csharpier.md (1406 files checked, clean second pass) |
| Analyze (.NET analyzers) | `msbuild ... /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` exit 0 | PASS | evidence/qa-gates/final-analyzers.md (0 errors; no new analyzer diagnostics) |
| Type-check (nullable) | per-file pragma gate `msbuild UtilitiesCS.csproj /t:Rebuild /p:TreatWarningsAsErrors=true` (no /p:Nullable=enable) | PASS | evidence/qa-gates/final-nullable-pragma-gate.md (CS86xx = 0 across all 25 Extensions files) |
| Test (vstest coverage) | full MSTest suite | PASS | evidence/qa-gates/final-tests-coverage.md (5702/5702 passed, exit 0) |

Notes verified independently:
- csproj carries zero `<Nullable>` elements and is not in the diff: `git show HEAD:UtilitiesCS/UtilitiesCS.csproj | grep -c "<Nullable"` = 0; `git diff --name-only ... -- UtilitiesCS/UtilitiesCS.csproj` = empty.
- Each of the 23 remediated files carries exactly one `#nullable enable` pragma (per-file grep = 1).
- The literal solution-wide `/t:Rebuild ... /p:TreatWarningsAsErrors=true` build exits non-zero, but only on pre-existing non-nullable warnings (CS0649 in vendored SVGControl; CS0168/CS0618 in UtilitiesCS) promoted by TreatWarningsAsErrors — CS86xx count is 0. Baseline counts match, confirming zero new diagnostics.

## Section 3 — net481 Constraint Compliance

| Constraint | Verdict | Evidence |
|---|---|---|
| No nullable post-condition attributes introduced | PASS | `git diff` added-line grep for NotNullWhen/MaybeNullWhen/NotNullIfNotNull/MaybeNull/AllowNull/DisallowNull/DoesNotReturn/MemberNotNull = 0 matches; evidence/qa-gates/final-no-postcondition-attrs.md |
| No System.Diagnostics.CodeAnalysis polyfill added | PASS | Added-line grep for `namespace System.Diagnostics.CodeAnalysis` = 0; CompilerServicesExtensions.cs change is a pragma line only in a pre-existing System.Runtime.CompilerServices file |
| ArrayExtensions.cs NOT split | PASS | Single file at HEAD; no sibling `ArrayExtensions.*.cs`; evidence/qa-gates/final-scope-guards.md |
| DfDeedle.EmailRecord remains plain private struct | PASS | `git show HEAD:...DfDeedle.cs` line 239 = `private struct EmailRecord`; no record/record struct/init |

## Section 4 — Unit Test Policy and Coverage (C#)

C# is the only language with changed production files in the branch diff. Coverage is
evaluated from the feature-scope Cobertura XMLs (baseline and post-change), whose repo-wide
figures were re-read directly from the `<coverage>` root elements.

- C# changed-line coverage (AC4, operative feature gate): PASS. Per-file covered-line and
  total-executable-line counts are identical between baseline and post-change for all 23
  remediated files and the 2 verify-only files (delta = 0 for every file; total 2256/2505
  unchanged). Independently confirmed: `lines-valid` is 103321 in both XMLs, proving zero new
  executable lines were added by the annotation-only change. Evidence: evidence/qa-gates/final-coverage-delta.md.
- C# new-file coverage tier: this diff adds 0 new C# source files (every changed `.cs` file
  pre-existed), so the new-file coverage tier has an empty member set and imposes no obligation.
- C# repo-wide branch coverage: PASS. 76.3446% at post-change (76.3368% baseline), at or above
  the 75% branch floor; the delta is a positive within-variance movement, not a regression.
- C# repo-wide line coverage: FAIL against the 85% uniform line floor, dispositioned
  non-blocking. Post-change repo line coverage is 83.7816% (baseline 83.7787%). This shortfall
  is a pre-existing repo-wide condition present at baseline, not caused by this feature; the
  feature added zero executable lines and produced zero regression. The 80%-versus-85% line
  floor divergence between CLAUDE.md (80/90) and .claude/rules (85/75) is a documented
  repo-wide threshold conflict deferred to the maintainer, so this row is a pre-existing
  condition rather than a defect in this change.
- C# canonical aggregate artifact `artifacts/csharp/coverage.xml`: FAIL on mandatory-artifact
  presence, dispositioned non-blocking. The canonical aggregate path is absent; C# coverage was
  instead verified from the feature-scope Cobertura at
  evidence/{baseline,qa-gates}/*-coverage.cobertura.xml, which carry the full repo-wide and
  per-file figures. This is a procedural artifact-location gap, not a code defect; recommend the
  epic capstone produce the canonical aggregate.

Coverage figures re-read directly from the XML roots:
- Baseline: `line-rate="0.837787" branch-rate="0.763368" lines-covered="86561" lines-valid="103321" branches-covered="19530" branches-valid="25584"`.
- Post-change: `line-rate="0.837816" branch-rate="0.763446" lines-covered="86564" lines-valid="103321" branches-covered="19532" branches-valid="25584"`.
- Disposition: no regression on changed lines (AC4 PASS); repo-wide line figure unchanged within variance and pre-existing relative to the 85% floor.

## Section 5 — Public Signature / Cross-Module Contract Safety (AC5)

All public-signature changes are additive nullability annotations reflecting actual null
behavior, verified by reading the diff for every touched public member. Highlights:
- `WinFormsExtensions.Clone<T>` (3 overloads) and `Clone(this RowStyle)` / `Clone(this ColumnStyle)`
  are UNCHANGED (return non-null), preserving the downstream #374 dialogs-misc contract.
- `GetAncestor<T>` returns `T?` (genuine null when no matching ancestor); the internal
  `control.Parent!` preserves the original parentless-control behavior and carries a `// why` comment.
- Batch C/E cross-module contracts (`CastNullSafe`, `ToStringArray`, `SliceColumn`, `To2D`,
  `Find`, `TryFindMax`, `UpdateOrRemove`, dataframe `From*` methods) annotated `T?`/`out TValue?`/
  nullable returns matching existing null handling.

Verdict: PASS.

## Blocking Findings

None. Three non-blocking, pre-existing conditions are recorded above (repo-wide line coverage
below the 85% uniform floor; canonical C# aggregate coverage artifact absent; ArrayExtensions.cs
above the 500-line limit). No remediation-required (blocking) finding exists, so no
remediation-inputs artifact is produced.

## Coverage Verdict Table (per language with changed files)

| Language | Changed files | Repo-wide line | Repo-wide branch | Changed-line (AC4) | Coverage verdict |
|---|---|---|---|---|---|
| C# / .NET (dotnet) | 23 | 83.78% | 76.34% | no regression (delta 0) | PASS on changed-line and branch; FAIL vs 85% line floor and on canonical-artifact presence, both dispositioned non-blocking pre-existing |

Languages with zero changed files on the branch (TypeScript, Python, PowerShell): no changed
files, so no coverage obligation applies.

## Overall Policy Verdict

PASS. Blocking findings: 0.
