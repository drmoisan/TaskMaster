# Code Review — people-tag-window-autotag (Issue #322)

- Timestamp: 2026-07-12T16-35
- Base: `main` @ `3faa0727211bc75741e433f5ef23ba9c9850ea22`
- Head: `bug/people-tag-window-autotag-322` @ `ee49fb15c12b77448ab69ea8307c1426ab6b4dd4`
- Files reviewed: `Tags/TagController.cs`, `TaskVisualization/TaskController.Actions.cs`, `Tags.Test/TagControllerSeamTests.cs`, `TaskVisualization.Test/AutoAssignPeopleTests.cs`, `TaskVisualization.Test/TaskControllerActionsTests.cs`

## Executive Summary

This is a small, well-scoped bugfix: a one-line argument correction in `AssignPeople()` plus a symmetrical branch addition in `TagController.ResolveMailItem` that mirrors an existing pattern in `AutoAssignPeople.AutoFind`. The change is minimal, targeted, and does not introduce new abstractions or touch unrelated code. Root-cause documentation is unusually thorough and precisely cites file:line evidence for both the primary and secondary (blocking) defect. Test additions are isolated, deterministic, and use the required MSTest/Moq/FluentAssertions stack with no live Outlook process or temporary files. One non-blocking test-design gap is noted below: the new `ResolveMailItem` test verifies the method in isolation rather than through the full `TagController` constructor path that production code actually exercises. No blocking findings.

## Findings Table

| Severity | File | Location | Finding | Recommendation | Rationale | Evidence |
|---|---|---|---|---|---|---|
| Low | `Tags.Test/TagControllerSeamTests.cs` | `ResolveMailItem_OutlookItemWrappedMail_ReturnsInnerMailItem` (new test) | The test calls `controller.ResolveMailItem(outlookItem.Object)` directly after constructing the `TagController` with `mailItem: null` (via `BuildWithAutoAssign`), because that helper's `mailItem` parameter is statically typed `MailItem` and cannot accept an `IOutlookItem` mock. As a result, no test exercises the real constructor-driven path (`_olMail = ResolveMailItem(_objItem)` → `_isMail = true` → `SetAutoAssignState` enabling the auto-assign button) for an `IOutlookItem`-wrapped-mail `objItemObject`, unlike the pre-existing raw-`MailItem` case, which is exercised end-to-end via the same helper. | Widen `BuildWithAutoAssign`'s `mailItem` parameter to `object objItemObject` (or add a small overload), pass the `IOutlookItem` mock through the real constructor, and assert `_viewer.AutoAssignVisible == true` (or the `_isMail` field) as an end-to-end check. This is a small, low-risk follow-up and does not need to block this PR. | The isolated call proves `ResolveMailItem`'s return-value logic is correct (and Cobertura confirms the branch executes and is covered), but does not empirically prove the constructor wiring (`_isMail`/button-enablement) that AC4 implicitly depends on for the People flow specifically; that inference currently rests on code reading in `evidence/other/secondary-fix-decision-322.2026-07-12T15-57.md` rather than a direct assertion. | `Tags.Test/TagControllerSeamTests.cs:66-84`; contrast with `ResolveMailItem_ReturnsMailForMailItemAndNullOtherwise` at lines 27-48, which does pass a real `MailItem` through the constructor. |
| Informational | `artifacts/pr_context.summary.txt` | "Changed files overview" section | The summary reports "Core logic changes: 0 files" and omits all 5 changed `.cs` files from its truncated top-10 bullet list, bucketing the entire diff under "Docs/templates/agents/tooling." This is a recurring defect in the PR-context tooling across prior reviews in this repository, not something introduced by this PR. | No action required in this PR. Track as a follow-up to the PR-context generation tooling (see `policy-audit.2026-07-12T16-35.md` `## PR-Context Tooling Defect`). | Confirmed via `git diff --name-status` and `artifacts/pr_context.appendix.txt`'s "Files by extension" (`5 .cs`). | `artifacts/pr_context.summary.txt:117-134`, `artifacts/pr_context.appendix.txt:75-81,123-129`. |
| Informational | `Tags/TagController.cs` | `ResolveMailItem`, lines 101-116 | Naming/design note only: the new `else if` branch and the pre-existing raw-`MailItem` branch both ultimately want "the `MailItem` behind whatever wrapper/raw object was passed," but the first branch returns the cast `_objItem` field rather than the local `objItem` parameter (pre-existing behavior, unchanged by this diff — see the `NOTE (report-only, per spec Non-Goals)` comment in the pre-existing test). The new branch correctly uses the parameter (`olItem.InnerObject`), which is the more defensible pattern. | No change requested; flagged only for visibility. A future cleanup could align the first branch to use the parameter consistently, but that is out of this PR's minimal-fix scope. | Consistency observation; does not affect current behavior since `_objItem` and `objItem` are always the same reference at the call site (`_olMail = ResolveMailItem(_objItem)`). | `Tags/TagController.cs:101-116`. |

## Positive Observations

- The root-cause evidence (`evidence/other/root-cause-322.2026-07-12T15-57.md`) traces the defect through three distinct layers (argument divergence, branch-routing analysis, and the secondary `ResolveMailItem` gate) with exact file:line citations, and explicitly reasons through why fixing only the primary defect would have caused a regression (hidden auto-assign button) — this is exactly the kind of two-defect interaction analysis the bugfix workflow calls for.
- The regression test (`AssignPeople_PassesOutlookItemWrapper_NotInnerObject`) was authored and run to a documented failure before the fix (`evidence/regression-testing/fail-before-322.2026-07-12T15-57.md`, EXIT_CODE 1) and to a documented pass after (`evidence/regression-testing/pass-after-322.2026-07-12T15-57.md`, EXIT_CODE 0), satisfying the bugfix-workflow's fail-first requirement with evidence rather than assertion.
- A coverage gap on the new `ResolveMailItem` branch (`hits="0"` on the branch body, `50% (2/4)` condition-coverage) was discovered mid-Phase-2 and disclosed transparently in `evidence/qa-gates/coverage-delta-322.2026-07-12T15-57.md`'s Notes section, with the closing test added and the rerun documented, rather than silently absorbed.
- No new production files were added, no unrelated files were touched, and all 5 changed `.cs` files remain well under the 500-line limit.

## Verdict

No blocking findings. One Low-severity, non-blocking test-design recommendation and two Informational notes are recorded above; none require remediation before merge.
