# tesseract-engine-initialization-failure (Remediation Plan — Cycle 1)

- **Issue:** #209
- **Branch:** bug/tesseract-engine-initialization-failure-209 (base: main) — remediation cycle 1, continuing on the same branch as the prior completed Phase 0–2 implementation.
- **Mode:** minor-audit (remediation_pass: 1)
- **Primary context (sole requirements source for this cycle):** `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/remediation-inputs.2026-07-18T17-42.md` — the single Blocking finding it documents (0% line coverage, `UtilitiesCS/EmailIntelligence/EmailParsingSorting/TesseractOcrTextExtractor.cs`, 0 of 13 executable lines hit).
- **Prior AC context (informational only, not re-litigated this cycle):** `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/issue.md` `## Acceptance Criteria` (AC1–AC5) — already satisfied per the prior cycle; no rework required.
- **Last Updated:** 2026-07-18T17-42
- **Status:** Not started

**Evidence location (non-overridable):** All evidence artifacts for this remediation cycle MUST be written under `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/<kind>/` (canonical sub-kinds used below: `remediation-baseline/`, `qa-gates/`). No artifact in this plan may be written to any `artifacts/` path. All evidence filenames in this cycle use the `remediation1-` prefix to disambiguate from the prior cycle's artifacts in the same folders.

**Fail-closed rule:** Every evidence-producing task below must produce its artifact with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` fields before its checkbox may be marked complete. Do not mark a task complete without its artifact on disk.

**Timestamp convention:** `<TIMESTAMP>` below denotes the ISO-8601 timestamp (`yyyy-MM-ddTHH-mm`) captured at the moment each artifact is written; each artifact file name and its internal `Timestamp:` field must use the same value.

**Full first-party MSTest assembly set** (identical set used in Phase 0 baseline and Phase 2 final-QC for a true before/after comparison, matching the prior cycle's set):
- `ToDoModel.Test\bin\Debug\ToDoModel.Test.dll`
- `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`
- `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`
- `TaskVisualization.Test\bin\Debug\TaskVisualization.Test.dll`
- `Tags.Test\bin\Debug\Tags.Test.dll`
- `TaskTree.Test\bin\Debug\TaskTree.Test.dll`
- `VBFunctions.Test\bin\Debug\VBFunctions.Test.dll`
- `TaskMaster.Test\bin\Debug\TaskMaster.Test.dll`

The full-suite MSTest+coverage command used in both Phase 0 and Phase 2 is the repo-standard wrapper `scripts\vscode\Invoke-MSTestWithCoverage.ps1`, which discovers all `*.Test.dll` under `-SearchRoot` and drives `vstest.console.exe` (via `dotnet-coverage collect`, satisfying the CUT3 `vstest.console.exe ... /EnableCodeCoverage` requirement with a Cobertura-format coverage artifact) with `/InIsolation`.

**Target file for this cycle:** `UtilitiesCS/EmailIntelligence/EmailParsingSorting/TesseractOcrTextExtractor.cs`, class `UtilitiesCS.EmailIntelligence.TesseractOcrTextExtractor`.

---

### Phase 0 — Baseline Capture (Target-File Coverage State)

- [x] [P0-T1] Read `CLAUDE.md` in full (policy reading order position 1) before any other action in this session.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md` in full (policy reading order position 2).
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md` in full (policy reading order position 3).
- [x] [P0-T4] Read `.claude/rules/csharp.md` in full (policy reading order position 4, C#-specific toolchain and testing standard for this change).
- [x] [P0-T5] Read `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/remediation-inputs.2026-07-18T17-42.md` in full and confirm: (a) there is exactly one Blocking finding, (b) it concerns 0% line coverage (0 of 13 executable lines) in `UtilitiesCS/EmailIntelligence/EmailParsingSorting/TesseractOcrTextExtractor.cs`, and (c) the directed remediation is Option A (extract the pure tessdata-path-resolution logic into a directly-testable member and add a test for it).
- [x] [P0-T6] Read `UtilitiesCS/Properties/AssemblyInfo.cs` and confirm `[assembly: InternalsVisibleTo("UtilitiesCS.Test")]` is present (currently line 17); record that this permits the extracted member to be declared `internal static` (not `public static`) while remaining directly testable from `UtilitiesCS.Test`.
- [x] [P0-T7] Write the Phase 0 policy-read evidence artifact at `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/remediation-baseline/remediation1-phase0-instructions-read.<TIMESTAMP>.md` containing `Timestamp:`, `Policy Order:` (the four-item order from P0-T1–P0-T4), and the explicit file list read in P0-T1–P0-T6.
- [x] [P0-T8] On branch `bug/tesseract-engine-initialization-failure-209`, before making any remediation changes, run the full-suite MSTest-with-coverage pass across all eight first-party `*.Test.dll` assemblies listed above to establish this remediation cycle's own baseline snapshot: `pwsh -File scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput 'docs\features\active\2026-06-19-tesseract-engine-initialization-failure-209\evidence\remediation-baseline\remediation1-coverage-baseline.cobertura.xml'`, redirecting the full console output via `Tee-Object` to `docs\features\active\2026-06-19-tesseract-engine-initialization-failure-209\evidence\remediation-baseline\remediation1-mstest-baseline-console.<TIMESTAMP>.log`; capture `EXIT_CODE` and the Total/Passed/Failed/Skipped counts. This command must execute unconditionally; `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P0-T9] From `remediation1-coverage-baseline.cobertura.xml`, locate the `<class ...>` element whose `filename` attribute is `UtilitiesCS\EmailIntelligence\EmailParsingSorting\TesseractOcrTextExtractor.cs` and record its `line-rate`, `lines-covered`, and `lines-valid` attributes (or equivalent hit/total line counts) as this cycle's target-file baseline coverage. Expected value going in: `line-rate="0"`, 0 of 13 lines covered.
- [x] [P0-T10] Write the baseline evidence artifact at `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/remediation-baseline/remediation1-baseline-coverage-target.<TIMESTAMP>.md` with `Timestamp:`, `Command:` (from P0-T8), `EXIT_CODE:`, and `Output Summary:` containing: Total/Passed/Failed/Skipped counts (from P0-T8), the repo-wide baseline line-rate/branch-rate from the coverage file's root `<coverage>` element, and the target-file-specific baseline coverage (from P0-T9).

---

### Phase 1 — Constrained Implementation (Extraction + Test)

- [x] [P1-T1] In `UtilitiesCS/EmailIntelligence/EmailParsingSorting/TesseractOcrTextExtractor.cs`, inside the `internal sealed class TesseractOcrTextExtractor`, add a new member directly above `ExtractText`: `internal static string ResolveTessdataPath() => $"{Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData)}{Path.DirectorySeparatorChar}TaskMaster{Path.DirectorySeparatorChar}tessdata";` — the exact interpolated-string expression currently assigned to the `tessdataPath` local in `ExtractText`, moved verbatim into this new expression-bodied method.
- [x] [P1-T2] In the same file, replace the `tessdataPath` assignment line inside `ExtractText(Bitmap bitmap)` (`string tessdataPath = $"{Environment.GetFolderPath(...)}...";`) with `string tessdataPath = ResolveTessdataPath();`. No other line in `ExtractText` (the `using (TesseractEngine engine = ...)` block, `engine.Process(bitmap)`, or `page.GetText()`) changes.
- [x] [P1-T3] Add a new test file `UtilitiesCS.Test/EmailIntelligence/EmailParsingSorting/TesseractOcrTextExtractor_Tests.cs` containing `[TestClass] public class TesseractOcrTextExtractor_Tests` with one `[TestMethod] public void ResolveTessdataPath_ReturnsLocalAppDataTaskMasterTessdataPath()` that: (Arrange) computes `string expected = $"{Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData)}{Path.DirectorySeparatorChar}TaskMaster{Path.DirectorySeparatorChar}tessdata";`; (Act) calls `string actual = TesseractOcrTextExtractor.ResolveTessdataPath();`; (Assert) `actual.Should().Be(expected);` via FluentAssertions. The test constructs no `TesseractEngine`, opens no file, and creates no temporary file.
- [x] [P1-T4] Add `<Compile Include="EmailIntelligence\EmailParsingSorting\TesseractOcrTextExtractor_Tests.cs" />` to `UtilitiesCS.Test/UtilitiesCS.Test.csproj` (legacy `packages.config`, non-SDK project with explicit `<Compile Include>` wiring and no glob-based inclusion; place adjacent to the existing `<Compile Include="EmailIntelligence\EmailParsingSorting\...` entries). The new test file will not compile into the assembly without this entry.
- [x] [P1-T5] [Extraction verification] Read the modified `UtilitiesCS/EmailIntelligence/EmailParsingSorting/TesseractOcrTextExtractor.cs` and confirm: (a) `ResolveTessdataPath()` is declared `internal static string ResolveTessdataPath()` with no parameters and no side effects; (b) `ExtractText(Bitmap bitmap)` calls `ResolveTessdataPath()` and no longer contains the inline interpolated tessdata-path expression; (c) the `TesseractEngine` construction, `engine.Process(bitmap)`, and `page.GetText()` calls are byte-identical to the pre-remediation version (no behavior change to the native-engine path).
- [x] [P1-T6] [Accessibility verification] Confirm `TesseractOcrTextExtractor_Tests.cs` (added in P1-T3) references `TesseractOcrTextExtractor.ResolveTessdataPath()` directly (no reflection, no visibility workaround) and that this resolves under the existing `InternalsVisibleTo("UtilitiesCS.Test")` grant confirmed in P0-T6, with no accessibility-modifier change required to either project.

---

### Phase 2 — Final QC (Full Toolchain + Target-File Coverage Delta)

- [x] [P2-T1] Run CSharpier format: `dotnet tool run csharpier .` (or `csharpier .`); capture `EXIT_CODE` and the list of any files it reformatted.
- [x] [P2-T2] If P2-T1 reformatted any file, re-run `dotnet tool run csharpier .` until one pass completes with `EXIT_CODE 0` and zero files changed, before proceeding to P2-T3.
- [x] [P2-T3] Write the final-QC CSharpier evidence artifact at `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/qa-gates/remediation1-final-csharpier.<TIMESTAMP>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P2-T4] Run the analyzer build: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; capture `EXIT_CODE` and error/warning counts.
- [x] [P2-T5] Write the final-QC analyzer-build evidence artifact at `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/qa-gates/remediation1-final-analyzer-build.<TIMESTAMP>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P2-T6] Run the nullable/type-check build: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`; capture `EXIT_CODE`.
- [x] [P2-T7] Write the final-QC nullable-build evidence artifact at `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/qa-gates/remediation1-final-nullable-build.<TIMESTAMP>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P2-T8] Run the post-remediation full-suite MSTest-with-coverage pass across the same eight first-party `*.Test.dll` assemblies: `pwsh -File scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput 'docs\features\active\2026-06-19-tesseract-engine-initialization-failure-209\evidence\qa-gates\remediation1-coverage-final.cobertura.xml'`, redirecting console output via `Tee-Object` to `docs\features\active\2026-06-19-tesseract-engine-initialization-failure-209\evidence\qa-gates\remediation1-mstest-final-console.<TIMESTAMP>.log`; capture `EXIT_CODE`. This command must execute unconditionally; `EXIT_CODE: SKIPPED` is not a valid outcome for this task.
- [x] [P2-T9] [Regression check] From `remediation1-mstest-final-console.<TIMESTAMP>.log`, record the post-change Total/Passed/Failed/Skipped counts and compare against the P0-T8 baseline counts for the identical eight-assembly set: confirm `Failed` is unchanged (`0`), confirm `Total`/`Passed` increased by exactly 1 (the new `ResolveTessdataPath_ReturnsLocalAppDataTaskMasterTessdataPath` test added in P1-T3), and confirm no other test changed status. If any unexplained new failure or unexplained count change exists, record this task's outcome as FAILED.
- [x] [P2-T10] From `remediation1-coverage-final.cobertura.xml`, locate the `<class ...>` element whose `filename` attribute is `UtilitiesCS\EmailIntelligence\EmailParsingSorting\TesseractOcrTextExtractor.cs` and record its post-change `line-rate`, `lines-covered`, and `lines-valid` attributes.
- [x] [P2-T11] [Target-file coverage delta — primary remediation verification] Compare the P2-T10 post-change target-file `line-rate`/`lines-covered` against the P0-T9 baseline target-file values. Confirm the post-change `line-rate` for `TesseractOcrTextExtractor.cs` is strictly greater than the baseline `0`, and record the exact before/after percentage and covered-line-count delta (the `ResolveTessdataPath()` lines are now expected to be hit; the native `TesseractEngine` construction, `Process`, and `GetText()` lines are expected to remain uncovered, which is the accepted, documented residual per the remediation-inputs Option A guidance). If the post-change `line-rate` is still `0`, record this task's outcome as FAILED.
- [x] [P2-T12] From the same coverage file's root `<coverage>` element, record the post-change repo-wide `line-rate`/`branch-rate` and compute the delta against the P0-T10 repo-wide baseline values; confirm no coverage regression beyond the small-magnitude noise band already documented in this feature's prior-cycle evidence.
- [x] [P2-T13] Write the final-QC MSTest/coverage evidence artifact at `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/qa-gates/remediation1-final-mstest.<TIMESTAMP>.md` with `Timestamp:`, `Command:` (from P2-T8), `EXIT_CODE:`, and `Output Summary:` containing: post-change test counts and the regression check (P2-T9), the target-file before/after coverage comparison (P2-T11), and the repo-wide before/after coverage comparison (P2-T12).
- [x] [P2-T14] [Toolchain regression re-verification] Confirm P2-T3 (CSharpier), P2-T5 (analyzer build), and P2-T7 (nullable build) all recorded `EXIT_CODE: 0` with zero analyzer errors and zero nullable-warnings-as-errors, and that P2-T9 recorded zero unexplained test regressions; write the confirmation to `docs/features/active/2026-06-19-tesseract-engine-initialization-failure-209/evidence/qa-gates/remediation1-final-regression-verification.<TIMESTAMP>.md` with `Timestamp:` and references to the P2-T3/P2-T5/P2-T7/P2-T13 artifacts. If any referenced task recorded a non-zero `EXIT_CODE` or an unexplained test regression, record this task's outcome as FAILED and do not report this remediation cycle as resolved.
- [x] [P2-T15] If any of P2-T1, P2-T4, P2-T6, or P2-T8 failed or changed files on its most recent run, restart the full loop from P2-T1; do not report this remediation cycle as complete until a single pass completes P2-T1 through P2-T14 with no failing or file-changing step.
