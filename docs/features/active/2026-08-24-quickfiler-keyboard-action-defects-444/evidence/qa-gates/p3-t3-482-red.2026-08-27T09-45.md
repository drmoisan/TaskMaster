# [P3-T3] `[expect-fail]` #482 interleaving test — RED before the fix

Timestamp: 2026-08-27T09-45
Command: `& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~QfcItemController_NavigationTests.ToggleExpansion_WhenAsyncOnThenSyncOffThenAsyncOn_DoesNotThrowAndBothRegistriesHoldOneBAndOneD"`
EXIT_CODE: 1
ExpectedExitCode: 1

A failing test is the expected outcome for this task only. This is the fail-before half of
`CLAUDE.md`'s Bugfix Workflow step 1 for issue #482.

## Result (verbatim)

```
Failed ToggleExpansion_WhenAsyncOnThenSyncOffThenAsyncOn_DoesNotThrowAndBothRegistriesHoldOneBAndOneD [459 ms]

Total tests: 1
     Failed: 1
Test Run Failed.
```

| Measure | Value |
| --- | --- |
| Total | 1 |
| Passed | **0** |
| Failed | **1** |

## Recorded failure (verbatim, workspace root substituted)

```
Did not expect System.ArgumentException, but found System.ArgumentException: Cannot add key because it
already exists. Key B SourceId expansion-entry
   at QuickFiler.Controllers.KbdActions`3.Add(String sourceId, TKey key, VDelegate delegate) in <repo-root>\QuickFiler\Controllers\KbdActions.cs:line 135
   at QuickFiler.Controllers.QfcItemController.RegisterExpandedAsyncActions() in <repo-root>\QuickFiler\Controllers\QfcItemController.EventWiring.cs:line 322
   at QuickFiler.Controllers.QfcItemController.<ToggleExpansionAsync>d__212.MoveNext() in <repo-root>\QuickFiler\Controllers\QfcItemController.Navigation.cs:line 198
```

The exception is an **`ArgumentException` raised on the third step**, which is what the acceptance
condition requires. The stack trace confirms the mechanism the issue describes:

1. Step 1, `ToggleExpansionAsync(On)`, set `_expanded = true` and added `'B'`/`'D'` to
   `CharActionsAsync` through `RegisterExpandedAsyncActions`.
2. Step 2, `ToggleExpansion(Off)`, cleared `_expanded` and called `UnregisterExpandedActions`, which
   removes from `CharActions` — a registry that never held the entries. `KbdActions.Remove` returned
   `false` silently, so nothing surfaced. `CharActionsAsync` still held `'B'` and `'D'`.
3. Step 3, `ToggleExpansionAsync(On)`, saw `_expanded == false`, ran the On branch, and
   `RegisterExpandedAsyncActions` re-added `'B'` into a registry that already held it.

## Acceptance evaluation

- The run reports `Failed: 1` and `Passed: 0`. PASS.
- The recorded failure is an `ArgumentException` raised on the third step. PASS.

Output Summary: 1 test run, 1 failed, 0 passed; the failure is `ArgumentException` ("Cannot add key
because it already exists. Key B") thrown from `RegisterExpandedAsyncActions` on the third step, the
exact divergence issue #482 files.
