# [P2-T3] `[expect-fail]` #472 width-fidelity tests — RED before the fix

Timestamp: 2026-08-27T09-45
EXIT_CODE: 1
ExpectedExitCode: 1

A failing test is the expected outcome for this task only. This is the fail-before half of
`CLAUDE.md`'s Bugfix Workflow step 1 for issue #472.

## Build

Command: `& $msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU"`
Result: exit code **0**, `5 Warning(s)`, `0 Error(s)`.

## Test run

Command:

```
& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~QfcCollectionControllerNavigationDigitsTests.UnregisterNavigation_AfterRegisteringAtTwoDigitsAndShrinkingToNine_RemovesTheTwoDigitKeys|FullyQualifiedName~QfcCollectionControllerNavigationDigitsTests.UnregisterNavigation_AfterRegisteringAtOneDigitAndGrowingToTen_RemovesTheOneDigitKeys"
```

Result (verbatim):

```
Failed UnregisterNavigation_AfterRegisteringAtTwoDigitsAndShrinkingToNine_RemovesTheTwoDigitKeys [355 ms]
Failed UnregisterNavigation_AfterRegisteringAtOneDigitAndGrowingToTen_RemovesTheOneDigitKeys [1 ms]

Total tests: 2
     Failed: 2
Test Run Failed.
```

| Measure | Value |
| --- | --- |
| Total | 2 |
| Passed | **0** |
| Failed | **2** |

## Recorded failure reasons (verbatim)

Shrink direction:

```
Expected remaining.Where(k => k.StartsWith("0", StringComparison.Ordinal)) to be empty because the
recorded registration width is replayed, so the '0'-prefixed keys go, but found at least one item {"01"}.
```

Grow direction:

```
Expected CollectionKeys(registry) to be empty because the recorded width 1 is replayed and the grown
loop bound reaches every registered key, but found at least one item {"1"}.
```

Both failures are the filed defect, reached through the assertion rather than through an arrangement
error. In the shrink direction the page registered `"01".."10"` at width 2, the count then dropped to
nine, and the live `Digits` getter evaluated to 1, so `UnregisterNavigation` removed the
never-registered `"1".."9"` and left `"01"` (and the rest of the `"0"`-prefixed set) orphaned. In the
grow direction the page registered `"1".."9"` at width 1, the count then grew to ten, `Digits`
evaluated to 2, and the loop removed the never-registered `"01".."10"`, leaving `"1"` orphaned.

## Acceptance evaluation

- The build reports exit code `0`. PASS.
- The run reports `Failed: 2` and `Passed: 0`. PASS.

Output Summary: build exit 0; 2 tests run, 2 failed, 0 passed; both failures are orphaned
registrations left by the width mismatch, in both the shrink and the grow direction.
