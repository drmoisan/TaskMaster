# quickfiler-teardown-and-dropdown-residuals (Plan)

- **Issue:** #810
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-07T21-59
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** `full-bug`. `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` is the sole authoritative acceptance-criteria source. `user-story.md` is intentionally absent and must stay absent.

**Fail-closed evidence rule:** every baseline artifact, QA-gate artifact and coverage-comparison artifact named below is mandatory. If any is missing, or any required field in it is absent, the verdict is BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** every evidence-producing task names its artifact path on its own task line. Do not check a task off without its artifact on disk carrying `Timestamp:`, `Command:`, `EXIT_CODE:` and `Output Summary:`.

## Decisions Record (binding on every task below)

- **D1 — Evidence location.** Every artifact resolves under `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/<kind>/` with `<kind>` in `baseline`, `regression-testing`, `qa-gates`, `issue-updates`, `other`. Paths under `artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/regression-testing/` and `artifacts/post-change/` are forbidden and must be rejected if any caller supplies one.
- **D2 — Diff anchor.** Every `git diff` and `git grep` comparison in this plan carries the explicit ref operand `origin/main`. No unanchored diff is permitted. `git rev-parse origin/main` is recorded once, in `evidence/baseline/p0-t2-branch-and-base.md`, as `BASE-SHA`.
- **D3 — No variable survives between tasks.** Every task that needs `vstest.console.exe` re-binds it inside its own command block with the two `vswhere` resolution lines pinned by [P0-T7]. Every task that needs the base SHA reads it from the [P0-T2] artifact or writes `origin/main` literally.
- **D4 — Toolchain order.** Format, then analyzers, then nullable, then tests with coverage. Restart from the format step whenever any step fails or changes a file. `/t:Rebuild` always; `/t:Build` never, because MSBuild's up-to-date check does not invalidate on a command-line `/p:` change and a warm `/t:Build` returns exit 0 with `CoreCompile` skipped on every project. `/p:Nullable=enable` is never added, because no project carries a `<Nullable>` element and CI omits it deliberately.
- **D5 — Outlook.** No task launches Outlook. If a rebuild reports MSB3061 or MSB3021 file-lock warnings, that is an Outlook or test-host process holding the build output: record the condition in the task's artifact and stop. Do not terminate any process.
- **D6 — No new script files.** This plan authors no `.ps1` and no `.psm1` file. Every step is expressible with `dotnet`, `msbuild`, `dotnet-coverage`, `vstest.console.exe`, `git`, and the Read/Grep/Glob/Edit/Write tools.
- **D7 — Commit pathspec.** Every `git commit` in this plan ends with `--` followed by one or more explicit paths. A commit with zero pathspec operands is denied by the pre-implementation gate.
- **D8 — Owned by a concurrent run on issue 809; MUST NOT be edited on this branch.** `QuickFiler.Test/Controllers/QfcHomeControllerRunAsyncTests.cs`, `TaskMaster/ThisAddIn.cs`, `UtilitiesCS/Threading/UiThread.cs`. None is required by any task here. If any task appears to require one, stop and report the collision rather than editing it.
- **D9 — The AC2 fence.** `QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs` must stay byte-unmodified for the whole plan. Its tests remaining green while the file is unmodified is itself the AC2 evidence.
- **D10 — Local hang set.** Four `UtilitiesCS.Test` shell-icon classes stall `vstest` on this workstation. Every repository-wide run in this plan carries the four `FullyQualifiedName!~` exclusion clauses recorded in [P0-T12]. Scoped `QuickFiler.Test` runs do not need them, because the four classes live in `UtilitiesCS.Test`.
- **D11 — Coverage instrument.** `dotnet-coverage collect --output-format cobertura` wrapping `vstest.console.exe`, not `vstest /EnableCodeCoverage`, because `/EnableCodeCoverage` writes a binary `.coverage` file from which no numeric line rate can be read and the two collectors conflict. The derived settings file `coverage/810-effective-coverage.config` is written by [P0-T12] and reused unchanged by [P7-T5], so both sides of the comparison come from one collector, one configuration, one assembly selection and one filter.
- **D12 — Gitignored run outputs.** `coverage/` is ignored by `.gitignore:144` (`coverage/*`) and `TestResults/` by `.gitignore:39` (`[Tt]est[Rr]esult*/`). Raw Cobertura documents and TRX files are local run outputs and are never committed. Only parsed values are transcribed into the markdown artifacts under D1.
- **D13 — No TRX content is reproduced in any artifact.** A TRX carries `runUser` and `computerName` attributes and its default filename embeds both. Artifacts record parsed counters only.
- **D14 — No absolute host path in any artifact,** with one exception: `evidence/baseline/p0-t7-vstest-resolution.md` records the resolved `vstest.console.exe` path in full, because pinning it is that task's entire purpose and it is a `Program Files` installation path carrying no user-profile segment and no machine name.
- **D15 — The AC1 parameter is required, not defaulted.** `QuickFiler/Controllers/QfcFormController.EventHandlers.cs:144` converts the method group `ParkFocusAndCancelSelectors` to `System.Action`, and C# method-group conversion does not apply optional-argument defaults, so an optional parameter yields CS0123. The teardown call site becomes an explicit lambda. The stage-name string literal `"park-focus"` must not change: it is the value `QfcFormControllerCancelTeardownTests.MarkerParkFocus` at `QuickFiler.Test/Controllers/QfcFormControllerCancelTeardownTests.cs:35` compares against.
- **D16 — Prohibited changes (issue-677 keyboard-lock contract).** Deleting the guard at `QuickFiler/Controllers/QfcFormController.Deactivate.cs:118` or making its condition unconditionally false; inverting the polarity of `IsDeactivationSelfInflictedByOwnPopup`; moving the guard above the focus-parking step at `:100-103`; touching `MayTakeFocus`, `FocusPending()`, `FocusAnchorIfPermitted()` or the `ItemViewer.Breadcrumb.cs` predicate wiring; removing `ParkFocusOffWebView2()` from either path; removing or reordering any `ActionCancelAsync` teardown stage. If any of these appears necessary, stop and report rather than making it.
- **D17 — Literals this plan instructs the executor to create.** Each is quoted verbatim here so a later search for it is a real assertion rather than a search for text no task creates: `bool honourSelfInflictedGuard`; `&& _formViewer?.IsDeactivationSelfInflictedByOwnPopup == true`; `ParkFocusAndCancelSelectors(honourSelfInflictedGuard: true)`; `() => ParkFocusAndCancelSelectors(honourSelfInflictedGuard: false)`; `_tokenSource = null;`; `_datamodel = null;`; `System.Action parentCleanup = _parentCleanup;`; `() => IsCommitPending = false`; `The two gates are independent`; `also clears it as its final operation`; `_breadcrumbPopupOwners.AnyOpen`; `new BreadcrumbPopupOwnerRegistry()`; `using QuickFiler.Viewers;`; `Viewers\BreadcrumbPopupOwnerRegistry.cs`; `Viewers\BreadcrumbPopupOwnerRegistryTests.cs`; `ActionCancelAsync_SelfInflictedByOwnPopup_StillCancelsEverySelector`; `Cleanup_NullsTokenSourceSoLaterCancelCannotReachDisposedSource`; `Cleanup_ViewerDisposeThrows_StillInvokesParentCleanupOnce`; `RestoreAfterOpenFailure_WithStaleCommitPending_StillCancelsAndClearsLatch`. The tokens this plan requires to disappear are `the cancel step above always runs`, `and nothing else clears it`, `SearchOwnsDropDownDismissal`, `_breadcrumbPopupOwners.Values.Any` and `using System.Linq;`. The last is scoped to one file and to one file only: [P6-T6] removes it from `QuickFiler/Viewers/QfcFormViewer.cs`, where it is present at `:7` and where `:246` is its only consumer, and every acceptance clause asserting its absence carries that pathspec. It remains present and required in other files across the repository and no task here removes it from any of them.
- **D18 — Wrap tolerance.** CSharpier's print width is 100 columns and it does not reflow comments. Every asserted source literal in this plan is short enough to survive on one line in both the wrapped and the unwrapped rendering of the statement that carries it. Do not substitute a longer whole-statement literal for one of these tokens: a whole-statement assertion on `if (honourSelfInflictedGuard && ...)` or on `RunTeardownStage("park-focus", () => ...)` is unsatisfiable, because both exceed 100 columns at their indentation and CSharpier splits them across lines.
- **D19 — What the AC5 clear does and does not do.** Clearing `IsCommitPending` as the final operation of `FinishClose` does not make the cancel run on the same call that reads a stale latch: the read at `QuickFiler/Viewers/BreadcrumbDropDownHost.cs:447` is an earlier operation in the same `CompleteAll` list. It fixes the defect by ensuring the latch cannot survive the close that consumed it, so the next close, including the one `RestoreAfterOpenFailure` performs, sees a cleared latch and cancels. The AC5 regression test must therefore drive two closes, not one.
- **D20 — Platform spelling asymmetry between solution-scoped and project-scoped builds.** Every solution-scoped `msbuild TaskMaster.sln` command in this plan — [P0-T4], [P0-T9], [P0-T10] and every task that reuses one of them — keeps `"/p:Platform=Any CPU"` with the space and the surrounding quotes exactly as CLAUDE.md and `.claude/rules/csharp.md` require. The direct-project command in [P1-T2] uses `/p:Platform=AnyCPU`, one word, no space, no quotes around the switch. The asymmetry is correct and must not be reconciled in either direction. Grounds: `QuickFiler.Test/QuickFiler.Test.csproj` keys its configuration property groups off `'$(Configuration)|$(Platform)'` and declares only four — `Debug|AnyCPU` at `:32`, carrying `<OutputPath>bin\Debug\</OutputPath>` at `:36`, `Release|AnyCPU` at `:41`, `Debug|x86` at `:49` and `Release|x86` at `:53` — and sets its own default spelling with `<Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>` at `:12`. There is no `Debug|Any CPU` group. The spaced name is a solution-level platform, declared at `TaskMaster.sln:52` and mapped onto this project's GUID `{834DE5B0-03C8-4483-A4BC-A7975064C5A2}` at `TaskMaster.sln:144-145`; MSBuild applies that mapping only when the solution is the entry point. Pointed straight at the `.csproj` no solution is loaded, no mapping occurs, no property group matches, `OutputPath` is never set, and the build fails in `_CheckForInvalidConfigurationAndPlatform` before `CoreCompile`, so the spaced spelling cannot exit 0 on any tree. A direct `/t:Rebuild` transitively builds the project's `ProjectReference` dependencies, so no solution context is required to produce the test assembly.

## Write Set (every path this plan creates or modifies)

Production: `QuickFiler/Controllers/QfcFormController.Deactivate.cs`, `QuickFiler/Controllers/QfcFormController.EventHandlers.cs`, `QuickFiler/Controllers/QfcFormController.SetupDisposal.cs`, `QuickFiler/Controllers/QfcHomeController.cs`, `QuickFiler/Controllers/QfcItemController.EventHandlers.cs`, `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs`, `QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` (new), `QuickFiler/Viewers/QfcFormViewer.cs`, `QuickFiler/QuickFiler.csproj`.

Test: `QuickFiler.Test/Controllers/QfcFormControllerCancelTeardownTests.cs`, `QuickFiler.Test/Controllers/QfcFormControllerCleanupTests.cs`, `QuickFiler.Test/Controllers/QfcHomeControllerCleanupTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbDropDownCloseOrderingTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs` (new), `QuickFiler.Test/QuickFiler.Test.csproj`.

Documents: `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/plan.2026-09-07T21-59.md`, `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md`, `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`.

Inherited paths, defined as a rule rather than as a closed list, because the branch advances between authoring and execution and the executor's own agent-memory writes land in a tracked tree this plan does not own. A path is inherited when either clause holds. Clause A: it is already changed relative to `origin/main` before [P0-T1] runs. Clause B: it lies under `.claude/agent-memory/`. The eight paths satisfying clause A at the time this plan was written are `.claude/agent-memory/atomic-planner/MEMORY.md`, `.claude/agent-memory/atomic-planner/project_810_teardown_dropdown_residuals_plan_seams.md`, `.claude/agent-memory/task-researcher/MEMORY.md`, `.claude/agent-memory/task-researcher/project_qfc810_teardown_dropdown_residuals.md`, `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/plan.2026-09-07T21-59.md`, `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/research/research.2026-09-07T22-10.md`, `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md`. That list is recorded for orientation and is not the authority: [P0-T2] captures the clause-A set mechanically at execution time and [P7-T11] subtracts the captured set, not this list. Three of the eight — `issue.md`, `spec.md` and `plan.2026-09-07T21-59.md` — are also Write Set members and are modified by later tasks, so their appearance in the final diff is expected under the Write Set rather than under the inherited rule. `git diff --name-only origin/main` reports the clause-A set on a tree with no task applied, because the branch head is ahead of the base commit `04a54e681bd21e841e124c016df30672ee701b75` by the preparation commits that added the feature-folder documents and the agent-memory entries. No task in this plan edits any clause-A or clause-B path other than the three Write Set documents.

Gitignored working file: `coverage/810-effective-coverage.config`.

Evidence artifacts: every path named on a task line below.

### Phase 0 — Baseline capture and worktree bootstrap

- [x] [P0-T1] Read the policy files in the required order and record the read into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/phase0-instructions-read.md`. Read, in this order: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, `.claude/rules/plan-acceptance-gates.md`. Acceptance: the artifact exists and carries `Timestamp:`, a `Policy Order:` line naming the seven files in that order, and an explicit bulleted list of the seven files read, one per line.
- [x] [P0-T2] Record the branch, the base commit and the inherited-path set into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t2-branch-and-base.md`. Run `git rev-parse --abbrev-ref HEAD`, `git rev-parse origin/main`, and `git merge-base --is-ancestor origin/main HEAD`. If the first does not print `bug/quickfiler-teardown-and-dropdown-residuals-810`, run `git switch -c bug/quickfiler-teardown-and-dropdown-residuals-810 origin/main` and re-run all three. If the third exits non-zero, run `git rebase origin/main` and re-run all three. Then run `git diff --name-only origin/main` and `git status --porcelain --untracked-files=all` and transcribe the union of their paths as the clause-A inherited set, this being the state of the tree before any task of this plan has edited a source file. The porcelain span is present because a name-listing diff cannot report an untracked path. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `BRANCH: bug/quickfiler-teardown-and-dropdown-residuals-810`, `BASE-SHA:` followed by a 40-character hexadecimal SHA, `ANCESTOR-CHECK: 0`, and an `INHERITED-CLAUSE-A:` block listing one path per line with `INHERITED-CLAUSE-A-COUNT:` set to the number of lines in it. This capture, not the orientation list in the Write Set, is what [P7-T11] subtracts.
- [x] [P0-T3] Verify the repository-local .NET SDK resolves and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t3-dotnet-sdk.md`. Run `dotnet --version` from the worktree root. `global.json` pins `sdk.version 8.0.205` with `rollForward: latestFeature` and `paths` naming `.dotnet-sdk`, so a worktree without `.dotnet-sdk` prints the `global.json` `errorMessage` instead of a version. If it does, run `pwsh -NoProfile -File ./scripts/vscode/Install-RepoDotNetSdk.ps1` from the worktree root and re-probe. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `SDK-VERSION:` set to the printed version, and `BRANCH-TAKEN:` set to `probe-only` or `installed`.
- [x] [P0-T4] Restore NuGet packages for `TaskMaster.sln` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t4-nuget-restore.md`. Run `msbuild TaskMaster.sln /t:Restore /m /p:RestorePackagesConfig=true /p:Configuration=Debug "/p:Platform=Any CPU"`. This is the `packages.config`-aware restore; without it every `<Reference>` `HintPath` under `packages/` and every `<Analyzer Include>` is unresolvable and the analyzer build fails with CS0006, which reads like a source defect. Then enumerate every `<Analyzer>` `Include` value declared by `QuickFiler/QuickFiler.csproj` and `QuickFiler.Test/QuickFiler.Test.csproj` and test each one for existence, resolved against the declaring project's own directory. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, one `RESOLVED:` line per analyzer item for both projects, and `UNRESOLVED-COUNT: 0`.
- [x] [P0-T5] Restore the manifest-pinned local tools and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t5-dotnet-tool-restore.md`. Run `dotnet tool restore` from the worktree root. The manifest is the repository-root file `dotnet-tools.json`, not `.config/dotnet-tools.json`, which does not exist in this checkout; the `dotnet` CLI accepts both locations, so the root file is the one restored. It pins CSharpier 1.2.6 with a single command name `csharpier`, and CSharpier 1 requires a subcommand, so the bare-path form does not run. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, and `CSHARPIER-VERSION: 1.2.6` read with the Read tool from the `tools.csharpier.version` value of the repository-root `dotnet-tools.json`. The version is read from the manifest rather than from a `--version` probe because CSharpier 1's success-case output for a root-level `--version` argument has not been observed on this branch, and an assertion over unobserved output cannot be relied on to distinguish a passing run from a failing one.
- [x] [P0-T6] Verify the `dotnet-coverage` global tool and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t6-dotnet-coverage.md`. Run `dotnet-coverage --version`. It is a global tool and is not in the local tool manifest, so `dotnet tool restore` does not supply it; if the probe fails, run `dotnet tool install --global dotnet-coverage` and re-probe. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `DOTNET-COVERAGE-VERSION:` set to the single printed version line, and `BRANCH-TAKEN:` set to `probe-only` or `installed`.
- [x] [P0-T7] Resolve `vstest.console.exe` through `vswhere` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t7-vstest-resolution.md`. Bind `$vswhere` to `"${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"`, then `$vstest = & $vswhere -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe' | Select-Object -First 1`. Neither `vstest.console.exe` nor `vswhere` is on `PATH` in this environment. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, and `VSTEST-PATH:` set to a path on which `Test-Path` returned `True`. D14 exempts this one artifact from the absolute-path reduction rule.
- [x] [P0-T8] Capture the CSharpier baseline into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t8-csharpier-check.md`. Run `dotnet tool run csharpier check .`. `check` is read-only and exits non-zero on drift, so exit 0 plus its single success line is the clean-tree observation; the success-case output is one line of the form `Checked <count> files in <duration>ms.`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim printed `Checked` line, and `BASELINE-CSHARPIER-CHECKED-FILES:` set to the integer that line reports. If the exit code is non-zero, record every drifting path in the artifact as the pre-existing drift set and stop.
- [x] [P0-T9] Capture the analyzer-build baseline into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t9-msbuild-analyzers.md`. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the verbatim `Build succeeded.` block including its `Warning(s)` and `Error(s)` lines, `BASELINE-ANALYZER-WARNINGS:` and `BASELINE-ANALYZER-ERRORS:` set to those two integers. D5 applies to any MSB3061 or MSB3021 warning.
- [x] [P0-T10] Capture the nullable-build baseline into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t10-msbuild-nullable.md`. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`. Do not add `/p:Nullable=enable`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the verbatim `Build succeeded.` block, `BASELINE-NULLABLE-WARNINGS:` and `BASELINE-NULLABLE-ERRORS:` set to those two integers.
- [x] [P0-T11] Capture the `QuickFiler.Test` baseline run into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t11-quickfiler-tests.md`. Re-bind `$vstest` per D3, then run `& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll '/Settings:scripts\vscode\TaskMaster.cli.runsettings' '/InIsolation' '/Logger:trx' '/ResultsDirectory:TestResults\810-p0-t11' '/Blame:CollectHangDump;TestTimeout=4min;HangDumpType=None' '/TestCaseFilter:TestCategory!=LiveOutlook'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and `BASELINE-QFT-TOTAL:`, `BASELINE-QFT-PASSED:`, `BASELINE-QFT-FAILED:` read from the TRX `ResultSummary/Counters` element. Per D13 no TRX content is reproduced. `BASELINE-QFT-FAILED` is the value [P7-T5] compares against; any later failure not in this set is newly failing.
- [x] [P0-T12] Capture the baseline Cobertura coverage into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t12-coverage.md`. First write `coverage/810-effective-coverage.config` as a copy of the repository-root `coverage.config` with one appended `<ModulePath>` exclusion matching `.*\.Test\.dll$`, so the test assemblies do not enter the denominator. Then re-bind `$vstest` per D3 and run `dotnet-coverage collect --output coverage\810-baseline.cobertura.xml --output-format cobertura --settings coverage\810-effective-coverage.config -- $vstest` against the nine explicitly named first-party assemblies `QuickFiler.Test`, `SVGControl.Test`, `Tags.Test`, `TaskMaster.Test`, `TaskTree.Test`, `TaskVisualization.Test`, `ToDoModel.Test`, `UtilitiesCS.Test`, `VBFunctions.Test`, each as `<project>\bin\Debug\<project>.dll`, with `'/Settings:scripts\vscode\TaskMaster.cli.runsettings' '/InIsolation' '/Logger:trx' '/ResultsDirectory:TestResults\810-p0-t12' '/Blame:CollectHangDump;TestTimeout=4min;HangDumpType=None'` and `'/TestCaseFilter:TestCategory!=LiveOutlook&FullyQualifiedName!~HelperClasses.ShellUtilities_Tests&FullyQualifiedName!~HelperClasses.ShellUtilitiesStatic_Tests&FullyQualifiedName!~HelperClasses.SysImageListHelperTests&FullyQualifiedName!~EmailIntelligence.OSBrowser_Tests'`. Naming the nine assemblies explicitly is what keeps any sibling worktree build out of the run. Then aggregate the first-party packages `Tags`, `ToDoModel`, `TaskVisualization`, `UtilitiesCS`, `QuickFiler`, `TaskTree`, `TaskMaster`, `SVGControl`, `VBFunctions` by summing every descendant `line` element: count each as valid, count it covered when its `hits` attribute is greater than zero, and add the two integers of each `condition-coverage` `(covered/valid)` pair into the branch totals. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, `BASELINE-TOTAL-TESTS:`, `BASELINE-FAILED-TESTS:`, `BASELINE-LINES-COVERED:`, `BASELINE-LINES-VALID:`, `BASELINE-BRANCHES-COVERED:`, `BASELINE-BRANCHES-VALID:`, `BASELINE-LINE-PERCENT:` and `BASELINE-BRANCH-PERCENT:` each set to a number, and no placeholder value anywhere.
- [x] [P0-T13] Record the pre-change line counts of every production file in the Write Set into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t13-line-counts.md`, measured with the Read tool. Measure `QuickFiler/Controllers/QfcFormController.Deactivate.cs`, `QuickFiler/Controllers/QfcFormController.EventHandlers.cs`, `QuickFiler/Controllers/QfcFormController.SetupDisposal.cs`, `QuickFiler/Controllers/QfcHomeController.cs`, `QuickFiler/Controllers/QfcItemController.EventHandlers.cs`, `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs`, `QuickFiler/Viewers/QfcFormViewer.cs`, and the four test files `QuickFiler.Test/Controllers/QfcFormControllerCancelTeardownTests.cs`, `QuickFiler.Test/Controllers/QfcFormControllerCleanupTests.cs`, `QuickFiler.Test/Controllers/QfcHomeControllerCleanupTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbDropDownCloseOrderingTests.cs`. Acceptance: the artifact carries `Timestamp:`, one `LINES: <count> <path>` line per measured file with twelve such lines, and `CEILING-HEADROOM:` naming every measured file whose count is at least 480, each with its remaining headroom to the 500-line ceiling. The threshold is at least 480 rather than greater than 490 because `QuickFiler/Controllers/QfcFormController.EventHandlers.cs` is exactly 490 lines and is grown by [P1-T6]: the rewritten `RunTeardownStage` call exceeds 100 columns at its indentation, so CSharpier splits it into a four-line argument list for a net growth of three lines, and a strictly-greater test would leave that file out of the record.
- [x] [P0-T14] Record the AC2 fence baseline for `QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs` into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t14-ac2-fence-baseline.md`. Re-bind `$vstest` per D3 and run it against `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with `'/Settings:scripts\vscode\TaskMaster.cli.runsettings' '/InIsolation' '/Logger:trx' '/ResultsDirectory:TestResults\810-p0-t14' '/TestCaseFilter:FullyQualifiedName~QfcFormControllerDeactivateTests'`. Also run `git diff --name-only origin/main -- QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs` and `git status --porcelain --untracked-files=all -- QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs`; the porcelain span is present because a name-listing diff cannot report an untracked path. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FENCE-TOTAL:` and `FENCE-PASSED:` equal to each other and greater than zero, `FENCE-FAILED: 0`, and both `FENCE-DIFF-LINES: 0` and `FENCE-PORCELAIN-LINES: 0`, and the run includes the two named cases `FormDeactivated_SelfInflictedByOwnPopup_DoesNotCancelAnySelector` and `FormDeactivated_CancelsSelectorOnEveryItemController` reported as passed.
- [x] [P0-T15] Commit the Phase 0 evidence with `git add docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence` followed by `git commit -m "evidence(810): phase 0 baseline" -- docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence`, then record the commit into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/baseline/p0-t15-baseline-commit.md`, then fold that artifact into the same commit with `git add docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence` followed by `git commit --amend --no-edit -- docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence`. The record-then-amend shape exists because an artifact written after its own commit would otherwise be left uncommitted. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, and `COMMITTED-FILES: 15` verified against `git show --name-only --format= HEAD` after the amend, being the fourteen artifacts of [P0-T1] through [P0-T14] plus this one.

### Phase 1 — AC1 and AC2: scope the self-inflicted guard to the deactivation caller

- [x] [P1-T1] Add the AC1 regression test `ActionCancelAsync_SelfInflictedByOwnPopup_StillCancelsEverySelector` to `QuickFiler.Test/Controllers/QfcFormControllerCancelTeardownTests.cs`, as a near-clone of `ActionCancelAsync_ParksFocusAndCancelsBreadcrumbSelectors` at `:188` with exactly one added Arrange line, `_mockFormViewer.SetupGet(x => x.IsDeactivationSelfInflictedByOwnPopup).Returns(true);`. Keep the two `Mock<IQfcItemController>` items, the `CreateController()` and `InjectGroups(...)` calls, and the three existing assertions unchanged. Acceptance: `git grep -c -F "ActionCancelAsync_SelfInflictedByOwnPopup_StillCancelsEverySelector" -- QuickFiler.Test/Controllers/QfcFormControllerCancelTeardownTests.cs` prints a count of at least 1, and the file's line count measured with the Read tool is at most 500.
- [x] [P1-T2] Build the test assembly `QuickFiler.Test/QuickFiler.Test.csproj` with `msbuild QuickFiler.Test\QuickFiler.Test.csproj /t:Rebuild /m /p:Configuration=Debug /p:Platform=AnyCPU` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p1-t2-test-build.md`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block. D5 applies to any MSB3061 or MSB3021 warning.
- [x] [P1-T3] [expect-fail] Run the AC1 regression test against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record the fail-before into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p1-t3-ac1-fail-before.md`. Re-bind `$vstest` per D3 and run it with `'/Settings:scripts\vscode\TaskMaster.cli.runsettings' '/InIsolation' '/Logger:trx' '/ResultsDirectory:TestResults\810-p1-t3' '/TestCaseFilter:FullyQualifiedName~ActionCancelAsync_SelfInflictedByOwnPopup_StillCancelsEverySelector'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, `Output Summary:`, `TOTAL: 1`, `FAILED: 1`, and a `FAILURE-REASON:` line recording that `CancelBreadcrumbSelector` was invoked zero times because the guard at `QuickFiler/Controllers/QfcFormController.Deactivate.cs:118` returned before the per-item loop.
- [x] [P1-T4] Change the signature at `QuickFiler/Controllers/QfcFormController.Deactivate.cs:91` to `internal void ParkFocusAndCancelSelectors(bool honourSelfInflictedGuard)`, add `honourSelfInflictedGuard &&` as the first conjunct of the guard at `:118`, and extend the `Issue #796 (AC2)` comment block at `:111-117` with one sentence stating that the predicate is meaningful only for a deactivation and is therefore supplied by the caller. Do not move the guard, invert the predicate, or change its position relative to the focus-parking step at `:100-103` (D16). Acceptance: `git grep -c -F "bool honourSelfInflictedGuard" -- QuickFiler/Controllers/QfcFormController.Deactivate.cs` prints 1, and `git grep -c -F "&& _formViewer?.IsDeactivationSelfInflictedByOwnPopup == true" -- QuickFiler/Controllers/QfcFormController.Deactivate.cs` prints 1. The second token is deliberately the conjunct tail rather than the whole `if` statement, because the whole statement exceeds 100 columns at its indentation and CSharpier splits it across lines (D18); the tail sits on one line in both renderings.
- [x] [P1-T5] Update the deactivation caller at `QuickFiler/Controllers/QfcFormController.Deactivate.cs:27` to `ParkFocusAndCancelSelectors(honourSelfInflictedGuard: true);`. This is the call that keeps the issue-677 contract active for a genuine `Form.Deactivate`. Acceptance: `git grep -c -F "ParkFocusAndCancelSelectors(honourSelfInflictedGuard: true)" -- QuickFiler/Controllers/QfcFormController.Deactivate.cs` prints 1.
- [x] [P1-T6] Update the teardown stage at `QuickFiler/Controllers/QfcFormController.EventHandlers.cs:144` to `RunTeardownStage("park-focus", () => ParkFocusAndCancelSelectors(honourSelfInflictedGuard: false));`. The explicit lambda is required by D15; a defaulted parameter would break the method-group conversion with CS0123. The stage-name literal must remain exactly `"park-focus"`. Acceptance: `git grep -c -F "() => ParkFocusAndCancelSelectors(honourSelfInflictedGuard: false)" -- QuickFiler/Controllers/QfcFormController.EventHandlers.cs` prints 1, and `git grep -c -F "park-focus" -- QuickFiler/Controllers/QfcFormController.EventHandlers.cs` prints 1. The first token is deliberately the lambda alone rather than the whole `RunTeardownStage` call, because the whole call exceeds 100 columns at its indentation and CSharpier splits it across lines (D18); the lambda sits on one line in both renderings.
- [x] [P1-T7] Rebuild `QuickFiler.Test/QuickFiler.Test.csproj` after the AC1 fix and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p1-t7-post-fix-build.md`, using the same command as [P1-T2]. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block. A CS0123 here means the teardown call site was left as a method group; fix [P1-T6] rather than defaulting the parameter.
- [x] [P1-T8] Run the AC1 regression test green against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p1-t8-ac1-pass-after.md`, using the [P1-T3] command with `'/ResultsDirectory:TestResults\810-p1-t8'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `TOTAL: 1`, `PASSED: 1`, `FAILED: 0`.
- [x] [P1-T9] Re-run the AC2 fence `QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs` after the AC1 change and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p1-t9-ac2-fence-after-ac1.md`, using the [P0-T14] filter with `'/ResultsDirectory:TestResults\810-p1-t9'`, plus the same `git diff --name-only origin/main` and `git status --porcelain --untracked-files=all` spans on that one path. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FENCE-PASSED:` equal to `FENCE-TOTAL:` and equal to the `FENCE-PASSED` value recorded in [P0-T14], `FENCE-FAILED: 0`, `FENCE-DIFF-LINES: 0` and `FENCE-PORCELAIN-LINES: 0`.
- [x] [P1-T10] Run the whole `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` suite after the AC1 change and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p1-t10-quickfiler-suite.md`, using the [P0-T11] command with `'/ResultsDirectory:TestResults\810-p1-t10'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:` set to the observed integer, `ExpectedExitCode:` set to 0 when `BASELINE-QFT-FAILED` from [P0-T11] is 0 and to 1 otherwise, `Output Summary:`, `POST-QFT-FAILED:` less than or equal to `BASELINE-QFT-FAILED` from [P0-T11], `POST-QFT-TOTAL:` equal to `BASELINE-QFT-TOTAL` plus 1, `POST-QFT-FAILED-SET:` naming every failing fully qualified test name, and `NEWLY-FAILING: NONE` established by showing that `POST-QFT-FAILED-SET` is a subset of the failing set [P0-T11] recorded. The exit code is keyed to the baseline for the reason given in [P7-T5]: [P0-T11] leaves its own `EXIT_CODE:` unpinned, so a pinned 0 here contradicts the baseline comparison it sits beside.

### Phase 2 — AC3: stop leaving a disposed token source reachable

- [x] [P2-T1] Extend `QuickFiler.Test/Controllers/QfcHomeControllerCleanupTests.cs` with the AC3 regression coverage. In `Cleanup_DisposesTokenSourceAndDetachesWorkerCompleted` at `:79`, hold the `Mock<IQfcDatamodel>` in a local, inject it with `SetPrivateField(controller, "_datamodel", datamodel.Object)`, call `controller.Cleanup()` a second time after the existing assertions, and assert `datamodel.Verify(x => x.Cleanup(), Times.Once)`. Then add a new test `Cleanup_NullsTokenSourceSoLaterCancelCannotReachDisposedSource` that builds a controller through the same two-argument `QfcHomeController(IApplicationGlobals, System.Action)` constructor the existing tests use, injects a live source with `SetPrivateField(controller, "_tokenSource", new CancellationTokenSource())`, asserts `controller.TokenSource` is not null before the act step, calls `controller.Cleanup()` once, and asserts `controller.TokenSource` is null afterwards. The injection and the pre-act non-null assertion are both load-bearing: the two-argument constructor at `QuickFiler/Controllers/QfcHomeController.cs:29-33` assigns only `Globals` and `ParentCleanup`, and `TokenSource` at `:470-473` is a plain getter over the `_tokenSource` field declared at `:469`, so a controller built without the injection reports null before `Cleanup()` as well as after and the test would pass against the unmodified production code, putting the `FAILED: 2` requirement in [P2-T3] out of reach. Acceptance: `git grep -c -F "Cleanup_NullsTokenSourceSoLaterCancelCannotReachDisposedSource" -- QuickFiler.Test/Controllers/QfcHomeControllerCleanupTests.cs` prints at least 1, and the file's line count measured with the Read tool is at most 500. The datamodel half is not asserted by a source search, whose literal would be wrap-fragile once a `because:` argument is added; it is asserted behaviourally by the `FAILED: 2` requirement in [P2-T3] and the `FAILED: 0` requirement in [P2-T6].
- [x] [P2-T2] Build the test assembly `QuickFiler.Test/QuickFiler.Test.csproj` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p2-t2-test-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block.
- [x] [P2-T3] [expect-fail] Run the two AC3 tests against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record the fail-before into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p2-t3-ac3-fail-before.md`. Re-bind `$vstest` per D3 and use `'/ResultsDirectory:TestResults\810-p2-t3' '/TestCaseFilter:FullyQualifiedName~QfcHomeControllerCleanupTests'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, `Output Summary:`, `FAILED: 2`, and a `FAILURE-REASON:` line naming both `Cleanup_DisposesTokenSourceAndDetachesWorkerCompleted` (the datamodel is not nulled, so its `Cleanup()` runs twice) and `Cleanup_NullsTokenSourceSoLaterCancelCannotReachDisposedSource` (the token source is disposed but not nulled).
- [x] [P2-T4] Add `_tokenSource = null;` on the line immediately after `_tokenSource?.Dispose();` at `QuickFiler/Controllers/QfcHomeController.cs:389`, and add `_datamodel = null;` alongside the five sibling nullings at `:390-394`. Do not mark the field `volatile`; a field of this shape produced a CS0420 build break in prior work and no memory barrier is required. If a rationale comment is wanted, extend the existing XML doc summary at `:370` rather than adding comment lines inside the method body. Acceptance: `git grep -c -F "_tokenSource = null;" -- QuickFiler/Controllers/QfcHomeController.cs` prints 1, `git grep -c -F "_datamodel = null;" -- QuickFiler/Controllers/QfcHomeController.cs` prints 1, and the file's line count measured with the Read tool is at most 500.
- [x] [P2-T5] Rebuild `QuickFiler.Test/QuickFiler.Test.csproj` after the AC3 fix and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p2-t5-post-fix-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block.
- [x] [P2-T6] Run the AC3 tests green against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p2-t6-ac3-pass-after.md`, using the [P2-T3] filter with `'/ResultsDirectory:TestResults\810-p2-t6'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FAILED: 0`, and `PASSED:` equal to `TOTAL:`.

### Phase 3 — AC4: ribbon-release callback under `finally`, exactly once

- [x] [P3-T1] Add the AC4 regression test `Cleanup_ViewerDisposeThrows_StillInvokesParentCleanupOnce` to `QuickFiler.Test/Controllers/QfcFormControllerCleanupTests.cs`. Build the controller through the same eight-argument constructor `CreateController` uses at `:60-77`, but pass a counting `System.Action` as the fifth argument instead of `() => { }`, then set `TimeProvider`, `UndoConsumerStarter` and `UndoItemProcessor` exactly as `CreateController` does. Arrange `_mockFormViewer.Setup(x => x.Dispose()).Throws(new InvalidOperationException("planted"));`. Act by invoking `controller.Cleanup()` twice, each inside an `Action` asserted to throw `InvalidOperationException`. Assert the counting delegate ran exactly once across both passes. Acceptance: `git grep -c -F "Cleanup_ViewerDisposeThrows_StillInvokesParentCleanupOnce" -- QuickFiler.Test/Controllers/QfcFormControllerCleanupTests.cs` prints at least 1, and the file's line count measured with the Read tool is at most 500.
- [x] [P3-T2] Build the test assembly `QuickFiler.Test/QuickFiler.Test.csproj` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p3-t2-test-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block.
- [x] [P3-T3] [expect-fail] Run the AC4 regression test against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record the fail-before into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p3-t3-ac4-fail-before.md`. Re-bind `$vstest` per D3 and use `'/ResultsDirectory:TestResults\810-p3-t3' '/TestCaseFilter:FullyQualifiedName~Cleanup_ViewerDisposeThrows_StillInvokesParentCleanupOnce'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, `Output Summary:`, `TOTAL: 1`, `FAILED: 1`, and a `FAILURE-REASON:` line recording that the counting delegate ran zero times because the throw at `QuickFiler/Controllers/QfcFormController.SetupDisposal.cs:251` skips `_parentCleanup?.Invoke()` at `:259`.
- [x] [P3-T4] Restructure `QuickFiler/Controllers/QfcFormController.SetupDisposal.cs` by wrapping the body of `Cleanup()` at `:215-258` in a `try` block and replacing `:259-260` with a `finally` block that reads the field into a local, clears the field, then invokes the local: `System.Action parentCleanup = _parentCleanup;` then `_parentCleanup = null;` then `parentCleanup?.Invoke();`. Reading and clearing before invoking is what makes exactly-once unconditional; a `finally` that invokes before clearing closes the earlier-throw hole but not the callback-throws hole. Introduce none of `.Wait(`, `.Result`, `Thread.Sleep` or `Task.Delay`, which `Cleanup_SourceContainsNoSynchronousWait` at `:377` scans this file's whole text for. Acceptance: `git grep -c -F "System.Action parentCleanup = _parentCleanup;" -- QuickFiler/Controllers/QfcFormController.SetupDisposal.cs` prints 1, `git grep -c -F "parentCleanup?.Invoke();" -- QuickFiler/Controllers/QfcFormController.SetupDisposal.cs` prints 1, and the file's line count measured with the Read tool is at most 500.
- [x] [P3-T5] Rebuild `QuickFiler.Test/QuickFiler.Test.csproj` after the AC4 fix and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p3-t5-post-fix-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block.
- [x] [P3-T6] Run the AC4 regression test green plus the whole `QfcFormControllerCleanupTests` class against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p3-t6-ac4-pass-after.md`, using `'/ResultsDirectory:TestResults\810-p3-t6' '/TestCaseFilter:FullyQualifiedName~QfcFormControllerCleanupTests'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FAILED: 0`, `PASSED:` equal to `TOTAL:`, and a line recording that `Cleanup_SourceContainsNoSynchronousWait` is among the passing cases.

### Phase 4 — AC5 and the AC6 host half, under the measured 500-line rule

- [x] [P4-T1] Add the AC5 regression coverage to `QuickFiler.Test/Viewers/BreadcrumbDropDownCloseOrderingTests.cs`. Add `RestoreAfterOpenFailure_WithStaleCommitPending_StillCancelsAndClearsLatch` using the existing `CloseOrderingHostHarness` at `:159`, driving two closes as D19 requires: call `harness.OpenAndSettle()`, set `harness.Host.IsCommitPending = true;`, call `harness.RaiseNativeClose(ToolStripDropDownCloseReason.AppFocusChange)` which consumes the latch without cancelling, assert `harness.CancelCount` is still 0, then call `harness.Host.RestoreAfterOpenFailure()` and assert `harness.CancelCount` is 1 and `harness.Host.IsCommitPending` is false. A single-close version of this test asserting a cancel is unsatisfiable even after the fix, because the read at `QuickFiler/Viewers/BreadcrumbDropDownHost.cs:447` is an earlier operation of the same `CompleteAll` list than the clear. Every mechanic used here is already exercised by this file: `OpenAndSettle` at `:201`, the `IsCommitPending` setter at `:80`, `RaiseNativeClose` at `:222`; `RestoreAfterOpenFailure` is `internal` and reachable through the assembly-level `InternalsVisibleTo("QuickFiler.Test")` at `QuickFiler/Controllers/QfcHomeController.cs:15`. Also append `harness.Host.IsCommitPending.Should().BeFalse(...)` to `NativeCloseWhileCommitPending_DoesNotCancelSelection` at `:74`, after its existing `CancelCount` assertion. Acceptance: `git grep -c -F "RestoreAfterOpenFailure_WithStaleCommitPending_StillCancelsAndClearsLatch" -- QuickFiler.Test/Viewers/BreadcrumbDropDownCloseOrderingTests.cs` prints at least 1, and the file's line count measured with the Read tool is at most 500.
- [x] [P4-T2] Build the test assembly `QuickFiler.Test/QuickFiler.Test.csproj` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p4-t2-test-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block.
- [x] [P4-T3] [expect-fail] Run the two AC5 cases against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record the fail-before into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p4-t3-ac5-fail-before.md`. Re-bind `$vstest` per D3 and use `'/ResultsDirectory:TestResults\810-p4-t3' '/TestCaseFilter:FullyQualifiedName~RestoreAfterOpenFailure_WithStaleCommitPending_StillCancelsAndClearsLatch|FullyQualifiedName~NativeCloseWhileCommitPending_DoesNotCancelSelection'`. vstest 18.x rejects `OR` inside a filter, so the two clauses are joined with a vertical bar. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, `Output Summary:`, `TOTAL: 2`, `FAILED: 2`, and a `FAILURE-REASON:` line recording that in both cases the latch survived the close that consumed it, so in the new case the later `RestoreAfterOpenFailure` still suppressed the cancel and `CancelCount` stayed 0.
- [x] [P4-T4] Apply the AC5 latch clear and the AC6 comment correction in place in `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` (Design A). Add `() => IsCommitPending = false` as a fourth operation in `FinishClose`'s `CompleteAll` list at `:434-452`, after `FocusAnchorIfPermitted`, with a short comment recording that it must be an element of the operation list rather than a statement after the call, because `CompleteAll` rethrows the first failing operation at `:486-487`. Replace the single comment line at `:450` with the two-clause corrected text, whose third line reads exactly `// The two gates are independent`. Acceptance: `git grep -c -F "() => IsCommitPending = false" -- QuickFiler/Viewers/BreadcrumbDropDownHost.cs` prints 1, `git grep -c -F "The two gates are independent" -- QuickFiler/Viewers/BreadcrumbDropDownHost.cs` prints 1, and `git grep -F "the cancel step above always runs" -- QuickFiler/Viewers` exits 1 with no output.
- [x] [P4-T5] Update the latch-lifetime XML doc at `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs:102-107`, which currently states that `ShowPopup` clears the latch and nothing else clears it. Rewrite that clause so that the words `also clears it as its final operation` sit contiguously on a single `///` line, and so that the superseded claim `and nothing else clears it` is gone. Acceptance: `git grep -c -F "also clears it as its final operation" -- QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` prints 1, and `git grep -F "and nothing else clears it" -- QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` exits 1 with no output.
- [x] [P4-T6] Format the two edited viewer files with `dotnet tool run csharpier format QuickFiler/Viewers/BreadcrumbDropDownHost.cs QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p4-t6-csharpier-format-viewers.md`. `format` rewrites tracked source and still exits 0 after rewriting, so the exit code alone cannot distinguish a clean run from a repairing one; its success-case output is one line of the form `Formatted <count> files in <duration>ms.` on both. The distinguishing observation is the pair of `git status --porcelain --untracked-files=all` and `git diff --stat origin/main` outputs captured immediately before and immediately after the invocation. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim printed `Formatted` line, `PATH_SETS_IDENTICAL:` and `DIFFSTAT_IDENTICAL:` each set to `True` or `False`, and both before and after values of the two spans transcribed in full.
- [x] [P4-T7] Measure the post-CSharpier line counts of `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` and `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` with the Read tool and record them into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p4-t7-host-line-measurement.md`. This measurement, not the pre-change 496-line figure and not the research artifact's assumption, is what decides the layout in [P4-T8]. Acceptance: the artifact carries `Timestamp:`, `HOST-LINES-AFTER-DESIGN-A:` set to an integer, `OPEN-LINES-AFTER-DESIGN-A:` set to an integer, and `DESIGN-A-EXCEEDS-CEILING:` set to `YES` when `HOST-LINES-AFTER-DESIGN-A` is greater than 500 and to `NO` otherwise.
- [x] [P4-T8] Apply the measured layout decision to `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` and `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p4-t8-layout-decision.md`. If `DESIGN-A-EXCEEDS-CEILING` from [P4-T7] is `NO`, take no code action and record `DESIGN-CHOSEN: A`. If it is `YES`, move `FinishClose` and `RestoreAfterOpenFailure` verbatim out of `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` into `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs`, which already declares `IsCommitPending` at `:109` and owns its other clear site `ShowPopup` at `:118`, and record `DESIGN-CHOSEN: B`. Relocation is a pure move between partial-class parts of `public sealed partial class BreadcrumbDropDownHost`; both files already carry `#nullable enable` and `using System;`, and no test resolves either method by file path. Acceptance: the artifact carries `Timestamp:`, `DESIGN-CHOSEN:` set to exactly `A` or `B`, `BRANCH-TAKEN:` naming the branch and the `DESIGN-A-EXCEEDS-CEILING` value that selected it, and under branch B two `git grep` records showing `git grep -F "private void FinishClose(BreadcrumbDropDownCloseReason reason)" -- QuickFiler/Viewers/BreadcrumbDropDownHost.cs` exiting 1 and `git grep -c -F "private void FinishClose(BreadcrumbDropDownCloseReason reason)" -- QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` printing 1.
- [x] [P4-T9] Format `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` and `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` again after the layout decision and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p4-t9-csharpier-format-viewers.md`, using the [P4-T6] command and the same before-and-after observation pair. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim printed `Formatted` line, and `PATH_SETS_IDENTICAL:` and `DIFFSTAT_IDENTICAL:` each set to `True` or `False` with both spans transcribed before and after.
- [x] [P4-T10] Audit the ceiling for `QuickFiler/Viewers/BreadcrumbDropDownHost.cs` and `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` with the Read tool and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p4-t10-viewer-ceiling-audit.md`. Acceptance: the artifact carries `Timestamp:`, `HOST-LINES-FINAL:` and `OPEN-LINES-FINAL:` each set to an integer, both integers are at most 500, and `CEILING: MET`.
- [x] [P4-T11] Rebuild `QuickFiler.Test/QuickFiler.Test.csproj` after the AC5 and AC6 host-half changes and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p4-t11-post-fix-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block.
- [x] [P4-T12] Run the whole `BreadcrumbDropDownCloseOrderingTests` class plus `BreadcrumbDropDownHostTests` against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p4-t12-ac5-pass-after.md`, using `'/ResultsDirectory:TestResults\810-p4-t12' '/TestCaseFilter:FullyQualifiedName~BreadcrumbDropDownCloseOrderingTests|FullyQualifiedName~BreadcrumbDropDownHostTests|FullyQualifiedName~BreadcrumbPendingOpenCloseTests'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FAILED: 0`, `PASSED:` equal to `TOTAL:`, and a line recording that `RestoreAfterOpenFailure_WithStaleCommitPending_StillCancelsAndClearsLatch`, `NativeCloseWhileCommitPending_DoesNotCancelSelection` and `NativeCloseWithNoCommitPending_StillCancelsSelection` all passed.

### Phase 5 — AC6 controller half: delete the dead accessor

- [x] [P5-T1] Delete `QuickFiler/Controllers/QfcItemController.EventHandlers.cs:205-209`, that is the XML doc block and the expression-bodied `internal bool SearchOwnsDropDownDismissal => _searchOwnedDismissal;` accessor. Leave the backing field `_searchOwnedDismissal` at `:203` and every one of its live write sites at `:183`, `:220`, `:234`, `:257`, `:265` and its read site at `:263` untouched; the field cannot regress to CS0649 or CS0169 because all of those remain. Acceptance: `git grep -F "SearchOwnsDropDownDismissal" -- QuickFiler QuickFiler.Test` exits 1 with no output, and `git grep -c -F "private bool _searchOwnedDismissal;" -- QuickFiler/Controllers/QfcItemController.EventHandlers.cs` prints 1.
- [x] [P5-T2] Rebuild the solution `TaskMaster.sln` with the analyzer gate after the deletion and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p5-t2-ac6-analyzer-build.md`, using the [P0-T9] command. This build is the AC6 evidence that the deletion compiles and raises no new diagnostic on the retained backing field. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim `Build succeeded.` block, `ANALYZER-WARNINGS:` and `ANALYZER-ERRORS:` each less than or equal to the corresponding `BASELINE-ANALYZER-` value from [P0-T9], and `CS0649-OR-CS0169: NONE`.
- [x] [P5-T3] Run the `QfcItemController` search-dismissal suite against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p5-t3-search-dismissal-suite.md`, using `'/ResultsDirectory:TestResults\810-p5-t3' '/TestCaseFilter:FullyQualifiedName~SearchDismissalTests'`. The `#796` AC4 re-pin test at `QuickFiler.Test/Controllers/QfcItemController.SearchDismissalTests.cs:85` reaches the retained field by reflection on its name, not through the deleted accessor, so it must stay green. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FAILED: 0`, and `PASSED:` equal to `TOTAL:` and greater than zero.

### Phase 6 — AC7: extract the popup-owner registry so the AC2 producer is measurable

- [x] [P6-T1] Create the new test file `QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs` with six MSTest cases over the not-yet-existing `QuickFiler.Viewers.BreadcrumbPopupOwnerRegistry`: no registration reports `AnyOpen` false, which is the GENUINE case and the load-bearing polarity; one registration whose predicate returns false reports false; one whose predicate returns true reports true; two registrations of which one returns true report true; re-registering the same `Control` key replaces rather than appends; and a null `Control` or a null `Func<bool>` is ignored. Use `new Control()` as the key, which creates no window handle, plus Moq and FluentAssertions per CLAUDE.md CUT1 and CUT2. Acceptance: after `git add -N QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs`, which is required because `git grep` does not search an untracked path, `git grep -c -F "BreadcrumbPopupOwnerRegistry" -- QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs` prints at least 1, `git grep -c -F "[TestMethod]" -- QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs` prints 6, and the file's line count measured with the Read tool is at most 500.
- [x] [P6-T2] Register the new test file in the legacy non-SDK project `QuickFiler.Test/QuickFiler.Test.csproj` by adding `<Compile Include="Viewers\BreadcrumbPopupOwnerRegistryTests.cs" />` beside the existing `Viewers\Breadcrumb*` entries at `:68-107`. A test file that is not registered compiles to nothing and its tests silently do not run. Acceptance: `git grep -c -F "Viewers\BreadcrumbPopupOwnerRegistryTests.cs" -- QuickFiler.Test/QuickFiler.Test.csproj` prints 1, and `git add -N QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs` followed by `git diff --name-only origin/main -- QuickFiler.Test` lists both the new test file and the project file.
- [x] [P6-T3] [expect-fail] Build `QuickFiler.Test/QuickFiler.Test.csproj` and record the compile-red fail-before into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p6-t3-ac7-compile-red.md`, using the [P1-T2] command. A runtime-red run is structurally impossible here because the type under test does not exist yet, so the fail-before is the compiler's own report. Under the [P1-T2] command as corrected by D20 the build reaches `CoreCompile`, so the `EXIT_CODE: 1` recorded here is the compiler's own failure rather than a platform-resolution failure that never reaches the compiler, and the `FAILURE-DIAGNOSTIC:` CS0246 clause is therefore the discriminating assertion while the exit code alone is not. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 1`, `ExpectedExitCode: 1`, `Output Summary:`, and `FAILURE-DIAGNOSTIC:` recording a CS0246 naming `BreadcrumbPopupOwnerRegistry`.
- [x] [P6-T4] Create the new production file `QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` carrying `#nullable enable` on its first line, namespace `QuickFiler.Viewers`, and no `[ExcludeFromCodeCoverage]` attribute. Give it a `private readonly Dictionary<Control, Func<bool>>` store, an `internal void Register(Control itemViewer, Func<bool> popupIsOpen)` that returns without recording when either argument is null and otherwise assigns by key so a repeat registration replaces its own entry, and an `internal bool AnyOpen` carrying the `Values.Any(popupIsOpen => popupIsOpen())` derivation. Add no `Unregister` member. Acceptance: after `git add -N QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs`, which is required because `git grep` does not search an untracked path, `git grep -c -F "#nullable enable" -- QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` prints 1, `git grep -F "ExcludeFromCodeCoverage" -- QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` exits 1 with no output, and the file's line count measured with the Read tool is at most 500.
- [x] [P6-T5] Register the new production file in the legacy non-SDK project `QuickFiler/QuickFiler.csproj` by adding `<Compile Include="Viewers\BreadcrumbPopupOwnerRegistry.cs" />` beside the existing `Viewers\Breadcrumb*` entries at `:401-419`. Acceptance: `git grep -c -F "Viewers\BreadcrumbPopupOwnerRegistry.cs" -- QuickFiler/QuickFiler.csproj` prints 1, and `git add -N QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` followed by `git diff --name-only origin/main -- QuickFiler` lists both the new production file and the project file.
- [x] [P6-T6] Reduce `QuickFiler/Viewers/QfcFormViewer.cs:209-246` to one registry field and two forwarding members, and add `using QuickFiler.Viewers;` to its using block, since the type sits in namespace `QuickFiler.Viewers` while `QfcFormViewer` sits in namespace `QuickFiler`. Replace the dictionary field at `:212-213` with `private readonly BreadcrumbPopupOwnerRegistry _breadcrumbPopupOwners = new BreadcrumbPopupOwnerRegistry();`, reduce `SetBreadcrumbPopupOwner` at `:226-234` to a single forwarding call to `Register`, and reduce `IsDeactivationSelfInflictedByOwnPopup` at `:245-246` to a single forwarding read of `AnyOpen`. Do not change the polarity: a form with no registered popup must still report false, which is the GENUINE case (D16). Remove `using System.Linq;` from `:7` in the same edit: the expression at `:246` is the only LINQ call site in this file, so the directive has no consumer once the forwarding read replaces it, and an unnecessary using directive is a candidate diagnostic against the [P7-T3] analyzer budget, which is capped at the [P0-T9] baseline. If removing it produces a CS0103 or CS1061 at [P6-T7], restore the directive and record the identifier that still requires it in that task's artifact. Acceptance: `git grep -c -F "using QuickFiler.Viewers;" -- QuickFiler/Viewers/QfcFormViewer.cs` prints 1, `git grep -c -F "new BreadcrumbPopupOwnerRegistry()" -- QuickFiler/Viewers/QfcFormViewer.cs` prints 1, `git grep -c -F "_breadcrumbPopupOwners.AnyOpen" -- QuickFiler/Viewers/QfcFormViewer.cs` prints 1, and `git grep -F "_breadcrumbPopupOwners.Values.Any" -- QuickFiler/Viewers/QfcFormViewer.cs` exits 1 with no output, and `git grep -F "using System.Linq;" -- QuickFiler/Viewers/QfcFormViewer.cs` exits 1 with no output.
- [x] [P6-T7] Rebuild `QuickFiler.Test/QuickFiler.Test.csproj` after the AC7 extraction and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p6-t7-post-fix-build.md`, using the [P1-T2] command. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the verbatim `Build succeeded.` block, which closes the compile-red window opened at [P6-T3].
- [x] [P6-T8] Run the six registry cases green against `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/regression-testing/p6-t8-ac7-pass-after.md`, using `'/ResultsDirectory:TestResults\810-p6-t8' '/TestCaseFilter:FullyQualifiedName~BreadcrumbPopupOwnerRegistryTests'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `TOTAL: 6`, `PASSED: 6`, `FAILED: 0`.
- [x] [P6-T9] Record the AC7 residual into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/other/p6-t9-ac7-residual.md`. AC7 covers the derivation only. The registration hop at `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs:216`, which is `FindForm() as QfcFormViewer`, stays untestable without a real form hierarchy and is not in scope; `QuickFiler/Viewers/QfcFormViewer.cs` keeps its class-level `[ExcludeFromCodeCoverage]` and therefore still emits no Cobertura class element. Acceptance: the artifact carries `Timestamp:`, an explicit statement of the residual naming that file and line, and `RESIDUAL-IN-SCOPE: NO`.

### Phase 7 — Final QC loop, coverage delta, and acceptance-criteria check-off

- [x] [P7-T1] Run the formatter over the whole tree with `dotnet tool run csharpier format .` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t1-csharpier-format.md`. `format` rewrites tracked source and still exits 0 after rewriting, and prints one line of the form `Formatted <count> files in <duration>ms.` in both cases, so the exit code alone cannot distinguish a clean run from a repairing one. Capture `git status --porcelain --untracked-files=all` and `git diff --stat origin/main` immediately before and immediately after the invocation and compare each pair. If either pair differs, this step changed files: restart the loop from [P7-T1] per D4 and append a further pass section to this same artifact. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim printed `Formatted` line for every pass, and a final pass whose `PATH_SETS_IDENTICAL:` and `DIFFSTAT_IDENTICAL:` are both `True`.
- [x] [P7-T2] Verify formatting read-only with `dotnet tool run csharpier check .` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t2-csharpier-check.md`. `check` exits non-zero on drift, so exit 0 plus its `Checked <count> files in <duration>ms.` line is the clean-tree observation. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim printed `Checked` line, `FINAL-CSHARPIER-CHECKED-FILES:` set to that integer, and `CHECKED-FILES-DELTA:` set to `FINAL-CSHARPIER-CHECKED-FILES` minus `BASELINE-CSHARPIER-CHECKED-FILES` from [P0-T8], which must be at least 2, together with a `DELTA-COMPOSITION:` block naming every path this plan created that CSharpier could count and stating for each whether it is inside or outside the counted set. The lower bound is 2 for the two new `.cs` files `QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` and `QuickFiler.Test/Viewers/BreadcrumbPopupOwnerRegistryTests.cs`, and that lower bound is the gate: a delta of 1 or fewer means at least one of the two new source files did not enter the counted set, which is a real defect in the project registration or in the file location. No upper bound is gated, because the checked-file count is not confined to the files this plan creates. In the immediately preceding feature on this code area the same repo-wide command moved from `Checked 1601 files in 6592ms.` at `docs/features/active/2026-09-06-quickfiler-folder-dropdown-closes-on-open-and-click-does-not-select-796/evidence/baseline/p0-t7-csharpier-check-baseline.md:16` to `Checked 1608 files in 5812ms.` at `docs/features/active/2026-09-06-quickfiler-folder-dropdown-closes-on-open-and-click-does-not-select-796/evidence/qa-gates/p9-t2-csharpier-check.md:25`, a delta of 7, while that branch contributed only three files CSharpier could count; four of the seven came from elsewhere and were never attributed. Pinning an upper bound derived from what the counter ought to include would therefore fail this task on a tree in which every other gate passed. `coverage/810-effective-coverage.config` is a known candidate for the residual: it is written by [P0-T12] after the [P0-T8] baseline and `.csharpierignore` excludes neither the `coverage/` directory nor the `.config` extension. Every artifact this plan produces beyond the two new `.cs` files and that one config is outside the counted set: `.csharpierignore` excludes `**/evidence/**`, `*.cobertura.xml`, `*.coverage`, `*.coveragexml`, `*.trx`, `*.csproj`, `*.props` and `*.targets`, the two `.csproj` edits add no new file, and evidence artifacts are Markdown. Acceptance additionally requires `DELTA-ATTRIBUTION:` set to `COMPLETE` when the composition block accounts for the whole delta and to `RESIDUAL: <integer>` naming the unattributed remainder otherwise, with `RESIDUAL` recorded as an observation and not as a failure.
- [x] [P7-T3] Run the analyzer gate over `TaskMaster.sln` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t3-msbuild-analyzers.md`, using the [P0-T9] command verbatim. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim `Build succeeded.` block, and `ANALYZER-WARNINGS:` and `ANALYZER-ERRORS:` each less than or equal to the corresponding `BASELINE-ANALYZER-` value from [P0-T9].
- [x] [P7-T4] Run the nullable gate over `TaskMaster.sln` and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t4-msbuild-nullable.md`, using the [P0-T10] command verbatim and adding no `/p:Nullable=enable`. `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, `QuickFiler/Viewers/BreadcrumbDropDownHost.Open.cs` and the new `QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` all carry `#nullable enable`, so their CS86xx diagnostics are promoted to errors here. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, the verbatim `Build succeeded.` block, and `NULLABLE-WARNINGS:` and `NULLABLE-ERRORS:` each less than or equal to the corresponding `BASELINE-NULLABLE-` value from [P0-T10].
- [x] [P7-T5] Run the post-change coverage collection over the nine first-party test assemblies and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t5-tests-coverage.md`, using the [P0-T12] command with `--output coverage\810-post.cobertura.xml`, the same unchanged `coverage/810-effective-coverage.config`, the same nine explicitly named assemblies, the same filter, and `'/ResultsDirectory:TestResults\810-p7-t5'`. Aggregate the same nine first-party packages by the same all-descendant `line` selection. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:` set to the observed integer, `ExpectedExitCode:` set to 0 when `BASELINE-FAILED-TESTS` from [P0-T12] is 0 and to 1 otherwise, `Output Summary:`, `POST-TOTAL-TESTS:`, `POST-FAILED-TESTS:` set to an integer, `POST-LINES-COVERED:`, `POST-LINES-VALID:`, `POST-BRANCHES-COVERED:`, `POST-BRANCHES-VALID:`, `POST-LINE-PERCENT:` and `POST-BRANCH-PERCENT:` each set to a number with no placeholder, `POST-FAILED-SET:` naming every failing fully qualified test name, and `NEWLY-FAILING: NONE` established by showing that `POST-FAILED-SET` is a subset of the `BASELINE-FAILED-TESTS` set recorded by [P0-T12]. The exit code is keyed to the baseline rather than pinned to 0 because the nine-assembly run tolerates a pre-existing failure by design: pinning both `EXIT_CODE: 0` and `POST-FAILED-TESTS: 0` while also comparing against a baseline failing set makes the task unsatisfiable whenever that set is non-empty.
- [x] [P7-T6] Record the toolchain loop closure into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t6-loop-closure.md`. `LOOP: CLEAN PASS` means every step met its own declared expectation, not that every step returned 0. [P7-T1] through [P7-T4] each declare `EXIT_CODE: 0` and must have returned 0. [P7-T5] declares an `ExpectedExitCode:` keyed to `BASELINE-FAILED-TESTS` from [P0-T12], so it meets its expectation when its observed `EXIT_CODE` equals that declared value and its `NEWLY-FAILING: NONE` line holds; a non-zero observed exit code on [P7-T5] is therefore not a loop failure and must not be recorded as one. Acceptance: the artifact carries `Timestamp:`, one line per step naming the artifact path of [P7-T1], [P7-T2], [P7-T3], [P7-T4] and [P7-T5] with each step's recorded `EXIT_CODE`, one `EXPECTATION-MET:` line per step set to `YES` or `NO` against the rule above, a statement that all five ran in that order with no intervening file change after the final [P7-T1] pass, and `LOOP: CLEAN PASS` written only when all five `EXPECTATION-MET:` lines read `YES`. If any reads `NO`, write `LOOP: NOT CLEAN` and restart the loop from [P7-T1] per D4 rather than proceeding to [P7-T7].
- [x] [P7-T7] Audit the 500-line ceiling across every file in the Write Set with the Read tool and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t7-file-size-audit.md`, measured on the tree as it stands after the final [P7-T1] pass. Measure all nine production `.cs` paths and all five test `.cs` paths in the Write Set. `QuickFiler.Test/QuickFiler.Test.csproj` exceeds 500 lines and is a pre-existing condition outside the rule's stated scope of production code, test code and reusable script files; record it as such rather than as a new violation. Acceptance: the artifact carries `Timestamp:`, one `LINES: <count> <path>` line per measured `.cs` file with fourteen such lines, `MAX-LINES:` set to the largest of them, `MAX-LINES` is at most 500, and `CEILING: MET`.
- [x] [P7-T8] Compute the repository coverage delta and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t8-coverage-delta.md`, comparing the [P0-T12] baseline counters against the [P7-T5] post-change counters. Report the 85 percent line floor and the 75 percent branch floor from `.claude/rules/general-unit-test.md` and `.claude/rules/quality-tiers.md`, and the 80 percent floor from CLAUDE.md UT2, each as a separate disposition with its measured value; where the two conflict, record both and do not silently pick one. Acceptance: the artifact carries `Timestamp:`, `BASELINE-LINE-PERCENT:`, `POST-LINE-PERCENT:`, `LINE-PERCENT-DELTA:`, `BASELINE-BRANCH-PERCENT:`, `POST-BRANCH-PERCENT:`, `BRANCH-PERCENT-DELTA:` each set to a number, `NO-REGRESSION: PASS` when `LINE-PERCENT-DELTA` is at least -0.10 and `BRANCH-PERCENT-DELTA` is at least -0.10 and `NO-REGRESSION: FAIL` otherwise, and three separate `FLOOR-<value>:` lines each set to `MET` or `NOT MET` with the measured value beside it.
- [x] [P7-T9] Compute changed-line coverage and record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t9-changed-line-coverage.md`. Derive the changed production line set mechanically from `git diff --unified=0 origin/main -- QuickFiler` and cross-reference each added line number against the `line` elements of `coverage/810-post.cobertura.xml` for the same file. `QuickFiler/Viewers/QfcFormViewer.cs` carries a class-level `[ExcludeFromCodeCoverage]` and therefore emits no Cobertura class element at all; record its changed lines as `NOT MEASURABLE` with that reason rather than as zero. The new module `QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` targets the 90 percent new-code bar. Acceptance: the artifact carries `Timestamp:`, `Command:`, one `CHANGED-LINES:` block per changed production file naming the file, its added line numbers and the hit count of each, `CHANGED-LINE-COVERAGE:` set to a percentage over the measurable subset, `NEW-MODULE-COVERAGE:` set to the percentage for `QuickFiler/Viewers/BreadcrumbPopupOwnerRegistry.cs` which must be at least 90, and `UNMEASURABLE-FILES:` naming every file recorded as `NOT MEASURABLE` with its reason.
- [x] [P7-T10] Record the final AC2 fence state for `QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs` into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t10-ac2-fence-final.md`. Run `git diff --name-only origin/main -- QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs`, `git status --porcelain --untracked-files=all -- QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs`, and the [P0-T14] scoped test command with `'/ResultsDirectory:TestResults\810-p7-t10'`. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `FENCE-DIFF-LINES: 0`, `FENCE-PORCELAIN-LINES: 0`, `FENCE-FAILED: 0`, `FENCE-PASSED:` equal to the [P0-T14] value, and both `FormDeactivated_SelfInflictedByOwnPopup_DoesNotCancelAnySelector` and `FormDeactivated_CancelsSelectorOnEveryItemController` recorded as passed.
- [x] [P7-T11] Record the scope-boundary check into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t11-scope-boundary.md`. Run `git diff --name-status origin/main` paired with `git status --porcelain --untracked-files=all`, because a name-listing diff cannot report a path that is still untracked. Compare the resulting path set against the Write Set in this plan. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, `CHANGED-PATHS:` listing every path from the union of the two spans, `INHERITED-PATHS:` listing every path of `CHANGED-PATHS` that satisfies either inherited clause with the clause letter recorded beside it, clause A being membership in the `INHERITED-CLAUSE-A:` block captured by [P0-T2] and clause B being a path under `.claude/agent-memory/`, `OUT-OF-WRITE-SET: NONE` evaluated over `CHANGED-PATHS` minus `INHERITED-PATHS` minus the Write Set, and three explicit `UNCHANGED:` lines for `QuickFiler.Test/Controllers/QfcHomeControllerRunAsyncTests.cs`, `TaskMaster/ThisAddIn.cs` and `UtilitiesCS/Threading/UiThread.cs` per D8, plus one for `QuickFiler.Test/Controllers/QfcFormControllerDeactivateTests.cs` per D9. The subtraction is required and not a waiver, and it has two distinct grounds. Clause A covers paths already changed relative to `origin/main` before [P0-T1] ran, which no task here touches, so an unsubtracted comparison reports them on a tree in which every task behaved correctly. Clause B covers `.claude/agent-memory/`, a tracked tree this plan does not own and into which the executing agent writes its own persistent memory entries during execution; those paths cannot be enumerated when the plan is authored, so the subtraction is stated as a rule over a path prefix rather than as a list. Neither clause subtracts any path this plan's Write Set names, so a Write Set path that changed unexpectedly is still reported.
- [x] [P7-T12] Check off AC1 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/regression-testing/p1-t3-ac1-fail-before.md` and `evidence/regression-testing/p1-t8-ac1-pass-after.md`. Acceptance: the AC1 checkbox line in both files reads `- [x] AC1:` and both cited artifacts exist on disk.
- [x] [P7-T13] Check off AC2 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/baseline/p0-t14-ac2-fence-baseline.md` and `evidence/qa-gates/p7-t10-ac2-fence-final.md`. Acceptance: the AC2 checkbox line in both files reads `- [x] AC2:` and both cited artifacts exist on disk.
- [x] [P7-T14] Check off AC3 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/regression-testing/p2-t3-ac3-fail-before.md` and `evidence/regression-testing/p2-t6-ac3-pass-after.md`. Acceptance: the AC3 checkbox line in both files reads `- [x] AC3:` and both cited artifacts exist on disk.
- [x] [P7-T15] Check off AC4 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/regression-testing/p3-t3-ac4-fail-before.md` and `evidence/regression-testing/p3-t6-ac4-pass-after.md`. Acceptance: the AC4 checkbox line in both files reads `- [x] AC4:` and both cited artifacts exist on disk.
- [x] [P7-T16] Check off AC5 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/regression-testing/p4-t3-ac5-fail-before.md` and `evidence/regression-testing/p4-t12-ac5-pass-after.md`. Acceptance: the AC5 checkbox line in both files reads `- [x] AC5:` and both cited artifacts exist on disk.
- [x] [P7-T17] Check off AC6 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/qa-gates/p5-t2-ac6-analyzer-build.md` and `evidence/qa-gates/p7-t4-msbuild-nullable.md`. Acceptance: the AC6 checkbox line in both files reads `- [x] AC6:` and both cited artifacts exist on disk.
- [x] [P7-T18] Check off AC7 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/regression-testing/p6-t8-ac7-pass-after.md` and `evidence/qa-gates/p7-t9-changed-line-coverage.md`. Acceptance: the AC7 checkbox line in both files reads `- [x] AC7:` and both cited artifacts exist on disk.
- [x] [P7-T19] Check off AC8 in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/spec.md` and in `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/issue.md`, citing `evidence/qa-gates/p7-t6-loop-closure.md`. Acceptance: the AC8 checkbox line in both files reads `- [x] AC8:` and the cited artifact exists on disk.
- [x] [P7-T20] Write the acceptance-criteria status summary to `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t20-ac-status-summary.md`. Acceptance: the artifact carries `Timestamp:`, one line per criterion for AC1 through AC8 in that order, each naming the criterion identifier, its status, and at least one evidence artifact path, and a `TOTAL: 8 of 8` line.
- [x] [P7-T21] Mirror the issue update to `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/issue-updates/issue-810.<TS>.md`, where `<TS>` is the `yyyy-MM-ddTHH-mm` timestamp read from the system clock at the moment the artifact is written, not a value carried forward from this plan. Record the exact text intended for issue #810, the AC1 through AC8 outcome list, and the nine report-only items from the spec's "Out of scope / report-only" section so none is lost at merge. Acceptance: exactly one file matching `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/issue-updates/issue-810.*.md` exists, its filename timestamp equals the value of its own `Timestamp:` field, and it carries `Timestamp:`, the exact text, `PostedAs:` set to `body`, `comment` or `unknown`, and a `REPORT-ONLY-ITEMS: 9` line enumerating all nine.
- [x] [P7-T22] Update the record of this plan at `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/plan.2026-09-07T21-59.md` so every task from [P0-T1] through [P7-T21] reads `- [x] [P`, and check no task whose artifact is absent or whose artifact fields are incomplete. Acceptance: `git grep -c -e "^- \[ \] \[P" -- docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/plan.2026-09-07T21-59.md` prints exactly 2, being this task's own line and [P7-T23]'s. The pattern is start-anchored rather than a fixed string, because both task lines quote the checked-checkbox token `- [x] [P` inside their own prose while neither quotes the unchecked token in unescaped form, so the anchor is what guarantees that only a real task-line prefix at the start of a line can match and no quotation embedded mid-line can.
- [x] [P7-T23] Commit the code change and all evidence with `git add QuickFiler QuickFiler.Test docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810` followed by `git commit -m "fix(810): quickfiler teardown and dropdown residuals" -- QuickFiler QuickFiler.Test docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810`, then record it into `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/evidence/qa-gates/p7-t23-final-commit.md`, then mark [P7-T22] and [P7-T23] as `- [x] [P` in this plan file, then fold both into the same commit with `git add QuickFiler QuickFiler.Test docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810` followed by `git commit --amend --no-edit -- QuickFiler QuickFiler.Test docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810`. The record-then-amend shape exists because an artifact written after a clean-tree commit would otherwise leave the tree dirty. Acceptance: the artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:`, and after the amend `git status --porcelain --untracked-files=all` prints no line whose path lies under `QuickFiler/`, `QuickFiler.Test/` or `docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/`, and `git grep -e "^- \[ \] \[P" -- docs/features/active/2026-09-07-quickfiler-teardown-and-dropdown-residuals-810/plan.2026-09-07T21-59.md` exits 1 with no output, start-anchored for the reason [P7-T22] gives. The porcelain assertion is scoped to those three path prefixes because `.claude/agent-memory/` is a tracked tree this plan does not own.
