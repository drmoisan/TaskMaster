# Phase 2 — Decisive net481 Isolation Check (Revision R2 re-run)

Timestamp: 2026-09-14T10-05

Command: (read of the [P2-T11] artifact)

The artifact read is
`evidence/regression-testing/expect-fail-run.2026-09-13T18-22.md`, regenerated by the Revision R2
re-run of `[P2-T11]` on 2026-09-14. The superseded copy `[P1-T5]` preserved at
`evidence/regression-testing/expect-fail-run-superseded-probe-surface.2026-09-13T18-22.md` was
deliberately NOT read: the five isolation outcomes are properties of the run, and the run changed.
This artifact overwrites the version 1.0 artifact at this path.

EXIT_CODE: 0

Output Summary:

```
ChildDomain_HasNoSvgControlAssemblyLoaded OUTCOME=Passed
ChildDomain_HasNoAssemblyResolveHandlerBeforeInstall OUTCOME=Passed
ChildDomain_ConfigurationFileDeclaresNoNetstandardRedirect OUTCOME=Passed
NegativeControl_WithoutInstall_Netstandard21Throws OUTCOME=Passed
NegativeControl_HasNoUtilitiesCsAssemblyLoaded OUTCOME=Passed
```

The private AppDomain assembly-resolution field is present and readable on net481; the isolation
assertion did not skip.

Acceptance Condition: MET. All five named outcomes are recorded as `Passed` in the regenerated
`[P2-T11]` artifact, so the halt branch this task carries is not taken.

Each of the five was verified by a `Select-String -SimpleMatch -CaseSensitive` search of the
`[P2-T11]` artifact for the full `<testName> OUTCOME=Passed` literal, and each returned exactly one
hit.

## What this establishes, and what it does not

`ChildDomain_HasNoAssemblyResolveHandlerBeforeInstall` carries its own positive control: it reads the
handler count before `InstallProductionFallback()` and asserts 0, then calls the installer and reads
the count a second time and asserts it is greater than 0. Its passing therefore means the reflected
field is genuinely readable and genuinely changes value, rather than returning 0 unconditionally. The
`[P2-T1]` seam subscribes its handler for exactly this reason, even though every ladder rung in that
seam returns null.

`NegativeControl_WithoutInstall_Netstandard21Throws` passing means the installer-free domain still
cannot bind the `2.1.0.0` identity, so isolation has not been lost and no positive result from this
harness is vacuous on isolation grounds.

This task's own condition is met, and it is recorded as met. It does not discharge `[P2-T11]`, whose
acceptance condition is NOT MET for an unrelated reason recorded in that artifact: the Deedle
observation returned `INVOKED-NO-EXCEPTION` rather than a value beginning with
`NETSTANDARD-BIND-FAILURE:`. The two results are complementary rather than in tension. Isolation
being intact is precisely what makes that `INVOKED-NO-EXCEPTION` reading a genuine observation about
the probed member, rather than an artefact of a leaking domain.
