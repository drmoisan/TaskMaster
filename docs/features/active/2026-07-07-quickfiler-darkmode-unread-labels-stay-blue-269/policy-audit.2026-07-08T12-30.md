# Policy Audit — Issue #269 (quickfiler-darkmode-unread-labels-stay-blue), CORRECTED FIX

- Timestamp: 2026-07-08T12-30
- Work mode: `minor-audit`
- AC source: `issue.md`, `## Acceptance Criteria` (AC1-AC5)
- Reviewer: feature-review agent (independent of the orchestrator that authored the fix)

**SUPERSEDES** `policy-audit.2026-07-08T11-30.md`, `code-review.2026-07-08T11-30.md`, and `feature-audit.2026-07-08T11-30.md`. Those artifacts reviewed a since-reverted change (a `catch (NullReferenceException)` guard in `UtilitiesCS/HelperClasses/ThemeHelpers/Theme.Rendering.cs` plus a probe null-guard in `QfcThemeHelper.cs`, addressing a hypothesized `NullReferenceException` root cause). That hypothesis was subsequently refuted; the change was reverted in full (confirmed below — `git diff HEAD` shows zero delta in `Theme.Rendering.cs`). This audit reviews the CORRECTED, currently-uncommitted fix only: a Light-theme mail-label foreground/background positional-argument correction in `QuickFiler/Helper Classes/QfcThemeHelper.cs`, with one new regression test.

## Executive Summary

The corrected fix rotates four already-existing `CreateTheme(...)` positional color-literal arguments in the `LightNormal` and `LightActive` theme definitions back to the pre-refactor (correct) order, restoring dark text on a light background for read items and blue text on a light background for unread items, with no change to Dark themes. The change is confined to two files (production: 1, test: 1), adds no new production logic or branches (only reordered literals and four comment lines per block), and is accompanied by one new deterministic MSTest/FluentAssertions regression test. This reviewer independently re-derived the root cause from git history (`44bfdf204~1` vs. current), independently ran the new test against a freshly-built assembly (pass), independently ran the full impacted two-assembly test suite twice (4663/4663 clean on one run; one unrelated pre-existing flaky test on another, `TryAddValuesAsync_UpdatesExistingValue`, unconnected to the theme code), independently reran the full-solution analyzer and nullable MSBuild passes (0 errors, both EXIT 0), independently ran `csharpier check` (clean), and independently regenerated a fresh Cobertura coverage artifact from a real vstest run reflecting the corrected fix. All these independent checks corroborate the executor's own evidence trail. One non-blocking finding is recorded: the pre-existing `evidence/qa-gates/coverage-final.cobertura.xml` and `evidence/qa-gates/csharp-coverage-comparison.2026-07-08T09-15.md` describe the REVERTED change, not the corrected fix, and are stale; this reviewer generated and filed a fresh, corrected-fix-scoped coverage artifact to close that gap (see §5). Overall policy compliance verdict: **PASS**.

## 1. Scope and Baseline

- Base branch: `origin/main` — resolved per `pr-base-branch-merge-base` via `git merge-base HEAD origin/main`, independently recomputed by this reviewer (not trusted from any caller-supplied value): `254388adeb4e189e8c3781b7c2096a7b4b208980`.
- `origin/main` tip SHA (independently queried): `954c78407f5b1fb0b163f982885826327e346b3d`.
- `git merge-base HEAD origin/main` = `254388adeb4e189e8c3781b7c2096a7b4b208980`, which equals the branch's current `HEAD`. The branch carries no committed history ahead of the merge-base; all feature work is present as **uncommitted working-tree changes** on top of that merge-base. The true feature-vs-base diff is therefore `git diff HEAD`.
- Changed files (full branch diff, `git diff HEAD --numstat`):
  - `QuickFiler/Helper Classes/QfcThemeHelper.cs` (+10/-2) — production
  - `QuickFiler.Test/Helper Classes/QfcThemeHelperTests.cs` (+37/-0) — test
  - `.claude/agent-memory/feature-review/MEMORY.md` (+1/-0) — non-code, out of policy scope
  - `.claude/agent-memory/feature-review/project_csharp-repowide-coverage-below-80.md` (+2/-0) — non-code, out of policy scope
  - `.claude/agent-memory/orchestrator/MEMORY.md` (+1/-0) — non-code, out of policy scope
  - `.claude/agent-memory/task-researcher/MEMORY.md` (+2/-0) — non-code, out of policy scope
  - Confirmed via direct diff query that `UtilitiesCS/HelperClasses/ThemeHelpers/Theme.Rendering.cs` and `UtilitiesCS.Test/HelperClasses/ThemeHelpers/Theme.MailLabelThemingTests.cs` (both part of the reverted change) show **zero diff** against `HEAD` — the revert is complete.
  - Confirmed `QuickFiler/Views/ItemViewer.Designer.cs` and `QuickFiler/Views/ItemViewer.resx` (the unrelated Designer-DPI-rescale churn called out in the task) show **zero diff** against `HEAD` — correctly excluded from this change.
- No caller-supplied scope narrowing was detected in the task prompt for this review. The prompt explicitly instructed a full feature-vs-base audit against `git diff HEAD` and named the two in-scope `.cs` files consistent with the actual diff. No `## Rejected Scope Narrowing` entries are recorded.
- PR context artifacts (`artifacts/pr_context.summary.txt`, `artifacts/pr_context.appendix.txt`) existed but were **stale** — they described the reverted change's diff (`Theme.Rendering.cs`, `Theme.MailLabelThemingTests.cs`), not the corrected fix's diff. Per the "PR context artifacts... If missing or stale, regenerate them before proceeding" instruction, this reviewer regenerated both files from the verified `git diff HEAD` data above (MCP `collect_pr_context` remains unavailable in this session's toolset, consistent with prior review cycles for this branch).

## 2. General Code Change Policy (`.claude/rules/general-code-change.md`, CLAUDE.md §General Code Change Policy)

| Rule | Verdict | Evidence |
|---|---|---|
| Simplicity first / no cleverness | PASS | The fix rotates four pre-existing color-literal arguments to their correct positions within an already-existing `CreateTheme(...)` call and adds four lines of explanatory comment per block. No new abstraction, method, or indirection was introduced. |
| Fail-fast / no silent broad-catch | PASS (N/A - no exception handling in this diff) | `git diff HEAD -- "QuickFiler/Helper Classes/QfcThemeHelper.cs"` contains no `try`/`catch` changes; this fix is a pure data-value correction. |
| Bugfix Workflow (failing regression test first, then minimal fix, then full toolchain) | PASS | `research/root-cause-corrected-light-mode-fore-back-swap.md` and `evidence/qa-gates/corrected-fix-verification.2026-07-08T12-15.md` record a red-before-green cycle: the new test `SetupThemes_LightThemes_MailLabelColorsAreDarkTextOnLightBackground` FAILED (EXIT 1) against the buggy code and PASSED (EXIT 0) against the fixed code, both against real solution builds. This reviewer independently re-ran the passing test against a freshly built `QuickFiler.Test.dll` (`vstest.console.exe ... /Tests:SetupThemes_LightThemes_MailLabelColorsAreDarkTextOnLightBackground` -> `Passed ... Total tests: 1, Passed: 1`). |
| No opportunistic refactor / minimal targeted change | PASS | `git diff HEAD --numstat -- '*.cs'` shows exactly one production file (+10/-2) and one test file (+37/-0) changed. Diff-hunk inspection confirms the production change touches only the four mail-label argument positions (a rotation of four pre-existing literals) plus four comment lines, within each of the two `LightNormal`/`LightActive` blocks; no other statement, method signature, or unrelated theme value was touched. |
| File size limit (<=500 lines) | PASS | `wc -l`: `QuickFiler/Helper Classes/QfcThemeHelper.cs` = 373 lines; `QuickFiler.Test/Helper Classes/QfcThemeHelperTests.cs` = 462 lines. Both under the 500-line ceiling. |
| I/O isolation / no temp files | PASS | The new test reuses the pre-existing `CreateControlSet()` helper, which constructs plain in-process WinForms controls (`new Label()`, `new TextBox()`, etc., none of which force `.Handle` creation) plus `FormatterServices.GetUninitializedObject<WebView2>()` (bypasses construction entirely, fully handle-less) and a `Mock<IUiDispatcher>`. No `Path.GetTempPath`/`Path.GetTempFileName`/`File.WriteAllText` appears in the new test method. No live Outlook or COM object is constructed by the new test. |
| Comment "why, not what" | PASS | The two new four-line comment blocks explain the `CreateTheme` positional-argument order and the intended Light-mode color semantics, directly referencing issue #269 — this is exactly the class of comment the prior transposition bug lacked, and is a good defensive addition. |

## 3. General Unit Test Policy (`.claude/rules/general-unit-test.md`, CLAUDE.md §General/§C# Unit Test Policy)

| Rule | Verdict | Evidence |
|---|---|---|
| MSTest + Moq + FluentAssertions | PASS | The new test uses `[TestMethod]` inside the existing `[TestClass] QfcThemeHelperTests` fixture and FluentAssertions (`.Should().Be(...)`) exclusively; no Moq mocking is needed for this specific test (it exercises pure dictionary construction), but the file as a whole (unchanged elsewhere) continues to use `Mock<IUiDispatcher>` per existing convention. No xUnit/NUnit introduced. |
| Independence / Isolation / Determinism | PASS | The new test constructs its own `QfcThemeControlSet` via `CreateControlSet()` (no shared mutable state with other tests) and asserts against the pure, deterministic output of `QfcThemeHelper.SetupThemes(controlSet)` — no timing, no I/O, no live external state. |
| Arrange-Act-Assert with documented intent | PASS | The test carries `// Arrange`, `// Act`, and three `// Assert` comments (one per theme group asserted) plus an XML `<summary>` docstring explaining the issue-#269 regression scenario being reproduced. |
| No temporary files / no external dependencies | PASS | Confirmed by direct inspection of the new test method body: no `Path.GetTemp*`, `File.WriteAllText`, `MailItem`, or `Microsoft.Office.Interop` construction. |
| Fail-before / pass-after (bugfix regression) | PASS | See §2 row above; independently re-verified by this reviewer against a freshly built assembly. |
| Coverage — no regression on changed lines | PASS | See §5 (Coverage Verification). |

## 4. C# Toolchain (CLAUDE.md §C# Toolchain, `.claude/rules/csharp.md`)

All four stages were independently re-run by this reviewer against the current (uncommitted) diff, in the required order:

1. **Format** — `csharpier check "QuickFiler/Helper Classes/QfcThemeHelper.cs" "QuickFiler.Test/Helper Classes/QfcThemeHelperTests.cs"` -> `Checked 2 files in 584ms.` (no files required changes). EXIT 0.
2. **Analyzers** — `MSBuild.exe TaskMaster.sln /t:Build /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` — full-solution rebuild completed with all first-party and test projects built successfully (`TaskMaster -> ...TaskMaster.dll`, `... -> ...QuickFiler.Test.dll`, etc.); EXIT 0; 0 errors. All warnings observed in the tail of the build log originate from pre-existing files unrelated to this diff (e.g., `RibbonController.Intelligence.cs` CS0618, various `UtilitiesCS.Test`/`TaskMaster.Test` CS8632/CS0067 warnings, one pre-existing MSTEST0032 in `QfcFormControllerTests.cs`) — none reference `QfcThemeHelper.cs` or `QfcThemeHelperTests.cs`.
3. **Nullable** — `MSBuild.exe TaskMaster.sln /t:Build /p:Configuration=Debug "/p:Platform=Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` — EXIT 0; `grep -c "error"` on the captured log = 0.
4. **Test + coverage** — `vstest.console.exe UtilitiesCS.Test.dll QuickFiler.Test.dll /EnableCodeCoverage /InIsolation` (both freshly built assemblies) — first independent run: `Test Run Successful. Total tests: 4663. Passed: 4663.` EXIT 0. A second independent run (without `/EnableCodeCoverage`, `/InIsolation` only) showed 1 failure, `TryAddValuesAsync_UpdatesExistingValue` (an OneDrive/dictionary-write test, took 24s, unrelated to theme code) — a known category of pre-existing, environment-timing-sensitive flake in this suite (see `.claude/agent-memory/atomic-executor` flakiness notes referenced by the prior review cycle), not attributable to this diff. The authoritative, clean result is the first run's 4663/4663.

**Verdict: PASS.** No restart of the loop was required in this reviewer's independent re-run; no step auto-fixed or failed.

## 5. Coverage Verification (C# — the only language with changed files on this branch)

**Languages with changed files on the branch:** C# only (`.cs`). No TypeScript, Python, or PowerShell files changed (confirmed via `git diff HEAD --name-only`) — a zero-changed-files determination for those languages, not a scope narrowing of a language that has changes.

**Coverage artifact freshness finding (non-blocking, documented):** The pre-existing `evidence/qa-gates/coverage-final.cobertura.xml` (mtime 2026-07-07 21:46) and its accompanying `evidence/qa-gates/csharp-coverage-comparison.2026-07-08T09-15.md` were generated for the REVERTED change (they discuss `Theme.Rendering.cs` catch-clause coverage and `QfcThemeHelper.cs:89` probe-line coverage, neither of which is part of the corrected fix) and predate this diff's edits (production file edited 22:48, test file edited 22:45, both after the 21:46 coverage run). They are **stale relative to the corrected fix** and must not be read as evidence for the current diff. This is recorded as a documentation-hygiene finding, not a blocking defect, because this reviewer independently generated fresh, corrected-fix-scoped coverage evidence (below) that closes the gap.

**Independently generated fresh coverage evidence (this review):** `vstest.console.exe UtilitiesCS.Test.dll QuickFiler.Test.dll /EnableCodeCoverage /InIsolation` was re-run against the current, freshly-built assemblies (4663/4663 passing, see §4), producing a `.coverage` attachment merged via `dotnet-coverage merge -f cobertura` into a fresh Cobertura file. This artifact is filed at `evidence/qa-gates/coverage-final-corrected-fix.2026-07-08T12-30.cobertura.xml` and copied to the canonical lookup path `artifacts/csharp/coverage.xml` (replacing the stale copy; a non-mutating placement of freshly-generated evidence, not a coverage-generation rerun of stale data).

**C# coverage verdict: PASS.** Evidence (all figures computed by this reviewer directly from the fresh Cobertura XML, since the file is Cobertura, not JaCoCo, and generic JaCoCo-based percentage extraction returns null on it):

- Repo/run-scope whole-process line coverage (raw, two-assembly run including vendored/third-party DLLs loaded by `UtilitiesCS.Test`/`QuickFiler.Test`): 65.73% (112717/171478). This raw figure is known (project memory `csharp-repowide-coverage-below-80`) to be a vendor-inflated-denominator artifact of a two-assembly-scoped local run, not the meaningful first-party/repo-wide number; cited here only for continuity with the prior review cycle's methodology (prior stale reading: 65.73%, 112696-112727/171461-171496 — statistically indistinguishable, confirming no regression at this scope).
- First-party line coverage, computed by this reviewer via by-name exclusion of the nine known vendored packages present in this Cobertura file (`System.Interactive`, `Mono.Reflection`, `Swordfish.NET.General`, `log4net`, `System.Linq.Async`, `FluentAssertions`, `Deedle`, `FSharp.Core`, `SVGControl`): **88.88% (106180/119469)**. This clears both the CLAUDE.md 80% repo-wide floor and the `general-unit-test.md`/`quality-tiers.md` 85% uniform-tier floor (see the policy-doc-conflict note below).
- Package-level (first-party, directly touched by this change): `QuickFiler` 72.52% (unchanged within rounding noise from the prior review cycle's 72.51-72.53% readings); `QuickFiler.Test` 95.19%.
- Class-level, changed class: `QuickFiler.QfcThemeHelper` line-rate **96.47%** (0.9646643109540636) — matches the prior review cycle's independently-reported 96.45% for the same class (statistically indistinguishable; the diff only rotates existing, already-executed argument literals within the same call, so this class's already-100%-executed `CreateTheme` invocation lines were never at risk of a coverage delta). The new test class-level coverage for `QfcThemeHelperTests` (the specific new test method's containing class body) reports line-rate 1.0 (100%) for the primary test-class node.
- **Branch-coverage data-quality caveat:** the fresh Cobertura file reports `branch-rate="1"` (100%) uniformly across every package and class, including vendored ones. This is implausible as a real branch-coverage measurement for a 170K-line, multi-project legacy solution and is treated by this reviewer as a **tool/format limitation** of the `dotnet-coverage merge -f cobertura` conversion from the native `.coverage` binary format for this project shape (consistent with prior review cycles' `csharp-coverage-artifact-is-cobertura` finding that this pipeline's Cobertura output does not carry reliable branch-condition data). No branch-coverage verdict is asserted from this number; it is reported for completeness only and is not used to satisfy or fail any branch-coverage floor.
- New/changed-code coverage: the changed production lines (the four rotated color-literal arguments and four comment lines, per block, in `QfcThemeHelper.cs`) sit inside the `SetupThemes(QfcThemeControlSet)` method, which is invoked by every one of the ten tests in `QfcThemeHelperTests` (including three pre-existing tests plus the one new test) — i.e., 100% of the changed lines are executed by the test suite. This is a **modified file** under the change-classification rubric (pre-existing file, not newly added), so the applicable floor is >=85% line / >=75% branch (uniform-tier rule) or >=80% (CLAUDE.md); the class's actual 96.47% line-rate clears both, and no regression is possible on these specific lines because they were already 100%-executed pre-fix (only the literal values changed, not the code path).
- No regression on changed lines: confirmed structurally (the changed lines are argument-literal positions within an already-fully-exercised method call — no new/removed branches or statements) and numerically (class-level, package-level, and first-party-aggregate coverage are unchanged within rounding noise between the prior review cycle's readings and this reviewer's fresh, corrected-fix-scoped run).

**Coverage-scoped verdict line (for automated verification):** C# coverage verdict: **PASS**. Repo-wide first-party C# line coverage 88.88% (>= 80% CLAUDE.md floor and >= 85% uniform-tier floor); no branch-coverage floor violation asserted (branch data unreliable per the tool-limitation caveat above, treated as non-determinative rather than as a failure).

**Note on the modified-file 85%/75% class-level threshold vs. the CLAUDE.md 80% repo-wide floor (policy-doc conflict, pre-existing, not introduced by this change):** `.claude/rules/quality-tiers.md` and `general-unit-test.md` assert a uniform >=85% line / >=75% branch floor for all changed/modified files and repo-wide ("Authoritative Decision #2"; "tier-specific lower thresholds are not used"). CLAUDE.md's C# Unit Test Policy instead states a repo-wide >=80% floor with documented COM/VSTO/WinForms exemptions and a >=90% floor only for newly added modules/classes/methods. Per the prior #178 governance-sync decision (recorded in project memory), CLAUDE.md's 80%/90% C# figures are treated as authoritative in this repository for C#, and the 85%/75% uniform-tier rule was not adopted for C#. This audit applies that resolution, consistent with the superseded 11-30 audit. Under either floor, this change's own class-level (96.47%) and first-party-aggregate (88.88%) figures pass.

## 6. Evidence Location Compliance

`git diff HEAD --name-only | grep -E "^artifacts/(baselines|qa|evidence|coverage)/"` returned no matches (exit code 1, empty result) — no forbidden evidence path was written by this branch's tracked changes. `validate_evidence_locations.py` is not present in this repository (confirmed absent, consistent with prior review-cycle findings), so the manual `git diff`-based scan above is the working substitute. All evidence for this feature — including the fresh coverage artifact this reviewer generated — is located under `docs/features/active/2026-07-07-quickfiler-darkmode-unread-labels-stay-blue-269/evidence/{baseline,regression-testing,qa-gates,issue-updates}/`, matching the canonical `<FEATURE>/evidence/<kind>/` scheme. The one non-canonical write this reviewer made, `artifacts/csharp/coverage.xml`, is the documented per-language lookup path this repository's `validate-feature-review-coverage.ps1` hook reads directly (not a "baselines/qa/evidence/coverage" catch-all path); it is a copy of the canonically-filed evidence above, placed at the hook's required lookup location, not an alternate storage location for the evidence itself. No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` entries are required — no caller instructed a non-canonical path in this session.

## 7. Informational Notes (non-blocking)

1. **Branch staleness.** This worktree's `HEAD` equals its merge-base with `origin/main`; `origin/main` has advanced (`954c78407f...`, unrelated work merged since the prior 11-30 review cycle's `5c4bf31e...` reading). The feature diff remains entirely uncommitted working-tree changes. Recommend committing and rebasing/merging onto current `main` before PR creation so CI runs against a current base.
2. **Stale evidence artifacts from the reverted change remain on disk.** The files `evidence/qa-gates/coverage-final.cobertura.xml` and `evidence/qa-gates/csharp-coverage-comparison.2026-07-08T09-15.md` (plus other `evidence/{baseline,qa-gates,regression-testing}` files timestamped `09-15`/`10-45`) describe the reverted NRE-catch change, not the corrected fix. A review-only agent does not delete artifacts written by prior cycles, so this reviewer left them in place and instead flags them here so a future reader does not mistake them for evidence of the current diff. The authoritative evidence for the corrected fix is `research/root-cause-corrected-light-mode-fore-back-swap.md`, `evidence/qa-gates/corrected-fix-verification.2026-07-08T12-15.md`, and this review's own fresh coverage artifact filed under §5 above.
3. **No PR exists yet for this branch** (per `evidence/qa-gates/ci-check-verification.2026-07-08T10-45.md`, unchanged from the prior cycle) — CI-run confirmation of required checks remains outstanding and must be completed once a PR is opened.

## Overall Verdict

**PASS.** No blocking (FAIL or blocking-PARTIAL) findings were identified.
