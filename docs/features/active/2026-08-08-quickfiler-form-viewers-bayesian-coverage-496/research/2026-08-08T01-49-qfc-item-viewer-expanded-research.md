## `QuickFiler/Viewers/QfcItemViewerExpanded.cs` (63 lines) — a `UserControl`, carries `[ExcludeFromCodeCoverage]`

- **Epic child:** F15 (`quickfiler-form-viewers-bayesian-coverage`, issue #496), parent epic #136.
- **Measured baseline:** absent from Cobertura output today (suppressed by its own type-level attribute), same "an absent file is not a covered file" situation as `QfcFormViewer.cs`.
- **`[ExcludeFromCodeCoverage]` location:** type-level, `QfcItemViewerExpanded.cs:18`, on `public partial class QfcItemViewerExpanded : UserControl` (verified by reading the file directly). Independently cross-checked against the F16 capstone's Q1 census, which lists this exact site (`QfcItemViewerExpanded.cs`, line 18, type-level, `partial`, 2 files suppressed) and against F1's own in-flight research note (`docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/research/2026-08-07T22-15-quickfiler-coverage-ledger-research.md:259`, same site).
- **Files suppressed by this one attribute:** 2 — `QfcItemViewerExpanded.cs` itself and `QfcItemViewerExpanded.Designer.cs` (942 lines).

### Independent re-verification: zero production consumers

Repo-wide search (this session, via `Grep`) for `new QfcItemViewerExpanded(`, `: QfcItemViewerExpanded`, `QuickFiler.QfcItemViewerExpanded`, and `partial class QfcItemViewerExpanded` returned:

- The class's own two declaring files (`QfcItemViewerExpanded.cs:19` and `QfcItemViewerExpanded.Designer.cs:3`).
- A **different, unrelated** class `QfcItemViewerExpandedLight` (`Viewers/QfcItemViewerExpandedLight.cs:15`, `Viewers/QfcItemViewerExpandedLight.Designer.cs:3`) — a similarly-named but distinct type, not a subclass or consumer of `QfcItemViewerExpanded`. `QfcItemViewerExpandedLight`'s own Designer file constructs no `QfcItemViewerExpanded` instance (verified: `QfcFormViewerExpanded.Designer.cs:42` constructs `QuickFiler.QfcItemViewerExpandedLight`, not `QfcItemViewerExpanded` — a third, still-different type).
- Coverage-XML artifacts under `docs/features/archive/**/evidence/**` recording historical `line-rate="0"` measurements for this class (three archived features across 2026-04 and 2026-06), consistent with it having been dead and exempted for some time.
- Cross-referencing research/plan artifacts from sibling features (F1's own ledger research, F16's capstone research) that independently record the same file at the same line and reach the same "2 files suppressed" conclusion.

**No production `.cs` file under `QuickFiler/` (or elsewhere in the repository) constructs, subclasses, or references `QfcItemViewerExpanded` by type name.** The class is compiled (it is in `QuickFiler.csproj`'s `<Compile Include>` list, confirmed by the F16 capstone's independent 121-file recount) but has zero live callers. This confirms the task brief's "already-verified findings" claim independently, using a broader search pattern (type declarations, constructor calls, base-list references) than a single `new QfcItemViewerExpanded(` grep alone.

**This is a candidate latent-defect finding: a fully dead, compiled `UserControl` type.** Per the epic's "Latent Defect Promotion" section, this should be routed through the MCP promotion lifecycle as a GitHub issue at execution time (not promoted by this research artifact, and not fixed here). The epic's no-behavior-change NFR and its Scope section make **deletion** of dead code a separate decision from this child's coverage mandate — F6 hit the analogous situation with its dead `#region Email Sorting To Rewrite` and the epic ruled deletion belongs to the owning child (F6) precisely because the dead code was entirely self-contained within F6's own file and had no sibling contention. `QfcItemViewerExpanded.cs` is different in one respect worth flagging for the planner: it is not a dead *region inside* a live file, it is an **entire dead file** with a paired 942-line dead Designer file, and unlike F6's region, deleting it would also remove the file's `<Compile Include>` entries from both `QuickFiler.csproj` and reduce F1's compiled-file denominator (120 vs. 121, the exact "F16 must re-derive and reconcile" scenario the epic already anticipates for compiled-file-count changes). **Recommendation for the plan: do not delete it in this child.** Deletion changes the epic's dynamic denominator mid-wave and is explicitly the kind of decision the epic routes to F16 for the structurally analogous `IQfcFormController.cs` dead-interface case. Cover it instead (see Disposition below), and let the promoted issue carry the deletion recommendation forward for a future, non-epic-scoped cleanup, exactly as F6's `IQfcFormController.cs` recommendation was routed to F16 rather than actioned by F6 itself. This is consistent with `issue.md`'s own framing that "it may simply need coverage rather than deletion" since issue #136's scope is no-behavior-change.

### Current structure

`public partial class QfcItemViewerExpanded : UserControl`. Members:

- `QfcItemViewerExpanded()` — calls `InitializeComponent()`, captures `_context = SynchronizationContext.Current`, calls `InitTipsLabelsList()`. No branch.
- `TipsLabels` (get-only, backed by `_tipsLabels`), `Controller` (get/set, backed by `_controller`), `UiSyncContext` (get-only) — plain forwards, no branch.
- `InitTipsLabelsList()` — builds a fixed 9-element `List<Label>` from Designer-declared label fields (`LblAcOpen`, `LblAcPopOut`, `LblAcTask`, `LblAcDelete`, `LblAcAttachments`, `LblAcConversation`, `LblAcEmail`, `LblAcFolder`, `LblAcSearch`). No branch — a straight-line list literal.

**This file has zero branch points.** Every member is either a trivial forward or a straight-line list construction. Dependencies: pure WinForms (`UserControl`, `Label`, `SynchronizationContext`), plus `Microsoft.Web.WebView2.Core` (`using` present, but no WebView2 type is referenced in this `.cs` file's own executable code — the reference is presumably consumed only by the Designer partial, e.g. a `CoreWebView2` control field declared there). `QuickFiler.Interfaces` (`IQfcItemController`) is used only as the `Controller` property's type — no interop, no COM, no I/O in this file.

### What is already tested vs. the gap

Nothing — no test file exists (`QuickFiler.Test/Viewers/QfcItemViewerExpanded*` does not exist, verified by `Glob`), and the type is suppressed from Cobertura today regardless. Because it has zero production consumers, "coverage" here is necessarily coverage of a construct-and-inspect shape (construct the control, assert `TipsLabels` contains the nine expected label references, assert `Controller`'s get/set round-trips) rather than coverage of any behavior a real caller exercises — a direct consequence of the type being dead, not a test-design choice.

### Proposed seams

**No seam is needed.** There is no COM dependency, no I/O, no un-mockable collaborator anywhere in this file. `InitTipsLabelsList()` is `private` but its effect (`TipsLabels`) is publicly observable through the `TipsLabels` getter, so no reflection or seam is required to verify it ran — construct the control and read `TipsLabels`.

### STA / DEC-1 last-resort clause

**Required for construction**, by the same reasoning as `QfcFormViewer.cs`: `QfcItemViewerExpanded` is `UserControl`-derived (not `Form`-derived, but the epic's Shared Design §3 STA clause explicitly covers "in-memory, never-shown WinForms **controls**," naming `TableLayoutPanel`, `Label`, `Panel`, `CheckBox` as examples — a `UserControl` with a `WebView2`-bearing Designer partial is squarely in this category, and is a stronger case for the clause than a plain `Panel`, given the WebView2 control's own construction is at least as apartment-sensitive as any listed example). Construct one instance on an STA thread, dispose in `finally`, reusing the `RunWithViewer` harness shape verbatim; a dedicated `QuickFiler.Test/Viewers/QfcItemViewerExpanded.StaTests.cs` (new file) documents that no seam suffices because `InitializeComponent()` cannot be invoked outside real construction. **Never** show the control or attach it to a live parent with a window handle.

**Verified directly (this session):** `QfcItemViewerExpanded.Designer.cs` constructs exactly one WebView2-related object, `this.L0v2h2_Web = new Microsoft.Web.WebView2.WinForms.WebView2();` (line 80), and separately builds a `CoreWebView2CreationProperties` value object (lines 31, 830-836) that is assigned to `L0v2h2_Web.CreationProperties` — a plain property assignment, not an initialization call. A repo-wide-scoped grep of this Designer file for `CoreWebView2Environment` and `EnsureCoreWebView2Async` returned no matches. Constructing the hosting `WebView2` WinForms control does not, by itself, invoke `EnsureCoreWebView2Async()` or otherwise touch the Evergreen runtime (that call happens later, typically on `HandleCreated`, and never fires for a control that is never shown/parented to a handle-bearing form). **The STA-construct-and-dispose pattern is therefore confirmed safe for this file and requires no external runtime dependency** — the fourth epic ground (prohibited-to-execute adapter) does not apply, and the disposition below (remove-and-cover) is not conditional.

### Disposition proposal: remove-and-cover

**Remove the `[ExcludeFromCodeCoverage]` attribute at `QfcItemViewerExpanded.cs:18` and cover the file**, for the same reasons as `QfcFormViewer.cs`: none of the three CLAUDE.md grounds nor the epic's fourth ground apply on inspection (it is not VSTO lifecycle; it is control-derived rather than the CLAUDE.md wording's more form-centric framing, but the epic's own STA clause already treats WinForms controls as coverable via unshown construction rather than exempt; it has zero Outlook Interop; it is not a 1:1 external-runtime forward). The epic's Shared Design §1 "refactor first, exempt only the irreducible remainder" applies, and here the irreducible remainder is zero — a hand-written, non-generated, 63-line file with zero branches and zero un-mockable dependencies is squarely testable via the ratified DEC-1 construction pattern, contingent on the WebView2-construction check noted above.

Since the WebView2-construction check above found no Evergreen-runtime-dependent call anywhere in the Designer partial's `InitializeComponent()`, no fallback exemption is warranted for any part of this file.

### Zero-branch caveat

**Applies to this file's own `.cs` content.** `QfcItemViewerExpanded.cs` has zero branch points (no `if`, `else`, loop, switch, or short-circuit operator anywhere in its 63 lines). Once instrumented (attribute removed), its Cobertura `branches-valid` will be `0`, and per the epic's binding rule this must be reported as branch coverage `N/A`, never `0%`, and must never count as a gate failure. Line coverage remains a real, gate-relevant percentage (target >= 80%) since the file has real executable lines (the constructor body, the three property accessors, `InitTipsLabelsList()`'s list construction).
