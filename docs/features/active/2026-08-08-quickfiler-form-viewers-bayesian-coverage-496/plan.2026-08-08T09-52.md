# 2026-08-08-quickfiler-form-viewers-bayesian-coverage — Plan

- **Issue:** #496
- **Parent epic:** #136 (`quickfiler-per-file-coverage`), child F15, wave 1
- **Owner:** drmoisan
- **Work Mode:** full-feature (AC source: `spec.md` and `user-story.md`)
- **Last Updated:** 2026-08-08T09-52
- **Status:** Draft
- **Version:** 0.2
- **Task count:** 8 phases, 108 tasks (`P0`=22, `P1`=14, `P2`=14, `P3`=10, `P4`=16, `P5`=9, `P6`=6, `P7`=17). Counted mechanically against `^- \[ \] \[P\d+-T\d+\]`.

## Required References

- `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md` (policy-compliance-order)
- `docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/issue.md`, `spec.md`, `user-story.md`
- `docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/research/*.md` (six artifacts)
- `docs/features/epics/quickfiler-per-file-coverage/epic.md` (binding rulings DEC-1, DEC-5, `#457`, zero-branch N/A rule, shared-csproj rules)
- `docs/features/active/2026-08-07-quickfiler-qfc-form-explorer-controller-coverage-435/spec.md` §"Cross-Child Contract Notes", §"Interface freeze rationale" (`IQfcFormViewer` is FROZEN — F15 owes no interface edit)

**All work must comply with these policies; do not duplicate their content here.**

## Evidence Location

All evidence is written under `docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/evidence/<kind>/` (canonical scheme per `evidence-and-timestamp-conventions`). No task in this plan writes to `artifacts/baselines/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/coverage/`, or any other non-canonical path.

## Decisions Record (binding on this plan; do not re-litigate at execution time)

1. `IQfcFormViewer.cs` is frozen — no task adds, removes, or renames a member on it.
2. Seam shape for `BayesianPerformanceController` is the injectable-delegate tier only (`ViewerShowAction`, `ViewerFactory`), following `EfcHomeController.cs:294` verbatim. No interface seam, no adapter.
3. `BayesianPerformanceViewer.cs` and `ToolStripMenuItemCb.cs` need no new production seam — existing `virtual`/`internal set` members and public constructors already suffice.
4. `QfcFormViewer.cs` and `QfcItemViewerExpanded.cs` are covered via DEC-1 unshown-STA-construction; their `[ExcludeFromCodeCoverage]` attributes are removed, not ratified as exemptions.
5. Generated Designer files (4) are `measured-not-gated` per DEC-5; `AssemblyInfo.cs` is `interface-only / not-measured`; `Resources.Designer.cs`/`Settings.Designer.cs` are `measured-not-gated`. No task writes a shape-assertion test to manufacture coverage for any of the seven.
6. `ReSortItem()`'s `item is not null` true branch is a documented residual gap (F8-owned `EfcHomeController` construction) — not fixed, not forced, recorded against F1's ledger evidence.
7. `BayesianPerformanceController.cs:58`'s unawaited `ProgressPackage.InitializeAsync(...)` call is left byte-for-byte unmodified and promoted as a GitHub issue, never fixed or awaited.
8. `QfcItemViewerExpanded` dead-code finding is covered (not deleted) and promoted as a GitHub issue recommending future removal.
9. `ToolStripMenuItemCb`'s constructor `if (Checked)` true branch is unreachable via any public construction path (verified: `_checked` defaults `false` and nothing sets it before the constructor's own read) — documented as a residual, irreducible branch gap, not forced.
10. `csharpier` is invoked as `dotnet tool run csharpier format .` / `... check .` (v1 CLI, pinned 1.2.6) — the bare form in `CLAUDE.md` is stale for this pinned version; this deviation is recorded in evidence, not by editing `CLAUDE.md`.
11. Repository-wide coverage is compared as a self-consistent before/after pair on this branch using the identical command and post-processing (`scripts/vscode/Invoke-MSTestWithCoverage.ps1`), never against an imported figure.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture & Toolchain Bootstrap

- [ ] [P0-T1] Read `CLAUDE.md` in full. Acceptance: no output artifact required; confirmed by inclusion in `[P0-T7]`'s file list.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md` in full.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md` in full.
- [ ] [P0-T4] Read `.claude/rules/csharp.md` in full.
- [ ] [P0-T5] Read `docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/issue.md`, `spec.md`, and `user-story.md` in order.
- [ ] [P0-T6] Read `docs/features/epics/quickfiler-per-file-coverage/epic.md`'s DEC-1, DEC-5, and `#457` lambda-suppression-trap sections, plus `docs/features/active/2026-08-07-quickfiler-qfc-form-explorer-controller-coverage-435/spec.md`'s "Cross-Child Contract Notes" and "Interface freeze rationale" sections.
- [ ] [P0-T7] Record `evidence/baseline/phase0-instructions-read.<ts>.md` with `Timestamp:`, `Policy Order:` (the four files from `[P0-T1]`-`[P0-T4]`), and the explicit list of all files read in `[P0-T1]`-`[P0-T6]`. Acceptance: artifact contains all six required fields/lists.
- [ ] [P0-T8] Record git baseline (`git status --porcelain`, `git rev-parse HEAD`, current branch name) at `evidence/baseline/git-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [ ] [P0-T9] Verify/install the repo-local .NET SDK via `pwsh -NoProfile -Command "& ./scripts/vscode/Install-RepoDotNetSdk.ps1"`; record `evidence/baseline/toolchain-bootstrap-sdk.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [ ] [P0-T10] Restore the local dotnet tool manifest via `./.dotnet-sdk/dotnet.exe tool restore` (installs csharpier 1.2.6); record `evidence/baseline/toolchain-bootstrap-tools.<ts>.md`.
- [ ] [P0-T11] Restore NuGet packages for `packages.config` projects via `pwsh -NoProfile -Command "& ./scripts/vscode/Invoke-Restore.ps1"` (`packages/` is gitignored; msbuild does not restore `packages.config` projects); record `evidence/baseline/nuget-restore.<ts>.md`.
- [ ] [P0-T12] Verify csharpier CLI resolves to 1.2.6 via `./.dotnet-sdk/dotnet.exe tool run csharpier --version`; record `evidence/baseline/csharpier-version.<ts>.md`.
- [ ] [P0-T13] Verify `dotnet-coverage` CLI is available via `dotnet-coverage --version`; record `evidence/baseline/dotnet-coverage-version.<ts>.md`.
- [ ] [P0-T14] Run baseline csharpier check (non-mutating): `dotnet tool run csharpier check .`; record `evidence/baseline/csharpier-baseline.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [ ] [P0-T15] Run baseline analyzer build: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; record `evidence/baseline/analyzer-build-baseline.<ts>.md`.
- [ ] [P0-T16] Run baseline nullable build: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`; record `evidence/baseline/nullable-build-baseline.<ts>.md`.
- [ ] [P0-T17] Run baseline MSTest with coverage across the full suite via `pwsh -NoProfile -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/evidence/baseline/coverage-baseline.cobertura.xml`; record `evidence/baseline/test-coverage-baseline.<ts>.md` with pass/fail counts, `EXIT_CODE:`, and coverage headline (line-rate, branch-rate).
- [ ] [P0-T18] From `coverage-baseline.cobertura.xml`, record baseline per-file line/branch coverage for `Controllers/BayesianPerformanceController.cs`, `Viewers/BayesianPerformanceViewer.cs`, and `Viewers/ToolStripMenuItemCb.cs`, and confirm `Viewers/QfcFormViewer.cs`/`Viewers/QfcItemViewerExpanded.cs` are absent (suppressed by attribute) at `evidence/baseline/per-file-coverage-baseline.<ts>.md`.
- [ ] [P0-T19] From the same Cobertura artifact, record baseline repository-wide `line-rate`, `branch-rate`, `lines-covered`/`lines-valid`, `branches-covered`/`branches-valid` at `evidence/baseline/repo-wide-coverage-baseline.<ts>.md`, for the later self-consistent before/after comparison ([P7-T8]).
- [ ] [P0-T20] Record the current `QuickFiler/Interfaces/IQfcFormViewer.cs` member list (name, signature, per member) at `evidence/baseline/iqfcformviewer-interface-snapshot.<ts>.md`, to support the "no member added/removed/renamed" acceptance criterion.
- [ ] [P0-T21] Record the current literal content of `BayesianPerformanceController.cs` line 58 (`var ppkg = (new ProgressPackage()).InitializeAsync(...)`) at `evidence/baseline/unawaited-task-baseline.<ts>.md`, to support the later byte-for-byte-unmodified check ([P7-T12]).
- [ ] [P0-T22] Confirm via `grep -n "ExcludeFromCodeCoverage" QuickFiler/Viewers/QfcFormViewer.cs QuickFiler/Viewers/QfcItemViewerExpanded.cs` that both attributes are present today (line 17 and line 18 respectively); record `evidence/baseline/attribute-baseline.<ts>.md`.

### Phase 1 — `Controllers/BayesianPerformanceController.cs`

- [ ] [P1-T1] In `QuickFiler/Controllers/BayesianPerformanceController.cs`, add the seam member `internal Action<BayesianPerformanceViewer> ViewerShowAction { get; set; } = viewer => viewer.Show();` immediately above the existing `_viewer`/`Viewer` property block, following the `EfcHomeController.cs:294` precedent verbatim in shape. Acceptance: file compiles; member is `internal` with the stated default.
- [ ] [P1-T2] In the same file, add `internal Func<BayesianPerformanceController, BayesianPerformanceViewer> ViewerFactory { get; set; } = c => new BayesianPerformanceViewer(c).Init();` adjacent to `ViewerShowAction`.
- [ ] [P1-T3] In `InvestigatePerformance()`, replace `Viewer = new BayesianPerformanceViewer(this).Init();` with `Viewer = ViewerFactory(this);`. Acceptance: no other line in the method changes.
- [ ] [P1-T4] In `InvestigatePerformance()`, replace `Viewer.Show();` with `ViewerShowAction(Viewer);`. Acceptance: no other line in the method changes; the discarded `ppkg` line at 58 is untouched.
- [ ] [P1-T5] In `QuickFiler.Test/Controllers/BayesianPerformanceControllerTests.cs`, add `[TestMethod] ClassSelectorSelectedIndexChanged_WithUnknownClass_LeavesActiveErrorAndFormValuesUnchanged` selecting a class string absent from `Errors[]` and asserting `controller.ActiveError` becomes `null` and no `AssignFormValues` side effect occurs (false branch of `if (ActiveError is not null)`).
- [ ] [P1-T6] Add `[TestMethod] OlvVerboseDetailsSelectionChanged_WithNoSelection_DoesNotUpdateActiveOutcome` setting `viewer.OlvVerboseDetails.SelectedObjects` to empty, calling `controller.OlvVerboseDetails_SelectionChanged()`, and asserting `controller.ActiveOutcome` is unchanged (false branch of the outer `(objects is not null) && (objects.Count != 0)` guard).
- [ ] [P1-T7] In `QuickFiler.Test/Controllers/BayesianPerformanceController.TestSupport.cs`, add `internal static Mock<IApplicationGlobals> BuildSafeApplicationGlobals()` returning a `Mock<IApplicationGlobals>` whose `AF` is stubbed with `CancelSource` returning a real `new CancellationTokenSource()`, `CancelToken` returning `CancellationToken.None`, and `ProgressTracker` returning `(ProgressTrackerPane)FormatterServices.GetUninitializedObject(typeof(ProgressTrackerPane))` (precedent: `UtilitiesCS.Test/Threading/ProgressTrackerPane_Tests.cs`'s `CreateHeadlessPane`), so `ProgressPackage.InitializeAsync(...)` never constructs a live `ProgressPane`/calls `.Show()`. Acceptance: new helper compiles; no production file is touched.
- [ ] [P1-T8] Add `[TestMethod] InvestigatePerformance_WithUnsetSerializationAndPresetErrors_ConstructsSerializationAndSkipsDeserialize` on `BayesianPerformanceControllerTests.cs`: set `controller.Globals = BuildSafeApplicationGlobals().Object`, pre-populate `controller.Errors` (non-null, non-empty), leave `Serialization` null, override `controller.ViewerFactory = c => viewer;` (the harness's own unshown viewer) and `controller.ViewerShowAction = v => { };`, call `await controller.InvestigatePerformance();`, assert it completes without exception and `controller.Serialization` is now non-null (covers `Serialization ??=` true branch, `Errors ??=` false branch — RHS never evaluated since `Errors` was already set).
- [ ] [P1-T9] Add `[TestMethod] InvestigatePerformance_WithPresetSerializationAndUnsetErrors_DeserializesViaMockedHelper`: set `controller.Globals = BuildSafeApplicationGlobals().Object`, set `controller.Serialization` to `new Mock<BayesianSerializationHelper>(controller.Globals).Object` with `.Setup(x => x.DeserializeAsync<ClassificationErrors[]>("ClassificationErrors[]", It.IsAny<string>())).ReturnsAsync(<preset array>)`, leave `controller.Errors` null, override `ViewerFactory`/`ViewerShowAction` as in `[P1-T8]`, call `await controller.InvestigatePerformance();`, assert `controller.Errors` equals the mocked array and no disk I/O occurred (covers `Serialization ??=` false branch, `Errors ??=` true branch).
- [ ] [P1-T10] Add `[TestMethod] InvestigatePerformance_Always_LeavesProgressPackageTaskUnobserved` asserting the current, unfixed behavior: reuse the `[P1-T8]` arrangement, call `await controller.InvestigatePerformance();`, and assert the method returns without throwing even though the discarded `ProgressPackage.InitializeAsync(...)` task is never awaited by production code. Add a code comment stating this test intentionally preserves, not fixes, the discarded-task defect (see `[P1-T13]`).
- [ ] [P1-T11] Add `[TestMethod] ReSortItem_WhenItemNotFound_DoesNotConstructEfcHomeController`: build `controller.Globals` with a mocked `IOlObjects.NamespaceMAPI` (`new Mock<Microsoft.Office.Interop.Outlook.NameSpace>(MockBehavior.Loose)`, precedent `QuickFiler.Test/Controllers/QfcInitEmailQueueZeroBatchTests.cs`) whose `GetItemFromID(It.IsAny<string>(), It.IsAny<string>())` returns `null`, set `controller.ActiveOutcome` to a built outcome, call `controller.ReSortItem()`, and assert no exception is thrown (false branch of `if (item is not null)`; `EfcHomeController` is never constructed).
- [ ] [P1-T12] Record the residual, documented gap for `ReSortItem()`'s `item is not null` true branch (constructing `EfcHomeController`, F8-owned, out of F15's file set) at `evidence/qa-gates/bayesianperformancecontroller-residual-gap.<ts>.md`, citing `spec.md`'s explicit allowance to document rather than edit an F8 file.
- [ ] [P1-T13] Promote the `BayesianPerformanceController.cs:58` unobserved/unawaited `ProgressPackage.InitializeAsync(...)` defect via the MCP promotion lifecycle (`mcp__drm-copilot__new_potential_bug_entry` then `mcp__drm-copilot__potential_to_issue`), recording the mirror at `evidence/issue-updates/issue-<new-issue-num>.<ts>.md` per `evidence-and-timestamp-conventions`.
- [ ] [P1-T14] Run `pwsh -NoProfile -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/evidence/qa-gates/coverage-p1.cobertura.xml`, compute `BayesianPerformanceController.cs`'s line/branch coverage from the output, and confirm both are `>= 80%`/`>= 75%`; record `evidence/qa-gates/bayesianperformancecontroller-coverage.<ts>.md`.

### Phase 2 — `Viewers/BayesianPerformanceViewer.cs`

- [ ] [P2-T1] In `QuickFiler.Test/Controllers/BayesianPerformanceController.TestSupport.cs`, add a second overload `internal static void RunWithViewer(Action<BayesianPerformanceViewer> action)` that constructs `new BayesianPerformanceViewer()` (parameterless; `Controller` stays `null`) on a dedicated STA thread, disposes in `finally`, and reuses the exact `SynchronizationContext` save/restore and `ExceptionDispatchInfo` marshalling shape of the existing overload verbatim. Acceptance: no `.Show()`/`.ShowDialog()` call; no message pump.
- [ ] [P2-T2] Create `QuickFiler.Test/Viewers/BayesianPerformanceViewerTests.cs` with a `[TestClass] public class BayesianPerformanceViewerTests` scaffold (`using` MSTest, FluentAssertions, `QuickFiler.Controllers.Tests` for the test-support helpers). Acceptance: file compiles with zero test methods, under 500 lines.
- [ ] [P2-T3] In `QuickFiler.Test/QuickFiler.Test.csproj`, add `<Compile Include="Viewers\BayesianPerformanceViewerTests.cs" />` immediately after the existing `<Compile Include="Viewers\BreadcrumbBridgeCoordinatorTests.cs" />` entry, preserving CRLF.
- [ ] [P2-T4] Add `[TestMethod] GroupKeyGetter_WithKeyValuePairArgument_ReturnsActualClass` calling `viewer.GroupKeyGetter(new KeyValuePair<VerboseTestOutcome, string>(outcome, "type"))` and asserting the result equals `outcome.Actual` (try-succeeds branch).
- [ ] [P2-T5] Add `[TestMethod] GroupKeyGetter_WithNonKeyValuePairArgument_ReturnsUnknown` calling `viewer.GroupKeyGetter(new object())` and asserting the result is `"unknown"` (catch branch).
- [ ] [P2-T6] Add `[TestMethod] OlvVerboseDetailsSelectionChanged_WithController_ForwardsToControllerHandler` using the existing `RunWithViewer(controller, viewer)` overload, invoking the private `OlvVerboseDetails_SelectionChanged(object, EventArgs)` handler via `typeof(BayesianPerformanceViewer).GetMethod(..., BindingFlags.NonPublic | BindingFlags.Instance).Invoke(viewer, new object[] { viewer, EventArgs.Empty })`, and asserting `controller.ActiveOutcome` updates (Controller != null side).
- [ ] [P2-T7] Add `[TestMethod] OlvVerboseDetailsSelectionChanged_WithoutController_DoesNotThrow` using the new parameterless `RunWithViewer(Action<BayesianPerformanceViewer>)` overload, invoking the same private handler via reflection, and asserting no exception is thrown (Controller == null side).
- [ ] [P2-T8] Add `[TestMethod] OlvDriversSelectionChanged_WithController_ForwardsToControllerHandler` (same pattern as `[P2-T6]` for `OlvDrivers_SelectionChanged`).
- [ ] [P2-T9] Add `[TestMethod] OlvDriversSelectionChanged_WithoutController_DoesNotThrow` (same pattern as `[P2-T7]` for `OlvDrivers_SelectionChanged`).
- [ ] [P2-T10] Add `[TestMethod] ClassSelectorSelectedIndexChanged_WithController_ForwardsToControllerHandler` (same pattern as `[P2-T6]` for `ClassSelector_SelectedIndexChanged`).
- [ ] [P2-T11] Add `[TestMethod] ClassSelectorSelectedIndexChanged_WithoutController_DoesNotThrow` (same pattern as `[P2-T7]` for `ClassSelector_SelectedIndexChanged`).
- [ ] [P2-T12] Add `[TestMethod] Button1Click_WithController_ForwardsToReSortItem` invoking the private `button1_Click(object, EventArgs)` handler via reflection on a viewer built with `RunWithViewer(controller, viewer)`, with `controller.Globals` set via `BuildSafeApplicationGlobals()` (from `[P1-T7]`) plus a mocked `NameSpace.GetItemFromID` returning `null` and `controller.ActiveOutcome` preset, asserting no exception is thrown (Controller != null side, `item is null` path).
- [ ] [P2-T13] Add `[TestMethod] Button1Click_WithoutController_DoesNotThrow` using the parameterless overload, invoking `button1_Click` via reflection, asserting no exception (Controller == null side).
- [ ] [P2-T14] Run coverage via `Invoke-MSTestWithCoverage.ps1` (output `evidence/qa-gates/coverage-p2.cobertura.xml`), compute `BayesianPerformanceViewer.cs`'s line/branch coverage, and confirm both are `>= 80%`/`>= 75%`; record `evidence/qa-gates/bayesianperformanceviewer-coverage.<ts>.md`.

### Phase 3 — `Viewers/ToolStripMenuItemCb.cs`

- [ ] [P3-T1] Create `QuickFiler.Test/Viewers/ToolStripMenuItemCbTests.cs` with a `[TestClass] public class ToolStripMenuItemCbTests` scaffold. Acceptance: file compiles with zero test methods, under 500 lines; no STA harness (not `Form`-derived; per research, construction needs the default apartment only).
- [ ] [P3-T2] In `QuickFiler.Test/QuickFiler.Test.csproj`, add `<Compile Include="Viewers\ToolStripMenuItemCbTests.cs" />` adjacent to the other `Viewers\` entries, preserving CRLF.
- [ ] [P3-T3] Add `[TestMethod] Constructor_DefaultChecked_DoesNotSetCheckedImage` asserting `new ToolStripMenuItemCb().Checked` is `false` and `((ToolStripMenuItem)cb).Image` is `null` (false branch of the constructor's `if (Checked)`; comment explains `_checked` defaults `false` so this is the only reachable side — see `[P3-T4]`).
- [ ] [P3-T4] Add a code comment (not a test) in `ToolStripMenuItemCbTests.cs` documenting that the constructor's `Checked == true` branch is unreachable via any public construction path (`_checked` is a private field defaulting `false`, read before any setter can run), and record this as a residual, irreducible branch gap at `evidence/qa-gates/toolstripmenuitemcb-residual-gap.<ts>.md`.
- [ ] [P3-T5] Add `[TestMethod] Checked_SetTrue_SetsBaseImageAndRaisesCheckedChanged` setting `cb.Checked = true` with a `CheckedChanged` subscriber attached, asserting `((ToolStripMenuItem)cb).Image` equals `Properties.Resources.CheckBoxChecked` and the subscriber was invoked exactly once (setter true branch, subscribed side of `CheckedChanged?.Invoke`).
- [ ] [P3-T6] Add `[TestMethod] Checked_SetFalse_ClearsBaseImage` setting `cb.Checked = true` then `cb.Checked = false`, asserting `((ToolStripMenuItem)cb).Image` is `null` (setter false branch).
- [ ] [P3-T7] Add `[TestMethod] Checked_SetWithoutSubscriber_DoesNotThrow` setting `cb.Checked = true` with no `CheckedChanged` subscriber attached, asserting no exception (unsubscribed side of `CheckedChanged?.Invoke`).
- [ ] [P3-T8] Add `[TestMethod] PerformClick_TogglesCheckedViaConstructorWiredHandler` constructing a fresh `ToolStripMenuItemCb`, calling `cb.PerformClick()`, and asserting `cb.Checked` becomes `true` (exercises `ToolStripMenuItemCb_Click` via the constructor's unconditional `this.Click += ...ToolStripMenuItemCb_Click` subscription from `InitializeComponent()`). Comment explaining this fires independent of `CheckOnClick`'s default value.
- [ ] [P3-T9] Add `[TestMethod] CheckOnClick_SetFalseThenTrue_TransitionsClickWiring`: set `cb.CheckOnClick = false`, call `cb.PerformClick()`, assert `cb.Checked` unchanged (unsubscribe branch); then set `cb.CheckOnClick = true`, call `cb.PerformClick()` again, assert `cb.Checked` toggles (resubscribe branch). Covers both `CheckOnClick` setter branches and the subscribe/unsubscribe/resubscribe state transition.
- [ ] [P3-T10] Run coverage via `Invoke-MSTestWithCoverage.ps1` (output `evidence/qa-gates/coverage-p3.cobertura.xml`), compute `ToolStripMenuItemCb.cs`'s line/branch coverage accounting for the one documented-unreachable branch side from `[P3-T4]`, and confirm line `>= 80%` and branch `>= 75%`; record `evidence/qa-gates/toolstripmenuitemcb-coverage.<ts>.md`.

### Phase 4 — `Viewers/QfcFormViewer.cs`

- [ ] [P4-T1] In `QuickFiler/Viewers/QfcFormViewer.cs`, remove the `[ExcludeFromCodeCoverage]` attribute at line 17 and remove the `using System.Diagnostics.CodeAnalysis;` directive if no longer referenced anywhere else in the file. Acceptance: file compiles; no other line changes.
- [ ] [P4-T2] Create `QuickFiler.Test/Viewers/QfcFormViewer.StaTests.cs` with: (a) a private static `RunOnSta(Action<QfcFormViewer> action)` helper reusing the `RunWithViewer` harness shape verbatim (STA thread, `finally` dispose, `SynchronizationContext` save/restore, `ExceptionDispatchInfo` marshalling) from `BayesianPerformanceController.TestSupport.cs`, constructing a `TestableQfcFormViewer`; and (b) a `private sealed class TestableQfcFormViewer : QfcFormViewer { public bool CallProcessCmdKey(ref Message m, Keys k) => ProcessCmdKey(ref m, k); }` test-only subclass. Acceptance: no `.Show()`/`.ShowDialog()` call anywhere in the file; under 500 lines.
- [ ] [P4-T3] In `QuickFiler.Test/QuickFiler.Test.csproj`, add `<Compile Include="Viewers\QfcFormViewer.StaTests.cs" />` adjacent to the other `Viewers\` entries, preserving CRLF.
- [ ] [P4-T4] Add `[TestMethod] Construct_UnshownQfcFormViewer_DoesNotThrowAndDisposesCleanly` constructing and disposing a `TestableQfcFormViewer` via `RunOnSta`, asserting no exception (drives `InitializeComponent()`, covering the paired Designer file as a byproduct).
- [ ] [P4-T5] Add `[TestMethod] ProcessCmdKey_WithAltCommandAndKeyboardHandler_ReturnsTrueAndInvokesHandler`: call `SetKeyboardHandler(mockHandler.Object)` where `mockHandler.Object.ToggleKeyboardDialogAsync()` is stubbed, call `CallProcessCmdKey` with a `Keys` value for which `Controllers.QfcFormKeyHandler.IsAltKeyCommand(keyData)` is `true`, assert the return is `true` and the mock's `ToggleKeyboardDialogAsync()` was invoked once (true branch of the compound `&&` guard).
- [ ] [P4-T6] Add `[TestMethod] ProcessCmdKey_WithoutKeyboardHandler_DelegatesToBase`: do not call `SetKeyboardHandler`, call `CallProcessCmdKey` with any `Keys` value, assert the result equals `base.ProcessCmdKey`'s expected return for a non-command key (false side of `_keyboardHandler is not null`).
- [ ] [P4-T7] Add `[TestMethod] ProcessCmdKey_WithHandlerButNonAltCommand_DelegatesToBase`: call `SetKeyboardHandler(mockHandler.Object)`, call `CallProcessCmdKey` with a `Keys` value for which `IsAltKeyCommand` is `false`, assert the handler's `ToggleKeyboardDialogAsync()` is never invoked (false side of the second `&&` operand, independently of the first).
- [ ] [P4-T8] Add `[TestMethod] ItemViewerTemplateMargin_WhenTemplateInitialized_ReturnsTemplateMargin` reading `viewer.ItemViewerTemplateMargin` immediately after construction and asserting it equals `_QfcItemViewerTemplate.Margin` (read via reflection for the assertion only) — false side of the `??` (template not null).
- [ ] [P4-T9] Add `[TestMethod] ItemViewerTemplateMargin_WhenTemplateNull_ReturnsDefaultPadding` using a reflection-based field setter (reuse `BayesianPerformanceControllerTestSupport.SetField` shape) to null out `_QfcItemViewerTemplate`, then asserting `viewer.ItemViewerTemplateMargin` equals `default(Padding)` (true side of the `??`).
- [ ] [P4-T10] Add `[TestMethod] CaptureTlpCellStates_WhenBothTemplatesInitialized_ReturnsPopulatedTlpCellStates` calling `viewer.CaptureTlpCellStates()` immediately after construction and asserting the result is non-null with both "Expanded" and "Compressed" keys present (false side of the `||` guard).
- [ ] [P4-T11] Add `[TestMethod] CaptureTlpCellStates_WhenTemplateNull_ReturnsNull` nulling `_QfcItemViewerTemplate` via reflection, calling `viewer.CaptureTlpCellStates()`, and asserting the result is `null` (true side of the `||` guard).
- [ ] [P4-T12] Add `[TestMethod] Panels_CalledTwice_ReturnsSameMemoizedInstance` asserting `viewer.Panels` returns the same list reference on two successive calls.
- [ ] [P4-T13] Add `[TestMethod] Buttons_CalledTwice_ReturnsSameMemoizedInstance` asserting `viewer.Buttons` returns the same list reference on two successive calls.
- [ ] [P4-T14] Add `[TestMethod] IQfcFormViewerMembers_PlainForwards_ExposeUnderlyingDesignerControls` exercising, on one constructed viewer: `Worker`, `L1v0L2L3v_TableLayout`, `L1v_TableLayout`, `L1v0L2_PanelMain` getters; `SkipButtonText`/`SkipButtonEnabled`/`ItemsPerLoadValue`/`ItemsPerLoadEnabled` get+set; subscribe-then-unsubscribe on `OkClicked`/`CancelClicked`/`UndoClicked`/`SkipClicked`/`ItemsPerLoadValueChanged`; `GetKeyEventExclusionControls()`; and `SwapItemTableLayout(new TableLayoutPanel())`; asserting each reflects the expected forwarded value or completes without exception.
- [ ] [P4-T15] Verify `QuickFiler/Interfaces/IQfcFormViewer.cs`'s member list is byte-identical to the `[P0-T20]` snapshot; record `evidence/qa-gates/iqfcformviewer-frozen-check.<ts>.md`.
- [ ] [P4-T16] Run coverage via `Invoke-MSTestWithCoverage.ps1` (output `evidence/qa-gates/coverage-p4.cobertura.xml`), compute `QfcFormViewer.cs`'s line/branch coverage, and confirm both are `>= 80%`/`>= 75%`; record `evidence/qa-gates/qfcformviewer-coverage.<ts>.md`.

### Phase 5 — `Viewers/QfcItemViewerExpanded.cs`

- [ ] [P5-T1] In `QuickFiler/Viewers/QfcItemViewerExpanded.cs`, remove the `[ExcludeFromCodeCoverage]` attribute at line 18 and remove the `using System.Diagnostics.CodeAnalysis;` directive if no longer referenced elsewhere in the file. Acceptance: file compiles; no other line changes.
- [ ] [P5-T2] Create `QuickFiler.Test/Viewers/QfcItemViewerExpanded.StaTests.cs` with a private static `RunOnSta(Action<QfcItemViewerExpanded> action)` helper reusing the `RunWithViewer` harness shape verbatim (STA thread, `finally` dispose, `SynchronizationContext` save/restore, `ExceptionDispatchInfo` marshalling). Acceptance: no `.Show()`/attach-to-a-handle-bearing-parent anywhere in the file; under 500 lines.
- [ ] [P5-T3] In `QuickFiler.Test/QuickFiler.Test.csproj`, add `<Compile Include="Viewers\QfcItemViewerExpanded.StaTests.cs" />` adjacent to the other `Viewers\` entries, preserving CRLF.
- [ ] [P5-T4] Add `[TestMethod] Construct_UnshownQfcItemViewerExpanded_PopulatesTipsLabelsWithNineEntries` constructing via `RunOnSta`, asserting `control.TipsLabels.Count == 9` and that it contains, in order, `LblAcOpen`, `LblAcPopOut`, `LblAcTask`, `LblAcDelete`, `LblAcAttachments`, `LblAcConversation`, `LblAcEmail`, `LblAcFolder`, `LblAcSearch`.
- [ ] [P5-T5] Add `[TestMethod] Controller_GetSet_RoundTrips` setting `control.Controller` to a `new Mock<IQfcItemController>().Object` and asserting the getter returns the same instance.
- [ ] [P5-T6] Add `[TestMethod] UiSyncContext_AfterConstruction_ReflectsCapturedContext` asserting `control.UiSyncContext` is non-null and equals the `SynchronizationContext` installed on the STA thread before construction.
- [ ] [P5-T7] Record that `QfcItemViewerExpanded.cs` has zero branch points; branch coverage reports `N/A`, never `0%`, per the epic's binding zero-branch rule; record `evidence/qa-gates/qfcitemviewerexpanded-branch-na.<ts>.md`.
- [ ] [P5-T8] Promote the dead-code finding (`QfcItemViewerExpanded` has zero production consumers repo-wide) via the MCP promotion lifecycle (`mcp__drm-copilot__new_potential_bug_entry` then `mcp__drm-copilot__potential_to_issue`), recommending a future, separately-scoped removal; do not delete the type in this feature; record the mirror at `evidence/issue-updates/issue-<new-issue-num>.<ts>.md`.
- [ ] [P5-T9] Run coverage via `Invoke-MSTestWithCoverage.ps1` (output `evidence/qa-gates/coverage-p5.cobertura.xml`), confirm `QfcItemViewerExpanded.cs` line coverage `>= 80%` (branch `N/A`); record `evidence/qa-gates/qfcitemviewerexpanded-coverage.<ts>.md`.

### Phase 6 — Seven generated / `Properties/` files: DEC-5 classification and verification

- [ ] [P6-T1] Verify via `grep -n "ExcludeFromCodeCoverage" QuickFiler/Viewers/QfcFormViewer.Designer.cs QuickFiler/Viewers/QfcItemViewerExpanded.Designer.cs QuickFiler/Viewers/BayesianPerformanceViewer.Designer.cs QuickFiler/Viewers/ToolStripMenuItemCb.Designer.cs` that none of the four Designer files carries the attribute (zero matches); record `evidence/qa-gates/generated-designer-attribute-check.<ts>.md`.
- [ ] [P6-T2] Verify `QuickFiler/Properties/AssemblyInfo.cs` declares zero classes and zero methods (assembly-level attributes only), confirming `interface-only / not-measured` classification (not `measured-not-gated`); record in `evidence/qa-gates/generated-files-classification.<ts>.md`.
- [ ] [P6-T3] Verify `QuickFiler/Properties/Resources.Designer.cs` and `QuickFiler/Properties/Settings.Designer.cs` carry no `[ExcludeFromCodeCoverage]` attribute and confirm their `<auto-generated>`/`[GeneratedCodeAttribute]` banners, confirming `measured-not-gated` classification; append to `evidence/qa-gates/generated-files-classification.<ts>.md`.
- [ ] [P6-T4] Self-audit Phases 1-5's test list and confirm no task authored a shape-assertion test purely to manufacture coverage for any of the seven generated/`Properties/` files; record the confirmation in `evidence/qa-gates/generated-files-classification.<ts>.md`.
- [ ] [P6-T5] Record the complete seven-file classification table (file, disposition, branch surface, zero-branch flag per file) at `evidence/qa-gates/generated-files-classification.<ts>.md`, marking `Settings.Designer.cs` and `AssemblyInfo.cs` branch coverage `N/A` and the four Designer Viewer files' single `Dispose(bool)` branch as `measured-not-gated` (not individually gated).
- [ ] [P6-T6] After Phase 4/5's attribute removals, verify from `coverage-p5.cobertura.xml` (or a fresh scoped run) that `QfcFormViewer.Designer.cs` and `QfcItemViewerExpanded.Designer.cs` now appear as instrumented Cobertura `<class>` elements (previously fully suppressed); append the confirmation to `evidence/qa-gates/generated-files-classification.<ts>.md`.

### Phase 7 — Final QC Loop (full C# toolchain, coverage reconciliation, AC check-off)

- [ ] [P7-T1] Verify no new or modified production or test file introduced by this feature exceeds 500 lines (the two `.Designer.cs` files are exempt as generated code); record `evidence/qa-gates/file-line-counts.<ts>.md`.
- [ ] [P7-T2] Run `dotnet tool run csharpier format .` then `dotnet tool run csharpier check .`; record `evidence/qa-gates/final-qc-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If `format` changed any file, re-run `check` until it exits 0 before proceeding.
- [ ] [P7-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; record `evidence/qa-gates/final-qc-analyzers.<ts>.md`. On failure, fix and restart the loop from `[P7-T2]`.
- [ ] [P7-T4] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`; record `evidence/qa-gates/final-qc-nullable.<ts>.md`. On failure, fix and restart the loop from `[P7-T2]`.
- [ ] [P7-T5] Run `pwsh -NoProfile -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-08-08-quickfiler-form-viewers-bayesian-coverage-496/evidence/qa-gates/coverage-final.cobertura.xml`; record `evidence/qa-gates/final-qc-tests.<ts>.md` with pass/fail counts, `EXIT_CODE:`, and coverage headline. On any failure or file change from `[P7-T2]`-`[P7-T4]`, restart the loop from `[P7-T2]`.
- [ ] [P7-T6] From `coverage-final.cobertura.xml`, compute and record final per-file coverage for `BayesianPerformanceController.cs`, `BayesianPerformanceViewer.cs`, `ToolStripMenuItemCb.cs`, `QfcFormViewer.cs` (line + branch, all `>= 80%`/`>= 75%`), and `QfcItemViewerExpanded.cs` (line `>= 80%`, branch `N/A`); record `evidence/qa-gates/per-file-coverage-final.<ts>.md`.
- [ ] [P7-T7] From the same artifact, compute and record the seven generated/`Properties/` files' final coverage figures (measured-not-gated, not compared against any floor) and confirm `AssemblyInfo.cs` emits no Cobertura `<class>` element; record `evidence/qa-gates/generated-files-coverage-final.<ts>.md`.
- [ ] [P7-T8] Compute the repository-wide before/after coverage comparison using `evidence/baseline/coverage-baseline.cobertura.xml` ([P0-T17]/[P0-T19]) and `evidence/qa-gates/coverage-final.cobertura.xml` ([P7-T5]), same command and post-processing; record `evidence/qa-gates/repo-wide-coverage-delta.<ts>.md`, confirming `line-rate` and `branch-rate` are retained or improved, citing both figures and the command.
- [ ] [P7-T9] Verify zero coverage regression on any pre-existing (not newly added) executable line in the five hand-written production files by diffing covered-line sets between the baseline and final Cobertura artifacts; record `evidence/qa-gates/changed-line-regression-check.<ts>.md`.
- [ ] [P7-T10] Verify via `grep -n "ExcludeFromCodeCoverage" QuickFiler/Viewers/QfcFormViewer.cs QuickFiler/Viewers/QfcItemViewerExpanded.cs` that both attributes are absent (zero matches); record `evidence/qa-gates/attribute-removal-check.<ts>.md`.
- [ ] [P7-T11] Verify `QuickFiler/Interfaces/IQfcFormViewer.cs`'s member list is byte-identical to the `[P0-T20]` snapshot; record `evidence/qa-gates/iqfcformviewer-frozen-final.<ts>.md`.
- [ ] [P7-T12] Verify `BayesianPerformanceController.cs` line 58 (the unawaited `ProgressPackage.InitializeAsync(...)` call) is byte-for-byte unmodified versus the `[P0-T21]` snapshot; record `evidence/qa-gates/unawaited-task-unmodified.<ts>.md`.
- [ ] [P7-T13] Confirm both latent-defect GitHub issues (`[P1-T13]` unawaited task; `[P5-T8]` dead `QfcItemViewerExpanded`) exist as tracked GitHub issues (not prose in this feature folder); record `evidence/issue-updates/promotion-summary.<ts>.md` with both issue URLs.
- [ ] [P7-T14] Check off satisfied acceptance-criteria checkboxes in `spec.md`'s `## Acceptance Criteria` and `## Definition of Done` sections per the `acceptance-criteria-tracking` skill, changing only `- [ ]` to `- [x]` for criteria verified by the evidence produced in `[P0]`-`[P7]`.
- [ ] [P7-T15] Check off satisfied acceptance-criteria checkboxes in `user-story.md`'s `## Acceptance Criteria` section per the same protocol.
- [ ] [P7-T16] Report the AC Status Summary (source files, total/checked/remaining counts, remaining criterion texts if any) per `acceptance-criteria-tracking`.
- [ ] [P7-T17] Confirm `git status` is clean and all evidence artifacts from `[P0]`-`[P7]` are committed; record `evidence/qa-gates/final-commit.<ts>.md`.

## Test Plan

- **Unit:** MSTest + Moq + FluentAssertions, mirrored under `QuickFiler.Test/`. Coverage-bearing test cases are itemized as individual tasks in Phases 1-5 above; each targets one branch side or one line-coverage gap.
- **Integration:** none — internal testability change only, per `spec.md`.
- **CLI/API:** none.
- **Coverage evidence:**
  - Baseline: `evidence/baseline/coverage-baseline.cobertura.xml` ([P0-T17]), per-file baseline ([P0-T18]), repo-wide baseline ([P0-T19]).
  - Post-change: `evidence/qa-gates/coverage-final.cobertura.xml` ([P7-T5]), per-file final ([P7-T6]), generated-files final ([P7-T7]).
  - Comparison: `evidence/qa-gates/repo-wide-coverage-delta.<ts>.md` ([P7-T8]), `evidence/qa-gates/changed-line-regression-check.<ts>.md` ([P7-T9]).

## Open Questions / Notes

- `[P1-T9]`'s `Mock<BayesianSerializationHelper>` requires the class's `DeserializeAsync<T>` overloads to remain `public virtual` (confirmed true as of this branch); if a future change seals the class or removes virtuality, the mock must be replaced with a lighter stub subclass.
- `[P3-T4]`/`[P3-T9]` depend on `ToolStripMenuItemCb.Designer.cs`'s `InitializeComponent()` unconditionally wiring `this.Click += ToolStripMenuItemCb_Click` — verified directly in this checkout; if a future Designer regeneration changes this wiring, `[P3-T8]`/`[P3-T9]`'s assertions must be re-verified against the new generated code.
