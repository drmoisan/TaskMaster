Timestamp: 2026-08-22T13-13

Exact text posted:

> Item 2 of the potential document — the three `internal` members `AttachBreadcrumbMessengerWhenReadyAsync`, `AttachBreadcrumbMessenger`, and `BreadcrumbOpenTask` in `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs` — is deferred to the later ItemViewer-owning epic child and is not dropped from tracking. This issue (#491) addresses only Item 1 (the dead `QuickFiler.Test.Form1` live-form type).

PostedAs: comment
Comment URL: https://github.com/drmoisan/TaskMaster/issues/491#issuecomment-5380720016

`gh` was available and authenticated (`gh auth status` confirmed logged in to github.com as `drmoisan`), so the text was posted as a comment via `gh issue comment 491 --body-file ...`. This artifact is the required issue-update mirror; no content was written under `docs/features/potential/`.
