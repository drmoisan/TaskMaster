Timestamp: 2026-07-02T15:12
Command: `MSYS_NO_PATHCONV=1 vstest.console.exe QuickFiler.Test\bin\Debug\QuickFiler.Test.dll UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /EnableCodeCoverage /InIsolation`
(coverage converted for numeric readout with: `Microsoft.CodeCoverage.Console.exe merge <file>.coverage -f xml -o TestResults/final-gate.coverage.xml`)
EXIT_CODE: 0
Output Summary:
- QuickFiler.Test.dll (net481): Passed 347, Failed 0, Skipped 0, Total 347.
- UtilitiesCS.Test.dll (net481): Passed 4093, Failed 0, Skipped 0, Total 4093. (No occurrence of the known pre-existing flaky dispatcher timing test.)
- QfcItemController affected-denominator coverage (sum of instrumented `type_name` containing "QfcItemController"; `[ExcludeFromCodeCoverage]` members are not instrumented and are therefore excluded from this denominator automatically): lines_covered=1243, lines_partially_covered=33, lines_not_covered=330, total=1606, coverage=77.40%.
- Repo-wide module line_coverage: QuickFiler.dll = 47.69%, UtilitiesCS.dll = 85.86%.
- `Theme`/`FolderPredictor`-area coverage for the changed lines (per-function breakdown from the merged coverage XML):
  - `Theme.SetQfcThemeAsync()` (compiler-generated `MoveNext`, `Theme.<SetQfcThemeAsync>d__133`): 100.00% line coverage (3/3 lines) — the sole changed line (`await _uiDispatcher.InvokeAsync(...)`) is covered by `SetQfcThemeAsync_RoutesThroughInjectedDispatcher`.
  - `Theme.SetQfcTheme(bool)`: 46.15% line coverage (6/13) — the changed `async:true` branch line (`_uiDispatcher.InvokeAsync(...)`) is covered by `SetQfcTheme_Async_RoutesThroughInjectedDispatcher` and (pre-existing, unchanged this cycle) `SetThemeDark_FromNormal_SelectsDarkNormalTheme`/`SetThemeLight_FromNormal_SelectsLightNormalTheme`; the uncovered lines are the pre-existing, unchanged `InvokeRequired`/else branches and guard clause, not part of this cycle's diff.
  - `Theme.SetMailRead(bool)`: 50.00% line coverage (7/14) — the changed `async:true` branch line (`_uiDispatcher.BeginInvoke(...)`) is covered by `SetMailRead_Async_RoutesThroughInjectedDispatcherBeginInvoke`; the uncovered lines are the pre-existing, unchanged guard clause and `else` (`_lblSender.Invoke`) branch.
  - `Theme(...)` big-overload constructor: 100.00% line coverage (90/90) — the new `_uiDispatcher = uiDispatcher ?? new WpfUiDispatcher();` line is exercised (both the injected-non-null path via every `SetQfcTheme`/`SetMailRead`/`SetQfcThemeAsync` dispatcher test, and the null-defaults-to-`WpfUiDispatcher` path via `Constructor_BigOverload_WithNullUiDispatcher_DefaultsToWpfUiDispatcher`).
  - `FolderPredictor.cs` itself: zero new executable lines this cycle (the file's only change is the `partial` keyword); the `IFolderSearchHandler` interface (declarations only, no executable lines) and the `FolderPredictor.IFolderSearchHandler.cs` partial-declaration file (a single empty type-declaration line, no executable body) contribute no measurable line-coverage denominator of their own. All new executable lines for this seam (the two factory-delegate fields/constructor-parameters/production-default assignments and every `_folderPredictorFactory`/`_folderPredictorEmptyFactory` call site) live in `QfcItemController*.cs` and are included in the QfcItemController affected-denominator figure above; every one of the five de-exempted `FolderPredictor`-cluster members (`LoadFolderHandler`, `LoadFolderHandlerAsync`, `PopulateFolderComboBox`, `PopulateFolderComboBoxAsync`, `TextBoxSearch_TextChanged`) has a directly passing dedicated test (P10-T11/T13/T14/T15/T16).
