# 2026-08-07-quickfiler-coverage-ledger-432 (Plan)

- **Issue:** #432
- **Issue URL:** https://github.com/drmoisan/TaskMaster/issues/432
- **Parent:** epic #136 `quickfiler-per-file-coverage`, child F1, wave 0
- **Integration branch:** `epic/quickfiler-per-file-coverage-integration`
- **Owner:** drmoisan
- **Last Updated:** 2026-08-07T20-41
- **Status:** Draft — awaiting executor preflight
- **Version:** 2.0 (revised to absorb epic manifest commit `c8d9e4c8` and the amended `spec.md` v0.3)
- **Work Mode:** full-feature (marker persisted in `issue.md`; AC source: `spec.md` `## Acceptance Criteria`, six items, mirrored identically in `user-story.md`)
- **Language in scope:** PowerShell + Markdown/JSON. **No C# is written, built, or run by this plan.**
- **Plan path (continuity contract):** `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/plan.2026-08-07T20-41.md` — update in place for all revision iterations; no sibling plan files.

## Execution Context

This plan is executed by `epic-orchestrator` in a worktree that is **not** the worktree in which it was authored. Every repository path below is repository-relative. Wherever a task requires an absolute workspace root (the PoshQC MCP calls), the executor supplies the absolute path of **its own current worktree root**. No absolute path is embedded in this plan.

## Requirements Sources (read in full before execution)

- `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/spec.md` — authoritative AC source (six unchecked items) and authoritative design contract, version 0.3 as amended for epic manifest commit `c8d9e4c8`.
- `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/user-story.md` — same six criteria, no additional criteria.
- `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/issue.md`
- `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/research/2026-08-07T22-15-quickfiler-coverage-ledger-research.md` — ground truth; sections 2, 3, 4 (including 4.3 on the `branch` and `condition-coverage` attributes and 4.7 on the absent-from-report set), 5, 6 and decisions D1-D17.
- `docs/features/epics/quickfiler-per-file-coverage/epic.md` — "Shared Design", "Feature File Assignments", `## Coverage-Target Reconciliation`, `## Directives for F1's Ledger and Harness`, `## Latent Defect Promotion`, `## Mid-Wave File Creation and the Ledger Denominator`, and `## Cross-Child Constraints Discovered During Preparation`.

## Toolchain Contract (PowerShell)

Run in this exact order; restart from stage 1 whenever any stage fails or modifies a file:

1. **Format** — MCP tool `mcp__drm-copilot__run_poshqc_format`, `workspace_root` = the executor's own current worktree root.
2. **Analyze** — MCP tool `mcp__drm-copilot__run_poshqc_analyze`, same `workspace_root`.
3. **Type check** — not applicable to PowerShell; skipped by policy (`.claude/rules/powershell.md` line 17).
4. **Test** — MCP tool `mcp__drm-copilot__run_poshqc_test`, same `workspace_root`. The tool's parameter schema is `workspace_root` plus `scan_folders` only; it exposes no coverage switch and emits coverage unconditionally, so no "coverage enabled" argument is supplied.

VS Code task wrappers are not an approved substitute. No C# toolchain stage runs in this plan.

The analyze stage carries a pre-existing repository baseline recorded in `[P0-T5]`. "Stage fails" means the stage's diagnostic count exceeds that baseline or the stage modified a file — not merely a non-zero exit code. `[P11-T4]` applies the same definition.

The test stage carries the same baseline-delta treatment, for the same reason. A recorded prior observation in this repository is that `mcp__drm-copilot__run_poshqc_test` can exit `-1` (`4294967295`) with no accompanying detail in TaskMaster worktrees, at baseline as well as post-change. **A failing test stage therefore means a non-zero `failures` total or a non-zero `skipped` total across the `<testsuite>` entries in `artifacts/pester/pester-junit.xml`, or an exit code that is worse than the `[P0-T6]` baseline exit code — not merely a non-zero exit code.** The tool's exit code is recorded verbatim as an observation on every test-stage artifact and is compared against the `[P0-T6]` baseline value. `[P9-T53]`, `[P10-T37]`, `[P11-T3]`, and `[P11-T4]` apply this definition.

## Evidence Rules (non-overridable)

- Every evidence artifact resolves under `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/<kind>/`, where `<kind>` is one of `baseline/`, `regression-testing/`, `qa-gates/`, `issue-updates/`, `other/`. Any `artifacts/`-rooted evidence path is invalid and must be rejected.
- Every command-bearing task's artifact records `Timestamp:` (ISO-8601 `yyyy-MM-ddTHH-mm`), `Command:`, `EXIT_CODE:`, and `Output Summary:`. For an MCP tool invocation, `Command:` records the tool name and the parameter values supplied.
- The policy-read artifact `phase0-instructions-read.<ts>.md` is not a command-step artifact; its required shape is `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- `<ts>` in any artifact filename is replaced with the actual execution timestamp.
- Coverage-bearing artifacts (Phase 0 baseline test run, Phase 11 final test run, Phase 11 coverage delta) record the numeric **line**-coverage percentage, computed from the report-level `<counter type="LINE" missed="" covered="">` element of `artifacts/pester/powershell-coverage.xml` — the JaCoCo report written by `mcp__drm-copilot__run_poshqc_test` — as `covered / (covered + missed)`. Placeholders such as `UNVERIFIED` or `N/A` are invalid where a number is required. **Branch coverage is not emitted by the PowerShell coverage toolchain:** the JaCoCo report contains only `INSTRUCTION`, `LINE`, `METHOD`, and `CLASS` counters and no `BRANCH` counter. Each coverage artifact records the literal line `Branch coverage: not emitted by Pester JaCoCo output (no BRANCH counter present)`. That statement is a recorded structural limitation of the PowerShell toolchain, not a placeholder, and does not invalidate the artifact. It is unrelated to the **Cobertura branch data for QuickFiler C# files** that this feature's harness computes: that data comes from a committed C# Cobertura report and is never sourced from Pester's JaCoCo output. **`artifacts/pester/powershell-coverage.xml` carries no `scripts/vscode/` sourcefile.** Verified: `mcp__drm-copilot__run_poshqc_test` loads the bundled `PoshQC/settings/pester.runsettings.psd1`, whose `CodeCoverage.Path` is a fixed allow-list of drm-copilot paths; `Invoke-PoshQCTest` prunes the entries that do not exist in this repository and keeps the survivors, which are `.claude/hooks/`, `.claude/lib/`, and `.codex/hooks/` only. No repo-local override exists (`scripts/powershell/PoshQC/settings/pester.runsettings.psd1` and `config/poshqc-scan.json` are both absent). The report-level LINE counter of that file is therefore a **hooks-only** figure and is used in this plan solely for the no-regression comparison. Per-file line coverage for the two new `scripts/vscode/` production scripts comes from the supplementary direct-`Invoke-Pester` JaCoCo artifact produced by `[P11-T3]`, never from `artifacts/pester/powershell-coverage.xml`.
- The PoshQC MCP tools return a summary string only; they emit no scanned-file list, no per-file diagnostics, and no count of files scanned. The format tool's per-file `Formatted:` / `Already formatted:` lines go to the information stream and are never surfaced to the caller, so no "number of files scanned" figure is obtainable from any PoshQC MCP tool and none is recorded anywhere in this plan; whether a run modified a file is determined from `git status --porcelain` instead. `mcp__drm-copilot__run_poshqc_test` additionally writes two tool artifacts inside the executor's worktree: `artifacts/pester/pester-junit.xml` (per-suite full path, `tests`, `failures`, `skipped`) and `artifacts/pester/powershell-coverage.xml` (JaCoCo; report-level and per-`<class>` `sourcefilename` LINE counters, scoped to the hooks-only allow-list described in the previous bullet — its `<package name=...>` values are only `.claude/hooks`, `.claude/lib/model-routing`, `.claude/lib/orchestrator-state`, and `.codex/hooks`, and none of its `sourcefilename` values lies under `scripts/vscode/`). Those two files are the authoritative numeric source for every test-count and suite-discovery figure in this plan, and for the repository-wide coverage figure within that recorded scope limitation. They are read-only tool outputs; no evidence artifact is written to `artifacts/`. All recorded numbers are copied into the feature's own `evidence/<kind>/` artifacts.
- `EXIT_CODE: SKIPPED` is invalid on every task in this plan. No task authorizes a skip branch.
- Fail-closed: if any required baseline, final-QC, or coverage artifact is missing or non-numeric where a number is required, the outcome is remediation-required, never PASS.

## Scope Constraints (each violation is a Blocking finding)

Files this plan may create, modify, or delete:

| Action | Path |
| --- | --- |
| create | `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.json` |
| create | `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.md` |
| create | `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` |
| create | `scripts/vscode/Get-PerFileCoverage.ps1` |
| create | `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1` |
| create | `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1` |
| create | `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` |
| create | `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` |
| create | `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` |
| delete | `scripts/temp-extract-coverage.ps1` |
| modify | documents and evidence artifacts under `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/` |

Files this plan MUST NOT touch:

- anything under `QuickFiler/` — zero production C# change. **This explicitly includes `QuickFiler/QuickFiler.csproj`.**
- `QuickFiler.Test/QuickFiler.Test.csproj` — the second shared csproj recorded by the manifest at `### 1b. QuickFiler.Test.csproj is the larger shared-file surface`. This plan adds no C# test file, so it adds no `<Compile Include>` entry and does not edit this file.
- `UtilitiesCS/Properties/AssemblyInfo.cs`.
- `.claude/rules/**`, `CLAUDE.md`, `.github/instructions/**` — no policy re-legislation.
- `coverage.config`, any `*.runsettings`, and any repository-wide coverage threshold.
- `scripts/vscode/Invoke-MSTestWithCoverage.ps1` and `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1` — read-only dependencies. The double-counted `lines-valid` defect in `Get-CoberturaCoverageSummary` is out of scope and tracked at issue #441 (D5).

**Shared-file position (manifest `## Cross-Child Constraints Discovered During Preparation`).** The manifest records three shared files that constrain every child: `QuickFiler/QuickFiler.csproj` (legacy non-SDK, no globbing, so any child adding a production `.cs` file must edit it); `QuickFiler.Test/QuickFiler.Test.csproj` (manifest `### 1b`, also non-SDK with **107 explicit `<Compile Include>` entries and no globbing**, and the **larger** conflict surface of the two, because every child in the epic adds C# test files and so must edit it, whereas only the subset that creates production files touches `QuickFiler.csproj`); and `UtilitiesCS/Properties/AssemblyInfo.cs` (no `InternalsVisibleTo` grant to `QuickFiler.Test`). Neither csproj is owned by any child; both are shared infrastructure the epic edits by necessity. **F1's position on all three is a no-op: this child adds no C# whatsoever — its five Pester test files live under `tests/scripts/vscode/` and are compiled by no csproj — so it adds no `<Compile Include>` entry to either project, it edits none of the three files, and this plan contains no task that edits any of them.** F1's obligation is documentary only — `[P6-T3]` records, inside the ledger itself, the rules a creating sibling must follow for **both** csproj files (append the ledger row in the same change as the `<Compile Include>` entry; minimal adjacent hunks; own entries only; no property, reference, or ordering changes; CRLF preserved; additive fan-in conflicts resolved by keeping both sides). `[P11-T7]` verifies the no-op by asserting zero changed paths under `QuickFiler/`, zero changed paths equal to `QuickFiler.Test/QuickFiler.Test.csproj`, and zero changed paths equal to `UtilitiesCS/Properties/AssemblyInfo.cs`.

## Decisions Record (planning-time choices the executor must not re-litigate)

1. **Research decisions D1-D17 are settled specification.** They are sequenced into tasks below, not re-opened. Where the epic manifest and the research artifact disagree on a verified count, the research artifact wins and the manifest's figure is recorded as a reconciliation note, never as a target. Where the manifest states a **binding directive** (the third classification bucket, dynamic denominator, per-file branch gate, mid-wave rules, latent-defect promotion, cross-child constraints), the manifest governs and `spec.md` v0.3 has already encoded it; this plan implements `spec.md` v0.3.
2. **Verified attribute figures.** 40 attribute usages across 21 compiled files; 14 type-level; 26 member-level; 24 compiled files fully coverage-suppressed once partial-class inheritance is applied; 11 informational `inherited` rows. The manifest's 33 decomposes without residual as 21 compiled-with-attribute + 5 compiled-comment-only + 7 not-compiled. The figure 33 must not appear anywhere in this feature's deliverables as a target.
3. **Disposition unit is the attribute usage, not the file (D2).** `kind` is one of `type`, `member`, `inherited`. `inherited` rows are informational and are excluded from the count of 40.
4. **Classification and disposition are separate axes.** Phases 2 and 3 record dispositions only. Phase 4 records the per-file `classification`. A file may be `ratified-exempt` (for example a generated designer file) while the type-level attribute that currently suppresses it is disposed `remove`; the two statements are consistent and both are recorded.
5. **`source_commit` is the executor's own HEAD short SHA at ledger-authoring time,** recorded in `[P0-T3]`. It is not hard-coded to the research artifact's `74be1964`. The SHA is an observation and is never pinned as a later gate.
6. **The coverage denominator is dynamic (manifest `## Mid-Wave File Creation and the Ledger Denominator`, rule 1).** It is the set of `<Compile Include=...>` entries in `QuickFiler/QuickFiler.csproj` **at evaluation time**. **121 is the authoring-time snapshot** at research HEAD `74be1964`, recorded as an observation and never asserted as a constant. The property this feature verifies is **completeness**: a compiled file that lacks a ledger row is a failure, and a ledger row with no compiled file is a failure; a changed count on its own is not. `[P0-T10]` records the evaluation-time count without halting on a change; `[P10-T19]` is the run-time completeness assertion.
7. **Entry function returns an exit code; it does not call `exit`.** `Invoke-PerFileCoverageGate` performs all reads, writes `-OutputPath` when supplied, emits per-row diagnostics via `Write-Error`, and returns `[pscustomobject] @{ ExitCode = <int>; ReportText = <string> }`. The dot-source-safe script guard writes `ReportText` via `Write-Output` and calls `exit $result.ExitCode`. This is what makes exit codes `0`, `1`, `2` assertable against the entry function in Pester without terminating the test host, as `spec.md` requires.
8. **One internal helper beyond the spec's six named functions is permitted:** `ConvertTo-CoveragePathKey`, the backslash/`OrdinalIgnoreCase` path normalisation shared by `Get-CoberturaPerFileCoverage` and `Test-PerFileCoverageThreshold` (D11). The spec's function list is a minimum public surface, not a maximum; duplicating normalisation in two functions would violate the reusability rule in `.claude/rules/general-code-change.md`. The pure-logic file therefore contains **six** functions: `ConvertTo-CoveragePathKey`, `Get-CoberturaConditionCoverageParts`, `Merge-CoberturaFileLines`, `Get-CoberturaPerFileCoverage`, `Test-PerFileCoverageThreshold`, `Format-PerFileCoverageReport`.
9. **`Get-CoberturaConditionCoverageParts` deliberately duplicates the regex at `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1:146`-`:165` rather than dot-sourcing it.** Two reasons, both recorded in the function's own comment-based help by `[P7-T4]`: the pure-logic file must stay dot-sourceable with no cross-file load-order dependency, and `Invoke-MSTestWithCoverage.Helpers.ps1` is a read-only dependency this feature must not modify. The duplication is a deliberate, documented exception to the reusability rule and is not a defect to be "fixed" during execution.
10. **`schema_version` validation is pure and lives in `Test-PerFileCoverageThreshold`,** which raises a bare `throw` with a specific literal message on an unrecognised value, matching `Invoke-MSTestWithCoverage.Helpers.ps1:113`. The entry function catches and maps to exit `2`. `schema_version` stays at `1` even though the JSON gains three new keys, because no consumer of the ledger exists yet and no reader can be broken by an additive change.
11. **`scripts/temp-extract-coverage.ps1` is deleted (D13),** not deprecated in place. The rejected alternative — a deprecation comment — preserves the risk of a sibling child using it for no benefit.
12. **The reference Cobertura sample used for cross-checks and the smoke run is** `docs/features/active/2026-08-06-quickfiler-high-confidence-queue-init-stall-424/evidence/qa-gates/coverage-final.cobertura.xml`. It is read-only input. The smoke run in `[P8-T6]` is expected to exit `1` because wave-1 coverage work has not happened yet; that expected non-zero result is recorded as an observation and is **not** a QC gate.
13. **Coverage gate figures.** `.claude/rules/powershell.md` requires line coverage >= 85% and branch coverage >= 75%, and treats coverage regression on changed lines as blocking. The change-scoped gate applied by this plan is: the two new production scripts each reach >= 85% **line** coverage, and the repository-wide PowerShell line-coverage figure does not regress against the `[P0-T6]` baseline. The >= 75% branch-coverage requirement in `.claude/rules/powershell.md` is not measurable by the PowerShell toolchain (Pester's JaCoCo output emits no BRANCH counter); that gap is recorded as an out-of-scope observation in `[P11-T5]` and is not re-legislated by this feature. This is a statement about **PowerShell** coverage only and has no bearing on the **Cobertura C# branch data** this feature's harness computes for QuickFiler files. If the recorded baseline repository-wide line figure is already below 85%, that pre-existing state is recorded numerically in the delta artifact and the no-regression rule governs. The two new scripts' >= 85% line figure is measured by a supplementary direct `Invoke-Pester` run scoped to those two files, recorded in `[P11-T3]`. The feasibility of the supplementary mechanism is proven in Phase 0 by `[P0-T12]`, so a missing Pester v5 or a non-functioning `New-PesterConfiguration` halts the plan at Phase 0 rather than at Phase 11.
14. **Tests read repository files but create none.** Reading `QuickFiler/QuickFiler.csproj`, `coverage-ledger.json`, and `coverage-ledger.md` from `$PSScriptRoot`-relative paths is required by the completeness assertion and is permitted. The prohibition in `.claude/rules/general-unit-test.md` is on **creating** temporary files; no test in this plan creates, writes, or deletes a filesystem path. The `-OutputPath` write is asserted through a mocked `Set-Content`. The two csproj-drift fixture cases in `[P10-T20]` and `[P10-T21]` operate on **in-memory** csproj and ledger strings, never on the real files.
15. **PoshQC scan-scope is verified before the harness is treated as gated (D17).** No repo-local scan configuration exists to inspect, and the PoshQC MCP tools return no scanned-file list. Phase 0 therefore proves scan membership for the two target directories by scoped invocation and observable effect (format, analyze) and by `artifacts/pester/pester-junit.xml` suite entries (test); Phase 11 confirms all **seven** new file paths the same way. A negative Phase 0 result is a Blocking finding to be recorded and escalated, never worked around by relocating the files.
16. **`scripts/vscode/` is not analyzer-clean at baseline.** Verified at a clean working tree: `mcp__drm-copilot__run_poshqc_analyze` exits `1` repo-wide with 24 issues, and exits `1` with 16 issues when scoped to `["scripts/vscode"]`; all 16 live in `scripts/vscode/`. `tests/scripts/vscode/` is clean at 0. This refutes the "analyzer-clean" premise carried in research section 5.5 and in the `spec.md` Constraints section. Because this plan may create only new files in those directories and may not modify existing ones, `EXIT_CODE: 0` from the analyze stage is unreachable within the plan's own scope constraints. The applied analyze gate is therefore a baseline-delta gate, defined in the Toolchain Contract and applied by `[P11-T2]` and `[P11-T4]`; the premise correction is recorded here rather than by editing the research artifact or `spec.md`.
17. **The pure-logic file's size target rose to 250-380 lines.** `spec.md` v0.3 raises it from 200-300 because of the branch-coverage derivation, the `condition-coverage` parser, and the two additional row states. The 500-line cap in `.claude/rules/general-code-change.md` still applies and is enforced by `[P7-T9]` and `[P11-T10]`. **Split contingency:** a measured length above 380 is recorded as an observation and is not a failure. A length at or above 500 is a Blocking halt escalated for a plan revision that adds a second pure-logic file to the Scope Constraints table; the split is never performed inside execution, because the Scope Constraints table admits no unnamed production file.
18. **The harness Pester suite is authored as four files from the outset, and the ledger suite is a fifth.** The revision that added branch coverage, the `condition-coverage` parser, the two new row states, the per-row line target, and eleven new seeded conditions raised the harness case count from 41 to 60 `It` blocks, which no three-file split absorbs safely. The four-file split is planned, not conditional; the Scope Constraints table admits no unnamed file, so an execution-time split would itself be a scope violation. Assignments, with no function's cases split across files:
    - `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1` — `Merge-CoberturaFileLines` (6) and `Get-CoberturaConditionCoverageParts` (3) = **9 `It`**.
    - `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1` — `Get-CoberturaPerFileCoverage` (11) and `Format-PerFileCoverageReport` (7) = **18 `It`**. These two functions are the row-derivation and row-rendering ends of one pipeline, so the pairing is cohesive rather than arbitrary.
    - `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` — `Test-PerFileCoverageThreshold` = **18 `It`**.
    - `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` — `Invoke-PerFileCoverageGate` = **15 `It`**.
    - `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` — ledger completeness, mapping, dispositions and drift = **17 `It`**.

    At the measured local density of 18.0 lines per `It` (`tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`, 451 lines for 25 `It`) the largest projection is 18 x 18.0 = 324 case lines plus a fixture section, leaving clear margin against 500. The alternative three-file split projected a 24-`It` file at roughly 432 case lines plus fixtures, which plausibly breaches 500; a 500-line breach at execution time is a Blocking halt, so the further split is taken up front.
19. **Five test files do not require a change-budget override.** `.claude/rules/powershell.md` line 40 caps a **batch** at 3 production files and 3 test files. This plan creates 2 production files and 5 test files, but the cap is per batch, not per feature: Phase 9 creates three test files (at the cap, not over it) and Phase 10 creates two (the entry-function suite and the ledger suite), in a separate batch. No batch exceeds three test files and no batch exceeds three production files, so no override is required. This feature creates **two production files and five test files, at seven new file paths in total**, plus the two ledger documents. This supersedes the "two test files" phrasing in the `spec.md` "Constraints & Risks" section, the two-file enumeration in AC5's *Verification* paragraph, and the "four new file paths" phrasing in AC6's, AC4's, and the `## API / CLI Surface` **Files (D6)** table, without changing any acceptance-criterion checkbox text. No `spec.md` or `user-story.md` **prose** is edited by this plan; the only edits are the checkbox-character updates directed by `[P11-T12]` and `[P11-T13]`. This Decisions Record entry is the supersession mechanism.
20. **Issue #441 is referenced, not re-promoted.** The manifest's `## Latent Defect Promotion` directs latent defects to GitHub issues via the MCP promotion lifecycle. F1's one latent defect — the double-counted `<line>` descendant axis in `Get-CoberturaCoverageSummary` — is already tracked at **issue #441**, created directly with `gh issue create` rather than through the MCP promotion surface. The directive's purpose is met: a persistent GitHub issue exists outside the feature folder and survives archival. **No second issue is created**, because that would produce a duplicate. `[P6-T6]` records the existing #441 reference and the mechanism deviation in the ledger's notes so a reviewer does not read the omission as a missing promotion. No task in this plan promotes a defect.

---

### Phase 0 — Baseline Capture, Policy Compliance, and PoshQC Scan-Scope Verification

- [ ] [P0-T1] Read the policy documents in `policy-compliance-order` order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md` — and write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/phase0-instructions-read.<ts>.md`.
  - Acceptance: artifact contains `Timestamp:`, `Policy Order:` listing those four documents in that order, and the explicit list of files read. No policy document is modified.
- [ ] [P0-T2] Read the five requirements sources listed under "Requirements Sources" above in full and append a `Requirements Sources Read:` section to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/phase0-instructions-read.<ts>.md`.
  - Acceptance: the section lists all five paths and records the six acceptance criteria verbatim from `spec.md`, confirming they match `user-story.md` item for item, and records that `spec.md` is at version 0.3 as amended for epic manifest commit `c8d9e4c8`.
- [ ] [P0-T3] Record the git baseline: `git rev-parse HEAD`, `git rev-parse --short HEAD`, `git status --porcelain`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/git-baseline.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the full and short HEAD SHAs and the porcelain output verbatim. The short SHA is the value that `[P5-T1]` writes into `source_commit`. The SHA is recorded as an observation and is never pinned as a later gate; later gates compare tree state against this recorded baseline.
- [ ] [P0-T4] Capture the formatting baseline: invoke `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the executor's own current worktree root; then run `git status --porcelain`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-format-baseline.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the returned `ok` value and `summary` string verbatim, and states whether the formatter modified any file as determined by the `git status --porcelain` output taken immediately after the invocation and compared against the `[P0-T3]` porcelain baseline. The tool returns no file count and no file list, so no scanned-file figure is recorded. If files were modified on a clean baseline, the list is recorded and committed separately before Phase 1 begins, and that same list is carried into `[P11-T9]` as an accounted-for change.
- [ ] [P0-T5] Capture the analyzer baseline: invoke `mcp__drm-copilot__run_poshqc_analyze` with the same `workspace_root` and no `scan_folders` argument, then invoke it again with `scan_folders: ["scripts/vscode"]` and again with `scan_folders: ["tests/scripts/vscode"]`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-analyze-baseline.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the unscoped diagnostic count and each scoped diagnostic count as numbers. The tool returns a summary string only and emits no per-file diagnostic list, so no file is named as carrying a diagnostic; attribution is by count delta against these recorded numbers. The recorded unscoped count is the analyze baseline that `[P11-T2]` and `[P11-T4]` compare against, so pre-existing debt is distinguishable from anything this feature introduces.
- [ ] [P0-T6] Capture the Pester + coverage baseline: invoke `mcp__drm-copilot__run_poshqc_test` with the same `workspace_root`; then read `artifacts/pester/pester-junit.xml` and `artifacts/pester/powershell-coverage.xml` from the executor's own worktree; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-test-coverage-baseline.<ts>.md`.
  - Acceptance: artifact has all four required fields. Test counts are taken by reading `artifacts/pester/pester-junit.xml` and summing the `tests`, `failures`, and `skipped` attributes across its `<testsuite>` elements, which are recorded in `Output Summary:` as numbers. The repository-wide **line**-coverage percentage is taken by reading `artifacts/pester/powershell-coverage.xml` and computing `covered / (covered + missed)` from the report-level `<counter type="LINE">`; the numeric value is mandatory and a placeholder invalidates the artifact. Branch coverage is not recorded as a number; the artifact instead carries the literal line `Branch coverage: not emitted by Pester JaCoCo output (no BRANCH counter present)`.
- [ ] [P0-T7] Verify empirically (D17) that `scripts/vscode/` and `tests/scripts/vscode/` are inside the PoshQC **format** scan set. The tool returns no file list, so the proof is by scoped invocation and observable effect: invoke `mcp__drm-copilot__run_poshqc_format` with `workspace_root` = the executor's own current worktree root and `scan_folders: ["scripts/vscode"]`, then again with `scan_folders: ["tests/scripts/vscode"]`, recording the returned `ok` value and `summary` for each, followed by `git status --porcelain` after each. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-scan-scope-format.<ts>.md`.
  - Acceptance: artifact has all four required fields and states `IN SCOPE` or `NOT IN SCOPE` per directory. `IN SCOPE` requires `ok: true` with a summary naming `1 selected scan folder(s)` and either no working-tree change or a working-tree change confined to that directory. A returned error, or a summary indicating the folder was not selected or contained no files, is `NOT IN SCOPE`, a Blocking finding that halts the plan for escalation; it is not worked around by relocating the new files.
- [ ] [P0-T8] Verify empirically (D17) that the same two directories are inside the PoshQC **analyze** scan set: invoke `mcp__drm-copilot__run_poshqc_analyze` with the same `workspace_root` and `scan_folders: ["scripts/vscode"]`, then with `scan_folders: ["tests/scripts/vscode"]`, recording the returned issue count for each, and compare against the unscoped `[P0-T5]` count. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-scan-scope-analyze.<ts>.md`.
  - Acceptance: artifact has all four required fields and records the three counts. `scripts/vscode` is `IN SCOPE` when its scoped count is non-zero and does not exceed the unscoped count (a non-zero scoped count proves the directory is read). `tests/scripts/vscode` is `IN SCOPE` when the invocation returns `ok: true` with a summary naming `1 selected scan folder(s)`; a returned error naming the scan folder is `NOT IN SCOPE`. That criterion is deterministic because `Resolve-PoshQCScanFolder` throws `Failed to resolve scan folder '<f>'` for an unresolvable folder, whereas a "no files found" condition is written to the information stream and is not observable by the caller. A `NOT IN SCOPE` result is a Blocking finding that halts the plan.
- [ ] [P0-T9] Verify empirically (D17) that `tests/scripts/vscode/` is inside the PoshQC **Pester discovery** set: read `artifacts/pester/pester-junit.xml` written by the `[P0-T6]` run and confirm each suite's `<testsuite name=...>` entry by full path for the four existing suites `Install-RepoDotNetSdk.Tests.ps1`, `Invoke-MSTest.RunSettings.Tests.ps1`, `Invoke-MSTestWithCoverage.Helpers.Tests.ps1`, and `Invoke-VSBuild.Tests.ps1`, recording each entry's `tests`, `failures`, and `skipped` attributes; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-scan-scope-test.<ts>.md`.
  - Acceptance: artifact has all four required fields, names all four discovered suites by the full path recorded in their `<testsuite name=...>` attribute, and records the three numeric attributes per suite. Any suite absent from `pester-junit.xml` is a Blocking finding that halts the plan.
- [ ] [P0-T10] Record the compiled-surface count **at evaluation time** and the authoring-time snapshot: count the `<Compile Include=` entries in `QuickFiler/QuickFiler.csproj` using `Select-String -Path 'QuickFiler/QuickFiler.csproj' -Pattern '<Compile Include=' -AllMatches | Measure-Object -Line`, with the exact command recorded in `Command:`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/compiled-surface-count.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the evaluation-time count as a number and records **121** as the authoring-time snapshot at research HEAD `74be1964`. Per Decisions Record item 6 the denominator is dynamic, so **a count other than 121 is not a halt condition**: the artifact records the added and removed `<Compile Include>` paths against the research baseline, and those paths are carried into `[P1-T1]` so that every compiled file present at evaluation time receives a ledger row. The plan halts only if the csproj cannot be read or an `<Compile Include>` value cannot be resolved to a ledger key, because in that case completeness cannot be established. The recorded evaluation-time count is the number `[P1-T1]`, `[P4-T9]`, `[P5-T2]`, and `[P5-T6]` transcribe; wherever those tasks name 121 they mean the authoring-time snapshot and use the recorded evaluation-time count if it differs.
- [ ] [P0-T11] Record the pre-change state of every path this plan creates or deletes: confirm that the seven new file paths `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`, `scripts/vscode/Get-PerFileCoverage.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1`, and `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1`, together with `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.json` and `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.md`, do not exist, and that `scripts/temp-extract-coverage.ps1` exists with its line count; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/target-paths-baseline.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records existence and line count per path. Also records the current line counts of `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1` (expected 357) and `scripts/vscode/Invoke-MSTestWithCoverage.ps1` (expected 348) as read-only dependency baselines.
- [ ] [P0-T12] Prove that direct-`Invoke-Pester` per-file coverage measurement works in the executor's environment, because the bundled PoshQC Pester configuration does not instrument `scripts/vscode/` and `[P11-T3]` depends on this mechanism. Run the same command shape `[P11-T3]` will use, but against an existing suite and an existing production script: `pwsh -NoProfile -Command '$c = New-PesterConfiguration; $c.Run.Path = "tests/scripts/vscode"; $c.Run.PassThru = $true; $c.CodeCoverage.Enabled = $true; $c.CodeCoverage.Path = @("scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1"); $c.CodeCoverage.OutputFormat = "JaCoCo"; $c.CodeCoverage.OutputPath = "docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/direct-pester-coverage-feasibility.<ts>.xml"; $r = Invoke-Pester -Configuration $c; "PASSED=$($r.PassedCount) FAILED=$($r.FailedCount)"'`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/direct-pester-coverage-feasibility.<ts>.md`. The outer quoting must be single quotes and every inner string literal double quotes, so that neither a Bash-hosted nor a PowerShell-hosted invocation expands `$c`, `$r`, or `$(...)` before `pwsh` receives the script. The command is run with the current directory set to the worktree root, because all four paths are repository-relative. Verified at preflight: this form runs to completion in both shells, `New-PesterConfiguration` resolves against Pester 5.6.1, `CodeCoverage.OutputPath` creates the target directory if absent, and the emitted JaCoCo carries a per-file `<class sourcefilename=...>` element with a `LINE` counter and no `BRANCH` counter. This run creates no temporary file: its only output is the named evidence artifact.
  - Acceptance: artifact has all four required fields and records the emitted JaCoCo file's `<class sourcefilename="Invoke-MSTestWithCoverage.Helpers.ps1">` LINE counter (`missed` and `covered`) as numbers, together with the Pester passed/failed counts. If Pester v5 is unavailable, if `New-PesterConfiguration` is not resolvable, or if no such `<class>` element is emitted, that is a Blocking finding that halts the plan for escalation; it is not worked around, because `[P11-T3]` has no other way to obtain per-file coverage for the two new production scripts.

### Phase 1 — Compiled-Surface and Attribute Inventories

- [ ] [P1-T1] Transcribe every `<Compile Include=...>` path from `QuickFiler/QuickFiler.csproj` into a working inventory at `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/compiled-surface-inventory.<ts>.md`, each expressed as the ledger key `QuickFiler\<Include value>` with backslash separators. The row count is the evaluation-time count recorded in `[P0-T10]`.
  - Acceptance: the inventory row count equals the `[P0-T10]` evaluation-time count; the authoring-time snapshot of 121 is recorded alongside it. Per-directory counts are recorded, expected at the authoring-time snapshot as Controllers 52, Helper Classes 13, Interfaces 14, Properties 3, Viewers 39, summing to 121. Any deviation from the snapshot is recorded with the added and removed paths transcribed from `[P0-T10]` and is not a halt; a row count that does not equal the `[P0-T10]` count is a transcription error and halts the phase.
- [ ] [P1-T2] Add the physical line count for each inventory row, transcribed from research §2.3 for rows present at the authoring-time snapshot and measured directly for any row added since, and independently re-count the three files the research corrects — `Viewers\BayesianPerformanceViewer.Designer.cs` (expect 499), `Viewers\EfcViewer.Designer.cs` (expect 4277), `Viewers\QfcFormViewer.Designer.cs` (expect 258) — using physical line count that includes a final line lacking a trailing newline (D14).
  - Acceptance: every row carries a numeric line count; the three re-counted values match the expected figures or the discrepancy is recorded with the observed value taken as authoritative.
- [ ] [P1-T3] Add the `owning_child` value for each inventory row, transcribed verbatim from the "Feature File Assignments" section of `docs/features/epics/quickfiler-per-file-coverage/epic.md`.
  - Acceptance: per-child counts equal F2 11, F3 11, F4 14, F5 5, F6 10, F7 5, F8 6, F9 4, F10 11, F11 2, F12 5, F13 15, F14 10, F15 12, summing to 121 at the authoring-time snapshot; no compiled file is unassigned; no compiled file is assigned to more than one child; no assigned path is absent from the compiled list. A row added since the snapshot takes the `owning_child` of the sibling that added it, recorded with that attribution.
- [ ] [P1-T4] Verify the two path-collision hazards recorded in research §2.2 and record the result in the same inventory artifact: `Controllers\IQfcFormController.cs` (43 lines) and `Interfaces\IQfcFormController.cs` (25 lines) are two distinct compiled rows both owned by F6; `Controllers\IQfcHomeController.cs` (20 lines) is compiled while `Interfaces\IQfcHomeController.cs` (18 lines) is not in the `<Compile>` list and is therefore not a row.
  - Acceptance: the inventory confirms full repo-relative path uniqueness across all rows and confirms that leaf filename alone is not unique. `Interfaces\IQfcHomeController.cs` is absent from the inventory.
- [ ] [P1-T5] Verify in the working tree all 14 type-level `[ExcludeFromCodeCoverage]` sites listed in research §3.2, checking path, line number, and decorated declaration for each; record them in `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/attribute-inventory.<ts>.md`.
  - Acceptance: 14 verified sites. Each records `<path>:<line>`, the decorated declaration text, whether the type is `partial`, and the owning child. `Controllers\QfcHighConfidencePreFilter.cs:166` is recorded as decorating the **second top-level type** `FolderScoringService`, not the file's primary type. Any site whose line number has shifted is recorded with the observed line number, which becomes authoritative.
- [ ] [P1-T6] Verify in the working tree all 26 member-level `[ExcludeFromCodeCoverage]` sites listed in research §3.3 and append them to the attribute inventory artifact.
  - Acceptance: 26 verified sites across exactly 7 files, distributed as `Controllers\QfcItemController.Initialization.cs` 7, `Controllers\QfcItemController.EventHandlers.cs` 5, `Controllers\QfcItemController.ViewerSetup.cs` 3, `Controllers\QfcItemController.Navigation.cs` 2, `Controllers\QfcItemController.Conversation.cs` 1, `Controllers\QfcItemController.EventWiring.cs` 1, `Viewers\BreadcrumbPopupUiOperations.cs` 7. Each records `<path>:<line>`, the decorated member name, and the owning child.
- [ ] [P1-T7] Verify that the five compiled files whose only match is a comment or XML-doc mention carry **no** attribute — `Controllers\QfcScanProgressBandMapper.cs:12`, `Viewers\ItemViewer.Commands.cs:10`, `Viewers\ItemViewer.DisplayState.cs:9`, `Viewers\ItemViewer.FolderSearch.cs:17`, `Viewers\ItemViewer.WebViewThread.cs:12` — and append the finding to the attribute inventory artifact.
  - Acceptance: all five confirmed as prose mentions with the surrounding context quoted; none is counted as a disposition. The artifact states explicitly that the epic manifest's `[X]` marker on `QfcScanProgressBandMapper.cs` is refuted.
- [ ] [P1-T8] Verify that the seven attribute-carrying files listed in research §3.6 are absent from the `<Compile Include>` list — `Viewers\EfcViewer3.cs`, `Viewers\QfcFormViewerDark.cs`, `Viewers\QfcFormViewerExpanded.cs`, `Viewers\QfcItemViewer.cs`, `Viewers\QfcItemViewerExpandedLight.cs`, `Viewers\QfcItemViewerLightSelected.cs`, `Viewers\QfcItemViewerV1.cs` — and append the finding to the attribute inventory artifact.
  - Acceptance: all seven confirmed absent from the compiled list; the artifact states they are out of scope and will appear in the ledger's reconciliation note only, never as ledger rows.
- [ ] [P1-T9] Record the reconciled attribute totals in the attribute inventory artifact: 40 non-inherited usages = 14 type-level + 26 member-level, spread across exactly 21 distinct compiled file paths; 24 compiled files fully coverage-suppressed once partial-class inheritance is applied; and the decomposition 33 = 21 compiled-with-attribute + 5 compiled-comment-only + 7 not-compiled.
  - Acceptance: all six figures present and arithmetically consistent with `[P1-T5]` through `[P1-T8]`. The artifact states that 33 is not a target and will not be restated as one in any deliverable. The artifact additionally states that this inventory is fixed at authoring time and describes the pre-existing attribute population: a file created mid-wave by a sibling carries no attribute and therefore no disposition.
- [ ] [P1-T10] Record the reference-report cross-check in `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/reference-report-crosscheck.<ts>.md`: from `docs/features/active/2026-08-06-quickfiler-high-confidence-queue-init-stall-424/evidence/qa-gates/coverage-final.cobertura.xml`, extract every distinct `filename` beginning `QuickFiler\` and compare against the inventory.
  - Acceptance: artifact has all four required fields; `Output Summary:` records 70 present filenames and 51 absent compiled files, with the absence decomposition 24 attribute-suppressed + 23 interface-only declarations + 1 enum-only (`Helper Classes\QfEnums.cs`) + 1 entirely commented out (`Helper Classes\cInfoMail.cs`) + 1 assembly-attributes-only (`Properties\AssemblyInfo.cs`) + 1 `DebuggerNonUserCode`-suppressed (`Properties\Resources.Designer.cs`) = 51, and 70 + 51 = 121 at the authoring-time snapshot. The artifact records explicitly that the first three of the last four groups — the 23 interface-only declarations plus `QfEnums.cs`, `cInfoMail.cs`, and `AssemblyInfo.cs`, **26 files in total** — are the third-bucket candidate population per research §4.7, and that `Properties\Resources.Designer.cs` is **not** among them because its absence is caused by `[DebuggerNonUserCodeAttribute]` on generated code rather than by an absence of executable IL. Also records that `Properties\Settings.Designer.cs` is **present** at `line-rate="0"`, proving that absence means no instrumentable code was emitted rather than never executed. This file is read-only input and is not modified.

### Phase 2 — Type-Level Attribute Dispositions and the Inherited Suppression Map

Each task in this phase records one disposition into `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/attribute-dispositions.<ts>.md` with the fields `kind: type`, `site`, `target`, `disposition`, `owning_child`, `rationale`, and `files_affected`. `disposition` is `ratified` only when the irreducible-remainder standard in `epic.md` "Shared Design" section 1 is met — no interface seam, injectable delegate, or adapter can isolate the logic — and the rationale names the specific host dependency. Otherwise the disposition is `remove` and carries the D16 sequencing instruction. Classification of the affected files is recorded separately in Phase 4.

- [ ] [P2-T1] Record the disposition for `Controllers\EfcFormController.cs:27` (`internal class EfcFormController : IFilerFormController`, non-partial, F9), affecting 1 file.
  - Acceptance: one disposition row with all seven fields; if `remove`, it carries the tests-first / attribute-removal-last instruction; if `ratified`, the rationale names the specific COM or WinForms host dependency that no seam can isolate.
- [ ] [P2-T2] Record the disposition for `Controllers\EfcItemController.cs:25` (`internal class EfcItemController : IItemControler`, non-partial, F9), affecting 1 file.
  - Acceptance: as `[P2-T1]`.
- [ ] [P2-T3] Record the disposition for `Controllers\KeyboardHandler.cs:22` (`internal class KeyboardHandler : IQfcKeyboardHandler`, non-partial, F3), affecting 1 file.
  - Acceptance: as `[P2-T1]`. The epic manifest's F3 entry states this file "needs seams to drop its exemption"; a `ratified` disposition must explain why that manifest expectation does not hold.
- [ ] [P2-T4] Record the disposition for `Controllers\QfcCollectionController.cs:21` (`public class QfcCollectionController : IQfcCollectionController`, non-partial, F11, 2,349 lines), affecting 1 file.
  - Acceptance: as `[P2-T1]`. The rationale records that this file also breaches the 500-line limit and that F11's plan must sequence the partial split before or with the coverage work, and that each file the split creates is appended to the ledger by F11 in the same change that adds its `<Compile Include>` entry.
- [ ] [P2-T5] Record the disposition for `Controllers\QfcDatamodel.cs:25` (`public partial class QfcDatamodel : IQfcDatamodel`, partial, F5), affecting 3 files: `Controllers\QfcDatamodel.cs`, `Controllers\QfcDatamodel.FrameBuilding.cs`, `Controllers\QfcDatamodel.QueueProcessing.cs`.
  - Acceptance: as `[P2-T1]`, and `files_affected` lists all three partials. If `remove`, the row states that the removal is a per-type decision executed as a single unit across all three files.
- [ ] [P2-T6] Record the disposition for `Controllers\QfcExplorerController.cs:20` (`internal class QfcExplorerController : IQfcExplorerController`, non-partial, F6), affecting 1 file.
  - Acceptance: as `[P2-T1]`.
- [ ] [P2-T7] Record the disposition for `Controllers\QfcHighConfidencePreFilter.cs:166` (`internal sealed class FolderScoringService : IFolderScoringService`, a **second top-level type** in the file, F2), affecting the `FolderScoringService` type only.
  - Acceptance: one disposition row with all seven fields; `files_affected` records that the file's primary type `QfcHighConfidencePreFilter` is unaffected and is measured at line-rate 1.0 in the reference report; the rationale cites the existing XML `<remarks>` at lines 157-164 and judges it against the irreducible-remainder standard rather than accepting it on assertion. The row states explicitly that the epic manifest's `[X]` marker on this file is mis-scoped.
- [ ] [P2-T8] Record the disposition for `Viewers\EfcViewer.cs:20` (`public partial class EfcViewer : Form`, partial, F9), affecting 2 files: `Viewers\EfcViewer.cs` and `Viewers\EfcViewer.Designer.cs` (4,277 lines).
  - Acceptance: as `[P2-T5]`. If `remove`, the row states that `EfcViewer.Designer.cs` re-enters the coverage denominator at the same moment.
- [ ] [P2-T9] Record the disposition for `Viewers\ItemViewer.cs:20` (`public partial class ItemViewer : UserControl, IItemViewer, IContainerControlLocal`, partial, F14), affecting 7 files: `ItemViewer.cs`, `ItemViewer.DisplayState.cs`, `ItemViewer.Commands.cs`, `ItemViewer.Breadcrumb.cs`, `ItemViewer.FolderSearch.cs`, `ItemViewer.WebViewThread.cs`, `ItemViewer.Designer.cs` (6,224 lines).
  - Acceptance: as `[P2-T5]`, with all seven `Viewers\`-prefixed paths listed. If `remove`, the row states that all seven partials, including the 6,224-line designer file, re-enter the denominator simultaneously and must be planned and executed by F14 as a single unit.
- [ ] [P2-T10] Record the disposition for `Viewers\QfcFormViewer.cs:17` (`public partial class QfcFormViewer : Form, IQfcFormViewer`, partial, F15), affecting 2 files: `Viewers\QfcFormViewer.cs` and `Viewers\QfcFormViewer.Designer.cs`.
  - Acceptance: as `[P2-T5]`.
- [ ] [P2-T11] Record the disposition for `Viewers\QfcItemViewerExpanded.cs:18` (`public partial class QfcItemViewerExpanded : UserControl`, partial, F15), affecting 2 files: `Viewers\QfcItemViewerExpanded.cs` and `Viewers\QfcItemViewerExpanded.Designer.cs`.
  - Acceptance: as `[P2-T5]`.
- [ ] [P2-T12] Record the disposition for `Viewers\WebView2BreadcrumbHost.cs:29` (`public sealed class WebView2BreadcrumbHost : IBreadcrumbWebHost`, non-partial, F13), affecting 1 file.
  - Acceptance: as `[P2-T1]`. A `ratified` rationale must name the specific WebView2 runtime dependency that the existing `IBreadcrumbWebHost` interface does not already isolate.
- [ ] [P2-T13] Record the disposition for `Viewers\WebView2CoreInitializer.cs:15` (`public sealed class WebView2CoreInitializer : IWebViewCoreInitializer`, non-partial, F13), affecting 1 file.
  - Acceptance: as `[P2-T12]`, against `IWebViewCoreInitializer`.
- [ ] [P2-T14] Record the disposition for `Viewers\WebView2Messenger.cs:20` (`public sealed class WebView2Messenger : IWebViewMessenger, IDisposable`, non-partial, F13), affecting 1 file.
  - Acceptance: as `[P2-T12]`, against `IWebViewMessenger`.
- [ ] [P2-T15] Record the 11 informational `kind: inherited` rows in the same artifact, one per partial file that emits no coverage data because a sibling partial carries the type-level attribute: 6 for `ItemViewer` (`.DisplayState.cs`, `.Commands.cs`, `.Breadcrumb.cs`, `.FolderSearch.cs`, `.WebViewThread.cs`, `.Designer.cs`, each with `from: QuickFiler\Viewers\ItemViewer.cs:20`), 2 for `QfcDatamodel` (`from: QuickFiler\Controllers\QfcDatamodel.cs:25`), 1 for `EfcViewer`, 1 for `QfcFormViewer`, 1 for `QfcItemViewerExpanded`.
  - Acceptance: exactly 11 rows, each carrying `kind: inherited` and a `from` value that matches one of the 14 type-level sites recorded in `[P1-T5]`. The artifact states that inherited rows are informational, are not dispositions, and are excluded from the count of 40.
- [ ] [P2-T16] Verify that every `remove` disposition recorded in `[P2-T1]` through `[P2-T14]` carries the complete D16 sequencing instruction: write the covering tests first and remove the attribute last, because removal expands the denominator immediately while the numerator grows only as tests land; and, for a `partial` type, that the removal is a per-type decision executed across every affected partial simultaneously.
  - Acceptance: the artifact records a per-disposition compliance check; any `remove` row lacking either clause is corrected before the phase closes.
- [ ] [P2-T17] Verify the type-level rollup in the dispositions artifact: exactly 14 rows with `kind: type`, across 14 distinct sites and 14 distinct files, each with a `disposition` value in `{ratified, remove}`, a non-empty `owning_child`, and a non-empty `rationale`; and exactly 24 distinct compiled file paths appear across all `files_affected` lists plus the 11 inherited rows.
  - Acceptance: both counts confirmed; the 24-file suppressed set matches research §3.5 exactly (8 non-partial types + 7 `ItemViewer` + 3 `QfcDatamodel` + 2 `EfcViewer` + 2 `QfcFormViewer` + 2 `QfcItemViewerExpanded`). The artifact additionally records that none of the 24 is a third-bucket candidate, so no `interface-only` row will carry an attribute disposition.

### Phase 3 — Member-Level Attribute Dispositions

Each task records `kind: member` rows into the same `attribute-dispositions.<ts>.md` artifact, one row per attribute usage, each with `site`, `target` (the decorated member declaration), `disposition`, `owning_child`, and `rationale`. Every `remove` carries the D16 sequencing instruction; a member-level removal returns only that member's lines to the denominator, and the rationale states that scope explicitly.

- [ ] [P3-T1] Record the 7 member-level dispositions in `Controllers\QfcItemController.Initialization.cs` at lines 138, 168, 200, 260, 291, 403, 436 (`Initialize(...)`, `Initialize(bool)`, `InitializeAsync`, `InitializeGraphicsAsync`, `InitializeSequentialAsync`, `CreateAsync`, `CreateSequentialAsync`; F10).
  - Acceptance: 7 rows, each with all six fields; each `ratified` rationale names the specific Outlook Interop or WinForms dependency that no seam can isolate; each `remove` carries the sequencing instruction and states that only the named member's lines are affected.
- [ ] [P3-T2] Record the 5 member-level dispositions in `Controllers\QfcItemController.EventHandlers.cs` at lines 60, 83, 97, 111, 125 (`BtnPopOut_Click`, `BtnReply_Click`, `BtnReplyAll_Click`, `BtnForward_Click`, `TxtboxBody_DoubleClick`; F10).
  - Acceptance: as `[P3-T1]`, 5 rows.
- [ ] [P3-T3] Record the 3 member-level dispositions in `Controllers\QfcItemController.ViewerSetup.cs` at lines 38, 132, 253 (`InitializeWebViewAsync`, `EnsureBreadcrumbPipeline`, `ResolveControlGroupsAsync`; F10).
  - Acceptance: as `[P3-T1]`, 3 rows.
- [ ] [P3-T4] Record the 2 member-level dispositions in `Controllers\QfcItemController.Navigation.cs` at lines 173, 191 (`ToggleExpansion`, `ToggleExpansionAsync`; F10).
  - Acceptance: as `[P3-T1]`, 2 rows.
- [ ] [P3-T5] Record the 1 member-level disposition in `Controllers\QfcItemController.Conversation.cs` at line 79 (`DoLoadConversationResolverCoreAsync`; F10).
  - Acceptance: as `[P3-T1]`, 1 row.
- [ ] [P3-T6] Record the 1 member-level disposition in `Controllers\QfcItemController.EventWiring.cs` at line 99 (`WebView2Control_CoreWebView2InitializationCompleted`; F10).
  - Acceptance: as `[P3-T1]`, 1 row.
- [ ] [P3-T7] Record the 7 member-level dispositions in `Viewers\BreadcrumbPopupUiOperations.cs` at lines 105, 380, 383, 390, 394, 412, 457 (`ShowOwnedPopup`, `CreateProductionControl`, `BeginProductionInitialization`, `ReadProductionCore`, `BeginProductionNavigation`, `DisposeProductionSurface`, `BindProductionNavigation`; F13).
  - Acceptance: as `[P3-T1]`, 7 rows.
- [ ] [P3-T8] Record in the dispositions artifact that all 7 member-bearing files are measured in the reference report and are therefore not part of the 24-file suppressed set, citing the reference-report cross-check from `[P1-T10]`.
  - Acceptance: the artifact names all 7 files and confirms each appears in the reference report's QuickFiler package.
- [ ] [P3-T9] Verify the combined disposition rollup: exactly 26 rows with `kind: member` across exactly 7 distinct files; exactly 40 non-inherited rows total (14 type + 26 member) across exactly 21 distinct compiled file paths; exactly 11 rows with `kind: inherited`, excluded from the 40.
  - Acceptance: all five counts confirmed and recorded. The 21 distinct paths are enumerated. Any deviation halts the phase for correction.

### Phase 4 — Per-File Classification of All Compiled Files

Each task records `classification`, `exempt_ground`, `line_target_percent`, and `rationale` for its named file set into `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/classification-worksheet.<ts>.md`. Per `spec.md` v0.3 the classification enum has **three** values — `testable`, `ratified-exempt`, `interface-only` — and the `exempt_ground` determines the classification:

| Ground | Classification |
| --- | --- |
| `generated-designer` | `ratified-exempt` |
| `irreducible-host-wiring` | `ratified-exempt` |
| `interface-only` | `interface-only` |
| `null` | `testable` |

Every `testable` row carries `exempt_ground: null` and `rationale: null`. Every non-`testable` row carries a non-empty `rationale`. `line_target_percent` is `null` on every row authored by this phase, because every file in the authoring-time compiled surface is pre-existing production code governed by the ledger-wide `threshold_percent`; the `90.0` value is reserved for files a sibling creates mid-wave.

- [ ] [P4-T1] Classify the 7 compiled WinForms designer files as `ratified-exempt` / `generated-designer`: `Viewers\BayesianPerformanceViewer.Designer.cs`, `Viewers\EfcViewer.Designer.cs`, `Viewers\ItemViewer.Designer.cs`, `Viewers\ItemViewerExpanded.Designer.cs`, `Viewers\QfcFormViewer.Designer.cs`, `Viewers\QfcItemViewerExpanded.Designer.cs`, `Viewers\ToolStripMenuItemCb.Designer.cs`.
  - Acceptance: 7 rows, each `ratified-exempt` / `generated-designer` with a non-empty rationale citing the WinForms Designer-generated ground in `CLAUDE.md` § UT2 (b). The worksheet notes that `ItemViewerExpanded.Designer.cs` and `BayesianPerformanceViewer.Designer.cs` currently appear in the reference report at line-rates 0.995 and 0.991, so the classification rests on the generated-code ground and not on an untestability claim.
- [ ] [P4-T2] Classify the 2 generated `Properties/` files that remain `ratified-exempt` / `generated-designer`: `Properties\Resources.Designer.cs` and `Properties\Settings.Designer.cs`. `Properties\AssemblyInfo.cs` is **not** in this set; it is classified in `[P4-T4]`.
  - Acceptance: exactly 2 rows with non-empty rationales. The rationale for `Resources.Designer.cs` records the `DebuggerNonUserCodeAttribute` at line 23 that causes its absence from the report and states explicitly that this file **does not** move to the third bucket, because its absence is caused by an attribute on generated code rather than by an absence of executable IL (research §4.7); its ground stays `generated-designer` and its classification stays `ratified-exempt`. The rationale for `Settings.Designer.cs` records that it is present at `line-rate="0"` and is exempt on the generated ground rather than on absence.
- [ ] [P4-T3] Classify the 23 interface-only declarations as `classification: interface-only` / `exempt_ground: interface-only`: `Controllers\IQfcFormController.cs`, `Controllers\IQfcHomeController.cs`, `Controllers\IQfcQueue.cs`, `Controllers\IQfcQueue1.cs`; the 13 `Interfaces\I*.cs` files (`IEmailMoveMonitor.cs`, `IFilerFormController.cs`, `IFilerHomeController.cs`, `IItemControler.cs`, `IKbdAction.cs`, `IQfcCollectionController.cs`, `IQfcDatamodel.cs`, `IQfcExplorerController.cs`, `IQfcFormController.cs`, `IQfcFormViewer.cs`, `IQfcItemController.cs`, `IQfcKeyboardHandler.cs`, `IMailItemActions.cs`); `Helper Classes\IConversationResolver.cs`; and `Viewers\IItemViewer.cs`, `Viewers\IBreadcrumbDropDownHost.cs`, `Viewers\IBreadcrumbWebHost.cs`, `Viewers\IWebViewCoreInitializer.cs`, `Viewers\IWebViewMessenger.cs`. **These rows are no longer `ratified-exempt`.**
  - Acceptance: exactly 23 rows, each with `classification: interface-only` and `exempt_ground: interface-only`, and a rationale citing the type-only/interface-only clause in `.claude/rules/general-unit-test.md` and stating that the file has no coverage denominator, is reported `N/A` rather than `0%`, carries no `[ExcludeFromCodeCoverage]` attribute, and may not be given a shape-assertion test written to manufacture coverage. `Interfaces\MailItemActionsAdapter.cs` is **not** in this set. The rationales for `Viewers\IBreadcrumbDropDownHost.cs` and `Interfaces\IQfcDatamodel.cs` record that each also declares an enum, which produces no executable lines. Every row in this set carries an empty `attribute_dispositions` set, confirmed against `[P2-T17]` and `[P3-T9]`.
- [ ] [P4-T4] Classify the 3 remaining zero-executable-line files as `classification: interface-only` / `exempt_ground: interface-only`: `Helper Classes\QfEnums.cs` (16 lines, enum-only), `Helper Classes\cInfoMail.cs` (231 lines, whole namespace commented out from line 13, only eight live `using` directives), and `Properties\AssemblyInfo.cs` (assembly attributes only).
  - Acceptance: exactly 3 rows, each `interface-only` / `interface-only`, with a rationale stating the verified structural fact from research §4.7 and the same third-bucket sub-rules recorded in `[P4-T3]`. None may be classified `testable`: all three are verified absent from the reference report with no instrumentable code, so a `testable` classification would produce a permanent `NO DATA` failure that no test could ever clear. None may be classified `ratified-exempt` either, because that classification asserts untestable production logic argued away against the irreducible-remainder standard and these files have no such logic. Both constraints are recorded in the worksheet. With `[P4-T3]` this brings the third bucket to exactly **26** files.
- [ ] [P4-T5] Classify the 20 files governed by a type-level disposition that are not already classified in `[P4-T1]`: the 8 non-partial types (`Controllers\EfcFormController.cs`, `Controllers\EfcItemController.cs`, `Controllers\KeyboardHandler.cs`, `Controllers\QfcCollectionController.cs`, `Controllers\QfcExplorerController.cs`, `Viewers\WebView2BreadcrumbHost.cs`, `Viewers\WebView2CoreInitializer.cs`, `Viewers\WebView2Messenger.cs`), the 3 `QfcDatamodel` partials, the 6 non-designer `ItemViewer` partials, `Viewers\EfcViewer.cs`, `Viewers\QfcFormViewer.cs`, `Viewers\QfcItemViewerExpanded.cs`.
  - Acceptance: 20 rows. Each row's classification is consistent with the Phase 2 disposition for its type: a `remove` disposition yields `testable` with `exempt_ground: null`; a `ratified` disposition yields `ratified-exempt` / `irreducible-host-wiring` with a rationale naming the specific host dependency that no interface seam, injectable delegate, or adapter can isolate. No row in this set may be classified `interface-only`, because each carries or inherits an `[ExcludeFromCodeCoverage]` attribute and a third-bucket file receives none. Inconsistency between a Phase 2 disposition and a Phase 4 classification is a Blocking finding recorded in the worksheet.
- [ ] [P4-T6] Classify the 40 remaining unclassified files under `QuickFiler/Controllers/`.
  - Acceptance: exactly 40 rows. Default classification is `testable` with `exempt_ground: null` and `rationale: null`. Any row classified `ratified-exempt` uses `irreducible-host-wiring` and names the specific host dependency, and the worksheet records why the seam hierarchy (interface seam, then injectable delegate, then adapter) cannot reach it. No row in this set may be classified `interface-only`, because every file in it is verified present in the reference report with executable lines. The 6 member-attribute-bearing `QfcItemController.*` partials and `Controllers\QfcHighConfidencePreFilter.cs` are in this set and are `testable` unless a specific irreducible-remainder rationale is recorded.
- [ ] [P4-T7] Classify the 11 remaining unclassified files under `QuickFiler/Helper Classes/` and `QuickFiler/Interfaces/`: the 10 measured `Helper Classes` files (`ConversationResolver.cs`, `ConversationResolver.Loading.cs`, `EfcThemeHelper.cs`, `EfcViewerQueue.cs`, `EmailMoveMonitor.cs`, `ItemViewerQueue.cs`, `QfcThemeControlSet.cs`, `QfcThemeHelper.cs`, `TlpCellSnapShot.cs`, `ViewerQueueCore.cs`) plus `Interfaces\MailItemActionsAdapter.cs`.
  - Acceptance: exactly 11 rows, same standard as `[P4-T6]`.
- [ ] [P4-T8] Classify the 15 remaining unclassified files under `QuickFiler/Viewers/`: the 12 compiled `Viewers\Breadcrumb*` files (`BreadcrumbBridgeCoordinator.cs`, `BreadcrumbCollapsedSurfaceController.cs`, `BreadcrumbCoordinatorUpgradeLifetime.cs`, `BreadcrumbDropDownHost.cs`, `BreadcrumbDropDownOpenCoordinator.cs`, `BreadcrumbDropDownOpenLifetime.cs`, `BreadcrumbItemViewerLifecycleCoordinator.cs`, `BreadcrumbMessengerHub.cs`, `BreadcrumbPopupPlacement.cs`, `BreadcrumbPopupUiOperations.cs`, `BreadcrumbUiDispatcher.cs`, `BreadcrumbWebViewSurfaceFactory.cs`), plus `Viewers\BayesianPerformanceViewer.cs`, `Viewers\ItemViewerExpanded.cs`, and `Viewers\ToolStripMenuItemCb.cs`.
  - Acceptance: exactly 15 rows, same standard as `[P4-T6]`. `Viewers\BreadcrumbPopupUiOperations.cs` is in this set and carries 7 member-level dispositions from `[P3-T7]`; its file-level classification and its member-level dispositions are recorded as independent facts. `Viewers\BayesianPerformanceViewer.cs` (F15) is in this set; only its `.Designer.cs` sibling is classified in `[P4-T1]`. `Controllers\BreadcrumbBridgeRouter.cs` and `Controllers\BreadcrumbOutboundQueue.cs` are Breadcrumb-named but live under `Controllers/` and belong to `[P4-T6]`; they are not in this set.
- [ ] [P4-T9] Verify classification completeness and validity across the worksheet: every inventory row from `[P1-T1]` has exactly one classification; the group sizes sum without overlap as **7 + 2 + 23 + 3 + 20 + 40 + 11 + 15 = 121** at the authoring-time snapshot, and equal the `[P0-T10]` evaluation-time count if it differs; every `classification` value is in `{testable, ratified-exempt, interface-only}`; every non-`testable` row carries an `exempt_ground` from the closed three-value enum and a non-empty `rationale`; every `testable` row carries `exempt_ground: null` and `rationale: null`; the ground-to-classification mapping in this phase's preamble holds in **both directions** for every row; every row carries `line_target_percent: null`; and no row invents a fourth ground.
  - Acceptance: all eight checks pass and are recorded, with the eight group sizes listed individually, their sum stated, the partition confirmed disjoint (no path appears in two groups) and exhaustive (no inventory path is unclassified). The third-bucket total is recorded as exactly 26 = 23 (`[P4-T3]`) + 3 (`[P4-T4]`). Any failure is corrected before the phase closes.
- [ ] [P4-T10] Verify the zero-executable-line invariant against `[P1-T10]`: of the 27 compiled files absent from the reference report for a reason other than a type-level attribute, exactly **26** — the 23 interface-only declarations plus `QfEnums.cs`, `cInfoMail.cs`, and `AssemblyInfo.cs` — are classified `interface-only`, and exactly **1**, `Properties\Resources.Designer.cs`, is classified `ratified-exempt` / `generated-designer`.
  - Acceptance: the 26/1 split is confirmed file by file and recorded. The worksheet cross-references the absence rule authored as lettered item (h) of the ledger's "Classification rules" section by `[P6-T3]`: absence from a coverage report is ambiguous, so the 24 attribute-suppressed files are excluded from this set for the specific reason that a type-level `[ExcludeFromCodeCoverage]` on any one partial suppresses instrumentation for every partial of that type, and none of them is a third-bucket file. Any of the 26 classified `testable` is a Blocking finding, because it would produce a permanent `NO DATA` failure for its owning child. Any of the 26 classified `ratified-exempt` is a Blocking finding, because that classification asserts untestable production logic the file does not contain. `Properties\Resources.Designer.cs` classified `interface-only` is a Blocking finding, because its absence is attribute-caused rather than IL-caused. The worksheet records the finding and the correction in each case.

### Phase 5 — Author the Authoritative Ledger JSON

- [ ] [P5-T1] Create `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.json` with the header fields `schema_version: 1`, `generated_from: "QuickFiler/QuickFiler.csproj"`, `source_commit` set to the short HEAD SHA recorded in `[P0-T3]`, `package: "QuickFiler"`, `threshold_percent: 80.0`, `branch_threshold_percent: 75.0`, `new_file_line_target_percent: 90.0`, and an empty `files` array.
  - Acceptance: the file exists, parses with `ConvertFrom-Json`, and carries all seven header fields with the stated values. `branch_threshold_percent` and `new_file_line_target_percent` are new keys required by `spec.md` v0.3 and are numeric, not strings. `schema_version` remains `1` per Decisions Record item 10: the three added keys are additive and no consumer of the ledger exists yet, so no reader can be broken. `source_commit` is the executor's own recorded HEAD short SHA, not a literal transcription of the research artifact's value.
- [ ] [P5-T2] Populate `files[]` with one entry per row of the `[P4-T9]`-verified worksheet, each carrying `path` (repo-relative, backslash separators, JSON-escaped as `\\`), `lines` (integer, from `[P1-T2]`), `owning_child` (from `[P1-T3]`), `classification`, `line_target_percent`, `exempt_ground`, `rationale`, and an `attribute_dispositions` array, ordinal-sorted by `path`.
  - Acceptance: `files.Count` equals the `[P0-T10]` evaluation-time count, which is 121 at the authoring-time snapshot; every entry carries all **eight** keys, `line_target_percent` being the key added by `spec.md` v0.3; every authoring-time entry carries `line_target_percent: null`; `path` values are ordinal-sorted ascending; both `QuickFiler\\Controllers\\IQfcFormController.cs` and `QuickFiler\\Interfaces\\IQfcFormController.cs` are present as distinct entries; `QuickFiler\\Interfaces\\IQfcHomeController.cs` is absent.
- [ ] [P5-T3] Populate the 14 `kind: type` entries into the `attribute_dispositions` array of their owning file entries, each carrying `kind`, `site`, `target`, `disposition`, `owning_child`, and `rationale`, transcribed from the `[P2-T17]`-verified artifact.
  - Acceptance: 14 entries across 14 distinct file entries; every `site` matches the pattern `<path>:<line>`; every `disposition` is in `{ratified, remove}`; every `remove` rationale contains the tests-first / attribute-removal-last sequencing instruction; no entry lands on a row whose `classification` is `interface-only`.
- [ ] [P5-T4] Populate the 26 `kind: member` entries into the `attribute_dispositions` arrays of their 7 owning file entries, transcribed from the `[P3-T9]`-verified artifact.
  - Acceptance: 26 entries distributed 7 / 5 / 3 / 2 / 1 / 1 / 7 across the seven files named in `[P1-T6]`; every entry carries all six keys; every `site` matches `<path>:<line>`; no entry lands on a row whose `classification` is `interface-only`.
- [ ] [P5-T5] Populate the 11 `kind: inherited` entries into the `attribute_dispositions` arrays of the 11 suppressed partial file entries, each carrying `kind: inherited` and `from: "<path>:<line>"` pointing at the responsible type-level site.
  - Acceptance: 11 entries; every `from` value equals one of the 14 `site` values recorded in `[P5-T3]`; no inherited entry carries a `disposition` key; no entry lands on a row whose `classification` is `interface-only`.
- [ ] [P5-T6] Verify the ledger JSON structural invariants by parsing it with `ConvertFrom-Json`: `files.Count` equals the `[P0-T10]` evaluation-time count and is recorded alongside the authoring-time snapshot of 121; non-inherited disposition total is exactly 40, comprising 14 `type` and 26 `member`, across exactly 21 distinct file paths; `files[]` is ordinal-sorted by `path`; every ledger `path` maps to exactly one `<Compile Include>` entry in `QuickFiler/QuickFiler.csproj` and every `<Compile Include>` entry maps to exactly one ledger `path`. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/ledger-json-invariants.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records each of the four invariant checks with its numeric result, and records the count as a **snapshot observation** rather than as a constant, per Decisions Record item 6. The bijection check is the gate; a count that differs from 121 with the bijection intact is not a failure. Any bijection failure halts the phase.
- [ ] [P5-T7] Verify the ledger JSON value domains and the ground-to-classification mapping: every `classification` is in `{testable, ratified-exempt, interface-only}`; every `exempt_ground` is `null` or in `{generated-designer, interface-only, irreducible-host-wiring}`; `generated-designer` and `irreducible-host-wiring` each imply `classification: ratified-exempt` and every `ratified-exempt` entry carries one of those two grounds; `exempt_ground: interface-only` implies `classification: interface-only` and every `interface-only` entry carries that ground; `exempt_ground: null` implies `classification: testable` and every `testable` entry carries `exempt_ground: null`; every non-`testable` entry has a non-empty `rationale`; no entry with `classification: interface-only` carries a non-empty `attribute_dispositions` array; every `line_target_percent` is `null` or a number in `[0, 100]` and is non-null only on a `testable` entry; every entry has a non-empty `owning_child`. Append the results to `ledger-json-invariants.<ts>.md`.
  - Acceptance: all nine checks recorded with numeric results, including the count of `interface-only` entries (expected 26 at the authoring-time snapshot) and the count of non-null `line_target_percent` values (expected 0). The string `33` does not appear anywhere in `coverage-ledger.json` as an attribute count.

### Phase 6 — Author the Human-Readable Ledger Markdown

- [ ] [P6-T1] Create `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.md` with a header section stating: that `coverage-ledger.json` is authoritative and this document is the human view regenerated from it at authoring time; the line-counting method (physical line count including a final line that lacks a trailing newline, per D14); `source_commit`; `package: QuickFiler`; `threshold_percent: 80.0`; `branch_threshold_percent: 75.0`; `new_file_line_target_percent: 90.0`; the closed `exempt_ground` enum; and the three-value `classification` enum with the note that `interface-only` is rendered in this document as `interface-only / not-measured`.
  - Acceptance: all nine header facts present; the header states that the gate never parses this Markdown at run time, and states that the per-file line figure (>= 80%) and the per-file branch figure (>= 75%) are two independent gates per the manifest's `## Coverage-Target Reconciliation`.
- [ ] [P6-T2] Add the classification table to `coverage-ledger.md` with columns File path, Lines, Owning child, Classification, Line target, Exempt ground, Rationale, in the same ordinal `path` order as the JSON.
  - Acceptance: the data-row count equals the JSON `files` length; every row's classification and exempt ground match the corresponding JSON entry; the Classification column renders all **three** values, with `interface-only` rendered as the literal `interface-only / not-measured` and appearing on exactly the 26 third-bucket rows; the Line target column renders the row's `line_target_percent` or a literal dash when it is `null`; rationale prose containing a `|` character is escaped so the table parses.
- [ ] [P6-T3] Add a **"Classification rules"** section to `coverage-ledger.md`. This is new authored content required by the manifest's `## Mid-Wave File Creation and the Ledger Denominator` rule 2 — the ledger carries rules, not just rows — so that a file which did not exist at authoring time can be classified by its creating child without re-running F1. The section must state, in the ledger's own words: (a) the three permitted grounds `generated-designer`, `irreducible-host-wiring`, `interface-only` with their meanings; (b) the ground-to-classification mapping in both directions, including that `interface-only` no longer yields `ratified-exempt`; (c) the third-bucket sub-rules — a third-bucket file receives no `[ExcludeFromCodeCoverage]` attribute, is reported `N/A` and never `0%` for both the line and the branch figure, never counts as a failure, and **may not be given shape-assertion tests written purely to manufacture coverage for it**, which is a Blocking finding because the file has no denominator to move; (d) the sibling append rule — a child that adds a production file appends its ledger row **in the same change** that adds the `<Compile Include>` entry to `QuickFiler/QuickFiler.csproj`, and the ledger, like the csproj, is an additive shared file whose fan-in conflicts are resolved by keeping both sides; (e) the new-file default — a newly created file defaults to `classification: testable` with `line_target_percent: 90.0` per `CLAUDE.md` § UT2's new-module target, and claiming `ratified-exempt` for a new file requires a rationale meeting one of the two `ratified-exempt` grounds; (f) the csproj editing rules a sibling must follow, stated for **both** shared csproj files — `QuickFiler/QuickFiler.csproj` and `QuickFiler.Test/QuickFiler.Test.csproj`, the latter per the manifest's `### 1b. QuickFiler.Test.csproj is the larger shared-file surface`, which records it as a non-SDK project with **107 explicit `<Compile Include>` entries and no globbing** and as the larger conflict surface of the two, because every child in this epic adds C# test files and so must edit it whereas only the subset that creates production files touches `QuickFiler.csproj` — the rules being minimal adjacent hunks, `<Compile Include>` entries only for files that child owns, no property, reference, or ordering changes, and **CRLF preserved**, because a git-bash `sed -i` strips it and produces a whole-file diff guaranteed to conflict, so the Edit tool or `perl -0777` with explicit `\r\n` must be used, and the section states that neither csproj is owned by any child, that both are shared infrastructure the epic edits by necessity, and that fan-in conflicts on either are additive and resolved by keeping both sides; (g) the F16 reconciliation — the capstone re-derives the denominator from the csproj at evaluation time and fails if any compiled file lacks a ledger row; and (h) the absence rule — **absence from a coverage report is ambiguous and never by itself establishes either zero coverage or third-bucket membership**, so before classifying an absent file as `interface-only / not-measured` the classifier must confirm that no other partial of the same type carries `[ExcludeFromCodeCoverage]`, because an attribute on any one partial suppresses instrumentation for **every** partial of that type; the two verified instances are `QuickFiler\Controllers\QfcDatamodel.cs:25`, whose type-level attribute suppresses all three `QfcDatamodel` partials so that `QfcDatamodel.QueueProcessing.cs` and `QfcDatamodel.FrameBuilding.cs` read as zero measured coverage despite carrying no attribute of their own, and `QuickFiler\Viewers\ItemViewer.cs`, whose single attribute suppresses six files; and a partial type may be annotated only once, because annotating two parts is CS0579, so the absence of an attribute in the file being classified is not evidence that the type is unsuppressed.
  - Acceptance: all eight lettered items are present as explicit prose in the section, each traceable to the manifest clause it implements; the section states that the denominator is the set of `<Compile Include=...>` entries at evaluation time and that 121 is the authoring-time snapshot and not a frozen list; item (h) names both verified instances and the CS0579 single-annotation fact, and states that an attribute-suppressed partial is never a third-bucket file. The section is authored content, not a table regenerated from the JSON.
- [ ] [P6-T4] Add the "Reconciliation with epic manifest" section to `coverage-ledger.md` recording: the verified figures 40 attribute usages / 21 compiled files / 14 type-level / 26 member-level / 24 fully-suppressed files; that the epic manifest's 33 is a count of files containing the string across the whole `QuickFiler/` tree and decomposes without residual as 21 + 5 + 7; that the manifest's `[X]` markers are advisory only, naming the refuted marker on `QfcScanProgressBandMapper.cs`, the mis-scoped markers on `QfcHighConfidencePreFilter.cs`, the six `QfcItemController.*` partials and `BreadcrumbPopupUiOperations.cs`, and the under-marked `ItemViewer.Breadcrumb.cs`, `QfcDatamodel.FrameBuilding.cs`, `QfcDatamodel.QueueProcessing.cs`; the three designer line-count corrections; the 5 comment-only mentions; and the 7 attribute-carrying non-compiled files, which are recorded here only and never as ledger rows. Cite `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/research/2026-08-07T22-15-quickfiler-coverage-ledger-research.md`.
  - Acceptance: every listed item present; the figure 33 appears only inside the decomposition explanation and never as a target.
- [ ] [P6-T5] Add three attribute-disposition tables to `coverage-ledger.md` — type-level (14 rows), member-level (26 rows), inherited (11 rows) — each row carrying site, target declaration, disposition, owning child, and rationale, with the inherited table carrying `from` instead of a disposition.
  - Acceptance: row counts are 14, 26, and 11; every `remove` row in the first two tables displays the tests-first / attribute-removal-last instruction; every type-level `remove` row names every partial file it affects; the inherited table is introduced with a sentence stating those rows are informational and excluded from the 40; the section states that no `interface-only` row appears in any of the three tables, because a declaration-only file receives no attribute.
- [ ] [P6-T6] Add a "Known pre-existing conditions, out of scope" section to `coverage-ledger.md` recording: that the double-counted repository-wide `lines-valid` figure produced by `Get-CoberturaCoverageSummary` in `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1` is a verified pre-existing defect, is out of scope for this feature, and is **tracked at issue #441** (D5); that this feature changes no repository-wide coverage threshold, no `coverage.config` entry, and no `*.runsettings` file; and the **latent-defect-promotion mechanism deviation** — the manifest's `## Latent Defect Promotion` directs promotion through the MCP promotion lifecycle, whereas issue #441 was created directly with `gh issue create`; the directive's purpose is met because a persistent GitHub issue exists outside the feature folder and survives archival, and **no second issue was created** because that would produce a duplicate of #441.
  - Acceptance: the section states the reason for non-remediation (changing the repository-wide figure would perturb every existing gate and every committed evidence baseline), states that the new harness does not reproduce the defect in its own per-file computation, cites issue #441 by number, and records the mechanism deviation explicitly so a reviewer does not read it as a missing promotion. No task in this plan creates a GitHub issue.
- [ ] [P6-T7] Verify the Markdown-to-JSON agreement: the classification table's data-row count equals `files.Count`, and each row's File path, Lines, Owning child, Classification, Line target, and Exempt ground match the corresponding JSON entry, with the Markdown's `interface-only / not-measured` rendering mapping to the JSON's `interface-only` value. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/ledger-md-json-agreement.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the row count and the number of mismatched fields (expected 0), and records the count of rows rendering `interface-only / not-measured` (expected 26 at the authoring-time snapshot). Any mismatch is corrected before the phase closes.

### Phase 7 — Pure-Logic Harness `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`

- [ ] [P7-T1] Create `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` containing `Set-StrictMode -Version Latest` and function definitions only, with no top-level side effects, no `param(...)` block, no `Write-Host`, and no console output of any kind, matching the structure of `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1`.
  - Acceptance: the file dot-sources cleanly into a fresh session without producing output or performing I/O; PowerShell 7+ compatible.
- [ ] [P7-T2] Implement `ConvertTo-CoveragePathKey` in `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`: an advanced function with `[CmdletBinding()]`, `[OutputType([string])]`, comment-based help, and a mandatory `[string]$Path` parameter, which trims whitespace and replaces every `/` with `\`, returning the normalised key (D11).
  - Acceptance: the function is pure, returns a string, performs no I/O, and handles a path containing embedded spaces without any quoting transformation.
- [ ] [P7-T3] Implement `Get-CoberturaConditionCoverageParts` in `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`: `[CmdletBinding()]`, `[OutputType([pscustomobject])]`, taking a single `[string]$ConditionCoverage` parameter carrying a raw `condition-coverage` attribute value such as `"50% (1/2)"`, extracting the fraction with the regex `\(([0-9]+)/([0-9]+)\)`, and returning `[pscustomobject] @{ Covered = <int>; Total = <int> }` on a match or `$null` when the value is null, empty, or does not match.
  - Acceptance: the function is pure, performs no I/O, emits no console output, never throws on an unmatched or empty input, and returns `$null` rather than a zero-valued object so that a caller can distinguish "no fraction present" from "0 of 0". The regex is byte-identical to the one at `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1:146`-`:165`.
- [ ] [P7-T4] Add the comment-based help block to `Get-CoberturaConditionCoverageParts` recording the deliberate-duplication rationale from Decisions Record item 9: the `.DESCRIPTION` states that this function duplicates the fraction regex in `Get-CoberturaLineConditionCoverageParts` at `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1:146`-`:165` **on purpose**, for two stated reasons — the pure-logic file must remain dot-sourceable with no cross-file load-order dependency, and `Invoke-MSTestWithCoverage.Helpers.ps1` is a read-only dependency this feature must not modify — and includes `.PARAMETER`, `.OUTPUTS`, and at least one `.EXAMPLE`.
  - Acceptance: the help block is present and includes `.SYNOPSIS`, `.DESCRIPTION` carrying both stated reasons and the exact source reference, `.PARAMETER`, `.OUTPUTS`, and `.EXAMPLE`; `Get-Help Get-CoberturaConditionCoverageParts -Full` renders it after dot-sourcing. The comment explains **why**, not what, per `.claude/rules/general-code-change.md`.
- [ ] [P7-T5] Implement `Merge-CoberturaFileLines` in `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`: `[CmdletBinding()]`, `[OutputType([pscustomobject])]`, comment-based help, taking the `<class>` nodes that share one `filename` and returning a deduplicated line set keyed by integer line number. On a line-number collision it takes `max(hits)` (D4) **and retains the richer `condition-coverage`** — the value with the larger denominator, and on equal denominators the value with the larger numerator — mirroring `Merge-CoberturaClassesByFilename` at `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1:240`-`:261`. Denominator and numerator are obtained by calling `Get-CoberturaConditionCoverageParts`. It reads only the direct-child `./lines/line` axis of each `<class>` node and never the `.//lines/line` descendant axis.
  - Acceptance: the function never evaluates a `line-rate` attribute; each returned line entry carries its line number, its retained `hits`, its `branch` value, and its retained `condition-coverage` string (or `$null`); on collision the retained `condition-coverage` is the one with the larger denominator, and on equal denominators the one with the larger numerator, with the incoming value discarded when neither is richer; a class node with no direct-child `<line>` returns a zero-length line set rather than throwing; the function performs no I/O and emits no console output.
- [ ] [P7-T6] Implement `Get-CoberturaPerFileCoverage` in `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`: `[CmdletBinding()]`, `[OutputType([pscustomobject])]`, comment-based help, mandatory `[xml]$CoberturaDocument` and `[string]$PackageName` (default `QuickFiler`), returning one row object per distinct `filename` carrying `PathKey`, `LineCovered`, `LineTotal`, `LineRatio`, `BranchCovered`, `BranchTotal`, and `BranchRatio`. It selects `<package>` by exact `name` match (D12), groups `<class>` nodes by normalised `filename`, and delegates line merging to `Merge-CoberturaFileLines`. The branch figures are derived from the merged line set by restricting to entries whose `branch` attribute is the literal `"True"`, parsing each retained `condition-coverage` with `Get-CoberturaConditionCoverageParts`, and computing `BranchCovered = sum(covered)` and `BranchTotal = sum(total)` across those entries; `BranchRatio` is `BranchCovered / BranchTotal` when `BranchTotal -gt 0` and `$null` otherwise. A `branch="True"` entry with no `condition-coverage` contributes to neither sum and the function infers no fraction.
  - Acceptance: the function raises a bare `throw` with the literal message `'Cobertura XML does not contain a <packages> node.'` when `<packages>` is absent, and a bare `throw` with a distinct literal message naming the requested package when the named `<package>` is absent; it never scans packages other than the named one; it never reads a `<class>` `line-rate` attribute; a file present with zero `<line>` nodes yields a row whose `LineTotal` is `0`, whose `LineRatio` is `$null` and never `0`, and whose `BranchTotal` is `0` and `BranchRatio` `$null`; the branch rate is the ratio of summed covered to summed total and is never the average of per-line percentages; the function performs no I/O and emits no console output.
- [ ] [P7-T7] Implement `Test-PerFileCoverageThreshold` in `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`: `[CmdletBinding()]`, `[OutputType([pscustomobject])]`, comment-based help, taking the row objects from `Get-CoberturaPerFileCoverage`, the parsed ledger object, `[double]$ThresholdPercent` (default `80.0`), `[double]$BranchThresholdPercent` (default `75.0`), and `[string[]]$ExemptFile` (default empty), and returning a verdict object carrying every evaluated row with its status, its applied line threshold, its applied branch threshold, and a per-row `FailedGates` list, plus an overall failure list. It implements the complete **nine-case** row-state matrix from `spec.md` "Data & State":

  | Case | Classification | Row status | Line column | Branch column | Exit contribution |
  | --- | --- | --- | --- | --- | --- |
  | Absent from report | `interface-only` | `NOT MEASURED (no executable lines)` | `n/a` | `n/a` | none |
  | Absent from report | `ratified-exempt` | `EXEMPT (not measured)` | `n/a` | `n/a` | none |
  | Absent from report | `testable` | `NO DATA` | `n/a` | `n/a` | failure |
  | Present, zero `<line>` nodes | any | `NO EXECUTABLE LINES` | `n/a` | `n/a` | none |
  | Present with lines, zero `branch="True"` lines | `testable` | percentage, line gate only | percentage | `n/a` | failure iff line below |
  | Present with lines | `ratified-exempt` | percentage, informational | percentage | percentage | none |
  | Present with lines | `testable` | percentage, both gates | percentage | percentage | failure iff either gate below |
  | Present with lines | `interface-only` | `RECLASSIFY` | percentage, informational | percentage, informational | none |
  | Present but not in ledger | — | `UNLEDGERED` | percentage | percentage | failure |

  The per-row applied line threshold is the row's `line_target_percent` when that field is non-null, and `ThresholdPercent` otherwise. The branch threshold is ledger-wide with no per-row override. It raises a bare `throw` with a specific literal message when `schema_version` is not `1` (Decisions Record item 10).
  - Acceptance: all nine cases are implemented with the exact status strings above; `NO EXECUTABLE LINES` applies to **any** classification, not only `testable`; `RECLASSIFY` is informational and contributes **nothing** to the failure list, so the manifest's "a third-bucket file never counts as a failure" holds in both the absent and the present-with-lines direction; each failing row's `FailedGates` list names `line`, `branch`, or both, and a `NO DATA` or `UNLEDGERED` row names its own condition; the line comparison is `-lt ($appliedLineThreshold / 100.0)` and the branch comparison is `-lt ($BranchThresholdPercent / 100.0)`, both against the **unrounded** ratio (D9) and never against a rounded display value; a row whose `BranchTotal` is `0` is never compared against the branch threshold; a path supplied through `-ExemptFile` is treated as exempt for that invocation only and the ledger object is not mutated; the function performs no I/O and emits no console output.
- [ ] [P7-T8] Implement `Format-PerFileCoverageReport` in `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`: `[CmdletBinding()]`, `[OutputType([string])]`, comment-based help, taking the verdict object and returning a single deterministic report string with **two percentage columns** — a line-coverage column and a branch-coverage column — alongside the path, classification, and status columns. Rows are ordered by an ordinal sort on the normalised path using `[StringComparer]::Ordinal`, not the culture-sensitive `Sort-Object` default. Percentages in both columns are rendered with `.ToString('0.0', [System.Globalization.CultureInfo]::InvariantCulture)`. A column with no defined value renders the literal `n/a`, except on an `interface-only` row where both columns render the literal `N/A` per `spec.md` "Data & State" (D10).
  - Acceptance: the returned string contains a distinct, labelled branch column; the `n/a` / `N/A` literals appear in place of any numeric value for the `NOT MEASURED (no executable lines)`, `EXEMPT (not measured)`, `NO DATA`, and `NO EXECUTABLE LINES` statuses and for a `testable` row with zero branch points, and `0.0` appears in none of them; the returned string contains no timestamp, no host name, and no path outside the repository; two calls on the same verdict object return identical strings across both columns; the function performs no I/O and emits no console output.
- [ ] [P7-T9] Verify `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` against the structural constraints: record the file's line count against the 250-380 target from `spec.md` v0.3 and the hard 500-line cap; it contains exactly the six functions `ConvertTo-CoveragePathKey`, `Get-CoberturaConditionCoverageParts`, `Merge-CoberturaFileLines`, `Get-CoberturaPerFileCoverage`, `Test-PerFileCoverageThreshold`, `Format-PerFileCoverageReport`; every function has `[CmdletBinding()]`, an explicit `[OutputType(...)]`, comment-based help, and an approved verb; no function calls `Write-Host`, `Write-Output`, `exit`, `Get-Content`, `Set-Content`, `Test-Path`, or any external executable. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/helpers-structure-check.<ts>.md`.
  - Acceptance: artifact has all four required fields and records the numeric line count and one pass/fail line per constraint. Per Decisions Record item 17, a measured length **above 380 but below 500** is recorded as an observation and is **not** a failure. A length at or above 500 is a Blocking finding recorded in the artifact and escalated for a plan revision that adds a second pure-logic file to the Scope Constraints table; the split is never performed inside execution, because the Scope Constraints table admits no unnamed production file.
- [ ] [P7-T10] Run an interim toolchain pass over the new helper file: `mcp__drm-copilot__run_poshqc_format`, then `mcp__drm-copilot__run_poshqc_analyze` unscoped, then `mcp__drm-copilot__run_poshqc_analyze` with `scan_folders: ["scripts/vscode"]`, all with `workspace_root` set to the executor's own current worktree root; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/interim-toolchain-phase7.<ts>.md`.
  - Acceptance: artifact records `Timestamp:`, one `Command:`/`EXIT_CODE:` pair per stage, and an `Output Summary:` stating whether the formatter modified the file and recording the unscoped and `scripts/vscode`-scoped analyzer counts as numbers. The tools emit no per-file diagnostic list, so attribution to the new helper file is by count delta: both counts must equal their `[P0-T5]` recorded values, which proves the new file added no diagnostic. Any increase is fixed and both stages re-run before the phase closes.

### Phase 8 — Entry Point `scripts/vscode/Get-PerFileCoverage.ps1` and Dead-Script Removal

- [ ] [P8-T1] Create `scripts/vscode/Get-PerFileCoverage.ps1` with a top-of-file `param(...)` block declaring `[Parameter(Mandatory = $true)][string]$CoberturaPath`, `[string]$LedgerJsonPath` defaulting to `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.json` resolved relative to the repository root computed from `$PSScriptRoot`, `[string]$PackageName = 'QuickFiler'`, `[double]$ThresholdPercent = 80.0`, `[double]$BranchThresholdPercent = 75.0`, `[string]$OutputPath`, and `[string[]]$ExemptFile = @()`, followed by `Set-StrictMode -Version Latest` and `$ErrorActionPreference = 'Stop'`.
  - Acceptance: the parameter set matches the "Inputs" table in `spec.md` v0.3 exactly, including the `-BranchThresholdPercent` parameter defaulting to `75.0`; every environment-derived default is an overridable parameter so tests never depend on ambient state; no default resolves through the current working directory.
- [ ] [P8-T2] Dot-source `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` into `scripts/vscode/Get-PerFileCoverage.ps1` using a `$PSScriptRoot`-relative path.
  - Acceptance: the dot-source path contains no absolute path and no dependence on the current working directory; the script resolves its helper correctly when invoked from any directory.
- [ ] [P8-T3] Implement `Invoke-PerFileCoverageGate` in `scripts/vscode/Get-PerFileCoverage.ps1`: `[CmdletBinding()]`, `[OutputType([pscustomobject])]`, comment-based help. It validates that `CoberturaPath` and `LedgerJsonPath` exist, loads the Cobertura document as `[xml]`, parses the ledger with `ConvertFrom-Json`, calls `Get-CoberturaPerFileCoverage`, `Test-PerFileCoverageThreshold` (passing both `ThresholdPercent` and `BranchThresholdPercent`), and `Format-PerFileCoverageReport`, writes the report to `OutputPath` when supplied, emits one `Write-Error` diagnostic per failing row naming the file and the gate or gates that failed, and returns `[pscustomobject] @{ ExitCode = <int>; ReportText = <string> }` per Decisions Record item 7. When neither threshold parameter is supplied it falls back to the ledger's `threshold_percent` and `branch_threshold_percent` header values.
  - Acceptance: `ExitCode` is `0` when every `testable` row is at or above both its applied line threshold and the branch threshold; `1` when any `testable` row is **below its line threshold or below the branch threshold**, or when any row is `NO DATA` or `UNLEDGERED`; `2` for every input error — missing file, XML not well-formed, missing `<packages>` node, missing `QuickFiler` package, missing or malformed ledger JSON, unrecognised `schema_version`. A `RECLASSIFY` row and a `NOT MEASURED (no executable lines)` row contribute nothing to the exit code. The function never calls `exit`. No `Write-Host` appears anywhere in the file.
- [ ] [P8-T4] Add the dot-source-safe entry guard at the end of `scripts/vscode/Get-PerFileCoverage.ps1`, matching the pattern at `scripts/vscode/Invoke-MSTestWithCoverage.ps1:346`-`348`: when `$MyInvocation.InvocationName -ne '.'`, call `Invoke-PerFileCoverageGate @PSBoundParameters`, write the returned `ReportText` via `Write-Output`, and `exit` with the returned `ExitCode`.
  - Acceptance: dot-sourcing the script defines the function without executing the gate; invoking the script executes the gate and terminates with the returned code.
- [ ] [P8-T5] Delete `scripts/temp-extract-coverage.ps1` (D13) after confirming with a repository-wide search that no live code, VS Code task, workflow, or script references it, and that the only remaining references are in research and archived planning documents, using `Get-ChildItem -Path . -Recurse -File | Where-Object { $_.FullName -notmatch '[\\/]\.git[\\/]' } | Select-String -Pattern 'temp-extract-coverage' -List`, with the exact command recorded in `Command:`. `Select-String` has no `-Recurse` parameter, so the recursion is performed by `Get-ChildItem` and the `.git/` exclusion by the pipeline filter. Preflight has independently verified the current match set: agent-memory notes, this plan, the feature research document, `spec.md`, and two archived planning documents under `docs/features/archive/2026-07-10-swordfish-scosorteddictionary-removal-309/`; no `.vscode/tasks.json`, workflow, hook, or script reference exists. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/temp-script-deletion.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the search command, the full list of matches with their file paths, a statement that none is a live invocation, and confirmation that the file no longer exists. If a live reference is found, the deletion is not performed and the finding is escalated instead.
- [ ] [P8-T6] Run the harness end to end against the committed reference report: invoke `scripts/vscode/Get-PerFileCoverage.ps1` with `-CoberturaPath docs/features/active/2026-08-06-quickfiler-high-confidence-queue-init-stall-424/evidence/qa-gates/coverage-final.cobertura.xml` and `-OutputPath docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/smoke-per-file-report.<ts>.txt`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/harness-smoke-run.<ts>.md`.
  - Acceptance: artifact has all four required fields. `EXIT_CODE:` is recorded verbatim; a value of `1` is the **expected** outcome because wave-1 coverage work has not happened, and the artifact states that expectation explicitly. `Output Summary:` records the count of rows in each of the nine status categories (`OK`, below-line-threshold, below-branch-threshold, `NO DATA`, `UNLEDGERED`, `EXEMPT (not measured)`, `NOT MEASURED (no executable lines)`, `NO EXECUTABLE LINES`, `RECLASSIFY`) and confirms the row total equals the ledger `files.Count` plus any `UNLEDGERED` rows. The `NOT MEASURED (no executable lines)` count is expected to be **26** and the `RECLASSIFY` count is expected to be **0**, because research §4.7 verified that all 26 third-bucket files are absent from this sample report; a non-zero `RECLASSIFY` count is recorded and investigated as a classification defect. A `2` is a genuine failure of the harness or its inputs and must be fixed before the phase closes. An `UNLEDGERED` count greater than zero means the reference report contains a QuickFiler filename absent from the ledger and is investigated as a ledger defect. This task is an observation, not a QC gate.
- [ ] [P8-T7] Verify `scripts/vscode/Get-PerFileCoverage.ps1` against the structural constraints: under 500 lines and within the 100-160 line target from `spec.md`; no `Write-Host`; no hard-coded absolute path; no `Invoke-Expression`; the only I/O is reading the Cobertura and ledger files and optionally writing the report. Append the results to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/helpers-structure-check.<ts>.md`.
  - Acceptance: line count recorded and one pass/fail line per constraint, all passing. A length outside 100-160 but under 500 is recorded as an observation, not a failure.
- [ ] [P8-T8] Run an interim toolchain pass over both new production scripts: `mcp__drm-copilot__run_poshqc_format`, then `mcp__drm-copilot__run_poshqc_analyze` unscoped, then `mcp__drm-copilot__run_poshqc_analyze` with `scan_folders: ["scripts/vscode"]`, all with `workspace_root` set to the executor's own current worktree root; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/interim-toolchain-phase8.<ts>.md`.
  - Acceptance: artifact records `Timestamp:`, one `Command:`/`EXIT_CODE:` pair per stage, and an `Output Summary:` recording the unscoped and `scripts/vscode`-scoped analyzer counts as numbers. The tools emit no per-file diagnostic list, so attribution is by count delta: the `scripts/vscode`-scoped count must equal its `[P0-T5]` recorded value, which proves the two new production scripts added no diagnostic. The unscoped count must be at or below its `[P0-T5]` value; any reduction is attributed to the deletion of `scripts/temp-extract-coverage.ps1` in `[P8-T5]` and that attribution is stated in the artifact. Any increase in either count is fixed and both stages re-run before the phase closes.

### Phase 9 — Pester Suites for the Pure Logic

All fixtures in this phase are in-memory here-strings cast to `[xml]` or parsed with `ConvertFrom-Json`, modelled on the real document shape recorded in research §4.2 including the method-level `<lines>` nesting and the `branch` / `condition-coverage` attributes recorded in research §4.3. No task in this phase creates, writes, or deletes a filesystem path.

Per Decisions Record items 18 and 19 this phase creates **three** test files — at the per-batch cap of three, not over it — each with one clear subject and its own in-file fixture-builder task, so no file depends on another's load order and no function's cases are split across files:

- `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1` — `Merge-CoberturaFileLines` and `Get-CoberturaConditionCoverageParts` (`[P9-T3]` through `[P9-T11]`, 9 `It`).
- `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1` — `Get-CoberturaPerFileCoverage` and `Format-PerFileCoverageReport` (`[P9-T14]` through `[P9-T31]`, 18 `It`).
- `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` — `Test-PerFileCoverageThreshold` (`[P9-T34]` through `[P9-T51]`, 18 `It`).

The entry-function suite is created in Phase 10, in a separate batch. The split is planned, not conditional; no task in this phase may create an unnamed additional file.

- [ ] [P9-T1] Create `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, the line-merging and condition-parsing suite, with `Set-StrictMode -Version Latest` as line 1 and a `BeforeAll` block that resolves the repository root from `$PSScriptRoot` and dot-sources `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`, matching the convention at `tests/scripts/vscode/Invoke-MSTestWithCoverage.Helpers.Tests.ps1:3`-`7`.
  - Acceptance: the file loads under Pester 5.x; the dot-source succeeds; no ambient working-directory assumption exists. The file's scope is `Merge-CoberturaFileLines` and `Get-CoberturaConditionCoverageParts` only.
- [ ] [P9-T2] Add a fixture-builder section to `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1` producing: `<class>` nodes composed from here-strings with both a method-level `<lines>` block and a class-level `<lines>` rollup; line elements carrying `number`, `hits`, `branch="True"` / `branch="False"`, and optional `condition-coverage` values in the `"50% (1/2)"` form; and a pair of colliding class nodes whose shared line numbers carry `condition-coverage` values of differing richness.
  - Acceptance: every fixture is an in-memory here-string; no fixture reads from or writes to disk; the class-level rollup in the composed fixture is a strict superset of the method-level lines, as the real document is. The fixtures are defined inside this file rather than dot-sourced from a sibling suite, so no test file depends on another's load order.
- [ ] [P9-T3] Add the `Merge-CoberturaFileLines` test: duplicate line numbers within one class node are deduplicated and the collision resolves to `max(hits)`.
  - Acceptance: one named `It` asserting the distinct line count and the retained hits value.
- [ ] [P9-T4] Add the `Merge-CoberturaFileLines` test: two `<class>` nodes sharing one `filename` are unioned and deduplicated by line number with `max(hits)`, so unmerged raw `dotnet-coverage` output is handled correctly.
  - Acceptance: one named `It` asserting the unioned distinct line count and the retained hits values.
- [ ] [P9-T5] Add the `Merge-CoberturaFileLines` test: a fixture carrying both a method-level `<lines>` block and a class-level `<lines>` rollup yields the distinct-line set, not the double-counted set.
  - Acceptance: one named `It` whose fixture would produce twice the line count under a `.//lines/line` descendant-axis implementation; the assertion pins the distinct count and would fail if the descendant axis were used.
- [ ] [P9-T6] Add the `Merge-CoberturaFileLines` test: a class node with zero direct-child `<line>` elements returns a zero-length line set without throwing.
  - Acceptance: one named `It` asserting a zero distinct-line count and no exception.
- [ ] [P9-T7] Add the `Merge-CoberturaFileLines` test: on a line-number collision the **richer** `condition-coverage` is retained, where richer means the larger denominator.
  - Acceptance: one named `It` whose fixture presents the same line number with `condition-coverage` values of `"50% (1/2)"` and `"75% (6/8)"`, asserting that the merged entry retains `"75% (6/8)"` regardless of the order in which the two class nodes are supplied, and asserting that the retained `hits` value is still `max(hits)` independently of the `condition-coverage` choice.
- [ ] [P9-T8] Add the `Merge-CoberturaFileLines` test: on a line-number collision with **equal denominators** the value with the larger numerator is retained.
  - Acceptance: one named `It` whose fixture presents the same line number with `"50% (1/2)"` and `"100% (2/2)"`, asserting that the merged entry retains `"100% (2/2)"` regardless of supply order, and asserting that a collision where neither incoming value is richer leaves the existing value unchanged.
- [ ] [P9-T9] Add the `Get-CoberturaConditionCoverageParts` test: a well-formed value parses to the covered/total pair.
  - Acceptance: one named `It` asserting that `"50% (1/2)"` returns an object whose `Covered` is `1` and whose `Total` is `2`, both as integers.
- [ ] [P9-T10] Add the `Get-CoberturaConditionCoverageParts` test: a multi-digit fraction parses correctly.
  - Acceptance: one named `It` asserting that `"75% (6/8)"` returns `Covered 6` / `Total 8` and that `"92% (12/13)"` returns `Covered 12` / `Total 13`, proving the regex is not single-digit-bound.
- [ ] [P9-T11] Add the `Get-CoberturaConditionCoverageParts` test: a value carrying no `(n/m)` fraction returns `$null` without throwing.
  - Acceptance: one named `It` covering an empty string, `$null`, and a non-matching string such as `"100%"`, asserting `$null` is returned in each case and explicitly asserting the result is not a zero-valued object, so a caller can distinguish "no fraction present" from "0 of 0".
- [ ] [P9-T12] Create `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, the row-derivation and report-rendering suite, with `Set-StrictMode -Version Latest` as line 1 and a `BeforeAll` block that resolves the repository root from `$PSScriptRoot` and dot-sources `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`, using the same convention as `[P9-T1]`.
  - Acceptance: the file loads under Pester 5.x; the dot-source succeeds; no ambient working-directory assumption exists. The file is named in the Scope Constraints create-list, so its creation is in scope. Its scope is `Get-CoberturaPerFileCoverage` and `Format-PerFileCoverageReport` only — the row-derivation and row-rendering ends of one pipeline.
- [ ] [P9-T13] Add a fixture-builder section to `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1` producing: minimal well-formed Cobertura documents with a `<package name="QuickFiler">`; a second `<package>` carrying a same-named `filename`; documents with and without a `<packages>` node; `<line>` elements carrying `branch="True"` with and without `condition-coverage`; and in-memory verdict objects for the rendering cases covering a `testable` row with both figures, a `testable` row with no branch points, an `interface-only` row, and each `n/a` status.
  - Acceptance: every fixture is an in-memory here-string or in-memory object; no fixture reads from or writes to disk; the fixtures are defined inside this file rather than dot-sourced from a sibling suite, so no test file depends on another's load order.
- [ ] [P9-T14] Add the `Get-CoberturaPerFileCoverage` test: a document with mixed hit and miss lines yields the correct per-file line figures.
  - Acceptance: one named `It` asserting `LineCovered`, `LineTotal`, and the unrounded `LineRatio` for a file with a known covered/total split.
- [ ] [P9-T15] Add the `Get-CoberturaPerFileCoverage` test: only the named package is scanned; a same-named `filename` inside a second `<package>` is not included in the result.
  - Acceptance: one named `It` whose fixture carries the same `filename` in two packages with different hit counts, asserting that only the `QuickFiler` package's values are returned.
- [ ] [P9-T16] Add the `Get-CoberturaPerFileCoverage` test: a document with no `<packages>` node throws with the literal message `'Cobertura XML does not contain a <packages> node.'`.
  - Acceptance: one named `It` asserting the exact literal message.
- [ ] [P9-T17] Add the `Get-CoberturaPerFileCoverage` test: a document whose `<packages>` node contains no `<package name="QuickFiler">` throws with a distinct literal message naming the requested package.
  - Acceptance: one named `It` asserting the exact literal message and asserting it differs from the message in `[P9-T16]`.
- [ ] [P9-T18] Add the `Get-CoberturaPerFileCoverage` test: the `<class>` `line-rate` attribute is not consulted.
  - Acceptance: one named `It` whose fixture carries a `line-rate` attribute deliberately inconsistent with its `<line>` nodes; the assertion pins the value computed from the `<line>` nodes and would fail if the attribute were read.
- [ ] [P9-T19] Add the `Get-CoberturaPerFileCoverage` test: a `filename` using forward-slash separators is normalised to backslash and matches the ledger key.
  - Acceptance: one named `It` whose fixture uses `QuickFiler/Controllers/QfcQueue.cs` and asserts the returned `PathKey` is `QuickFiler\Controllers\QfcQueue.cs`.
- [ ] [P9-T20] Add the `Get-CoberturaPerFileCoverage` test: a `filename` containing an embedded space is matched correctly.
  - Acceptance: one named `It` using `QuickFiler\Helper Classes\EfcViewerQueue.cs` and asserting the row is returned with that exact `PathKey` and no quoting artifact.
- [ ] [P9-T21] Add the `Get-CoberturaPerFileCoverage` test: a file present in the report with zero `<line>` nodes yields a row whose `LineRatio` is `$null`.
  - Acceptance: one named `It` asserting `LineTotal -eq 0`, `LineRatio` is `$null`, and explicitly asserting `LineRatio` is not `0`.
- [ ] [P9-T22] Add the `Get-CoberturaPerFileCoverage` test: the branch rate is derived as `sum(covered) / sum(total)` across the file's deduplicated `<line branch="True">` `condition-coverage` fractions, **not** as the average of the per-line percentages.
  - Acceptance: one named `It` whose fixture carries exactly two branch lines with `condition-coverage` values `"50% (1/2)"` and `"75% (6/8)"`, asserting `BranchCovered -eq 7`, `BranchTotal -eq 10`, and `BranchRatio` equal to `0.70` within floating-point tolerance, and explicitly asserting `BranchRatio` is **not** `0.625`, which is the value a per-line average would produce. This is the seeded condition that pins the derivation semantics.
- [ ] [P9-T23] Add the `Get-CoberturaPerFileCoverage` test: a `branch="True"` element carrying no `condition-coverage` attribute contributes to neither branch sum and no fraction is inferred.
  - Acceptance: one named `It` whose fixture carries one branch line with `"50% (1/2)"` and one `branch="True"` line with no `condition-coverage`, asserting `BranchCovered -eq 1` and `BranchTotal -eq 2`, and asserting that the totals are unchanged from an otherwise-identical fixture that omits the second element entirely.
- [ ] [P9-T24] Add the `Get-CoberturaPerFileCoverage` test: a `<class>` whose `line-rate` attribute is the literal `"0"` but which has zero `<line>` children yields a row with no denominator, proving the denominator is decided on `<line>` child count and never on `line-rate`.
  - Acceptance: one named `It` asserting `LineTotal -eq 0`, `BranchTotal -eq 0`, `LineRatio` is `$null`, and `BranchRatio` is `$null`, with explicit assertions that neither ratio is `0`. The corresponding `NO EXECUTABLE LINES` row status for this row shape is asserted in the verdict suite by `[P9-T42]`.
- [ ] [P9-T25] Add the `Format-PerFileCoverageReport` test: two calls on identical rows produce byte-identical strings across **both** the line-coverage and the branch-coverage column.
  - Acceptance: one named `It` asserting string equality between two independent invocations on a fixture that exercises both a numeric line value and a numeric branch value, and asserting that the rendered text of each column is identical between the two invocations.
- [ ] [P9-T26] Add the `Format-PerFileCoverageReport` test: rows are ordered by an ordinal sort on the path.
  - Acceptance: one named `It` whose fixture contains paths that a culture-sensitive sort would order differently from an ordinal sort (for example differing only by case or by an underscore versus a letter), asserting the ordinal order in the rendered output.
- [ ] [P9-T27] Add the `Format-PerFileCoverageReport` test: percentages in both columns render with `InvariantCulture` and a `.` decimal separator even when the current thread culture uses a comma.
  - Acceptance: one named `It` that sets `[System.Threading.Thread]::CurrentThread.CurrentCulture` to a comma-decimal culture and restores the original value in a `finally` block, asserting that both the line and the branch rendered percentage contain `.` and not `,`. The restore is unconditional so no test-order dependency is introduced.
- [ ] [P9-T28] Add the `Format-PerFileCoverageReport` test: the report body contains no timestamp, host name, or path outside the repository.
  - Acceptance: one named `It` asserting the rendered string does not match a date-time pattern, does not contain the machine name, and contains no drive-letter or UNC prefix.
- [ ] [P9-T29] Add the `Format-PerFileCoverageReport` test: the branch figure is rendered as a distinct, labelled column alongside the line figure.
  - Acceptance: one named `It` asserting that the rendered header names both a line-coverage column and a branch-coverage column, and that a row with a line ratio of 1.0 and a branch ratio of 0.6667 renders `100.0` and `66.7` in the respective columns rather than a single combined value.
- [ ] [P9-T30] Add the `Format-PerFileCoverageReport` test: the `n/a` statuses render the literal `n/a` in both columns rather than a numeric value.
  - Acceptance: one named `It` covering `EXEMPT (not measured)`, `NO DATA`, `NO EXECUTABLE LINES`, and a `testable` row with lines but zero branch points in a single rendered report, asserting `n/a` in each affected column and asserting `0.0` appears in none of them.
- [ ] [P9-T31] Add the `Format-PerFileCoverageReport` test: a row classified `interface-only` renders `N/A` in both columns and never `0%` or `0.0`.
  - Acceptance: one named `It` covering a `NOT MEASURED (no executable lines)` row, asserting the literal `N/A` in both the line and the branch column and explicitly asserting that neither `0%` nor `0.0` appears on that row.
- [ ] [P9-T32] Create `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1`, the verdict suite, with `Set-StrictMode -Version Latest` as line 1 and a `BeforeAll` block that resolves the repository root from `$PSScriptRoot` and dot-sources `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`, using the same convention as `[P9-T1]`.
  - Acceptance: the file loads under Pester 5.x; the dot-source succeeds; no ambient working-directory assumption exists. The file is named in the Scope Constraints create-list, so its creation is in scope. Its scope is `Test-PerFileCoverageThreshold` only.
- [ ] [P9-T33] Add a fixture-builder section to `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` producing the in-memory row objects and the parsed ledger object the verdict cases consume, including a ledger here-string with `schema_version: 1`, `threshold_percent: 80.0`, `branch_threshold_percent: 75.0`, `new_file_line_target_percent: 90.0`, and a small `files[]` set covering a `testable` row with `line_target_percent: null`, a `testable` row with `line_target_percent: 90.0`, a `ratified-exempt` row, an `interface-only` row, and a row absent from the report.
  - Acceptance: every fixture is an in-memory here-string or in-memory object; no fixture reads from or writes to disk; the fixtures are defined inside this file rather than dot-sourced from a sibling suite, so no test file depends on another's load order.
- [ ] [P9-T34] Add the `Test-PerFileCoverageThreshold` test: a `testable` file at or above both thresholds produces no failure.
  - Acceptance: one named `It` asserting the row status, an empty `FailedGates` list on the row, and an empty overall failure list.
- [ ] [P9-T35] Add the `Test-PerFileCoverageThreshold` test: a `testable` file at exactly 80.0% line (for example 800/1000) passes the line gate.
  - Acceptance: one named `It` asserting no failure at the inclusive boundary.
- [ ] [P9-T36] Add the `Test-PerFileCoverageThreshold` test: a `testable` file at 7999/10000 displays as 80.0 but fails, proving the line comparison uses the unrounded ratio.
  - Acceptance: one named `It` asserting both that the rendered percentage is `80.0` and that the row is in the failure list.
- [ ] [P9-T37] Add the `Test-PerFileCoverageThreshold` test: a `testable` file clearly below the line threshold fails and its verdict names the line gate.
  - Acceptance: one named `It` asserting the row is in the failure list with its percentage recorded, and that the row's `FailedGates` list contains `line` and does not contain `branch`.
- [ ] [P9-T38] Add the `Test-PerFileCoverageThreshold` test: a `ratified-exempt` file below both thresholds is reported informationally and does not fail.
  - Acceptance: one named `It` asserting both percentages are present on the row and the failure list is empty.
- [ ] [P9-T39] Add the `Test-PerFileCoverageThreshold` test: a file absent from the report and classified `testable` produces a `NO DATA` failure whose message names the file.
  - Acceptance: one named `It` asserting the row status is `NO DATA`, both coverage columns are `n/a`, the row is in the failure list, and the diagnostic text contains the file path.
- [ ] [P9-T40] Add the `Test-PerFileCoverageThreshold` test: a file absent from the report and classified `ratified-exempt` yields `EXEMPT (not measured)` with no failure.
  - Acceptance: one named `It` asserting the row status, `n/a` in both coverage columns, and an empty failure list.
- [ ] [P9-T41] Add the `Test-PerFileCoverageThreshold` test: a file absent from the report and classified `interface-only` yields `NOT MEASURED (no executable lines)`, reports `N/A` for both the line and the branch column, is never rendered as `0%`, and contributes no failure.
  - Acceptance: one named `It` asserting the exact status string `NOT MEASURED (no executable lines)`, asserting both columns carry the `N/A` sentinel and explicitly asserting neither is `0` or `0.0`, and asserting an empty failure list. This is the seeded third-bucket condition.
- [ ] [P9-T42] Add the `Test-PerFileCoverageThreshold` test: a file present with zero `<line>` nodes yields `NO EXECUTABLE LINES` for **any** classification, with `n/a` in both columns, contributes no failure, and is never reported as 0%.
  - Acceptance: one named `It` covering the row shape produced by `[P9-T24]` for a `testable` row, a `ratified-exempt` row, and an `interface-only` row, asserting the status in each case, asserting both columns are `n/a`, explicitly asserting neither is `0.0`, and asserting an empty failure list.
- [ ] [P9-T43] Add the `Test-PerFileCoverageThreshold` test: a file present in the report **with** executable lines but classified `interface-only` yields `RECLASSIFY`, reports both percentages informationally, and contributes nothing to the failure list.
  - Acceptance: one named `It` asserting the exact status string `RECLASSIFY`, asserting both percentages appear on the row, and asserting the overall failure list is empty, which is what makes the manifest's "a third-bucket file never counts as a failure" hold in the present-with-lines direction as well as the absent direction.
- [ ] [P9-T44] Add the `Test-PerFileCoverageThreshold` test: a file present in the report but absent from the ledger yields `UNLEDGERED` and a failure with a distinct message.
  - Acceptance: one named `It` asserting the status, the presence of the row in the failure list, and that the message differs from the `NO DATA` message.
- [ ] [P9-T45] Add the `Test-PerFileCoverageThreshold` test: a path supplied through `-ExemptFile` suppresses the failure for an otherwise-failing `testable` row.
  - Acceptance: one named `It` asserting the row is reported informationally and the failure list is empty, and a second assertion in the same `It` confirming the ledger object itself is unmodified.
- [ ] [P9-T46] Add the `Test-PerFileCoverageThreshold` test: a ledger whose `schema_version` is not `1` throws with a specific literal message rather than attempting a best-effort read.
  - Acceptance: one named `It` asserting the exact literal message.
- [ ] [P9-T47] Add the `Test-PerFileCoverageThreshold` test: supplying `-ThresholdPercent 90.0` changes the verdict for a row at 85% line coverage.
  - Acceptance: one named `It` asserting the row passes at the default 80.0 and fails at 90.0.
- [ ] [P9-T48] Add the `Test-PerFileCoverageThreshold` test: a `testable` file at 100% line and 66.67% branch fails the **branch** gate and the verdict names which gate failed.
  - Acceptance: one named `It` reproducing the shape F8 measured on `EfcHomeController.Timing.cs`, asserting the row is in the failure list, asserting the row's `FailedGates` list contains `branch` and does **not** contain `line`, and asserting the line column still renders `100.0`. This proves the two gates are independent.
- [ ] [P9-T49] Add the `Test-PerFileCoverageThreshold` test: exactly 75.0% branch passes and 74.99% branch fails, against the unrounded ratio.
  - Acceptance: one named `It` asserting no failure for a row at 75/100 branch and a failure for a row at 7499/10000 branch, and asserting that the failing row's rendered branch percentage is `75.0` so the comparison is demonstrably against the unrounded value rather than the display value.
- [ ] [P9-T50] Add the `Test-PerFileCoverageThreshold` test: a `testable` file with lines but no `branch="True"` elements reports `n/a` for branch, is not compared against the branch threshold, and does not fail the branch gate.
  - Acceptance: one named `It` on a row whose `BranchTotal` is `0`, asserting the branch column is `n/a` and explicitly not `0.0`, asserting the row's `FailedGates` list does not contain `branch`, and asserting the line gate is still applied by including a second row of the same shape that is below the line threshold and does fail.
- [ ] [P9-T51] Add the `Test-PerFileCoverageThreshold` test: a row carrying `line_target_percent: 90.0` is evaluated against `90.0` rather than the ledger-wide `threshold_percent` of `80.0`.
  - Acceptance: one named `It` asserting that a row with `line_target_percent: 90.0` at 85% line coverage **fails**, that the same row at 92% **passes**, and that a sibling row with `line_target_percent: null` at 85% passes under the ledger-wide 80.0 threshold in the same invocation, proving the override is per row and not global.
- [ ] [P9-T52] Audit all three Phase 9 test files — `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, and `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` — for hygiene: no test creates, writes, or deletes a filesystem path; no test depends on network access, machine PATH, profile state, or the current working directory; every mocked command is a filesystem cmdlet, not an external executable; each file is under 500 lines; every `It` has a descriptive prose name; and no file dot-sources a sibling test file. Write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/helpers-tests-hygiene.<ts>.md`.
  - Acceptance: artifact has all four required fields with one pass/fail line per constraint per file, all passing, and a numeric line count per file. The recorded `It` counts are 9, 18, and 18 respectively, totalling 45. An overage after the planned four-file split is a Blocking finding recorded in the artifact and escalated for a plan revision; it is not resolved by creating a further file, because the Scope Constraints table admits no additional test file and `.claude/rules/powershell.md` line 40 caps a batch at three test files.
- [ ] [P9-T53] Run the Phase 9 suites: invoke `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the executor's own current worktree root; then read `artifacts/pester/pester-junit.xml` and confirm each suite's `<testsuite name=...>` entry by full path, recording its `tests`, `failures`, and `skipped` attributes; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/regression-testing/helpers-suites.<ts>.md`.
  - Acceptance: artifact has all four required fields. The gate is zero `failures` and zero `skipped` across the `<testsuite>` entries in `artifacts/pester/pester-junit.xml`; the tool's exit code is recorded as an observation and is compared against the `[P0-T6]` baseline value, per the Toolchain Contract's definition of a failing test stage. `Output Summary:` names all three of `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, and `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` by the full path recorded in their `<testsuite name=...>` attributes and records the three numeric attributes for each, with zero failures and zero skips. A suite absent from `pester-junit.xml` is a Blocking finding. `EXIT_CODE: SKIPPED` is invalid.

### Phase 10 — Pester Suites for the Entry Function and Ledger Completeness

This phase creates the remaining **two** test files — `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` (`Invoke-PerFileCoverageGate`, `[P10-T3]` through `[P10-T17]`, 15 `It`) and `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` (`[P10-T19]` through `[P10-T35]`, 17 `It`) — in a batch separate from Phase 9, so neither batch exceeds the three-test-file per-batch cap in `.claude/rules/powershell.md` line 40. All fixtures are in-memory; no task in this phase creates, writes, or deletes a filesystem path.

- [ ] [P10-T1] Create `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1`, the entry-function suite, with `Set-StrictMode -Version Latest` as line 1 and a `BeforeAll` block that resolves the repository root from `$PSScriptRoot` and dot-sources both `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` and `scripts/vscode/Get-PerFileCoverage.ps1`, using the same convention as `[P9-T1]`.
  - Acceptance: the file loads under Pester 5.x; both dot-sources succeed; dot-sourcing the entry script does not execute the gate; no ambient working-directory assumption exists. The file is named in the Scope Constraints create-list, so its creation is in scope. Its scope is the entry function only; the helper cases live in the three Phase 9 suites.
- [ ] [P10-T2] Add a fixture-builder section to `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` producing the in-memory Cobertura and ledger here-strings the entry-function cases consume, mirroring the fixtures added in `[P9-T2]`, `[P9-T13]`, and `[P9-T33]`, including a ledger carrying `branch_threshold_percent: 75.0` and a Cobertura document carrying `branch="True"` lines with `condition-coverage` values.
  - Acceptance: every fixture is an in-memory here-string; no fixture reads from or writes to disk; the fixtures are defined inside this file rather than dot-sourced from any helper suite, so no test file depends on another's load order.
- [ ] [P10-T3] Add the entry-script test: dot-sourcing `scripts/vscode/Get-PerFileCoverage.ps1` defines `Invoke-PerFileCoverageGate` without executing the gate.
  - Acceptance: one named `It` asserting the command exists after dot-sourcing and asserting that no report output was produced by the dot-source itself.
- [ ] [P10-T4] Add the entry-function test: `ExitCode` is `0` when every `testable` row is at or above both its applied line threshold and the branch threshold.
  - Acceptance: one named `It` with `Test-Path` and `Get-Content` mocked to return the in-memory Cobertura and ledger fixtures, asserting `ExitCode -eq 0` and a non-empty `ReportText` containing both a line and a branch column.
- [ ] [P10-T5] Add the entry-function test: `ExitCode` is `1` when a `testable` row is below the line threshold.
  - Acceptance: one named `It` asserting `ExitCode -eq 1` with every row at or above the branch threshold, so the code is attributable to the line gate alone.
- [ ] [P10-T6] Add the entry-function test: `ExitCode` is `1` when a `testable` row is at 100% line but 66.67% branch, and the emitted diagnostic names the branch gate.
  - Acceptance: one named `It` reproducing the shape F8 measured on `EfcHomeController.Timing.cs`, asserting `ExitCode -eq 1` with no row below the line threshold, capturing the error stream, and asserting that the diagnostic for that row names the branch gate and the file path. This is the seeded condition that proves the widened exit-code contract.
- [ ] [P10-T7] Add the entry-function test: `ExitCode` is `1` for a `NO DATA` row.
  - Acceptance: one named `It` asserting `ExitCode -eq 1` with no below-threshold row present, so the code is attributable to the `NO DATA` path alone.
- [ ] [P10-T8] Add the entry-function test: `ExitCode` is `1` for an `UNLEDGERED` row.
  - Acceptance: one named `It` asserting `ExitCode -eq 1` with no below-threshold and no `NO DATA` row present.
- [ ] [P10-T9] Add the entry-function test: `ExitCode` is `0` when the only non-standard rows are `RECLASSIFY` and `NOT MEASURED (no executable lines)`.
  - Acceptance: one named `It` whose fixture carries one `interface-only` row present with executable lines and one `interface-only` row absent from the report, asserting `ExitCode -eq 0` and asserting that both rows appear in `ReportText` with their distinct statuses. This pins the manifest's rule that a third-bucket file never counts as a failure.
- [ ] [P10-T10] Add the entry-function test: `-BranchThresholdPercent` defaults to `75.0` and an explicit override changes the verdict.
  - Acceptance: one named `It` asserting that a row at 80% branch yields `ExitCode -eq 0` at the default and `ExitCode -eq 1` when `-BranchThresholdPercent 85.0` is supplied, and that the ledger's `branch_threshold_percent` header value is used when the parameter is not bound.
- [ ] [P10-T11] Add the entry-function test: `ExitCode` is `2` when the Cobertura path does not exist.
  - Acceptance: one named `It` with `Test-Path` mocked to return `$false` for the Cobertura path, asserting `ExitCode -eq 2`.
- [ ] [P10-T12] Add the entry-function test: `ExitCode` is `2` when the Cobertura content is not well-formed XML.
  - Acceptance: one named `It` with `Get-Content` mocked to return a truncated document, asserting `ExitCode -eq 2` and not `1`.
- [ ] [P10-T13] Add the entry-function test: `ExitCode` is `2` when the document contains no `<package name="QuickFiler">`.
  - Acceptance: one named `It` asserting `ExitCode -eq 2`, explicitly distinguishing an input error from a coverage regression.
- [ ] [P10-T14] Add the entry-function test: `ExitCode` is `2` when the ledger JSON is missing, and `2` again when it is present but not valid JSON.
  - Acceptance: one named `It` covering both branches with separate mock configurations, asserting `ExitCode -eq 2` in each.
- [ ] [P10-T15] Add the entry-function test: `ExitCode` is `2` for an unrecognised `schema_version`.
  - Acceptance: one named `It` asserting `ExitCode -eq 2`, confirming the pure-layer `throw` from `[P9-T46]` is caught and mapped rather than propagating.
- [ ] [P10-T16] Add the entry-function test: `-OutputPath` writes the report to the supplied path.
  - Acceptance: one named `It` that mocks `Set-Content` and asserts via `Should -Invoke` that it was called exactly once with the supplied path and with the same text returned as `ReportText`. No filesystem path is created.
- [ ] [P10-T17] Add the entry-function test: per-row diagnostics for failing rows are emitted on the error stream, not on stdout.
  - Acceptance: one named `It` capturing the error stream and asserting one diagnostic per failing row, and asserting the returned `ReportText` does not contain the diagnostic prefix.
- [ ] [P10-T18] Create `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` with `Set-StrictMode -Version Latest` as line 1 and a `BeforeAll` block that resolves the repository root from `$PSScriptRoot` and loads `QuickFiler/QuickFiler.csproj`, `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.json` (via `ConvertFrom-Json`), and `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.md`. The `BeforeAll` block also defines a reusable **pure comparison function** that takes a csproj text and a ledger object and returns the unmatched paths from each side, so the same logic serves both the real-file assertion and the in-memory drift fixtures.
  - Acceptance: all three loads use `$PSScriptRoot`-relative paths with no current-working-directory dependence; the suite creates, writes, and deletes nothing; the comparison function is pure and takes both sides as parameters so it can be exercised against in-memory fixtures without touching the real files.
- [ ] [P10-T19] Add the completeness (bijection) test: every `<Compile Include=...>` path read from `QuickFiler/QuickFiler.csproj` **at run time**, prefixed with `QuickFiler\` and normalised to backslash, has exactly one matching `files[].path` entry, and every `files[].path` entry has exactly one matching csproj path.
  - Acceptance: one named `It` asserting both directions with `OrdinalIgnoreCase` comparison and reporting the unmatched paths from each side in its failure message. **The suite contains no `files.Count -eq 121` assertion**: per Decisions Record item 6 and the manifest's `## Mid-Wave File Creation and the Ledger Denominator`, the denominator is dynamic and a changed count with every compiled file carrying a row is not a failure. The only failure conditions are a compiled file with no ledger row and a ledger row with no compiled file. This is the D15 staleness guard.
- [ ] [P10-T20] Add the csproj-drift fixture test: a fixture csproj that gains a `<Compile Include>` entry **and** a fixture ledger that gains the matching row together produce no unmatched path.
  - Acceptance: one named `It` that builds both fixtures in memory, calls the `[P10-T18]` comparison function, and asserts both unmatched lists are empty, proving that a sibling which adds a production file and its ledger row in the same change passes. The fixture count deliberately differs from the real ledger count, so a residual count assertion anywhere in the suite would fail this test.
- [ ] [P10-T21] Add the csproj-drift fixture test: a fixture csproj that gains a `<Compile Include>` entry **without** a matching ledger row produces exactly one unmatched csproj path.
  - Acceptance: one named `It` asserting the unmatched-csproj list contains exactly the added path, the unmatched-ledger list is empty, and the failure message names the added path, proving that a sibling which adds the file without the row fails.
- [ ] [P10-T22] Add the ownership test: every `files[]` entry carries a non-empty `owning_child`.
  - Acceptance: one named `It` asserting no entry has a null, empty, or whitespace-only `owning_child`.
- [ ] [P10-T23] Add the classification-enum test: every `files[].classification` is in `{testable, ratified-exempt, interface-only}`.
  - Acceptance: one named `It` asserting closed-set membership across the **three**-value enum and naming any offending path.
- [ ] [P10-T24] Add the exempt-ground test: every entry with a `classification` other than `testable` carries an `exempt_ground` drawn from `{generated-designer, interface-only, irreducible-host-wiring}` and a non-empty `rationale`, and no entry invents a fourth ground.
  - Acceptance: one named `It` asserting closed-set membership across all non-`testable` rows and a non-empty rationale on each. This mechanises acceptance criterion 2's closed-ground requirement.
- [ ] [P10-T25] Add the ground-to-classification mapping test, forward direction: `exempt_ground -eq 'generated-designer'` implies `classification -eq 'ratified-exempt'`; `exempt_ground -eq 'irreducible-host-wiring'` implies `classification -eq 'ratified-exempt'`; `exempt_ground -eq 'interface-only'` implies `classification -eq 'interface-only'`; `exempt_ground -eq $null` implies `classification -eq 'testable'`.
  - Acceptance: one named `It` asserting all four implications and naming any offending path. The assertion for the third implication is the one that enforces the manifest's rule that `interface-only` never yields `ratified-exempt`.
- [ ] [P10-T26] Add the ground-to-classification mapping test, reverse direction: every `classification -eq 'ratified-exempt'` entry carries an `exempt_ground` in `{generated-designer, irreducible-host-wiring}`; every `classification -eq 'interface-only'` entry carries `exempt_ground -eq 'interface-only'`; every `classification -eq 'testable'` entry carries `exempt_ground -eq $null`.
  - Acceptance: one named `It` asserting all three implications and naming any offending path. Together with `[P10-T25]` this makes the mapping a biconditional, so no row can carry a ground that disagrees with its classification in either direction.
- [ ] [P10-T27] Add the third-bucket attribute test: no entry with `classification -eq 'interface-only'` carries a non-empty `attribute_dispositions` array.
  - Acceptance: one named `It` asserting an empty array on every `interface-only` row and naming any offending path, mechanising the manifest's rule that a declaration-only file receives no `[ExcludeFromCodeCoverage]` attribute.
- [ ] [P10-T28] Add the per-row line-target test: every `line_target_percent` is `null` or a number in `[0, 100]`, and a non-null value appears only on an entry whose `classification` is `testable`.
  - Acceptance: one named `It` asserting the domain constraint and the testable-only constraint, naming any offending path. The test asserts the constraint rather than a fixed count, so a sibling that appends a mid-wave row carrying `line_target_percent: 90.0` passes.
- [ ] [P10-T29] Add the disposition-total test: non-inherited `attribute_dispositions` total exactly 40, comprising exactly 14 with `kind -eq 'type'` and exactly 26 with `kind -eq 'member'`, spread across exactly 21 distinct file paths.
  - Acceptance: one named `It` asserting all four numbers. This inventory is fixed at authoring time and describes the pre-existing attribute population, so the fixed counts are correct here even though the file count is dynamic: a file created mid-wave carries no attribute and therefore no disposition. This mechanises acceptance criterion 3.
- [ ] [P10-T30] Add the disposition-shape test: every non-inherited disposition carries a `site` matching the pattern `<path>:<line>`, a `disposition` in `{ratified, remove}`, a non-empty `owning_child`, and a non-empty `rationale`.
  - Acceptance: one named `It` asserting all four field constraints, with the site pattern asserted by regex against a backslash path followed by a colon and one or more digits.
- [ ] [P10-T31] Add the sequencing-instruction test: every disposition with `disposition -eq 'remove'` carries the tests-first, attribute-removal-last sequencing instruction in its rationale.
  - Acceptance: one named `It` asserting the presence of the instruction text on every `remove` row and naming any row that lacks it.
- [ ] [P10-T32] Add the inherited-reference test: every disposition with `kind -eq 'inherited'` carries a `from` value equal to the `site` of an existing `kind -eq 'type'` disposition, and inherited rows are excluded from the count of 40.
  - Acceptance: one named `It` asserting the referential constraint for all 11 inherited rows and asserting the exclusion.
- [ ] [P10-T33] Add the Markdown-agreement test: the data-row count of the classification table in `coverage-ledger.md` equals `files.Count`, and the Classification column renders the third value.
  - Acceptance: one named `It` asserting the row-count equality against `files.Count` rather than against a literal, counting only data rows and excluding the header and separator rows, and additionally asserting that the number of rows rendering the literal `interface-only / not-measured` equals the number of JSON entries whose `classification` is `interface-only`.
- [ ] [P10-T34] Add the ordering test: `files[]` is ordinal-sorted ascending by `path` as authored.
  - Acceptance: one named `It` asserting that re-sorting the path list with `[StringComparer]::Ordinal` produces the same sequence. The `It` carries a comment recording that ordering is an authoring-time property only, because siblings append rows and fan-in resolution keeps both sides, and that report determinism does not depend on it because the harness sorts ordinally at run time.
- [ ] [P10-T35] Add the header test: `schema_version -eq 1`, `package -eq 'QuickFiler'`, `threshold_percent -eq 80.0`, `branch_threshold_percent -eq 75.0`, `new_file_line_target_percent -eq 90.0`, `generated_from -eq 'QuickFiler/QuickFiler.csproj'`, and `source_commit` is a non-empty string.
  - Acceptance: one named `It` asserting all seven header fields, including the three keys added by `spec.md` v0.3, each asserted as a number where the JSON declares a number.
- [ ] [P10-T36] Audit both Phase 10 test files — `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` and `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` — for hygiene: neither creates, writes, or deletes a filesystem path; neither depends on network access, machine PATH, profile state, or the current working directory; every mocked command is a filesystem cmdlet, not an external executable; each file is under 500 lines; every `It` has a descriptive prose name; neither dot-sources a sibling test file. Append the results to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/other/helpers-tests-hygiene.<ts>.md`.
  - Acceptance: one pass/fail line per constraint per file, all passing, with a numeric line count per file. The recorded `It` counts are 15 and 17 respectively, which with the Phase 9 counts of 9, 18, and 18 totals **77** across the five new suites. The reading of `QuickFiler/QuickFiler.csproj` and the two ledger documents by `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` is a **read**, permitted by Decisions Record item 14, and the two drift fixtures in `[P10-T20]` and `[P10-T21]` operate on in-memory strings only. An overage is a Blocking finding escalated for a plan revision.
- [ ] [P10-T37] Run the Phase 10 suites: invoke `mcp__drm-copilot__run_poshqc_test` with `workspace_root` set to the executor's own current worktree root; then read `artifacts/pester/pester-junit.xml` and confirm each suite's `<testsuite name=...>` entry by full path, recording its `tests`, `failures`, and `skipped` attributes; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/regression-testing/entry-and-ledger-suites.<ts>.md`.
  - Acceptance: artifact has all four required fields. The gate is zero `failures` and zero `skipped` across the `<testsuite>` entries in `artifacts/pester/pester-junit.xml`; the tool's exit code is recorded as an observation and is compared against the `[P0-T6]` baseline value, per the Toolchain Contract's definition of a failing test stage. `Output Summary:` names both `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` and `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` by the full path recorded in their `<testsuite name=...>` attributes and records the three numeric attributes for each, with zero failures and zero skips. Absence of either suite from `pester-junit.xml` is a Blocking finding. `EXIT_CODE: SKIPPED` is invalid.

### Phase 11 — Final QC Loop, Constraint Verification, and Acceptance-Criteria Check-Off

- [ ] [P11-T1] Run the final formatting stage: invoke `mcp__drm-copilot__run_poshqc_format` with `workspace_root` set to the executor's own current worktree root; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/final-poshqc-format.<ts>.md`.
  - Acceptance: artifact has `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; `EXIT_CODE: 0`; `Output Summary:` records the returned `ok` value and `summary` string verbatim, and states whether any file was modified as determined by a `git status --porcelain` run immediately after the invocation. The tool returns no file count and no file list, so no scanned-file figure is recorded. `EXIT_CODE: SKIPPED` is invalid.
- [ ] [P11-T2] Run the final linting stage: invoke `mcp__drm-copilot__run_poshqc_analyze` with the same `workspace_root` and no `scan_folders` argument, then invoke it again with `scan_folders: ["tests/scripts/vscode"]`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/final-poshqc-analyze.<ts>.md`.
  - Acceptance: artifact has all four required fields. The repository-wide diagnostic count is recorded numerically and is **equal to or lower than** the `[P0-T5]` baseline count; a count above baseline is a Blocking finding. `EXIT_CODE: 0` is not attainable because the repository carries a pre-existing PSScriptAnalyzer baseline in `scripts/vscode/` that this feature must not modify; the recorded exit code is the observed value and the baseline-delta figure is the gate. The artifact additionally records the scoped `mcp__drm-copilot__run_poshqc_analyze` invocation with `scan_folders: ["tests/scripts/vscode"]` whose issue count must be `0`, proving the five new test files introduce no diagnostic. A clean scoped run returns no numeric count, only `ok: true` with a summary naming `1 selected scan folder(s)`; that response is recorded as a count of `0`, per the criterion established in `[P0-T8]`. `EXIT_CODE: SKIPPED` is invalid.
- [ ] [P11-T3] Run the final testing stage: invoke `mcp__drm-copilot__run_poshqc_test` with the same `workspace_root`; then read `artifacts/pester/pester-junit.xml` and `artifacts/pester/powershell-coverage.xml` from the executor's own worktree; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/final-poshqc-test-coverage.<ts>.md`. Then measure the two new production scripts directly, because the bundled PoshQC Pester configuration does not instrument `scripts/vscode/`: run `pwsh -NoProfile -Command '$c = New-PesterConfiguration; $c.Run.Path = "tests/scripts/vscode"; $c.Run.PassThru = $true; $c.CodeCoverage.Enabled = $true; $c.CodeCoverage.Path = @("scripts/vscode/Get-PerFileCoverage.Helpers.ps1","scripts/vscode/Get-PerFileCoverage.ps1"); $c.CodeCoverage.OutputFormat = "JaCoCo"; $c.CodeCoverage.OutputPath = "docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/newcode-coverage.<ts>.xml"; $r = Invoke-Pester -Configuration $c; "PASSED=$($r.PassedCount) FAILED=$($r.FailedCount)"'` and record its exit code and the per-`<class sourcefilename=...>` LINE counters from the emitted JaCoCo file. The outer quoting must be single quotes and every inner string literal double quotes, so that neither a Bash-hosted nor a PowerShell-hosted invocation expands `$c`, `$r`, or `$(...)` before `pwsh` receives the script. The command is run with the current directory set to the worktree root, because all four paths are repository-relative. Verified at preflight: this form runs to completion in both shells, `New-PesterConfiguration` resolves against Pester 5.6.1, `CodeCoverage.OutputPath` creates the target directory if absent, and the emitted JaCoCo carries a per-file `<class sourcefilename=...>` element with a `LINE` counter and no `BRANCH` counter. This run creates no temporary file: its only output is the named evidence artifact. The mechanism is proven feasible in Phase 0 by `[P0-T12]`, which is this task's precedent; a Phase 0 failure there halts the plan before this task is reached.
  - Acceptance: artifact has all four required fields. The gate is zero `failures` and zero `skipped` across the `<testsuite>` entries in `artifacts/pester/pester-junit.xml`; the tool's exit code is recorded as an observation and is compared against the `[P0-T6]` baseline value, per the Toolchain Contract's definition of a failing test stage. Test counts are taken by reading `artifacts/pester/pester-junit.xml`, confirming each suite's `<testsuite name=...>` entry by full path and recording its `tests`, `failures`, and `skipped` attributes; the totals show zero failed and zero skipped tests. `Output Summary:` records the **numeric** repository-wide PowerShell line-coverage percentage computed from the report-level `<counter type="LINE">` of `artifacts/pester/powershell-coverage.xml` as `covered / (covered + missed)`, together with the literal line `Scope of the repository-wide figure: the bundled PoshQC Pester configuration instruments only .claude/hooks/, .claude/lib/, and .codex/hooks/; scripts/vscode/ is not in its CodeCoverage.Path allow-list and no repo-local override exists.` The **numeric** per-file line coverage for `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` and `scripts/vscode/Get-PerFileCoverage.ps1` is taken from the supplementary direct-`Invoke-Pester` JaCoCo artifact named above, not from `artifacts/pester/powershell-coverage.xml`. Placeholders are invalid. Branch coverage is not recorded as a number; the artifact carries the literal line `Branch coverage: not emitted by Pester JaCoCo output (no BRANCH counter present)`. `EXIT_CODE: SKIPPED` is invalid. PowerShell has no type-checking stage; its absence is noted in the artifact per `.claude/rules/powershell.md` line 17.
- [ ] [P11-T4] Enforce the toolchain restart rule: if `[P11-T1]`, `[P11-T2]`, or `[P11-T3]` failed or modified any file, re-run the loop from `[P11-T1]` and record each additional cycle as a new numbered artifact under `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/`; write the loop summary to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/final-toolchain-loop.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` records the number of cycles run and confirms that the final cycle completed format, analyze, and test consecutively with no stage exceeding its `[P0-T5]`/`[P0-T6]` baseline and no file modification. A single clean consecutive pass under that definition is required; a partial pass is remediation-required.
- [ ] [P11-T5] Produce the coverage delta artifact `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/coverage-delta.<ts>.md` comparing the `[P0-T6]` baseline against the `[P11-T3]` final run.
  - Acceptance: artifact has all four required fields and records, all numerically: baseline repository-wide line coverage; post-change repository-wide line coverage; the delta; and the new-code line coverage for `scripts/vscode/Get-PerFileCoverage.Helpers.ps1` and `scripts/vscode/Get-PerFileCoverage.ps1`. The two repository-wide figures come from the report-level `<counter type="LINE">` of `artifacts/pester/powershell-coverage.xml` and carry the literal line `Scope of the repository-wide figure: the bundled PoshQC Pester configuration instruments only .claude/hooks/, .claude/lib/, and .codex/hooks/; scripts/vscode/ is not in its CodeCoverage.Path allow-list and no repo-local override exists.` The two new-code figures come from the supplementary direct-`Invoke-Pester` JaCoCo artifact produced by `[P11-T3]`, not from `artifacts/pester/powershell-coverage.xml`, which carries no `scripts/vscode/` sourcefile. Gate per Decisions Record item 13: each new production script reaches line coverage >= 85%, and the repository-wide line figure does not regress against baseline. If the baseline repository-wide line figure was already below 85%, that pre-existing state is stated explicitly and the no-regression rule is the applied gate. Branch coverage is not recorded as a number; the artifact carries the literal line `Branch coverage: not emitted by Pester JaCoCo output (no BRANCH counter present)` and records as an out-of-scope observation that the >= 75% branch requirement in `.claude/rules/powershell.md` is not measurable by the PowerShell toolchain. The artifact additionally states that this limitation concerns **PowerShell** coverage only and does not affect the Cobertura C# branch figures this feature's harness computes, which are asserted by `[P9-T22]`, `[P9-T23]`, `[P9-T48]`, `[P9-T49]`, `[P9-T50]`, and `[P10-T6]`. Failure of either applied gate is remediation-required, never PASS.
- [ ] [P11-T6] Close the D17 verification for all seven new file paths; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/poshqc-scan-scope-closure.<ts>.md`. For the two production scripts, apply the same scoped-invocation standard as `[P0-T7]` and `[P0-T8]`: invoke `mcp__drm-copilot__run_poshqc_format` and `mcp__drm-copilot__run_poshqc_analyze` with `scan_folders: ["scripts/vscode"]` and record `ok`, the returned summary, the issue count, and `git status --porcelain`. For the five test files, apply the `artifacts/pester/pester-junit.xml` name-level proof: confirm each file's `<testsuite name=...>` entry by full path from the `[P11-T3]` run.
  - Acceptance: artifact has all four required fields and names all seven new paths — `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`, `scripts/vscode/Get-PerFileCoverage.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1`, `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` — with the stage and the evidence method that proved each. Any new file whose directory is not proven in scope, or any test file absent from `pester-junit.xml`, is a Blocking finding: the harness is not gated and the finding is escalated rather than worked around.
- [ ] [P11-T7] Verify the no-touch constraints: run `git status --porcelain` and `git diff --name-only` against the baseline recorded in `[P0-T3]`, and confirm that no changed, added, or deleted path lies under `QuickFiler/`, `.claude/rules/`, `.github/instructions/`, or equals `CLAUDE.md`, `QuickFiler.Test/QuickFiler.Test.csproj`, or `UtilitiesCS/Properties/AssemblyInfo.cs`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/no-touch-constraints.<ts>.md`.
  - Acceptance: artifact has all four required fields; `Output Summary:` quotes the full changed-path list verbatim and states `0` matches for each of the six forbidden scopes. The zero-match result for `QuickFiler/` includes `QuickFiler/QuickFiler.csproj` specifically; the changed-path list is additionally asserted to contain **zero** entries equal to `QuickFiler.Test/QuickFiler.Test.csproj`, the second shared csproj recorded by the manifest at `### 1b`. The artifact records F1's no-op position against all three manifest cross-child constraints: this child adds no C# — neither a compiled production file nor a compiled test file — so it edits none of `QuickFiler/QuickFiler.csproj`, `QuickFiler.Test/QuickFiler.Test.csproj`, or `UtilitiesCS/Properties/AssemblyInfo.cs`, and its obligation on all three is documentary and discharged by `[P6-T3]`. Any match is a Blocking finding.
- [ ] [P11-T8] Verify the read-only-dependency constraints: confirm the changed-path list from `[P11-T7]` contains no entry for `coverage.config`, any `*.runsettings` file, `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, or `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1`, and that the line counts of the latter two are unchanged from `[P0-T11]`. Append the results to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/no-touch-constraints.<ts>.md`.
  - Acceptance: `0` matches for each named path and the two line counts equal their `[P0-T11]` baselines. Any change is a Blocking finding; the issue #441 defect remains unmodified by design, and the artifact records that no second issue was created for it per Decisions Record item 20.
- [ ] [P11-T9] Verify the changed-path list from `[P11-T7]` contains exactly the ten in-scope repository paths — the two ledger files, the two production scripts, the five test files (`tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1`, `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1`), and the deletion of `scripts/temp-extract-coverage.ps1` — plus documents and evidence artifacts under `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/`, plus any file modified by the Phase 0 baseline formatter run and enumerated in `[P0-T4]`, which is accounted for separately and is not a scope violation. Append the results to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/no-touch-constraints.<ts>.md`.
  - Acceptance: every changed path is accounted for by that list; `scripts/temp-extract-coverage.ps1` is the only deletion; any unaccounted path is a Blocking finding. Any path attributed to the Phase 0 baseline formatter run is recorded with that attribution and cross-referenced to the `[P0-T4]` artifact.
- [ ] [P11-T10] Verify file-size compliance across the seven new file paths: record the line count of `scripts/vscode/Get-PerFileCoverage.Helpers.ps1`, `scripts/vscode/Get-PerFileCoverage.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1`, `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1`, and `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1`; write `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/file-size-compliance.<ts>.md`.
  - Acceptance: artifact has all four required fields and one numeric line count per file, each strictly under 500. The pure-logic helper's count is additionally reported against its 250-380 target from Decisions Record item 17; a value in 380-499 is an observation, not a failure. An overage at or above 500 on any file is a Blocking finding escalated for a plan revision, because the Scope Constraints table admits no additional production or test file; it is not resolved by an unplanned split.
- [ ] [P11-T11] Write the acceptance-criteria verification summary `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/acceptance-criteria-verification.<ts>.md`, mapping each of the six criteria in `spec.md` to the specific evidence artifact paths and Pester test names that satisfy it.
  - Acceptance: all six criteria addressed, with citations matching the Traceability table exactly. AC1 cites `[P10-T19]`, `[P10-T20]`, `[P10-T21]`, `[P10-T22]`, `[P10-T23]`, `[P10-T33]` — a **completeness** assertion against the csproj read at run time, plus the two csproj-drift fixtures, and explicitly **not** a `files.Count -eq 121` count assertion, per `spec.md`'s note on the number 121; the artifact records the evaluation-time count from `[P0-T10]` alongside the authoring-time snapshot as an observation. AC2 cites `[P10-T24]`, `[P10-T25]`, `[P10-T26]`, `[P10-T27]`, `[P10-T28]`, including the ground-to-classification biconditional in both directions and the third-bucket empty-`attribute_dispositions` assertion. AC3 cites `[P10-T29]`, `[P10-T30]`, `[P10-T31]`, `[P10-T32]`. AC4 cites `[P7-T3]` through `[P7-T8]`, `[P8-T1]`, `[P8-T3]`, `[P8-T4]`, `[P9-T5]`, `[P9-T18]`, `[P9-T22]` through `[P9-T31]`, `[P9-T34]` through `[P9-T51]`, and `[P10-T3]` through `[P10-T17]`, matching the Traceability table row for criterion 4 exactly, and the artifact states explicitly that the branch assertions `[P9-T22]`, `[P9-T23]`, `[P9-T48]`, `[P9-T49]`, `[P9-T50]`, and `[P10-T6]` satisfy the manifest's independent >= 75% branch gate alongside the 80% line gate named in the criterion text. AC5 cites `[P9-T1]`, `[P9-T12]`, `[P9-T32]`, `[P9-T52]`, `[P10-T1]`, `[P10-T18]`, `[P10-T36]`. AC6 cites `[P11-T1]` through `[P11-T4]` and `[P11-T6]`. Each citation names the artifact path that holds the evidence.
- [ ] [P11-T12] Check off the satisfied acceptance criteria in `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/spec.md` and `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/user-story.md` per the `acceptance-criteria-tracking` protocol, changing `- [ ]` to `- [x]` only where `[P11-T11]` records satisfying evidence.
  - Acceptance: both documents carry identical check states across all six criteria; no criterion is checked without a cited artifact; a criterion lacking evidence remains unchecked and is reported as remediation-required. No text other than the checkbox characters is altered; the acceptance-criterion wording itself is unchanged by this revision.
- [ ] [P11-T13] Update the "Definition of Done" checklist in `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/spec.md` and the "Seeded Test Conditions" checklist, checking only items backed by the evidence recorded in this plan's artifacts.
  - Acceptance: every checked Definition-of-Done item and every checked Seeded Test Condition maps to a named artifact path or Pester test name; the "Telemetry/logging" item is either checked with its rationale or explicitly marked not applicable; unbacked items remain unchecked. The Seeded Test Conditions list includes the eleven conditions added by the epic-manifest amendment, each mapped as follows: third-bucket `N/A` reporting to `[P9-T41]`; `line-rate="0"` with zero `<line>` children to `[P9-T24]` and `[P9-T42]`; ground-mapping enforcement to `[P10-T25]`, `[P10-T26]`, and `[P10-T27]`; the `sum(covered)/sum(total)` derivation yielding 70.0% to `[P9-T22]`; the 100%-line / 66.67%-branch exit-1 case to `[P10-T6]` and `[P9-T48]`; the 75.0% / 74.99% branch boundary to `[P9-T49]`; lines-but-no-branch-points to `[P9-T50]`; `branch="True"` with no `condition-coverage` to `[P9-T23]`; the `line_target_percent: 90.0` override to `[P9-T51]`; and the two csproj-drift fixtures to `[P10-T20]` and `[P10-T21]`.
- [ ] [P11-T14] Write the plan-completion summary to `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/plan-completion.<ts>.md` recording the final toolchain status, the coverage figures, the no-touch verification result, and the acceptance-criteria state.
  - Acceptance: artifact has all four required fields; `Output Summary:` states the final format, analyze, and test outcomes with their exit codes, the analyze baseline-delta figure, the post-change repository-wide **line** coverage taken from the report-level `<counter type="LINE">` of `artifacts/pester/powershell-coverage.xml` and carrying its recorded hooks-only scope statement, the new-code line coverage for both production scripts taken from the supplementary direct-`Invoke-Pester` JaCoCo artifact produced by `[P11-T3]` rather than from `artifacts/pester/powershell-coverage.xml`, `0` forbidden-scope matches across all six forbidden scopes, and the count of checked acceptance criteria out of six. Branch coverage is not recorded as a number; the artifact carries the literal line `Branch coverage: not emitted by Pester JaCoCo output (no BRANCH counter present)`. If any required figure is missing or any applied gate failed, the recorded outcome is remediation-required, never PASS.

## Test Plan

- **Unit (Pester 5.x), pure logic:** `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Cobertura.Tests.ps1` covers `Merge-CoberturaFileLines` and `Get-CoberturaConditionCoverageParts`, planned individually as `[P9-T3]` through `[P9-T11]` (9 `It`); `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Rows.Tests.ps1` covers `Get-CoberturaPerFileCoverage` and `Format-PerFileCoverageReport`, planned individually as `[P9-T14]` through `[P9-T31]` (18 `It`); `tests/scripts/vscode/Get-PerFileCoverage.Helpers.Verdict.Tests.ps1` covers `Test-PerFileCoverageThreshold`, planned individually as `[P9-T34]` through `[P9-T51]` (18 `It`). `ConvertTo-CoveragePathKey` is exercised through the normalisation cases `[P9-T19]` and `[P9-T20]`.
- **Unit (Pester 5.x), entry function:** `tests/scripts/vscode/Get-PerFileCoverage.Tests.ps1` covers the entry function's exit-code contract including the widened exit-`1` condition, planned individually as `[P10-T3]` through `[P10-T17]` (15 `It`).
- **Contract / drift:** `tests/scripts/vscode/QuickFilerCoverageLedger.Tests.ps1` asserts the csproj-to-ledger completeness bijection, the two csproj-drift fixtures, the ledger's value domains, the ground-to-classification biconditional, the third-bucket constraints, the per-row line-target constraint, the disposition totals, and the Markdown-to-JSON agreement across 17 test cases planned individually as `[P10-T19]` through `[P10-T35]`. `[P10-T19]` is the D15 staleness guard that turns a later change to the compiled surface into a test failure at the next toolchain run; it asserts completeness, never a count.
- **Total:** five new suites, 77 `It` blocks. The four-file harness split and the five-file total are planned up front per Decisions Record items 18 and 19; all fixtures are in-memory here-strings defined inside each file; the only mocks are the filesystem cmdlets `Test-Path`, `Get-Content`, and `Set-Content`.
- **Integration / smoke:** `[P8-T6]` runs the entry script end to end against a committed reference Cobertura report. Expected exit code `1`; recorded as an observation, not a gate.
- **Manual / CLI:** none required.
- **Coverage evidence:** baseline `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/baseline/poshqc-test-coverage-baseline.<ts>.md`; post-change `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/final-poshqc-test-coverage.<ts>.md`; comparison `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/qa-gates/coverage-delta.<ts>.md`.
- **No temporary files:** no test in any of the five suites creates, writes, or deletes a filesystem path; verified by `[P9-T52]` and `[P10-T36]`. The direct-`Invoke-Pester` coverage runs in `[P0-T12]` and `[P11-T3]` write only the named evidence artifacts under `docs/features/active/2026-08-07-quickfiler-coverage-ledger-432/evidence/`, and create no temporary file.

## Traceability

| Acceptance criterion (spec.md) | Satisfying tasks |
| --- | --- |
| 1. Ledger accounts for all compiled files | P1-T1, P1-T3, P4-T9, P5-T2, P5-T6, P6-T2, P6-T7, P10-T19, P10-T20, P10-T21, P10-T22, P10-T23, P10-T33 |
| 2. Every `ratified-exempt` row carries a permitted-ground rationale | P4-T1, P4-T2, P4-T3, P4-T4, P4-T5, P4-T9, P4-T10, P5-T7, P6-T3, P10-T24, P10-T25, P10-T26, P10-T27, P10-T28 |
| 3. Every attribute has a recorded disposition naming the owning child | P1-T5, P1-T6, P1-T9, P2-T1 through P2-T17, P3-T1 through P3-T9, P5-T3, P5-T4, P5-T5, P6-T5, P10-T29, P10-T30, P10-T31, P10-T32 |
| 4. Deterministic per-file report; non-zero exit below 80% line or 75% branch | P7-T3 through P7-T8, P8-T1, P8-T3, P8-T4, P9-T5, P9-T18, P9-T22 through P9-T31, P9-T34 through P9-T51, P10-T3 through P10-T17 |
| 5. Pure logic unit-tested at the mirrored `tests/` path; no temp files | P9-T1, P9-T2, P9-T12, P9-T13, P9-T32, P9-T33, P9-T52, P9-T53, P10-T1, P10-T2, P10-T18, P10-T36, P10-T37 |
| 6. Full PowerShell toolchain passes in final form | P0-T4 through P0-T9, P0-T12, P7-T10, P8-T8, P11-T1 through P11-T4, P11-T6 |

## Open Questions / Notes

- **Halt conditions.** Five classes of task can halt the plan for escalation rather than proceeding: `[P0-T7]`/`[P0-T8]`/`[P0-T9]` if either target directory is outside a PoshQC scan set; `[P0-T12]` if direct-`Invoke-Pester` coverage measurement does not work in the executor's environment, because `[P11-T3]` has no other way to obtain per-file coverage for the two new production scripts; `[P8-T5]` if a live reference to `scripts/temp-extract-coverage.ps1` is found; `[P7-T9]`/`[P9-T52]`/`[P10-T36]`/`[P11-T10]` if any new file reaches 500 lines, because the Scope Constraints table admits no unnamed additional file; and `[P11-T2]`/`[P11-T4]` if the analyzer diagnostic count exceeds its `[P0-T5]` baseline. None is worked around. **`[P0-T10]` is no longer a halt condition:** under the manifest's dynamic-denominator rule a compiled-file count other than 121 is recorded as an observation and carried into the inventory, because a sibling adding a compiled file must not trip a frozen-count gate.
- **The denominator is dynamic; 121 is a snapshot.** Every task that names 121 names it as the authoring-time snapshot at research HEAD `74be1964` and defers to the `[P0-T10]` evaluation-time count where they differ. The only asserted property is completeness against `QuickFiler/QuickFiler.csproj` read at run time, mechanised by `[P10-T19]` and exercised in both directions by the drift fixtures `[P10-T20]` and `[P10-T21]`. The Pester suite carries no `files.Count -eq 121` assertion.
- **Two independent coverage gates, and two unrelated branch topics.** The harness applies a per-file line gate at 80% and a per-file **branch** gate at 75% to **C# QuickFiler files** measured from a committed Cobertura report; that branch data is verified present in the real document by research §4.3 and is asserted by `[P9-T22]`, `[P9-T23]`, `[P9-T48]`, `[P9-T49]`, `[P9-T50]`, and `[P10-T6]`. Separately, **PowerShell** branch coverage is not emitted at all by Pester's JaCoCo output, so no numeric PowerShell branch figure is required or recorded anywhere in this plan. The two are different measurements of different languages by different tools and must not be conflated.
- **In-flight features #400 and #424.** For this child's own diff they are a non-issue: no file under `QuickFiler/` is modified, so there is no textual conflict. For this child's deliverable they are a staleness risk, mitigated by the `[P10-T19]` csproj-to-ledger completeness assertion, which converts any later change to the compiled surface into a test failure at the next toolchain run while allowing an accompanied addition to pass.
- **The figure 33 is never a target.** It appears in exactly one place in the deliverables — the `[P6-T4]` reconciliation section — and only as the decomposition 21 + 5 + 7. The acceptance-criterion text in `spec.md` is left unchanged and is satisfied by 40 dispositions.
- **Issue #441 remains open by design, and no second issue is created.** The double-counted repository-wide `lines-valid` figure is recorded in the ledger by `[P6-T6]`, together with the mechanism deviation described in Decisions Record item 20, and is verified unmodified by `[P11-T8]`. Correcting it would perturb every existing gate and every committed evidence baseline. Creating a second issue through the MCP promotion lifecycle would duplicate #441, so the plan records the existing reference instead.
- **F1 touches no shared C# file.** All three manifest cross-child constraints — the `QuickFiler/QuickFiler.csproj` shared-file rule, the `QuickFiler.Test/QuickFiler.Test.csproj` shared-file rule added by manifest `### 1b`, and the missing `UtilitiesCS` `InternalsVisibleTo` grant to `QuickFiler.Test` — are no-ops for F1, which adds no C# at all: its two production scripts and five Pester suites are PowerShell under `scripts/vscode/` and `tests/scripts/vscode/`, compiled by no csproj. The plan contains no task that edits any of the three files, all three appear on the MUST-NOT-touch list, and `[P11-T7]` asserts zero changed paths for each. F1's only obligation is documentary and is discharged by `[P6-T3]`, which records the minimal-hunk, own-entries-only and CRLF-preservation rules for **both** csproj files inside the ledger, for the siblings that do have to edit them.
- **Coverage-threshold context.** `.claude/rules/powershell.md` and `.claude/rules/general-unit-test.md` state line >= 85% and branch >= 75%; `CLAUDE.md` § UT2 states line >= 80% repository-wide. This plan does not adjudicate that tension: it applies the >= 85% **line** threshold to the new production scripts as the change-scoped gate and applies a no-regression rule to the repository-wide line figure, recording both numbers in `[P11-T5]`. The two figures come from different measurements by necessity: the repository-wide figure is the hooks-only figure the bundled PoshQC Pester configuration emits, and the per-script figure comes from the supplementary direct-`Invoke-Pester` run in `[P11-T3]`, whose feasibility is proven in `[P0-T12]`. The >= 75% PowerShell branch threshold is not applied because the PowerShell coverage toolchain emits no branch data; per Decisions Record item 13 that gap is recorded as an out-of-scope observation in `[P11-T5]` and is not re-legislated here.
- **Analyzer baseline context.** `scripts/vscode/` carries a pre-existing PSScriptAnalyzer baseline (Decisions Record item 16), so the analyze stage cannot reach exit code `0` within this plan's scope constraints. The applied gate is a baseline delta plus a scoped zero-diagnostic assertion over `tests/scripts/vscode/`. Remediating the 16 pre-existing `scripts/vscode/` diagnostics is out of scope for this child and should be promoted to its own issue rather than absorbed here.
