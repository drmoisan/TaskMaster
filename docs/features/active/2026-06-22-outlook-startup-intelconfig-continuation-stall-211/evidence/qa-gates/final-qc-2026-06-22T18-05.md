# Final QC — Issue #211 Phase 1 (continuation-latency attribution probe)

- Timestamp: 2026-06-22T22:40 (local)
- Branch: bug/outlook-startup-intelconfig-continuation-stall-211
- Scope: ApplicationGlobals.cs (probe + 5 call sites), ApplicationGlobalsStartupTimingTests.cs +
  ApplicationGlobalsTests.cs (rename propagation), new ContinuationProbeSequenceTests.cs (+ csproj include).

## Toolchain (run in order)

1. CSharpier — `dotnet tool run csharpier check .` -> EXIT 0, "Checked 1089 files" (after one `format`
   pass that normalized line endings on the new test file). PASS.
2. .NET analyzers — `msbuild TaskMaster.sln -t:Build -p:EnableNETAnalyzers=true
   -p:EnforceCodeStyleInBuild=true` -> Build succeeded, 0 Errors, 7 Warnings. The 7 warnings are
   pre-existing CS8632 (nullable-annotation) in untouched `*.Test` files surfaced on test-project
   recompile; NONE reference `ContinuationProbeSequenceTests.cs` (verified by grep). PASS (gate = 0 errors).
3. Nullable / TreatWarningsAsErrors — `msbuild TaskMaster.sln -t:Build -p:Nullable=enable
   -p:TreatWarningsAsErrors=true` (with the test project recompiling after the change) -> Build
   succeeded, 0 Warnings, 0 Errors. PASS.
4. MSTest + coverage, gated — `vstest.console.exe <7 *.Test.dll> /EnableCodeCoverage
   /TestCaseFilter:"TestCategory!=LiveOutlook"` -> Total 4312, Passed 4311, Failed 1.

## The two new probe tests PASS

`ContinuationProbeSequenceTests.LoadSequentialAsync_InvokesProbeForEachBoundaryInStartupOrder` and
`...InvokesProbeExactlyOncePerBoundary` both pass (they assert the probe fires once per inter-phase
boundary with names `IntelConfig, OlObjects, ToDo, AutoFile, Engines`).

## The single failure is a pre-existing, unrelated flake

`UtilitiesCS.Test.ReusableTypeClasses.TimedAsyncTask_Tests.RequestTask_WithProvidedTask_InvokesTaskAfterInterval`
failed once in the full coverage run at **24 s** (a real-interval timer test, timing-sensitive under
full-suite/coverage load). Run in isolation it passes deterministically (2/2). It is in `UtilitiesCS`,
unrelated to the #211 probe change, and predates this work. Recommendation: track as a separate
flaky-test follow-up (real-time interval in a unit test). It is not a regression from this change.

Note on the suite's parallelization sensitivity: running the suite WITHOUT `/EnableCodeCoverage`
produces ~1043 failures (shared-state/STA parallelization interference); the canonical gate uses
`/EnableCodeCoverage` (more serialized), where only the one TimedAsyncTask flake appears. This is a
pre-existing suite characteristic, not introduced here.

## File sizes (<= 500)

`ApplicationGlobals.cs` ~273 lines (was 248; +~25 for the probe); `ContinuationProbeSequenceTests.cs`
~110 lines. All touched files <= 500.

## AC status

- AC1 PASS (probe emits the seven `[continuation-resume]` fields).
- AC2 PASS (behavior-preserving; Stopwatch only; no banned API; same phase order/count).
- AC3 PASS (deterministic probe-sequence tests via overriding seam, no live COM/timer/filesystem).
- AC4 PASS for the change (toolchain green; the lone failure is a pre-existing unrelated flake, not a
  regression; new probe seam covered by the two tests).
- AC5 (maintainer non-debugger runtime capture) — pending.
- AC6 (Phase 2 off-STA fix) — evidence-gated on AC5.
