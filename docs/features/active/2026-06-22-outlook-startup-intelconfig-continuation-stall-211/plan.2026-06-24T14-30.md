# Atomic Plan — Phase 3.4: StoresWrapper store-filter attribution probe (issue #211)

- **Feature folder:** `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/`
- **Work Mode (from `issue.md`):** `full-bug` (spec.md required; user-story optional/absent by default)
- **Scope:** Phase 3.4 only (AC16). Diagnosis-first, behavior-preserving instrumentation of the store-FILTER path that is currently NOT timed. NO latency fix (the fix remains AC10-gated and out of scope).
- **Primary source files:**
  - `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs` (COM-bound enumeration/predicate; reads `store.ExchangeStoreType`, `store.DisplayName`, `store.FilePath` on the STA) — gains the per-store `Stopwatch`-wrapped COM property reads and a `GetFilteredStores`-level summary on the synchronous `Init()` path. Baseline 267 lines.
  - A NEW coverable helper file `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs` (NOT `[ExcludeFromCodeCoverage]`) — holds the PURE `[store-filter]` line formatter and the PURE include/exclude DECISION over already-read primitive property values (matched-rule enum). Requires explicit `<Compile Include>` wiring in `UtilitiesCS.csproj` (legacy packages.config, no glob).
  - `UtilitiesCS.Test/OutlookObjects/Store/StoreFilterAttributionTests.cs` (NEW deterministic seam tests; MSTest + Moq + FluentAssertions). Requires explicit `<Compile Include>` wiring in `UtilitiesCS.Test.csproj` (legacy packages.config, no glob).
- **Requirements sources:** `issue.md`, `spec.md` (`## Acceptance Criteria`). The latest verified capture `evidence/other/runtime-capture-allphase-uiheartbeat-gc-2026-06-24T10-24.md` establishes the ~108 s STA freeze; this plan instruments the store-filter path to test whether store enumeration is a contributor.

## Verified context (do not re-litigate)

- `StoresWrapper.GetFilteredStores()` (StoresWrapper.cs:119-122) = `Globals.Ol.NamespaceMAPI.Stores.Cast<Outlook.Store>().Where(ShouldIncludeStore)`.
- `ShouldIncludeStore` (StoresWrapper.cs:187-241) reads `store.ExchangeStoreType`, `store.DisplayName`, and `store.FilePath` (live COM, on the STA). The GWSO/Gmail exclusion matches `store.FilePath` against `GwsoFilePathContains` (defaults include `\Google\Google Apps Sync\` and `\Google\Google Workspace Sync\`). `store.FilePath` is read inside a `try { } catch { }` and may be the blocking call on a failing Gmail store.
- The existing static `StoreIsIncluded` (StoresWrapper.cs:124-185) takes primitive list inputs but still reads `store.ExchangeStoreType`/`store.DisplayName`/`store.FilePath` directly off the COM `Outlook.Store`; it is therefore NOT fully pure and is not the coverable decision target by itself. This plan adds a genuinely-pure decision that accepts already-read primitive values.
- `Init()` (StoresWrapper.cs:35-39) calls `GetFilteredStores()` then `new StoreWrapper(store).Init()` for each included store, and currently logs NO `GetFilteredStores` summary. `RewireOlObjectsAsync` (StoresWrapper.cs:73-117) already logs a `[Startup timing] GetFilteredStores completed` summary; do NOT duplicate that line on the async path.
- `StoreWrapper.Init()` (StoreWrapper.cs:27-60) and `GetSmtpAddressFromStore()` (StoreWrapper.cs:131-169) ALREADY carry per-COM-call `[Startup timing]` Stopwatch logging. Do NOT touch or duplicate the per-store Init path; the gap closed here is the FILTER path only.
- Neither `StoresWrapper` nor `StoreWrapper` is `[ExcludeFromCodeCoverage]` (only `StoreWrapperController` has the attribute). The filter COM reads must live in `StoresWrapper` (COM-bound); the pure attribution/decision logic moves to the new coverable `StoreFilterAttribution` helper so the new-code `>= 90%` rule is satisfiable without live COM.
- `UtilitiesCS/UtilitiesCS.csproj` and `UtilitiesCS.Test/UtilitiesCS.Test.csproj` are legacy `packages.config` projects with explicit `<Compile Include>` items and no wildcard glob. Any NEW `.cs` file MUST be wired with an explicit `<Compile Include>` item or it will not compile into the assembly.
- The UtilitiesCS test project is `UtilitiesCS.Test` (NOT `TaskMaster.Test`). `UtilitiesCS.Test/OutlookObjects/Store/StoresWrapperTests.cs` already exists and is wired at `UtilitiesCS.Test.csproj:288`.

## Hard constraints (guardrails — apply to every implementation task)

- Behavior-preserving / diagnosis-only. MUST NOT change the filter result, the included store set, the store enumeration order, the predicate short-circuit order in a way that alters which stores are included, or any load/serialization semantics. The instrumentation wraps the existing COM property reads with `Stopwatch` and emits log lines; it adds no new awaits to the startup path.
- `Stopwatch` only. NO banned APIs: `DateTime.Now`, `DateTime.UtcNow`, `Random.Shared`, `Thread.Sleep`, `Task.Delay` (enforced by BannedApiAnalyzers / `BannedSymbols.txt`). Target framework net48.
- Use the existing `log4net` logger already present in `StoresWrapper`; emit a distinct `[store-filter]` tag. Keep ALL existing instrumentation untouched: `[ui-heartbeat]`, `[gc-delta]`, `[continuation-resume]`, `[engine-init]`, `[startup-lifetime-heartbeat]`, and the `StoreWrapper` `[Startup timing]` lines (including `Init`/`GetSmtpAddressFromStore`). Do NOT touch `PreserveReferencesHandling.All`.
- All touched files (production AND test) MUST remain `<= 500` lines. Baselines: `StoresWrapper.cs` ~267 lines; the new `StoreFilterAttribution.cs` and `StoreFilterAttributionTests.cs` start empty. If a change would push `StoresWrapper.cs` over 500 lines, extract instrumentation glue rather than inflate the file.
- Decompose for testability: the PURE `[store-filter]` line formatter (given displayName, exchangeStoreTypeMs, filePathMs, included bool, matched-rule enum) AND the PURE include/exclude decision (given already-read primitive property values, returning included bool + matched-rule enum) live in `StoreFilterAttribution` (coverable, NOT `[ExcludeFromCodeCoverage]`) and are covered by deterministic MSTest with no live COM/timer/filesystem/network and no temporary files. The COM property reads + `Stopwatch` + per-store guarded `DisplayName` read stay in `StoresWrapper` (COM-bound). Do NOT add live-COM/timer logic to any coverable type.
- New code `>= 90%` coverage; repository-wide line coverage `>= 80%` with no regression versus baseline.
- C# toolchain order mandatory per implementation task and at final QA: CSharpier -> .NET analyzers -> nullable/TWAE -> MSTest with coverage (`/TestCaseFilter:"TestCategory!=LiveOutlook"`). Restart the loop from CSharpier if any step changes files or fails.
- If a `protected internal virtual` seam is added to `StoresWrapper` (which has existing test subclasses/Moq usage in `StoresWrapperTests.cs`), include a task to update those overrides so the suite still compiles. Verify whether `StoresWrapperTests.cs` subclasses `StoresWrapper` before relying on this.

## Scope-lock (files this plan may modify or create)

- MODIFY `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs` (filter-path Stopwatch instrumentation + sync-`Init` GetFilteredStores summary).
- CREATE `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs` AND wire `<Compile Include="OutlookObjects\Store\StoreFilterAttribution.cs" />` into `UtilitiesCS/UtilitiesCS.csproj`.
- CREATE `UtilitiesCS.Test/OutlookObjects/Store/StoreFilterAttributionTests.cs` AND wire `<Compile Include="OutlookObjects\Store\StoreFilterAttributionTests.cs" />` into `UtilitiesCS.Test/UtilitiesCS.Test.csproj`.
- MODIFY `UtilitiesCS/UtilitiesCS.csproj` and `UtilitiesCS.Test/UtilitiesCS.Test.csproj` (Compile Include wiring only).
- No other files in scope. Do NOT modify `StoreWrapper.cs`.

## Acceptance criterion introduced by this plan

- **AC16 — Per-store filter attribution probe (diagnosis-only).** For each store enumerated by `StoresWrapper.GetFilteredStores()`/`ShouldIncludeStore`, exactly one structured `[store-filter]` line is emitted via the existing `log4net` logger capturing: the store DisplayName (guarded for null/COM failure), the `Stopwatch` ms to read `ExchangeStoreType`, the `Stopwatch` ms to read `FilePath`, the final include/exclude boolean, and which rule produced the exclusion (one of `PublicFolder` / `NameContains` / `GwsoFilePath` / `FilePathContains` / `Included`). On the SYNCHRONOUS `Init()` path, a `GetFilteredStores`-level summary line (count of stores enumerated and total ms) is also emitted (the async `RewireOlObjectsAsync` path already logs its own summary and is left unchanged). Behavior is preserved: identical filter result, identical included store set, identical enumeration order, identical predicate short-circuit semantics. `Stopwatch` only; no banned APIs; net48; all touched files `<= 500` lines; `PreserveReferencesHandling.All` and all existing instrumentation tags untouched. The PURE `[store-filter]` line formatter and the PURE include/exclude decision (over already-read primitive property values, returning the included bool + matched-rule enum) live in the coverable `StoreFilterAttribution` helper (NOT `[ExcludeFromCodeCoverage]`) and are covered by deterministic MSTest (MSTest + Moq + FluentAssertions) with no live COM/timer/filesystem/network and no temporary files. New code reaches `>= 90%` coverage with no repository-wide regression below `80%`; the full C# toolchain passes in order. A maintainer cold-start capture records the `[store-filter]` lines during a slow startup so the largest `FilePath`/`ExchangeStoreType` read time and the Gmail store's include/exclude decision are identifiable.

## Evidence location invariant

All evidence artifacts MUST be written under `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Non-canonical `artifacts/...` evidence paths are forbidden; if any caller instruction supplies one, reject it, substitute the canonical path, and record `EVIDENCE_LOCATION_OVERRIDE_REJECTED`.

---

### Phase 0 — Policy Read and Baseline Capture

- [x] [P0-T1] Read policy files in the required order and record a Phase 0 evidence artifact at `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/phase0-instructions-read-2026-06-24T14-30.md` containing `Timestamp:`, `Policy Order:` (CLAUDE.md -> `.claude/rules/general-code-change.md` -> `.claude/rules/general-unit-test.md` -> `.claude/rules/csharp.md`), and the explicit list of files read. Acceptance: the artifact exists and lists all four files in order.
- [x] [P0-T2] Capture the baseline line counts of the affected files. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/baseline-file-size-2026-06-24T14-30.md` with `Timestamp:`, `Command:` (PowerShell `(Get-Content <path>).Length`), `EXIT_CODE:`, and `Output Summary:` reporting line counts for `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs` and `UtilitiesCS.Test/OutlookObjects/Store/StoresWrapperTests.cs`, and noting that `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs` and `UtilitiesCS.Test/OutlookObjects/Store/StoreFilterAttributionTests.cs` do not yet exist (0 lines). Acceptance: existing counts recorded and each is `<= 500`; non-existence of the two new files is recorded.
- [x] [P0-T3] Capture the csproj wiring baseline confirming the legacy `packages.config` + explicit `<Compile Include>` pattern for BOTH projects. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/baseline-csproj-wiring-2026-06-24T14-30.md` with `Timestamp:`, `Command:` (grep for `<Compile Include` and `packages.config`), `EXIT_CODE:`, and `Output Summary:` recording: (a) `UtilitiesCS/UtilitiesCS.csproj` already has `<Compile Include="OutlookObjects\Store\StoresWrapper.cs" />` and `StoreWrapper.cs` and uses explicit includes (no glob); (b) `UtilitiesCS.Test/UtilitiesCS.Test.csproj` already has `<Compile Include="OutlookObjects\Store\StoresWrapperTests.cs" />` and uses explicit includes (no glob); (c) therefore the two NEW `.cs` files (`StoreFilterAttribution.cs`, `StoreFilterAttributionTests.cs`) WILL each require a new `<Compile Include>` item to compile. Acceptance: artifact confirms both projects use explicit includes and records that two new `<Compile Include>` items are required.
- [x] [P0-T4] Run CSharpier in check mode and capture the baseline. Command: `dotnet tool run csharpier . --check`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/baseline-csharpier-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact records exit code and whether the tree is already formatted.
- [x] [P0-T5] Run the analyzer build and capture the baseline. Command: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/baseline-analyzers-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (warning/error counts). Acceptance: artifact records the build result.
- [x] [P0-T6] Run the nullable/TWAE build and capture the baseline. Command: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/baseline-nullable-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact records the build result.
- [x] [P0-T7] Run the MSTest suite with coverage and capture the baseline coverage headline. Command: `vstest.console.exe <UtilitiesCS.Test assembly path> /EnableCodeCoverage /TestCaseFilter:"TestCategory!=LiveOutlook"`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/baseline/baseline-tests-coverage-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric repo-wide line-coverage percent and the `StoresWrapper` module/file coverage percent. Acceptance: numeric baseline coverage values recorded (no `UNVERIFIED` placeholder).
- [x] [P0-T8] Confirm whether `UtilitiesCS.Test/OutlookObjects/Store/StoresWrapperTests.cs` subclasses `StoresWrapper` or overrides any virtual member (e.g., `Init`, `GetFilteredStores`, `ShouldIncludeStore`). Record the finding in the P0-T3 artifact (or an inline task note). Acceptance: it is recorded whether existing test subclasses exist; if they do, P2/P3 tasks that add or alter a virtual seam must include matching override updates.

### Phase 1 — Pure attribution + decision logic in `StoreFilterAttribution` (coverable, new file)

- [x] [P1-T1] Create `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs` in namespace `UtilitiesCS.OutlookObjects.Store` containing a public enum `StoreFilterRule` with members `PublicFolder`, `NameContains`, `GwsoFilePath`, `FilePathContains`, `Included` (in predicate short-circuit order, with `Included` last). In the SAME task, wire `<Compile Include="OutlookObjects\Store\StoreFilterAttribution.cs" />` into `UtilitiesCS/UtilitiesCS.csproj` (legacy packages.config, explicit includes, no glob). Acceptance: the file exists with the five-member enum, the `<Compile Include>` item is present in `UtilitiesCS.csproj`, the project builds (file compiles into `UtilitiesCS.dll`), no banned APIs, and the file is `<= 500` lines.
- [x] [P1-T2] In `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs`, add a PURE static decision method, e.g. `public static (bool Included, StoreFilterRule Rule) Decide(bool isPublicFolder, string displayName, string filePath, IList<string> excludedStoreNameContains, IList<string> excludedStoreFilePathContains, IList<string> gwsoFilePathContains, bool excludePublicFolderStores, bool excludeGwsoStores)`. It MUST evaluate the exact same conditions in the exact same short-circuit order as `StoresWrapper.ShouldIncludeStore` (PublicFolder -> NameContains -> GwsoFilePath -> FilePathContains -> default Included), using `StringComparison.OrdinalIgnoreCase` and the same null/whitespace guards, returning `(false, <matched rule>)` on the first matching exclusion and `(true, StoreFilterRule.Included)` otherwise. It accepts already-read primitive values only (no `Outlook.Store`, no COM, no `Stopwatch`, no clock). Acceptance: method exists, is pure, mirrors the existing predicate's branch order and comparison semantics exactly, references no COM/`Stopwatch`/banned APIs, and the file remains `<= 500` lines.
- [x] [P1-T3] In `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs`, add a PURE static formatter, e.g. `public static string FormatLine(string displayName, double exchangeStoreTypeMs, double filePathMs, bool included, StoreFilterRule rule)` that returns exactly one `[store-filter] displayName=<guarded> exchangeStoreTypeMs=<F1> filePathMs=<F1> included=<true|false> rule=<StoreFilterRule>` line, formatting the two ms fields with `"F1"` and `CultureInfo.InvariantCulture`, rendering a null/empty displayName as `<null>`, and rendering `rule` as the enum name. Acceptance: the method produces the single `[store-filter]`-tagged line with the five fields in the stated order and F1 InvariantCulture formatting; no banned APIs; file remains `<= 500` lines.
- [x] [P1-T4] Run CSharpier on the new production file (`dotnet tool run csharpier .`). Acceptance: file formatter-clean (no diff on re-run).

### Phase 2 — Stopwatch-wrapped COM reads + emission in `StoresWrapper` (COM-bound, thin glue)

- [x] [P2-T1] In `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs`, add a private instance helper, e.g. `private bool ShouldIncludeStoreInstrumented(Outlook.Store store)`, that: (a) reads `store.DisplayName` under a guarded `try/catch` into a local; (b) wraps the `store.ExchangeStoreType` read in its own `Stopwatch` capturing `exchangeStoreTypeMs`; (c) wraps the `store.FilePath` read in its own `Stopwatch` (preserving the existing `try { } catch { }` guard) capturing `filePathMs`; (d) calls `StoreFilterAttribution.Decide(...)` with those already-read primitive values and the existing instance configuration lists/flags (`ExcludePublicFolderStores`, `ExcludedStoreNameContains`, `ExcludedStoreFilePathContains`, `GwsoFilePathContains`, `ExcludeGwsoStores`); (e) emits one line via `logger.Debug(StoreFilterAttribution.FormatLine(...))`; and (f) returns the decision's `Included` boolean. The branch order and comparison semantics MUST be identical to the current `ShouldIncludeStore` so the included set and order are unchanged. `Stopwatch` only; no banned APIs. Acceptance: the instrumented predicate returns the same boolean as `ShouldIncludeStore` for the same inputs, emits exactly one `[store-filter]` line per store, uses `Stopwatch` only, and `StoresWrapper.cs` remains `<= 500` lines.
- [x] [P2-T2] In `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs`, route `GetFilteredStores()` (lines 119-122) through `ShouldIncludeStoreInstrumented` instead of `ShouldIncludeStore` so every enumerated store is attributed, WITHOUT changing the enumeration source (`Globals.Ol.NamespaceMAPI.Stores.Cast<Outlook.Store>()`) or the result set. Keep the original `ShouldIncludeStore` method in place (it is `public` and may be referenced by tests); do not delete it. Acceptance: `GetFilteredStores` uses the instrumented predicate; enumeration source and included set are unchanged; the existing `ShouldIncludeStore` public method is retained.
- [x] [P2-T3] In `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs` `Init()` (lines 35-39), add a `GetFilteredStores`-level summary on the SYNCHRONOUS path only: start a `Stopwatch`, materialize the filtered stores into a list, emit one `[store-filter] GetFilteredStores completed: <count> stores in <ms> ms` summary line via `logger.Debug`, then proceed to build `Stores` from that materialized list. The included set and order MUST be identical to the current `GetFilteredStores().Select(...)`. Do NOT add a summary to `RewireOlObjectsAsync` (it already logs its own `[Startup timing] GetFilteredStores completed` line). `Stopwatch` only; no banned APIs. Acceptance: `Init()` emits exactly one synchronous-path summary line tagged `[store-filter]`; the resulting `Stores` list is built from the same filtered set in the same order; `RewireOlObjectsAsync` is untouched; `StoresWrapper.cs` remains `<= 500` lines.
- [x] [P2-T4] In `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs`, add a short `// why` comment at the instrumentation site stating the `[store-filter]` attribution is diagnosis-only (issue #211, Phase 3.4), measures the previously-untimed filter path (per-store `ExchangeStoreType`/`FilePath` read times and include/exclude decision), and is to be removed or gated after diagnosis. Confirm by diff review that `PreserveReferencesHandling.All` is not referenced here and that the `StoreWrapper` `[Startup timing]` lines, `[ui-heartbeat]`, `[gc-delta]`, `[continuation-resume]`, `[engine-init]`, and `[startup-lifetime-heartbeat]` instrumentation are not modified. Acceptance: the diagnostic-only rationale comment is present; diff review confirms no change to other instrumentation tags, no change to `StoreWrapper.cs`, and no change to `PreserveReferencesHandling.All`.
- [x] [P2-T5] If P2-T1/P2-T2 introduce or alter any `virtual`/`protected internal virtual` member on `StoresWrapper` AND P0-T8 found existing test subclasses/overrides, update those overrides in `UtilitiesCS.Test/OutlookObjects/Store/StoresWrapperTests.cs` so the suite compiles and existing behavior is preserved. If no new virtual seam was added (the instrumented predicate is a private non-virtual helper), record that no override update is required. Acceptance: either the overrides are updated to match the new seam, or it is recorded that no virtual seam was added and no override change is needed.
- [x] [P2-T6] Run CSharpier on the touched production file(s) (`dotnet tool run csharpier .`). Acceptance: file(s) formatter-clean (no diff on re-run).

### Phase 3 — Deterministic seam tests (AC16, new test file)

- [x] [P3-T1] Create `UtilitiesCS.Test/OutlookObjects/Store/StoreFilterAttributionTests.cs` in namespace `UtilitiesCS.Test.OutlookObjects.Store` with a `[TestClass]` `StoreFilterAttributionTests` using MSTest + FluentAssertions. In the SAME task, wire `<Compile Include="OutlookObjects\Store\StoreFilterAttributionTests.cs" />` into `UtilitiesCS.Test/UtilitiesCS.Test.csproj` (legacy packages.config, explicit includes, no glob). Acceptance: the file exists with the test class skeleton, the `<Compile Include>` item is present in `UtilitiesCS.Test.csproj`, the test project compiles, and the file is `<= 500` lines.
- [x] [P3-T2] In `StoreFilterAttributionTests.cs`, add deterministic `[TestMethod]`s for `StoreFilterAttribution.Decide` covering each branch in order: (a) public-folder store with `excludePublicFolderStores=true` returns `(false, PublicFolder)`; (b) DisplayName containing an `ExcludedStoreNameContains` token returns `(false, NameContains)`; (c) FilePath containing a `GwsoFilePathContains` token (e.g., `\Google\Google Workspace Sync\`) with `excludeGwsoStores=true` returns `(false, GwsoFilePath)`; (d) FilePath containing an `ExcludedStoreFilePathContains` token returns `(false, FilePathContains)`; (e) a normal store returns `(true, Included)`. Each test uses AAA structure and FluentAssertions; no live COM/timer/filesystem/network; no temporary files. Acceptance: all five branches asserted with the correct matched rule.
- [x] [P3-T3] In `StoreFilterAttributionTests.cs`, add deterministic `[TestMethod]`s for short-circuit precedence and edge cases of `Decide`: (a) a store matching BOTH PublicFolder and a later rule returns `PublicFolder` (earliest rule wins); (b) null/empty `displayName` and null/empty `filePath` do not throw and are treated as non-matching for the contains rules; (c) when `excludeGwsoStores=false`, a Gmail-style FilePath is NOT excluded by the GWSO rule (mirroring the predicate's flag guard). Acceptance: precedence and null/whitespace/flag-guard edge cases asserted; AAA; FluentAssertions.
- [x] [P3-T4] In `StoreFilterAttributionTests.cs`, add deterministic `[TestMethod]`s for `StoreFilterAttribution.FormatLine`: assert the line starts with `[store-filter] `, contains `displayName=<value>` (and `<null>` for null/empty), `exchangeStoreTypeMs` and `filePathMs` formatted F1 InvariantCulture (assert with a known value such as `12.0`), `included=true`/`included=false`, and `rule=<EnumName>` for at least two distinct rules. Acceptance: tag, all five fields, F1 InvariantCulture formatting, and the `<null>` displayName rendering are asserted; AAA; FluentAssertions.
- [x] [P3-T5] Confirm `UtilitiesCS.Test/OutlookObjects/Store/StoreFilterAttributionTests.cs` is referenced by the `<Compile Include>` item added in P3-T1 (re-verify the item exists in `UtilitiesCS.Test.csproj`). Acceptance: wiring confirmed; the test class is discovered and executed by vstest (verified in P3-T7).
- [x] [P3-T6] Run CSharpier on the new test file (`dotnet tool run csharpier .`). Acceptance: file formatter-clean (no diff on re-run).
- [x] [P3-T7] Run the MSTest suite with coverage and confirm the new tests pass. Command: `vstest.console.exe <UtilitiesCS.Test assembly path> /EnableCodeCoverage /TestCaseFilter:"TestCategory!=LiveOutlook"`. Acceptance: new tests pass; no pre-existing tests regress.

### Phase 4 — Maintainer store-filter cold-start capture (runtime, evidence-gated)

- [x] [P4-T1] Write maintainer capture instructions to `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/other/coldstart-store-filter-capture-instructions-2026-06-24T14-30.md`. Instructions MUST specify: enable the startup timing/diagnostic logging used for the existing `[Startup timing]` lines; perform a non-debugger COLD start of Outlook with the Gmail/GWSO store present; capture the full startup window in DebugView; the expected line shape `[store-filter] displayName=<...> exchangeStoreTypeMs=<F1> filePathMs=<F1> included=<true|false> rule=<PublicFolder|NameContains|GwsoFilePath|FilePathContains|Included>` appearing once per enumerated store, plus the synchronous-`Init` summary line `[store-filter] GetFilteredStores completed: <count> stores in <ms> ms`; confirmation that the existing `[Startup timing]` `StoreWrapper.Init`/`GetSmtpAddressFromStore` lines, `[ui-heartbeat]`, `[gc-delta]`, `[continuation-resume]`, `[engine-init]`, and `[startup-lifetime-heartbeat]` lines still appear. The analysis goal: identify whether reading `FilePath` or `ExchangeStoreType` on the Gmail store shows a large ms value (indicating the filter path blocks the STA), and whether the Gmail store is being `included=true`. Acceptance: instructions file exists and enumerates the expected line shapes and the analysis goal.
- [x] [P4-T2] Create the evidence placeholder `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/other/runtime-capture-store-filter-PLACEHOLDER.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` schema fields and a `PENDING MAINTAINER CAPTURE` header, to be replaced by the dated capture artifact once collected. This is a runtime maintainer task not executed by the automated toolchain. Acceptance: placeholder exists with the schema fields and clearly marks the capture as pending.

### Phase 5 — Final QA Loop (full C# toolchain, in order) + coverage delta

- [x] [P5-T1] Run CSharpier across the repo and capture evidence. Command: `dotnet tool run csharpier .`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/final-qc-csharpier-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If files change, restart the loop from this step. Acceptance: formatter-clean pass recorded.
- [x] [P5-T2] Run the analyzer build and capture evidence. Command: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/final-qc-analyzers-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: no new analyzer errors; result recorded.
- [x] [P5-T3] Run the nullable/TWAE build and capture evidence. Command: `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/final-qc-nullable-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: no nullable/TWAE errors; result recorded.
- [x] [P5-T4] Run the MSTest suite with coverage and capture evidence. Command: `vstest.console.exe <UtilitiesCS.Test assembly path> /EnableCodeCoverage /TestCaseFilter:"TestCategory!=LiveOutlook"`. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/final-qc-tests-coverage-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change repo-wide line-coverage percent and the `StoreFilterAttribution` new-code coverage percent, plus pass/fail counts. Acceptance: all tests pass; numeric post-change coverage recorded.
- [x] [P5-T5] Verify the coverage delta and thresholds. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/final-qc-coverage-delta-2026-06-24T14-30.md` reporting baseline coverage (from P0-T7), post-change coverage (from P5-T4), and new/changed-code coverage for the `StoreFilterAttribution` additions. Acceptance: repo-wide line coverage `>= 80%` with no regression versus baseline, and new/changed code `>= 90%`; if any threshold is unmet, mark the plan outcome remediation-required (not PASS).
- [x] [P5-T6] Verify final file sizes. Write `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/final-qc-filesize-2026-06-24T14-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` reporting final line counts for `UtilitiesCS/OutlookObjects/Store/StoresWrapper.cs`, `UtilitiesCS/OutlookObjects/Store/StoreFilterAttribution.cs`, and `UtilitiesCS.Test/OutlookObjects/Store/StoreFilterAttributionTests.cs`. Acceptance: every touched file `<= 500` lines.
- [x] [P5-T7] Record the AC16 check-off summary at `docs/features/active/2026-06-22-outlook-startup-intelconfig-continuation-stall-211/evidence/qa-gates/p5-acceptance-criteria-checkoff-2026-06-24T14-30.md`, mapping AC16 to the satisfying tasks (matched-rule enum + pure decision: P1-T1/P1-T2; pure line formatter: P1-T3; Stopwatch-wrapped per-store COM reads + emission: P2-T1/P2-T2; synchronous-`Init` GetFilteredStores summary: P2-T3; unchanged existing instrumentation and `PreserveReferencesHandling.All`: P2-T4; virtual-seam override handling: P2-T5; deterministic tests: P3-T2..P3-T4; csproj wiring: P1-T1/P3-T1; coverage/toolchain/file-size: P5-T1..P5-T6) and noting the runtime portion (P4-T1/P4-T2) as maintainer-gated. Acceptance: AC16 mapped to evidence; runtime portion noted as maintainer-gated.
