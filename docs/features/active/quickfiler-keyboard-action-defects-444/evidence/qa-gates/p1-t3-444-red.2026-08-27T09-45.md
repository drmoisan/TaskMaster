# [P1-T3] `[expect-fail]` #444 duplicate-guard test — RED before the fix

Timestamp: 2026-08-27T09-45
Command: `& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~KbdActionsRemainingBranchesTests.EnumerableConstructor_WhenSeedContainsDuplicateSourceAndStoredKey_ThrowsArgumentException"`
EXIT_CODE: 1
ExpectedExitCode: 1

A failing test is the expected outcome for this task only. This is the fail-before half of
`CLAUDE.md`'s Bugfix Workflow step 1 for issue #444.

## Result (verbatim)

```
Failed EnumerableConstructor_WhenSeedContainsDuplicateSourceAndStoredKey_ThrowsArgumentException [140 ms]

Total tests: 1
     Failed: 1
Test Run Failed.
```

| Measure | Value |
| --- | --- |
| Total | 1 |
| Passed | **0** |
| Failed | **1** |

## Recorded failure reason (verbatim)

```
Error Message:
   Expected a <System.ArgumentException> to be thrown because a seed sequence repeating one source
   and stored key violates the registry invariant, but no exception was thrown.
```

The failure reason is that **no exception was thrown**. That is exactly the defect #444 files: the
`KbdActions(IEnumerable<UClass>)` constructor materialises the seed with
`_list = new List<UClass>(list);` and performs no duplicate check, so a seed repeating
`("src", Keys.Down)` is admitted silently. The failure is a clean deterministic red, not an incidental
arrangement error: the assertion was reached and evaluated.

## Acceptance evaluation

- The run reports `Failed: 1` and `Passed: 0`. PASS.
- The recorded failure reason is that no exception was thrown. PASS.

Output Summary: 1 test run, 1 failed, 0 passed; failure reason is "no exception was thrown"; the
expected RED state for issue #444 is captured.
