# [P3-T27] AC-482-08 — Phase 3 half recorded, check-off deferred to Phase 4

Timestamp: 2026-08-27T09-45
EXIT_CODE: 0

Criterion (`### Issue #482` ordinal 8, `spec.md` line 1129):

> `SyncExpandedRegistrations` is exercised directly for both `true` and `false` through
> `QfcItemControllerTestSupport.InvokeNonPublic` by a named test, and reaches `>= 90%` line coverage as
> a new member.

## Why this criterion cannot be checked off in Phase 3

The criterion is **conjunctive**. Its two clauses are measured by different tasks:

| Clause | Measured by | Status at this point |
| --- | --- | --- |
| exercised directly for both `true` and `false` through `QfcItemControllerTestSupport.InvokeNonPublic` by a named test | `[P3-T13]` | **satisfied** |
| reaches `>= 90%` line coverage as a new member | `[P4-T9]` | not yet measured |

`[P4-T9]` has not run at this point in the task order, so the coverage figure does not exist. This task
therefore does not branch on a figure that cannot yet exist: it leaves the checkbox `- [ ]` with its
text unmodified and records the deferral.

No `REMEDIATION-REQUIRED` line is written. A figure that has not yet been measured is not an unmet
criterion.

## Evidence for the satisfied clause

`[P3-T13]` ran
`QuickFiler.Controllers.Tests.QfcItemController_NavigationTests.SyncExpandedRegistrations_WhenInvokedWithTrueThenFalse_AddsThenRemovesBothRegistries`
and recorded it as `Passed [1 ms]` within a 4-passed, 0-failed run. That test drives the private member
through `QfcItemControllerTestSupport.InvokeNonPublic(c, "SyncExpandedRegistrations", true)` and then
`…, false)`, asserting after the first call that both registries hold exactly one `'B'` and one `'D'`
entry and after the second that both hold none.

DEFERRED-TO-PHASE-4: AC-482-08 direct-exercise clause satisfied by [P3-T13]; the coverage clause is measured by [P4-T9] and the check-off is performed by [P4-T20]

## On-disk state of the criterion

AC-482-08 in `docs/features/active/quickfiler-keyboard-action-defects-444/spec.md` is still `- [ ]`
with its text byte-identical to its pre-execution form.

Output Summary: AC-482-08's direct-exercise clause is satisfied and evidenced by `[P3-T13]`; its
coverage clause is unmeasured until `[P4-T9]`; the checkbox is left unchecked and `[P4-T20]` will
discharge this deferral.
