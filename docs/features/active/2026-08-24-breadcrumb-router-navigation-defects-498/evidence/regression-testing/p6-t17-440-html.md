# P6-T17 — #440 FolderBreadcrumb.html arrow reconciliation

Timestamp: 2026-08-26T10-05

Command:

```
pwsh -NoProfile -Command '& "scripts\vscode\Invoke-VSBuild.ps1" -Target Build; $vsw = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"; $vs = & $vsw -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe" | Select-Object -First 1; & $vs "QuickFiler.Test\bin\Debug\QuickFiler.Test.dll" /InIsolation "/TestCaseFilter:FullyQualifiedName~FolderBreadcrumbAssetContractTests" "/Logger:trx;LogFileName=results.trx" "/ResultsDirectory:docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t17"; "EXIT_CODE: $LASTEXITCODE"'
```

EXIT_CODE: 0

TRX: `<repo-root>/docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t17/results.trx`

Supporting commands:

```
pwsh -NoProfile -Command '(Get-Content -LiteralPath "QuickFiler\Resources\FolderBreadcrumb.html").Count'
git status --porcelain --untracked-files=all -- QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs
```

Output Summary:

- TRX `<Counters total="15" executed="15" passed="15" failed="0" ... />`. The complete `FolderBreadcrumbAssetContractTests` class passes, including `LeftAndRightBreadcrumbMessages_RemainSupported`.
- `QuickFiler/Resources/FolderBreadcrumb.html` measures 490 lines, at or under the 500-line limit (489 before this task; net +1 line, within the 11 lines of headroom the plan allotted).
- `git status --porcelain --untracked-files=all -- QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs` produced no output: the must-not-write contract test class is unmodified.
- Change made: `onArrow` no longer pre-filters a suggestion row by its view state. It previously posted `arrowKey` only when the row was collapsed (Right) or leaf-expanded (Left), which suppressed both #440 tree transitions before they could reach the C# router. It now posts `arrowKey` for any selected suggestion row and leaves the unhandled determination to `FolderBreadcrumbBridgeRouter.ArrowAsync`, which already emits `unhandledArrow` when no transition applies. The `arrowKey` and `unhandledArrow` message shapes are byte-identical to their previous form, as `LeftAndRightBreadcrumbMessages_RemainSupported` pins.
- No degradation branch was taken. The `P0-T15` `BASELINE_FAILURE_SET` is empty, so the primary acceptance condition (`failed 0`) applies absolutely and was met.
