# P1-T8 [expect-fail] — RED for #486 D2, the three dead ItemViewer members

Timestamp: 2026-08-28T00-26
Command: & $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation "/Logger:trx;LogFileName=p1-t8.trx" "/TestCaseFilter:FullyQualifiedName~ItemViewer_DeclaresNoMenuItemCheckedChangedMembers|FullyQualifiedName~ItemViewer_DeclaresNoMoveOptionsMenuClickHandler" /ResultsDirectory:docs\features\active\itemviewer-surface-defects-489\evidence\regression-testing
EXIT_CODE: 1
ExpectedExitCode: 1

Ran: 2
Failed: 2
Passed: 0
Skipped: 0

## Acceptance — this is a `[expect-fail]` task and both failures are the required outcome

`Total tests: 2 / Failed: 2 / Test Run Failed.` Exactly two tests ran, both failed, none passed.
`vstest.console.exe` exits non-zero whenever an executed test fails, so `ExpectedExitCode: 1` is
declared and the artifact normalizes to `pass`.

```
Failed ItemViewer_DeclaresNoMenuItemCheckedChangedMembers [130 ms]
Failed ItemViewer_DeclaresNoMoveOptionsMenuClickHandler   [< 1 ms]
```

## The failure messages name each surviving member

```
Expected eventHandlerForm to be <null> because the EventHandler-shaped overload is dead code on
ItemViewer, but found ItemViewer.MenuItem_CheckedChanged.
Expected typedForm to be <null> because the typed overload is dead code on ItemViewer, but found
ItemViewer.MenuItem_CheckedChanged.
```

```
Expected handler to be <null> because the empty MoveOptionsMenu_Click body has no caller and no
designer wiring, but found ItemViewer.MoveOptionsMenu_Click.
```

Three surviving members are named across the two tests, matching the three declarations at
`QuickFiler/Viewers/ItemViewer.cs:171-175`, `:177-187` and `:205` that P2-T4 deletes. Both tests
live in `QuickFiler.Test/Viewers/ItemViewerBreadcrumbDropDownContractTests.cs`, whose `[TestMethod]`
count rose from 5 to 7 at P1-T3.

TRX artifact: `evidence/regression-testing/p1-t8.trx`, sanitised in place — 6 occurrences of the
worktree root to `<repo-root>`, 5 of the machine name to `<host>`, 3 of the account name to
`<user>`; a case-insensitive search for either identifier returns 0.

Output Summary: The RED for #486 D2 **fails as required**. Exactly 2 tests ran, 2 failed, 0 passed,
0 skipped, `EXIT_CODE: 1` against `ExpectedExitCode: 1`. The messages name all three dead members
still declared on `QuickFiler.ItemViewer` — the `EventHandler`-shaped `MenuItem_CheckedChanged`
overload, the typed `ToolStripMenuItem` overload, and the empty-bodied `MoveOptionsMenu_Click`.
P2-T4 deletes all three and P2-T8 re-runs both tests green.

**XML-escaping note.** The placeholder is written into the TRX in entity form as `&lt;repo-root&gt;` (likewise `&lt;host&gt;` and `&lt;user&gt;`).
XML forbids a raw less-than character in a text node or an attribute value, so writing the
five literal characters would make the document unparseable; an XML reader decodes the
entity form back to the required literal. Each sanitised TRX was re-verified with a strict
parser and its UnitTestResult element count matches the test total recorded above.
