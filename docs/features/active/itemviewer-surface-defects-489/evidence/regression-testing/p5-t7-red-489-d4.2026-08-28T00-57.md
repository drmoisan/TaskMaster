# P5-T7 [expect-fail] — RED for #489 D4, the unconsumed `UiScheduler` interface member

Timestamp: 2026-08-28T00-57
Command: & $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation "/Logger:trx;LogFileName=p5-t7.trx" "/TestCaseFilter:FullyQualifiedName~IItemViewer_DeclaresNoUiSchedulerMember" /ResultsDirectory:<temp>
EXIT_CODE: 1
ExpectedExitCode: 1

Ran: 1
Failed: 1
Passed: 0
Skipped: 0

## Acceptance — this is a `[expect-fail]` task and the failure is the required outcome

`Total tests: 1 / Failed: 1 / Test Run Failed.` Exactly one test ran, it failed, and none passed.
`vstest.console.exe` exits non-zero whenever an executed test fails, so `ExpectedExitCode: 1` is
declared and the artifact normalizes to `pass`.

## The failure message names the surviving member

```
Failed IItemViewer_DeclaresNoUiSchedulerMember [135 ms]
  Expected members to be empty because no production consumer reaches UiScheduler through the viewer
  interface, but found at least one item {IItemViewer.UiScheduler}.
```

The test failed because `UiScheduler` is still declared on the interface, and FluentAssertions names
the exact member found: `{IItemViewer.UiScheduler}`. It did not fail for an unrelated reason such as
a missing type or an assembly load failure — the reflection lookup resolved `IItemViewer` and
returned a non-empty member set.

The declaration under test is `TaskScheduler UiScheduler { get; }` at `QuickFiler/Viewers/IItemViewer.cs:37`,
sitting between `Dispatcher UiDispatcher { get; }` at `:36` and
`SynchronizationContext UiSyncContext { get; }` at `:38`. The lookup uses
`GetMember("UiScheduler", BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public)`,
the same form as the Phase 3 REDs.

## Over-deletion is separately guarded

The two pins `IItemViewer_StillDeclaresUiDispatcher` and `IItemViewer_StillDeclaresUiSyncContext`
both **passed** at P5-T5 against this same unmodified interface. They exist because P6-T2 removes
`:37` from a three-line block whose other two members still have live production consumers, and they
are what make an over-broad deletion detectable rather than silently green.

P6-T2 removes the declaration and P6-T5 re-runs this test together with the two pins and the three
D2 tests.

## TRX artifact

`evidence/regression-testing/p5-t7.trx`, sanitised with the same case-insensitive, XML-entity
substitution scheme used at P3-T3, P4-T6, P5-T5 and P5-T6. After redaction the file parses as XML,
its `<UnitTestResult>` count is **1** — matching the `Ran: 1` recorded above — its `ResultSummary`
counters read `total=1 passed=0 failed=1`, and a case-insensitive search for the account name, the
short 8.3 account name and the machine name returns **0** residual occurrences.

Output Summary: The #489 D4 regression test fails as required. `Total tests: 1 / Failed: 1`,
0 passed, 0 skipped, `EXIT_CODE: 1` against `ExpectedExitCode: 1`. The FluentAssertions message names
the surviving member `{IItemViewer.UiScheduler}`, proving the RED is caused by the still-declared
interface member at `IItemViewer.cs:37` and not by any unrelated fault. The two neighbouring
over-deletion pins passed at P5-T5, so a Phase 6 fix that removed too much from the same block would
be caught.
