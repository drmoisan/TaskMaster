# P3-T3 [expect-fail] — RED for #487 D1, the dead ParentChanged handlers

Timestamp: 2026-08-28T00-42
Command: & $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation "/Logger:trx;LogFileName=p3-t3.trx" "/TestCaseFilter:FullyQualifiedName~ItemViewer_DeclaresNoParentChangedHandler|FullyQualifiedName~ItemViewerExpanded_DeclaresNoParentChangedHandler" /ResultsDirectory:<temp>
EXIT_CODE: 1
ExpectedExitCode: 1

Ran: 2
Failed: 2
Passed: 0
Skipped: 0

## Acceptance — this is a `[expect-fail]` task and the failure is the required outcome

`Total tests: 2 / Failed: 2 / Test Run Failed.` in 1.23 seconds. Exactly two tests ran, both failed,
and none passed, which is precisely the acceptance condition. `vstest.console.exe` exits non-zero
whenever an executed test fails, so `ExpectedExitCode: 1` is declared and the artifact normalizes to
`pass`.

The `TestCaseFilter` uses the `|` operator. `vstest.console.exe` rejects the spelled-out `OR`, and
the two clauses are not redundant: `FullyQualifiedName~ItemViewer_DeclaresNoParentChangedHandler`
does not substring-match `ItemViewerExpanded_DeclaresNoParentChangedHandler`, because the expanded
type's test name interposes `Expanded` before the underscore.

## Both failure messages name the surviving member — the intended reason

```
Failed ItemViewer_DeclaresNoParentChangedHandler [138 ms]
  Expected members to be empty because the ParentChanged handler only wrote a console diagnostic and
  has no caller, but found at least one item {ItemViewer.L0v2h2_WebView2_ParentChanged}.

Failed ItemViewerExpanded_DeclaresNoParentChangedHandler [1 ms]
  Expected members to be empty because the expanded viewer's ParentChanged handler is the same dead
  console diagnostic, but found at least one item {ItemViewerExpanded.L0v2h2_WebView2_ParentChanged}.
```

Each test failed because the member it reflects for is still declared, and the FluentAssertions
message names the exact member found — `ItemViewer.L0v2h2_WebView2_ParentChanged` and
`ItemViewerExpanded.L0v2h2_WebView2_ParentChanged`. Neither failed for an unrelated reason such as a
missing type, a binding error, or an assembly load failure: both resolved their target type, ran the
reflection lookup, and reported a non-empty member set.

The assertions are at `QuickFiler.Test/Viewers/ItemViewerBreadcrumbDropDownContractTests.cs:203` and
`:224`, in class `QuickFiler.Test.Viewers.ItemViewerBreadcrumbDropDownContractTests`. The lookup uses
`GetMember(name, BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public)` exactly as
P3-T1 specifies, so it observes the `private` members the source declares at
`QuickFiler/Viewers/ItemViewer.cs:166` and `QuickFiler/Viewers/ItemViewerExpanded.cs:154`.

This is the failure-first evidence for #487 D1. P4-T1 and P4-T3 delete the two members, P4-T2 and
P4-T4 delete the corresponding designer `+=` wirings, and P4-T6 re-runs both tests green.

## TRX artifact

`evidence/regression-testing/p3-t3.trx`, sanitised before being committed. The substitutions are
case-insensitive, because `vstest.console.exe` writes the `storage=` attribute in all-lower-case
while the stack traces carry the original casing; a case-sensitive pass would leave the lower-cased
`storage=` value un-redacted. Each placeholder is substituted in its **XML entity** form
(`&lt;repo-root&gt;`, `&lt;host&gt;`, `&lt;user&gt;`), never as a raw angle bracket, which would make
the document unparseable.

After redaction the file parses as XML, its `<UnitTestResult>` count is **2** — matching the
`Ran: 2` recorded above — its `ResultSummary` counters read `total=2 passed=0 failed=2`, and a
case-insensitive search for the account name, the short 8.3 account name, and the machine name
returns **0** residual occurrences.

The run wrote to a results directory outside the repository, so the `Deploy_<user> <timestamp>`
deployment folders `vstest.console.exe` creates alongside the TRX were never placed under
`evidence/`; only the sanitised TRX was copied in. Three such folders left behind by the Phase 1 and
Phase 2 runs were found under `evidence/regression-testing/` at the start of this batch and removed.
They were untracked and invisible to `git status` because git does not track directories, but their
names embedded the account name.

Output Summary: Both new #487 D1 regression tests fail as required. `Total tests: 2 / Failed: 2`,
0 passed, 0 skipped, `EXIT_CODE: 1` against `ExpectedExitCode: 1`. Each failure message names the
surviving member it found — `ItemViewer.L0v2h2_WebView2_ParentChanged` and
`ItemViewerExpanded.L0v2h2_WebView2_ParentChanged` — proving the RED is caused by the dead handlers
still being declared and not by any unrelated fault. The sanitised TRX parses and its
`<UnitTestResult>` count of 2 matches the recorded totals.
