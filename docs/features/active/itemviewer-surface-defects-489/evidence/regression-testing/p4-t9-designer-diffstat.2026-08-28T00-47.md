# P4-T9 — Neither `.Designer.cs` file was reformatted wholesale

Timestamp: 2026-08-28T00-47
Command: git diff --stat <BASELINE_SHA> -- QuickFiler/Viewers/ItemViewer.Designer.cs QuickFiler/Viewers/ItemViewerExpanded.Designer.cs
EXIT_CODE: 0
ExpectedExitCode: 0

## Acceptance

```
 QuickFiler/Viewers/ItemViewer.Designer.cs         | 1 -
 QuickFiler/Viewers/ItemViewerExpanded.Designer.cs | 5 -----
 2 files changed, 6 deletions(-)
```

The corresponding `--numstat` output:

```
0	1	QuickFiler/Viewers/ItemViewer.Designer.cs
0	5	QuickFiler/Viewers/ItemViewerExpanded.Designer.cs
```

AddedItemViewerDesigner: 0
DeletedItemViewerDesigner: 1
AddedItemViewerExpandedDesigner: 0
DeletedItemViewerExpandedDesigner: 5

`ItemViewer.Designer.cs` shows exactly `1` deleted and `0` added — the single P4-T2 wiring statement.
`ItemViewerExpanded.Designer.cs` shows exactly `5` deleted and `0` added — the cumulative total of
P2-T3's four wirings plus P4-T4's one. Both match the acceptance condition exactly.

## Governing U1 branch

U1Branch: A

P0-T10 recorded **Branch A**: the `BaselineUnformattedSet:` block captured by P0-T9 on the untouched
worktree was empty, so Phase 11 runs the policy form `dotnet tool run csharpier format .` rather than
the explicit-file-list form. Branch B was not selected.

Branch A does not weaken this task's guarantee. `ItemViewer.Designer.cs` is 6223 lines and
`ItemViewerExpanded.Designer.cs` is 816 lines after these deletions, and a wholesale reformat of
either would show thousands of added and deleted lines rather than the six deletions and zero
additions recorded above. Independently, CSharpier 1.2.6 does not process either file at all: a
`csharpier check` invocation naming all four edited production files reported `Checked 2 files`,
because CSharpier detects `*.Designer.cs` as generated by filename and skips it. That skip is a
property of the formatter, not of `.csharpierignore`, whose eight exclusion patterns do not mention
`*.Designer.cs`.

## No line-count ceiling is asserted over either file

Both files were already far above 500 lines at baseline — 6224 and 821 respectively — and this
feature only removes lines from them. Consistent with P0-T15, no "must be under 500 lines" condition
is asserted over either, and none could be satisfied by any edit within this feature's scope.

Output Summary: `git diff --stat` against `<BASELINE_SHA>` shows `ItemViewer.Designer.cs` at 1
deletion and 0 additions, and `ItemViewerExpanded.Designer.cs` at 5 deletions and 0 additions, for 2
files changed and 6 deletions total. Neither generated file was reformatted wholesale; the only
changes are the four P2-T3 wirings and the two Phase 4 `ParentChanged` wirings. The governing U1
branch from P0-T10 is **Branch A**, and CSharpier skips both files by generated-filename detection
regardless, reporting `Checked 2 files` when all four edited production files are named.
