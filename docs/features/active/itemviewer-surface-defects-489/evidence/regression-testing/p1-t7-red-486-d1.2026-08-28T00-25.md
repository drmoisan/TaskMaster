# P1-T7 [expect-fail] — RED for #486 D1, ItemViewerExpanded.MenuItem_CheckedChanged

Timestamp: 2026-08-28T00-25
Command: & $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation "/Logger:trx;LogFileName=p1-t7.trx" "/TestCaseFilter:FullyQualifiedName~ItemViewerExpanded_DeclaresNoMenuItemCheckedChangedHandler" /ResultsDirectory:docs\features\active\itemviewer-surface-defects-489\evidence\regression-testing
EXIT_CODE: 1
ExpectedExitCode: 1

Ran: 1
Failed: 1
Passed: 0
Skipped: 0

## Acceptance — this is a `[expect-fail]` task and the failure is the required outcome

`Total tests: 1 / Failed: 1 / Test Run Failed.` in 1.20 seconds. Exactly one test ran, it failed,
and none passed. `vstest.console.exe` exits non-zero whenever an executed test fails, so
`ExpectedExitCode: 1` is declared and the artifact normalizes to `pass`.

## The failure message names the surviving member

```
Expected eventHandlerForm to be <null> because the EventHandler-shaped overload duplicates the
ToolStripMenuItemCb setter and must be deleted, but found ItemViewerExpanded.MenuItem_CheckedChanged.
Expected typedForm to be <null> because the typed overload clears the image the setter just assigned
and must be deleted, but found ItemViewerExpanded.MenuItem_CheckedChanged.
```

Both clauses of the `AssertionScope` reported, so the test proves both overloads are still present
on `QuickFiler.ItemViewerExpanded`: the `(object sender, EventArgs e)` form and the typed
`(ToolStripMenuItem menuItem)` form. The assertion is at
`QuickFiler.Test/Viewers/ToolStripMenuItemCbTests.cs:162`, in class
`QuickFiler.Test.Viewers.ToolStripMenuItemCbTests`.

This is the failure-first evidence for #486 D1. P2-T2 deletes both overloads and P2-T8 re-runs this
test green. The reflection lookup uses
`BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public` with an explicit parameter
type array, so it observes the `private` members the source declares at
`QuickFiler/Viewers/ItemViewerExpanded.cs:163-167` and `:169-179`.

TRX artifact: `evidence/regression-testing/p1-t7.trx`, sanitised in place — 3 occurrences of the
worktree root to `<repo-root>`, 4 of the machine name to `<host>`, 3 of the account name to
`<user>`; a case-insensitive search for either identifier returns 0.

Output Summary: The RED for #486 D1 **fails as required**. Exactly 1 test ran, 1 failed, 0 passed,
0 skipped, `EXIT_CODE: 1` against `ExpectedExitCode: 1`. The failure message names the surviving
member on both clauses — `but found ItemViewerExpanded.MenuItem_CheckedChanged` for the
`EventHandler`-shaped overload and again for the typed `ToolStripMenuItem` overload — which is the
condition P1-T7 requires. The redundancy this proves is that
`QuickFiler/Viewers/ToolStripMenuItemCb.cs:32-50` already sets and clears the image in its `Checked`
setter, as the four P1-T6 pins confirmed minutes earlier.

**XML-escaping note.** The placeholder is written into the TRX in entity form as `&lt;repo-root&gt;` (likewise `&lt;host&gt;` and `&lt;user&gt;`).
XML forbids a raw less-than character in a text node or an attribute value, so writing the
five literal characters would make the document unparseable; an XML reader decodes the
entity form back to the required literal. Each sanitised TRX was re-verified with a strict
parser and its UnitTestResult element count matches the test total recorded above.
