# 2026-08-07-quickfiler-search-keystroke-focus-steal-438 — Remediation Plan (Cycle 1, R1)

- **Issue:** #438 (https://github.com/drmoisan/TaskMaster/issues/438)
- **Feature folder:** `docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438` (referenced below as `<FEATURE>`)
- **Branch:** `bug/quickfiler-search-keystroke-focus-steal-438` at `ff9d14ab` (clean tree)
- **Remediation input:** `<FEATURE>/remediation-inputs.2026-08-08T13-25.md` (authoritative source for R1 scope and the do-not-do list)
- **Scope:** exactly one blocking finding, R1 — new-file branch coverage below the 75% floor in `QuickFiler/Viewers/BreadcrumbItemViewerLifecycleCoordinator.Search.cs`. R2 is a non-blocking disposition item, not a code change, and is out of scope for this plan.
- **Work Mode:** targeted remediation (test-only; no production file is touched)
- **Owner:** drmoisan
- **Version:** 1.0

**Fail-closed evidence rule:** Baseline artifacts and final-QA artifacts are mandatory. If any is missing or non-numeric, the outcome is BLOCKED/INCOMPLETE, never PASS.

**Evidence accounting rule:** Every evidence-producing task names its artifact path under the canonical `<FEATURE>/evidence/<kind>/` scheme (`evidence-and-timestamp-conventions`). No non-canonical path was supplied by the remediation inputs, so no override is required. Every artifact includes `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. `<ts>` denotes the ISO-8601 `yyyy-MM-ddTHH-mm` execution timestamp recorded by the executor.

## Environment Warnings (binding on every task)

1. `msbuild`, `vstest.console.exe`, and the repo-local `dotnet` are NOT resolvable from the Bash shell, and the Bash tool returns exit code 0 on `command not found` — a skipped command can masquerade as a pass. Every toolchain invocation MUST go through `pwsh -NoProfile -Command "..."` and MUST end with `; exit $LASTEXITCODE`, with `$LASTEXITCODE` checked explicitly.
2. CSharpier MUST be the repo-pinned **1.2.6** via `./.dotnet-sdk/dotnet.exe tool run csharpier`, using the `format` / `check` subcommands. A global csharpier 1.3.0 on PATH MUST NOT be used.
3. `vstest.console.exe` is at `C:/Program Files/Microsoft Visual Studio/18/Community/Common7/IDE/Extensions/TestPlatform/vstest.console.exe`.
4. Test filters MUST use the single-token underscore form (e.g. `FullyQualifiedName~BreadcrumbItemViewerLifecycleCoordinator`), never a dotted `Class.Method` form — a dotted filter can select zero tests and still exit 0 (D10, prior cycle). Every scoped-filter task in this plan requires the executor to record the discovered/selected test count and confirm it is non-zero.
5. Test-assembly discovery MUST exclude any path containing `\.claude\`.
6. Toolchain order is format -> analyzers -> nullable -> test. If any step fails or changes files, restart the loop from format.
7. Any new test file needs an explicit `<Compile Include>` in `QuickFiler.Test/QuickFiler.Test.csproj`. This plan adds no new test file (extension in place), so no csproj edit is expected; P2-T8 verifies that.

## Canonical Commands

- **[CMD-FORMAT]** `pwsh -NoProfile -Command "& ./.dotnet-sdk/dotnet.exe tool run csharpier format . ; exit $LASTEXITCODE"`
- **[CMD-FORMAT-CHECK]** `pwsh -NoProfile -Command "& ./.dotnet-sdk/dotnet.exe tool run csharpier check . ; exit $LASTEXITCODE"`
- **[CMD-ANALYZE]** `pwsh -NoProfile -Command "& msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true ; exit $LASTEXITCODE"`
- **[CMD-NULLABLE]** `pwsh -NoProfile -Command "& msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true ; exit $LASTEXITCODE"`
- **[CMD-TEST-SCOPED]** `pwsh -NoProfile -Command "& 'C:/Program Files/Microsoft Visual Studio/18/Community/Common7/IDE/Extensions/TestPlatform/vstest.console.exe' QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts/vscode/TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:\"FullyQualifiedName~BreadcrumbItemViewerLifecycleCoordinator\" ; exit $LASTEXITCODE"`
- **[CMD-COVERAGE(\<out\>)]** `pwsh -NoProfile -Command "& ./scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -CoverageOutput '<out>' ; exit $LASTEXITCODE"`

## Do-Not-Do List (reproduced from remediation-inputs.2026-08-08T13-25.md)

- Do not modify any production file to satisfy R1; it is a test-coverage gap, not a production defect. If a production seam change proves genuinely necessary, STOP and require re-planning — do not proceed.
- Do not add `[ExcludeFromCodeCoverage]` or any coverage-tool exclusion.
- Do not weaken, remove, or relax any existing test or assertion.
- Do not touch the EfcViewer search path, the suggestions path, or any explicit-gesture semantics.
- Do not exceed 500 lines in any file. `BreadcrumbDropDownHostTests.cs` is at 499 and may only be extended via its `Part2` partial (not touched by this plan).
- Do not edit policy documents, `spec.md` acceptance criteria, or the issue #400 feature folder.

## Decisions Record

- **D1 — No new file, no csproj edit.** `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs` is 345 lines (verified P0-T3 baseline reconfirms). Two new `[TestMethod]`s (~35 lines) extend it in place, per remediation-inputs' explicit authorization, staying well under 500. No `Part2` partial and no `<Compile Include>` edit are needed; P2-T8 verifies this.
- **D2 — Test design covers both null arms.** `_openCoordinator` is null unless `ConfigureHost` was called; `_bridgeCoordinator` is null unless `SetBridgeCoordinator` was called. `LifecycleFixture` (existing test-file fixture) constructs both null by default. Test 1 calls `SetBridgeCoordinator` only (open coordinator stays null) and asserts the bridge coordinator observably received the items. Test 2 calls neither (both stay null) and asserts a deterministic no-op. Together they exercise both null branches; the existing non-null branches are already covered by `BreadcrumbDropDownSearchIntegrationTests.Part2.cs`'s two `PresentSearchResults_*` integration tests (both `_openCoordinator` and `_bridgeCoordinator` non-null), which is why the file currently measures exactly 2/4 (50%) rather than 0/4.
- **D3 — No provider-mock risk.** `LifecycleFixture.CreateBridge()` wires a `Mock<IFolderHierarchyProvider>(MockBehavior.Strict)` with no method setups. `BreadcrumbBridgeCoordinator.PresentSearchResults` -> `ReplaceItemsPreservingSession` / `OpenSelector` / `HighlightRow` never call `IFolderHierarchyProvider` (only `SetSuggestionsAsync`/`SetSuggestions` do), so the strict mock will not throw. This is the same pattern already exercised safely by the existing `SetBridgeCoordinator_SameReference_DoesNotDuplicateSubscriptions` test in the same file (`bridge.AddItems(...)`, `bridge.SelectRow(...)`).
- **D4 — Evidence file naming avoids clobbering Cycle-1 evidence.** Cycle-1 already wrote `evidence/baseline/coverage-baseline.cobertura.xml` and `evidence/qa-gates/coverage-final.cobertura.xml`. This remediation cycle writes distinct filenames — `evidence/remediation-baseline/coverage-remediation-baseline.cobertura.xml` and `evidence/qa-gates/coverage-remediation-final.cobertura.xml` — so both cycles' evidence remain on disk and auditable.

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read policies in order: `CLAUDE.md`; `.claude/rules/general-code-change.md`; `.claude/rules/general-unit-test.md`; `.claude/rules/csharp.md`. Then read `<FEATURE>/remediation-inputs.2026-08-08T13-25.md` (R1 section and do-not-do list) and `<FEATURE>/spec.md` § Acceptance Criteria (confirm AC-1…AC-14 currently `[x]`). Verifying command: `pwsh -NoProfile -Command "Test-Path CLAUDE.md, .claude/rules/general-code-change.md, .claude/rules/general-unit-test.md, .claude/rules/csharp.md, docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/remediation-inputs.2026-08-08T13-25.md, docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/spec.md ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0` (all paths resolve; `Test-Path` on a list returns one boolean per item, all must be `True`). Accept: artifact lists every file read with `Policy Order:`. Evidence: `<FEATURE>/evidence/remediation-baseline/phase0-instructions-read.<ts>.md`.
- [x] [P0-T2] Record git baseline. File: repository root (no file edit — inspection only). Verifying command: `pwsh -NoProfile -Command "git rev-parse HEAD; git status --porcelain ; git status --porcelain -- '*.cs' '*.csproj' ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`. Accept: `HEAD` equals `ff9d14ab...` (record the full SHA); the scoped check `git status --porcelain -- '*.cs' '*.csproj'` is empty (this is the binding clean-tree condition — it is the property P0-T8's and P2-T7's coverage-delta gates actually depend on); the unscoped `git status --porcelain` output is recorded informationally in full. HALT and notify the caller only if the unscoped output contains a path outside the expected set of review-cycle documentation and agent-memory artifacts (`.claude/agent-memory/feature-review/*` and the cycle-1 `docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/{code-review,feature-audit,policy-audit,remediation-inputs,remediation-plan}.2026-08-08T13-25.md` artifacts) — a `.cs` or `.csproj` entry anywhere in the unscoped output is always a HALT condition regardless of path. Evidence: `<FEATURE>/evidence/remediation-baseline/git-baseline.<ts>.md`.
- [x] [P0-T3] Confirm the current line count of the test file to be extended. File: `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs`. Verifying command: `pwsh -NoProfile -Command "(Get-Content QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs).Count ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`, output `345`. Accept: recorded count matches 345 (confirms D1's headroom assumption before any edit). Evidence: `<FEATURE>/evidence/remediation-baseline/target-file-linecount.<ts>.md`.
- [x] [P0-T4] Confirm the current branch coverage of the R1 target method from the existing Cycle-1 coverage artifact (no full-suite re-run needed for this specific confirmation — the artifact reflects the same clean, unchanged tree recorded at P0-T2). File: `<FEATURE>/evidence/qa-gates/coverage-final.cobertura.xml` (read-only). Verifying command: `pwsh -NoProfile -Command "[xml]$x = Get-Content docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/evidence/qa-gates/coverage-final.cobertura.xml; $m = $x.SelectNodes('//class[@filename=\"QuickFiler\Viewers\BreadcrumbItemViewerLifecycleCoordinator.Search.cs\"]/methods/method[@name=\"PresentSearchResults\"]'); if ($m.Count -ne 1) { Write-Error \"Expected exactly 1 matching node, found $($m.Count)\" ; exit 1 } ; $m | ForEach-Object { $_.'line-rate'; $_.'branch-rate' } ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`, node count `1`, output `1` (line-rate) then `0.5` (branch-rate). The Cobertura `filename` attribute uses backslashes (`\`), not forward slashes — an XPath predicate using `/` selects zero nodes and must not be used. Accept: node count equals exactly 1 (a count of 0 is an automatic FAIL, not an inconclusive result — mirrors D10's zero-selection lesson); recorded `branch-rate` equals `0.5` (2/4); recorded `line-rate` equals `1` (5/5), matching R1's stated measurement. Evidence: `<FEATURE>/evidence/remediation-baseline/coverage-target-baseline.<ts>.md`.
- [x] [P0-T5] Format baseline. File: repository-wide (no edit — check mode only). Verifying command: [CMD-FORMAT-CHECK] — expected `EXIT_CODE: 0`. Accept: zero violations reported (pre-existing clean state re-confirmed). If non-zero, HALT and notify the caller (pre-existing conflict, out of this remediation's scope). Evidence: `<FEATURE>/evidence/remediation-baseline/format-baseline.<ts>.md`.
- [ ] [P0-T6] Analyzer build baseline. File: `TaskMaster.sln` (no edit — build check only). Verifying command: [CMD-ANALYZE] — expected `EXIT_CODE: 0`, zero errors. Accept: zero errors reported. Evidence: `<FEATURE>/evidence/remediation-baseline/analyzer-baseline.<ts>.md`.
- [ ] [P0-T7] Nullable build baseline. File: `TaskMaster.sln` (no edit — build check only). Verifying command: [CMD-NULLABLE] — expected `EXIT_CODE: 0`, zero errors. Accept: zero errors reported. Evidence: `<FEATURE>/evidence/remediation-baseline/nullable-baseline.<ts>.md`.
- [ ] [P0-T8] Full coverage-enabled test baseline (mandatory numeric baseline before the test-only edit). File: full first-party `*.Test.dll` suite (no production/test edit — measurement only). Verifying command: [CMD-COVERAGE(`docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/evidence/remediation-baseline/coverage-remediation-baseline.cobertura.xml`)] — expected `EXIT_CODE: 0`. Accept: all tests pass (expected total `6348`, matching Cycle-1's final count since the tree is unchanged); zero discovered assembly paths contain `\.claude\`; repository-wide `line-rate` equals `0.858665` and `branch-rate` equals `0.792502` (must match Cycle-1's final figures exactly, since no code changed between the two runs — a mismatch is a HALT condition requiring investigation before proceeding). Evidence: `<FEATURE>/evidence/remediation-baseline/test-coverage-remediation-baseline.<ts>.md` plus the XML.

### Phase 1 — Add Branch-Coverage Tests (test-only; no production file touched)

- [ ] [P1-T1] Add test method `PresentSearchResults_NoOpenCoordinator_BridgeReceivesItemsWithoutThrow` to `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs`. Construct a `LifecycleFixture`, call `fixture.Coordinator.SetBridgeCoordinator(fixture.CreateBridge())` (leaving `_openCoordinator` null — `ConfigureHost` is never called), call `fixture.Coordinator.PresentSearchResults(new[] { "Alpha", "Beta" })` wrapped in a `FluentAssertions` `Action`, assert `.Should().NotThrow()`, drain `fixture.Queue.DrainOnCreatorThread()`, then assert `bridge.GetFolderItems().Should().Equal("Alpha", "Beta")` to prove the bridge coordinator received the items through the null-`_openCoordinator` branch (D2). No existing test method in the file is edited. Verifying command: [CMD-ANALYZE] — expected `EXIT_CODE: 0` (confirms compile). Accept: build succeeds; `git diff -- QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs` shows only additive lines (no existing line removed or altered).
- [ ] [P1-T2] Add test method `PresentSearchResults_NoBridgeCoordinator_IsDeterministicNoOp` to `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs`. Construct a fresh `LifecycleFixture` with neither `ConfigureHost` nor `SetBridgeCoordinator` called (both `_openCoordinator` and `_bridgeCoordinator` remain null), call `fixture.Coordinator.PresentSearchResults(new[] { "Gamma" })` wrapped in an `Action`, assert `.Should().NotThrow()`, then assert `fixture.Coordinator.BridgeCoordinator.Should().BeNull()` and `fixture.SelectionChangedCount.Should().Be(0)` to prove the call is a deterministic no-op through the null-`_bridgeCoordinator` branch (D2). No existing test method in the file is edited. Verifying command: [CMD-ANALYZE] — expected `EXIT_CODE: 0` (confirms compile). Accept: build succeeds; `git diff -- QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs` shows only additive lines added since P1-T1 (no existing line removed or altered).
- [ ] [P1-T3] Confirm the two new tests are discovered and pass, and no existing test in the class regressed. File: `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs` (verification only, no edit). Verifying command: [CMD-TEST-SCOPED] — expected `EXIT_CODE: 0`. Accept: selected test count is exactly `12` (the 10 pre-existing methods plus the 2 added; a selected count of `0` is an automatic FAIL per Environment Warning 4, not a pass); all 12 pass; both new method names appear in the pass list. Evidence: `<FEATURE>/evidence/other/target-tests-scoped-run.<ts>.md`.

### Phase 2 — Final QA Loop and Coverage Gates

Run in order; if any step fails or changes files, restart from P2-T1. No `SKIPPED` outcome is valid for any task in this phase.

- [ ] [P2-T1] Format. File: repository-wide (may touch `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs` if the added test methods are not yet CSharpier-formatted). Verifying commands: [CMD-FORMAT] then [CMD-FORMAT-CHECK] — expected `EXIT_CODE: 0` for the check. Accept: check passes with zero violations; if `format` changed any file, note it and restart the loop from this step once formatting is stable. Evidence: `<FEATURE>/evidence/qa-gates/final-format-remediation.<ts>.md`.
- [ ] [P2-T2] Analyzers. File: `TaskMaster.sln` (build check only). Verifying command: [CMD-ANALYZE] — expected `EXIT_CODE: 0`, zero errors. Accept: zero errors. Evidence: `<FEATURE>/evidence/qa-gates/final-analyze-remediation.<ts>.md`.
- [ ] [P2-T3] Nullable type-check. File: `TaskMaster.sln` (build check only). Verifying command: [CMD-NULLABLE] — expected `EXIT_CODE: 0`, zero errors. Accept: zero errors. Evidence: `<FEATURE>/evidence/qa-gates/final-nullable-remediation.<ts>.md`.
- [ ] [P2-T4] Post-format file-size audit. File: `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs`. Verifying command: `pwsh -NoProfile -Command "(Get-Content QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs).Count ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`, output `<= 500`. Accept: line count is `<= 500` (expected ~380). Evidence: `<FEATURE>/evidence/qa-gates/file-size-audit-remediation.<ts>.md`.
- [ ] [P2-T5] Full coverage-enabled test run (final). File: full first-party `*.Test.dll` suite (no further edit — measurement only). Verifying command: [CMD-COVERAGE(`docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/evidence/qa-gates/coverage-remediation-final.cobertura.xml`)] — expected `EXIT_CODE: 0`. Accept: all tests pass (expected total `6350` = `6348` baseline + `2` new); zero discovered assembly paths contain `\.claude\`; numeric repository-wide `line-rate`/`branch-rate` recorded in `Output Summary:`. Evidence: `<FEATURE>/evidence/qa-gates/test-coverage-remediation-final.<ts>.md` plus the XML.
- [ ] [P2-T6] R1 target-file branch-coverage gate. File: `<FEATURE>/evidence/qa-gates/coverage-remediation-final.cobertura.xml` (read-only). Verifying command: `pwsh -NoProfile -Command "[xml]$x = Get-Content docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/evidence/qa-gates/coverage-remediation-final.cobertura.xml; $m = $x.SelectNodes('//class[@filename=\"QuickFiler\Viewers\BreadcrumbItemViewerLifecycleCoordinator.Search.cs\"]/methods/method[@name=\"PresentSearchResults\"]'); if ($m.Count -ne 1) { Write-Error \"Expected exactly 1 matching node, found $($m.Count)\" ; exit 1 } ; $m | ForEach-Object { $_.'line-rate'; $_.'branch-rate' } ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`, node count `1`. The Cobertura `filename` attribute uses backslashes (`\`), not forward slashes — an XPath predicate using `/` selects zero nodes, and a threshold check against an empty node set would pass vacuously (the same failure mode as D10's zero-selection filter). Accept, evaluated ONLY after the node-count check passes: node count equals exactly 1 (a count of 0 is an automatic FAIL, never treated as "threshold met"); `branch-rate >= 0.75` (target `1` = 4/4); `line-rate` remains `1` (5/5, no regression). If node count is 0, HALT — the gate is inconclusive, not passing; investigate the filename/path form before re-running. If `branch-rate < 0.75` with node count 1, this is a HALT condition — do not add `[ExcludeFromCodeCoverage]` or relax the gate; add the missing test scenario and restart Phase 2 from P2-T1. Evidence: `<FEATURE>/evidence/qa-gates/coverage-target-final.<ts>.md`.
- [ ] [P2-T7] Repository-wide coverage no-regression gate. File: `<FEATURE>/evidence/remediation-baseline/coverage-remediation-baseline.cobertura.xml` vs `<FEATURE>/evidence/qa-gates/coverage-remediation-final.cobertura.xml` (read-only comparison). Verifying command: `pwsh -NoProfile -Command "[xml]$b = Get-Content docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/evidence/remediation-baseline/coverage-remediation-baseline.cobertura.xml; [xml]$f = Get-Content docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/evidence/qa-gates/coverage-remediation-final.cobertura.xml; '{0} {1} {2} {3}' -f $b.coverage.'line-rate', $b.coverage.'branch-rate', $f.coverage.'line-rate', $f.coverage.'branch-rate' ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`. Accept: final `line-rate >= 0.858665` and final `branch-rate >= 0.792502` (both must be greater than or equal to, not merely close). Evidence: `<FEATURE>/evidence/qa-gates/coverage-delta-remediation.<ts>.md`.
- [ ] [P2-T8] Scope, compliance, and AC-regression guard. Files: repository-wide diff scope (no edit). Verifying commands: `pwsh -NoProfile -Command "git diff --name-only ; exit $LASTEXITCODE"` (expected `EXIT_CODE: 0`, output limited to `QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs` plus `<FEATURE>/` evidence/docs paths — zero `.csproj` entries, zero non-test `.cs` entries); `pwsh -NoProfile -Command "Select-String -Path QuickFiler.Test/Viewers/BreadcrumbItemViewerLifecycleCoordinatorTests.cs -Pattern 'ExcludeFromCodeCoverage' ; exit 0"` (expected zero matches); `pwsh -NoProfile -Command "git diff -- docs/features/active/2026-08-07-quickfiler-search-keystroke-focus-steal-438/spec.md ; exit $LASTEXITCODE"` (expected empty diff — all 14 ACs remain `[x]` unchanged). Accept: (a) zero production `.cs` file diffs; (b) zero `.csproj` diffs (confirms D1 — no new file, no wiring edit needed); (c) zero `[ExcludeFromCodeCoverage]` additions; (d) `spec.md` byte-unmodified; (e) the only existing-test diff is the additive P1-T1/P1-T2 hunk in `BreadcrumbItemViewerLifecycleCoordinatorTests.cs` with no line removed from the 10 pre-existing test methods. Evidence: `<FEATURE>/evidence/qa-gates/scope-compliance-guard.<ts>.md`.
- [ ] [P2-T9] Final status and wrap-up. Files: repository root (no edit — verification and commit only). Verifying command: `pwsh -NoProfile -Command "git add -A ; git status --porcelain ; exit $LASTEXITCODE"` — expected `EXIT_CODE: 0`, empty porcelain after commit. Accept: all 20 checkboxes in this plan are `[x]`; every evidence path listed above resolves on disk; working tree is committed and clean. Evidence: `<FEATURE>/evidence/other/remediation-final-status.<ts>.md`.

## Traceability — R1 Acceptance → Plan Tasks

| R1 Acceptance Clause | Delivered by | Verified by |
|---|---|---|
| File-level branch coverage >= 75% (target 4/4) | P1-T1, P1-T2 | P2-T6 |
| No existing test modified | P1-T1, P1-T2 (additive-only diffs) | P2-T8 |
| Repo-wide figures not lower than 0.858665 line / 0.792502 branch | P0-T8 (baseline capture) | P2-T7 |
| Full four-stage toolchain passes in one uninterrupted final pass | P2-T1…P2-T5 | P2-T9 |
| All 14 gating ACs remain PASS | — (no production change) | P2-T8 |

## Constraints Reasserted for the Executor

- .NET Framework 4.8.1: no `init` setters, no `record`, no `record struct`.
- MSTest + Moq + FluentAssertions; Arrange–Act–Assert; no temporary files; no `Thread.Sleep`/`Task.Delay`/wall-clock waits.
- No new packages, no persisted configuration, no feature flags.
- Zero production file (`.cs` outside `QuickFiler.Test/`) is touched by this plan. If executing P1-T1 or P1-T2 reveals that a production seam change is genuinely required to make the test constructible, STOP immediately and report back for re-planning — do not proceed under this plan.
- Formatter output wins over hand formatting.
