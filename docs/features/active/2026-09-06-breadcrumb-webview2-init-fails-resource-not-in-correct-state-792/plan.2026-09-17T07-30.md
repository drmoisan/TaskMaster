# breadcrumb-webview2-init-fails-resource-not-in-correct-state (Plan)

- **Issue:** #792
- **Work Mode:** full-bug (marker read from `issue.md` line 12; AC source is `spec.md` only)
- **Plan Path:** docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/plan.2026-09-17T07-30.md
- **Version:** 1.2 (revision round 3 applied 2026-09-17: four preflight deltas and two non-blocking items on top of the nineteen round-2 deltas; no design change, no task, test, name, phase or ID added, removed or renumbered)
- **Status:** Awaiting executor preflight (round 3)
- **Authoritative source tree:** the item worktree for #792 (branch `bug/breadcrumb-webview2-init-fails-resource-not-in-correct-state-792`, merged to current `origin/main`), referred to below as `<repo-root>`. Every line citation below was re-derived against that tree in this pass.
- **Task Count:** 93 (P0 16, P1 4, P2 15, P3 8, P4 12, P5 9, P6 6, P7 18, P8 5), counted mechanically over `^- \[ \] \[P\d+-T\d+\]`.

## Inputs

- Requirements: `$FEATURE/spec.md` (nine criteria AC-U1 through AC-U9, lines 306-314).
- Manual runbook: `$FEATURE/user-story.md` (AC-U5 runbook, lines 52-86).
- Research (current): `$FEATURE/research/2026-09-17T11-20-breadcrumb-webview2-init-research.md`. The older `2026-09-12T10-30-...` record is superseded and was not relied on.
- `$FEATURE` abbreviates the repository-relative path `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792`. Every evidence path in this plan is `$FEATURE/evidence/<kind>/...` with kind in {baseline, regression-testing, qa-gates, other}; no `artifacts/` evidence location is used anywhere.

## Premise verification (all re-derived against the item worktree, `<repo-root>`, in this pass)

| Premise from the brief | Measured |
|---|---|
| `QuickFiler/Viewers/WebView2BreadcrumbHost.cs:250` constructs options with no arguments | Confirmed: `var options = new CoreWebView2EnvironmentOptions();` |
| `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs:61` dead comment, `:62` live target-typed `new("--incognito ")` | Confirmed |
| `QuickFiler/Controllers/EfcItemController.cs:177` `IncognitoArgument`, `:188-190` construction, `:195-199` direct `CoreWebView2Environment.CreateAsync` | Confirmed |
| `BreadcrumbBridgeRouter.Selection.cs:168-180` stash; `BreadcrumbBridgeRouter.cs:320-329` sole drain calling `_outboundQueue.OnInitializationCompleted()` at `:328`; `BreadcrumbOutboundQueue.cs:59-65` FIFO drain | Confirmed |
| Sole `CoreInitialized` subscription `EfcFormController.cs:1064`; host returns at `:341` without raising; `InitializeAsync` `:239-270` has no retry | Confirmed |
| `TryReportBoundaryFault` defined `:150-168`, eight call sites 556, 573, 591, 653, 668, 1015, 1127, 1270 | Confirmed by grep |
| `InitializeBreadcrumbHostAsync` `:1072-1082`, log-only at `:1080` | Confirmed |
| Line totals: EfcFormController.cs 1321, EfcItemController.cs 1122, QfcCollectionController.cs 2333, ViewerSetup 479, EfcFormControllerTests.cs 485, EfcDataModel.cs 499, EfcHomeController.cs 447 | Confirmed by tail reads |
| Designer `CreationProperties` inert; `EfcViewer.FolderListBox` declares none | Not re-verified beyond the research record; no plan task depends on it |

Two corrections to the brief, neither a premise failure:

1. The brief names six not-yet-existing write-set entries. The write set actually creates seventeen `.cs` files (nine production, eight test), every one of which needs a Compile item. All seventeen are wired in this plan (see AC-U8 parity, [P6-T3]).
2. The existing test `EfcFormControllerTests.PopulateFolderCombobox_WhenDataModelFaults_LogsOnceAndDoesNotFault` (`QuickFiler.Test/Controllers/EfcFormControllerTests.cs:299-328`) already asserts the boundary-sink call count through a substituted `BoundaryErrorSink`; it does not assert the user-facing surface. The strengthened test therefore pins `UserFaultNotifier` (the user surface behind the default sink), and its discriminating mutation is dropping the notifier call inside `DefaultBoundaryErrorSink`, which the existing test cannot detect.

## Design decisions (fixed; do not relitigate during execution)

- **D1 One owner.** New `QuickFiler/Viewers/WebView2EnvironmentContract.cs` (`internal static class`) owns `AdditionalBrowserArguments = "--incognito "`, `UserDataFolderName = "WindowsFormsWebView2"`, `ResolveUserDataFolder()` (a `Path.Combine` over `LocalApplicationData`; creates nothing on disk) and `CreateOptions()`. All three sites read from it. Direction is settled by the research record (question 1 closed); no audit task exists.
- **D2 Site 3 through the seam.** `EfcItemController.InitializeWebViewAsync` and `IncognitoArgument` move to `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs`. The moved method gains an `internal IWebViewCoreInitializer WebViewInitializer` property (lazy default `new WebView2CoreInitializer()`) and awaits both seam calls instead of the `ContinueWith` chain, so a failure now reaches `InitializeWebViewGuardedAsync` (`EfcItemController.WebViewFaultBoundary.cs:25-42`). `IncognitoArgument` stays declared (the #463 test `EfcItemControllerTests.cs:371-396` pins it) as an alias `= WebView2EnvironmentContract.AdditionalBrowserArguments`.
- **D3 One defect, one trigger.** The retry and the failure notification live on the awaited path in `EfcFormController.InitializeBreadcrumbHostAsync` (made `internal`), never in the host's `[ExcludeFromCodeCoverage]` SDK handler. Fixed attempt count `BreadcrumbInitializationAttemptLimit = 3`, no wall-clock delay, `OperationCanceledException` is non-fault and stops the loop. Seam: `internal Func<Task> BreadcrumbHostInitializer { get; set; }`; null selects the production `_breadcrumbHost.InitializeAsync(_formViewer.UiSyncContext)`. No new host event is added: the spec's "wires the new failure notification" sentence is delivered by a direct call after the loop, because a host-side event would live in the structurally untestable handler.
- **D4 Visible error state.** A document navigated into a WebView2 whose core never initialized is not visible: `WebView2.NavigateToString` requires a live core (host remark at `WebView2BreadcrumbHost.cs:171-172`). The visible carrier is therefore the existing folder-area label `EfcViewer.label2` (internal field, `EfcViewer.Designer.cs:4248`, text `"Matched Folders:"` at `:239`), whose text becomes `FolderAreaInitializationFailedText` on final failure. No new WinForms control and no Designer edit. The router still renders the banner document (spec decision 2) and hands it to the host; the host gains the same pre-initialization log-and-drop guard on `NavigateToString` that `PostMessageJson` already has (`WebView2BreadcrumbHost.cs:196-208`), so the failure path throws nothing. This refines spec "Settled scope decision 2" without changing any AC text; the orchestrator is asked to acknowledge it (reported in the handoff).
- **D5 Router failure entry point.** `BreadcrumbBridgeRouter.NotifyInitializationFailed(Exception)` is a sibling of `NotifyCoreInitialized`, which is unchanged. It discards `_pendingDocument` explicitly, calls `BreadcrumbOutboundQueue.DiscardPending()` (new, returns the discarded count), replaces `_rows` with one banner row `InitializationFailedBannerText`, clears the selection (raising `SelectedFolderPathChanged` only when it changes), navigates the rendered error document through the host, and logs one error line with both discard facts. Discard, not drain: the host cannot post to a core that does not exist.
- **D6 Pop-out carry.** New partial `QuickFiler/Controllers/QfcCollectionController.PopOut.cs` receives the two `PopOut*` members. `ReadPopOutCarry(QfcItemGroup)` (pure, static) reads the handler through the new `QfcItemController.FolderHandler` internal accessor by pattern match on the concrete type, and the helper through `IQfcItemController.ItemHelper`; it runs BEFORE `RemoveSpecificControlGroup(Async)` because `QfcItemController.Cleanup` nulls both `_folderHandler` (`ViewerSetup.cs:444`) and `ItemHelper` (`:455`). Construction goes through `PopOutHomeControllerFactory` (named default `CreatePopOutHomeController`). `EfcHomeController` constructors (`EfcHomeController.cs:47-52` and `:54-95`) gain trailing optional `IFolderSearchHandler carriedFolderHandler = null, MailItemHelper carriedMailHelper = null` and deposit both on `DataModel` between `DataModelFactory` (`:66`) and `FormControllerWithDataFactory` (`:85`), because the production factory calls `Initialize()`, which fires `PopulateFolderCombobox` and therefore `InitFolderHandlerAsync`.
- **D7 Adoption.** `QuickFiler/Controllers/EfcDataModel.Carry.cs` receives `InitFolderHandlerAsync` (moved from `EfcDataModel.cs:188-221`), `CarriedFolderHandler`, `CarriedMailHelper`, and the pure decision `TryAdoptCarriedFolderHandler(object folderList, IFolderSearchHandler carried, out FolderPredictor adopted)`: true only when `folderList is null` and `carried is FolderPredictor`. When adopted, `FolderHelper = adopted` and the carry is released. Otherwise the existing body runs, with `MailInfo ?? CarriedMailHelper` as the scoring input in the null-list branch (identical to today whenever no helper was carried), and the carry is released at the end of that branch. The explicit-list branch is untouched.
- **D8 UI-thread construction.** `EfcViewerQueue.ProductionBlockingPriorityScheduler` default (line 25) and its reset (line 68) change from the inline lambda to the named method `InvokeOnUiDispatcher(Action, DispatcherPriority)`, expression-bodied over `UiThread.Dispatcher.Invoke(action, priority)` so it carries exactly one sequence point, mirroring `ItemViewerQueue.cs:26-27`. `UiThread.Dispatcher` throws `InvalidOperationException` when `Init` has not run (`UtilitiesCS/Threading/UiThread.cs:264-282`), so no test invokes the default; tests assert delegate identity by `.Method.Name` against the literal `InvokeOnUiDispatcher` (a named method group, not a lambda). This half is verifiable only as a scheduler-delegate assertion, as the spec states.
- **D9 Six-way split.** `EfcFormController.cs` (1321) becomes `internal partial class` and is split by its existing regions: retained `EfcFormController.cs` = lines 1-264 (usings, constructors, private properties, the sink members); `EfcFormController.SetupAndProperties.cs` = 266-479; `EfcFormController.EventHandlers.cs` = 481-834; `EfcFormController.Actions.cs` = 836-990; `EfcFormController.Breadcrumb.cs` = 1044-1129 (`ConfigureBreadcrumbControl`, `InitializeBreadcrumbHostAsync`, `BindFolderRows`, `BindSourceFolderRows`, `BindBreadcrumbRowsAsync`); `EfcFormController.Helpers.cs` = 992-1043 plus 1131-1289 plus 1291-1319. Each new part carries the retained file's full using block (lines 1-22). The split is a pure move proven by the conservation gate in [P2-T1], then behaviour lands only in `EfcFormController.Breadcrumb.cs` ([P4-T7]).
- **D10 Coverage scope and floors.** Baseline and final coverage are measured over `QuickFiler.Test.dll` only, via `scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot QuickFiler.Test`. Reason: the only production project in the write set is `QuickFiler`; the only projects referencing it are `TaskMaster` (`TaskMaster/TaskMaster.csproj:501`) and `QuickFiler.Test` (`QuickFiler.Test/QuickFiler.Test.csproj:481`); and four `UtilitiesCS.Test` shell-icon classes hang `vstest` on this workstation (environmental, recorded in the maintainer's memory 2026-09-04). `TaskMaster.Test.dll` is run as a plain regression sweep with the hang and LiveOutlook exclusions. The repository-wide floor stated by `CLAUDE.md` (UT2, `>= 80%`), `.claude/rules/csharp.md` (`>= 80%`), `.claude/rules/general-unit-test.md` (`>= 85%` line, `>= 75%` branch) and `.claude/rules/quality-tiers.md` is therefore NOT measured repository-wide by this plan; the document-level `line-rate` of the single-assembly run is recorded as `QUICKFILER-SCOPED-DOCUMENT-LINE-RATE: <n>` and is compared to no floor; every coverage artifact also carries the line `REPO-WIDE-FLOOR: NOT MEASURED (single test assembly; see D10)`; the tokens `BASELINE_FLOOR`, `FINAL_FLOOR`, `MET` and `NOT MET` appear in no artifact. A multi-assembly run is not available through the runner: its `/TestCaseFilter` is hard-coded (`Invoke-MSTestWithCoverage.ps1:91`), it exposes no filter parameter (`:1-13`), and `-SearchRoot .` sweeps every `*.Test.dll` (`:330-337`) including the four `UtilitiesCS.Test` shell-icon classes; the runner is outside the write set. The operative hard gates are: per-file `>= 90%` for the wholly new instrumented production file `WebView2EnvironmentContract.cs`; `>= 90%` over the new executable lines of the instrumented relocated parts (`EfcFormController.Breadcrumb.cs`, `EfcDataModel.Carry.cs`); changed-line no-regression on every instrumented edited file; and type-level covered-count no-regression across the split parts. `EfcItemController` (`EfcItemController.cs:26`) and `QfcCollectionController` (`QfcCollectionController.cs:22`) carry class-level `[ExcludeFromCodeCoverage]`, so their new partials have no Cobertura rows and are recorded under `Uninstrumented, not comparable`; `QfcItemController.InitializeWebViewAsync` is method-level exempt (`ViewerSetup.cs:48`).
- **D11 Manual gates.** AC-U5 is human-executed per the runbook and is never automated, never a `[TestMethod]`, never counted toward any figure. Outlook must be CLOSED through its own exit, never killed, before every `msbuild` invocation in this plan; every build task begins with the `CMD-OUTLOOK` check and HALTS for a human if `OUTLOOK` is running. No task calls `Stop-Process`.
- **D12 Evidence.** Per the maintainer decision recorded in spec decision 6, no `.trx` and no `.cobertura.xml` is written under the feature folder; raw tool output goes to the gitignored `coverage/` tree (`.gitignore:144`) and only Markdown projections are committed. Every evidence artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- **D13 Diff bases.** Every diff, merge base and footprint gate is anchored to `BASE-SHA` = `git merge-base HEAD origin/main` captured after `git fetch origin` in [P0-T7], re-read by every consuming task from that artifact. Bare local `main` is never used.
- **D14 Test parallelism.** `scripts/vscode/TaskMaster.cli.runsettings` stays byte-identical (`Workers=0`, `ClassLevel`). No new `[DoNotParallelize]`, no sleeps, no retries. New test classes touch no shared static except reads of `EfcViewerQueue` delegates and no writes; `ResetProductionCoreDefaultsForTesting()` is called by no new test because `ViewerQueueStaticWrapperTests.cs:254` substitutes the same delegate and restores it at `:18`; the only writer of those statics is that pre-existing `[DoNotParallelize]` class. `UserFaultNotifier` is `AsyncLocal` (`EfcFormController.cs:170-185`); tests set it on their own flow and restore in `finally`.

## Conventions binding every task

1. **Working directory.** Every command runs from `<repo-root>`, the item worktree root, with that directory current; `<repo-root>` is substituted at run time and is never written into any artifact. Repository-relative paths below are relative to it.
2. **No shell state survives between tasks.** Every fenced block re-binds what it uses. `$BaseSha` is always derived by `Select-String -CaseSensitive -Pattern '^BASE-SHA: ([0-9a-f]{40})$'` and `$SpecRefSha` by `'^SPEC-REF-SHA: ([0-9a-f]{40})$'` over `$FEATURE/evidence/baseline/p0-t7-git-base.md` (CMD-BASE binds both); a missing match is a HALT.
3. **Line counts** use `(Get-Content -LiteralPath <path>).Count` (total lines). Never `Measure-Object -Line`, never non-blank counts. The 500-line ceiling applies to `.cs` files only; `.csproj` files are exempt (`.csharpierignore:9-14`).
4. **Formatter observation.** `dotnet tool run csharpier format .` prints `Formatted N files in` whether or not it rewrote anything; the rewritten count is defined as the number of write-set `.cs` files whose SHA-256 differs before and after. `dotnet tool run csharpier check .` prints `Checked N files in` and exits 0 on a clean tree.
5. **Green vstest output** prints `Test Run Successful.`, `Total tests: N`, `Passed: N`; it prints no `Failed:` or `Skipped:` line, which is transcribed as `Failed: 0 (omitted category)`.
6. **Scoped test runs never compile.** Every scoped run is preceded by `CMD-BUILD-PLAIN` in the same task. A run that discovers zero tests is a FAILURE of that task.
7. **No `EXIT_CODE: SKIPPED`.** No task authorizes a skip branch. Human checkpoints ([P0-T2], [P7-T1], [P8-T1], [P8-T2]) HALT and wait; they do not skip.
8. **Backslashes.** No doubled backslash appears in this plan. Path comparisons normalise with `.Replace('\', '/')` first. Any helper snippet longer than one statement is written to the single gitignored helper path `coverage/plan792-helper.ps1` (rewritten in place; not a deliverable, registered in no project, asserted by no gate) and run with `pwsh -NoProfile -WorkingDirectory <repo-root> -File coverage/plan792-helper.ps1`. The helper's opening branch assertion is the worktree proof; `(Get-Location).Path` is never written to an artifact, and every path the helper prints is repository-relative.
9. **Git gates** are scoped: `git status --porcelain -- '*.cs' '*.csproj' '*.sln' 'packages.config'` for clean-source gates; docs, evidence and `.claude/agent-memory/**` are expected to be dirty and never enumerated as tolerated files.
10. **Mutation tasks** (Phase 5) run only after the Phase 4 commit, so `git checkout -- <path>` restores the implemented state.
11. **Halt rule.** Any acceptance clause that fails HALTS the task with a `BLOCKED:` report; the executor never edits `spec.md` criteria text and never widens the write set.

## Named literals the plan creates (quoted here so a later search is not judged vacuous)

Production identifiers: `WebView2EnvironmentContract`, `AdditionalBrowserArguments`, `UserDataFolderName`, `ResolveUserDataFolder`, `CreateOptions`, `WebViewInitializer`, `NotifyInitializationFailed`, `InitializationFailedBannerText`, `DiscardPending`, `BreadcrumbHostInitializer`, `BreadcrumbInitializationAttemptLimit`, `FolderAreaInitializationFailedText`, `ShowFolderAreaError`, `InitializeBreadcrumbHostOnceAsync`, `CarriedFolderHandler`, `CarriedMailHelper`, `TryAdoptCarriedFolderHandler`, `ReleaseCarry`, `FolderHandler`, `ReadPopOutCarry`, `PopOutHomeControllerFactory`, `CreatePopOutHomeController`, `InvokeOnUiDispatcher`.

Production string literals: `"--incognito "`, `"WindowsFormsWebView2"`, `"Matched Folders: unavailable (breadcrumb initialization failed)"`, `" Folder list unavailable: breadcrumb initialization failed"` (appended to `BreadcrumbRowBuilder.BannerPrefix`), `"NavigateToString called before CoreWebView2 initialization; document dropped."`, `"Breadcrumb initialization canceled."`, and the fault message shape `Breadcrumb WebView2 initialization failed after 3 attempts:` (interpolated from the const, asserted by the token `after 3 attempts`).

Test method names (one per line so each is a single-line token):
`InitializeAsync_PassesTheSharedFolderAndIncognitoArgumentToTheSeam`
`NavigateToString_BeforeCoreInitialization_WithNoDispatcher_DropsTheDocumentWithoutThrowing`
`PopulateFolderCombobox_WhenDataModelFaults_NotifiesTheUserThroughTheDefaultSink`
`InitializeBreadcrumbHostAsync_WhenHostIsNull_ReportsThroughTheBoundarySinkToTheUser`
`ProductionBlockingPriorityScheduler_DefaultIsTheNamedUiDispatcherInvoke`
`AdditionalBrowserArguments_IsAsciiDoubleHyphenIncognitoWithTrailingSpace`
`ResolveUserDataFolder_CombinesLocalApplicationDataWithTheSharedLeafName`
`CreateOptions_CarriesTheSharedArgumentsOnAFreshInstance`
`EfcItemController_InitializeWebViewAsync_PassesTheContractValuesThroughTheSeam`
`NotifyInitializationFailed_ClearsThePendingDocumentAndNavigatesTheErrorBanner`
`NotifyInitializationFailed_LeavesNoStashForALaterInitialization`
`NotifyInitializationFailed_WithNullFailure_Throws`
`NotifyCoreInitialized_AfterAnEarlierStash_StillNavigatesIt`
`DiscardPending_ReturnsTheDiscardedCountAndLeavesZeroPending`
`DiscardPending_OnAnEmptyQueue_ReturnsZero`
`NotifyInitializationFailed_DiscardsTheOutboundQueueWithoutPosting`
`InitializeBreadcrumbHostAsync_RetriesUpToTheAttemptLimitThenReportsOnce`
`InitializeBreadcrumbHostAsync_SucceedsOnALaterAttempt_ReportsNothing`
`InitializeBreadcrumbHostAsync_WhenCanceled_DoesNotRetryOrReport`
`InitializeBreadcrumbHostAsync_OnFinalFailure_ShowsTheErrorTextInTheFolderAreaLabel`
`InitializeBreadcrumbHostAsync_OnFinalFailure_NotifiesTheRouter`
`TryAdoptCarriedFolderHandler_WithNullListAndConcretePredictor_Adopts`
`TryAdoptCarriedFolderHandler_WithNullCarry_DoesNotAdopt`
`TryAdoptCarriedFolderHandler_WithNonPredictorHandler_DoesNotAdopt`
`TryAdoptCarriedFolderHandler_WithExplicitListAndPredictor_DoesNotAdopt`
`InitFolderHandlerAsync_WithCarriedPredictor_AdoptsItAndReleasesTheCarry`
`InitFolderHandlerAsync_WithNonPredictorCarry_RunsTheExistingPathAndReleasesTheCarry`
`ReadPopOutCarry_WithConcreteItemController_ReturnsHandlerAndHelper`
`ReadPopOutCarry_WithInterfaceOnlyController_ReturnsNullHandlerAndTheHelper`
`ReadPopOutCarry_WithNullController_ReturnsNulls`
`PopOutHomeControllerFactory_DefaultIsTheNamedProductionFactory`
`EfcHomeController_DepositsTheCarryOnTheDataModelBeforeConstructingTheFormController`

Test class names: `WebView2BreadcrumbHostIssue792Tests`, `WebView2EnvironmentContractTests`, `BreadcrumbBridgeRouterIssue792Tests`, `BreadcrumbOutboundQueueIssue792Tests`, `EfcFormControllerIssue792Tests`, `EfcDataModelIssue792CarryTests`, `QfcCollectionControllerIssue792PopOutTests`, `EfcViewerQueueIssue792Tests`.

## Command reference (paste verbatim into each task that names it)

CMD-OUTLOOK (human checkpoint inside every build task):

```powershell
$outlook = Get-Process -Name OUTLOOK -ErrorAction SilentlyContinue
if ($outlook) { Write-Output 'HALT: Outlook is running. Close Outlook through File > Exit (never end the process), then re-run this task.'; exit 2 }
Write-Output 'OUTLOOK-CLOSED: true'
```

CMD-BASE (binds `$BaseSha` and `$SpecRefSha`):

```powershell
$m = Select-String -LiteralPath 'docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/p0-t7-git-base.md' -CaseSensitive -Pattern '^BASE-SHA: ([0-9a-f]{40})$'
if (-not $m) { throw 'BASE-SHA line missing' }
$BaseSha = $m.Matches[0].Groups[1].Value
$s = Select-String -LiteralPath 'docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/p0-t7-git-base.md' -CaseSensitive -Pattern '^SPEC-REF-SHA: ([0-9a-f]{40})$'
if (-not $s) { throw 'SPEC-REF-SHA line missing' }
$SpecRefSha = $s.Matches[0].Groups[1].Value
```

CMD-VSTEST (binds `$vstest`; vswhere is not on PATH):

```powershell
$vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio/Installer/vswhere.exe'
$vstest = & $vswhere -latest -products * -find 'Common7/IDE/Extensions/TestPlatform/vstest.console.exe' | Select-Object -First 1
if (-not $vstest) { throw 'vstest.console.exe not found' }
```

CMD-BUILD-PLAIN (produces test assemblies; not a gate): `msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU"`

CMD-BUILD-ANALYZE (gate): `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`

CMD-BUILD-NULLABLE (gate): `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true` (no `/p:Nullable=enable`, ever)

CMD-SCOPED-RUN (QuickFiler.Test only; `<FILTER>` is spelled out by each task; TRX goes to the gitignored `coverage/test-results/<task>` directory):

```powershell
& $vstest QuickFiler.Test/bin/Debug/QuickFiler.Test.dll /Settings:scripts/vscode/TaskMaster.cli.runsettings /InIsolation "/TestCaseFilter:<FILTER>" "/ResultsDirectory:coverage/test-results/<task>" "/Logger:trx;LogFileName=<task>.trx"
```

CMD-COVERAGE (QuickFiler.Test scope; raw output gitignored):

```powershell
pwsh -NoProfile -WorkingDirectory <repo-root> -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot QuickFiler.Test -Configuration Debug -CoverageOutput coverage/<stage>.cobertura.xml
```

The script accepts only `-SearchRoot`, `-Configuration`, `-CoverageOutput` and `-NoExecute` (`Invoke-MSTestWithCoverage.ps1:1-13`); the TRX of every stage is the runner default `coverage/test-results/mstest-coverage-run.trx` and is overwritten by the next stage, so each stage transcribes its console `Total tests:` and `Passed:` lines before the next stage runs. The runner also writes `coverage/<stage>.jacoco.xml` and `coverage/test-results/mstest-coverage-run.summary.txt` (both gitignored). The processed document is retained only because its parent directory is exactly `coverage/` (`Invoke-MSTestWithCoverage.Projection.ps1:148-163`); it is never redirected into a subdirectory. `<repo-root>` is the item worktree root, substituted at run time and never written into an artifact. The runner writes the post-processed document at its line 384 BEFORE the 80 percent assertion at line 386 throws, so a non-zero exit caused solely by the message `is below the required 80% threshold` still leaves a processed document (it contains the literal `<sources>`) and the task continues. Any other non-zero exit is a HALT. On success the runner prints `Done. Coverage artifact:`.

CMD-SWEEP (TaskMaster.Test regression sweep, no coverage):

```powershell
& $vstest TaskMaster.Test/bin/Debug/TaskMaster.Test.dll /Settings:scripts/vscode/TaskMaster.cli.runsettings /InIsolation "/TestCaseFilter:TestCategory!=LiveOutlook" "/Blame:CollectHangDump;TestTimeout=4min;HangDumpType=None" "/ResultsDirectory:coverage/test-results/<task>" "/Logger:trx;LogFileName=<task>.trx"
```

CMD-AC-U6-GATE (structural parity; run unchanged in [P0-T14] expecting FAIL and in [P6-T1] expecting PASS; write it once to `coverage/plan792-helper.ps1` and invoke as `pwsh -NoProfile -WorkingDirectory <repo-root> -File coverage/plan792-helper.ps1`; the opening branch assertion is the worktree proof and `(Get-Location).Path` is never written to an artifact):

```powershell
$branch = git rev-parse --abbrev-ref HEAD
if ($branch -ne 'bug/breadcrumb-webview2-init-fails-resource-not-in-correct-state-792') { throw "wrong worktree: $branch" }
$root = (Get-Location).Path
function Rel([string]$p) { return $p.Substring($root.Length).TrimStart('\', '/').Replace('\', '/') }
$files = git ls-files -- ':(glob)QuickFiler/**/*.cs' | Where-Object { $_ -notmatch '\.Designer\.cs$' }
$hits = foreach ($f in $files) { Select-String -LiteralPath $f -Pattern 'CoreWebView2EnvironmentOptions' | Where-Object { $_.Line -notmatch '^\s*//' } }
$constructions = @($hits | Where-Object { $_.Line -match 'new\s+CoreWebView2EnvironmentOptions\s*\(' -or $_.Line -match 'CoreWebView2EnvironmentOptions\s+\w+\s*=\s*new\s*\(' })
$createAsync = @(foreach ($f in $files) { Select-String -LiteralPath $f -Pattern 'CoreWebView2Environment\.CreateAsync\(' | Where-Object { $_.Line -notmatch '^\s*//' -and $_.Path.Replace('\', '/') -notmatch '(^|/)WebView2CoreInitializer\.cs$' } })
$seamCallers = @(foreach ($f in $files) { Select-String -LiteralPath $f -Pattern '\.CreateEnvironmentAsync\(' | Where-Object { $_.Line -notmatch '^\s*//' } })
$contractReaders = @(foreach ($f in $files) { Select-String -LiteralPath $f -Pattern 'WebView2EnvironmentContract\.CreateOptions\(' | Where-Object { $_.Line -notmatch '^\s*//' } })
Write-Output "PRIMARY-CONSTRUCTION-COUNT: $($constructions.Count)"
$constructions | ForEach-Object { Write-Output "PRIMARY-SITE: $(Rel $_.Path):$($_.LineNumber)" }
Write-Output "CREATEASYNC-OUTSIDE-ADAPTER: $($createAsync.Count)"
$createAsync | ForEach-Object { Write-Output "CREATEASYNC-SITE: $(Rel $_.Path):$($_.LineNumber)" }
Write-Output "SEAM-CALLER-COUNT: $($seamCallers.Count)"
$seamCallers | ForEach-Object { Write-Output "SEAM-CALLER: $(Rel $_.Path):$($_.LineNumber)" }
Write-Output "CONTRACT-READER-COUNT: $($contractReaders.Count)"
$contractReaders | ForEach-Object { Write-Output "CONTRACT-READER: $(Rel $_.Path):$($_.LineNumber)" }
$pass = ($constructions.Count -eq 1) -and ($constructions[0].Path.Replace('\', '/') -match '(^|/)QuickFiler/Viewers/WebView2EnvironmentContract\.cs$') -and ($createAsync.Count -eq 0) -and ($seamCallers.Count -eq 3) -and ($contractReaders.Count -eq 3)
Write-Output "AC-U6-STRUCTURAL: $(if ($pass) { 'PASS' } else { 'FAIL' })"
```

Expected on the unfixed tree: `PRIMARY-CONSTRUCTION-COUNT: 3` (host 250, ViewerSetup 62, EfcItemController 188), `CREATEASYNC-OUTSIDE-ADAPTER: 1` (EfcItemController 195), `SEAM-CALLER-COUNT: 2` (host 265, ViewerSetup 71), `CONTRACT-READER-COUNT: 0`, `AC-U6-STRUCTURAL: FAIL`. Expected on the fixed tree: 1 (the contract file), 0, 3, 3, PASS. The dead comment lines `ViewerSetup.cs:61`, `EfcItemController.cs:187` and `ViewerSetup.cs:124` are excluded by the `^\s*//` filter, which is what defeats the file-granularity trap recorded in the research record.

## Write set and scope lock

Production (QuickFiler.csproj): `Viewers/WebView2EnvironmentContract.cs` (new), `Viewers/WebView2BreadcrumbHost.cs`, `Controllers/QfcItemController.ViewerSetup.cs`, `Controllers/EfcItemController.cs`, `Controllers/EfcItemController.WebViewEnvironment.cs` (new), `Controllers/BreadcrumbBridgeRouter.cs`, `Controllers/BreadcrumbOutboundQueue.cs`, `Controllers/EfcFormController.cs`, `Controllers/EfcFormController.Breadcrumb.cs` (new), `Controllers/EfcFormController.SetupAndProperties.cs` (new), `Controllers/EfcFormController.EventHandlers.cs` (new), `Controllers/EfcFormController.Actions.cs` (new), `Controllers/EfcFormController.Helpers.cs` (new), `Controllers/QfcCollectionController.cs`, `Controllers/QfcCollectionController.PopOut.cs` (new), `Controllers/EfcHomeController.cs`, `Controllers/EfcDataModel.cs`, `Controllers/EfcDataModel.Carry.cs` (new), `Controllers/QfcItemController.cs`, `Helper Classes/EfcViewerQueue.cs`, `QuickFiler/QuickFiler.csproj`.

Tests (QuickFiler.Test.csproj): `Viewers/WebView2BreadcrumbHostIssue792Tests.cs`, `Viewers/WebView2EnvironmentContractTests.cs`, `Controllers/BreadcrumbBridgeRouterIssue792Tests.cs`, `Controllers/BreadcrumbOutboundQueueIssue792Tests.cs`, `Controllers/EfcFormControllerIssue792Tests.cs`, `Controllers/QfcCollectionControllerIssue792PopOutTests.cs`, `Controllers/EfcDataModelIssue792CarryTests.cs`, `Helper Classes/EfcViewerQueueIssue792Tests.cs` (all new), `Controllers/EfcFormControllerTests.cs` (listed by the spec; this plan makes NO edit to it because its 15-line headroom cannot hold the strengthened test, which lives in `EfcFormControllerIssue792Tests.cs` instead; recorded as a zero-diff observation in [P6-T3]), `QuickFiler.Test/QuickFiler.Test.csproj`.

Scope lock: no other `.cs`, `.csproj`, `.config`, `.runsettings` or policy file changes. Compile items are bare self-closing `<Compile Include="..." />` elements with no metadata, inserted adjacent to the neighbours named in each creating task.

Files with class-level `[ExcludeFromCodeCoverage]` touched by this plan: `EfcItemController` (`EfcItemController.cs:26`), `QfcCollectionController` (`QfcCollectionController.cs:22`). Their partials are uninstrumented.

## Constraint blocks (propagated verbatim; every child prompt must carry all five)

[BLOCK: OBSERVED-FAILING CRITERIA — propagate verbatim]
Every acceptance criterion used as a gate must be OBSERVED FAILING on the unfixed tree before it is accepted, and the plan must include running it against the unfixed tree to demonstrate that. A criterion never seen failing is unproven. Two failure modes: one that cannot be satisfied at all, and one satisfied with no fix present — the second is more dangerous because it reports success. Prove non-vacuity by mutation where practical, with each mutation failing on its pre-predicted assertion.
[END BLOCK]

[BLOCK: TEST PARALLELISM — propagate verbatim]
Tests must ALWAYS run in parallel. `scripts/vscode/TaskMaster.cli.runsettings` with Workers=0 and Scope=ClassLevel is correct and must remain byte-identical. Never fix a parallel-execution failure with Workers=1, removing Parallelize, [DoNotParallelize], dropping /Settings:, retries, timing tolerance, or sleeps. If a test needs a distinct thread, create and join a dedicated Thread so the property is controlled, not assumed — see issue #900 for the working pattern.
[END BLOCK]

[BLOCK: DIFF BASES — propagate verbatim]
Anchor every diff base, merge base and change-footprint gate to `origin/main` after a fetch, never to bare local `main`. Only the pulling checkout advances local `main`, so it is almost always stale and any gate anchored to it is unsatisfiable by construction. Three-dot does not rescue it: when the pinned ref is an ancestor of HEAD, `merge-base(PINNED,HEAD)` IS the pinned SHA. Run both forms and compare.
[END BLOCK]

[BLOCK: SCOPE AND COMMIT — propagate verbatim]
Commit your work to the item worktree branch before ending. Never write to another item's canonical state — if a hook can only be satisfied by modifying a file another item owns, STOP AND REPORT. Do not write to `.claude/agent-memory` in a session worktree you do not own; note that `Set-Location` does not update .NET's `CurrentDirectory`, so `System.IO` calls with relative paths can escape the worktree.
[END BLOCK]

[BLOCK: BASH DISCIPLINE — propagate verbatim]
Settings allow only `git *`, `pwsh *`, `poetry run *` and three lib scripts; every chained segment must match, so `cd X && ...`, grep, sed and cat via Bash will prompt. Use `git -C` and the Read/Grep/Glob tools. Gated-command hooks scan the WHOLE command string, so prose merely naming a gated tool inside a commit message or a `-b` body trips the block — write text to a file and use `-F body=@file`. Never chain a state write with a gated command.
[END BLOCK]

## Observed-failing map (which run demonstrates each gate failing on the unfixed tree)

| AC | Observed failing at | Non-vacuity mutation |
|---|---|---|
| AC-U1 | [P3-T7] (retry, label, router tests) | [P5-T5] attempt limit |
| AC-U2 | [P3-T7] (pending-cleared tests) | [P5-T7] pending not cleared |
| AC-U3 | [P3-T7] (adoption, carry read); UI-thread half at [P1-T4] | [P5-T6] deposit removed, [P5-T8] adoption disabled |
| AC-U4 | `InitializeBreadcrumbHostAsync` half at [P1-T4]; `PopulateFolderCombobox` half CANNOT be observed failing (already satisfied at `EfcFormController.cs:1270`), dossier at [P0-T15] | [P5-T1] notifier dropped |
| AC-U5 | manual; not a gate | none |
| AC-U6 | [P0-T14] structural gate FAIL; [P1-T4] site-1 seam test | [P5-T2] site-1 regression and site-2 regression (structural), [P5-T3] constant |
| AC-U7 | [P3-T7] (queue tests) | [P5-T4] discard skipped |
| AC-U8 | [P0-T8] records the seventeen new paths ABSENT and the pre-change counts (the gate's FAIL state) | measured, not mutated |
| AC-U9 | documentary; measured before/after in [P6-T2] | none |

### Phase 0 — Policy Reads, Toolchain Bootstrap, and Baselines

- [x] [P0-T1] Read the policy files in the `policy-compliance-order` sequence — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, `.claude/rules/plan-acceptance-gates.md` — then read `$FEATURE/spec.md`, `$FEATURE/user-story.md` and `$FEATURE/research/2026-09-17T11-20-breadcrumb-webview2-init-research.md`, and write `$FEATURE/evidence/baseline/phase0-instructions-read.md`.
  - Acceptance: the artifact contains `Timestamp:`, `Policy Order:` listing the seven policy files in the order above, an explicit `Files Read:` list naming all ten files, and the line `Work Mode: full-bug` copied from `issue.md` line 12. The executor records that `spec.md` lines 306-314 hold exactly nine `- [ ] AC-U` checkbox lines (mechanical count: `Select-String -LiteralPath $FEATURE/spec.md -Pattern '^- \[ \] AC-U[1-9]:'` returns 9 matches).
- [x] [P0-T2] Human checkpoint before any build: confirm Outlook is closed through its own exit and record `$FEATURE/evidence/other/p0-t2-outlook-closed.md`.
  - Command: CMD-OUTLOOK.
  - Acceptance: output contains `OUTLOOK-CLOSED: true`; artifact carries `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` and the sentence that Outlook was closed by a person through its normal exit and no process was ended. If the command printed `HALT:`, the task waits for the human and is re-run; `EXIT_CODE: 2` is never a pass.
- [x] [P0-T3] Bootstrap the repo-local .NET SDK with `pwsh -NoProfile -File scripts/vscode/Install-RepoDotNetSdk.ps1` and record `$FEATURE/evidence/baseline/p0-t3-sdk.md`.
  - Acceptance: `dotnet --version` run from `<repo-root>` prints a version beginning `8.0.` (not the `global.json` `errorMessage`); the artifact records `DOTNET-VERSION: <value>` and `SDK-UNDER-REPO-LOCAL-DIR: true|false` computed from the `--list-sdks` output (true when at least one line's path segment ends `.dotnet-sdk/sdk` after `.Replace('\', '/')`), which is not transcribed because it carries the account name; `SDK-UNDER-REPO-LOCAL-DIR: false` is a HALT. `.dotnet-sdk/` is gitignored (`.gitignore:350`), so the tree stays clean.
- [x] [P0-T4] Restore the manifest tool with `dotnet tool restore` (manifest is the repository-root `dotnet-tools.json`) and record `$FEATURE/evidence/baseline/p0-t4-tool-restore.md`.
  - Acceptance: `EXIT_CODE: 0`; `dotnet tool list` prints a row whose first column is `csharpier` and second column is `1.2.6`; both outputs are in `Output Summary:`.
- [x] [P0-T5] Restore NuGet packages for the solution with `pwsh -NoProfile -File scripts/vscode/Invoke-Restore.ps1` (msbuild `/t:Restore /p:RestorePackagesConfig=true`) and record `$FEATURE/evidence/baseline/p0-t5-nuget-restore.md`.
  - Acceptance: `EXIT_CODE: 0`; `Test-Path packages/Moq.4.20.72` or, if that exact folder does not exist, `(Get-ChildItem packages -Directory -Filter 'Moq.*').Count -ge 1` is true (the executor records which form held); every `<Analyzer Include>` path in `QuickFiler/QuickFiler.csproj` and `QuickFiler.Test/QuickFiler.Test.csproj` resolves under `Test-Path` when joined to the declaring project's directory (record `ANALYZER-PATHS-RESOLVED: <n> of <n>`); a mismatch is a HALT (environment defect, not a plan defect).
- [x] [P0-T6] Ensure the `dotnet-coverage` global tool exists (`if (-not (Get-Command dotnet-coverage -ErrorAction SilentlyContinue)) { dotnet tool install --global dotnet-coverage }`) and record `$FEATURE/evidence/baseline/p0-t6-dotnet-coverage.md`.
  - Acceptance: `Get-Command dotnet-coverage` succeeds; the artifact records only `DOTNET-COVERAGE-PRESENT: true` and the tool's `--version` output, never the resolved path (it contains the account name).
- [x] [P0-T7] Capture the git base: `git fetch origin`, then record `$FEATURE/evidence/baseline/p0-t7-git-base.md`.
  - Commands: `git rev-parse HEAD`; `git rev-parse origin/main`; `git merge-base HEAD origin/main`; `git merge-base --is-ancestor origin/main HEAD; $LASTEXITCODE`; `git status --porcelain --untracked-files=all`; `git diff --name-status (git merge-base HEAD origin/main) HEAD`.
  - Acceptance: the artifact contains exactly one line `BASE-SHA: <40 hex>` whose value equals the merge-base output; a line `ORIGIN-MAIN-IS-ANCESTOR: true|false` with the explicit note that when true the merge base IS the `origin/main` SHA (both forms compared, per the DIFF BASES block); `HEAD-OBSERVED:` (observation only, never an expectation); a line `SPEC-REF-SHA: <40 hex>` equal to the `git rev-parse HEAD` output, with the note that the feature folder does not exist at `BASE-SHA`, so criterion-text immutability is checked against `SPEC-REF-SHA`; the verbatim porcelain output; and the type assertion `PORCELAIN-SOURCE-PATHS: 0` computed as the count of porcelain lines whose path ends `.cs`, `.csproj`, `.sln` or `packages.config`. A non-zero source-path count is a HALT. The name-status list is recorded verbatim under a heading `BASE-TO-HEAD-NAME-STATUS` for later scope gates.
- [x] [P0-T8] Record the baseline line counts of every write-set `.cs` file and the absence of the seventeen new files in `$FEATURE/evidence/baseline/p0-t8-line-counts.md` (instrument: `(Get-Content -LiteralPath <path>).Count`).
  - Acceptance: the artifact lists one row per existing write-set `.cs` path (eleven production, one test) with its count, and the following expected values hold exactly: `QuickFiler/Controllers/EfcFormController.cs` 1321, `QuickFiler/Controllers/EfcItemController.cs` 1122, `QuickFiler/Controllers/QfcCollectionController.cs` 2333, `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` 479, `QuickFiler/Controllers/EfcDataModel.cs` 499, `QuickFiler/Controllers/EfcHomeController.cs` 447, `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` 407, `QuickFiler/Viewers/WebView2BreadcrumbHost.cs` 368, `QuickFiler/Controllers/QfcItemController.cs` 334, `QuickFiler/Helper Classes/EfcViewerQueue.cs` 101, `QuickFiler/Controllers/BreadcrumbOutboundQueue.cs` 67, `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` 485. A different value for any of these twelve is a HALT (stale checkout). Each of the seventeen new paths named in the Write set section is recorded with `ABSENT: true` (`Test-Path` false); any present is a HALT. The line `OVER-CEILING-BEFORE: 3` names the three files above 500.
- [x] [P0-T9] Formatter baseline (read-only): `dotnet tool run csharpier check .`; record `$FEATURE/evidence/baseline/p0-t9-csharpier-check.md`.
  - Acceptance: `EXIT_CODE:` recorded (0 or 1); the `Checked N files in` line recorded verbatim; the set of files the check reports as unformatted recorded verbatim under `BASELINE-DRIFT-SET` (empty when exit 0). The line `DRIFT-IN-WRITE-SET: <n>` counts drift paths that are write-set members; a non-zero value is recorded, not a halt, and binds [P7-T2].
- [x] [P0-T10] Analyzer baseline: CMD-OUTLOOK then CMD-BUILD-ANALYZE; record `$FEATURE/evidence/baseline/p0-t10-analyzers.md`.
  - Acceptance: `OUTLOOK-CLOSED: true` first; `EXIT_CODE: 0`; `Output Summary:` carries the msbuild summary lines matching `Warning\(s\)` and `Error\(s\)` verbatim with `0 Error(s)` asserted by an exact-line match (`^\s+0 Error\(s\)$`, so `10 Error(s)` cannot satisfy it).
- [x] [P0-T11] Nullable baseline: CMD-OUTLOOK then CMD-BUILD-NULLABLE; record `$FEATURE/evidence/baseline/p0-t11-nullable.md`.
  - Acceptance: as [P0-T10] (`EXIT_CODE: 0`, exact `0 Error(s)` line).
- [x] [P0-T12] Coverage baseline over `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` via CMD-COVERAGE with `<stage>` = `p0-baseline`; record `$FEATURE/evidence/baseline/p0-t12-coverage-baseline.md`.
  - Acceptance: the processed document `coverage/p0-baseline.cobertura.xml` exists and contains the literal `<sources>`; `EXIT_CODE:` recorded; when the runner exits non-zero and this run's console carries `is below the required 80% threshold` and its `Failed:` category is omitted, the artifact declares `ExpectedExitCode: 1`; when the runner exits 0 no `ExpectedExitCode` field is written; any other cause is a HALT (the expectation is keyed off this run, never off any other run). The artifact records numerically: root `line-rate`, `branch-rate`, `lines-covered`, `lines-valid`, `branches-covered`, `branches-valid`; the same six for the `package` element named `QuickFiler`; `Total tests:` and `Passed:` transcribed from the console before any later stage overwrites the runner-default TRX; `QUICKFILER-SCOPED-DOCUMENT-LINE-RATE: <root line-rate>` and `REPO-WIDE-FLOOR: NOT MEASURED (single test assembly; see D10)`. Per-file rows (max-hits merge over both `./lines/line` and `./methods/method/lines/line` axes of every `class` whose `filename`, after `.Replace('\', '/')`, matches `(^|/)QuickFiler/<relative path>$`) for each of: `Viewers/WebView2BreadcrumbHost.cs`, `Controllers/BreadcrumbBridgeRouter.cs`, `Controllers/BreadcrumbBridgeRouter.Selection.cs`, `Controllers/BreadcrumbOutboundQueue.cs`, `Controllers/EfcFormController.cs`, `Controllers/EfcHomeController.cs`, `Controllers/EfcDataModel.cs`, `Controllers/QfcItemController.cs`, `Controllers/QfcItemController.ViewerSetup.cs`, `Helper Classes/EfcViewerQueue.cs` — each row is `covered/valid` plus the full per-line `number:hits` map; and under `Uninstrumented files` the rows `Controllers/EfcItemController.cs` and `Controllers/QfcCollectionController.cs` with `CLASS-ELEMENTS: 0` and the citation of their class-level attribute. A `CLASS-ELEMENTS: 0` result for any of the ten instrumented files is a HALT. The raw XML is not copied under the feature folder.
- [x] [P0-T13] Regression-sweep baseline over `TaskMaster.Test/bin/Debug/TaskMaster.Test.dll` via CMD-VSTEST then CMD-SWEEP with `<task>` = `p0-t13`; record `$FEATURE/evidence/baseline/p0-t13-taskmaster-sweep.md`.
  - Acceptance: console `Total tests:` and `Passed:` transcribed (with the omitted-category convention); `BASELINE_FAILURE_SET:` lists every failed fully-qualified test name verbatim or `none`; `EXIT_CODE:` recorded (non-zero only when the failure set is non-empty). A hang (no summary within the Blame timeout) is a HALT naming the in-progress tests from the Blame output.
- [x] [P0-T14] Run CMD-AC-U6-GATE against the unfixed tree (write the block to `coverage/plan792-helper.ps1`, run it as `pwsh -NoProfile -WorkingDirectory <repo-root> -File coverage/plan792-helper.ps1`; its branch assertion is the worktree proof) and record `$FEATURE/evidence/regression-testing/p0-t14-ac-u6-structural-fail-before.md`.
  - Acceptance: output lines are exactly `PRIMARY-CONSTRUCTION-COUNT: 3`, three `PRIMARY-SITE:` lines naming `QuickFiler/Controllers/EfcItemController.cs:188`, `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs:62`, `QuickFiler/Viewers/WebView2BreadcrumbHost.cs:250`, `CREATEASYNC-OUTSIDE-ADAPTER: 1` followed by one `CREATEASYNC-SITE:` line naming `QuickFiler/Controllers/EfcItemController.cs:195`, `SEAM-CALLER-COUNT: 2` followed by two `SEAM-CALLER:` lines naming `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs:71` and `QuickFiler/Viewers/WebView2BreadcrumbHost.cs:265` (the three `PRIMARY-SITE:` and two `SEAM-CALLER:` lines appear in `git ls-files` order), `CONTRACT-READER-COUNT: 0` (followed by no `CONTRACT-READER:` line), and `AC-U6-STRUCTURAL: FAIL`. The artifact records `OBSERVED-FAILING: AC-U6 structural gate` and the sentence that the dead comment lines 61 (ViewerSetup) and 187 (EfcItemController) were excluded by the comment filter. Any other output is a HALT.
- [x] [P0-T15] Record the AC-U4 pass-before observation for the `PopulateFolderCombobox` half: CMD-VSTEST, CMD-BUILD-PLAIN (after CMD-OUTLOOK), then CMD-SCOPED-RUN with `<FILTER>` = `FullyQualifiedName~EfcFormControllerTests.PopulateFolderCombobox_WhenDataModelFaults_LogsOnceAndDoesNotFault` and `<task>` = `p0-t15`; write `$FEATURE/evidence/regression-testing/fail-before-exception.p0-t15.md`.
  - Acceptance: `Total tests: 1`, `Passed: 1`; the dossier carries `WhyFailingRunImpossible:` stating that `TryReportBoundaryFault(ex.Message, ex)` already exists at `QuickFiler/Controllers/EfcFormController.cs:1270`, so this half of AC-U4 cannot be observed failing and its work is the strengthened user-surface test whose non-vacuity is proven by mutation in [P5-T1]; plus `SearchScope:`, `SearchPatterns:`, `SearchResult:` fields.
- [x] [P0-T16] Commit the Phase 0 artifacts: `git add -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "chore(792): phase 0 baselines and plan"`; record `$FEATURE/evidence/baseline/p0-t16-commit.md`.
  - Acceptance: `EXIT_CODE: 0`; `git show --name-only --format= HEAD` lists only paths under `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/`; `git status --porcelain -- '*.cs' '*.csproj' '*.sln' 'packages.config'` prints nothing; the commit SHA is recorded as an observation.

### Phase 1 — Compile-Clean Regression Tests Observed Failing

These tests reference only members that exist on the unfixed tree, so they compile today and fail at assertion time. Every test class is `[TestClass] public sealed class`, MSTest + Moq + FluentAssertions, Arrange-Act-Assert, no temp files, no sleeps, no `[DoNotParallelize]`.

- [x] [P1-T1] Create `QuickFiler.Test/Viewers/WebView2BreadcrumbHostIssue792Tests.cs` (class `WebView2BreadcrumbHostIssue792Tests`) with two tests, and add `<Compile Include="Viewers\WebView2BreadcrumbHostIssue792Tests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the existing `Viewers\WebView2BreadcrumbHostTests.cs` item (line 218).
  - Test 1 `InitializeAsync_PassesTheSharedFolderAndIncognitoArgumentToTheSeam`: on a `WinFormsPumpHost` (pattern of `WebView2BreadcrumbHostTests.InitializeAsync_InstallsUiDispatcherFromUiSyncContext`, lines 258-293) construct a `WebView2` and a host over a `Mock<IWebViewCoreInitializer>` whose `CreateEnvironmentAsync` setup uses `.Callback<string, CoreWebView2EnvironmentOptions>` to capture both arguments and returns `Task.FromResult<CoreWebView2Environment>(null)`, and whose `EnsureCoreWebView2Async` returns `Task.CompletedTask`; act `await subject.InitializeAsync(pump.SyncContext)`; assert the captured folder equals `Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "WindowsFormsWebView2")` and the captured `options.AdditionalBrowserArguments` equals the literal `"--incognito "`. Pre-fix the second assertion fails (the parameterless options carry null).
  - Test 2 `NavigateToString_BeforeCoreInitialization_WithNoDispatcher_DropsTheDocumentWithoutThrowing`: construct the host with the internal three-argument constructor and a null dispatcher (never call `InitializeAsync`), act `subject.NavigateToString("<html></html>")` from the MSTest thread, assert it does not throw and `subject.IsCoreInitialized` is false. Pre-fix the forward reaches `WebView2.NavigateToString` on a control with no core and throws `InvalidOperationException`.
  - Acceptance: file exists; contains both method names on single lines; the csproj item exists exactly once and is a bare self-closing element adjacent to line 218's item; file line count is at most 200.
- [x] [P1-T2] Create `QuickFiler.Test/Controllers/EfcFormControllerIssue792Tests.cs` (class `EfcFormControllerIssue792Tests`, namespace `QuickFiler.Controllers.Tests`) with private helpers `CreateMinimalController()` (private no-arg constructor via reflection, as `EfcFormControllerTests.cs:24-34`), `SetPrivateField(object, string, object)` (`BindingFlags.Instance | BindingFlags.NonPublic`), and a `CaptureUserFaults(List<string>)` helper that sets `EfcFormController.UserFaultNotifier` for the current async flow and returns an `IDisposable` restoring the previous value; add two tests; add `<Compile Include="Controllers\EfcFormControllerIssue792Tests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Controllers\EfcFormControllerTests.Part2.cs` item (line 127).
  - Test 1 `PopulateFolderCombobox_WhenDataModelFaults_NotifiesTheUserThroughTheDefaultSink`: minimal controller, `_formViewer` set to `FormatterServices.GetUninitializedObject(typeof(EfcViewer))`, `BoundaryErrorSink` left at its default, notifier captured; act `await controller.PopulateFolderCombobox()`; assert no throw and the captured list has exactly one entry. This is the strengthened AC-U4 test for the already-satisfied half; it PASSES before the fix and is mutation-proven in [P5-T1].
  - Test 2 `InitializeBreadcrumbHostAsync_WhenHostIsNull_ReportsThroughTheBoundarySinkToTheUser`: minimal controller (`_breadcrumbHost`, `_router`, `_formViewer` all null), notifier captured; act by invoking the method through `typeof(EfcFormController).GetMethod("InitializeBreadcrumbHostAsync", BindingFlags.Instance | BindingFlags.NonPublic)` and awaiting the returned `Task`; assert no throw and the captured list has exactly one entry whose text contains the token `after 3 attempts`. Pre-fix the null host raises `NullReferenceException`, which the old catch logs only, so the list stays empty.
  - Acceptance: file exists with both method names on single lines; csproj item present exactly once adjacent to line 127's item; line count at most 260 (Phase 3 appends five more tests; the final ceiling is checked in [P7-T3]).
- [x] [P1-T3] Create `QuickFiler.Test/Helper Classes/EfcViewerQueueIssue792Tests.cs` (class `EfcViewerQueueIssue792Tests`, namespace `QuickFiler.Test.HelperClasses`) with one identity test, and add `<Compile Include="Helper Classes\EfcViewerQueueIssue792Tests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Helper Classes\ViewerQueueStaticWrapperTests.cs` item (line 230).
  - The test `ProductionBlockingPriorityScheduler_DefaultIsTheNamedUiDispatcherInvoke`: read `EfcViewerQueue.ProductionBlockingPriorityScheduler` (no mutation, no write to any static), assert `.Method.Name` equals the literal `"InvokeOnUiDispatcher"` and `.Target` is null. The test does not invoke the delegate (D8) and does not call `ResetProductionCoreDefaultsForTesting()`, because that write belongs to the `[DoNotParallelize]` class `ViewerQueueStaticWrapperTests` (substitutes at `:254`, restores at `:18`). Pre-fix it fails because the default is a lambda whose compiler-generated name is not `InvokeOnUiDispatcher`.
  - Acceptance: file exists with the method name on a single line; csproj item present exactly once adjacent to line 230's item; the file contains no `DoNotParallelize` token and no `ResetProductionCoreDefaultsForTesting` token; line count at most 120.
- [x] [P1-T4] [expect-fail] Build and run the Phase 1 classes: CMD-OUTLOOK, CMD-BUILD-PLAIN, CMD-VSTEST, then CMD-SCOPED-RUN with `<FILTER>` = `FullyQualifiedName~WebView2BreadcrumbHostIssue792Tests|FullyQualifiedName~EfcFormControllerIssue792Tests|FullyQualifiedName~EfcViewerQueueIssue792Tests` and `<task>` = `p1-t4`; record `$FEATURE/evidence/regression-testing/p1-t4-fail-before.md`.
  - Acceptance: build `EXIT_CODE: 0` with `0 Error(s)` (exact line); the run reports `Total tests: 5`, `Passed: 1`, `Failed: 4`; the artifact lists per test `FAIL-BEFORE: <FullyQualifiedName> | <first assertion message line>` for exactly these four: the two host tests, `InitializeBreadcrumbHostAsync_WhenHostIsNull_ReportsThroughTheBoundarySinkToTheUser`, and `ProductionBlockingPriorityScheduler_DefaultIsTheNamedUiDispatcherInvoke`; and `PASS-BEFORE-CONTROL: EfcFormControllerIssue792Tests.PopulateFolderCombobox_WhenDataModelFaults_NotifiesTheUserThroughTheDefaultSink`. Any other split is a HALT. `EXIT_CODE:` records vstest's non-zero exit with `ExpectedExitCode: 1`.

### Phase 2 — Pure Moves, Declaration-Only Seams, and Project Wiring

No behaviour changes in this phase. Every move is verified by a conservation gate: the multiset of non-structural lines (after trimming, dropping blank lines, `using` lines, `namespace` lines, brace-only lines and lines containing `partial class <Type>` or `class <Type>`) across the resulting files equals that of the original file at `BASE-SHA`. The gate is:

```powershell
$filter = { param($lines) $lines | ForEach-Object { $_.Trim() } | Where-Object { $_ -ne '' -and $_ -notmatch '^using ' -and $_ -notmatch '^namespace ' -and $_ -notmatch '^[{}]$' -and $_ -notmatch 'class <Type>\b' } | Sort-Object }
$before = & $filter (git show "${BaseSha}:<original path>")
$after = & $filter (Get-Content -LiteralPath <result path 1>, <result path 2>, ...)
$diff = @(Compare-Object $before $after)
Write-Output "CONSERVATION-DIFF-COUNT: $($diff.Count)"
```

`<Type>` is the moved type's simple name; each task states its own operands. `CONSERVATION-DIFF-COUNT: 0` is the pass condition.

- [x] [P2-T1] Split `QuickFiler/Controllers/EfcFormController.cs` into six partial parts per D9 (retained `EfcFormController.cs` lines 1-264 plus closing braces, and new `QuickFiler/Controllers/EfcFormController.SetupAndProperties.cs`, `EfcFormController.EventHandlers.cs`, `EfcFormController.Actions.cs`, `EfcFormController.Breadcrumb.cs`, `EfcFormController.Helpers.cs` with the line ranges stated in D9, each opened by the retained file's using block lines 1-22, a blank line, `namespace QuickFiler.Controllers`, `{`, `internal partial class EfcFormController`, `{`), change line 26 to `internal partial class EfcFormController : IFilerFormController`, and add five bare Compile items to `QuickFiler/QuickFiler.csproj` immediately after the `Controllers\EfcFormController.cs` item (line 296) in the order Actions, Breadcrumb, EventHandlers, Helpers, SetupAndProperties.
  - Acceptance: the conservation gate over the six files with `<Type>` = `EfcFormController` prints `CONSERVATION-DIFF-COUNT: 0`; every one of the six files is at most 400 lines (`(Get-Content -LiteralPath).Count`); `Select-String -Pattern 'partial class EfcFormController' -LiteralPath` each part returns exactly 1 match and the retained file's match also contains `: IFilerFormController`; `git diff --numstat $BaseSha -- QuickFiler/QuickFiler.csproj` reports 5 insertions and 0 deletions at this point; `Select-String -Pattern 'EfcFormController\.(Actions|Breadcrumb|EventHandlers|Helpers|SetupAndProperties)\.cs' -LiteralPath QuickFiler/QuickFiler.csproj` returns 5 matches, each on a line that, after `.Replace('\', '/')`, matches `^\s*<Compile Include="Controllers/EfcFormController\.[A-Za-z]+\.cs" />$`. The `#region`/`#endregion` lines travel with their members; `ConfigureBreadcrumbControl` through `BindBreadcrumbRowsAsync` (original 1044-1129) sit in `Breadcrumb.cs` and `ToggleExpansionStyle` (original 1291-1319) sits in `Helpers.cs`. Compilation is proven by [P2-T13], not here.
- [x] [P2-T2] Create `QuickFiler/Viewers/WebView2EnvironmentContract.cs` per D1 (`#nullable enable`; usings `System`, `System.IO`, `Microsoft.Web.WebView2.Core`; `namespace QuickFiler.Viewers` (the namespace of `WebView2BreadcrumbHost.cs:12`); `internal static class WebView2EnvironmentContract` with `internal const string AdditionalBrowserArguments = "--incognito ";`, `internal const string UserDataFolderName = "WindowsFormsWebView2";`, `internal static string ResolveUserDataFolder()` returning `Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), UserDataFolderName)`, and `internal static CoreWebView2EnvironmentOptions CreateOptions()` returning `new CoreWebView2EnvironmentOptions(AdditionalBrowserArguments)`; XML docs stating the shared-browser-process rule and HRESULT 0x8007139F), and add `<Compile Include="Viewers\WebView2EnvironmentContract.cs" />` to `QuickFiler/QuickFiler.csproj` immediately after the `Viewers\WebView2BreadcrumbHost.cs` item (line 426).
  - Acceptance: file exists, at most 60 lines; `Select-String -SimpleMatch 'internal const string AdditionalBrowserArguments = "--incognito ";'` returns 1; `Select-String -SimpleMatch 'UserDataFolderName = "WindowsFormsWebView2"'` returns 1; the csproj item exists exactly once, bare and adjacent. The type has no consumers yet; [P0-T14]'s gate would now count this file's construction (3 sites become 4), which is expected and not re-run here.
- [x] [P2-T3] Create `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs` (`internal partial class EfcItemController`, usings `System`, `System.IO`, `System.Threading.Tasks`, `Microsoft.Web.WebView2.Core`, `QuickFiler.Viewers`) by moving `IncognitoArgument` with its doc block (`EfcItemController.cs:168-177`) and `InitializeWebViewAsync` (`:179-212`) verbatim out of `EfcItemController.cs`, and add `<Compile Include="Controllers\EfcItemController.WebViewEnvironment.cs" />` to `QuickFiler/QuickFiler.csproj` immediately after the `Controllers\EfcItemController.WebViewFaultBoundary.cs` item (line 304).
  - Acceptance: conservation gate over `EfcItemController.cs` plus the new file with `<Type>` = `EfcItemController` prints `CONSERVATION-DIFF-COUNT: 0`; `EfcItemController.cs` count equals 1122 minus 46 = 1076 or 1077 (the executor records the exact value; a difference outside that pair is a HALT); the new file is at most 80 lines; csproj item present once, bare, adjacent. The `#region Item Setup and Disposal Methods` marker stays in `EfcItemController.cs`.
- [x] [P2-T4] Create `QuickFiler/Controllers/QfcCollectionController.PopOut.cs` (`public partial class QfcCollectionController`, usings `System`, `System.Threading.Tasks`, `Microsoft.Office.Interop.Outlook`, `QuickFiler.Interfaces`, `UtilitiesCS`) by moving `PopOutControlGroup` (`QfcCollectionController.cs:714-724`) and `PopOutControlGroupAsync` (`:726-739`) verbatim, and add `<Compile Include="Controllers\QfcCollectionController.PopOut.cs" />` to `QuickFiler/QuickFiler.csproj` immediately after the `Controllers\QfcCollectionController.CarrierLoad.cs` item (line 315).
  - Acceptance: conservation gate (`<Type>` = `QfcCollectionController`) prints `CONSERVATION-DIFF-COUNT: 0`; `QfcCollectionController.cs` count is 2306 or 2307 (record exact); csproj item present once, bare, adjacent. The class-level `[ExcludeFromCodeCoverage]` at `QfcCollectionController.cs:22` is untouched and, being type-level, covers the new part.
- [x] [P2-T5] Create `QuickFiler/Controllers/EfcDataModel.Carry.cs` (`internal partial class EfcDataModel`, usings `System.Threading.Tasks`, `UtilitiesCS`) by moving `InitFolderHandlerAsync` (`EfcDataModel.cs:188-221`) verbatim, and add `<Compile Include="Controllers\EfcDataModel.Carry.cs" />` to `QuickFiler/QuickFiler.csproj` immediately after the `Controllers\EfcDataModel.FilingStem.cs` item (line 290).
  - Acceptance: conservation gate (`<Type>` = `EfcDataModel`) prints `CONSERVATION-DIFF-COUNT: 0`; `EfcDataModel.cs` count is 464 or 465 (record exact); csproj item present once, bare, adjacent.
- [x] [P2-T6] Declare the router and queue seams without behaviour in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` and `QuickFiler/Controllers/BreadcrumbOutboundQueue.cs`: in the router, after `NotifyCoreInitialized` (line 329), add `internal const string InitializationFailedBannerText = BreadcrumbRowBuilder.BannerPrefix + " Folder list unavailable: breadcrumb initialization failed";` and `public void NotifyInitializationFailed(Exception failure)` whose only statement is `if (failure == null) { throw new ArgumentNullException(nameof(failure)); }`; in the queue, after `OnInitializationCompleted` (line 65), add `public int DiscardPending()` whose only statement is `return 0;`. Each carries a one-line `/// <summary>` ending with `Body lands in Phase 4 (#792).`.
  - Acceptance: `Select-String -SimpleMatch 'public void NotifyInitializationFailed(Exception failure)' -LiteralPath QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` returns 1; `Select-String -SimpleMatch 'public int DiscardPending()' -LiteralPath QuickFiler/Controllers/BreadcrumbOutboundQueue.cs` returns 1; `Select-String -SimpleMatch ' Folder list unavailable: breadcrumb initialization failed' -LiteralPath QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` returns 1; `NotifyCoreInitialized` body (`:320-329`) is byte-identical to `BASE-SHA` (`git diff $BaseSha -- QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` shows no removed lines: `git diff --numstat` deletions column is 0).
- [x] [P2-T7] Declare the form-controller seams in `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`: add `internal const int BreadcrumbInitializationAttemptLimit = 3;`, `internal const string FolderAreaInitializationFailedText = "Matched Folders: unavailable (breadcrumb initialization failed)";`, `internal Func<Task> BreadcrumbHostInitializer { get; set; }` (each with a one-line summary), and change `private async Task InitializeBreadcrumbHostAsync()` to `internal async Task InitializeBreadcrumbHostAsync()` leaving its body unchanged.
  - Acceptance: `Select-String -SimpleMatch 'internal async Task InitializeBreadcrumbHostAsync()' -LiteralPath QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` returns 1 and `private async Task InitializeBreadcrumbHostAsync()` returns 0 across `QuickFiler/Controllers/EfcFormController*.cs`; `Select-String -SimpleMatch 'internal Func<Task> BreadcrumbHostInitializer { get; set; }'` returns 1; `Select-String -SimpleMatch 'BreadcrumbInitializationAttemptLimit = 3;'` returns 1; `Select-String -SimpleMatch '"Matched Folders: unavailable (breadcrumb initialization failed)"'` returns 1; the file is at most 160 lines.
- [x] [P2-T8] Declare the site-3 seam in `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs`: a private field `private IWebViewCoreInitializer _webViewInitializer;` and `internal IWebViewCoreInitializer WebViewInitializer { get => _webViewInitializer ??= new WebView2CoreInitializer(); set => _webViewInitializer = value; }` with a summary stating it is not yet consumed until Phase 4. `InitializeWebViewAsync` is unchanged in this task.
  - Acceptance: `Select-String -SimpleMatch 'internal IWebViewCoreInitializer WebViewInitializer' -LiteralPath QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs` returns 1; `Select-String -SimpleMatch 'CoreWebView2Environment.CreateAsync(' -LiteralPath` the same file returns 1 (still the direct SDK call; routed in [P4-T3]).
- [x] [P2-T9] Declare the carry seams in `QuickFiler/Controllers/EfcDataModel.Carry.cs`: `internal IFolderSearchHandler CarriedFolderHandler { get; set; }`, `internal MailItemHelper CarriedMailHelper { get; set; }`, and `internal static bool TryAdoptCarriedFolderHandler(object folderList, IFolderSearchHandler carried, out FolderPredictor adopted)` whose body is `adopted = null; return false;` (summary ending `Decision lands in Phase 4 (#792).`). `InitFolderHandlerAsync` is unchanged in this task.
  - Acceptance: each of the three signatures is found exactly once by `Select-String -SimpleMatch` in that file; `Select-String -SimpleMatch 'TryAdoptCarriedFolderHandler(' -LiteralPath` the file returns exactly 1 (declaration only, no call yet); file at most 90 lines.
- [x] [P2-T10] Add the read-only accessor `internal IFolderSearchHandler FolderHandler => _folderHandler;` to `QuickFiler/Controllers/QfcItemController.cs` directly after the `TopFolderScore` member (line 265, with a two-line summary), and declare in `QuickFiler/Controllers/QfcCollectionController.PopOut.cs` the seam members `private Func<IApplicationGlobals, System.Action, MailItem, IFolderSearchHandler, MailItemHelper, EfcHomeController> _popOutHomeControllerFactory;`, `internal Func<IApplicationGlobals, System.Action, MailItem, IFolderSearchHandler, MailItemHelper, EfcHomeController> PopOutHomeControllerFactory { get => _popOutHomeControllerFactory ?? CreatePopOutHomeController; set => _popOutHomeControllerFactory = value; }`, `private static EfcHomeController CreatePopOutHomeController(IApplicationGlobals globals, System.Action parentCleanup, MailItem mailItem, IFolderSearchHandler carriedFolderHandler, MailItemHelper carriedMailHelper)` returning `new EfcHomeController(globals, parentCleanup, mailItem, carriedFolderHandler, carriedMailHelper)`, and `internal static (IFolderSearchHandler FolderHandler, MailItemHelper MailHelper) ReadPopOutCarry(QfcItemGroup group)` whose body is `return (null, null);` (summary ending `Body lands in Phase 4 (#792).`). The two `PopOut*` members are unchanged in this task.
  - Acceptance: `Select-String -SimpleMatch 'internal IFolderSearchHandler FolderHandler => _folderHandler;' -LiteralPath QuickFiler/Controllers/QfcItemController.cs` returns 1 and that file is at most 345 lines; in the PopOut file `Select-String -SimpleMatch` finds each of `PopOutHomeControllerFactory`, `CreatePopOutHomeController(`, `ReadPopOutCarry(QfcItemGroup group)` at least once and `new EfcHomeController(_globals, () => { }, mailItem);` exactly 2 times (the unchanged moved members); file at most 120 lines. This task compiles only together with [P2-T11], which adds the five-argument constructor; [P2-T13] is the first compile gate.
- [x] [P2-T11] Extend both `EfcHomeController` constructors in `QuickFiler/Controllers/EfcHomeController.cs` with trailing optional parameters `IFolderSearchHandler carriedFolderHandler = null, MailItemHelper carriedMailHelper = null` (public constructor `:47-52` forwarding both to the internal one; internal constructor `:54-95`), and in the internal constructor insert, immediately after the `DataModel = _dependencies.DataModelFactory(...)` statement (`:66-71`) and before `if (DataModel.Mail is not null)` (`:73`), the two statements `DataModel.CarriedFolderHandler = carriedFolderHandler;` and `DataModel.CarriedMailHelper = carriedMailHelper;` with a comment naming #792 AC-U3 and the reason (the form-controller factory fires `PopulateFolderCombobox`, which consumes the carry).
  - Acceptance: `Select-String -SimpleMatch 'MailItemHelper carriedMailHelper = null' -LiteralPath QuickFiler/Controllers/EfcHomeController.cs` returns 2; `Select-String -SimpleMatch 'DataModel.CarriedFolderHandler = carriedFolderHandler;'` returns 1 and its line number is smaller than the line number of `if (DataModel.Mail is not null)`; `git diff --numstat $BaseSha -- QuickFiler/Controllers/EfcHomeController.cs` reports at most 26 insertions and at most 4 deletions; file at most 470 lines.
- [x] [P2-T12] Declare, without wiring, `internal static void InvokeOnUiDispatcher(Action action, DispatcherPriority priority)` expression-bodied as `=> UiThread.Dispatcher.Invoke(action, priority);` (one executable line, the one gate (3) of [P7-T8] records `n/a`; a block body would add brace sequence points that no test can hit) in `QuickFiler/Helper Classes/EfcViewerQueue.cs` directly after `ResetProductionCoreDefaultsForTesting` (line 69), with a summary naming `ItemViewerQueue` parity and #792 AC-U3. Lines 25 and 68 keep their inline lambdas in this task.
  - Acceptance: `Select-String -SimpleMatch 'InvokeOnUiDispatcher' -LiteralPath 'QuickFiler/Helper Classes/EfcViewerQueue.cs'` returns exactly 1 (declaration only); `Select-String -SimpleMatch '(action, priority) => action();'` returns exactly 2 (lines 25 and 68 unchanged).
- [x] [P2-T13] Compile gate for Phase 2: CMD-OUTLOOK, CMD-BUILD-ANALYZE, CMD-BUILD-NULLABLE, then `dotnet tool run csharpier check QuickFiler/Controllers QuickFiler/Viewers "QuickFiler/Helper Classes" QuickFiler.Test/Controllers QuickFiler.Test/Viewers "QuickFiler.Test/Helper Classes"`; record `$FEATURE/evidence/qa-gates/p2-t13-compile-gate.md`.
  - Acceptance: both builds `EXIT_CODE: 0` with the exact `0 Error(s)` line; the scoped csharpier check exits 0 and prints a `Checked N files in` line, OR its reported unformatted set is a subset of `BASELINE-DRIFT-SET` from [P0-T9] and contains no path created or edited in Phase 1 or Phase 2 (record which branch held). A new-file formatting failure is repaired with `dotnet tool run csharpier format <that file>` and re-checked inside this task; the repaired file's SHA-256 before and after is recorded.
- [x] [P2-T14] Pure-move proof by the pre-existing suites: CMD-OUTLOOK, CMD-VSTEST, CMD-BUILD-PLAIN, then CMD-SCOPED-RUN with `<FILTER>` = `FullyQualifiedName~EfcFormControllerTests|FullyQualifiedName~EfcItemControllerTests|FullyQualifiedName~EfcDataModel|FullyQualifiedName~QfcCollectionControllerTests|FullyQualifiedName~ViewerQueueStaticWrapperTests|FullyQualifiedName~BreadcrumbBridgeRouterQueueTests|FullyQualifiedName~WebView2BreadcrumbHostTests|FullyQualifiedName~EfcHomeController` and `<task>` = `p2-t14`; record `$FEATURE/evidence/regression-testing/p2-t14-pure-move-proof.md`.
  - Acceptance: console prints `Test Run Successful.`; `Total tests:` equals `Passed:` (value recorded, required to be at least 60); `Failed: 0 (omitted category)` transcribed; `EXIT_CODE: 0`. A single failure is a HALT naming the test.
- [x] [P2-T15] Commit Phase 2: `git add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "refactor(792): partial splits, environment contract, declaration-only seams"`; record `$FEATURE/evidence/qa-gates/p2-t15-commit.md`.
  - Acceptance: `EXIT_CODE: 0`; `git show --name-only --format= HEAD` lists the nine new production files, the three new test files, the two csproj files, the edited `.cs` files named in [P2-T1] through [P2-T12], and paths under the feature folder, and nothing else; `git status --porcelain -- '*.cs' '*.csproj' '*.sln' 'packages.config'` prints nothing.

### Phase 3 — Seam-Dependent Regression Tests Observed Failing

These tests reference the Phase 2 declarations, so they compile now and fail at assertion time against the declaration-only bodies. Control tests (pass-before by construction) are named as such and are mutation-proven in Phase 5.

- [x] [P3-T1] Create `QuickFiler.Test/Viewers/WebView2EnvironmentContractTests.cs` (class `WebView2EnvironmentContractTests`) with three contract tests, and add `<Compile Include="Viewers\WebView2EnvironmentContractTests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Viewers\WebView2BreadcrumbHostIssue792Tests.cs` item added in [P1-T1].
  - Tests: `AdditionalBrowserArguments_IsAsciiDoubleHyphenIncognitoWithTrailingSpace` (equals `"--incognito "`, every char at most 0x7F, chars 0 and 1 are `'-'`, mirroring `EfcItemControllerTests.cs:371-396`); `ResolveUserDataFolder_CombinesLocalApplicationDataWithTheSharedLeafName` (equals `Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "WindowsFormsWebView2")`; no `Directory.Exists` or file I/O); `CreateOptions_CarriesTheSharedArgumentsOnAFreshInstance` (two calls return distinct references, both with `AdditionalBrowserArguments == WebView2EnvironmentContract.AdditionalBrowserArguments`). All three are controls (pass-before); non-vacuity is [P5-T3].
  - Acceptance: file exists with the three method names on single lines; csproj item present once, bare, adjacent; at most 120 lines (Phase 4 appends one more test).
- [x] [P3-T2] Create `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue792Tests.cs` (class `BreadcrumbBridgeRouterIssue792Tests`, namespace `QuickFiler.Test.Controllers`) using the `Mock<IBreadcrumbWebHost>` arrangement of `BreadcrumbBridgeRouterQueueTests.cs:34-101` (settable `_initialized`, recorded `_navigated`/`_posted`, the same `Bind()` helper over `LeafPath = "Inbox\Projects\Alpha"` and the same `Inbound(string json)` helper, `:98-101`, that runs `_router.ProcessInboundAsync(json)` synchronously), and add `<Compile Include="Controllers\BreadcrumbBridgeRouterIssue792Tests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Controllers\BreadcrumbBridgeRouterQueueTests.Part2.cs` item (line 59).
  - Tests: `NotifyInitializationFailed_ClearsThePendingDocumentAndNavigatesTheErrorBanner` (Bind with host uninitialized so a stash exists; select row `row-0` with the same `rowSelected` inbound payload as `BreadcrumbBridgeRouterQueueTests.cs:111`, then subscribe `SelectedFolderPathChanged` into a sentinel-initialised `observed` variable as `BreadcrumbBridgeRouterQueueTests.Part2.cs:233-235` does; act `_router.NotifyInitializationFailed(new InvalidOperationException("boom"))`; assert `_navigated` has exactly one entry containing `Folder list unavailable` and not containing `Alpha`, `_router.SelectedFolderPath` is null, and `observed` is null; the selection assertions cover the selection-clearing branch that [P7-T8] gate (3) measures; pre-fix the `_navigated` assertion fails first, before either selection assertion is reached); `NotifyInitializationFailed_LeavesNoStashForALaterInitialization` (same arrange and act, then `_initialized = true; _router.NotifyCoreInitialized();` assert `_navigated.Count` is 1 AND `_navigated[0]` contains `Folder list unavailable` and does not contain `Alpha`; pre-fix the only navigation is the stale stash, which contains `Alpha`, so the content assertion fails; under [P5-T7] the count assertion is what fails); `NotifyInitializationFailed_WithNullFailure_Throws` (`ArgumentNullException`; control); `NotifyCoreInitialized_AfterAnEarlierStash_StillNavigatesIt` (Bind uninitialized, set `_initialized = true`, `NotifyCoreInitialized`, assert one navigation containing `Alpha`; retained-behaviour control). Pre-fix the first two fail (the declaration-only body navigates nothing).
  - Acceptance: file exists with the four method names on single lines; csproj item present once, bare, adjacent; at most 200 lines.
- [x] [P3-T3] Create `QuickFiler.Test/Controllers/BreadcrumbOutboundQueueIssue792Tests.cs` (class `BreadcrumbOutboundQueueIssue792Tests`) and add `<Compile Include="Controllers\BreadcrumbOutboundQueueIssue792Tests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Controllers\BreadcrumbBridgeRouterIssue792Tests.cs` item added in [P3-T2].
  - Tests: `DiscardPending_ReturnsTheDiscardedCountAndLeavesZeroPending` (host mock `IsCoreInitialized` false; `PostOrQueue` three payloads; assert `DiscardPending()` returns 3 and `PendingCount` is 0 and `PostMessageJson` was never invoked); `DiscardPending_OnAnEmptyQueue_ReturnsZero` (control); `NotifyInitializationFailed_DiscardsTheOutboundQueueWithoutPosting` (construct the queue, hold the reference, pass it to a real `BreadcrumbBridgeRouter` over the same host mock and a loose `Mock<IFolderHierarchyProvider>`; enqueue two payloads through `queue.PostOrQueue`; act `router.NotifyInitializationFailed(new InvalidOperationException("boom"))`; assert `queue.PendingCount == 0` and `PostMessageJson` never invoked). Pre-fix the first and third fail.
  - Acceptance: file exists with the three method names on single lines; csproj item present once, bare, adjacent; at most 150 lines.
- [x] [P3-T4] Append five tests to `QuickFiler.Test/Controllers/EfcFormControllerIssue792Tests.cs` using a recording seam `Func<Task>` assigned to `controller.BreadcrumbHostInitializer` that counts invocations and throws or completes per a scripted list: `InitializeBreadcrumbHostAsync_RetriesUpToTheAttemptLimitThenReportsOnce` (always throws `InvalidOperationException`; assert invocations == 3, captured notifications == 1 containing `after 3 attempts`); `InitializeBreadcrumbHostAsync_SucceedsOnALaterAttempt_ReportsNothing` (throws once then completes; invocations == 2, notifications == 0); `InitializeBreadcrumbHostAsync_WhenCanceled_DoesNotRetryOrReport` (throws `OperationCanceledException`; invocations == 1, notifications == 0); `InitializeBreadcrumbHostAsync_OnFinalFailure_ShowsTheErrorTextInTheFolderAreaLabel` (`_formViewer` = uninitialized `EfcViewer` whose internal field `label2` is set by reflection to `new System.Windows.Forms.Label()`; after three failures assert `label2.Text == EfcFormController.FolderAreaInitializationFailedText`); `InitializeBreadcrumbHostAsync_OnFinalFailure_NotifiesTheRouter` (`_router` set by reflection to a real router over `Mock<IBreadcrumbWebHost>` (uninitialized) with a held `BreadcrumbOutboundQueue` holding one queued payload; after three failures assert the host received exactly one `NavigateToString` whose html contains `Folder list unavailable` and `queue.PendingCount == 0`). Every test captures `UserFaultNotifier` for its own flow and restores it in `finally`.
  - Acceptance: the five method names appear on single lines in that file; the file is at most 470 lines; `Select-String -SimpleMatch 'DoNotParallelize'` returns 0; `Select-String -Pattern 'Thread\.Sleep|Task\.Delay'` returns 0.
- [x] [P3-T5] Create `QuickFiler.Test/Controllers/EfcDataModelIssue792CarryTests.cs` (class `EfcDataModelIssue792CarryTests`, namespace `QuickFiler.Controllers.Tests`) and add `<Compile Include="Controllers\EfcDataModelIssue792CarryTests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Controllers\EfcDataModelArchiveRootTests.cs` item (line 125).
  - Tests: `TryAdoptCarriedFolderHandler_WithNullListAndConcretePredictor_Adopts` (predictor obtained by `FormatterServices.GetUninitializedObject(typeof(FolderPredictor))`; expect true and `adopted` same instance; fails pre-fix on the stub); `TryAdoptCarriedFolderHandler_WithNullCarry_DoesNotAdopt` (control); `TryAdoptCarriedFolderHandler_WithNonPredictorHandler_DoesNotAdopt` (`Mock<IFolderSearchHandler>().Object`; control); `TryAdoptCarriedFolderHandler_WithExplicitListAndPredictor_DoesNotAdopt` (`folderList = new[] { "Inbox" }`; control); `InitFolderHandlerAsync_WithCarriedPredictor_AdoptsItAndReleasesTheCarry` (data model obtained by `FormatterServices.GetUninitializedObject(typeof(EfcDataModel))`, `CarriedFolderHandler` set to an uninitialized predictor; act `await model.InitFolderHandlerAsync()`; assert `model.FolderHelper` is the same instance and `CarriedFolderHandler` and `CarriedMailHelper` are null afterwards; pre-fix this fails either on the assertion or with the exception the unguarded construction path raises against null `Globals`, and either outcome is recorded as the fail-before); `InitFolderHandlerAsync_WithNonPredictorCarry_RunsTheExistingPathAndReleasesTheCarry` (data model by `FormatterServices.GetUninitializedObject(typeof(EfcDataModel))`; `_globals` set by reflection to a `Mock<IApplicationGlobals>` whose `Ol` returns a `Mock<IOlObjects>().Object` so that `Ol.App` resolves — the arrangement of `QfcItemController.FolderHandlingTests.Part2.cs:378` and `QfcItemController.TestSupport.cs:348`; `FolderPredictor(IApplicationGlobals)` reads `AppGlobals.Ol.App` at `FolderPredictor.cs:39`; `Token` and `Globals` are plain field reads, `EfcDataModel.cs:157-168`, so the uninitialized instance yields a default token; `CarriedFolderHandler` = `new Mock<IFolderSearchHandler>().Object`, `CarriedMailHelper` null, `MailInfo` null because no `ConversationResolver` is set (`EfcDataModel.cs:241`); act `await model.InitFolderHandlerAsync()`; assert `model.FolderHelper` is not null and is not the carried object, and `CarriedFolderHandler` is null afterwards. Pre-fix `InitFolderHandlerAsync` is the verbatim moved body of [P2-T5] and never consults the [P2-T9] adoption stub (the stub is declared and has no call site until [P4-T8]), so the existing null-list path runs and the carry is never released; the release assertion fails).
  - Acceptance: file exists with the six method names on single lines; csproj item present once, bare, adjacent; at most 230 lines.
- [x] [P3-T6] Create `QuickFiler.Test/Controllers/QfcCollectionControllerIssue792PopOutTests.cs` (class `QfcCollectionControllerIssue792PopOutTests`, namespace `QuickFiler.Controllers.Tests`) and add `<Compile Include="Controllers\QfcCollectionControllerIssue792PopOutTests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after the `Controllers\QfcCollectionControllerTests.Part2.cs` item (line 167).
  - Tests: `ReadPopOutCarry_WithConcreteItemController_ReturnsHandlerAndHelper` (uninitialized `QfcItemController` with `_folderHandler` set to `Mock<IFolderSearchHandler>().Object` and `_itemInfo` set to `new Mock<MailItemHelper>(MockBehavior.Loose).Object` by reflection, wrapped in a `QfcItemGroup` whose `ItemController` is set through its internal setter; expect both references back; fails pre-fix on the stub); `ReadPopOutCarry_WithInterfaceOnlyController_ReturnsNullHandlerAndTheHelper` (`Mock<IQfcItemController>` with `ItemHelper` set up; expect null handler and that helper; fails pre-fix); `ReadPopOutCarry_WithNullController_ReturnsNulls` (control); `PopOutHomeControllerFactory_DefaultIsTheNamedProductionFactory` (uninitialized `QfcCollectionController`; assert `PopOutHomeControllerFactory.Method.Name == "CreatePopOutHomeController"`; set a recording lambda and read it back by reference; set null and assert the named default is back; control); `EfcHomeController_DepositsTheCarryOnTheDataModelBeforeConstructingTheFormController` (an `EfcHomeControllerDependencies` built as in `EfcHomeControllerLifecycleTests.cs:302-344`: `dataModelFactory` returns an uninitialized `EfcDataModel` whose `_mail` field is set to `new Mock<MailItem>().Object`, `viewerFactory` returns an uninitialized `EfcViewer`, keyboard and explorer factories return loose mocks, `formControllerWithDataFactory` captures `dataModel.CarriedFolderHandler` and `dataModel.CarriedMailHelper` at call time and returns an uninitialized `EfcFormController`; construct `new EfcHomeController(globals, () => { }, dependencies, mail, handler, helper)`; assert the captured pair are the supplied instances; control after [P2-T11], mutation-proven in [P5-T6]).
  - Acceptance: file exists with the five method names on single lines; csproj item present once, bare, adjacent; at most 260 lines.
- [x] [P3-T7] [expect-fail] Whole-set fail-before run: CMD-OUTLOOK, CMD-BUILD-PLAIN, CMD-VSTEST, then CMD-SCOPED-RUN with `<FILTER>` = `FullyQualifiedName~Issue792|FullyQualifiedName~WebView2EnvironmentContractTests` and `<task>` = `p3-t7`; record `$FEATURE/evidence/regression-testing/p3-t7-fail-before.md`.
  - Acceptance: build `0 Error(s)` exact line; `Total tests: 31` (2 + 2 + 1 from Phase 1, 3 + 4 + 3 + 5 + 6 + 5 from Phase 3); the artifact carries one `FAIL-BEFORE:` line per failing test and one `PASS-BEFORE-CONTROL:` line per passing test, and the two sets are exactly: FAIL (18) = the two host tests, `InitializeBreadcrumbHostAsync_WhenHostIsNull_ReportsThroughTheBoundarySinkToTheUser`, `ProductionBlockingPriorityScheduler_DefaultIsTheNamedUiDispatcherInvoke`, `NotifyInitializationFailed_ClearsThePendingDocumentAndNavigatesTheErrorBanner`, `NotifyInitializationFailed_LeavesNoStashForALaterInitialization`, `DiscardPending_ReturnsTheDiscardedCountAndLeavesZeroPending`, `NotifyInitializationFailed_DiscardsTheOutboundQueueWithoutPosting`, the five [P3-T4] tests, `TryAdoptCarriedFolderHandler_WithNullListAndConcretePredictor_Adopts`, `InitFolderHandlerAsync_WithCarriedPredictor_AdoptsItAndReleasesTheCarry`, `InitFolderHandlerAsync_WithNonPredictorCarry_RunsTheExistingPathAndReleasesTheCarry`, `ReadPopOutCarry_WithConcreteItemController_ReturnsHandlerAndHelper`, `ReadPopOutCarry_WithInterfaceOnlyController_ReturnsNullHandlerAndTheHelper`; PASS (13) = the remaining tests (the `PopulateFolderCombobox` control, the three contract tests, `NotifyInitializationFailed_WithNullFailure_Throws`, `NotifyCoreInitialized_AfterAnEarlierStash_StillNavigatesIt`, `DiscardPending_OnAnEmptyQueue_ReturnsZero`, the three `TryAdoptCarriedFolderHandler_*_DoesNotAdopt` controls, `ReadPopOutCarry_WithNullController_ReturnsNulls`, `PopOutHomeControllerFactory_DefaultIsTheNamedProductionFactory`, `EfcHomeController_DepositsTheCarryOnTheDataModelBeforeConstructingTheFormController`). `Passed: 13`, `Failed: 18`; `ExpectedExitCode: 1`. Any other partition is a HALT.
- [x] [P3-T8] Commit Phase 3: `git add -- QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "test(792): seam-dependent regression tests recorded failing before the fix"`; record `$FEATURE/evidence/regression-testing/p3-t8-commit.md`.
  - Acceptance: `EXIT_CODE: 0`; `git show --name-only --format= HEAD` lists only `QuickFiler.Test/**` paths and feature-folder paths; scoped porcelain (convention 9) prints nothing.

### Phase 4 — Implementation

- [x] [P4-T1] Converge site 1 and guard delivery in `QuickFiler/Viewers/WebView2BreadcrumbHost.cs`: replace the five-line folder computation and the parameterless construction (`:246-250`) with `string cacheFolder = WebView2EnvironmentContract.ResolveUserDataFolder();` and `CoreWebView2EnvironmentOptions options = WebView2EnvironmentContract.CreateOptions();`; update the class summary sentence about the cache folder to name the contract; and rewrite `NavigateToString` (`:157-167`) to route through a local `void NavigateCore()` that reads `_control.CoreWebView2`, logs `"NavigateToString called before CoreWebView2 initialization; document dropped."` at error level and returns when it is null, otherwise calls `ForwardNavigateToString(html)`, with the same dispatcher-or-inline branch as `PostMessageJson` (`:193-218`). `using System.IO;` is removed if no other member uses `Path`.
  - Acceptance: `Select-String -SimpleMatch 'WebView2EnvironmentContract.CreateOptions()' -LiteralPath QuickFiler/Viewers/WebView2BreadcrumbHost.cs` returns 1; `Select-String -SimpleMatch 'new CoreWebView2EnvironmentOptions('` returns 0 in that file; `Select-String -SimpleMatch '"WindowsFormsWebView2"'` returns 0 in that file; `Select-String -SimpleMatch 'document dropped.'` returns 1; `ForwardNavigateToString` and its `[ExcludeFromCodeCoverage]` attribute are unchanged (`Select-String -SimpleMatch 'private void ForwardNavigateToString(string html)'` returns 1); file at most 390 lines.
- [x] [P4-T2] Converge site 2 in `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs`: replace lines 55-62 (the cache-directory comment, the four-line folder computation, the dead comment line 61 and the target-typed construction) with `string cacheFolder = WebView2EnvironmentContract.ResolveUserDataFolder();` and `CoreWebView2EnvironmentOptions options = WebView2EnvironmentContract.CreateOptions();` preceded by a one-line comment naming #792. Nothing else in the file changes.
  - Acceptance: `Select-String -SimpleMatch 'WebView2EnvironmentContract.CreateOptions()' -LiteralPath QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` returns 1; `Select-String -SimpleMatch '--disk-cache-size=1'` returns 0; `Select-String -SimpleMatch 'new("--incognito ")'` returns 0; `Select-String -SimpleMatch '"WindowsFormsWebView2"'` returns 0; the `#351` comment block (`:110-112`) and the commented-out block (`:124-130`) are unchanged; `(Get-Content -LiteralPath).Count` is at most 476; `git diff --numstat $BaseSha -- QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` reports at most 4 insertions and 7 or 8 deletions.
- [x] [P4-T3] Converge and route site 3 in `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs`: change the constant to `internal const string IncognitoArgument = WebView2EnvironmentContract.AdditionalBrowserArguments;` and rewrite its remark to state that the owner is `WebView2EnvironmentContract` and this alias exists so the #463 pin `EfcItemControllerTests.IncognitoArgument_IsAsciiDoubleHyphenIncognitoWithTrailingSpace` keeps asserting the value the preview actually passes; rewrite `InitializeWebViewAsync` to `string cacheFolder = WebView2EnvironmentContract.ResolveUserDataFolder(); CoreWebView2EnvironmentOptions options = WebView2EnvironmentContract.CreateOptions(); await _itemViewer.UiSyncContext; _webViewEnvironment = await WebViewInitializer.CreateEnvironmentAsync(cacheFolder, options); await WebViewInitializer.EnsureCoreWebView2Async(_itemViewer.L0v2h2_WebView2, _webViewEnvironment);` with a comment that both seam calls are awaited so a failure reaches `InitializeWebViewGuardedAsync`.
  - Acceptance: in that file `Select-String -SimpleMatch 'CoreWebView2Environment.CreateAsync('` returns 0, `Select-String -SimpleMatch 'ContinueWith('` returns 0, `Select-String -SimpleMatch 'WebViewInitializer.CreateEnvironmentAsync('` returns 1, `Select-String -SimpleMatch 'WebViewInitializer.EnsureCoreWebView2Async('` returns 1, `Select-String -SimpleMatch 'IncognitoArgument = WebView2EnvironmentContract.AdditionalBrowserArguments;'` returns 1, `Select-String -SimpleMatch '"WindowsFormsWebView2"'` returns 0; `using System.IO;` is absent from the file; file at most 90 lines.
- [x] [P4-T4] Append the site-3 seam test `EfcItemController_InitializeWebViewAsync_PassesTheContractValuesThroughTheSeam` to `QuickFiler.Test/Viewers/WebView2EnvironmentContractTests.cs`: install `new SynchronizationContext()` as `SynchronizationContext.Current` (restore in `finally`, as `EfcItemControllerTests.cs:354-357`), obtain an uninitialized `EfcItemController` and an uninitialized `ItemViewer` whose `_context` field is set to that same context (so `await _itemViewer.UiSyncContext` completes inline: the awaiter's `IsCompleted` returns true when the ambient context is reference-equal to the captured one, `UiThread.cs:155-163`), set `controller.WebViewInitializer` to a `Mock<IWebViewCoreInitializer>` capturing the `CreateEnvironmentAsync` arguments and returning completed tasks; act `await controller.InitializeWebViewAsync()`; assert the captured folder equals `WebView2EnvironmentContract.ResolveUserDataFolder()` and the captured `options.AdditionalBrowserArguments` equals `WebView2EnvironmentContract.AdditionalBrowserArguments`, and `EnsureCoreWebView2Async` was invoked once. This test is authored only AFTER [P4-T3] and is never run against the direct-SDK body, because that body would start the Evergreen browser process and write a user-data folder in the test host; its non-vacuity is [P5-T3] plus the mutation note in this task (temporarily passing `new CoreWebView2EnvironmentOptions()` at the seam makes the argument assertion fail — record the observed failing message, then restore).
  - Acceptance: the method name appears on a single line in that file; the mutation observation is recorded in `$FEATURE/evidence/regression-testing/p4-t4-site3-mutation.md` with the predicted and observed failing assertion and a `git diff --numstat HEAD -- QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs` showing `0 0` after restoration; file at most 200 lines.
- [x] [P4-T5] Implement `DiscardPending` in `QuickFiler/Controllers/BreadcrumbOutboundQueue.cs`: body `int discarded = _pending.Count; _pending.Clear(); return discarded;`; summary states it discards without posting because a never-initialized host has no core to post to (#792 AC-U7).
  - Acceptance: `Select-String -SimpleMatch '_pending.Clear();' -LiteralPath QuickFiler/Controllers/BreadcrumbOutboundQueue.cs` returns 1; `OnInitializationCompleted` body (`:59-65`) unchanged; file at most 90 lines.
- [x] [P4-T6] Implement `NotifyInitializationFailed` in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` per D5: after the null guard, `bool hadPendingDocument = _pendingDocument != null; _pendingDocument = null; int discardedPayloads = _outboundQueue.DiscardPending(); _rows = _builder.BuildRows(new[] { InitializationFailedBannerText }, _ => null, Array.Empty<FolderScore>()); _selectedRowId = null;` then clear `SelectedFolderPath` and raise `SelectedFolderPathChanged` only when it was non-null, then `_host.NavigateToString(_renderer.RenderDocument(_rows, _darkMode, null));` then one `log.Error(...)` line naming the failure, `hadPendingDocument` and `discardedPayloads`, passing `failure` as the exception. `NotifyCoreInitialized` is untouched. Update the summary of `NotifyCoreInitialized` (`:316-319`) only if needed for the sibling reference; do not change its body.
  - Acceptance: `Select-String -SimpleMatch '_outboundQueue.DiscardPending();' -LiteralPath QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` returns 1; `Select-String -SimpleMatch 'InitializationFailedBannerText }'` returns 1; `git diff $BaseSha -- QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` shows zero removed lines inside `NotifyCoreInitialized` (mechanically: `git diff --numstat` deletions column is 0 for the file); file at most 470 lines.
- [x] [P4-T7] Implement the bounded retry and reporting in `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` per D3 and D4: add `private Task InitializeBreadcrumbHostOnceAsync()` (returns `BreadcrumbHostInitializer()` when set, else `_breadcrumbHost.InitializeAsync(_formViewer.UiSyncContext)`); rewrite `InitializeBreadcrumbHostAsync` as a `for` loop from 1 to `BreadcrumbInitializationAttemptLimit` whose `try` awaits the once-step and returns, whose `catch (OperationCanceledException)` logs `"Breadcrumb initialization canceled."` at debug level and returns, and whose `catch (System.Exception ex)` stores `lastFailure` and logs a warning naming the attempt number; after the loop call `_router?.NotifyInitializationFailed(lastFailure);`, `ShowFolderAreaError(FolderAreaInitializationFailedText);` and `TryReportBoundaryFault($"Breadcrumb WebView2 initialization failed after {BreadcrumbInitializationAttemptLimit} attempts: {lastFailure.Message}", lastFailure);`; add `private void ShowFolderAreaError(string message)` that reads `_formViewer?.label2`, returns when null, and sets `Text` directly when `InvokeRequired` is false, else through `BeginInvoke`; replace the stale comment above the method (original `:1070-1071`, "the router queues every outbound payload until CoreWebView2InitializationCompleted fires") with one stating that on failure the router discards its pending document and outbound queue explicitly through `NotifyInitializationFailed`.
  - Acceptance: `Select-String -SimpleMatch 'until CoreWebView2InitializationCompleted fires' -LiteralPath QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` returns 0; `Select-String -SimpleMatch '_router?.NotifyInitializationFailed(lastFailure);'` returns 1; `Select-String -SimpleMatch 'ShowFolderAreaError(FolderAreaInitializationFailedText);'` returns 1; `Select-String -SimpleMatch 'after {BreadcrumbInitializationAttemptLimit} attempts:'` returns 1; `Select-String -Pattern 'Thread\.Sleep|Task\.Delay'` returns 0 across `QuickFiler/Controllers/EfcFormController*.cs`; `Select-String -SimpleMatch 'catch (OperationCanceledException)'` returns 1 in the Breadcrumb file; file at most 230 lines. `ConfigureBreadcrumbControl` is byte-identical to its moved form (its `_ = InitializeBreadcrumbHostAsync();` line count stays 1).
- [x] [P4-T8] Implement adoption in `QuickFiler/Controllers/EfcDataModel.Carry.cs` per D7: `TryAdoptCarriedFolderHandler` returns true with `adopted = predictor` when `folderList is null && carried is FolderPredictor predictor`, else false with `adopted = null`; `InitFolderHandlerAsync` first calls it with `CarriedFolderHandler`, and on true sets `FolderHelper = adopted;`, calls `ReleaseCarry();` and returns; in the existing null-list branch the scoring input becomes a local `MailItemHelper scoringInput = MailInfo ?? CarriedMailHelper;` used in place of all three `MailInfo` reads of that branch (the `MailInfo is null` test and the two lambda arguments, `EfcDataModel.cs:192`, `:202` and `:204` at `BASE-SHA`), followed by `ReleaseCarry();` at the end of that branch; `private void ReleaseCarry()` nulls both carry properties; the explicit-list branch is untouched.
  - Acceptance: `Select-String -SimpleMatch 'carried is FolderPredictor predictor' -LiteralPath QuickFiler/Controllers/EfcDataModel.Carry.cs` returns 1; `Select-String -SimpleMatch 'MailInfo ?? CarriedMailHelper'` returns 1; `Select-String -SimpleMatch 'ReleaseCarry();'` returns 2; `Select-String -SimpleMatch 'FolderPredictor.InitOptions.FromArrayOrString'` returns 2 (the unchanged explicit-list branch); file at most 120 lines.
- [x] [P4-T9] Implement the pop-out carry in `QuickFiler/Controllers/QfcCollectionController.PopOut.cs` per D6: `ReadPopOutCarry` returns `(controller is QfcItemController concrete ? concrete.FolderHandler : null, controller?.ItemHelper)` where `controller = group?.ItemController`; both `PopOutControlGroup` and `PopOutControlGroupAsync` read `QfcItemGroup group = _itemGroups[selection - 1];`, its `MailItem`, then `(IFolderSearchHandler handler, MailItemHelper helper) = ReadPopOutCarry(group);` BEFORE the removal call, and construct through `PopOutHomeControllerFactory(_globals, () => { }, mailItem, handler, helper)`; the summary on each states why the read precedes removal (`Cleanup` nulls `_folderHandler` and `ItemHelper`).
  - Acceptance: `Select-String -SimpleMatch 'new EfcHomeController(_globals, () => { }, mailItem);' -LiteralPath QuickFiler/Controllers/QfcCollectionController.PopOut.cs` returns 0; `Select-String -SimpleMatch 'PopOutHomeControllerFactory(_globals,'` returns 2; `Select-String -SimpleMatch '= ReadPopOutCarry(group);'` returns 2; file at most 140 lines. Ordering is gated mechanically in [P6-T4].
- [x] [P4-T10] Wire the dispatcher default in `QuickFiler/Helper Classes/EfcViewerQueue.cs`: the initializer of `ProductionBlockingPriorityScheduler` (line 25) and its assignment in `ResetProductionCoreDefaultsForTesting` (line 68) become the method group `InvokeOnUiDispatcher`.
  - Acceptance: `Select-String -SimpleMatch '(action, priority) => action();' -LiteralPath 'QuickFiler/Helper Classes/EfcViewerQueue.cs'` returns 0; `Select-String -SimpleMatch 'InvokeOnUiDispatcher'` returns 3 (declaration plus two uses); `ProductionSynchronousScheduler` and `ProductionPriorityScheduler` lines are unchanged; file at most 115 lines.
- [x] [P4-T11] Green gate: CMD-OUTLOOK, CMD-BUILD-ANALYZE, CMD-BUILD-NULLABLE, CMD-VSTEST, then CMD-SCOPED-RUN with `<FILTER>` = `FullyQualifiedName~Issue792|FullyQualifiedName~WebView2EnvironmentContractTests|FullyQualifiedName~EfcFormControllerTests|FullyQualifiedName~EfcItemControllerTests|FullyQualifiedName~EfcDataModel|FullyQualifiedName~QfcCollectionControllerTests|FullyQualifiedName~ViewerQueueStaticWrapperTests|FullyQualifiedName~BreadcrumbBridgeRouterQueueTests|FullyQualifiedName~WebView2BreadcrumbHostTests|FullyQualifiedName~EfcHomeController|FullyQualifiedName~QfcItemController_InitializationTests` (the partial class declared at `QfcItemController.InitializationTests.cs:30`) and `<task>` = `p4-t11`; record `$FEATURE/evidence/regression-testing/p4-t11-pass-after.md`.
  - Acceptance: both builds `EXIT_CODE: 0` with the exact `0 Error(s)` line; console `Test Run Successful.`; the artifact carries one `PASS-AFTER: <FullyQualifiedName>` line for each of the 32 Issue792/contract tests (31 from [P3-T7] plus [P4-T4]) and records `Total tests:` equal to `Passed:`; `Failed: 0 (omitted category)`; `EXIT_CODE: 0`.
- [x] [P4-T12] Commit the implementation: `git add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "fix(792): converge WebView2 environment contract, bounded breadcrumb retry, explicit discard, pop-out carry"`; record `$FEATURE/evidence/qa-gates/p4-t12-commit.md`.
  - Acceptance: `EXIT_CODE: 0`; scoped porcelain (convention 9) prints nothing; `git diff --name-only $BaseSha HEAD -- '*.cs' '*.csproj'` lists exactly the 31 write-set paths minus `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` (30 paths) and nothing else; the list is recorded verbatim.

### Phase 5 — Non-Vacuity Mutations

Each task applies one edit, runs CMD-OUTLOOK, CMD-BUILD-PLAIN, CMD-VSTEST and CMD-SCOPED-RUN over the named test(s), confirms the pre-predicted assertion failed, restores the file with `git checkout -- <path>`, rebuilds with CMD-BUILD-PLAIN, re-runs the same filter and confirms it is green again. Each artifact records `MUTATION:`, `PREDICTED-FAILING-ASSERTION:`, `OBSERVED:` (verbatim first assertion line), `RESTORED: git diff --numstat HEAD -- <path> prints nothing`. `<task>` for CMD-SCOPED-RUN is the task id in lower case.

- [x] [P5-T1] AC-U4 mutation in `QuickFiler/Controllers/EfcFormController.cs`: delete the statement `UserFaultNotifier?.Invoke(message);` from `DefaultBoundaryErrorSink` (`:137-141`); filter `FullyQualifiedName~EfcFormControllerIssue792Tests.PopulateFolderCombobox_WhenDataModelFaults_NotifiesTheUserThroughTheDefaultSink|FullyQualifiedName~EfcFormControllerTests.PopulateFolderCombobox_WhenDataModelFaults_LogsOnceAndDoesNotFault`; record `$FEATURE/evidence/regression-testing/p5-t1-ac-u4-mutation.md`.
  - Acceptance: predicted and observed: the new test fails on the notifier-count assertion (expected 1, actual 0) while the pre-existing test PASSES (it substitutes the sink), which is the recorded proof that the pre-existing test alone could not pin the user surface; after restoration both pass.
- [x] [P5-T2] AC-U6 site-1 regression mutation in `QuickFiler/Viewers/WebView2BreadcrumbHost.cs`: replace `WebView2EnvironmentContract.CreateOptions()` with `new CoreWebView2EnvironmentOptions()`; filter `FullyQualifiedName~WebView2BreadcrumbHostIssue792Tests.InitializeAsync_PassesTheSharedFolderAndIncognitoArgumentToTheSeam`; additionally run CMD-AC-U6-GATE; record `$FEATURE/evidence/regression-testing/p5-t2-ac-u6-site1-mutation.md`.
  - Acceptance: predicted and observed: the seam test fails on the `AdditionalBrowserArguments` assertion (expected `"--incognito "`, actual null) and the structural gate prints `PRIMARY-CONSTRUCTION-COUNT: 2` and `AC-U6-STRUCTURAL: FAIL`; after restoration the test passes and the gate prints PASS. Then, with site 1 restored, apply a second mutation to `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` replacing `WebView2EnvironmentContract.CreateOptions()` with `new("--incognito ")`, run CMD-AC-U6-GATE, and record the predicted and observed `PRIMARY-CONSTRUCTION-COUNT: 2`, `CONTRACT-READER-COUNT: 2`, `AC-U6-STRUCTURAL: FAIL`; restore with `git checkout -- QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` and confirm the gate prints PASS (`RESTORED:` for both files). This second half is the site-2 proof cited by [P7-T14].
- [x] [P5-T3] AC-U6 constant mutation in `QuickFiler/Viewers/WebView2EnvironmentContract.cs`: change the constant to `"--incognito"` (no trailing space); filter `FullyQualifiedName~WebView2EnvironmentContractTests|FullyQualifiedName~EfcItemControllerTests.IncognitoArgument_IsAsciiDoubleHyphenIncognitoWithTrailingSpace|FullyQualifiedName~WebView2BreadcrumbHostIssue792Tests.InitializeAsync_PassesTheSharedFolderAndIncognitoArgumentToTheSeam`; record `$FEATURE/evidence/regression-testing/p5-t3-ac-u6-constant-mutation.md`.
  - Acceptance: predicted and observed failures: `AdditionalBrowserArguments_IsAsciiDoubleHyphenIncognitoWithTrailingSpace`, `IncognitoArgument_IsAsciiDoubleHyphenIncognitoWithTrailingSpace` (the alias carries the mutation), and the host seam test, each on its string-equality assertion; `EfcItemController_InitializeWebViewAsync_PassesTheContractValuesThroughTheSeam` PASSES (it asserts relative to the contract) and this is recorded as expected; after restoration all pass.
- [x] [P5-T4] AC-U7 mutation in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`: replace `int discardedPayloads = _outboundQueue.DiscardPending();` with `int discardedPayloads = 0;`; filter `FullyQualifiedName~BreadcrumbOutboundQueueIssue792Tests.NotifyInitializationFailed_DiscardsTheOutboundQueueWithoutPosting`; record `$FEATURE/evidence/regression-testing/p5-t4-ac-u7-mutation.md`.
  - Acceptance: predicted and observed: fails on `PendingCount` (expected 0, actual 2); green after restoration.
- [x] [P5-T5] AC-U1 two-mutation task in `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` (no BOM at HEAD; the four HEAD files that carry a UTF-8 BOM are `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`, `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs`, `QuickFiler/Controllers/EfcFormController.cs` and `QuickFiler/Controllers/EfcHomeController.cs`; none of them is edited here). Precondition: `git diff --numstat HEAD -- QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` prints nothing (otherwise `git checkout -- QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` first and record `PRE-STATE: restored`); then record `SHA256-BEFORE-MUTATION:` from `Get-FileHash -Algorithm SHA256 -LiteralPath QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` and `BOM-BEFORE-MUTATION:` (`True` when the first three bytes are `EF BB BF`, else `False`). Mutation A: change `internal const int BreadcrumbInitializationAttemptLimit = 3;` (`:29`) to `internal const int BreadcrumbInitializationAttemptLimit = 1;`; filter `FullyQualifiedName~EfcFormControllerIssue792Tests.InitializeBreadcrumbHostAsync_RetriesUpToTheAttemptLimitThenReportsOnce|FullyQualifiedName~EfcFormControllerIssue792Tests.InitializeBreadcrumbHostAsync_SucceedsOnALaterAttempt_ReportsNothing` with `<task>` = `p5-t5-a`; restore with `git checkout -- QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`, CMD-BUILD-PLAIN, re-run the same filter with `<task>` = `p5-t5-a-restored`. Mutation B (applied only after mutation A is restored, so the limit is 3): inside the `catch (System.Exception ex)` block of `InitializeBreadcrumbHostAsync` (`:84-92`), insert the single statement `TryReportBoundaryFault(ex.Message, ex);` on its own line immediately after `lastFailure = ex;` (`:86`), so that every failed attempt reports through the boundary sink instead of only the final failure at `:97-100`; same filter with `<task>` = `p5-t5-b`; restore the same way, CMD-BUILD-PLAIN, re-run with `<task>` = `p5-t5-b-restored`; record `$FEATURE/evidence/regression-testing/p5-t5-ac-u1-mutation.md` (rewritten in this task; the halted `2026-09-17T20-27` content may be retained under a heading `## Prior halted run` and satisfies no clause below).
  - Acceptance: each prediction is written into the artifact before its mutated run, and each mutated build prints the exact `0 Error(s)` line (mutation B calls a private member of the same partial class, `EfcFormController.cs:150`, so a compile error is a HALT, not a prediction miss). Mutation A, predicted and observed: `Failed InitializeBreadcrumbHostAsync_RetriesUpToTheAttemptLimitThenReportsOnce` with its `OBSERVED:` line containing `Invocations to be 3` and `but found 1`; `Failed InitializeBreadcrumbHostAsync_SucceedsOnALaterAttempt_ReportsNothing` with its `OBSERVED:` line containing `Invocations to be 2` and `but found 1`; `Total tests: 2`, `Failed: 2`; the artifact carries the sentence that under mutation A the notification assertion `captured.Should().BeEmpty(...)` at `QuickFiler.Test/Controllers/EfcFormControllerIssue792Tests.cs:244` is downstream of the `Be(2)` assertion at `:241-243`, which fails first and ends the test, so mutation A does not prove that assertion. Mutation B, predicted and observed: `Failed InitializeBreadcrumbHostAsync_SucceedsOnALaterAttempt_ReportsNothing` with its `OBSERVED:` line containing `to be empty` and `boom` (one captured notification instead of zero; the `:240` and `:241-243` assertions passed because the scripted initializer still fails once and then succeeds, so `Invocations` is 2, and FluentAssertions outside an assertion scope raises on the first failing assertion, so an `OBSERVED:` line naming `captured` proves the two earlier assertions passed); `Failed InitializeBreadcrumbHostAsync_RetriesUpToTheAttemptLimitThenReportsOnce` with its `OBSERVED:` line containing `to contain a single item` (three per-attempt reports plus the final report, four captured, while its `Invocations` assertion of 3 passed; recorded as expected, not as the load-bearing proof); `Total tests: 2`, `Failed: 2`. After each restoration: `Passed InitializeBreadcrumbHostAsync_RetriesUpToTheAttemptLimitThenReportsOnce` and `Passed InitializeBreadcrumbHostAsync_SucceedsOnALaterAttempt_ReportsNothing`, `Test Run Successful.`, `Total tests: 2`, `Passed: 2`; `SHA256-AFTER-RESTORE-A:` and `SHA256-AFTER-RESTORE-B:` each equal to `SHA256-BEFORE-MUTATION:`; `BOM-AFTER-MUTATION-A:`, `BOM-AFTER-RESTORE-A:`, `BOM-AFTER-MUTATION-B:` and `BOM-AFTER-RESTORE-B:` each equal to `BOM-BEFORE-MUTATION:` (expected `False` for this file; a different observed value is recorded and the gate is equality, so BOM state is preserved through both mutations); `RESTORED:` per the Phase 5 preamble after each restoration.
- [x] [P5-T6] AC-U3 deposit mutation in `QuickFiler/Controllers/EfcHomeController.cs`: delete the two `DataModel.Carried... = ...;` statements; filter `FullyQualifiedName~QfcCollectionControllerIssue792PopOutTests.EfcHomeController_DepositsTheCarryOnTheDataModelBeforeConstructingTheFormController`; record `$FEATURE/evidence/regression-testing/p5-t6-ac-u3-deposit-mutation.md`.
  - Acceptance: predicted and observed: fails on the captured-handler reference assertion (actual null); green after restoration.
- [x] [P5-T7] AC-U2 mutation in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`: delete the statement `_pendingDocument = null;` inside `NotifyInitializationFailed` only; filter `FullyQualifiedName~BreadcrumbBridgeRouterIssue792Tests.NotifyInitializationFailed_LeavesNoStashForALaterInitialization`; record `$FEATURE/evidence/regression-testing/p5-t7-ac-u2-mutation.md`.
  - Acceptance: predicted and observed: fails on `_navigated.Count` (expected 1, actual 2, the stale stash was navigated by the later `NotifyCoreInitialized`); green after restoration.
- [x] [P5-T8] AC-U3 adoption mutation in `QuickFiler/Controllers/EfcDataModel.Carry.cs`: make `TryAdoptCarriedFolderHandler` always return false; filter `FullyQualifiedName~EfcDataModelIssue792CarryTests.TryAdoptCarriedFolderHandler_WithNullListAndConcretePredictor_Adopts|FullyQualifiedName~EfcDataModelIssue792CarryTests.InitFolderHandlerAsync_WithCarriedPredictor_AdoptsItAndReleasesTheCarry`; record `$FEATURE/evidence/regression-testing/p5-t8-ac-u3-adoption-mutation.md`.
  - Acceptance: predicted and observed: both fail (the first on the boolean, the second on `BeSameAs` or on the unguarded construction path's exception); green after restoration.
- [x] [P5-T9] Confirm the tree is restored and commit the mutation evidence: `git status --porcelain -- '*.cs' '*.csproj' '*.sln' 'packages.config'` prints nothing; `git add -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "test(792): non-vacuity mutation evidence"`; record `$FEATURE/evidence/regression-testing/p5-t9-commit.md`.
  - Acceptance: the scoped porcelain output is empty and is recorded verbatim as `SOURCE-PORCELAIN: empty`; `EXIT_CODE: 0` for the commit; `git show --name-only --format= HEAD` lists feature-folder paths only.

### Phase 6 — Structural Gates, Size Audit, Project Parity, and Debt Statement

- [x] [P6-T1] Run CMD-AC-U6-GATE against the fixed tree (`pwsh -NoProfile -WorkingDirectory <repo-root> -File coverage/plan792-helper.ps1`; its branch assertion is the worktree proof) and record `$FEATURE/evidence/qa-gates/p6-t1-ac-u6-structural-pass.md`.
  - Acceptance: output is exactly `PRIMARY-CONSTRUCTION-COUNT: 1` followed by one `PRIMARY-SITE: QuickFiler/Viewers/WebView2EnvironmentContract.cs:<n>` line, `CREATEASYNC-OUTSIDE-ADAPTER: 0` (followed by no `CREATEASYNC-SITE:` line), `SEAM-CALLER-COUNT: 3` followed by three `SEAM-CALLER:` lines naming `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs`, `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` and `QuickFiler/Viewers/WebView2BreadcrumbHost.cs` (each with its line number), `CONTRACT-READER-COUNT: 3` followed by three `CONTRACT-READER:` lines naming the same three files (each with its line number), and `AC-U6-STRUCTURAL: PASS`; the `SEAM-CALLER:` and `CONTRACT-READER:` lines appear in `git ls-files` order, which is the order the three files are listed in here; the artifact cites [P0-T14] as the observed-failing run of the identical script.
- [x] [P6-T2] Advisory line-count audit and AC-U9 measurement over every write-set `.cs` path (instrument per convention 3); record `$FEATURE/evidence/qa-gates/p6-t2-line-counts-advisory.md`.
  - Acceptance: every write-set `.cs` file except `QuickFiler/Controllers/EfcItemController.cs` and `QuickFiler/Controllers/QfcCollectionController.cs` is at most 500; the artifact carries `OVER-CEILING-AFTER: 2` and the AC-U9 statement `PRE-EXISTING-DEBT: EfcItemController.cs before 1122 after <n>; QfcCollectionController.cs before 2333 after <m>` with `<n>` and `<m>` the measured values (expected 1076 or 1077 and 2306 or 2307), and the sentence that the remaining over-ceiling size is pre-existing debt this change neither introduces nor resolves; `EfcFormController.cs` is recorded as before 1321, after at most 270, and the retained file's `OVER-CEILING: false`. This audit is advisory; [P7-T3] is authoritative.
- [x] [P6-T3] AC-U8 Compile-item parity: with CMD-BASE, run `git diff --name-status $BaseSha HEAD -- '*.cs'` and `git diff $BaseSha HEAD -- QuickFiler/QuickFiler.csproj QuickFiler.Test/QuickFiler.Test.csproj`; record `$FEATURE/evidence/qa-gates/p6-t3-compile-item-parity.md`.
  - Acceptance: the `.cs` name-status list carries exactly seventeen `A` rows (the nine new production and eight new test files named in the Write set), zero `D` rows, and `M` rows only for write-set members; the two csproj diffs carry exactly seventeen added lines, each matching `^\+\s*<Compile Include="[^"]+\.cs" />$` (bare, self-closing, no metadata, no `DependentUpon`), zero removed lines, and the set of `Include` values (after `.Replace('\', '/')` and prefixing the project directory) equals the set of `A` paths; `git diff --numstat $BaseSha HEAD -- QuickFiler.Test/Controllers/EfcFormControllerTests.cs` prints nothing (recorded as `EFCFORMCONTROLLERTESTS-DIFF: none`, the deliberate zero-edit noted in the Write set section); `git status --porcelain --untracked-files=all -- '*.cs' '*.csproj'` prints nothing, recorded as `UNTRACKED-SOURCE: none`, which proves that every path the name-status diff must enumerate is committed and therefore visible to it (an untracked new file is invisible to an anchored diff).
- [x] [P6-T4] Carry-before-removal ordering gate over `QuickFiler/Controllers/QfcCollectionController.PopOut.cs`: for each of the two members, the line number of `= ReadPopOutCarry(group);` is smaller than the line number of the following `RemoveSpecificControlGroup` call, and both calls sit between that member's declaration line and the next member's declaration line; record `$FEATURE/evidence/qa-gates/p6-t4-popout-ordering.md`.
  - Acceptance: the artifact lists the four line numbers (two reads, two removals) and `ORDERING: PASS`; a violation is a HALT.
- [x] [P6-T5] Stale-comment and banned-symbol sweep over the write-set production files: `Select-String -SimpleMatch 'until CoreWebView2InitializationCompleted fires'` across `QuickFiler/Controllers/EfcFormController*.cs` returns 0; `Select-String -Pattern 'Thread\.Sleep\(|Task\.Delay\('` across the twenty production write-set `.cs` files returns 0; `Select-String -SimpleMatch '"WindowsFormsWebView2"'` across the files enumerated by `git ls-files -- ':(glob)QuickFiler/**/*.cs'` (a PowerShell `-Path` wildcard does not recurse) returns exactly 1 (the contract); `Select-String -SimpleMatch '"--incognito "'` across the same enumeration returns exactly 1 (the contract); record `$FEATURE/evidence/qa-gates/p6-t5-literal-sweep.md`.
  - Acceptance: all four counts as stated, recorded with the matching file and line for the two single hits.
- [x] [P6-T6] Commit Phase 6 evidence: `git add -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "chore(792): structural gates and parity evidence"`; record `$FEATURE/evidence/qa-gates/p6-t6-commit.md`.
  - Acceptance: `EXIT_CODE: 0`; `git show --name-only --format= HEAD` lists feature-folder paths only; scoped porcelain (convention 9) prints nothing.

### Phase 7 — Final Toolchain QA Loop, Coverage Delta, and Acceptance Check-off

Loop rule: the four steps run in order format, analyzers, nullable, tests-with-coverage. If any step fails or rewrites a file, the loop restarts at [P7-T2] and every artifact of the failed pass is superseded by the re-run (the artifact records `PASS-NUMBER:`). The loop ends only when all four steps pass in one pass ([P7-T9]).

- [x] [P7-T1] Human checkpoint: Outlook closed before the final rebuilds; run CMD-OUTLOOK and record `$FEATURE/evidence/other/p7-t1-outlook-closed.md`.
  - Acceptance: as [P0-T2] (`OUTLOOK-CLOSED: true`, `EXIT_CODE: 0`).
- [x] [P7-T2] Format step: capture SHA-256 (`Get-FileHash -Algorithm SHA256`) of every write-set `.cs` path and `git status --porcelain --untracked-files=all` before; run `dotnet tool run csharpier format .`; capture both again; run `dotnet tool run csharpier check .`; record `$FEATURE/evidence/qa-gates/p7-t2-format.md`.
  - Acceptance: hashes are recorded against repository-relative paths; the `Formatted N files in` line is recorded and explicitly NOT used as the rewritten count; `REWRITTEN-WRITE-SET-FILES: <n>` is the number of write-set files whose hash changed; any rewritten path OUTSIDE the write set is restored with `git checkout -- <path>` and listed under `OUT-OF-SCOPE-REWRITE-RESTORED:` (it must be a member of `BASELINE-DRIFT-SET` from [P0-T9]; otherwise HALT); `dotnet tool run csharpier check .` exits 0 printing `Checked N files in`, OR exits non-zero with a reported set equal to the restored out-of-scope set and containing no write-set path (record which branch held); if `REWRITTEN-WRITE-SET-FILES` is greater than 0 the artifact records `RESTART-REQUIRED: true` and the loop restarts at this task immediately, per the phase rule; [P7-T3] through [P7-T8] run only in a pass whose [P7-T2] recorded `RESTART-REQUIRED: false`.
- [x] [P7-T3] Authoritative post-format file-size audit over every write-set `.cs` path and the AC-U9 figures; record `$FEATURE/evidence/qa-gates/p7-t3-file-size-audit.md`.
  - Acceptance: every write-set `.cs` file except `QuickFiler/Controllers/EfcItemController.cs` and `QuickFiler/Controllers/QfcCollectionController.cs` is at most 500 lines measured AFTER [P7-T2]; the artifact repeats `OVER-CEILING-AFTER: 2` and the `PRE-EXISTING-DEBT:` line with post-format figures; if any other file exceeds 500 the executor HALTS with `BLOCKED: file-size` naming the file (no split is authorized by this plan without a revision).
- [x] [P7-T4] Analyzer step: CMD-OUTLOOK then CMD-BUILD-ANALYZE; record `$FEATURE/evidence/qa-gates/p7-t4-analyzers.md`.
  - Acceptance: `EXIT_CODE: 0`; exact `0 Error(s)` line; the `Warning(s)` line recorded and its count is not greater than the [P0-T10] baseline count.
- [x] [P7-T5] Nullable step: CMD-OUTLOOK then CMD-BUILD-NULLABLE; record `$FEATURE/evidence/qa-gates/p7-t5-nullable.md`.
  - Acceptance: `EXIT_CODE: 0`; exact `0 Error(s)` line.
- [x] [P7-T6] Test-with-coverage step over `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` via CMD-COVERAGE with `<stage>` = `p7-final`; record `$FEATURE/evidence/qa-gates/p7-t6-coverage-final.md`.
  - Acceptance: the processed document `coverage/p7-final.cobertura.xml` exists and contains `<sources>`; `Total tests:` equals `Passed:` (recorded numerically) and `Failed: 0 (omitted category)`; a test failure is a loop failure (restart); `EXIT_CODE:` recorded: when the runner exits non-zero and this run's console carries `is below the required 80% threshold` and its `Failed:` category is omitted, the artifact declares `ExpectedExitCode: 1`; when the runner exits 0 no `ExpectedExitCode` field is written; any other cause is a HALT (keyed off this run, never off the baseline); record `QUICKFILER-SCOPED-DOCUMENT-LINE-RATE: <root line-rate>` and `REPO-WIDE-FLOOR: NOT MEASURED (single test assembly; see D10)`; the six root and six `QuickFiler` package attributes recorded numerically; per-file rows (same derivation as [P0-T12]) for the ten baseline files plus `Viewers/WebView2EnvironmentContract.cs`, `Controllers/EfcFormController.Breadcrumb.cs`, `Controllers/EfcFormController.SetupAndProperties.cs`, `Controllers/EfcFormController.EventHandlers.cs`, `Controllers/EfcFormController.Actions.cs`, `Controllers/EfcFormController.Helpers.cs`, `Controllers/EfcDataModel.Carry.cs`; `Uninstrumented, not comparable` rows for `Controllers/EfcItemController.cs`, `Controllers/EfcItemController.WebViewEnvironment.cs`, `Controllers/QfcCollectionController.cs`, `Controllers/QfcCollectionController.PopOut.cs` each with `CLASS-ELEMENTS: 0` and the attribute citation.
- [x] [P7-T7] Regression sweep over `TaskMaster.Test/bin/Debug/TaskMaster.Test.dll` via CMD-VSTEST and CMD-SWEEP with `<task>` = `p7-t7`; record `$FEATURE/evidence/qa-gates/p7-t7-taskmaster-sweep.md`.
  - Acceptance: the set of failed fully-qualified names is a subset of `BASELINE_FAILURE_SET` from [P0-T13] (`NEW-FAILURES: none`); a new failure is a loop failure; a hang is a HALT naming the Blame output.
- [x] [P7-T8] Coverage delta artifact `$FEATURE/evidence/qa-gates/p7-t8-coverage-delta.md` computed from `coverage/p0-baseline.cobertura.xml` and `coverage/p7-final.cobertura.xml` with CMD-BASE for the diff.
  - Gates, each recorded `PASS|FAIL` with the numbers: (1) `WebView2EnvironmentContract.cs` per-file line rate at least 90 percent. (2) For `EfcFormController.Breadcrumb.cs` and `EfcDataModel.Carry.cs`, the NEW executable lines — Cobertura line rows of that file whose trimmed source text does not occur (trimmed) anywhere in `git show $BaseSha:QuickFiler/Controllers/EfcFormController.cs` or `git show $BaseSha:QuickFiler/Controllers/EfcDataModel.cs` respectively — are covered (`hits` at least 1) at a ratio of at least 90 percent; the ratio is computed over the new executable lines other than the pre-declared misses, which are exactly: `EfcFormController.Breadcrumb.cs` — the `BeginInvoke` branch of `ShowFolderAreaError`; `EfcDataModel.Carry.cs` — the lines of the `FromField` scoring lambda in the null-list branch that read `scoringInput` (reachable only with a live `MailItemHelper`). Every other new executable line counts, and the artifact lists every uncovered line by number and text. (3) Changed-line no-regression for each instrumented edited file (`WebView2BreadcrumbHost.cs`, `BreadcrumbBridgeRouter.cs`, `BreadcrumbOutboundQueue.cs`, `EfcHomeController.cs`, `QfcItemController.cs`, `EfcViewerQueue.cs`, `QfcItemController.ViewerSetup.cs`): every added line from `git diff --unified=0 $BaseSha HEAD -- <file>` is recorded as `hits=<n>` or `hits=non-executable` (no Cobertura row), and the covered ratio over executable added lines is at least 90 percent, except `QfcItemController.ViewerSetup.cs` whose added lines sit in the method-level-exempt `InitializeWebViewAsync` and are recorded `n/a` by citation of `ViewerSetup.cs:48`; `QuickFiler/Helper Classes/EfcViewerQueue.cs`: the single body line of `InvokeOnUiDispatcher` is recorded `n/a` by citation of D8 (`UiThread.Dispatcher` throws outside an initialized UI thread, `UiThread.cs:264-282`); its remaining added executable lines must be covered; `QuickFiler/Controllers/EfcHomeController.cs`: the public constructor's forwarding initializer (`: this(` through `) { }`) is recorded `n/a` by citation of `EfcHomeController.cs:40` (`CreateDefaultDependencies()` binds production factories, and every QuickFiler.Test call site passes a dependencies instance: `EfcHomeControllerLifecycleTests.cs:291`, `EfcHomeControllerMetricsTests.cs:373`, `EfcHomeControllerSeamTests.cs:147`); its two deposit statements must be covered; and `QuickFiler/Viewers/WebView2BreadcrumbHost.cs`: the `ForwardNavigateToString(html);` call inside the `NavigateCore` local function is recorded `n/a` by citation of the forwarder's exemption remark (`WebView2BreadcrumbHost.cs:170-172`, a live CoreWebView2 is required); the ratio is computed over its other added executable lines. Per-file expectation once the three `n/a` clauses are applied (each ratio is over the added executable lines other than the recorded `n/a` lines): `WebView2BreadcrumbHost.cs` 100 percent — the two contract reads in `InitializeAsync` run under [P1-T1] test 1 and `WebView2BreadcrumbHostTests.InitializeAsync_InstallsUiDispatcherFromUiSyncContext`, the `NavigateCore` core read, null guard, log line and return run inline under [P1-T1] test 2 (null dispatcher), and the `dispatcher.Dispatch(NavigateCore)` line runs under `WebView2BreadcrumbHostTests.NavigateToString_PostsExactlyOnceToTheUiContext` (`:83-125`, which records the post and does not depend on the forward throwing); `BreadcrumbBridgeRouter.cs` 100 percent — every added executable line of `NotifyInitializationFailed`, the selection-clearing branch included, runs under the [P3-T2] and [P3-T3] tests; `BreadcrumbOutboundQueue.cs` 100 percent — the three `DiscardPending` statements run under [P3-T3]; `EfcHomeController.cs` 100 percent — the two deposit statements run under `EfcHomeController_DepositsTheCarryOnTheDataModelBeforeConstructingTheFormController`; `QfcItemController.cs` 100 percent — the `FolderHandler` accessor line runs under `ReadPopOutCarry_WithConcreteItemController_ReturnsHandlerAndHelper`; `EfcViewerQueue.cs` 100 percent — the line-25 initializer runs in the static constructor of any test touching the type and the line-68 reset runs in the `ViewerQueueStaticWrapperTests` `[TestCleanup]` (`:18`). No file in this gate carries an added executable line that no test can reach other than the lines the three `n/a` clauses name, so a measured ratio below 90 percent indicates a test that did not run or an unplanned line, is reported as `BLOCKED: coverage` with the uncovered line listed, and is never resolved by adding an `n/a` clause. (4) Type-level no-regression for the split: the sum of `lines-covered` over the six `EfcFormController*.cs` files is at least the [P0-T12] `lines-covered` of `EfcFormController.cs`; likewise `EfcDataModel.cs` plus `EfcDataModel.Carry.cs` against baseline `EfcDataModel.cs`. (5) Per-file no-regression for the seven files in (3): `lines-covered` post at least `lines-covered` base minus the count of deleted covered lines (deleted lines from the diff that had `hits` at least 1 in the baseline map), with the arithmetic shown. (6) Package comparability: if the `QuickFiler` package `lines-valid` figures differ by at most 1 percent of the baseline figure, `BRANCH: A` and the package `line-rate` post is at least base minus 0.005; otherwise `BRANCH: B`, recorded and not gated, with one sentence stating the denominators are not comparable. (7) `QUICKFILER-SCOPED-DOCUMENT-LINE-RATE` baseline and final repeated side by side, reported, not gated (D10), with the line `REPO-WIDE-FLOOR: NOT MEASURED (single test assembly; see D10)`.
  - Acceptance: gates (1) through (5) are `PASS`; exactly one branch of (6) is named; a FAIL is a loop failure with `BLOCKED: coverage` naming the gate.
- [x] [P7-T9] Single-pass closure: write `$FEATURE/evidence/qa-gates/p7-t9-toolchain-pass.md` naming the four step artifacts of the final pass ([P7-T2], [P7-T4], [P7-T5], [P7-T6]) with their `PASS-NUMBER:`, the format observation, and the statement that all four steps passed without errors in that one pass.
  - Acceptance: the four cited artifacts share the same `PASS-NUMBER:`; [P7-T2] of that pass recorded `REWRITTEN-WRITE-SET-FILES: 0`; [P7-T7] and [P7-T8] of that pass are PASS.
- [x] [P7-T10] Check off AC-U1 in `$FEATURE/spec.md` (line 306, `- [ ]` to `- [x]`, text unchanged), citing [P3-T7] (observed failing), [P4-T11] (pass-after), [P5-T5] (mutation), and the D4 label carrier; record the check-off line in `$FEATURE/evidence/issue-updates/p7-t10-ac-u1.md`.
  - Acceptance: `Select-String -LiteralPath $FEATURE/spec.md -Pattern '^- \[x\] AC-U1:'` returns 1 and `'^- \[ \] AC-U1:'` returns 0; the criterion text after the checkbox is byte-identical to the corresponding line of `git show "${SpecRefSha}:$FEATURE/spec.md"` (CMD-BASE binds `$SpecRefSha`; the feature folder does not exist at `BASE-SHA`).
- [x] [P7-T11] Check off AC-U2 in `$FEATURE/spec.md` (line 307), citing [P3-T7], [P4-T11], [P5-T7]; record `$FEATURE/evidence/issue-updates/p7-t11-ac-u2.md`.
  - Acceptance: as [P7-T10] for `AC-U2:`.
- [x] [P7-T12] Check off AC-U3 in `$FEATURE/spec.md` (line 308), citing [P1-T4] (scheduler identity observed failing), [P3-T7], [P4-T11], [P5-T6], [P5-T8], [P6-T4]; record `$FEATURE/evidence/issue-updates/p7-t12-ac-u3.md`.
  - Acceptance: as [P7-T10] for `AC-U3:`.
- [x] [P7-T13] Check off AC-U4 in `$FEATURE/spec.md` (line 309), citing [P0-T15] (dossier for the already-satisfied half), [P1-T4], [P4-T11], [P5-T1]; record `$FEATURE/evidence/issue-updates/p7-t13-ac-u4.md`.
  - Acceptance: as [P7-T10] for `AC-U4:`.
- [x] [P7-T14] Check off AC-U6 in `$FEATURE/spec.md` (line 311), citing [P0-T14] (structural FAIL), [P6-T1] (structural PASS), [P1-T4], [P4-T4], [P4-T11], [P5-T2] (site-1 regression and, in its second half, the site-2 structural proof), [P5-T3]; record `$FEATURE/evidence/issue-updates/p7-t14-ac-u6.md`.
  - Acceptance: as [P7-T10] for `AC-U6:`.
- [x] [P7-T15] Check off AC-U7 in `$FEATURE/spec.md` (line 312), citing [P3-T7], [P4-T11], [P5-T4]; record `$FEATURE/evidence/issue-updates/p7-t15-ac-u7.md`.
  - Acceptance: as [P7-T10] for `AC-U7:`.
- [x] [P7-T16] Check off AC-U8 in `$FEATURE/spec.md` (line 313), citing [P7-T3] (post-format sizes) and [P6-T3] (Compile-item parity); record `$FEATURE/evidence/issue-updates/p7-t16-ac-u8.md`.
  - Acceptance: as [P7-T10] for `AC-U8:`.
- [x] [P7-T17] Check off AC-U9 in `$FEATURE/spec.md` (line 314), citing the `PRE-EXISTING-DEBT:` line of [P7-T3]; record `$FEATURE/evidence/issue-updates/p7-t17-ac-u9.md`.
  - Acceptance: as [P7-T10] for `AC-U9:`; `Select-String -LiteralPath $FEATURE/spec.md -Pattern '^- \[x\] AC-U[1-9]:'` returns 8 and `'^- \[ \] AC-U[1-9]:'` returns 1 (AC-U5 remains open).
- [x] [P7-T18] Commit the final-pass source and evidence: `git add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "chore(792): final toolchain pass, coverage delta, acceptance check-off"`; record `$FEATURE/evidence/qa-gates/p7-t18-commit.md`.
  - Acceptance: `EXIT_CODE: 0`; scoped porcelain (convention 9) prints nothing; `git diff --name-only $BaseSha HEAD -- '*.cs' '*.csproj'` still lists exactly the 30 paths recorded in [P4-T12].

### Phase 8 — Manual Verification After the Toolchain Pass, AC-U5 Check-off, and Terminal Commit

- [x] [P8-T1] Human checkpoint and add-in rebuild: CMD-OUTLOOK (Outlook closed by a person through its own exit), then `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU"` so `TaskMaster/bin/Debug` holds the fixed add-in, then the person reopens Outlook and confirms the add-in loaded; record `$FEATURE/evidence/other/p8-t1-addin-rebuild.md`.
  - Acceptance: `OUTLOOK-CLOSED: true` before the build; build `EXIT_CODE: 0` with the exact `0 Error(s)` line; the artifact records the build timestamp and the person's confirmation that Outlook was reopened with the add-in loaded (no `Stop-Process` anywhere).
- [x] [P8-T2] Human execution of the AC-U5 runbook in `$FEATURE/user-story.md` lines 52-86 (steps 1-9, both entry points: pop-out from QuickFiler, then ribbon Sort Email; QuickFiler is opened FIRST so an item-body WebView is already running), recorded in `$FEATURE/evidence/other/p8-t2-ac-u5-manual-verification.md`.
  - Acceptance: the artifact carries `Timestamp:`, the Outlook session start time, the four observations of the runbook's pass/fail section each marked `PASS` or `FAIL`, the log lines inspected in step 9 transcribed as text (no log file committed), and `AC-U5: PASS` only when all four hold. On any `FAIL` the executor HALTS with `BLOCKED: AC-U5` and does not proceed to [P8-T3]. This task is human-executed and is not automated.
- [x] [P8-T3] Check off AC-U5 in `$FEATURE/spec.md` (line 310), citing [P8-T2]; record `$FEATURE/evidence/issue-updates/p8-t3-ac-u5.md`.
  - Acceptance: as [P7-T10] for `AC-U5:`.
- [x] [P8-T4] Acceptance reconciliation over `$FEATURE/spec.md`: `Select-String -Pattern '^- \[x\] AC-U[1-9]:'` returns 9, `'^- \[ \] AC-U[1-9]:'` returns 0, and every criterion's text is byte-identical to `SPEC-REF-SHA`'s (with CMD-BASE, `git diff $SpecRefSha -- $FEATURE/spec.md` shows exactly nine changed lines, each differing only in `[ ]` versus `[x]`); write `$FEATURE/evidence/issue-updates/p8-t4-ac-status.md` containing the `### Acceptance Criteria Status` block (Source `$FEATURE/spec.md`, Total 9, Checked off 9, Remaining 0).
  - Acceptance: the counts as stated; the status block present verbatim in the artifact.
- [ ] [P8-T5] Terminal commit and clean-tree proof for `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792`: `git add -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` then `git commit -m "chore(792): manual verification evidence and acceptance reconciliation"`; then write `$FEATURE/evidence/other/p8-t5-terminal.md` capturing `git status --porcelain --untracked-files=all -- . ':(exclude).claude/agent-memory'` and `git rev-parse HEAD`; then `git add -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` and `git commit --amend --no-edit`; then check this task's own box in the plan as the final edit.
  - Acceptance: the porcelain capture in the artifact lists nothing outside the feature folder and nothing ending `.cs`, `.csproj`, `.sln` or `packages.config`; after the amend, `git diff --numstat HEAD -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/plan.2026-09-17T07-30.md` prints `1 1` for exactly that one file (this task's own check-off is the sole permitted residual) and `git status --porcelain --untracked-files=all -- . ':(exclude).claude/agent-memory' ':(exclude)docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/plan.2026-09-17T07-30.md'` prints nothing; the head SHA is recorded as an observation.

## Notes

- Git gate scoping (convention 9) is binding on [P0-T7], [P0-T16], [P2-T15], [P3-T8], [P4-T12], [P5-T9], [P6-T6], [P7-T18], [P8-T5]; `.claude/agent-memory/**` is tracked and agent-written and is never enumerated as tolerated dirt.
- `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` is in the spec's write set but receives no edit; the strengthened AC-U4 test lives in `EfcFormControllerIssue792Tests.cs` per the brief's placement instruction and the 15-line headroom measured in [P0-T8].
- The brief's sentence that the existing PopulateFolderCombobox test "asserts only that the method logs once and does not fault" is imprecise; the test asserts a substituted-sink call count. The plan's mutation [P5-T1] is chosen so that the pre-existing test cannot detect it and the new one must.
- No task in this plan edits any criterion text in `spec.md`; check-off tasks flip the checkbox only.
- Coverage raw output lives under the gitignored `coverage/` directory for the duration of execution and is never copied under the feature folder (D12).

## Planner self-review

SELF-REVIEW: RE-DERIVED THIS PASS

Citations re-derived in this pass (file | locator):
- QuickFiler/Viewers/WebView2BreadcrumbHost.cs | lines 12 (round 2: `namespace QuickFiler.Viewers`, the namespace [P2-T2] states), 146, 157-167, 171-172, 193-218, 239-270, 246-250, 265-268, 329-354, 341, 352-353; total 368
- QuickFiler/Controllers/QfcItemController.ViewerSetup.cs | lines 48-49, 55-62, 71-78, 110-124, 426-462 (Cleanup nulls at 444, 455); total 479
- QuickFiler/Controllers/EfcItemController.cs | lines 1-24 (usings), 26 (attribute), 31-60 (constructors), 156-158, 168-177, 179-212, 195-199, 359; total 1122
- QuickFiler/Controllers/EfcItemController.WebViewFaultBoundary.cs | lines 8-15, 25-42
- QuickFiler/Controllers/BreadcrumbBridgeRouter.cs | lines 19, 35-40, 47-63, 310-329 (NotifyCoreInitialized 320-329, drain call 328); total 407
- QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs | lines 143-180 (DeliverDocument 168-180)
- QuickFiler/Controllers/BreadcrumbOutboundQueue.cs | lines 15-18, 29, 37-52, 59-65; total 67
- QuickFiler/Controllers/EfcFormController.cs | lines 1-27, 26, 77, 79-97, 121-264, 128-141, 150-168, 170-185, 231-262, 266, 365, 367, 479, 481, 834, 836, 990, 992, 1044-1129, 1064, 1072-1082, 1080, 1131, 1250-1272, 1270, 1289, 1291-1319; total 1321; eight TryReportBoundaryFault call sites 556, 573, 591, 653, 668, 1015, 1127, 1270
- QuickFiler/Controllers/EfcHomeController.cs | lines 1-14 (usings), 47-52, 54-95, 66-71, 73, 85-93, 264-269; total 447
- QuickFiler/Controllers/EfcHomeControllerDependencies.cs | lines 13-32, 34-79
- QuickFiler/Controllers/EfcHomeControllerDependencyFactories.cs | lines 70-80, 112, 117-120
- QuickFiler/Controllers/EfcDataModel.cs | lines 1-17 (usings), 21, 48-87, 156-175 (Globals, Token, TokenSource are plain field reads), 177-186, 188-221 (round 2: null-list branch 190-208, `MailInfo is null` at 192), 241 (`MailInfo => ConversationResolver?.MailHelper`); total 499
- QuickFiler/Controllers/QfcCollectionController.cs | lines 1-14 (usings), 22-23, 714-724, 726-739, 863, 917; total 2333
- QuickFiler/Controllers/QfcItemController.cs | lines 41, 67, 135-140, 259, 265; total 334
- QuickFiler/Controllers/QfcItemController.FolderHandling.cs | lines 57-86
- QuickFiler/Controllers/QfcItemGroup.cs | lines 16-18, 39-44, 59
- QuickFiler/Interfaces/IQfcItemController.cs | line 41
- QuickFiler/Helper Classes/EfcViewerQueue.cs | lines 10-27, 25, 62-69, 68; total 101
- QuickFiler/Helper Classes/ItemViewerQueue.cs | lines 23-27
- QuickFiler/Helper Classes/ViewerQueueCore.cs | lines 18-35, 98-101, 126-142
- QuickFiler/Viewers/EfcViewer.cs | lines 20-30, 36-40, 85
- QuickFiler/Viewers/EfcViewer.Designer.cs | lines 239, 4244-4251 (round 2: `internal System.Windows.Forms.Label label2;` is line 4248, not 4247, which is `label1`)
- QuickFiler/Viewers/ItemViewer.cs | lines 26, 58-61
- QuickFiler/Viewers/IWebViewCoreInitializer.cs | lines 49-52, 64
- QuickFiler/Viewers/WebView2CoreInitializer.cs | lines 35-56, 67-73
- QuickFiler/Viewers/IBreadcrumbWebHost.cs | lines 11-26
- QuickFiler/QuickFiler.csproj | Compile items at lines 289-290, 296, 303-304, 314-315, 335, 356, 426, 428
- QuickFiler.Test/QuickFiler.Test.csproj | Compile items at lines 58-59, 123-127, 141, 167, 216-218, 223-230, 481 (ProjectReference)
- QuickFiler/Properties/AssemblyInfo.cs | line 5 (InternalsVisibleTo QuickFiler.Test)
- QuickFiler.Test/Controllers/EfcFormControllerTests.cs | lines 17, 24-34, 61-159, 245-280, 282-294, 299-328, 476-483; total 485
- QuickFiler.Test/Controllers/EfcFormControllerTests.Part2.cs | line 24, closing at 489-490
- QuickFiler.Test/Controllers/EfcItemControllerTests.cs | lines 26-30, 89-109, 354-357, 360-396
- QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs | lines 22-32, 34-101
- QuickFiler.Test/Controllers/QfcCollectionControllerTests.cs | lines 24, 30-74
- QuickFiler.Test/Controllers/QfcItemController.InitializationTests.Part2.cs | lines 29 (`public partial class QfcItemController_InitializationTests`), 223-237, 247-267
- QuickFiler.Test/Controllers/QfcItemController.InitializationTests.cs | line 30 (round 2: the class name carries an underscore, so the [P4-T11] filter token is `QfcItemController_InitializationTests`; Part3.cs:27 is the third partial)
- QuickFiler.Test/Controllers/QfcItemController.FolderHandlingTests.Part2.cs | lines 373-383 (round 2: `new FolderPredictor(globals)` at 378 over a mocked `IApplicationGlobals`)
- QuickFiler.Test/Controllers/QfcItemController.TestSupport.cs | lines 344-352 (round 2: `Mock<IOlObjects>` at 344, `globals.SetupGet(g => g.Ol)` at 348)
- UtilitiesCS/Interfaces/IGlobals/IOlObjects.cs | line 13 (`Application App { get; }`; a loose mock returns null, which `FolderPredictor(IApplicationGlobals)` stores without dereferencing)
- QuickFiler.Test/Controllers/EfcHomeControllerLifecycleTests.cs | lines 289-344
- QuickFiler.Test/Viewers/WebView2BreadcrumbHostTests.cs | lines 22-25, 31-75, 83, 258-293, 349, 399-419, 428-438; total 440
- QuickFiler.Test/Helper Classes/ViewerQueueStaticWrapperTests.cs | lines 11-22 (round 2: `[DoNotParallelize]` at 11, `EfcViewerQueue.ResetProductionCoreDefaultsForTesting()` in `[TestCleanup]` at 18), 238-269 (round 2: `EfcViewerQueue.ProductionBlockingPriorityScheduler = ...` substituted at 254), 330-334
- QuickFiler.Test/TestSupport/WinFormsPumpHost.cs | lines 26-60
- UtilitiesCS/Threading/UiThread.cs | lines 17, 140-169 (round 2: `SynchronizationContextAwaiter.IsCompleted` at 155-163 returns true when the ambient context is reference-equal to the captured one; line 100 is `_syncContextForm.Hide();` and was a wrong citation), 260-283
- UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs | lines 5, 19, 35-60, 152-159
- UtilitiesCS/OutlookObjects/Folder/BreadcrumbHtmlRenderer.cs | lines 32-52
- UtilitiesCS/OutlookObjects/Folder/IFolderSearchHandler.cs | lines 4, 14-20
- UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs | lines 15-17, 26-43 (round 2: `FolderPredictor(IApplicationGlobals AppGlobals)` at 36-41 reads `AppGlobals.Ol.App` at 39)
- UtilitiesCS/OutlookObjects/Folder/FolderPredictor.IFolderSearchHandler.cs | line 10
- QuickFiler/Helper Classes/ConversationResolver.cs | lines 282-286
- BannedSymbols.txt | lines 4-7
- .gitignore | lines 39, 57, 84, 144, 191, 350
- .csharpierignore | lines 4-14
- global.json | lines 2-10
- dotnet-tools.json | lines 5-11
- scripts/vscode/TaskMaster.cli.runsettings | lines 4-7
- scripts/vscode/Invoke-MSTestWithCoverage.ps1 | lines 1-13 (round 2: `param()` accepts only `-SearchRoot`, `-Configuration`, `-CoverageOutput`, `-NoExecute`), 82-94 (round 2: `/TestCaseFilter:TestCategory!=LiveOutlook` hard-coded at 91), 283-284 (inner defaults `coverage\test-results` and `mstest-coverage-run.trx`), 305-306 (`$repoRoot` from the script location; `-SearchRoot` joined to it), 330-337 (round 2: `*.Test.dll` sweep under the search root), 381-388, 393-401 (`<stage>.jacoco.xml`), 408-432 (`.summary.txt` and the raw-document discard decision), 434
- scripts/vscode/Invoke-MSTestWithCoverage.Projection.ps1 | lines 148-163 (round 2: `Test-RawCoverageDocumentRetained` keeps the document only when its parent directory equals the repository `coverage` directory; a subdirectory is discarded)
- scripts/vscode/Invoke-MSTestWithCoverage.Threshold.ps1 | lines 3, 54, 58, 124
- scripts/vscode/Invoke-Restore.ps1 | lines 1-10, 105-112
- scripts/vscode/Install-RepoDotNetSdk.ps1 | lines 1-11, 36
- TaskMaster/TaskMaster.csproj | line 501 (ProjectReference to QuickFiler)
- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md | lines 10, 113, 155-167, 269-277, 306-314, 318-352
- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/user-story.md | lines 52-86
- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/issue.md | line 12
- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/research/2026-09-17T11-20-breadcrumb-webview2-init-research.md | sections 2, 3, Numeric Derivation Evidence, 6-9, 11

Sibling regions re-checked: `NotifyCoreInitialized` (unchanged), `PostMessageJson` (guard pattern mirrored), `LoadFolderHandlerAsync` in `QfcItemController.FolderHandling.cs` (the #678 receiver contract the adoption mirrors), the `IncognitoArgument` doc comment (the "exactly one owner" sentence is rewritten in [P4-T3]), `ConfigureBreadcrumbControl` (moved, unchanged), the `ViewerQueueStaticWrapperTests` cleanup (restores the delegate the single identity test reads), the existing `PopulateFolderCombobox_WhenDataModelFaults_LogsOnceAndDoesNotFault` (unchanged; its mutation blindness is the reason for [P5-T1]'s shape).

Round 2 sibling re-checks (2026-09-17): `EfcDataModel.InitFolderHandlerAsync` lines 188-221 in full, for the sixth [P3-T5] test — with `MailInfo` null the null-list branch runs `new FolderPredictor(Globals)` on a `Task.Run` with the default `Token`, so an uninitialized instance with `_globals` set completes and the pre-fix failure is deterministic on the release assertion; `EfcDataModel.Globals`/`Token`/`TokenSource` (156-175) are plain field accessors with no lazy initialization; `MailInfo` (241) is null when `ConversationResolver` is null; `IOlObjects.App` (13) is a getter a loose mock satisfies with null, and the `FolderPredictor(IApplicationGlobals)` constructor (36-41) stores it without dereferencing; `EfcViewer.Designer.cs` 4244-4251 (`label1` at 4247 and `label2` at 4248, both `internal`, so the D4 carrier is unchanged and only the line number moves); `UiThread.cs` 140-169 (the awaiter's `IsCompleted` short-circuit is the mechanism [P4-T4] relies on; `OnCompleted` posts otherwise, which a bare `new SynchronizationContext()` would run inline on the thread pool, so the reference-equal arrangement is the one that keeps the test on the MSTest thread); the runner's `param()` block and its inner defaults (283-284), confirming that the `-ResultsDirectory` and `-LogFileName` arguments the previous CMD-COVERAGE passed are inner-function parameters not exposed at the script boundary; `Invoke-MSTestWithCoverage.Projection.ps1` 148-163, confirming that redirecting the output into a subdirectory would delete the processed document after the summary is written; `ViewerQueueStaticWrapperTests.cs` 238-269 (the wrapper test substitutes `ProductionBlockingPriorityScheduler` at 254 and its `[TestCleanup]` at 18 restores it, so any new test that wrote or reset that static would race a `[DoNotParallelize]` class from a parallel class; the surviving identity test performs a read only); the three partials of `QfcItemController_InitializationTests` (`.cs:30`, `.Part2.cs:29`, `.Part3.cs:27`) for the corrected [P4-T11] filter token; the [P3-T2] `LeavesNoStash` test's neighbours (`ClearsThePendingDocument` asserts on the same `_navigated` list and the two assertions do not conflict pre-fix, post-fix or under [P5-T7]); the test arithmetic across [P1-T4] (5 = 2 + 2 + 1; 1 pass, 4 fail), [P3-T7] (31 = 5 + 26; 13 pass, 18 fail) and [P4-T11] (32 = 31 + 1), re-summed after defects 3, 5 and 6 were applied together.

Round 3 citations re-derived in this pass (2026-09-17; every citation a round-3 edit touched, plus its sibling region, read from the item worktree):
- QuickFiler/Viewers/WebView2BreadcrumbHost.cs | 157-167 (`NavigateToString`: null-dispatcher inline forward at 160-164, dispatch at 166); 169-180 (forwarder summary 169, exemption remark 170-175, `[ExcludeFromCodeCoverage]` 176, `ForwardNavigateToString` 177-180); 193-218 (`PostMessageJson` with the `PostCore` local function, the shape [P4-T1] mirrors); 239-270 (`InitializeAsync`; folder computation 246-249, parameterless options 250, seam call 265-268)
- QuickFiler/Controllers/QfcItemController.ViewerSetup.cs | 61 (dead comment), 62 (target-typed construction), 71 (`_webViewInitializer.CreateEnvironmentAsync(`), 124 (commented-out `CreateAsync`)
- QuickFiler/Controllers/EfcItemController.cs | 187 (dead comment), 188 (construction), 195 (direct `CoreWebView2Environment.CreateAsync(`)
- QuickFiler/Viewers/WebView2CoreInitializer.cs | 72 (the adapter's `CreateAsync`, excluded by the gate's path filter)
- `git ls-files` order of the gate's files: `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs` < `QuickFiler/Controllers/EfcItemController.cs` < `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` < `QuickFiler/Viewers/WebView2BreadcrumbHost.cs` (bytewise path order; `W` at 0x57 precedes `c` at 0x63, so the dotted-suffix part sorts before the base file, as the tracked pair `EfcItemController.WebViewFaultBoundary.cs` then `EfcItemController.cs` shows today; the helper iterates `$files` in that order and each file top to bottom). No gate depends on this ordering: post-fix `EfcItemController.cs` emits no line of any category, and pre-fix `EfcItemController.WebViewEnvironment.cs` does not exist.
- QuickFiler/Controllers/EfcHomeController.cs | 40-43 (`CreateDefaultDependencies()` returns `_defaultDependenciesFactory()`), 47-52 (public constructor, forwarding initializer at 52), 54-95 (internal constructor; `DataModelFactory` at 66-71, `if (DataModel.Mail is not null)` at 73)
- QuickFiler.Test/Controllers/EfcHomeControllerLifecycleTests.cs | 291-299 (`new EfcHomeController(Globals, ParentCleanup, CreateDependencies(...), mail)`, internal overload)
- QuickFiler.Test/Controllers/EfcHomeControllerMetricsTests.cs | 373 (`new EfcHomeController(globals, () => { }, dependencies)`, internal overload)
- QuickFiler.Test/Controllers/EfcHomeControllerSeamTests.cs | 147-151 (`new EfcHomeController(probe.Globals.Object, probe.ParentCleanup, dependencies)`, internal overload); these three are the only `new EfcHomeController(` sites under `QuickFiler.Test`, so the public overload has no test caller
- QuickFiler/Controllers/EfcDataModel.cs | 188-221 (`InitFolderHandlerAsync`; the null-list branch reads `MailInfo` at 192, 202 and 204 — the brief's `:198` and `:201` were off, 198 is `FolderHelper = await Task.Run(` and 201 is `Globals,`)
- QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs | 34-74 (setup: `_initialized` false, `_navigated`/`_posted` recorders, provider returning a two-segment chain, router over a real `BreadcrumbOutboundQueue`), 86-96 (`Bind()`), 98-101 (`Inbound(string json)` runs `ProcessInboundAsync` synchronously), 111 (`{"type":"rowSelected","rowId":"row-0"}`)
- QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs | 217-219 (`_router.SelectedFolderPath` is publicly readable), 232-235 (`Bind()`, the same `rowSelected` payload, `string observed = "sentinel";`, `SelectedFolderPathChanged += (s, path) => observed = path;`)
- QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs | 83-119 (`SelectRow`; with no bound root, `CommitSelection(row, row.FilingTarget)`), 143-151 (`CommitSelection` sets `_selectedRowId`, `SelectedFolderPath`, posts a render through the queue, raises `SelectedFolderPathChanged`), 168-180 (`DeliverDocument` stashes when the host is uninitialized) — so after `Bind()` and the `rowSelected` payload on an uninitialized host, a stash exists, `SelectedFolderPath` is non-null and the render payload is queued, which is the arrangement the revised [P3-T2] first test relies on
- QuickFiler.Test/Viewers/WebView2BreadcrumbHostTests.cs | 83-125 (`NavigateToString_PostsExactlyOnceToTheUiContext`: a `RecordingSynchronizationContext` behind a `BreadcrumbUiDispatcher`, asserts `PostCount == 1` and an empty error list; it observes the dispatch, not the forward, so the [P4-T1] rewrite leaves it green and it covers the `dispatcher.Dispatch(NavigateCore)` line), 349-391 (`PostMessageJson_WithNoDispatcher_ExecutesInlineAndDropsThePayload`, the pattern [P1-T1] test 2 mirrors)
- QuickFiler/Helper Classes/EfcViewerQueue.cs | 1-4 (`using System.Windows.Threading;` and `using UtilitiesCS;` are already present, so the expression-bodied `InvokeOnUiDispatcher` needs no new using; `UiThread` is already referenced at 20), 22-25 (initializer `(action, priority) => action();` at 25), 62-69 (`ResetProductionCoreDefaultsForTesting`, reset at 68)

Round 3 sibling re-checks: the [P3-T2] second test (`LeavesNoStash`) shares the arrange with the revised first test only up to `Bind()`; it does not select a row, so its `_navigated.Count` and content assertions and the [P5-T7] mutation prediction are unchanged. The [P3-T7] partition, the 31/32 test arithmetic and every test name are unchanged by round 3. The [P5-T8] mutation (post-fix, stub always false) is unaffected by the [P3-T5] fail-before wording correction, which describes the Phase-2 tree only. The [P2-T11] insertion ceiling (26) still holds with the forwarding initializer wrapped by csharpier across eight lines (expected 18 insertions, 3 deletions). The [P4-T10] count of 3 `InvokeOnUiDispatcher` matches (declaration plus two uses) holds for the expression-bodied form, which places the identifier on one line. [P7-T8] gate (3) was re-summed per file after Defects B, C and D landed and is restated inside the task; gates (1), (2), (4), (5) and (6) are untouched.

VALIDATOR NOT RUN: the `mcp__drm-copilot__validate_orchestration_artifacts` tool is not in this agent's tool surface; the structural self-check (canonical phase headings, sequential digit-only task IDs, a path token on every task line, Phase 0 policy-read and baseline tasks, QA keywords in the final phase, canonical evidence kinds only) was performed by grep over this file.

PLANNER-INTERNAL-REVIEW: PASS
CITATION-TO-TREE: PASS
AC-TRACEABILITY: PASS
SCOPE-BOUNDARY: PASS
CITATION: QuickFiler/Viewers/WebView2BreadcrumbHost.cs | lines 157-167, 170-180, 193-218, 239-270, 250, 265, 329-354
CITATION: QuickFiler/Controllers/QfcItemController.ViewerSetup.cs | lines 48-62, 71, 444-455
CITATION: QuickFiler/Controllers/EfcItemController.cs | lines 26, 168-212, 195
CITATION: QuickFiler/Viewers/WebView2CoreInitializer.cs | line 72
CITATION: QuickFiler/Controllers/BreadcrumbBridgeRouter.cs | lines 320-329
CITATION: QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs | lines 83-119, 143-151, 168-180
CITATION: QuickFiler/Controllers/BreadcrumbOutboundQueue.cs | lines 59-65
CITATION: QuickFiler/Controllers/EfcFormController.cs | lines 26, 121-264, 1044-1129, 1064, 1072-1082, 1250-1272
CITATION: QuickFiler/Controllers/EfcHomeController.cs | lines 40-43, 47-95
CITATION: QuickFiler.Test/Controllers/EfcHomeControllerLifecycleTests.cs | lines 291-299
CITATION: QuickFiler.Test/Controllers/EfcHomeControllerMetricsTests.cs | line 373
CITATION: QuickFiler.Test/Controllers/EfcHomeControllerSeamTests.cs | lines 147-151
CITATION: QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs | lines 34-74, 86-101, 111
CITATION: QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs | lines 217-219, 232-235
CITATION: QuickFiler.Test/Viewers/WebView2BreadcrumbHostTests.cs | lines 83-125, 258-293, 349-391
CITATION: QuickFiler/Controllers/EfcDataModel.cs | lines 156-175, 177-221, 192, 202, 204, 241
CITATION: QuickFiler/Controllers/QfcCollectionController.cs | lines 22, 714-739
CITATION: QuickFiler/Controllers/QfcItemController.cs | lines 41, 135-140, 259-265
CITATION: QuickFiler/QuickFiler.csproj | Compile item for Helper Classes/EfcViewerQueue.cs (that file: lines 22-25, 62-69; the path segment carries a space, so it is cited through its project file)
CITATION: QuickFiler/Viewers/EfcViewer.Designer.cs | lines 239, 4248, 4250
CITATION: UtilitiesCS/Threading/UiThread.cs | lines 155-163, 264-282
CITATION: UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs | lines 36-41
CITATION: UtilitiesCS/Interfaces/IGlobals/IOlObjects.cs | line 13
CITATION: QuickFiler.Test/Controllers/EfcFormControllerTests.cs | lines 24-34, 299-328
CITATION: QuickFiler.Test/Controllers/QfcItemController.FolderHandlingTests.Part2.cs | line 378
CITATION: QuickFiler.Test/Controllers/QfcItemController.TestSupport.cs | lines 344-352
CITATION: QuickFiler.Test/Controllers/QfcItemController.InitializationTests.cs | line 30
CITATION: QuickFiler.Test/QuickFiler.Test.csproj | Compile item at line 230 for Helper Classes/ViewerQueueStaticWrapperTests.cs (that file: lines 18, 254; the path segment carries a space, so it is cited through its project file)
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | lines 1-13, 91, 283-284, 330-337
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.Projection.ps1 | lines 148-163
CITATION: docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md | lines 306-314
AC-INVENTORY: AC-U1, AC-U2, AC-U3, AC-U4, AC-U5, AC-U6, AC-U7, AC-U8, AC-U9
AC-MAPPING: AC-U1 | IMPLEMENTATION: P4-T6, P4-T7, P4-T1 | TESTS: P3-T4, P3-T2, P5-T5 | EVIDENCE: p3-t7-fail-before.md, p4-t11-pass-after.md, p5-t5-ac-u1-mutation.md, p7-t10-ac-u1.md
AC-MAPPING: AC-U2 | IMPLEMENTATION: P4-T6 | TESTS: P3-T2, P5-T7 | EVIDENCE: p3-t7-fail-before.md, p4-t11-pass-after.md, p5-t7-ac-u2-mutation.md, p7-t11-ac-u2.md
AC-MAPPING: AC-U3 | IMPLEMENTATION: P2-T10, P2-T11, P4-T8, P4-T9, P4-T10 | TESTS: P1-T3, P3-T5, P3-T6, P5-T6, P5-T8 | EVIDENCE: p1-t4-fail-before.md, p3-t7-fail-before.md, p4-t11-pass-after.md, p5-t6-ac-u3-deposit-mutation.md, p5-t8-ac-u3-adoption-mutation.md, p6-t4-popout-ordering.md, p7-t12-ac-u3.md
AC-MAPPING: AC-U4 | IMPLEMENTATION: P4-T7 | TESTS: P1-T2, P5-T1 | EVIDENCE: fail-before-exception.p0-t15.md, p1-t4-fail-before.md, p4-t11-pass-after.md, p5-t1-ac-u4-mutation.md, p7-t13-ac-u4.md
AC-MAPPING: AC-U5 | IMPLEMENTATION: P8-T1 | TESTS: P8-T2 manual runbook | EVIDENCE: p8-t2-ac-u5-manual-verification.md, p8-t3-ac-u5.md
AC-MAPPING: AC-U6 | IMPLEMENTATION: P2-T2, P4-T1, P4-T2, P4-T3 | TESTS: P1-T1, P3-T1, P4-T4, P5-T2, P5-T3 | EVIDENCE: p0-t14-ac-u6-structural-fail-before.md, p6-t1-ac-u6-structural-pass.md, p4-t11-pass-after.md, p5-t2-ac-u6-site1-mutation.md, p5-t3-ac-u6-constant-mutation.md, p7-t14-ac-u6.md
AC-MAPPING: AC-U7 | IMPLEMENTATION: P4-T5, P4-T6 | TESTS: P3-T3, P5-T4 | EVIDENCE: p3-t7-fail-before.md, p4-t11-pass-after.md, p5-t4-ac-u7-mutation.md, p7-t15-ac-u7.md
AC-MAPPING: AC-U8 | IMPLEMENTATION: P2-T1, P2-T2, P2-T3, P2-T4, P2-T5, P1-T1, P1-T2, P1-T3, P3-T1, P3-T2, P3-T3, P3-T5, P3-T6 | TESTS: P6-T3 parity gate, P7-T3 size audit | EVIDENCE: p0-t8-line-counts.md, p6-t3-compile-item-parity.md, p7-t3-file-size-audit.md, p7-t16-ac-u8.md
AC-MAPPING: AC-U9 | IMPLEMENTATION: P2-T3, P2-T4 | TESTS: P6-T2, P7-T3 measurement | EVIDENCE: p6-t2-line-counts-advisory.md, p7-t3-file-size-audit.md, p7-t17-ac-u9.md
UNRESOLVED-GAPS: NONE

The executor's preflight signal is not recorded in this file; it belongs to the atomic-executor return under `DIRECTIVE: PREFLIGHT VALIDATION ONLY`. Rounds 1 and 2 returned `PREFLIGHT: REVISIONS REQUIRED`; every delta from both rounds is applied above. Round 3 of that preflight is outstanding.

