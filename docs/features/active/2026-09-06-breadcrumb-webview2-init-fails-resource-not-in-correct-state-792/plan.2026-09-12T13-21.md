# 2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state (Plan)

- **Issue:** #792
- **Kind:** bug
- **Work Mode:** full-bug
- **Owner:** drmoisan
- **Last Updated:** 2026-09-12T13-21
- **Status:** Draft
- **Version:** 1.3 (preflight revision round 3, prose-only, applied 2026-09-12 in place; see "Preflight round 3 revision record" below. Version 1.2 was preflight revision round 2 applied 2026-09-12 in place; every citation the round touched was re-derived against commit 2405a829d; see "Preflight round 2 revision record" below. Version 1.1 was the adversarial self-review pass, corrections 7 through 13 and the acceptance-condition repairs listed under "Self-review revision record")
- **Acceptance-criteria source:** `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`, section `## Acceptance Criteria`, AC-U1 through AC-U9. No other document supplies acceptance criteria for this item.
- **Base anchor:** commit `2405a829d6afd3b12eb7c228d57158a97cb4e2ca`. Every anchored diff in this plan uses that SHA as its ref operand.
- **Fixed artifact timestamp token:** `2026-09-12T13-21`. Every evidence artifact this plan names uses that token so its path is deterministic and checkable.

## Task counts (mechanical)

Counted as lines matching the task-line form `- [ ] [P#-T#]`.

| Phase | Tasks |
| --- | --- |
| Phase 0 | 11 |
| Phase 1 | 14 |
| Phase 2 | 10 |
| Phase 3 | 17 |
| Phase 4 | 19 |
| Phase 5 | 5 |
| Phase 6 | 20 |
| **Total** | **96** |

## Fail-closed evidence rules

**Evidence location.** Every evidence artifact resolves under `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/<kind>/` where `<kind>` is one of baseline, regression-testing, qa-gates, issue-updates, other. No artifacts directory is used for evidence. That directory does not yet exist in the tree and is created by P0-T1.

**Evidence accounting.** Every evidence-producing task names its artifact path on the task line. A task is not complete without the artifact, and every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:` and `Output Summary:`.

**Projections only.** Per the maintainer decision on issue 671 of 2026-09-11, no test-result file and no raw coverage file is committed. No task in this plan passes a trx logger. The coverage runner writes raw Cobertura into the repository-ignored coverage directory named by its own default output parameter (the ignore rule is the coverage/* entry at line 144 of the root ignore file); the numeric figures are transcribed into the Markdown evidence artifacts and the raw file is left uncommitted.

**Commit-record artifacts.** A task that both commits and records that commit in an artifact cannot satisfy an empty-porcelain clause unless the record is folded into the commit. Every such task (P0-T11, P5-T5, P6-T20) therefore commits, writes its record, stages the record with the same scoped pathspec and amends the commit it just made with `git -C . commit --amend --no-edit`. The amend touches only that task's own commit; no task amends a preceding task's commit.

## Repository discipline this plan encodes

**Bash allowlist.** Only `git`, `gh`, `pwsh`, `poetry run` and the three allowlisted scripts under the .claude lib directory are permitted, and every chained segment of a command line is checked independently. No task states a command of the form that changes directory and then chains a second command. Git is always invoked as `git -C .` from the worktree root. File inspection uses the Read, Grep and Glob tools, never cat, grep, sed, find or head.

**No Python toolchain.** This repository has no scripts/dev_tools tree and no extensions tree. No task passes a coverage argument to a Python runner and no task references pytest.

**C# toolchain order.** Format, then analyzer rebuild, then nullable rebuild, then test. Any failure or any file change restarts the loop at format. `dotnet tool restore` runs once before the first CSharpier invocation. No task adds the solution-wide nullable opt-in property, because no project in this repository carries a nullable element and CI deliberately omits it. No gate uses an incremental build target, because a warm incremental build skips compilation and the gate cannot fail.

**Not SDK-style.** Both project files list every source file explicitly. Every added .cs file gets a bare self-closing Compile element with a backslash-separated project-relative path and no metadata. DependentUpon appears only on Designer and resx pairings in these project files and is never added to a hand-written partial part. Verified anchors, re-derived this pass: the production project file lists the existing partial parts of the breadcrumb router at its lines 291 through 293 and the existing data-model partial pair at its lines 289 and 290, each on its own line with no metadata.

**Tests.** MSTest, Moq and FluentAssertions. No temporary files. No live Outlook. No Thread.Sleep, no Task.Delay and no wall-clock wait anywhere; the retry's delay is an injected delegate whose production default completes synchronously and which tests replace with a no-op. An MSTest Timeout attribute is a hang guard, not a wait, and is used only where the established pump-hosted tests already use one.

**Test file location deviation, recorded explicitly.** The cross-language policy in the repository's general unit-test rule file under the .claude rules directory requires a mirrored tests directory tree. This repository does not use a top-level tests directory for C#: the established convention is a per-area folder inside the test project, for example the Controllers and Viewers folders of the QuickFiler test project. Matching the existing style takes precedence, so every test file this plan creates follows the tree convention QuickFiler.Test/<Area>/<Type>Issue792Tests.cs. This deviation is deliberate and is recorded here so a reviewer does not read it as an oversight.

**Test namespaces, re-derived this pass.** The test project mixes two namespace conventions and each new file follows the file it mirrors: the Viewers tests (WebView2BreadcrumbHostTests.cs line 13) use `QuickFiler.Test.Viewers`; the router queue tests (BreadcrumbBridgeRouterQueueTests.cs line 13) use `QuickFiler.Test.Controllers`; the form-controller, data-model, collection-controller and Efc item-controller tests use `QuickFiler.Controllers.Tests`; the Helper Classes tests (ViewerQueueStaticWrapperTests.cs line 9) use `QuickFiler.Test.HelperClasses`. Each task below names the namespace its file uses.

**500-line ceiling.** Stated per file in the Write Set table below. The two files named by AC-U9 are over the ceiling before this change and remain over it after; their counts must strictly decrease and are recorded as pre-existing debt.

**Staging scope.** Every staging span and every porcelain-status span in this plan carries an explicit pathspec limited to the QuickFiler production project directory, the QuickFiler test project directory, and this item's feature folder. No span in this plan reaches the potential-features directory, because this item's diff does not write there.

**Search-literal defect, verified this run.** Git Bash rewrites a leading-slash argument into a Windows path before git sees it, so a git-side search for a literal beginning with a forward slash returns zero matches and exit 1 against a file that genuinely contains it. No acceptance condition in this plan searches for a literal beginning with a forward slash. Structural searches are stated as Grep tool invocations with an explicit pattern, an explicit path and an expected match count, which avoids shell quoting entirely.

**Line-count instrument.** Every line count in this plan is the Grep tool run with the pattern `^` in count output mode against the single file. Verified this pass: that instrument returns 101 for `QuickFiler/Helper Classes/EfcViewerQueue.cs`, which is the value the Write Set table records.

## Execution-phase notes to RECORD, not to act on

1. The sandbox refuses `pwsh` under Agent worktree isolation, so the execution child for this item must be launched non-isolated.
2. The `atomic-executor` agent definition grants no msbuild and no dotnet Bash permission. Every msbuild step, every `dotnet tool run csharpier` step and every coverage-runner step in this plan must therefore be executed by an agent that holds those permissions. The plan names that handoff rather than assuming the executor can build; see P6-T19.

## Manual, human-executed steps

**M1. Outlook must be closed before any rebuild.** Close Outlook through its own normal exit path. Never end the process. A killed process leaves the build output locked and MSBuild fails with a file-lock error or produces a stale assembly. This is a precondition of every msbuild task in this plan and is captured as its own task at P0-T3 and again at P6-T1.

**M2. AC-U5 is a manual live-Outlook verification.** It is executed by a person against a live Outlook session following the runbook already written in the user story for this item. It is not an automated gate and no acceptance condition in this plan asserts a command exit code for it. Its evidence is a Markdown record carrying the operator, the timestamp, the observed outcome per entry point, and a pass or fail verdict. See P6-T9.

## Corrections to the input documents, established against the tree this pass

1. **The AC-U4 combobox half is already fully pinned, not merely incidentally true.** The research record and the spec's test strategy both state that `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` "today asserts only that the method logs once and does not fault". That is false. The method body at lines 300 through 328 of that file already installs a boundary sink counter at line 310 and asserts the count equals 1 at lines 321 through 327. Only the method **name** is stale. The work for that half is therefore a rename, not an assertion addition, and is planned as such at P3-T16. This correction is load-bearing: a plan that added a sink assertion there would have added a duplicate.
2. **`QuickFiler.Test/Controllers/EfcFormControllerTests.cs` is 485 lines**, so it has 15 lines of headroom. A rename is line-neutral and is the only edit that fits without a split. The sibling part of the same partial class is 490 lines and is not edited by this change.
3. **The host seam does not carry the initialization member.** The narrow breadcrumb host interface in the QuickFiler Viewers folder declares only navigate, post, the inbound event and the initialized flag; it has no initialization member. That interface is not in the binding Write Set, so the AC-U1 seam must not be an interface widening. It is an injectable initialization delegate on the form controller instead. See P3-T3.
4. **The Efc item controller holds no core-initializer field.** Its environment creation calls the SDK factory directly. Routing it through the mockable seam therefore requires introducing the seam property in the new partial, which P1-T11 does.
5. **All four other target types are already declared partial** (the collection controller, the home controller, the item controller and the data model). Only the Efc form controller is not; P2-T1 adds the modifier.
6. **The breadcrumb document has no browsing-storage dependence** on the evidence available at authoring time: a case-insensitive search of the QuickFiler Resources breadcrumb HTML file and of the UtilitiesCS folder-breadcrumb renderer and document assets for the six storage tokens returns zero matches. P1-T1 re-establishes this as a recorded verification.
7. **The Efc incognito constant is pinned by a test outside the Write Set.** QuickFiler.Test/Controllers/EfcItemControllerTests.cs lines 372 through 378 declare `IncognitoArgument_IsAsciiDoubleHyphenIncognitoWithTrailingSpace`, which reads the constant `EfcItemController.IncognitoArgument`. Deleting the constant, as the spec's proposed fix implies, would break the test project's compilation from a file this change may not edit. The constant is therefore moved into the new partial as a forwarding constant whose value is the contract's constant, not deleted. See P1-T11. This is also why the D1 fallback branch halts rather than inverting: an empty argument value would fail that pinned test.
8. **The item-controller interface has two implementers outside the Write Set.** QuickFiler.Test/Helper Classes/QfcThemeHelperTests.cs line 337 declares a private fake implementing the interface, and QuickFiler/Legacy/QfcController.cs line 20 implements it (that legacy file is not listed in the production project file and is not compiled, but the test fake is). Adding a member to the interface would break the test build from a file this change may not edit. The pop-out carry therefore pattern-matches the item group's controller to the concrete `QfcItemController` type and reads an internal accessor declared there; the interface file is in the Write Set of neither document (preflight round 2 moved it from the spec's Write Set into its prose exclusion paragraph) and is not written by this plan. See P4-T1, P4-T2 and P4-T6.
9. **Two target classes carry a class-level coverage exclusion.** `QuickFiler/Controllers/EfcItemController.cs` line 25 and `QuickFiler/Controllers/QfcCollectionController.cs` line 21 each carry a class-level exclude-from-code-coverage attribute. An attribute on one partial declaration applies to the whole type, so the two new partials this plan creates for those types produce no class element in the Cobertura document and no per-file rate can be demanded for them. P6-T7 records those two rows as unmeasurable by pre-existing exclusion and names the tests that stand as their evidence, rather than demanding a rate that cannot exist.
10. **Two non-code occurrences would defeat the one-owner gate.** `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` line 60 is a commented-out alternative construction that still contains the options-constructor token, and `QuickFiler/Viewers/WebView2BreadcrumbHost.cs` line 18 is a summary-comment line that names the shared folder literal. P1-T10 removes the commented-out line and P1-T9 rewords the summary line, so the P1-T13 gate counts only real sites.
11. **The data model is constructible in a test without Outlook, and its token is settable only through the constructor.** `QuickFiler/Controllers/EfcDataModel.cs` lines 48 through 75: with a null mail the constructor calls a selection probe that catches every exception and returns null, so a loose globals mock and a null mail construct the model with no resolver. `Token` (line 164) has a protected setter assigned from the constructor argument (line 58). Passing an already-cancelled token makes every `Task.Run(..., Token)` in the moved folder-handler initialization return a cancelled task without running its delegate, which is what makes the construction path observable without Outlook. A carried predictor is constructible as `new FolderPredictor(globals.Object)` with a loose globals mock, the pattern already used at line 378 of QuickFiler.Test/Controllers/QfcItemController.FolderHandlingTests.Part2.cs. See P4-T9.
12. **`UiThread.Dispatcher` throws when uninitialized, and the fixture's parked dispatcher never pumps.** UtilitiesCS/Threading/UiThread.cs lines 251 through 269 throw an invalid-operation exception when no dispatcher was captured. The test project's `UiThreadDispatcherFixture` (QuickFiler.Test/Controllers/QfcItemController.UiThreadDispatcherFixture.cs) exposes `Exchange` at line 55, `CompareExchange` at line 70 and `BeginTransactionAsync` at line 122 with a transaction type at line 220 carrying `Install` at line 242; its `EnsureDispatcher` at line 99 parks a dispatcher that never runs a frame (lines 145 through 147), so a blocking `Invoke` against it would never return. `QfcItemControllerTestSupport.StartRunningDispatcher` (QuickFiler.Test/Controllers/QfcItemController.TestSupport.cs line 251) starts a pumping STA dispatcher and `ShutdownDispatcher` (line 277) stops it. P4-T13 uses the transaction plus the running dispatcher, never the parked one. A dispatcher priority is not observable from inside the invoked action, so the spec's "forwards the priority" assertion is replaced by an assertion on the reset member, which is the second production site the change edits.
13. **The two discard tests that pin an empty buffer or a silent discard already pass against the defect-preserving stub, and one form-controller test already passes against the single-attempt body.** P3-T11's fail-before enumeration names exactly which methods must fail and which pin preserved behaviour, instead of demanding that every method fail.

## Design decisions settled here, so the executor does not decide them

**D1. Options direction.** All three environment creations converge on the additional browser argument `--incognito ` owned by the new contract file. Fallback branch, stated explicitly: if P1-T1 finds any browsing-storage dependence in the breadcrumb document, execution halts at P1-T1 and reports remediation-required, because the pinned test named in correction 7 lives outside the Write Set and an inverted direction cannot be landed within this plan's file set. Correction 6 records that the tree at the base anchor takes the primary branch.

**D2. AC-U1 seam shape.** The form controller gains an injectable initialization delegate and an injectable retry-delay delegate. The delegate default must not be written as a property initializer capturing the host field, because a field initializer cannot reference a non-static instance field and that spelling is a compile error. The default is supplied lazily from a backing field in the property getter instead.

**D3. AC-U1 error-state delivery.** The failed host cannot render, so the error banner is composed and rendered by the router and then delivered by the same two-state mechanism the router already uses in its private document delivery member (QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs lines 168 through 180): navigated immediately when the host reports initialized, retained as the pending document otherwise so a later successful initialization delivers it. The user-visible surfacing in the never-initialized case is the existing modeless fault notice reached through the form controller's boundary reporter, which AC-U4 covers. The banner row uses the existing banner prefix constant `BannerPrefix` (value `====`, UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs line 19) and banner rows are already non-selectable, so the error row cannot be chosen as a folder. No new WinForms control is added.

**D4. AC-U7 discard primitive.** The outbound queue gains a discard member that clears the buffer and returns the discarded count. The router's failure notification flushes the buffer when the host is initialized and discards it otherwise. Either way the pending count is zero afterwards.

**D5. AC-U3 carry typing and carry read.** The carried folder handler is typed as the existing folder-search-handler interface, which lives in the UtilitiesCS root namespace that the edited QuickFiler files already import, so no new using directive is required. The data model's concrete predictor property is not retyped and the UtilitiesCS interface is not widened. Adoption happens only when no explicit folder list was supplied and the carried instance matches the concrete predictor type by pattern match. The carried mail-item helper is held in a private carry field on the new data-model partial and preferred over the resolver-derived value inside the folder-handler initialization; the resolver property is not retyped and its protected setter is not touched. On the producing side, per correction 8, the pop-out reads the mail-item helper through the interface member `ItemHelper` that already exists (QuickFiler/Interfaces/IQfcItemController.cs line 41) and reads the folder handler through a new internal accessor on the concrete `QfcItemController`, reached by pattern-matching the item group's `ItemController` (typed as the interface at QuickFiler/Controllers/QfcItemGroup.cs line 39) to the concrete type.

**D6. AC-U3 pop-out decomposition.** The two pop-out members keep their current ordering: the carry is read before the group is removed, and the home controller is created after. The read and the creation are each extracted into their own internal member so both are unit-testable without WinForms, and a wiring-sensitive structural gate asserts that each extracted member name occurs exactly three times in the new partial, once as a declaration and once in each of the two pop-out members. Comments in that file must not repeat either member name, so the count stays a wiring count.

**D7. AC-U8 versus AC-U9.** AC-U8's ceiling is verified for every file in the Write Set except the two files AC-U9 names as pre-existing debt. For those two the gate is that the post-change count is strictly lower than the Phase 0 baseline count, which is the only reading consistent with the spec's own out-of-scope statement that a full split of either is out of scope and their remaining size is debt this change neither introduces nor resolves.

## Write Set and expected resulting line counts

The source Write Set below lists the 31 paths of the `## Write Set` section of the spec, element for element; the spec and this table agree on those 31 written paths (the spec was brought into agreement in preflight round 2, when the item-controller interface was moved from its Write Set into its plain-prose exclusion paragraph per correction 8). Evidence artifacts and the spec file itself are additionally written by this plan and are listed after the table. One read-only control path is measured but not written and is stated after the table in plain text so the blast-radius extractor does not harvest it.

| Path | Before | Expected after | Ceiling status |
| --- | --- | --- | --- |
| `QuickFiler/Viewers/WebView2EnvironmentContract.cs` | new | 40 to 100 | under |
| `QuickFiler/Viewers/WebView2BreadcrumbHost.cs` | 368 | 360 to 372 | under |
| `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` | 467 | 458 to 472 | under |
| `QuickFiler/Controllers/EfcItemController.cs` | 1121 | 1060 to 1085 | AC-U9 debt, must strictly decrease |
| `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs` | new | 55 to 110 | under |
| `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` | 407 | 430 to 475 | under |
| `QuickFiler/Controllers/BreadcrumbOutboundQueue.cs` | 67 | 76 to 95 | under |
| `QuickFiler/Controllers/EfcFormController.cs` | 1321 | 250 to 290 | under |
| `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` | new | 180 to 260 | under |
| `QuickFiler/Controllers/EfcFormController.SetupAndProperties.cs` | new | 215 to 270 | under |
| `QuickFiler/Controllers/EfcFormController.EventHandlers.cs` | new | 355 to 410 | under |
| `QuickFiler/Controllers/EfcFormController.Actions.cs` | new | 160 to 210 | under |
| `QuickFiler/Controllers/EfcFormController.Helpers.cs` | new | 205 to 260 | under |
| `QuickFiler/Controllers/QfcCollectionController.cs` | 2329 | 2295 to 2315 | AC-U9 debt, must strictly decrease |
| `QuickFiler/Controllers/QfcCollectionController.PopOut.cs` | new | 80 to 150 | under |
| `QuickFiler/Controllers/EfcHomeController.cs` | 447 | 452 to 480 | under |
| `QuickFiler/Controllers/EfcDataModel.cs` | 499 | 460 to 470 | under |
| `QuickFiler/Controllers/EfcDataModel.Carry.cs` | new | 70 to 140 | under |
| `QuickFiler/Controllers/QfcItemController.cs` | 334 | 337 to 352 | under |
| `QuickFiler/Helper Classes/EfcViewerQueue.cs` | 101 | 99 to 106 | under |
| `QuickFiler/QuickFiler.csproj` | project file | plus 9 Compile elements | project file, not size-gated |
| `QuickFiler.Test/Viewers/WebView2BreadcrumbHostIssue792Tests.cs` | new | 80 to 180 | under |
| `QuickFiler.Test/Viewers/WebView2EnvironmentContractTests.cs` | new | 70 to 160 | under |
| `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue792Tests.cs` | new | 140 to 270 | under |
| `QuickFiler.Test/Controllers/BreadcrumbOutboundQueueIssue792Tests.cs` | new | 80 to 160 | under |
| `QuickFiler.Test/Controllers/EfcFormControllerIssue792Tests.cs` | new | 160 to 320 | under |
| `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` | 485 | 485 | under, rename is line-neutral |
| `QuickFiler.Test/Controllers/QfcCollectionControllerIssue792PopOutTests.cs` | new | 120 to 250 | under |
| `QuickFiler.Test/Controllers/EfcDataModelIssue792CarryTests.cs` | new | 120 to 260 | under |
| `QuickFiler.Test/Helper Classes/EfcViewerQueueIssue792Tests.cs` | new | 70 to 160 | under |
| `QuickFiler.Test/QuickFiler.Test.csproj` | project file | plus 8 Compile elements | project file, not size-gated |

Two of the paths above contain a space in the directory name, under Helper Classes in each project. Those are the real tracked paths and are reproduced exactly.

Control row, plain text, NOT written (correction 8): QuickFiler/Interfaces/IQfcItemController.cs, 113 lines at the base anchor, expected 113 and unchanged after. It is not in the Write Set of either document. P0-T9 and P5-T2 measure it as one additional labelled control row so P4-T1 has a recorded baseline to compare against.

Additionally written by this plan: `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md` (AC check-off marks only, in Phase 6) and the evidence artifacts named on the task lines below.

## Files deliberately not modified

Stated in prose with no backticks, because the blast-radius extractor has no notion of polarity and would harvest an excluded path as though it were written.

The UtilitiesCS folder-search-handler interface and the folder predictor that implements it are not modified: the carry is typed as the existing interface and adopted by pattern match on the concrete predictor type, so neither needs widening and no change leaves the QuickFiler project. The QuickFiler item-controller interface in the Interfaces folder is not modified, and neither document lists it as written: two implementers outside the Write Set would fail to compile if a member were added, so the carry read pattern-matches the concrete item controller instead. The Efc item-controller test file in the test project's Controllers folder is not modified: its pinned constant test keeps compiling because the constant is forwarded rather than deleted. The narrow breadcrumb web-host interface in the QuickFiler Viewers folder is not modified: the AC-U1 seam is an injectable delegate on the form controller, not an interface member. The core-initializer interface in the QuickFiler Viewers folder is not modified: the Efc item controller adopts it as a consumer only. The breadcrumb UI dispatcher is not modified: research assessed its boundary check as self-consistent and a sibling item explicitly scoped it out. The breadcrumb row builder, the breadcrumb HTML renderer and the breadcrumb document assets in UtilitiesCS are not modified: the error state reuses the existing banner-prefix convention and the existing rendering path. The breadcrumb HTML resource under the QuickFiler Resources folder is not modified: it is read in P1-T1 and nothing else. The router's Selection and Arrows partial parts are not modified: the failure notification is added to the main router file next to the success notification. The item viewer files owned by a sibling item are not modified. The sibling part of the form-controller test class is not modified. The collection-controller test support helper and the item-controller test support and dispatcher fixture files are not modified; they are consumed as-is.

## Known contention

Sibling issue 645 edits the same collection-controller file at three date and time formatting call sites, which are disjoint from the pop-out members this item moves, and it also edits the QuickFiler production project file. A merge on the project file's item-group ordering is expected and accepted. New Compile elements are appended adjacent to their existing neighbours as bare self-closing elements with no metadata, which minimises the conflict surface. Sibling issue 781 is scoped to the item viewer files this item does not write; the overlap is conceptual only and this item adopts the owner-thread-identity idiom that sibling ratified rather than inventing a third convention.

## Self-review revision record (2026-09-12, version 1.1)

Applied in place during the adversarial self-review pass. Each entry names the defect and the repair so a later reader does not re-derive it.

1. P1-T11 deleted a constant pinned by a test outside the Write Set; now forwarded (correction 7).
2. P4-T1 widened an interface with two out-of-Write-Set implementers; now a read-only non-widening verification, with the accessor moved to the concrete type (correction 8).
3. P6-T7 demanded a 0.90 rate for two files that cannot have a class element (correction 9); rows re-specified per file, with the exempt member and the pre-existing moved lambdas named by the planner rather than chosen by the executor.
4. P1-T13's "exactly 1" gates were defeated by a commented-out construction and a summary-comment folder name (correction 10); both occurrences are now removed by P1-T10 and P1-T9 and the gate is stated as "zero outside the contract file".
5. P2-T7's pattern also matched the parent's own Compile entry (six hits, not five); pattern narrowed to the five new names.
6. P0-T9 and P5-T3 said ten new files; the Write Set creates seventeen.
7. P2-T8's sum window (1400 to 1500) was below the arithmetic: 1316 retained source lines plus five file headers of roughly 10 to 14 lines each lands near 1370 to 1390; window is now 1360 to 1460.
8. Several lower bounds on new-file line counts were above the arithmetic of the moved spans plus a header; loosened.
9. P3-T11 demanded that all fifteen Phase 3 tests fail before the fix; two discard tests and one form-controller test pass against the stubs (correction 13); enumeration corrected. P4-T15 likewise.
10. P3-T14's `TryReportBoundaryFault` floor of 2 was already met by the two moved call sites (pre-split lines 1127 and 1270); floor raised to 3.
11. P1-T4's directory-existence test was environment-dependent; replaced by a rooted-path assertion. A forwarding-constant test was added so the Efc site's argument agreement is asserted by a test, not only by a structural gate.
12. P4-T13 relied on an uninitialized dispatcher and an unobservable priority (correction 12); re-specified over the running-dispatcher transaction.
13. P0-T11, P5-T5 and the final commit task (P6-T19 in version 1.1, P6-T20 since round 2) wrote their commit record after committing and then demanded empty porcelain; the amend step was added.
14. P6-T6 demanded exit 0 and zero failures unconditionally; the coverage runner asserts a 0.80 floor and exits non-zero below it, and a pre-existing failing test would make the clause unsatisfiable; both are now baseline-relative, and P0-T8 records the failing-test names the comparison needs.
15. P5-T1 and P6-T2 formatted two whole directories, which would sweep pre-existing drift outside the Write Set into the diff and break the "exactly the written paths" clause; both now format the explicit Write Set file list, and P6-T3 is baseline-relative.
16. Test namespaces were stated as `QuickFiler.Viewers.Tests` and `QuickFiler.Controllers.Tests` uniformly; corrected per file to the mirrored sibling's namespace.
17. P4-T11's fifth test was a cleanup assertion, not a test; replaced by a non-concrete-controller case.
18. The D1 fallback inverted the argument direction downstream; it now halts, because the pinned test outside the Write Set cannot absorb an inverted value.

## Preflight round 2 revision record (2026-09-12, version 1.2)

Applied in place from the executor's round-2 delta plus one orchestrator item. Every tree fact each entry relies on was re-derived against the base anchor in this pass.

1. P0-T4 assumed a bootstrapped worktree; the worktree holds no repo-local SDK directory and no packages directory, and the PackageReference-only restore is a no-op against packages.config projects. Now runs the repo-local SDK installer, the tool restore and a packages.config-aware restore, and gates on observed installs rather than on exit codes alone.
2. P4-T1's bare accessor-token gate was already false at the base anchor because the pre-existing member `LoadFolderHandlerAsync` at interface line 77 contains the token; the gate now searches the typed declaration and pins the pre-existing member's count.
3. P6-T7's Breadcrumb rule gated moved members that no unit test can reach; the gate is now over the members this change writes, and every moved member is listed as information with its reason.
4. P6-T10 through P6-T18 appended evidence citations to acceptance-criterion lines, which the acceptance-criteria-tracking skill forbids; the only spec edit is now the marker flip, and citations go to an ac-status-summary artifact under evidence/issue-updates.
5. The final commit wrote evidence after itself; the handoff record is now P6-T19 and the final commit is P6-T20, with captures taken before the artifact write.
6. Four excluded paths were backticked (an Efc item-controller test file, the UiThread file, the router Selection part, and the coverage ignore glob); backticks removed.
7. P1-T6's second test asserted against the empty string, which the SDK's no-argument constructor does not produce (it leaves the property null); the test now asserts not-null-or-empty.
8. P1-T8 lacked the rebuild precondition that P3-T11 and P4-T15 carry; added.
9. P0-T11's listing clause omitted the five requirement documents untracked at the base anchor; the clause is now exact.
10. P3-T3 did not state how the single attempt reaches the injected delegate, and its match floor counted a lower-camel backing field that a case-sensitive pattern does not match; wiring stated, floor lowered to 2.
11. P4-T9's second test did not state its fail-before mechanism; stated.
12. P2-T10 demanded an exact passed-count equality that pre-existing flaky or newly passing tests could break; now a floor.
13. P6-T7's repository-rate clause had no formula; now stated over `lines-covered` and `lines-valid` with the 0.80 floor.
14. P6-T7's exclusion rows asserted no class element exists, which a compiler-generated closure type could falsify; now records whether one exists and gates no rate on it.
15. P6-T20's projections-only Grep had no concrete target; now the record artifact, whose prose must not carry either literal.
16. P1-T13's hit decomposition was wrong (count right); corrected to the two live sites plus the two commented-out lines, with the target-typed ViewerSetup site noted as a non-match.
17. P4-T11 and P4-T13 mutate process-wide statics under a class-parallel runsettings; both classes now carry `[DoNotParallelize]`, and P4-T13's timeout constant is stated as a value.
18. Orchestrator item: spec.md's `## Write Set` listed 32 paths including the interface this plan does not write; the spec now lists 31, the interface sits in its prose exclusion paragraph, the spec's summary and function-impact sentences no longer name the interface as written, and this plan's table and count statements agree on 31.

## Preflight round 3 revision record (2026-09-12, version 1.3)

Prose-only. The executor confirmed every round-2 delta landed and re-verified every tree citation, task count and gate; the sole remaining findings were prose residuals of the 32-to-31 Write Set reduction. No task identifier, task count, phase heading, count statement, gate value or command changed in this round.

1. Spec regression-test list said seven new test files; the write set names eight. Corrected.
2. Spec backward-compatibility paragraph still described one interface change adding a read-only member; replaced with the non-widening statement (internal get-only property on the concrete item controller, reached by pattern match).
3. Spec compatibility note still said "the added interface member"; now "the added internal accessor on the concrete item controller".
4. Spec pop-out data-flow sentence still said both carried values are read through the interface; now states the mail item helper comes through the existing interface member and the folder handler through the internal accessor on the concrete type, null when the group's controller is not the concrete type.
5. Correction 8 still said the interface file is listed in the spec's Write Set; now states it is in the Write Set of neither document.
6. The files-not-modified paragraph still said "although the spec lists it"; now "neither document lists it as written".
7. The spec CITATION locator said Write Set 308-341; now 308-344 with the 31 paths at 310-340 and the exclusion paragraph at 344.
8. P6-T7's Breadcrumb row is advisory by design: every P3-T9 test substitutes both delegates, so the two lazy-default getter expressions are excluded from the gated span and recorded as information with their hit counts.

---

### Phase 0 — Baseline Capture and Policy Reads

- [ ] [P0-T1] Read the repository policy documents in the mandated order and record the read in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/phase0-instructions-read.2026-09-12T13-21.md`, creating the evidence directory tree as part of this task.
  - Read, in this order and in full, using the Read tool: CLAUDE.md at the repository root; then .claude/rules/general-code-change.md; then .claude/rules/general-unit-test.md; then .claude/rules/csharp.md; then .claude/rules/quality-tiers.md; then .claude/rules/tonality.md; then .claude/rules/plan-acceptance-gates.md. All seven exist at the base anchor.
  - Acceptance: the artifact exists and contains a `Timestamp:` field, a `Policy Order:` field listing those seven paths in that order, and a `Files Read:` list whose entries are exactly those seven paths.

- [ ] [P0-T2] Capture the base anchor and confirm it, recording the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/base-anchor.2026-09-12T13-21.md`.
  - Run `git -C . rev-parse HEAD`.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE: 0` and an `Output Summary:` whose recorded SHA is exactly `2405a829d6afd3b12eb7c228d57158a97cb4e2ca`. If the recorded SHA differs, stop and report a base-anchor mismatch rather than continuing, because every anchored diff in this plan names that SHA as its ref operand.

- [ ] [P0-T3] MANUAL HUMAN STEP. Close Outlook through its normal exit path before any msbuild task in this phase runs, and record the confirmation in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/outlook-closed-precondition.2026-09-12T13-21.md`.
  - Runbook: use Outlook's own File then Exit path, or the window close button, and wait until the process is gone from the task list of its own accord. Do not end the process from the task manager and do not use any process-kill command. A killed process leaves the debug build output locked, and MSBuild then fails with a file-lock error or silently links a stale assembly.
  - Acceptance: the artifact records the operator name, the ISO-8601 local timestamp of the confirmed exit, and the sentence that Outlook was closed through its normal exit path and was not killed. This task has no exit code and no automated gate.

- [ ] [P0-T4] Bootstrap the toolchain in this worktree and record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/toolchain-bootstrap.2026-09-12T13-21.md`.
  - This worktree holds no repo-local SDK directory and no packages directory at the base anchor (verified this pass with the Glob tool: no dotnet executable under the repo-local SDK directory and no entry under a packages directory). global.json pins SDK 8.0.205 with the latestFeature roll-forward and search paths of the repo-local SDK directory then the host, so `dotnet tool restore` exits non-zero with global.json's own error message unless a compatible SDK is present. `msbuild TaskMaster.sln /t:Restore` handles PackageReference only; against this repository's packages.config projects it exits 0, prints that none of the projects contain packages to restore, and installs nothing.
  - Run, in this order, from the worktree root under PowerShell 7: scripts/vscode/Install-RepoDotNetSdk.ps1 (skip only when the repo-local SDK executable already exists, and record which branch was taken; the installer itself prints that the SDK is already installed and returns when its own marker directory, the sdk-slash-version directory under the install directory, exists at its lines 56 through 61); then `dotnet tool restore`; then `msbuild TaskMaster.sln /t:Restore /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:RestorePackagesConfig=true`.
  - Acceptance: the artifact records all three commands, an `EXIT_CODE:` for each, and an `Output Summary:` that states the CSharpier version the manifest pinned as reported by the tool restore output, the number of packages the restore installed as printed by the restore output, and the Glob tool observations that the repo-local SDK executable exists and that the packages directory contains at least one package directory. Every exit code must be 0, and a restore that prints the nothing-to-do line or installs zero packages stops the phase. Both writes land on ignored or restore-output paths only (the root ignore file's dot-dotnet wildcard directory entry at its line 350 covers the SDK directory, and its bracket-class packages entry at line 191 covers the packages directory), so neither widens any anchored-diff footprint.

- [ ] [P0-T5] Capture the CSharpier baseline into `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/format-check.2026-09-12T13-21.md`.
  - Run `dotnet tool run csharpier check .`. This is the read-only verify form; the write-mode form is not used in Phase 0, because a formatter that repairs pre-existing drift before the baseline is taken turns the baseline into a blanket waiver.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:` and an `Output Summary:` carrying the count of files CSharpier reported as needing formatting and the repository-relative path of every such file under a `Reported set:` heading, or the literal statement that it reported none. The exit code is recorded as observed and is not asserted to be 0, because a solution-wide pre-existing drift is a property of the tree rather than of this change. P6-T3 compares against the `Reported set:` heading.

- [ ] [P0-T6] Capture the analyzer-build baseline into `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/analyzer-build.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:` and an `Output Summary:` carrying the warning count and the error count exactly as the build summary reports them, plus the deduplicated set of diagnostic identifiers observed. Record the counts as the two figures printed on the summary lines; do not infer them. The exit code is recorded as observed and is not asserted to be 0.

- [ ] [P0-T7] Capture the nullable-build baseline into `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/nullable-build.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`. Do not add the solution-wide nullable opt-in property.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:` and an `Output Summary:` carrying the error count from the build summary and the deduplicated set of diagnostic identifiers observed. The exit code is recorded as observed and is not asserted to be 0.

- [ ] [P0-T8] Capture the test and coverage baseline into `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/test-coverage.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Run the repository coverage runner at scripts/vscode/Invoke-MSTestWithCoverage.ps1 under PowerShell 7 with an explicit search root of the current directory and the Debug configuration. The runner already appends the isolation switch, the CLI runsettings and the live-Outlook test-category exclusion (its line 76), discovers assemblies relative to the search root so an agent worktree is not self-excluded (its line 301), post-processes the document so class elements are merged by source file name, and writes the processed Cobertura to its default output path under the repository-ignored coverage directory. Do not pass a trx logger. The runner asserts a 0.80 repository line-rate floor after writing the document and exits non-zero below it, so the document exists even when the exit code is non-zero.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:` as observed, and an `Output Summary:` carrying five numeric figures read with the Read tool from the root element of the runner's Cobertura output: `lines-valid`, `lines-covered`, `line-rate`, `branches-valid` and `branch-rate`; the total, passed, failed and skipped test counts; and, under a `Failed set:` heading, the fully qualified name of every failed test, or the literal `none`. The figures are read from the XML attributes rather than from console text, so the acceptance does not depend on what the runner prints. The raw Cobertura file is left uncommitted. If the run does not complete, the phase stops and the item is reported as remediation-required with the runner's last output quoted; the runner is not edited, because it is outside the Write Set.

- [ ] [P0-T9] Capture the baseline line count of every one of the 31 Write Set paths, plus the one control path, into `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/write-set-line-counts.2026-09-12T13-21.md`.
  - Measure each existing file with the Grep tool using the pattern `^` in count output mode against that single file. Record one row per Write Set path. For the seventeen paths that do not yet exist, record the literal value `absent`. Then record one additional row, labelled `control (not written)`, for the item-controller interface named in the control-row paragraph under the Write Set table.
  - Acceptance: the artifact contains exactly 31 Write Set rows plus exactly one control row, 32 rows in all; the Write Set rows for the existing production files record 368 for the breadcrumb host, 467 for the item-controller viewer-setup part, 1121 for the Efc item controller, 407 for the router, 67 for the outbound queue, 1321 for the Efc form controller, 2329 for the collection controller, 447 for the home controller, 499 for the data model, 334 for the QuickFiler item controller and 101 for the Efc viewer queue; the row for `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` records 485; exactly seventeen rows record `absent`; the control row records 113. AC-U8 and AC-U9 are judged against this artifact, and P4-T1 reads the control row.

- [ ] [P0-T10] Capture the baseline Compile-item inventory of `QuickFiler/QuickFiler.csproj` and `QuickFiler.Test/QuickFiler.Test.csproj` into `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/compile-item-inventory.2026-09-12T13-21.md`.
  - Measure with the Grep tool using the pattern `<Compile Include=` in count output mode against each project file separately.
  - Acceptance: the artifact records one integer per project file, labelled with that project file's repository-relative path. AC-U8's parity check in P5-T3 subtracts these two integers from the post-change counts and requires the differences to be exactly 9 and 8 respectively.

- [ ] [P0-T11] Commit the Phase 0 evidence with a scoped pathspec, and record the commit in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/baseline/phase0-commit.2026-09-12T13-21.md`.
  - Run `git -C . add -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792`, then `git -C . commit -m "#792 Phase 0 baseline evidence"`. Then write the record artifact carrying `Command:`, `EXIT_CODE:` and the commit subject, run the same `git -C . add` span again, and run `git -C . commit --amend --no-edit` so the record is inside the commit it describes.
  - Acceptance: `git -C . status --porcelain --untracked-files=all -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines, and `git -C . diff --name-only 2405a829d6afd3b12eb7c228d57158a97cb4e2ca -- docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` lists exactly the ten baseline artifact paths produced by P0-T1 through P0-T10, this task's own record artifact, and the five requirement documents that were untracked at the base anchor (issue.md, spec.md, user-story.md, the research record and this plan file), and every path it lists lies inside that folder. The staging span and the porcelain span in this task are what make the name-listing diff able to see files this phase created.

### Phase 1 — Prerequisite Verification and the Shared Environment Contract

- [ ] [P1-T1] PREREQUISITE. Verify that the breadcrumb document has no persisted-browsing-storage dependence, and record the verdict in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/other/breadcrumb-storage-dependence.2026-09-12T13-21.md`.
  - Read-only inspection, no production file is touched. Run the Grep tool, case-insensitive, with the pattern `localStorage|sessionStorage|indexedDB|document\.cookie|openDatabase|serviceWorker`, in count output mode, against each of these read-only targets separately: the breadcrumb HTML resource under the QuickFiler Resources folder named FolderBreadcrumb.html; and the UtilitiesCS OutlookObjects Folder directory, which holds the breadcrumb HTML renderer, the breadcrumb document assets and the breadcrumb row builder that together generate the delivered document. Both targets exist at the base anchor.
  - Acceptance: the artifact records the exact pattern, both target descriptions, both match counts, and a `Verdict:` line reading either `NO-STORAGE-DEPENDENCE` or `STORAGE-DEPENDENCE-FOUND`.
  - Branch, stated here so the executor does not decide it: on `NO-STORAGE-DEPENDENCE`, proceed with D1 and author the contract constant as the four-character incognito switch followed by a single trailing space, that is the value the existing Efc constant already carries. On `STORAGE-DEPENDENCE-FOUND`, stop: record the verdict, do not begin P1-T2, and report the item as remediation-required per the D1 fallback, because the pinned test named in correction 7 cannot be edited within this Write Set.

- [ ] [P1-T2] Create the shared owner `QuickFiler/Viewers/WebView2EnvironmentContract.cs`.
  - Contents: an internal static class in the QuickFiler.Viewers namespace holding an internal const string named `AdditionalBrowserArguments` carrying the value chosen by P1-T1; an internal static method named `ResolveUserDataFolder` returning the combination of the local application data special folder and the folder name `WindowsFormsWebView2`, which is the expression currently duplicated at three sites; and an internal static method named `CreateOptions` returning a new environment-options instance constructed with `AdditionalBrowserArguments`. Include XML documentation stating that this type is the single owner of the option set and that a second construction site anywhere in the production tree reintroduces the shared-browser-process options conflict; the documentation must not repeat the folder-name literal or the options-constructor token, so that the P1-T13 counts stay site counts.
  - Acceptance: the file exists, its measured line count is between 40 and 100, the Grep tool run with the pattern `AdditionalBrowserArguments` against this file returns at least 2 matches, one for the constant declaration and one for its use inside `CreateOptions`, the Grep tool run with the pattern `WindowsFormsWebView2` against this file returns exactly 1 match, and the Grep tool run with the pattern `new CoreWebView2EnvironmentOptions` against this file returns exactly 1 match.

- [ ] [P1-T3] Add the Compile element for the contract to `QuickFiler/QuickFiler.csproj`.
  - Insert a bare self-closing Compile element with the project-relative backslash path for the new Viewers file, adjacent to the existing Viewers block that already lists the breadcrumb host at line 423 and the core initializer at line 421. No metadata, no DependentUpon.
  - Acceptance: the Grep tool run with the pattern `Viewers\\WebView2EnvironmentContract\.cs` against `QuickFiler/QuickFiler.csproj` returns exactly 1 match, and the matched line contains neither `DependentUpon` nor `SubType`.

- [ ] [P1-T4] Create the contract unit tests in `QuickFiler.Test/Viewers/WebView2EnvironmentContractTests.cs`.
  - MSTest class in the `QuickFiler.Test.Viewers` namespace using FluentAssertions, with a using directive for QuickFiler.Controllers so the internal Efc item controller is reachable (the production assembly already declares internals visible to the test assembly). Tests: `AdditionalBrowserArguments_MatchesTheSettledOptionsDirection`, asserting the constant equals the value recorded by P1-T1; `ResolveUserDataFolder_EndsWithTheSharedFolderName`, asserting the returned path ends with the shared folder name and that two successive calls return equal strings; `ResolveUserDataFolder_IsRootedUnderLocalApplicationData`, asserting the returned path starts with the local-application-data special folder path, which is deterministic on every machine, unlike a directory-existence check; `CreateOptions_CarriesTheContractArguments`, asserting the returned options instance exposes additional browser arguments equal to the constant; and `EfcItemController_IncognitoArgument_ForwardsTheContractConstant`, asserting `EfcItemController.IncognitoArgument` equals `WebView2EnvironmentContract.AdditionalBrowserArguments`, which is the test-level assertion that the Efc item-body site agrees with the contract.
  - Acceptance: the file exists, its measured line count is between 70 and 160, and it declares exactly five test methods with those five names.

- [ ] [P1-T5] Add the Compile element for the contract tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Insert a bare self-closing Compile element with the project-relative backslash path, adjacent to the existing Viewers block that lists the breadcrumb host tests at lines 213 and 214.
  - Acceptance: the Grep tool run with the pattern `Viewers\\WebView2EnvironmentContractTests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P1-T6] [expect-fail] Create the breadcrumb-host environment regression tests in `QuickFiler.Test/Viewers/WebView2BreadcrumbHostIssue792Tests.cs`.
  - MSTest class in the `QuickFiler.Test.Viewers` namespace. Follow the existing pattern in the breadcrumb host tests file, which constructs a real WebView2 control on the WinForms pump host (its lines 36 through 38) and drives initialization through a Moq double of the core-initializer seam whose environment-creation member is set up at its line 404. Test: `InitializeAsync_HandsTheContractFolderAndArgumentsToEnvironmentCreation`, which captures the two arguments passed to the seam's environment-creation member and asserts the folder equals the contract's resolved folder and the options instance exposes additional browser arguments equal to the contract constant. Second test: `InitializeAsync_DoesNotPassTheNoArgumentOptionsDefault`, asserting the captured options instance's additional browser arguments are neither null nor the empty string (FluentAssertions `NotBeNullOrEmpty`), because the SDK's no-argument constructor leaves that property null and an assertion against the empty string alone would pass before the fix.
  - This task is expected to fail because the host still constructs its own no-argument options instance at line 250. That is the defect, preserved until P1-T9.
  - Acceptance: the file exists, its measured line count is between 80 and 180, and it declares exactly two test methods with those two names.

- [ ] [P1-T7] Add the Compile element for the breadcrumb-host regression tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the Grep tool run with the pattern `Viewers\\WebView2BreadcrumbHostIssue792Tests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P1-T8] [expect-fail] Run the two new Viewers test classes and record the fail-before evidence in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/regression-testing/p1-fail-before.2026-09-12T13-21.md`.
  - Rebuild with the analyzer command first so the run is a runtime red rather than a compile red; precondition: P0-T3 is complete. Then run vstest against the QuickFiler test assembly with a test-case filter selecting the fully qualified names of the two test classes created in P1-T4 and P1-T6, with the isolation switch and without a trx logger.
  - Acceptance: the artifact records `Command:`, the observed `EXIT_CODE:`, an `ExpectedExitCode:` field set to the non-zero value the run actually produced, and an `Output Summary:` naming which of the seven test methods failed and quoting each failure's assertion message. The two methods declared in P1-T6 must be among the failures; the five methods declared in P1-T4 must pass, because the contract itself already exists and the Efc constant already carries the contract's value. A run in which the entire assembly fails to load is not valid fail-before evidence and requires a rebuild before the run is repeated.

- [ ] [P1-T9] Repoint the breadcrumb host's environment creation in `QuickFiler/Viewers/WebView2BreadcrumbHost.cs` at lines 246 through 250 to the shared contract, and reword the summary comment at line 18.
  - Replace the four-line local folder computation and the local options construction with one call to the contract's folder resolution and one call to the contract's options factory. This is the behaviour-changing line of the whole fix: the breadcrumb environment acquires the same additional browser arguments as the two item-body environments and therefore stops conflicting with the shared browser process. Reword the summary-comment line 18 so it says the user-data folder is resolved by the shared environment contract and no longer names the folder literal.
  - Acceptance: the Grep tool run with the pattern `WebView2EnvironmentContract` against this file returns at least 2 matches, the Grep tool run with the pattern `new CoreWebView2EnvironmentOptions` against this file returns 0 matches, and the Grep tool run with the pattern `WindowsFormsWebView2` against this file returns 0 matches. The measured line count is between 360 and 372.

- [ ] [P1-T10] Repoint the QuickFiler item-body environment creation in `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs` at lines 55 through 61 to the shared contract.
  - Replace the three-line local-application-data and combine expression at lines 55 through 58 with one contract call, delete the commented-out alternative construction at line 60 because its text carries the options-constructor token, and replace the target-typed options construction at line 61 with the contract's options factory. Add a using directive for QuickFiler.Viewers only if the file does not already import it.
  - Acceptance: the Grep tool run with the pattern `WebView2EnvironmentContract` against this file returns at least 2 matches, the Grep tool run with the pattern `new\("--incognito ` against this file returns 0 matches, the Grep tool run with the pattern `new CoreWebView2EnvironmentOptions` against this file returns 0 matches, the Grep tool run with the pattern `WindowsFormsWebView2` against this file returns 0 matches, and the measured line count is between 458 and 472.

- [ ] [P1-T11] Move the Efc item-body environment creation out of `QuickFiler/Controllers/EfcItemController.cs` into the new partial `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs`, repointed at the contract and routed through the mockable core-initializer seam.
  - Delete lines 167 through 176, the argument constant and its documentation, and lines 178 through 211, the initialization member, from the parent file; line 186 inside that span is the commented-out alternative construction and is deleted with it. Create the new partial in the QuickFiler.Controllers namespace declaring `internal partial class EfcItemController`, containing: the forwarding constant `internal const string IncognitoArgument = WebView2EnvironmentContract.AdditionalBrowserArguments;` with a comment stating it is retained because the test `IncognitoArgument_IsAsciiDoubleHyphenIncognitoWithTrailingSpace` in the existing Efc item-controller test file pins it (correction 7); an internal settable core-initializer seam property whose backing field defaults lazily to the production core-initializer adapter; and the moved initialization member rewritten to obtain its folder and options from the contract and to reach the SDK through the seam's environment-creation and ensure-core members rather than through the SDK factory directly. Preserve the existing await on the item viewer's UI synchronization context and the existing continuation ordering. The class-level coverage exclusion at line 25 of the parent applies to this partial too, so this member's coverage is not measurable (correction 9).
  - Acceptance: the Grep tool run with the pattern `IncognitoArgument` against `QuickFiler/Controllers/EfcItemController.cs` returns 0 matches and against the new file returns at least 1 match; the Grep tool run with the pattern `InitializeWebViewAsync` against `QuickFiler/Controllers/EfcItemController.cs` returns 0 matches; the Grep tool run with the pattern `CoreWebView2Environment\.CreateAsync` against the new file returns 0 matches; the Grep tool run with the pattern `WindowsFormsWebView2` against the new file returns 0 matches; the new file's measured line count is between 55 and 110; and the parent file's measured line count is strictly less than the 1121 recorded in the P0-T9 artifact and lies between 1060 and 1085.

- [ ] [P1-T12] Add the Compile element for the Efc item-controller environment partial to `QuickFiler/QuickFiler.csproj`.
  - Insert adjacent to the existing Efc item-controller block that lists the parent at line 303 and the fault-boundary part at line 304.
  - Acceptance: the Grep tool run with the pattern `Controllers\\EfcItemController\.WebViewEnvironment\.cs` against `QuickFiler/QuickFiler.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P1-T13] Verify the one-owner property across the production tree and record it in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/one-owner-parity.2026-09-12T13-21.md`.
  - Run the Grep tool with the pattern `new CoreWebView2EnvironmentOptions` in content output mode over the QuickFiler production project directory. Run it a second time with the pattern `WindowsFormsWebView2` over the same directory. At the base anchor the first pattern has four hits (the Efc site at line 187 and the host site at line 250, plus the two commented-out lines at EfcItemController line 186 and ViewerSetup line 60; the ViewerSetup site at line 61 is target-typed and does not match this pattern) and the second has four (the three sites plus the host summary comment at line 18); P1-T9, P1-T10 and P1-T11 remove all of them and P1-T2 adds one of each inside the contract.
  - Acceptance: the artifact records both patterns and both result sets; for each pattern, every match lies inside `QuickFiler/Viewers/WebView2EnvironmentContract.cs` and the count of matches outside that file is 0, and the count inside it is exactly 1. Any match outside the contract file means a site kept its own option set or its own folder string and the phase is not complete. This gate is wiring-sensitive: a site that stops calling the contract reintroduces its own construction and produces an outside match.

- [ ] [P1-T14] Rebuild and re-run the Phase 1 test classes green, then commit, recording the run in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/regression-testing/p1-pass-after.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Rebuild with the analyzer command, then re-run the same filtered vstest invocation as P1-T8.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE: 0` and an `Output Summary:` stating that all seven test methods from P1-T4 and P1-T6 passed and that zero failed. Then run `git -C . add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` and `git -C . commit -m "#792 Phase 1 shared WebView2 environment contract"`; afterwards `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines and `git -C . diff --name-only 2405a829d6afd3b12eb7c228d57158a97cb4e2ca -- QuickFiler QuickFiler.Test` lists exactly the nine paths this phase wrote: five production .cs paths (the contract, the host, the viewer-setup part, the Efc item controller and its new partial), the production project file, two test .cs paths (the two new test files) and the test project file.

### Phase 2 — Behaviour-Neutral Split of the Efc Form Controller

- [ ] [P2-T1] Add the partial modifier to the type declaration at line 26 of `QuickFiler/Controllers/EfcFormController.cs` and remove nothing else in this task.
  - Acceptance: the Grep tool run with the pattern `internal partial class EfcFormController` against this file returns exactly 1 match, and the Grep tool run with the pattern `internal class EfcFormController` against this file returns 0 matches.

- [ ] [P2-T2] Pure-move the setup, cleanup and public-property regions out of `QuickFiler/Controllers/EfcFormController.cs` into the new partial `QuickFiler/Controllers/EfcFormController.SetupAndProperties.cs`.
  - Move source lines 266 through 479, that is the Setup and Cleanup Methods region delimited at lines 266 and 365 and the Public Properties region delimited at lines 367 and 479, verbatim. Add only the namespace, the using directives the moved members require, and the partial class declaration. Change no member body, no signature and no accessibility.
  - Acceptance: the new file's measured line count is between 215 and 270; the Grep tool run with the pattern `Setup and Cleanup Methods` against `QuickFiler/Controllers/EfcFormController.cs` returns 0 matches and against the new file returns exactly 2 matches, being the region opening and closing directives, both of which carry that text at the base anchor.

- [ ] [P2-T3] Pure-move the event-handler region out of `QuickFiler/Controllers/EfcFormController.cs` into the new partial `QuickFiler/Controllers/EfcFormController.EventHandlers.cs`.
  - Move source lines 481 through 834, the Event Handlers region, verbatim. The closing directive at line 834 carries no text, so only the opening directive matches the pattern below.
  - Acceptance: the new file's measured line count is between 355 and 410; the Grep tool run with the pattern `region Event Handlers` against `QuickFiler/Controllers/EfcFormController.cs` returns 0 matches and against the new file returns exactly 1 match.

- [ ] [P2-T4] Pure-move the major-actions region out of `QuickFiler/Controllers/EfcFormController.cs` into the new partial `QuickFiler/Controllers/EfcFormController.Actions.cs`.
  - Move source lines 836 through 990, the Major Actions region, verbatim.
  - Acceptance: the new file's measured line count is between 160 and 210; the Grep tool run with the pattern `region Major Actions` against `QuickFiler/Controllers/EfcFormController.cs` returns 0 matches and against the new file returns exactly 1 match.

- [ ] [P2-T5] Pure-move the breadcrumb members out of `QuickFiler/Controllers/EfcFormController.cs` into the new partial `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`.
  - Move source lines 1044 through 1129, that is the breadcrumb control configuration with its explanatory comment (1044 through 1068), the host-initialization member (1070 through 1082), the two row-binding members (1084 through 1109) and the async bind boundary (1111 through 1129); and source lines 1250 through 1287, that is the folder-combobox population member (1250 through 1272) and the three selection-classification members (1274 through 1287). Move them verbatim in this task; no behaviour changes here. The two host fields declared at lines 242 and 243 stay in the parent file with the other fields.
  - Acceptance: the new file's measured line count is between 125 and 175 at the end of this task (Phase 3 then grows it to between 180 and 260); the Grep tool run with the pattern `InitializeBreadcrumbHostAsync` against `QuickFiler/Controllers/EfcFormController.cs` returns 0 matches and against the new file returns exactly 2 matches, being the declaration and the fire-and-forget call inside the configuration member.

- [ ] [P2-T6] Pure-move the remaining helper-region members out of `QuickFiler/Controllers/EfcFormController.cs` into the new partial `QuickFiler/Controllers/EfcFormController.Helpers.cs`.
  - Move source lines 992 through 1043, source lines 1131 through 1249 and source lines 1289 through 1319, that is everything in the Helper Methods region that P2-T5 did not take plus the expansion-style member that follows the region. The region opening directive at line 992 and the region closing directive at line 1289 travel with this part. Line 1320 closes the class and line 1321 closes the namespace; both stay in the parent file, as does everything from line 1 through line 265.
  - Acceptance: the new file's measured line count is between 205 and 260; the Grep tool run with the pattern `RunKbdGuardedAsync` against `QuickFiler/Controllers/EfcFormController.cs` returns 0 matches and against the new file returns at least 1 match; the parent file's measured line count is between 250 and 290.

- [ ] [P2-T7] Add the five Compile elements for the new form-controller partials to `QuickFiler/QuickFiler.csproj`.
  - Insert five bare self-closing Compile elements adjacent to the existing entry for the parent at line 296, using project-relative backslash paths and no metadata, following the convention already visible in the two collection-controller entries at lines 314 and 315 and the two Efc item-controller entries at lines 303 and 304.
  - Acceptance: the Grep tool run with the pattern `Controllers\\EfcFormController\.(SetupAndProperties|EventHandlers|Actions|Breadcrumb|Helpers)\.cs` against `QuickFiler/QuickFiler.csproj` returns exactly 5 matches, the Grep tool run with the pattern `Controllers\\EfcFormController\.cs` against the same file still returns exactly 1 match, and none of the six matched lines contains `DependentUpon`.

- [ ] [P2-T8] Record the six form-controller file sizes in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p2-split-line-counts.2026-09-12T13-21.md`.
  - Measure each of `QuickFiler/Controllers/EfcFormController.cs`, `QuickFiler/Controllers/EfcFormController.SetupAndProperties.cs`, `QuickFiler/Controllers/EfcFormController.EventHandlers.cs`, `QuickFiler/Controllers/EfcFormController.Actions.cs`, `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs` and `QuickFiler/Controllers/EfcFormController.Helpers.cs` with the Grep tool in count output mode.
  - Acceptance: the artifact records six rows; every recorded value is at most 500; the sum of the six values lies between 1360 and 1460. The arithmetic: the moves retain 1316 of the original 1321 lines (the five blank separator lines at 480, 835, 991, 1130 and 1288 are not moved) and add five file headers of a namespace, a class declaration, braces and using directives, so the sum is 1316 plus roughly 50 to 70; this confirms the split moved members rather than dropping them. This is an interim measurement; the binding ceiling audit runs after the final format pass at P5-T2.

- [ ] [P2-T9] Rebuild under both gates and confirm the split introduced no new diagnostic, recording the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p2-build-parity.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Run the analyzer rebuild command, then the nullable rebuild command, both exactly as written in the toolchain section.
  - Acceptance: the artifact records both commands with their exit codes and their deduplicated diagnostic-identifier sets. The gate passes when each command's exit code is 0, or when it equals the exit code recorded for that same command in the P0-T6 and P0-T7 artifacts and the diagnostic-identifier set recorded here is a subset of the set recorded there. A new diagnostic identifier fails this gate.

- [ ] [P2-T10] Confirm behavioural neutrality by test-count identity and commit, recording the run in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p2-behaviour-neutral.2026-09-12T13-21.md`.
  - Run the same coverage-runner invocation as P0-T8.
  - Acceptance: the artifact records the total, passed, failed and skipped counts and the `Failed set:`; the passed count is at least the P0-T8 passed count plus the seven test methods Phase 1 added, the `Failed set:` is a subset of the P0-T8 `Failed set:`, and the skipped count equals the P0-T8 skipped count. Then run `git -C . add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` and `git -C . commit -m "#792 Phase 2 behaviour-neutral EfcFormController split"`; afterwards `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines and `git -C . diff --name-status 2405a829d6afd3b12eb7c228d57158a97cb4e2ca -- QuickFiler/Controllers` lists the five new form-controller partials as added.

### Phase 3 — AC-U1, AC-U2, AC-U4 and AC-U7

- [ ] [P3-T1] Add the defect-preserving discard seam to `QuickFiler/Controllers/BreadcrumbOutboundQueue.cs`.
  - Add `internal int DiscardPending()` whose body returns 0 without clearing the buffer, with an XML comment stating that the body is deliberately defect-preserving until P3-T12 so that the regression test fails at its assertion rather than at assembly load. Change nothing else in the file.
  - Acceptance: the Grep tool run with the pattern `DiscardPending` against this file returns exactly 1 match; the file's measured line count is between 72 and 85.

- [ ] [P3-T2] Add the defect-preserving failure-notification seam to `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`.
  - Add `public void NotifyInitializationFailed(System.Exception error)` immediately after the existing success notification that spans lines 320 through 329, with an empty body and an XML comment stating that the empty body reproduces today's swallow behaviour and is replaced at P3-T13. Do not modify the success notification.
  - Acceptance: the Grep tool run with the pattern `NotifyInitializationFailed` against this file returns exactly 1 match, and the Grep tool run with the pattern `NotifyCoreInitialized` against this file still returns exactly 1 match, which is the count at the base anchor.

- [ ] [P3-T3] Add the defect-preserving initialization seams to `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`.
  - Change the moved host-initialization member's accessibility from private to internal. Add an internal attempt-limit constant named `BreadcrumbInitializationAttemptLimit` with the value 3. Add an internal settable initialization delegate property named `BreadcrumbInitializer` taking a synchronization context and returning a task, and an internal settable retry-delay delegate property named `BreadcrumbRetryDelay` taking the attempt ordinal and returning a task. Per D2, both defaults are supplied lazily from a private backing field inside the property getter, never as a property initializer, because a field initializer cannot reference the non-static host field and that spelling does not compile. The initialization delegate's lazy default forwards to the host field's initialization member; the retry-delay delegate's lazy default returns a completed task, so production adds no wall-clock wait. In this task the member body still makes exactly one attempt and still logs on failure, which preserves the defect. The single attempt is made through `BreadcrumbInitializer`, passing the form viewer's synchronization context read through a null-safe local, so the P3-T9 red is an assertion red rather than a swallowed null dereference of the unset viewer field.
  - Acceptance: the Grep tool run with the pattern `internal async Task InitializeBreadcrumbHostAsync` against this file returns exactly 1 match; the Grep tool run with the pattern `BreadcrumbInitializer` against this file returns at least 2 matches (the property declaration and the call site inside the member; the backing field is lower-camel and does not match this case-sensitive pattern); and the Grep tool run with the pattern `BreadcrumbInitializer \{ get; set; \} =` against this file returns 0 matches, which is what proves the property-initializer spelling was not used.

- [ ] [P3-T4] Rebuild the solution so the seams compile before any red test is written, recording the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p3-seam-build.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Run the analyzer rebuild command.
  - Acceptance: the artifact records the command, the exit code and the diagnostic-identifier set; the gate passes on the same baseline-relative rule stated in P2-T9. This task exists because a missing member reddens the whole test assembly at load, which would make the later fail-before evidence non-specific.

- [ ] [P3-T5] [expect-fail] Create the outbound-queue regression tests in `QuickFiler.Test/Controllers/BreadcrumbOutboundQueueIssue792Tests.cs`.
  - MSTest class in the `QuickFiler.Test.Controllers` namespace driving the queue over a Moq double of the narrow host seam with a settable initialized flag, which is the pattern the router queue tests use at their lines 38 through 48. Tests: `DiscardPending_WhenBufferHoldsPayloads_ReturnsTheDiscardedCount`; `DiscardPending_WhenBufferHoldsPayloads_LeavesPendingCountZero`; `DiscardPending_WhenBufferIsEmpty_ReturnsZeroAndPostsNothing`; and `DiscardPending_DoesNotPostToTheHost`, asserting the host's post member is never invoked by a discard. The first two fail against the P3-T1 stub; the last two pass against it and pin preserved behaviour (correction 13).
  - Acceptance: the file exists, its measured line count is between 80 and 160, and it declares exactly four test methods with those four names.

- [ ] [P3-T6] Add the Compile element for the outbound-queue regression tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the Grep tool run with the pattern `Controllers\\BreadcrumbOutboundQueueIssue792Tests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P3-T7] [expect-fail] Create the router failure-notification regression tests in `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue792Tests.cs`.
  - MSTest class in the `QuickFiler.Test.Controllers` namespace, constructing the real router over a Moq host double with a settable initialized flag, a Moq hierarchy provider, the real codec, the real renderer and a real outbound queue, which is the arrangement at lines 34 through 74 of the router queue tests. Tests: `NotifyInitializationFailed_WhenHostIsInitialized_NavigatesAnErrorBannerDocument`, asserting the navigate member received a document containing `====`, the banner prefix; `NotifyInitializationFailed_WhenHostIsNotInitialized_RetainsTheErrorDocumentForLaterDelivery`, asserting that a subsequent success notification navigates a document containing the banner prefix; `NotifyInitializationFailed_ReplacesAnEarlierStashRatherThanDroppingIt`, asserting that after a stash produced by a theme application while uninitialized and then a failure notification, a subsequent success notification navigates exactly once and the navigated document contains the banner prefix; `NotifyInitializationFailed_LeavesTheOutboundQueuePendingCountZero`, which first drives one outbound payload into the queue through the router while the host reports uninitialized so the pending count is 1 before the notification, then asserts it is 0 afterwards; and `NotifyCoreInitialized_AfterASuccessfulInitialization_StillNavigatesAnEarlierStash`, which pins that adding the failure path did not remove the success path. The first four fail against the P3-T2 empty body; the fifth passes against it.
  - Acceptance: the file exists, its measured line count is between 140 and 270, and it declares exactly five test methods with those five names.

- [ ] [P3-T8] Add the Compile element for the router regression tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the Grep tool run with the pattern `Controllers\\BreadcrumbBridgeRouterIssue792Tests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P3-T9] [expect-fail] Create the form-controller retry and boundary regression tests in `QuickFiler.Test/Controllers/EfcFormControllerIssue792Tests.cs`.
  - Author the file as a further part of the existing `EfcFormControllerTests` partial class in the `QuickFiler.Controllers.Tests` namespace so the established `CreateMinimalController` (line 24 of the 485-line file) and `SetPrivateField` (line 467) helpers are reused rather than duplicated. Do not repeat the test-class attribute; it is applied once at line 16 of the 485-line file, and the sibling part records this convention in its summary at lines 20 through 22 above its own partial declaration at line 24. Tests: `InitializeBreadcrumbHostAsync_WhenInitializationAlwaysFaults_AttemptsExactlyThreeTimes`; `InitializeBreadcrumbHostAsync_WhenInitializationFaultsThenSucceeds_StopsRetryingAfterTheSuccess`; `InitializeBreadcrumbHostAsync_WhenAllAttemptsFault_ReportsThroughTheBoundarySinkExactlyOnce`, using the existing `BoundaryErrorSink` seam the way line 310 of the 485-line file does; `InitializeBreadcrumbHostAsync_WhenAllAttemptsFault_NotifiesTheRouterOfTheFailure`, which sets the private router field through the helper the way line 124 does, to a real router over a Moq host reporting uninitialized with one payload already queued, and asserts the outbound queue's pending count is zero afterwards; `InitializeBreadcrumbHostAsync_WhenCanceled_DoesNotReportAFault`; and `InitializeBreadcrumbHostAsync_UsesTheInjectedRetryDelayAndNeverAWallClockWait`, asserting the injected delay delegate was invoked once per failed attempt other than the last. Substitute the initialization delegate and the retry-delay delegate; never call Thread.Sleep, Task.Delay or any wall-clock wait. Five of the six fail against the P3-T3 single-attempt body; the cancellation test passes against it, because the single-attempt body already swallows without reporting (correction 13).
  - Acceptance: the file exists, its measured line count is between 160 and 320, it declares exactly six test methods with those six names, and the Grep tool run with the pattern `Task\.Delay|Thread\.Sleep` against this file returns 0 matches.

- [ ] [P3-T10] Add the Compile element for the form-controller regression tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Insert adjacent to the existing entries for the two form-controller test parts at lines 124 and 125.
  - Acceptance: the Grep tool run with the pattern `Controllers\\EfcFormControllerIssue792Tests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P3-T11] [expect-fail] Rebuild, run the three new Phase 3 test classes, and record the fail-before evidence in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/regression-testing/p3-fail-before.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Rebuild with the analyzer command first so the run is a runtime red, then run vstest filtered to the two new class names plus the form-controller test class with the isolation switch and no trx logger; the form-controller class run includes the pre-existing tests of that partial class, whose outcomes are recorded but not gated here.
  - Acceptance: the artifact records `Command:`, the observed `EXIT_CODE:`, an `ExpectedExitCode:` field set to the non-zero value this run actually produced, and an `Output Summary:` naming each of the fifteen test methods declared in P3-T5, P3-T7 and P3-T9 and its outcome. Exactly these eleven must be recorded as failing: the two discard-count tests (`DiscardPending_WhenBufferHoldsPayloads_ReturnsTheDiscardedCount`, `DiscardPending_WhenBufferHoldsPayloads_LeavesPendingCountZero`), the four failure-notification tests of P3-T7, and the five form-controller tests of P3-T9 other than the cancellation test. The remaining four (the two empty-buffer and no-post discard tests, the router success-path test and the cancellation test) are recorded with their actual outcome, which is expected to be a pass. A run in which the assembly fails to load is not valid fail-before evidence.

- [ ] [P3-T12] Implement the discard behaviour in `QuickFiler/Controllers/BreadcrumbOutboundQueue.cs`.
  - Replace the defect-preserving body so the member records the current buffer count, clears the buffer without posting to the host, and returns the recorded count. Update the XML comment to describe the real contract and to state that discard is the failure-path terminal state while the existing flush member remains the success-path terminal state.
  - Acceptance: the four test methods declared in P3-T5 pass. The file's measured line count is between 76 and 95.

- [ ] [P3-T13] Implement the failure notification in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`.
  - Per D3 and D4: compose a single banner row through the router's existing row-builder field (line 36) by calling its `BuildRow` member (UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs line 76) with a fixed row identifier, a presented text that begins with `BannerPrefix` followed by a short operator-facing message naming the breadcrumb initialization failure, a null ancestor chain and an empty probability dictionary; replace the router's row collection with that single row; render a document through the existing renderer; navigate it when the host reports initialized and otherwise retain it as the pending document, replacing any earlier stash rather than dropping it; then flush the outbound queue when the host reports initialized and otherwise call the queue's discard member. The existing success notification is not modified, so a later successful initialization still navigates whatever document is pending.
  - Acceptance: the five test methods declared in P3-T7 pass. The Grep tool run with the pattern `DiscardPending` against this file returns exactly 1 match, which is the wiring-sensitive proof that the router reaches the discard member; the XML comment must not repeat that name. The file's measured line count is between 430 and 475.

- [ ] [P3-T14] Implement the bounded deterministic retry and the boundary reporting in `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`.
  - Rewrite the internal initialization member as a bounded loop over the attempt-limit constant. Each attempt awaits the injection point with the form viewer's synchronization context read through a null-safe local. A cancellation is classified as non-fault, logged at debug level, and stops the loop without reporting and without notifying the router, mirroring the classification the bind boundary already uses at lines 1121 through 1124 of the pre-split parent file. Any other failure awaits the injected retry delay and retries until the limit is reached. On exhaustion the member calls the controller's existing boundary fault reporter with a message naming the breadcrumb WebView2 initialization failure, and then calls the router's failure notification through a null-conditional access so a minimally constructed controller does not throw. Correct the stale comment that states the queue is released when initialization fires: on failure it is released by the failure notification.
  - Acceptance: the six test methods declared in P3-T9 pass. The Grep tool run with the pattern `TryReportBoundaryFault` against this file returns at least 3 matches, being the new initialization call site plus the two moved call sites that were at pre-split lines 1127 and 1270, which is why a floor of 2 would already be met before this task. The Grep tool run with the pattern `logger\.Error\(\$"Breadcrumb WebView2 initialization failed` against this file returns 0 matches, which proves the bare logger call that was at pre-split line 1080 was replaced rather than supplemented.

- [ ] [P3-T15] Wire the failure notification next to the existing success subscription in `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`.
  - The success subscription is the lambda that was at line 1064 of the pre-split parent file. Leave it unchanged. The failure path is reached from the initialization member rather than from an event, because the SDK completion handler is structurally unreachable from a unit test; add an explanatory comment stating that, and stating that the retry and the surfacing deliberately do not live in the SDK completion handler. The comment must not repeat the name of the failure-notification member, so the count below stays a wiring count.
  - Acceptance: the Grep tool run with the pattern `NotifyCoreInitialized` against this file returns exactly 1 match and the Grep tool run with the pattern `NotifyInitializationFailed` against this file returns exactly 1 match. The file's measured line count is between 180 and 260.

- [ ] [P3-T16] Correct the stale test name in `QuickFiler.Test/Controllers/EfcFormControllerTests.cs` so the AC-U4 combobox half is pinned by name as well as by assertion.
  - Per the correction recorded above, the body at lines 300 through 328 already installs a boundary sink counter and already asserts the count equals 1; only the name still says the method merely logs. Rename the method at line 300 to `PopulateFolderCombobox_WhenDataModelFaults_ReportsThroughBoundarySinkOnce`. Change no other line in the file, so the edit is line-neutral.
  - Acceptance: the Grep tool run with the pattern `PopulateFolderCombobox_WhenDataModelFaults_ReportsThroughBoundarySinkOnce` against this file returns exactly 1 match; the Grep tool run with the pattern `PopulateFolderCombobox_WhenDataModelFaults_LogsOnceAndDoesNotFault` against the QuickFiler.Test project directory returns 0 matches; and the file's measured line count is exactly 485, which is the value recorded in the P0-T9 artifact.

- [ ] [P3-T17] Rebuild, run the affected test classes green, and commit, recording the run in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/regression-testing/p3-pass-after.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Rebuild with the analyzer command, then run vstest filtered to the two new Phase 3 test classes plus the form-controller test class, which now includes the P3-T9 part and the renamed method.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE: 0` and an `Output Summary:` stating that all fifteen Phase 3 test methods passed, that the renamed method passed, and that zero tests failed. Then run `git -C . add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` and `git -C . commit -m "#792 Phase 3 bounded retry, failure notification, queue drain"`; afterwards `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines.

### Phase 4 — AC-U3 Pop-out Carry and UI-thread Construction

- [ ] [P4-T1] Verify, read-only, that the item-controller interface is not widened, and record it in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/other/iqfcitemcontroller-not-widened.2026-09-12T13-21.md`.
  - Per correction 8, the interface at QuickFiler/Interfaces/IQfcItemController.cs has an implementer in the test project outside the Write Set (the private fake at line 337 of the QfcThemeHelperTests file under the test project's Helper Classes folder) and a legacy implementer that is not compiled. The artifact records both implementer locations, the statement that the folder handler is therefore read from the concrete item controller by pattern match (D5), and the interface file's measured line count.
  - Acceptance: the Grep tool run with the pattern `IFolderSearchHandler FolderHandler` against QuickFiler/Interfaces/IQfcItemController.cs returns 0 matches; the Grep tool run with the pattern `LoadFolderHandlerAsync` against that file still returns exactly 1 match (line 77 at the base anchor, which is the pre-existing member whose name contains the accessor token and is why a bare token search is not the gate); and its measured line count is exactly 113, the P0-T9 control-row value. This gate fails if any task widens the interface.

- [ ] [P4-T2] Add the internal folder-handler accessor to `QuickFiler/Controllers/QfcItemController.cs` over the existing private field.
  - Add `internal IFolderSearchHandler FolderHandler` as a get-only property returning the private handler field declared at line 41, placed next to the existing top-score accessor at line 265, with an XML comment stating that it exists so the pop-out can carry an already-initialized handler, that it is internal because the interface cannot be widened, and that it does not expose a setter. The handler interface lives in the UtilitiesCS root namespace this file already uses (the field at line 41 is declared with it).
  - Acceptance: the Grep tool run with the pattern `internal IFolderSearchHandler FolderHandler` against this file returns exactly 1 match; the file's measured line count is between 337 and 352.

- [ ] [P4-T3] Pure-move the folder-handler initialization out of `QuickFiler/Controllers/EfcDataModel.cs` into the new partial `QuickFiler/Controllers/EfcDataModel.Carry.cs`, with the carry deposited but not yet adopted.
  - Delete lines 188 through 221 from the parent file. Create the new partial declaring `internal partial class EfcDataModel`, which is the accessibility the parent declares at its line 21, holding a private carried-handler field typed as the folder-search-handler interface, a private carried-helper field typed as the mail-item helper, an internal deposit member named `DepositCarry` taking both and assigning both, and the moved initialization member verbatim. In this task the moved member does not consult either carry field, which preserves today's behaviour.
  - Acceptance: the Grep tool run with the pattern `InitFolderHandlerAsync` against `QuickFiler/Controllers/EfcDataModel.cs` returns 0 matches and against the new file returns exactly 1 match; the parent file's measured line count is between 460 and 470; the new file's measured line count is between 50 and 100 at the end of this task.

- [ ] [P4-T4] Add the Compile element for the data-model carry partial to `QuickFiler/QuickFiler.csproj`.
  - Insert adjacent to the existing data-model entries at lines 289 and 290, which already demonstrate the bare self-closing partial-part convention for this exact type.
  - Acceptance: the Grep tool run with the pattern `Controllers\\EfcDataModel\.Carry\.cs` against `QuickFiler/QuickFiler.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P4-T5] Add the trailing optional carry parameters and the deposit call to `QuickFiler/Controllers/EfcHomeController.cs`.
  - Add two trailing optional parameters, a folder-search-handler and a mail-item helper, both defaulting to null, to the public constructor at lines 47 through 52 and to the internal constructor at lines 54 through 59, and forward them from the public constructor's chained call at line 52. Inside the internal constructor, call the data model's deposit member immediately after the data-model assignment that ends at line 71 and strictly before the form-controller factory call at line 85, because the form controller's initialization fires a fire-and-forget folder-combobox population that would otherwise overwrite the deposited handler. Use a null-conditional access on the data model so the existing null-data-model paths are unchanged. Every existing call site compiles unchanged because both parameters are trailing and optional.
  - Acceptance: the Grep tool run with the pattern `DepositCarry` against this file returns exactly 1 match; the file's measured line count is between 452 and 480.

- [ ] [P4-T6] Pure-move the two pop-out members out of `QuickFiler/Controllers/QfcCollectionController.cs` into the new partial `QuickFiler/Controllers/QfcCollectionController.PopOut.cs`, adding the seams but not yet the carry values.
  - Delete lines 710 through 735 from the parent file. Create the new partial declaring `public partial class QfcCollectionController`, the accessibility the parent declares at its line 22, holding: an internal sealed carry-holder class with three get-only properties, being the mail item, the folder handler and the mail-item helper, set through its constructor, written as a plain class because this target framework has no support for the init accessor or for record types; an internal instance member named `ReadPopOutCarry` taking the one-based selection and returning that holder, whose body in this task returns the group's mail item with both carry values null, preserving today's behaviour; an internal static settable factory property named `PopOutHomeControllerFactory` whose lazy default constructs the home controller with the carry arguments; an internal instance member named `CreatePopOutHomeController` taking the holder and returning the home controller through the factory; and the two moved pop-out members, each reading the carry before removing the group and creating the home controller after, exactly as today's ordering does. Per D6, no comment in this file repeats the two extracted member names. The class-level coverage exclusion at line 21 of the parent applies to this partial too (correction 9).
  - Acceptance: the Grep tool run with the pattern `PopOutControlGroup` against `QuickFiler/Controllers/QfcCollectionController.cs` returns 0 matches (the base anchor has exactly the two declarations at lines 710 and 722 and no other reference in that file); the Grep tool run with the pattern `ReadPopOutCarry` against the new file returns exactly 3 matches and the same for `CreatePopOutHomeController`, which is the wiring-sensitive proof that both pop-out members route through the extracted members; the new file's measured line count is between 75 and 130 at the end of this task; the parent file's measured line count is strictly less than the 2329 recorded in the P0-T9 artifact and lies between 2295 and 2315.

- [ ] [P4-T7] Add the Compile element for the pop-out partial to `QuickFiler/QuickFiler.csproj`.
  - Insert adjacent to the existing collection-controller entries at lines 314 and 315.
  - Acceptance: the Grep tool run with the pattern `Controllers\\QfcCollectionController\.PopOut\.cs` against `QuickFiler/QuickFiler.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P4-T8] Rebuild so the Phase 4 seams compile before any red test is written, recording the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p4-seam-build.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Run the analyzer rebuild command.
  - Acceptance: the artifact records the command, the exit code and the diagnostic-identifier set; the gate passes on the same baseline-relative rule stated in P2-T9.

- [ ] [P4-T9] [expect-fail] Create the data-model carry regression tests in `QuickFiler.Test/Controllers/EfcDataModelIssue792CarryTests.cs`.
  - MSTest class in the `QuickFiler.Controllers.Tests` namespace. Per correction 11, construct the data model through its public constructor with a loose Moq globals, a null mail, a fresh token source and an already-cancelled token (`new CancellationToken(true)`), which is Outlook-free and makes every `Task.Run(..., Token)` on the construction path complete as cancelled without running its delegate. Construct the carried predictor as `new FolderPredictor(globals.Object)`, the pattern at line 378 of the QfcItemController folder-handling tests, Part2. Deposit the carry through the internal deposit member, then drive the folder-handler initialization. Tests: `InitFolderHandlerAsync_WithNoFolderListAndAPredictorCarry_AdoptsTheCarriedHandler`, asserting the folder-helper property is reference-equal to the carried predictor and the call completed without a cancellation exception; `InitFolderHandlerAsync_AfterAdoption_ReleasesTheCarriedHandler`, asserting a second call with the same arguments completes as cancelled, which proves the carry field was nulled and the construction path ran; the first call is awaited without a guard, so against the P4-T3 non-adopting body the test fails at that first call with the cancellation exception; `InitFolderHandlerAsync_WithAnExplicitFolderList_IgnoresTheCarryAndRunsTheConstructionPath`, `InitFolderHandlerAsync_WithACarryOfAnUnexpectedRuntimeType_RunsTheConstructionPath` (carrying a Moq double of the handler interface) and `InitFolderHandlerAsync_WithNoCarry_RunsTheConstructionPath`, each asserting the call completes with an operation-cancelled exception and the folder-helper property is still null, which is the deterministic observation of the construction path under a cancelled token. The first two fail against the P4-T3 non-adopting body; the three construction-path tests pass against it and pin preserved behaviour.
  - Acceptance: the file exists, its measured line count is between 120 and 260, it declares exactly five test methods with those five names, and the Grep tool run with the pattern `GetUninitializedObject` against this file returns 0 matches.

- [ ] [P4-T10] Add the Compile element for the data-model carry tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Insert adjacent to the existing data-model test entries at lines 121 through 123.
  - Acceptance: the Grep tool run with the pattern `Controllers\\EfcDataModelIssue792CarryTests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P4-T11] [expect-fail] Create the pop-out carry regression tests in `QuickFiler.Test/Controllers/QfcCollectionControllerIssue792PopOutTests.cs`.
  - MSTest class in the `QuickFiler.Controllers.Tests` namespace, consuming the existing collection-controller test support helper (`CreateUninitializedController` at its line 146, `SetField` at line 36) to allocate an uninitialized controller and to inject the item-group collection by reflection, and the item-controller test support helper (`SetField` at line 40 of the QfcItemController test-support file) to place a folder handler and a mail-item helper on an uninitialized concrete item controller. Tests: `ReadPopOutCarry_ReadsTheFolderHandlerFromTheConcreteSourceItemController`; `ReadPopOutCarry_ReadsTheMailItemHelperFromTheSourceItemController`; `ReadPopOutCarry_WhenTheSourceItemControllerIsNull_ReturnsANullCarryWithoutThrowing`; `ReadPopOutCarry_WhenTheSourceItemControllerIsNotTheConcreteType_LeavesTheFolderHandlerNull`, using a Moq double of the item-controller interface; and `CreatePopOutHomeController_PassesBothCarryValuesToTheFactory`, substituting the static factory property with a recorder. Reset the static factory property in both the initialize and the cleanup methods so the suite stays order-independent, which is the discipline the existing test support helper documents for static state. The class carries `[DoNotParallelize]`, as the mirrored ViewerQueueStaticWrapperTests.cs does at its line 11, because the CLI runsettings run classes in parallel and this class mutates a process-wide static. The first two fail against the P4-T6 null-returning body; the other three pass against it and pin preserved behaviour, because the factory routing is complete at P4-T6.
  - Acceptance: the file exists, its measured line count is between 120 and 250, it declares exactly five test methods with those five names, it declares both a test-initialize and a test-cleanup method that reset the static factory property, and the Grep tool run with the pattern `DoNotParallelize` against this file returns exactly 1 match.

- [ ] [P4-T12] Add the Compile element for the pop-out carry tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Insert adjacent to the existing collection-controller test entries at lines 139 and 165.
  - Acceptance: the Grep tool run with the pattern `Controllers\\QfcCollectionControllerIssue792PopOutTests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P4-T13] [expect-fail] Create the viewer-queue scheduler regression tests in `QuickFiler.Test/Helper Classes/EfcViewerQueueIssue792Tests.cs`.
  - MSTest class in the `QuickFiler.Test.HelperClasses` namespace, following the shape of the existing viewer-queue static-wrapper tests and resetting the production defaults in a cleanup method using the existing reset member, as that file does at its line 18. Per correction 12, each test acquires the dispatcher transaction through `UiThreadDispatcherFixture.BeginTransactionAsync`, installs a running dispatcher obtained from `QfcItemControllerTestSupport.StartRunningDispatcher` through the transaction's `Install` member, disposes the transaction in a finally block so the previous value is restored, and shuts the dispatcher down through `ShutdownDispatcher`; the parked dispatcher of the fixture's ensure member is never used, because it runs no frame and a blocking invoke against it never returns. Each test carries the MSTest Timeout attribute with a private constant of 60000 milliseconds, the value the pump-hosted tests declare as PumpTimeoutMs, as a hang guard. The class carries `[DoNotParallelize]`, as the mirrored ViewerQueueStaticWrapperTests.cs does at its line 11, because the CLI runsettings run classes in parallel and this class mutates process-wide statics (the viewer-queue scheduler defaults and the UiThread dispatcher). Tests: `ProductionBlockingPriorityScheduler_RunsTheActionOnTheUiThreadDispatcherNotTheCaller`, which invokes the production default with an action that records its managed thread identifier and asserts that identifier equals the installed dispatcher's thread identifier and differs from the calling thread's; and `ResetProductionCoreDefaultsForTesting_RestoresADispatcherBackedBlockingScheduler`, which first replaces the scheduler with an inline recorder, calls the reset member, invokes the restored default and asserts the same two thread-identifier facts, which is what proves the reset member's copy at line 68 was changed too. Record in the class comment that this half of AC-U3 is verifiable only as a scheduler-delegate assertion and not as a real thread-affinity assertion of the pop-out continuation, so this change is parity with the sibling item viewer queue rather than a reproduction. Both tests fail against the inline production default, because the action then runs on the calling thread.
  - Acceptance: the file exists, its measured line count is between 70 and 160, it declares exactly two test methods with those two names, it declares a cleanup method that calls the existing production-defaults reset member, the Grep tool run with the pattern `DoNotParallelize` against this file returns exactly 1 match, and the Grep tool run with the pattern `EnsureDispatcher|EnsureUiThreadDispatcher` against this file returns 0 matches.

- [ ] [P4-T14] Add the Compile element for the viewer-queue tests to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Insert adjacent to the existing Helper Classes entries at lines 225 and 226. The directory name contains a space and must be reproduced exactly in the Include attribute.
  - Acceptance: the Grep tool run with the pattern `Helper Classes\\EfcViewerQueueIssue792Tests\.cs` against `QuickFiler.Test/QuickFiler.Test.csproj` returns exactly 1 match with no metadata on the matched line.

- [ ] [P4-T15] [expect-fail] Rebuild, run the three new Phase 4 test classes, and record the fail-before evidence in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/regression-testing/p4-fail-before.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Rebuild with the analyzer command first so the run is a runtime red, then run vstest filtered to the three class names with the isolation switch and no trx logger.
  - Acceptance: the artifact records `Command:`, the observed `EXIT_CODE:`, an `ExpectedExitCode:` field set to the non-zero value this run actually produced, and an `Output Summary:` naming each of the twelve test methods and its outcome. Exactly these six must be recorded as failing: the two adoption tests of P4-T9, the two carry-read tests of P4-T11 (folder handler and mail-item helper), and the two scheduler tests of P4-T13. The remaining six (the three construction-path tests, the null-controller test, the non-concrete-type test and the factory-argument test) pin preserved behaviour and are recorded with their actual outcome, which is expected to be a pass.

- [ ] [P4-T16] Implement the real carry read in `QuickFiler/Controllers/QfcCollectionController.PopOut.cs`.
  - Replace the null placeholders in the carry-read member: read the mail-item helper through the interface member `ItemHelper` of the group's item controller with a null-conditional access, and read the folder handler through the internal accessor added in P4-T2 when the group's item controller pattern-matches the concrete `QfcItemController` type (`is QfcItemController`), so a group with no controller or with a non-concrete controller yields a null value rather than throwing.
  - Acceptance: the five test methods declared in P4-T11 pass. The Grep tool run with the pattern `is QfcItemController` against this file returns exactly 1 match. The file's measured line count is between 80 and 150.

- [ ] [P4-T17] Implement the carry adoption in `QuickFiler/Controllers/EfcDataModel.Carry.cs`.
  - Per D5: at the top of the moved initialization member, when no folder list was supplied and the carried handler matches the concrete predictor type by pattern match, assign it to the folder-helper property, null the carry field, and return before any task is started. Otherwise fall through to the existing construction path, with the mail-item source taken from a local that prefers the carried helper over the resolver-derived value. Document the adoption contract, namely that the carried handler must already be initialized for this item under the same initialization sequence the construction branch would otherwise run, mirroring the receiver contract established for the in-QuickFiler carry.
  - Acceptance: the five test methods declared in P4-T9 pass. The file's measured line count is between 70 and 140.

- [ ] [P4-T18] Change the production blocking scheduler in `QuickFiler/Helper Classes/EfcViewerQueue.cs` from an inline invocation to a dispatcher invocation.
  - Replace the delegate body at line 25 and the identical body inside the production-defaults reset member at line 68 with a dispatcher invoke that forwards both the action and the priority, matching the sibling item viewer queue whose blocking scheduler already does exactly that at its lines 27 and 90. The file already imports the UtilitiesCS namespace that owns the dispatcher accessor (its line 4).
  - Acceptance: the two test methods declared in P4-T13 pass. The Grep tool run with the pattern `priority\) => action\(\)` against this file returns 0 matches, and the Grep tool run with the pattern `UiThread\.Dispatcher\.Invoke\(action, priority\)` against this file returns exactly 2 matches; that pattern does not match the two existing asynchronous-invoke sites at lines 20 and 67, because the token after `Invoke` there is `Async`. The file's measured line count is between 99 and 106.

- [ ] [P4-T19] Rebuild, run the three Phase 4 test classes green, and commit, recording the run in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/regression-testing/p4-pass-after.2026-09-12T13-21.md`.
  - Precondition: P0-T3 is complete. Rebuild with the analyzer command, then run vstest filtered to the three Phase 4 class names.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE: 0` and an `Output Summary:` stating that all twelve Phase 4 test methods passed and zero failed. Then run `git -C . add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` and `git -C . commit -m "#792 Phase 4 pop-out carry and dispatcher viewer construction"`; afterwards `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines.

### Phase 5 — AC-U8 Ceiling and Compile-item Parity, AC-U9 Debt Record

- [ ] [P5-T1] Run the formatter over the written .cs files so the ceiling audit measures formatted files, and record the observation in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p5-scoped-format.2026-09-12T13-21.md`.
  - Run `dotnet tool run csharpier format` followed by the twenty production .cs paths of the Write Set table (every production row except the project file), then run it a second time followed by the nine test .cs paths of the Write Set table; quote the two paths under Helper Classes because they contain a space. Do not pass a directory: a directory-scoped pass would also rewrite pre-existing drift in files outside the Write Set and break the exactly-the-written-paths clause of P6-T20.
  - Acceptance: this is a write-mode command whose exit code is 0 both when it changed nothing and when it repaired drift, so the exit code alone is not the gate. The artifact records both commands, both exit codes, the processed-file counts the tool printed, the output of `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test` captured immediately before and immediately after the two commands, and the result of `dotnet tool run csharpier check` run afterwards over the same twenty-nine paths. The gate passes when both format exit codes are 0, the check exit code is 0 and it reports no file needing formatting, and every path in the after-capture is one of the Write Set paths.

- [ ] [P5-T2] AC-U8 ceiling audit. Measure every one of the 31 Write Set paths, plus the one control path, after formatting and record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/ac-u8-line-counts.2026-09-12T13-21.md`.
  - Measure with the Grep tool using the pattern `^` in count output mode, the same instrument P0-T9 used, so the before and after figures are comparable. Record the control row for the item-controller interface with the same `control (not written)` label P0-T9 used.
  - Acceptance: the artifact contains 31 Write Set rows plus one control row, 32 rows in all, each carrying the path, the Phase 0 baseline value from the P0-T9 artifact and the measured post-change value. Every Write Set row except the two AC-U9 debt rows records a post-change value of at most 500. The two project-file rows are recorded but are not size-gated. The control row records 113, unchanged. The row for `QuickFiler/Controllers/EfcItemController.cs` records a value strictly less than 1121 and the row for `QuickFiler/Controllers/QfcCollectionController.cs` records a value strictly less than 2329. Any row over 500 that is not one of those two fails this gate.

- [ ] [P5-T3] AC-U8 Compile-item parity. Compare the added and removed .cs files against the Compile element edits and record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/ac-u8-compile-parity.2026-09-12T13-21.md`.
  - Run `git -C . add -- QuickFiler QuickFiler.Test`, then `git -C . diff --name-status --cached 2405a829d6afd3b12eb7c228d57158a97cb4e2ca -- QuickFiler QuickFiler.Test`. The staging span is what makes the name-listing diff able to see the seventeen files this change created. Then measure the post-change Compile element count in each project file with the Grep tool using the pattern `<Compile Include=` in count output mode.
  - Acceptance: the artifact records the diff's added-path list, the diff's deleted-path list, and both post-change counts. The added-path list contains exactly seventeen .cs paths, being the nine new production files and the eight new test files; the deleted-path list contains zero .cs paths. The production project's post-change Compile count minus the P0-T10 baseline equals exactly 9, and the test project's post-change Compile count minus its P0-T10 baseline equals exactly 8. The two differences equal the counts of new .cs files in each project, which is the parity AC-U8 requires.

- [ ] [P5-T4] AC-U9 pre-existing debt record. Write the debt statement to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/ac-u9-preexisting-debt.2026-09-12T13-21.md`.
  - Contents: a statement that `QuickFiler/Controllers/QfcCollectionController.cs` and `QuickFiler/Controllers/EfcItemController.cs` were already over the 500-line ceiling before this change and remain over it after; the before count and the after count for each, taken from the P0-T9 and P5-T2 artifacts; the statement that a full split of either is out of scope for a bug fix per the spec's own out-of-scope section; and the statement that this change neither introduced nor resolved that debt and that both files' counts strictly decreased.
  - Acceptance: the artifact records four integers, being two before values and two after values, each matching the corresponding value in the P0-T9 and P5-T2 artifacts, and each after value is strictly less than its before value.

- [ ] [P5-T5] Commit the Phase 5 evidence and the formatter's changes with a scoped pathspec, recording the commit in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/p5-commit.2026-09-12T13-21.md`.
  - Run `git -C . add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792`, then `git -C . commit -m "#792 Phase 5 ceiling audit, Compile-item parity, debt record"`. Then write the record artifact carrying `Command:`, `EXIT_CODE:` and the commit subject, run the same `git -C . add` span again, and run `git -C . commit --amend --no-edit`.
  - Acceptance: `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines, and the artifact records the resulting commit subject.

### Phase 6 — Final QA Loop, AC Check-off and Manual Verification

- [ ] [P6-T1] MANUAL HUMAN STEP. Confirm Outlook is closed through its normal exit path before the final toolchain loop, and record it in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/outlook-closed-final.2026-09-12T13-21.md`.
  - Runbook: identical to P0-T3. Close Outlook through File then Exit or the window close button and wait for the process to end on its own. Never end the process. A locked build output makes the rebuild fail or silently link a stale assembly, which would make every gate below untrustworthy.
  - Acceptance: the artifact records the operator name, the ISO-8601 timestamp of the confirmed exit and the statement that Outlook was closed and not killed. No exit code is asserted for this task.

- [ ] [P6-T2] Toolchain step 1, format. Run the write-mode formatter and record the observation in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-format.2026-09-12T13-21.md`.
  - Run the same two `dotnet tool run csharpier format` invocations as P5-T1, over the same twenty production paths and nine test paths.
  - Acceptance: this is a write-mode command, so the exit code alone cannot distinguish a clean run from a repairing one. The artifact records both commands, both exit codes, the processed-file counts the tool printed, and a `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test` capture taken immediately after. The gate passes when that porcelain capture prints no lines, which is the tree observation proving the formatter changed nothing. If it prints lines, the loop restarts at this task after those changes are committed.

- [ ] [P6-T3] Toolchain step 1 verification, format check. Record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-format-check.2026-09-12T13-21.md`.
  - Run `dotnet tool run csharpier check .`. This is the read-only CI-parity form.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:` and an `Output Summary:` carrying the count of files reported as needing formatting and the reported set. The gate passes when the exit code is 0, or when it equals the exit code recorded in the P0-T5 artifact, the reported set is a subset of the `Reported set:` recorded there, and no Write Set path appears in it. The second branch exists because a pre-existing drift elsewhere in the tree is a property of the tree rather than of this change; a Write Set path in the set, or any path not in the baseline set, fails the gate.

- [ ] [P6-T4] Toolchain step 2, analyzers. Record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-analyzers.2026-09-12T13-21.md`.
  - Precondition: P6-T1 is complete. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:`, the warning and error counts read from the build summary lines, and the deduplicated diagnostic-identifier set. The gate passes when the exit code is 0, or when it equals the P0-T6 exit code and the diagnostic-identifier set recorded here is a subset of the set recorded there. If this step changed any file, the loop restarts at P6-T2.

- [ ] [P6-T5] Toolchain step 3, nullable type check. Record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-nullable.2026-09-12T13-21.md`.
  - Precondition: P6-T1 is complete. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`. Do not add the solution-wide nullable opt-in property.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:`, the error count from the build summary and the deduplicated diagnostic-identifier set. The gate passes on the same two-branch rule as P6-T4, comparing against the P0-T7 artifact. If this step changed any file, the loop restarts at P6-T2.

- [ ] [P6-T6] Toolchain step 4, tests with coverage. Record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-test-coverage.2026-09-12T13-21.md`.
  - Precondition: P6-T1 is complete. Run the same coverage-runner invocation as P0-T8, with an explicit search root of the current directory. Do not pass a trx logger and do not commit the raw Cobertura file.
  - Acceptance: the artifact records `Command:`, `EXIT_CODE:` as observed, and an `Output Summary:` carrying the total, passed, failed and skipped test counts, the `Failed set:`, and the five Cobertura root-element figures read with the Read tool: `lines-valid`, `lines-covered`, `line-rate`, `branches-valid` and `branch-rate`. The gate passes when the exit code is 0, or when it equals the P0-T8 exit code and the runner's floor assertion is the only recorded reason (a non-zero exit with any other reason fails); when the `Failed set:` is a subset of the P0-T8 `Failed set:`; and when the passed count is at least the P0-T8 passed count plus 34, being the seven contract and host test methods declared in P1-T4 and P1-T6, the fifteen declared in P3-T5, P3-T7 and P3-T9, and the twelve declared in P4-T9, P4-T11 and P4-T13. Record the exact observed figure rather than inferring it.

- [ ] [P6-T7] Coverage delta and new-file coverage record. Write it to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/coverage-delta.2026-09-12T13-21.md`.
  - Read the Cobertura document produced by P6-T6 with the Read tool. The runner's post-processing already merges every class element sharing a `filename` into one, so a per-file row reads the single class element carrying that file name; if more than one element carries the same file name, sum their covered and valid line counters before computing the rate, because an async state machine would otherwise split the denominator. Per-file rules, fixed by the planner so the executor selects no evidence:
    - `QuickFiler/Viewers/WebView2EnvironmentContract.cs`: whole-file line rate at least 0.90.
    - `QuickFiler/Controllers/EfcFormController.Breadcrumb.cs`: record the whole-file rate as information. The gate is the rate over every `line` element whose number lies inside the spans of the members this change writes, namely `InitializeBreadcrumbHostAsync`, the `BreadcrumbInitializationAttemptLimit` constant, and the `BreadcrumbInitializer` and `BreadcrumbRetryDelay` properties with their backing fields; the two lazy-default expressions inside the BreadcrumbInitializer and BreadcrumbRetryDelay getters are excluded from that span and recorded as information with their hit counts, because every P3-T9 test substitutes both delegates and the initializer's default cannot run without a real host; record each span from the post-change file; that rate must be at least 0.90. Every other member of the file is a verbatim move from the pre-split parent and is recorded as information with its covered and uncovered line numbers, not gated: `ConfigureBreadcrumbControl` (pre-split 1047-1068) constructs the real host and an Outlook folder-hierarchy provider and cannot run under the unit-test policy; `BindSourceFolderRows` (pre-split 1099-1109) is reached only from the post-await line of `PopulateFolderCombobox` (pre-split 1266), which the existing test at EfcFormControllerTests.cs line 300 never reaches because it faults at the data-model call (pre-split 1262); the `BindFolderRows` dispatch line (pre-split 1094) is reached only past a guard the existing test at line 392 leaves unsatisfied; the cancellation catch of `BindBreadcrumbRowsAsync` (pre-split 1121-1124) and the instance property `IsValidSelection` (pre-split 1287) have no existing test.
    - `QuickFiler/Controllers/EfcDataModel.Carry.cs`: record the whole-file rate as information; the gate is that every `line` element of the deposit member, the two carry fields and the adoption branch (every line of the moved member preceding its first task start) carries a hit count of at least 1. The construction-path lambda bodies are moved lines that run only against a live predictor and are excluded from the gate by that span, which is recorded from the post-change file.
    - `QuickFiler/Controllers/EfcItemController.WebViewEnvironment.cs` and `QuickFiler/Controllers/QfcCollectionController.PopOut.cs`: unmeasurable by pre-existing class-level exclusion (correction 9). The row records whether any class element carries that file name; if one does, it is a compiler-generated closure or state-machine type, its counters are recorded as information, and no rate is gated for it. The row cites the attribute line (parent line 25 and parent line 21 respectively), and names the evidence that stands in: the P1-T4 forwarding-constant test and the P1-T13 one-owner gate for the first, and the five P4-T11 tests for the second.
  - Acceptance: the artifact records the baseline repository line rate and branch rate from the P0-T8 artifact, the post-change repository line rate and branch rate from the P6-T6 artifact, and their differences; five rows in the shapes above, each satisfying its stated rule; and the post-change `lines-covered` must be at least the baseline `lines-covered` minus max(0, baseline `lines-valid` minus post-change `lines-valid`), and the post-change `line-rate` must be at least 0.80.

- [ ] [P6-T8] Toolchain-loop closure record. Write it to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-loop-closure.2026-09-12T13-21.md`.
  - Acceptance: the artifact names the four commands of the final pass in the mandated order, cites the four step artifacts from P6-T2 through P6-T6 by path, states how many complete passes were run before a clean pass was achieved, and states explicitly that no step in the final pass failed against its stated gate and that no step in the final pass changed a file.

- [ ] [P6-T9] MANUAL HUMAN STEP, AC-U5. Execute the live-Outlook verification runbook and record the result in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/other/ac-u5-manual-verification.2026-09-12T13-21.md`.
  - Follow the runbook already written in the user story for this item, section "AC-U5 manual verification runbook", steps 1 through 9 at its lines 65 through 73, including its preconditions. In summary: close Outlook through its normal exit, rebuild, reopen Outlook, open QuickFiler from the ribbon and confirm an item body renders so that an item-body WebView is running in the process, pop a row out and observe the folder area, type into the folder search box and observe the rows, close the Efc view and invoke ribbon Sort Email and observe the area under the Matched Folders label, type into that folder search box, then open the add-in debug log for the current date and search it for the host's own failure text, `Breadcrumb CoreWebView2 initialization failed` (QuickFiler/Viewers/WebView2BreadcrumbHost.cs line 338), which is the string the runbook's step 9 names.
  - Acceptance: this task has no exit code and no automated gate. The artifact records the operator name, the ISO-8601 timestamp, the Outlook session start time, the observed outcome for each of the two entry points, the log lines inspected in step 9 quoted as text, and a `Verdict:` line reading `PASS` or `FAIL`. The verdict is PASS only if all four of the runbook's decision conditions (its lines 79 through 82) hold. Raw log files are not committed; the observation is recorded as text.

- [ ] [P6-T10] Check off AC-U1 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U1 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U1 line; the criterion text is not modified. The evidence citation, the P3-T17 pass-after artifact path and the six test-method names declared in P3-T9, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which this task creates with a `Timestamp:` header and later tasks append to. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T11] Check off AC-U2 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U2 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U2 line; the criterion text is not modified. The evidence citation, the P3-T17 pass-after artifact path and the five router test-method names declared in P3-T7, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T12] Check off AC-U3 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U3 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U3 line; the criterion text is not modified. The evidence citation, the P4-T19 pass-after artifact path, the five data-model carry test names, the five pop-out test names and the two scheduler test names, together with the recorded statement that the UI-thread half is proven as a scheduler-delegate assertion over an installed running dispatcher and not as a thread-affinity assertion of the pop-out continuation, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T13] Check off AC-U4 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U4 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U4 line; the criterion text is not modified. The evidence citation, the P3-T17 pass-after artifact path, the boundary-sink test name declared in P3-T9 and the renamed combobox test name from P3-T16, together with the recorded correction that the combobox half was already asserted on the base commit and that the work there was a rename, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T14] Check off AC-U5 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U5 checkbox is marked complete only when the P6-T9 artifact records `Verdict: PASS`. When it is marked, the only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U5 line; the criterion text is not modified. The evidence citation, the P6-T9 artifact path named as a human-executed verification, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header; the row records the verdict either way. If the verdict is FAIL the checkbox stays unmarked and the item is reported as remediation-required. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T15] Check off AC-U6 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U6 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U6 line; the criterion text is not modified. The evidence citation, the P1-T13 one-owner parity artifact path and the P1-T14 pass-after artifact path, the five contract test methods and the two host test methods, and the statement of how the three sites are shown to agree (the host by the captured seam arguments, the Efc item-body site by the forwarding-constant test, and the QuickFiler item-body site by the one-owner gate's zero outside-matches together with the contract references in its file), is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T16] Check off AC-U7 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U7 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U7 line; the criterion text is not modified. The evidence citation, the P3-T17 pass-after artifact path and the two test-method names that assert the pending count is zero (the queue discard test declared in P3-T5 and the router failure-notification test declared in P3-T7), is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T17] Check off AC-U8 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U8 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U8 line; the criterion text is not modified. The evidence citation, the P5-T2 ceiling-audit artifact path and the P5-T3 Compile-item parity artifact path, together with the recorded AC-U9 carve-out for the two debt files, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T18] Check off AC-U9 in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md`.
  - Acceptance: the AC-U9 checkbox is marked complete. The only edit to spec.md is the marker change from `- [ ]` to `- [x]` on the AC-U9 line; the criterion text is not modified. The evidence citation, the P5-T4 debt artifact path and the four integers, being the before and after counts for `QuickFiler/Controllers/QfcCollectionController.cs` and for `QuickFiler/Controllers/EfcItemController.cs`, is appended as one row, labelled with the AC identifier, to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/issue-updates/ac-status-summary.2026-09-12T13-21.md`, which P6-T10 created with a `Timestamp:` header. No other acceptance-criteria line is edited by this task.

- [ ] [P6-T19] Write the execution-capability and contention handoff record to `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/other/execution-handoff.2026-09-12T13-21.md`.
  - Contents, recorded and not acted on by the planner: that the sandbox refuses pwsh under Agent worktree isolation so this item's execution child must be launched non-isolated; that the atomic-executor agent definition grants no msbuild and no dotnet Bash permission, so every msbuild task, every csharpier task and every coverage-runner task in this plan must be executed by an agent that holds those permissions, and the identity of that agent; that Outlook must be closed through its normal exit before every rebuild; and the two sibling-contention notes for issues 645 and 781 recorded in the Known contention section above.
  - Acceptance: the artifact exists and contains four labelled sections named Isolation, Toolchain permissions, Outlook precondition and Contention, each non-empty.

- [ ] [P6-T20] Commit the final state with a scoped pathspec and confirm a clean tree, recording it in `docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/evidence/qa-gates/final-commit.2026-09-12T13-21.md`.
  - Run `git -C . add -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792`, then `git -C . commit -m "#792 Phase 6 final QA, AC check-off and manual verification record"`. Then capture and retain the porcelain span below and the anchored diff below, write the record artifact from the retained captures carrying `Command:`, `EXIT_CODE:`, the commit subject and the diff listing, run the same `git -C . add` span again, and run `git -C . commit --amend --no-edit`. The check-off of this task and the amend that carries it are housekeeping outside this task's acceptance; the captures taken before the artifact write are the acceptance.
  - Acceptance: `git -C . status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792` prints no lines; `git -C . diff --name-only 2405a829d6afd3b12eb7c228d57158a97cb4e2ca -- QuickFiler QuickFiler.Test` lists exactly the thirty-one paths of the Write Set table and does not list the interface file; and the Grep tool run with the pattern `\.cobertura\.xml|\.trx` against this task's record artifact, whose prose must not carry either literal so the only candidate matches are diff paths, returns 0 matches, which confirms the projections-only evidence convention held.

---

## Traceability

| AC | Implementation tasks | Test tasks | Evidence |
| --- | --- | --- | --- |
| AC-U1 | P3-T3, P3-T13, P3-T14, P3-T15 | P3-T9, P3-T11, P3-T17 | p3-fail-before and p3-pass-after artifacts |
| AC-U2 | P3-T2, P3-T13 | P3-T7, P3-T11, P3-T17 | p3-fail-before and p3-pass-after artifacts |
| AC-U3 | P4-T2, P4-T3, P4-T5, P4-T6, P4-T16, P4-T17, P4-T18 (P4-T1 is the non-widening verification) | P4-T9, P4-T11, P4-T13, P4-T15, P4-T19 | p4-fail-before, p4-pass-after and iqfcitemcontroller-not-widened artifacts |
| AC-U4 | P3-T14, P3-T16 | P3-T9, P3-T17 | p3-pass-after artifact |
| AC-U5 | none, manual | none, manual | ac-u5-manual-verification artifact from P6-T9 |
| AC-U6 | P1-T2, P1-T9, P1-T10, P1-T11 | P1-T4, P1-T6, P1-T8, P1-T13, P1-T14 | one-owner-parity and p1-pass-after artifacts |
| AC-U7 | P3-T1, P3-T12, P3-T13 | P3-T5, P3-T11, P3-T17 | p3-fail-before and p3-pass-after artifacts |
| AC-U8 | P1-T3, P1-T12, P2-T7, P3-T6, P3-T8, P3-T10, P4-T4, P4-T7, P4-T10, P4-T12, P4-T14 | P5-T2, P5-T3 | ac-u8-line-counts and ac-u8-compile-parity artifacts |
| AC-U9 | none, record only | P5-T4 | ac-u9-preexisting-debt artifact |

## Planner Internal Review Record (2026-09-12, version 1.1)

PLANNER-INTERNAL-REVIEW: PASS
CITATION-TO-TREE: PASS
AC-TRACEABILITY: PASS
SCOPE-BOUNDARY: PASS
CITATION: QuickFiler/Viewers/WebView2BreadcrumbHost.cs | lines 18, 239-250 (summary folder literal; no-argument options at 250)
CITATION: QuickFiler/Controllers/QfcItemController.ViewerSetup.cs | lines 48-67 (folder at 55-58, commented construction at 60, target-typed options at 61)
CITATION: QuickFiler/Controllers/EfcItemController.cs | line 25 class-level ExcludeFromCodeCoverage; line 26 internal partial; lines 167-176 constant; 178-211 InitializeWebViewAsync; 367 _itemViewer
CITATION: QuickFiler.Test/Controllers/EfcItemControllerTests.cs | lines 372-378 IncognitoArgument pinned test
CITATION: QuickFiler/Controllers/EfcFormController.cs | line 26 non-partial; regions 266/365, 367/479, 481/834, 836/990, 992/1289; members 1047, 1072, 1080, 1086, 1099, 1112, 1121-1124, 1127, 1251, 1270, 1275-1287; 1291-1319; 1320-1321
CITATION: QuickFiler/Controllers/BreadcrumbBridgeRouter.cs | lines 36-40 fields; 320-329 NotifyCoreInitialized (sole occurrence)
CITATION: QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs | lines 168-180 DeliverDocument two-state delivery
CITATION: QuickFiler/Controllers/BreadcrumbOutboundQueue.cs | 67 lines; PendingCount 29; OnInitializationCompleted 59-65
CITATION: QuickFiler/Controllers/EfcDataModel.cs | line 21 internal partial; ctor 48-75; Token 164-168 protected set; FolderHelper 177-186; InitFolderHandlerAsync 188-221; MailInfo 241; TryGetFirstInSelection 243-257
CITATION: QuickFiler/Controllers/QfcCollectionController.cs | line 21 class-level ExcludeFromCodeCoverage; 22 public partial; 304 _itemGroups; 710-735 pop-out members (sole references in file)
CITATION: QuickFiler/Controllers/QfcItemGroup.cs | lines 26-44 MailItem and ItemController (typed as the interface)
CITATION: QuickFiler/Controllers/EfcHomeController.cs | ctors 47-52 and 54-59; DataModel assignment ends 71; form-controller factory call 85
CITATION: QuickFiler/Interfaces/IQfcItemController.cs | 113 lines; using UtilitiesCS at 12; ItemHelper at 41
CITATION: QuickFiler.Test/Helper%20Classes/QfcThemeHelperTests.cs | line 337 FakeQfcItemController implements the interface
CITATION: QuickFiler/Controllers/QfcItemController.cs | 41 _folderHandler; 259 _carriedFolderHandler; 265 TopFolderScore
CITATION: QuickFiler/Helper%20Classes/EfcViewerQueue.cs | 101 lines; blocking scheduler 25 and 68; InvokeAsync sites 20 and 67
CITATION: QuickFiler/Helper%20Classes/ItemViewerQueue.cs | lines 27 and 89-90 Dispatcher.Invoke parity
CITATION: QuickFiler/QuickFiler.csproj | lines 289-296, 303-304, 314-315, 421-423
CITATION: QuickFiler.Test/QuickFiler.Test.csproj | lines 121-125, 139, 165, 213-214, 225-226
CITATION: QuickFiler.Test/Controllers/EfcFormControllerTests.cs | TestClass 16, partial 17, CreateMinimalController 24, method 300-328, sink 310, Be(1) 321-327, SetPrivateField 467
CITATION: QuickFiler.Test/Controllers/EfcFormControllerTests.Part2.cs | lines 20-24 no-second-TestClass convention
CITATION: QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs | line 13 namespace; 34-74 host/provider/router arrangement
CITATION: QuickFiler.Test/Viewers/WebView2BreadcrumbHostTests.cs | line 13 namespace; 36-38 pump-hosted control; 404 seam setup
CITATION: QuickFiler.Test/Controllers/QfcItemController.UiThreadDispatcherFixture.cs | Exchange 55, CompareExchange 70, EnsureDispatcher 99, BeginTransactionAsync 122, parked never pumps 145-147, transaction 220, Install 242
CITATION: QuickFiler.Test/Controllers/QfcItemController.TestSupport.cs | SetField 40; StartRunningDispatcher 251; ShutdownDispatcher 277
CITATION: QuickFiler.Test/Controllers/QfcCollectionController.TestSupport.cs | SetField 36; CreateUninitializedController 146
CITATION: UtilitiesCS/Threading/UiThread.cs | Dispatcher 251-269 throws when uninitialized; GetAwaiter 198
CITATION: UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs | BannerPrefix 19; BuildRow 76
CITATION: UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs | ctor(IApplicationGlobals) 36
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | line 76 hard-coded filter; 301 relative-path worktree exclusion; 342-344 write-then-floor
CITATION: docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/spec.md | Acceptance Criteria 296-306; Write Set 308-344 (31 paths at 310-340; exclusion paragraph at 344)
CITATION: docs/features/active/2026-09-06-breadcrumb-webview2-init-fails-resource-not-in-correct-state-792/user-story.md | runbook steps 65-73; decision conditions 79-82
AC-INVENTORY: AC-U1, AC-U2, AC-U3, AC-U4, AC-U5, AC-U6, AC-U7, AC-U8, AC-U9
AC-MAPPING: AC-U1 | IMPLEMENTATION: P3-T3,P3-T13,P3-T14,P3-T15 | TESTS: P3-T9,P3-T11,P3-T17 | EVIDENCE: p3-fail-before and p3-pass-after artifacts
AC-MAPPING: AC-U2 | IMPLEMENTATION: P3-T2,P3-T13 | TESTS: P3-T7,P3-T11,P3-T17 | EVIDENCE: p3-fail-before and p3-pass-after artifacts
AC-MAPPING: AC-U3 | IMPLEMENTATION: P4-T2,P4-T3,P4-T5,P4-T6,P4-T16,P4-T17,P4-T18 | TESTS: P4-T9,P4-T11,P4-T13,P4-T15,P4-T19 | EVIDENCE: p4-fail-before, p4-pass-after and iqfcitemcontroller-not-widened artifacts
AC-MAPPING: AC-U4 | IMPLEMENTATION: P3-T14,P3-T16 | TESTS: P3-T9,P3-T17 | EVIDENCE: p3-pass-after artifact
AC-MAPPING: AC-U5 | IMPLEMENTATION: manual runbook, no code task | TESTS: P6-T9 human-executed | EVIDENCE: ac-u5-manual-verification artifact
AC-MAPPING: AC-U6 | IMPLEMENTATION: P1-T2,P1-T9,P1-T10,P1-T11 | TESTS: P1-T4,P1-T6,P1-T8,P1-T13,P1-T14 | EVIDENCE: one-owner-parity and p1-pass-after artifacts
AC-MAPPING: AC-U7 | IMPLEMENTATION: P3-T1,P3-T12,P3-T13 | TESTS: P3-T5,P3-T11,P3-T17 | EVIDENCE: p3-fail-before and p3-pass-after artifacts
AC-MAPPING: AC-U8 | IMPLEMENTATION: P1-T3,P1-T12,P2-T7,P3-T6,P3-T8,P3-T10,P4-T4,P4-T7,P4-T10,P4-T12,P4-T14 | TESTS: P5-T2,P5-T3 | EVIDENCE: ac-u8-line-counts and ac-u8-compile-parity artifacts
AC-MAPPING: AC-U9 | IMPLEMENTATION: record only | TESTS: P5-T4 | EVIDENCE: ac-u9-preexisting-debt artifact
UNRESOLVED-GAPS: NONE
