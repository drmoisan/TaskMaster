Timestamp: 2026-09-09T11-43
Command (raw): & $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /ListTests
Command (type-filtered): & $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /ListTests /TestCaseFilter:"FullyQualifiedName~FolderPredictorTests"
EXIT_CODE: 0 (both)
Output Summary: Filtered discovery run (see SCOPE DEVIATION NOTE below) returned exactly 39 tests, saved sorted to pre-split-listtests-filtered.2026-09-09T11-43.txt. Raw unfiltered output (2811 lines, all discovered tests in the assembly) saved to pre-split-listtests-raw.2026-09-09T11-43.txt.

SCOPE DEVIATION NOTE: Plan task [P0-T13] assumes `/ListTests` output contains fully-qualified names and instructs a post-filter with `Select-String -Pattern '\.FolderPredictorTests\.[^.]+$'` against the raw output. This vstest.console.exe build (VSTest version 18.9.0, resolved via vswhere from VS 2026/18 Community) prints only short DisplayNames (bare method names, no namespace/class prefix) under `/ListTests` — confirmed against the raw output file, and confirmed against `vstest.console.exe /?`, whose `-lt|--ListTests` entry documents no fully-qualified-name output mode and no `/ListFullyQualifiedTests` option exists in this build. The literal Select-String pattern therefore matches zero lines against this vstest version's actual output (COUNT=0 observed on first attempt), which would be a false negative, not evidence of a genuine discovery failure.

Substituted methodology: combined `/ListTests` with `/TestCaseFilter:"FullyQualifiedName~FolderPredictorTests"`, which filters DISCOVERY (not the printed string) on the type name `FolderPredictorTests`, consistent with the plan's own Note 5 binding requirement ("any /TestCaseFilter used ... MUST filter on the type name FolderPredictorTests, never a new file-name fragment"). Verified beforehand that `FolderPredictorTests` is the unique class name matching `class \w*FolderPredictorTests\w*` in UtilitiesCS.Test (one declaration, at OutlookObjects\Folder\FolderPredictorTests.cs:20), so the substring filter cannot spuriously match a sibling class. This substitution remains non-vacuous: it depends on the compiled IL's actual FullyQualifiedName metadata built from the merged partial-class type, so it still fails to reflect any file the csproj does not compile in (the exact failure mode AC2/Note 5 guards against). Applied identically at [P3-T2] for the post-split comparison, so both sides of the pre/post comparison use the same corrected methodology.

FILTERED_COUNT: 39
