# Atomic Plan — quickfiler-qfc-home-controller-coverage (Issue #433, epic child F7)

- **Feature folder (`<FEATURE>`):** `docs/features/active/2026-08-07-quickfiler-qfc-home-controller-coverage-433`
- **Epic:** `quickfiler-per-file-coverage` (parent issue #136), child **F7**, wave 1, band C3
- **Integration branch:** `epic/quickfiler-per-file-coverage-integration`
- **Work Mode:** `full-feature` — `spec.md` **and** `user-story.md` are together the authoritative acceptance-criteria source
- **Worktree:** `C:\Users\DanMoisan\repos\TaskMaster\.claude\worktrees\agent-afcf27830d48e5590`
- **Plan timestamp:** 2026-08-07T20-41
- **Upstream dependency:** F1 `quickfiler-coverage-ledger` (wave 0) — consumed at execution time, not at planning or preflight time

## Conventions Used By Every Task

- `<FEATURE>` = `docs/features/active/2026-08-07-quickfiler-qfc-home-controller-coverage-433`.
- `<TS>` = the ISO-8601 `yyyy-MM-ddTHH-mm` timestamp captured at the moment the artifact is written.
- All evidence resolves under `<FEATURE>/evidence/<kind>/` with `<kind>` in `baseline`, `qa-gates`, `regression-testing`, `other`. Paths under `artifacts/` are forbidden for evidence and no upstream instruction may override this.
- Every command-bearing task writes one artifact containing `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- Tests are MSTest + Moq + FluentAssertions, Arrange–Act–Assert, with a `<summary>` comment naming the scenario and the expected outcome.
- Prohibited in every new or modified test: `Thread.Sleep`, `Task.Delay`, any real wall-clock wait or poll loop, `DateTime.Now` / `DateTime.UtcNow` / `DateTimeOffset.Now`, unseeded randomness, temporary or real file writes, external services or processes, live Outlook COM objects, live WinForms `Form` construction, `Show()`, `MessageBox`, `UiThread.Init`, and `SynchronizationContext.SetSynchronizationContext` in Arrange. `[Timeout(...)]` is permitted strictly as a hang guard on a path that consumes zero wall-clock time, with the justification written into the test's own doc comment.
- Time comes from the injected `TimeProvider` seam, supplied as `FakeTimeProvider` in tests.

## Resolved Toolchain Commands

| Stage | Command |
| --- | --- |
| Restore local tools | `dotnet tool restore` |
| Format | `dotnet tool run csharpier format .` |
| Format check | `dotnet tool run csharpier check .` |
| Analyze | `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` |
| Type-check | `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` |
| Full-suite test with coverage | `pwsh -File scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput <cobertura-path>` |
| Scoped test run | `$vstest = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -find Common7\IDE\Extensions\TestPlatform\vstest.console.exe` then `& $vstest QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~<Name>"` |

Notes carried into every task that runs a command:

- The repo pins **csharpier 1.2.6** in root `dotnet-tools.json`; its subcommands are `format` / `check`. Bare `csharpier .` is v0 syntax and fails.
- `vstest.console.exe` is not on `PATH`; resolve it through `vswhere.exe` as shown. vstest 18.x rejects `OR` inside `/TestCaseFilter` — join clauses with `|`.
- `.csharpierignore` already excludes `**/evidence/**`, `*.cobertura.xml`, `*.trx`, and `*.csproj` / `*.props` / `*.targets`. It does **not** exclude `*.cs`, so every new production and test file must be csharpier-clean.

## Decisions Record — Settled, Do Not Re-Open

| # | Decision | Basis |
| --- | --- | --- |
| D-1 | Both interface files (`QuickFiler/Controllers/IQfcHomeController.cs`, `QuickFiler/Interfaces/IFilerHomeController.cs`) are **read-only**. Zero executable IL; F1 classification `interface-only / not-measured`; **no tests of any kind**. | `spec.md` § Scope; `IQfcHomeController.cs.research` § 2.1-2.3; `IFilerHomeController.cs.research` § 2.1-2.3 |
| D-2 | **Interface shape-assertion and reflection-shape tests are PROHIBITED anywhere in this child's diff.** They buy zero coverage (the files emit no IL and test-assembly lines are excluded from measurement), duplicate a check the compiler performs more strongly, have no unit of behavior under test, and would convert a legitimate future coordinated interface change into a spurious failure. | `spec.md` § Scope (Out of scope); research § 4.1 of both interface artifacts; AC8 |
| D-3 | Every seam is an `internal` member on the `QfcHomeController` class, enabled by `[assembly: InternalsVisibleTo("QuickFiler.Test")]` at `QuickFiler/Controllers/QfcHomeController.cs:18`. Neither interface is modified. `IFilerHomeController` is also implemented by sibling-**F8**-owned `EfcHomeController`, so a member addition there is a prohibited cross-child change (CS0535). | `spec.md` § Cross-Child Contract Notes 1-4; AC12 |
| D-4 | The partial split is **mandatory** (487 + ~15 = ~502 against a hard 500-line limit) and it has **no effect on either interface contract** (a partial class is one `TypeDef`; the CLR interface map is per-type, not per-file). | `spec.md` § Mandatory Partial Split; AC11 |
| D-5 | `Iterate()` and `Iterate2()` are **covered, not removed**. Removal is tracked as issue **#447**, sequenced after F6 removes the never-invoked `QfcFormController.Iterate` delegate field. | `spec.md` § Decision: Dead Iteration Methods; AC19 |
| D-6 | Latent defects **#442, #443, #446, #447** are OUT OF SCOPE and must not be fixed. Every test that pins current defective behavior carries `CHARACTERIZATION` in its own summary comment and names its tracking issue. | `spec.md` § Out-of-Scope Defects; AC19, AC20 |
| D-7 | Coverage acceptance is measured **only** by F1's per-file harness at execution time. The indicative research figures (`QfcHomeController.cs` ~71.39% line / ~51% branch; `Metrics.cs` ~65.09% / ~62.5%; `Iteration.cs` estimated mid-to-high 80s) are **planning inputs only** and must never appear in a task's acceptance condition as a measured fact for this run. | `spec.md` § Upstream Contract 3; AC2-AC7 |
| D-8 | These five test files are **FROZEN** under #424 AC 12 and must be byte-identical to base `74be1964`: `QfcHomeControllerIterationTests.cs`, `QfcInitEmailQueueZeroBatchTests.cs`, `QfcHighConfidencePreFilterTests.cs`, `QfcFormControllerTests.cs`, `QfcHomeControllerIssue218Tests.cs`. All new tests go in NEW test files, named explicitly in every task. | `spec.md` § Frozen Test Files; AC15 |
| D-9 | **Tier C is NOT adopted.** S4 (`LaunchCoreAsync` extraction), S5a (`QfcFormViewerLoader`), S5b (`UiSchedulerLoader`), the second partial split (`QfcHomeController.Lifecycle.cs`), and TC19-TC23 are all excluded. Justification: the per-file target is projected to clear 80% without them (≈86.6% on the main file after Split 1 + Tier A + Tier B), while S5a/S5b carry the highest breakage risk in this child — `QfcHomeController.cs:133` and `:136` are invisibly coupled, because `TaskScheduler.FromCurrentSynchronizationContext()` succeeds today only as a side effect of the preceding line constructing a live `QfcFormViewer` that installs a `WindowsFormsSynchronizationContext`. Adopting a viewer seam without a scheduler seam in the same change turns `InitAsync_InitializesCorrectly` into an `InvalidOperationException`. The residual `LaunchAsync` lines are recorded as irreducible host wiring against F1's ledger. | `spec.md` § Required Seams (conditional items), R7; `QfcHomeController.cs.research` § 4 Tier C, § 5 S4/S5 |
| D-10 | **S3 (`IUiDispatcher`) is NOT adopted.** Coverage gain is zero (lines 343-347 are already hit) and adoption requires modifying an existing test. The `UiThread.Init(false)` process-global dependency in `QfcHomeControllerRunAsyncTests.cs:329` is left in place and recorded as **known, unresolved pre-existing policy debt** (with the live-`QfcFormViewer` construction in the two `Init*` tests) rather than silently ignored, per `spec.md` R8. | `spec.md` § Required Seams (S3), R8 |
| D-11 | **D7 (removal of the duplicated `Globals.FS.SpecialFolders.TryGetValue("MyDocuments", ...)` at `QfcHomeController.Metrics.cs:84`) IS adopted**, as task [P3-T5]. Justification: the `false` arm of line 84 is permanently unreachable, because line 33 already returned on exactly that condition. It is therefore a permanently-partial branch that caps this child's own branch-coverage number on an F7-owned file, and removing it by reusing `folderRoot` from line 33 is behaviour-identical by construction. The edit lands in `QfcHomeController.Metrics.cs`, which #424 did not touch, so it adds no merge exposure against the integration branch. | `spec.md` § Required Seams (D7); `Metrics.cs.research` § 9 D7; AC12 |
| D-12 | **The `GetMoveDiagnostics_NullAppointment_DoesNotThrow` retarget IS adopted**, as its own task [P3-T28] carrying a written justification, per AC16. `QfcHomeControllerMetricsTests.cs` is not on the frozen list. Justification is recorded in the task itself. | `spec.md` § One proposal, not a decision; AC16 |
| D-13 | **TC18 is buffer, not baseline.** It depends on four early-return null guards inside sibling-**F6**-owned `QfcFormController.SetupDisposal.cs` (lines 24-30, 50-57, 77-80, 151-154). The coverage target must hold without it, and it does. [P2-T13] carries an explicit, authorized skip branch: if those guards are absent or narrowed at execution time, record a one-line waiver and **drop the test** rather than editing F6. | `spec.md` § Required Seams (TC18), A-3; `QfcHomeController.cs.research` § 4 TC18, R6 |
| D-14 | New test files require `<Compile Include>` entries in `QuickFiler.Test/QuickFiler.Test.csproj` (a legacy `packages.config` project with explicit compile wiring; existing home-controller entries are at lines 125-131). This file is **not** sibling-owned and its wiring is mechanically necessary for the tests to compile at all; AC17's enumeration of "new test files" is read to include it. Each scaffold task adds exactly the entries for the files it creates. | Verified on disk at `QuickFiler.Test/QuickFiler.Test.csproj:118-137` |
| D-15 | **File-name collision hazard.** Two files named `IQfcHomeController.cs` exist. Only `QuickFiler/Controllers/IQfcHomeController.cs` is compiled (`QuickFiler.csproj:304`). `QuickFiler/Interfaces/IQfcHomeController.cs` is an uncompiled orphan and must not be edited, deleted, or added to the ledger. **Every task naming this file uses the full path.** | `spec.md` § File-name collision hazard |
| D-16 | The fire-and-forget ordering in `Iterate2` is observed deterministically by **antecedent gating with a `TaskCompletionSource`** on the mocked `DequeueNextItemGroupAsync`, asserting synchronously while the gate is still open. `Task.Yield()`, scheduler drains, and polling `Mock.Invocations.Count` are rejected as non-deterministic under load. | `spec.md` § Deterministic ordering; `Iteration.cs.research` § 5.2, § 5.3 |

## Sibling-Owned Files — Must Not Be Edited

`IQfcDatamodel` / `QfcDatamodel*` (F5); `IQfcQueue` / `QfcQueue*` / `FilerQueue` (F2); `QfcCollectionController*` (F11); `QfcFormController*` / `QfcExplorerController` (F6); `KeyboardHandler` / `Kbd*` / `Ka*` (F3); `EfcHomeController*` (F8); `coverage.config`; any shared build property file.

The only shared production project file this child touches is `QuickFiler/QuickFiler.csproj`, and only to add **one** `<Compile Include>` line adjacent to lines 325-327. No reordering, no whitespace normalization, no property edit.

## Upstream Dependency Handling (F1) — Non-Blocking

F1 (`quickfiler-coverage-ledger`, wave 0) delivers the ratified ledger at `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.md` and the per-file coverage harness. Neither artifact exists on disk today; F1 is being prepared concurrently and will have merged to the epic integration branch before this child executes. Tasks [P0-T8], [P0-T9], and every per-file coverage verification task consume those deliverables as a **normal execution-time dependency**. No task in this plan has an acceptance condition that is evaluated at planning or preflight time against the existence of an F1 artifact. If F1's ledger or harness is genuinely absent when execution begins, that is an epic-orchestrator sequencing failure to be raised at that moment, not a planning defect.

---

### Phase 0 — Baseline Capture and Upstream Contract Consumption

- [ ] [P0-T1] Read `CLAUDE.md` in full and create `<FEATURE>/evidence/baseline/phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:` (the `policy-compliance-order` sequence), and an explicit list of files read so far with `CLAUDE.md` as the first entry.
  - Acceptance: the artifact exists, contains all three required fields, and lists `CLAUDE.md`.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md` in full and append it to the file list in `<FEATURE>/evidence/baseline/phase0-instructions-read.<TS>.md`, recording the 500-line file-size limit and the mandatory toolchain-loop ordering as the two clauses that bind this child.
  - Acceptance: the artifact lists `.claude/rules/general-code-change.md` and names both clauses.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md` in full and append it to the same artifact, recording the coverage thresholds, the Coverage Exclusion Policy, the Test File Location rule, and the Determinism Infrastructure clause.
  - Acceptance: the artifact lists `.claude/rules/general-unit-test.md` and names all four clauses.
- [ ] [P0-T4] Read `.claude/rules/csharp.md` in full and append it to the same artifact, recording the DI-seam hierarchy (interface > injectable delegate > adapter), the `TimeProvider` time-seam guidance, and the banned-symbol set.
  - Acceptance: the artifact lists `.claude/rules/csharp.md` and names all three clauses.
- [ ] [P0-T5] Read `.claude/skills/policy-compliance-order/SKILL.md`, `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`, and `.claude/skills/acceptance-criteria-tracking/SKILL.md`, and append all four to the same artifact.
  - Acceptance: the artifact lists all four skill paths.
- [ ] [P0-T6] Read `docs/features/epics/quickfiler-per-file-coverage/epic.md` (sections "Shared Design", "F7", "Known Conflict Risks"), `<FEATURE>/issue.md`, `<FEATURE>/spec.md`, and `<FEATURE>/user-story.md`, and append all four to the same artifact together with the count of acceptance criteria found (27 in `spec.md`, 13 in `user-story.md`).
  - Acceptance: the artifact lists all four documents and records both AC counts.
- [ ] [P0-T7] Read all five research artifacts under `<FEATURE>/research/` (`QfcHomeController.cs`, `QfcHomeController.Metrics.cs`, `QfcHomeController.Iteration.cs`, `IQfcHomeController.cs`, `IFilerHomeController.cs`, all dated `2026-08-07T20-50`) and append them to the same artifact with the per-file proposed-test-case count for each (15+3 buffer / 17 / 12 / 0 / 0).
  - Acceptance: the artifact lists all five research paths and records all five counts.
- [ ] [P0-T8] Read F1's ratified ledger at `docs/features/epics/quickfiler-per-file-coverage/coverage-ledger.md` and record the classification row **verbatim** for each of the five in-scope files into `<FEATURE>/evidence/qa-gates/f1-ledger-rows.<TS>.md`.
  - Expected: `QuickFiler/Controllers/QfcHomeController.cs`, `QuickFiler/Controllers/QfcHomeController.Metrics.cs`, and `QuickFiler/Controllers/QfcHomeController.Iteration.cs` are `testable`; `QuickFiler/Controllers/IQfcHomeController.cs` and `QuickFiler/Interfaces/IFilerHomeController.cs` are `interface-only / not-measured`.
  - Also record F1's chosen ledger row format for zero-line files, and F1's answer for files created mid-wave (`QfcHomeController.Properties.cs` has no row in the 121-file ledger).
  - Acceptance: the artifact contains five verbatim rows plus the two mechanism answers. Any classification other than the expected pair halts work and requires `spec.md` to be revised before further edits. The ledger file itself is not modified by this child. This is an execution-time dependency read; if the ledger is absent when this task runs, raise an epic-orchestrator sequencing failure (F1 is wave 0 and must merge first).
- [ ] [P0-T9] Read F1's per-file coverage harness deliverable and record its exact invocation command, its inputs, and its output shape into `<FEATURE>/evidence/baseline/f1-harness-invocation.<TS>.md`. The harness is derived from the Cobertura output of `scripts/vscode/Invoke-MSTestWithCoverage.ps1` and is the **only** accepted evidence mechanism for every per-file figure in this child. No substitute harness may be constructed.
  - Acceptance: the artifact records a runnable command line and the field names the harness emits per file (at minimum file path, line rate, branch rate).
- [ ] [P0-T10] Record the merge-base commit SHA and the working-tree state into `<FEATURE>/evidence/baseline/merge-base.<TS>.md` using `git rev-parse HEAD`, `git merge-base HEAD origin/epic/quickfiler-per-file-coverage-integration`, and `git status --porcelain`.
  - Acceptance: the artifact records both SHAs, an empty `git status --porcelain`, and `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`.
- [ ] [P0-T11] Compute and record the SHA-256 of each of the five frozen test files into `<FEATURE>/evidence/baseline/frozen-test-files.<TS>.md`: `QuickFiler.Test/Controllers/QfcHomeControllerIterationTests.cs`, `QfcInitEmailQueueZeroBatchTests.cs`, `QfcHighConfidencePreFilterTests.cs`, `QfcFormControllerTests.cs`, `QfcHomeControllerIssue218Tests.cs`.
  - Acceptance: five SHA-256 values recorded with the exact `Get-FileHash` command and `EXIT_CODE`.
- [ ] [P0-T12] Measure and record the current physical line count of every file this child may touch into `<FEATURE>/evidence/baseline/file-line-counts.<TS>.md`: the five in-scope production files, `QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs`, and `QuickFiler.Test/Controllers/QfcHomeControllerIterationTests.cs`.
  - Acceptance: a table of file path and measured line count with the exact command used. `QfcHomeController.cs` is expected at 487; any other value means the base has moved and [P1-T1]'s line range must be re-derived from the current `#region Public Properties` boundaries rather than from the literal 406-485.
- [ ] [P0-T13] Resolve and record the toolchain into `<FEATURE>/evidence/baseline/toolchain-resolution.<TS>.md`: run `dotnet tool restore` (root `dotnet-tools.json` pins csharpier 1.2.6); resolve `msbuild` through the same `vswhere.exe` lookup (`-latest -products * -find MSBuild\**\Bin\MSBuild.exe`, the VS 18 full-framework MSBuild, **not** the .NET Core SDK `dotnet msbuild`); resolve `vstest.console.exe` through `vswhere.exe`; confirm the `dotnet-coverage` global tool is installed (install it if absent).
  - Acceptance: the artifact records a resolved, runnable invocation for all four tools with each command's `EXIT_CODE`. This task must complete before any later task that runs a toolchain command.
- [ ] [P0-T14] Run `dotnet tool run csharpier check .` and record `<FEATURE>/evidence/baseline/csharpier-check.<TS>.md`.
  - Acceptance: artifact contains `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the count of files reported unformatted.
- [ ] [P0-T15] Run the analyzer build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and record `<FEATURE>/evidence/baseline/msbuild-analyzers.<TS>.md`.
  - Acceptance: artifact contains all four required fields, with warning and error counts in `Output Summary:`.
- [ ] [P0-T16] Run the nullable build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and record `<FEATURE>/evidence/baseline/msbuild-nullable.<TS>.md`.
  - Acceptance: artifact contains all four required fields, with warning and error counts in `Output Summary:`.
- [ ] [P0-T17] Run the coverage-enabled full-suite test `pwsh -File scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput <FEATURE>\evidence\baseline\coverage-baseline.cobertura.xml` and record `<FEATURE>/evidence/baseline/vstest-coverage.<TS>.md`.
  - Acceptance: the Cobertura XML exists at the stated canonical path, and `Output Summary:` records **numeric** headline values — total tests, passed, failed, skipped, and the repository-wide `line-rate` and `branch-rate` from the Cobertura root `<coverage>` element expressed as percentages. AC2, AC18, and AC26 are evaluated against this run.
- [ ] [P0-T18] Run F1's per-file coverage harness (command recorded in [P0-T9]) against the baseline Cobertura from [P0-T17] and record `<FEATURE>/evidence/baseline/per-file-coverage.<TS>.md` with the numeric line rate and branch rate for `QuickFiler/Controllers/QfcHomeController.cs`, `QfcHomeController.Metrics.cs`, and `QfcHomeController.Iteration.cs`, plus the harness's zero-line row (or `not measured` label) for `QuickFiler/Controllers/IQfcHomeController.cs` and `QuickFiler/Interfaces/IFilerHomeController.cs`.
  - Acceptance: five rows with numeric values or the ledger's zero-line format, no placeholders such as `UNVERIFIED`, plus `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`. These are the baseline figures AC3-AC7 compare against. The indicative research percentages must not be substituted for any of them.

---

### Phase 1 — Mandatory Partial Split of QfcHomeController.cs

- [ ] [P1-T1] Create `QuickFiler/Controllers/QfcHomeController.Properties.cs` declaring `namespace QuickFiler.Controllers` and `public partial class QfcHomeController` and containing the **verbatim** member text of the `#region Public Properties` block currently at `QuickFiler/Controllers/QfcHomeController.cs:406-485`, plus the `using` directives those members require.
  - Moved members: `_explorerController`/`ExplorerController`, `_formController`/`FormController`, `_keyboardHandler`/`KeyboardHandler`, `_datamodel`/`DataModel`, `FilerQueue`, `_uiScheduler`/`UiScheduler`, `_stopWatchMoved`, `_stopWatch`, `StopWatch`, `_formViewer`, `CreateCancellationToken()`, `_tokenSource`/`TokenSource`, `_token`/`Token`, `_workerComplete`/`WorkerComplete`, `_uiSyncContext`/`UiSyncContext`.
  - Acceptance: the file exists, declares the partial class, and **no declaration text is altered** — accessibility modifiers, `internal set` on `DataModel`, `private set` on `WorkerComplete`, and the `FilerQueue` initializer are all reproduced exactly.
- [ ] [P1-T2] Delete lines 406-485 (the `#region Public Properties` block including its `#region` / `#endregion` markers) from `QuickFiler/Controllers/QfcHomeController.cs`, leaving the rest of the file untouched.
  - Acceptance: `QfcHomeController.cs` no longer contains `#region Public Properties`; `[assembly: InternalsVisibleTo("QuickFiler.Test")]` **remains** in `QfcHomeController.cs` (the whole child's test access depends on that attribute staying in this compiled file); `public bool Loaded` at former line 400 remains in `QfcHomeController.cs`.
- [ ] [P1-T3] Add exactly one line `<Compile Include="Controllers\QfcHomeController.Properties.cs" />` to `QuickFiler/QuickFiler.csproj`, inserted immediately adjacent to the three existing `QfcHomeController*` entries at lines 325-327.
  - Acceptance: `git diff QuickFiler/QuickFiler.csproj` shows exactly one added line and zero other changes — no reordering, no whitespace normalization, no property edit. This ItemGroup is a known wave-1 merge-conflict hotspot (F9, F11, F13 are expected to add entries in the same wave); the single-line adjacent insertion is the mitigation.
- [ ] [P1-T4] Measure the physical line count of `QuickFiler/Controllers/QfcHomeController.cs` and `QuickFiler/Controllers/QfcHomeController.Properties.cs` and record `<FEATURE>/evidence/qa-gates/file-line-counts-after-split.<TS>.md`.
  - Acceptance: both counts are recorded numerically and both are `<= 500`. `QfcHomeController.cs` is expected at ~407 (487 − 80), leaving headroom for the Phase 2 seams.
- [ ] [P1-T5] Prove the split is compile- and behaviour-neutral: run the analyzer build, then run the eight existing home-controller suites scoped with `/TestCaseFilter:"FullyQualifiedName~QfcHomeController"`, and record `<FEATURE>/evidence/regression-testing/split-neutrality.<TS>.md`.
  - Acceptance: the analyzer build exits 0, the scoped run reports zero failures, and the pass count equals the baseline pass count for the same filter. A partial class is one `TypeDef` and the CLR interface map is per-type, so no interface contract, vtable layout, or reflection-based test resolution changes; this task is the empirical confirmation of that argument.

---

### Phase 2 — QfcHomeController.cs Seams and Coverage

Production file: `QuickFiler/Controllers/QfcHomeController.cs`. New test files, all under `QuickFiler.Test/Controllers/`: `QfcHomeControllerLifecycleTests.cs`, `QfcHomeControllerWorkerCompletionTests.cs`, `QfcHomeControllerMetricsConsumerTests.cs`, `QfcHomeControllerModeGuardTests.cs`.

Tests this phase must NOT write (each would contradict a #424 or #218 pin): the retired two-argument `DequeueNextItemGroupAsync(itemsPerIteration, 1000)` at the pre-UI call site; any "harmonized" poll interval (`200` at the post-UI site or `2000` at the pre-UI site); any duplicate or contradiction of the 0→30 progress-band assertions; any assertion that the legacy synchronous `Run()` uses the deadline/progress overload; any assertion that `HighConfidencePreFilterLoader` is invoked from `RunAsync`; and any test of the provably dead null arm of `progress?.Report(100)` at `QfcHomeController.cs:312`.

- [ ] [P2-T1] Add seam **S1** to `QuickFiler/Controllers/QfcHomeController.cs`: `internal Action<string> ShowUserMessage { get; set; } = msg => MessageBox.Show(msg);` with an XML doc comment, and replace the direct `MessageBox.Show(msg)` call in the `e.Error != null` arm of `Worker_RunWorkerCompleted` with an invocation of the seam.
  - Acceptance: the property exists with that exact default expression, the former `MessageBox.Show` call site now calls `ShowUserMessage`, and the analyzer build exits 0. Rung 2 (injectable delegate) is chosen under the `.claude/rules/csharp.md` § DI Seams carve-out for "a single call path when a full interface is excessive": one call path, one `string` parameter, no expected second implementation. `Tags/IUserPrompt.cs` is not reusable because QuickFiler does not reference the Tags project.
- [ ] [P2-T2] Add seam **S2** to `QuickFiler/Controllers/QfcHomeController.cs`: `internal Func<string, string[], string, CancellationToken, Task> MetricsFileWriter { get; set; } = FileIO2.WriteTextFileAsync;` with an XML doc comment, and replace the direct `FileIO2.WriteTextFileAsync(...)` call in `TimedConsumerAsync` with an invocation of the seam.
  - Acceptance: the property exists with a **method-group** default (so no default lambda body is left uncovered), the former static call site now calls `MetricsFileWriter`, and the analyzer build exits 0. S2 is async / four-argument and is a distinct seam from the synchronous / three-argument `MetricsLineWriter` added in [P3-T2]; both are required and neither reuses `EfcHomeControllerDependencies`, which is sibling-F8-owned.
- [ ] [P2-T3] Measure `QuickFiler/Controllers/QfcHomeController.cs` after S1 and S2 and append the result to `<FEATURE>/evidence/qa-gates/file-line-counts-after-split.<TS>.md`.
  - Acceptance: the measured count is recorded numerically and is `<= 500` (expected ~422).
- [ ] [P2-T4] Create `QuickFiler.Test/Controllers/QfcHomeControllerLifecycleTests.cs` with `[TestClass]`, the `using` set, and a `[TestInitialize]` fixture supplying the mocked `IApplicationGlobals` graph used by the existing home-controller suites, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj` adjacent to the existing `QfcHomeController*` entries at lines 125-131.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P2-T5] Create `QuickFiler.Test/Controllers/QfcHomeControllerWorkerCompletionTests.cs` with `[TestClass]` and a `[TestInitialize]` fixture, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P2-T6] Create `QuickFiler.Test/Controllers/QfcHomeControllerMetricsConsumerTests.cs` with `[TestClass]` and a `[TestInitialize]` fixture, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P2-T7] Create `QuickFiler.Test/Controllers/QfcHomeControllerModeGuardTests.cs` with `[TestClass]` and a `[TestInitialize]` fixture, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P2-T8] Add **TC1** `PrivateParameterlessConstructor_LeavesEveryLoaderSeamAtItsNonNullDefault` to `QfcHomeControllerLifecycleTests.cs`.
  - Arrange: nothing. Act: `Activator.CreateInstance(typeof(QfcHomeController), BindingFlags.Instance | BindingFlags.NonPublic, null, null, null)`. Assert: the instance is a `QfcHomeController`; each of the seven loader properties `.Should().NotBeNull()`; `Loaded.Should().BeFalse()`; `FilerQueue.Should().NotBeNull()`.
  - Acceptance: the single named test exists and passes. It covers the private parameterless constructor and lifts the seven lambda-cache conditions that are the dominant driver of the file's branch rate.
- [ ] [P2-T9] Add **TC2** `CreateCancellationToken_AssignsFreshSourceAndItsMatchingToken` to `QfcHomeControllerLifecycleTests.cs`.
  - Arrange: controller from the public constructor. Act: `controller.CreateCancellationToken()`. Assert: `TokenSource.Should().NotBeNull()`; `Token.Should().Be(TokenSource.Token)`; `Token.IsCancellationRequested.Should().BeFalse()`.
  - Acceptance: the single named test exists and passes. This is the test that carries `QfcHomeController.Properties.cs` to its AC7 target, because `CreateCancellationToken()` moved into that file in [P1-T1].
- [ ] [P2-T10] Add **TC3** `CreateCancellationToken_CalledTwice_ReplacesSourceWithoutCancellingThePrevious` to `QfcHomeControllerLifecycleTests.cs`.
  - Arrange: call once, capture `first = TokenSource` and `firstToken = Token`. Act: call again. Assert: `TokenSource.Should().NotBeSameAs(first)`; `first.IsCancellationRequested.Should().BeFalse()`; `Token.Should().NotBe(firstToken)`.
  - Acceptance: the single named test exists and passes. State-transition scenario required by AC23; distinct post-condition set from TC2.
- [ ] [P2-T11] Add **TC16** `QfcExplorerControllerLoaderDefault_ConstructsAnExplorerControllerFromGlobals` to `QfcHomeControllerLifecycleTests.cs`.
  - Arrange: the fixture's globals mock, whose `Ol.App.ActiveExplorer()` is already stubbed. Act: invoke `controller.QfcExplorerControllerLoader(QfEnums.InitTypeEnum.Sort, globals, controller)`. Assert: result `.Should().NotBeNull()` and `.Should().BeAssignableTo<IQfcExplorerController>()`.
  - Acceptance: the single named test exists and passes. `QfcExplorerController` is sibling-F6-owned and is only constructed here, never edited.
- [ ] [P2-T12] Add **TC17** `HighConfidencePreFilterLoaderDefault_WithEmptyBatch_ReturnsEmptyWithoutScoring` to `QfcHomeControllerLifecycleTests.cs`.
  - Arrange: fixture only. Act: `await controller.HighConfidencePreFilterLoader(new List<MailItem>(), globals, 0.90, CancellationToken.None)`. Assert: result `.Should().BeEmpty()`.
  - Acceptance: the single named test exists and passes. It exercises the **delegate in isolation**, never through `RunAsync` — that distinction is essential, because issue #218 established that the pre-filter must not run in `RunAsync`. Distinct from the existing `HighConfidencePreFilterLoader_CanBeOverridden_ForTesting`, which never touches the default body.
- [ ] [P2-T13] Add **TC18** `QfcFormControllerLoaderDefault_ConstructsAndInitializesAgainstAMockViewer` to `QfcHomeControllerLifecycleTests.cs` — **buffer task with an authorized skip branch**.
  - Precondition check: read `QuickFiler/Controllers/QfcFormController.SetupDisposal.cs` and confirm the four early-return null guards at lines 24-30, 50-57, 77-80, and 151-154 are present and unnarrowed.
  - If present: Arrange a loose `Mock<IApplicationGlobals>`, a loose `Mock<IQfcFormViewer>` returning `null` for `L1v0L2L3v_TableLayout`, `Panels`, `Buttons`, and `Controls`, a loose `Mock<IQfcQueue>`, and a controller whose `KeyboardHandler` is left `null`. Act: invoke the default `QfcFormControllerLoader` delegate. Assert: result `.Should().NotBeNull()` and no exception is thrown.
  - If absent or narrowed: **drop the test** and record a one-line waiver in `<FEATURE>/evidence/other/tc18-waiver.<TS>.md` naming the changed guard. **Do not edit any F6-owned file under any circumstance.**
  - Acceptance: either the named test exists and passes, or the waiver artifact exists. The phase's coverage target must hold in the waiver case, and it does.
- [ ] [P2-T14] Add **TC4** `WorkerRunWorkerCompleted_WhenCancelled_DoesNotEnableViewerControlsAndLeavesWorkerCompleteFalse` to `QfcHomeControllerWorkerCompletionTests.cs`.
  - Arrange: a loose `Mock<IQfcFormViewer>` with `ItemsPerLoadEnabled` and `SkipButtonEnabled` set up as properties defaulting `false`; reflection-inject `_formViewer`. **Do not call `UiThread.Init`.** Act: reflection-invoke `Worker_RunWorkerCompleted(null, new RunWorkerCompletedEventArgs(null, null, cancelled: true))`. Assert: both viewer flags `.Should().BeFalse()`; `controller.WorkerComplete.Should().BeFalse()`.
  - Acceptance: the single named test exists and passes, and no modal dialog appears during the run.
- [ ] [P2-T15] Add **TC5** `WorkerRunWorkerCompleted_WhenErrorPresent_ReportsTheFormattedErrorThroughTheNotificationSeam` to `QfcHomeControllerWorkerCompletionTests.cs`.
  - Arrange: `string captured = null; controller.ShowUserMessage = m => captured = m;` plus a loose viewer mock. Act: reflection-invoke the handler with `new RunWorkerCompletedEventArgs(null, new InvalidOperationException("boom"), false)`. Assert: `captured.Should().Be("An error occurred: boom")`; both viewer flags still `false`; `WorkerComplete.Should().BeFalse()`.
  - Acceptance: the single named test exists and passes; no `MessageBox` is shown, which is the entire purpose of seam S1.
- [ ] [P2-T16] Add **TC6** `ShowUserMessageSeam_DefaultsToANonNullNotifier` to `QfcHomeControllerWorkerCompletionTests.cs`.
  - Arrange: a fresh controller. Assert: `controller.ShowUserMessage.Should().NotBeNull()`. The default is never invoked.
  - Acceptance: the single named test exists and passes. Required by AC13 (production default pinned non-null without invoking a prohibited side effect).
- [ ] [P2-T17] Add **TC7** `TimedConsumerAsync_WhenMetricsPendingAndMyDocumentsResolves_WritesTheSessionFileThroughTheSeam` to `QfcHomeControllerMetricsConsumerTests.cs`.
  - Arrange: loose globals with `FS.SpecialFolders["MyDocuments"] = @"C:\Fake"` and `FS.Filenames.EmailSession = "session.csv"`; a recording `MetricsFileWriter` capturing all four arguments; reflection-obtain `_metrics`, `Add("line-1")`, `Add("line-2")`, then `CompleteAdding()`. Act: reflection-invoke `TimedConsumerAsync(null, null)`. Assert: the seam was invoked once with filename `"session.csv"`, payload `["line-1","line-2"]`, folder `@"C:\Fake"`.
  - Acceptance: the single named test exists and passes, and **no real file is written**. `CompleteAdding()` is mandatory in Arrange because `GetConsumingEnumerable().ToArray()` otherwise blocks indefinitely — a property of the production code, not of the seam. Label **CHARACTERIZATION** in the test's own summary comment naming **#442**: `TimedConsumerAsync` is unreachable in production.
- [ ] [P2-T18] Add **TC8** `TimedConsumerAsync_WhenNoMetricsArePending_DoesNotInvokeTheWriterSeam` to `QfcHomeControllerMetricsConsumerTests.cs`.
  - Arrange: as TC7 but call `CompleteAdding()` with nothing added. Act: reflection-invoke. Assert: the seam was never invoked.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** naming **#442**.
- [ ] [P2-T19] Add **TC9** `TimedConsumerAsync_WhenMyDocumentsIsNotResolvable_DoesNotInvokeTheWriterSeam` to `QfcHomeControllerMetricsConsumerTests.cs`.
  - Arrange: as TC7 but with an empty `SpecialFolders` dictionary. Act: reflection-invoke. Assert: the seam was never invoked.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** naming **#442**.
- [ ] [P2-T20] Add **TC10** `TimedConsumerAsync_WhenTheWriterSeamThrows_PropagatesTheException` to `QfcHomeControllerMetricsConsumerTests.cs`.
  - Arrange: as TC7 but the seam throws synchronously. Act: reflection-invoke. Assert: a `TargetInvocationException` whose `InnerException` is the seeded exception. Deterministic because the seam completes synchronously, so no true async suspension occurs and the `async void` never escapes to a captured context.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** naming **#442**.
- [ ] [P2-T21] Add **TC11** `TimedConsumerAsync_DecrementsTheMetricsConsumerCount` to `QfcHomeControllerMetricsConsumerTests.cs`.
  - Arrange: as TC7, reflection-reading `_metricsConsumers` before the act. Act: reflection-invoke. Assert: the value decreased by exactly 1.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** naming **#442**; it documents the decrement-only counter and must not assert that the behaviour is correct.
- [ ] [P2-T22] Add **TC12** `Run_WhenGlobalsIsNull_UsesTheFullInitializationBatchAndSkipsStreamingDequeue` to `QfcHomeControllerModeGuardTests.cs`.
  - Arrange: `Mock<IQfcDatamodel>` returning an empty list from `InitEmailQueue`; `Mock<IQfcFormController>` with `ItemsPerIteration = 5`; `Mock<IQfcFormViewer>`; set `controller.Globals = null` through the `internal` setter. Act: `controller.Run()`. Assert: `InitEmailQueue(5, It.IsAny<BackgroundWorker>())` `Times.Once`; `DequeueNextItemGroupAsync` never invoked.
  - Acceptance: the single named test exists and passes. Closes the `Globals == null` half of the `Globals?.QfSettings?.HighConfidenceModeEnabled == true` guard in `Run()`. Satisfies part of US1.
- [ ] [P2-T23] Add **TC13** `Run_WhenQfSettingsIsNull_UsesTheFullInitializationBatchAndSkipsStreamingDequeue` to `QfcHomeControllerModeGuardTests.cs`.
  - Arrange: as TC12 but with a globals mock whose `QfSettings` returns `null`. Act / Assert: as TC12.
  - Acceptance: the single named test exists and passes. Distinct short-circuit point from TC12; both are required because the two `?.` operators are independent branch points in the Cobertura branch report.
- [ ] [P2-T24] Add **TC14** `RunAsync_WhenGlobalsIsNull_UsesTheFullInitializationBatchAndSkipsStreamingDequeue` to `QfcHomeControllerModeGuardTests.cs`.
  - Arrange: a mocked `ProgressTracker` following the existing `SetupMockProgressTracker` pattern; a data model returning empty from `InitEmailQueueAsync` with `Complete == true`; form controller `ItemsPerIteration = 5`; `controller.Globals = null`. Act: `await controller.RunAsync(progress)`. Assert: `InitEmailQueueAsync(5, ...)` `Times.Once`; the four-argument `DequeueNextItemGroupAsync` never invoked.
  - Acceptance: the single named test exists and passes. It must not assert anything about the four-argument overload's arguments, the 0→30 progress band, or the pre-filter — all are owned by #424 and #218 suites.
- [ ] [P2-T25] Add **TC15** `RunAsync_WhenQfSettingsIsNull_UsesTheFullInitializationBatchAndSkipsStreamingDequeue` to `QfcHomeControllerModeGuardTests.cs`.
  - Arrange: as TC14 but with `QfSettings` returning `null`. Act / Assert: as TC14.
  - Acceptance: the single named test exists and passes.
- [ ] [P2-T26] Measure the physical line count of all four new test files created in this phase and append to `<FEATURE>/evidence/qa-gates/test-file-line-counts.<TS>.md`.
  - Acceptance: four counts recorded numerically, each `<= 500`. If any file exceeds 450, split it into a second file along its test-group boundary and record the split in the same artifact before proceeding.
- [ ] [P2-T27] Run F1's per-file coverage harness (command from [P0-T9]) after this phase's changes and record `<FEATURE>/evidence/qa-gates/per-file-coverage-qfchomecontroller.<TS>.md` with the numeric line rate and branch rate for `QuickFiler/Controllers/QfcHomeController.cs` and `QuickFiler/Controllers/QfcHomeController.Properties.cs`.
  - Acceptance: both files have numeric figures recorded with `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`. `QfcHomeController.cs` is `>= 80%` line and `>= ` its [P0-T18] baseline line rate, with its branch rate recorded and `>= ` its baseline branch rate (AC3). `QfcHomeController.Properties.cs` is `>= 90%` line as a newly created file (AC7). No indicative research figure may be substituted for any measured value.

---

### Phase 3 — QfcHomeController.Metrics.cs Seams and Coverage

Production file: `QuickFiler/Controllers/QfcHomeController.Metrics.cs` (234 lines, ~266 of headroom). New test files, all under `QuickFiler.Test/Controllers/`: `QfcHomeControllerMetricsCoverageTests.cs`, `QfcHomeControllerMetricsCalendarTests.cs`, `QfcHomeControllerMetricsProducerTests.cs`, `QfcHomeControllerMetricsDurationTests.cs`. The four-file split is planned up front rather than discovered later: 17 test cases appended to a single file would project past 500 lines, and appending them to the existing 421-line `QfcHomeControllerMetricsTests.cs` (79 lines of headroom) would breach the limit outright.

Framework-behaviour assumptions to confirm at implementation time, each with its documented fallback: `BlockingCollection<T>.TryAdd(item, 20, ct)` throwing for an already-cancelled token (fallback: route the case through the `MetricsAdder` seam); the interop `Items.Add` optional `object Type` parameter's Moq setup shape (reference: existing `Mock<Items>` usages in `TaskMaster.Test` and `UtilitiesCS.Test`); and Moq's inability to match a specific instance for a `ref` parameter (fallback: capture the argument in a `Callback`).

- [ ] [P3-T1] Add seam **M1** to `QuickFiler/Controllers/QfcHomeController.Metrics.cs`: `internal static (double duration, string durationText, string durationMinutesText) BuildDurationTexts(TimeSpan elapsed, int emailsLoaded, IFormatProvider formatProvider = null)`, and rewire both former inline call sites (the `QuickFileMetrics_WRITE` block at source lines 42/48-56 and the `WriteMetricsAsync` block at 121/127-135) to delegate to it.
  - Acceptance: the method exists; its body reproduces current semantics **exactly** — `elapsed.Seconds` (not `TotalSeconds`), the `emailsLoaded > 0` guard, the `"##0"` and `"##0.00"` formats, and a default `formatProvider` of `CultureInfo.CurrentCulture`; both call sites delegate to it with no change in emitted values; the analyzer build exits 0. This is a **rank-0 pure-function extraction, not a seam**: `Stopwatch.Elapsed` is a non-virtual property on a concrete class, so no interface, delegate, or adapter can control it without replacing the stopwatch field — and replacing it is blocked by two live pins (`RunAsync_ExecutesCorrectly:303` asserts `StopWatch.IsRunning`; `StopWatch_PropertyWorksCorrectly:232` asserts identity). Satisfies AC14. Additionally, in `WriteMetricsAsync` the pre-division elapsed seconds must be preserved for source line 123: capture the raw value at former line 121, compute `OlStartTime = OlEndTime.Subtract(new TimeSpan(0, 0, 0, (int)<rawElapsedSeconds>));` unchanged, and only then call `BuildDurationTexts(StopWatch.Elapsed, emailsLoaded)` after `emailsLoaded` is read at line 125. Assigning the method's returned (post-division) `duration` into the variable consumed at line 123 is a silent behaviour change and fails this task. No delivered test can pin `AppointmentItem.Start`, because `Stopwatch.Elapsed` is not injectable, so this clause is the sole control. `QuickFileMetrics_WRITE` is unaffected: its `startTime` at source line 44 is derived from `_stopWatchMoved.Elapsed` directly, not from `duration`.
- [ ] [P3-T2] Add seam **M2** to `QuickFiler/Controllers/QfcHomeController.Metrics.cs`: `internal Action<string, string[], string> MetricsLineWriter { get; set; } = FileIO2.WriteTextFile;` with an XML doc comment, and replace the direct `FileIO2.WriteTextFile(...)` call at source line 86 with an invocation of the seam.
  - Acceptance: the property exists with that method-group default, the former static call site now calls `MetricsLineWriter`, and the analyzer build exits 0. Declared **locally on `QfcHomeController`**; `EfcHomeControllerDependencies.MetricsLineWriter` is cited as ratified precedent only and is sibling-F8-owned. This seam is what makes group C testable without writing a real file: today the write is survivable only because every existing test returns `Array.Empty<string>()` from `GetMoveDiagnostics`, so the writer loop iterates zero times.
- [ ] [P3-T3] Add seam **M3** to `QuickFiler/Controllers/QfcHomeController.Metrics.cs` as a backing-field property with a lazy default (**not** a property initializer): a private field `private Func<string, int, CancellationToken, bool> _metricsAdder;`, an XML-documented `internal Func<string, int, CancellationToken, bool> MetricsAdder { get => _metricsAdder ?? DefaultMetricsAdder; set => _metricsAdder = value; }`, and `private bool DefaultMetricsAdder(string line, int timeoutMs, CancellationToken ct) => _metrics.TryAdd(line, timeoutMs, ct);`. Replace the direct `_metrics.TryAdd(...)` call at source line 211 with `success = MetricsAdder(line, 20, ct);`.
  - Acceptance: `MetricsAdder` returns a non-null delegate on a freshly constructed controller and that default delegates to `_metrics.TryAdd`; the call site at former line 211 invokes the seam; the analyzer build exits 0. **A property initializer of the form `{ get; set; } = (line, timeoutMs, ct) => _metrics.TryAdd(...)` must not be used: capturing the instance field `_metrics` in a field/property initializer is a `CS0236` compile error.** Required because the `else` branch at 219-223 is unreachable through the real `BlockingCollection<T>` — `TryAdd(T,int,CancellationToken)` only lets an `OperationCanceledException` escape when the caller's own token is cancelled, and `TryAdd` is non-virtual so subclassing cannot intercept it.
- [ ] [P3-T4] Apply **M4** to `QuickFiler/Controllers/QfcHomeController.Metrics.cs`: widen both `NonBlockingProducer` overloads (source lines 190 and 201) from `private` to `internal`.
  - Acceptance: both overloads are `internal`, the public surface is unchanged, and the analyzer build exits 0. This is a visibility widening, not a seam, and is the lowest-cost option available; `InternalsVisibleTo("QuickFiler.Test")` is already declared. It replaces `MethodInfo.Invoke`, which for `async Task` methods requires unwrapping a `Task` from `object` and converts every argument-shape mistake into a run-time `TargetInvocationException` instead of a compile error.
- [ ] [P3-T5] Apply the adopted conditional item **D7** to `QuickFiler/Controllers/QfcHomeController.Metrics.cs`: remove the duplicated `Globals.FS.SpecialFolders.TryGetValue("MyDocuments", out ...)` at source line 84 and reuse `folderRoot` from source line 33 at the write call site.
  - Justification (recorded per AC12): the `false` arm of line 84 is permanently unreachable, because line 33 already returned on exactly that condition. It is a permanently-partial branch that caps this child's own branch-coverage figure on an F7-owned file. The change is behaviour-identical by construction, and it lands in a file #424 did not touch, so it adds no merge exposure.
  - Acceptance: line 84's duplicate lookup is gone, the write call site uses `folderRoot`, the analyzer build exits 0, and every existing `QfcHomeControllerMetricsTests` test still passes.
- [ ] [P3-T6] Measure `QuickFiler/Controllers/QfcHomeController.Metrics.cs` after M1-M4 and D7 and append to `<FEATURE>/evidence/qa-gates/file-line-counts-after-split.<TS>.md`.
  - Acceptance: the measured count is recorded numerically and is `<= 500` (expected ~264).
- [ ] [P3-T7] Create `QuickFiler.Test/Controllers/QfcHomeControllerMetricsCoverageTests.cs` with `[TestClass]` and a `[TestInitialize]` fixture using `FakeTimeProvider`, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P3-T8] Create `QuickFiler.Test/Controllers/QfcHomeControllerMetricsCalendarTests.cs` with `[TestClass]` and a `[TestInitialize]` fixture that builds the Moq interop calendar graph (`NameSpace` → `Folders` → `Folder` named `"Email Time"` → `Items` → `AppointmentItem`) following the ratified pattern at `UtilitiesCS.Test/OutlookObjects/Calendar/CalendarTests.cs:58-79`, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0. No live Outlook COM object is created anywhere in the fixture.
- [ ] [P3-T9] Create `QuickFiler.Test/Controllers/QfcHomeControllerMetricsProducerTests.cs` with `[TestClass]` and a `[TestInitialize]` fixture using `FakeTimeProvider`, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P3-T10] Create `QuickFiler.Test/Controllers/QfcHomeControllerMetricsDurationTests.cs` with `[TestClass]`, and add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0. This class needs no controller fixture: `BuildDurationTexts` is a pure static.
- [ ] [P3-T11] Add Metrics **A1** `WriteMetricsAsync_WhenMyDocumentsIsAbsent_ReturnsWithoutBuildingDiagnostics` to `QfcHomeControllerMetricsCoverageTests.cs`.
  - Arrange: a loose-mock controller with an **empty** `SpecialFolders`; a `Mock<IQfcCollectionController>` behind `_formController.Groups`. Act: `await controller.WriteMetricsAsync("metrics.csv")`. Assert: `GetMoveDiagnostics` `Times.Never` and the reflection-read `_metrics` is empty.
  - Acceptance: the single named test exists and passes.
- [ ] [P3-T12] Add Metrics **A2** `QuickFileMetrics_WRITE_WhenMyDocumentsIsAbsent_AbortsBeforeDiagnostics` to `QfcHomeControllerMetricsCoverageTests.cs`.
  - Arrange: a loose-mock controller with an empty `SpecialFolders`; `_stopWatchMoved = new Stopwatch()` by reflection. Act: `controller.QuickFileMetrics_WRITE("metrics.csv")`. Assert: `GetMoveDiagnostics` `Times.Never`.
  - Acceptance: the single named test exists and passes. Distinct post-condition from the existing `GetMoveDiagnostics_NullAppointment_DoesNotThrow`, which asserts only `NotThrow`; the plan's task text states this so a reviewer does not read it as duplication. [P3-T28] retargets that existing test so the two do not overlap.
- [ ] [P3-T13] Add Metrics **A9** `TimeProvider_WhenNotInjected_DefaultsToSystemProvider` to `QfcHomeControllerMetricsCoverageTests.cs`.
  - Arrange / Act: construct a `QfcHomeController` with loose-mocked globals. Assert: `controller.TimeProvider.Should().BeSameAs(TimeProvider.System)`.
  - Acceptance: the single named test exists and passes. Required by AC13 (`TimeProvider` must be shown to default to `TimeProvider.System`).
- [ ] [P3-T14] Add Metrics **C1** `QuickFileMetrics_WRITE_WithDiagnosticLines_InvokesInjectedWriterWithMyDocumentsRoot` to `QfcHomeControllerMetricsCoverageTests.cs`.
  - Arrange: a recording `MetricsLineWriter`; `SpecialFolders["MyDocuments"] = @"C:\FakeDocs"`; `GetMoveDiagnostics` returning `["l1","l2"]`. Act: `controller.QuickFileMetrics_WRITE("metrics.csv")`. Assert: exactly one write with filename `"metrics.csv"`, lines `["l1","l2"]`, folder root `@"C:\FakeDocs"`.
  - Acceptance: the single named test exists and passes, and **no file is created on disk** — the entire reason seam M2 exists. Also satisfies the AC13 obligation that `MetricsLineWriter` is pinned non-null via its injected form.
- [ ] [P3-T15] Add Metrics **A3** `QuickFileMetrics_WRITE_WhenEmailTimeCalendarExists_CreatesPrivateAppointmentForFiledEmails` to `QfcHomeControllerMetricsCalendarTests.cs`.
  - Arrange: the fixture calendar graph, with `Items.Add(It.IsAny<object>())` returning a `Mock<AppointmentItem>`; `EmailsToMove = 3`; `_stopWatchMoved = new Stopwatch()`; a fixed `FakeTimeProvider`. Act: `controller.QuickFileMetrics_WRITE("metrics.csv")`. Assert: `VerifySet` on the appointment for `Subject == "Quick Filed 3 emails"`, `End == ` the fake local now, `Categories == "@ Email"`, `ReminderSet == false`, `Sensitivity == OlSensitivity.olPrivate`, and `Verify(x => x.Save(), Times.Once)`.
  - Acceptance: the single named test exists and passes with no live COM object.
- [ ] [P3-T16] Add Metrics **A4** `WriteMetricsAsync_WhenEmailTimeCalendarExists_PassesCreatedAppointmentToDiagnostics` to `QfcHomeControllerMetricsCalendarTests.cs`.
  - Arrange: the same calendar graph; `_stopWatch = new Stopwatch()`; `GetMoveDiagnostics` returning `Array.Empty<string>()`. Act: `await controller.WriteMetricsAsync("metrics.csv")`. Assert: `Save()` once **and** the `ref AppointmentItem` handed to `GetMoveDiagnostics` is the mocked appointment, captured through a `Callback` on the `ref It.Ref<AppointmentItem>.IsAny` setup rather than through an argument matcher.
  - Acceptance: the single named test exists and passes. Distinct member (`WriteMoveToCalendar`) and distinct assertion from A3. Documented fallback if `ref` capture proves impractical in the pinned Moq version: assert `Save()` once and that the returned diagnostics reached the producer.
- [ ] [P3-T17] Add Metrics **A5** `WriteMetricsAsync_WithDiagnosticLines_EnqueuesEveryLineInOrder` to `QfcHomeControllerMetricsProducerTests.cs`.
  - Arrange: a loose controller with `MyDocuments` seeded; `GetMoveDiagnostics` returning `["l1","l2","l3"]`; `_stopWatch = new Stopwatch()`. Act: `await controller.WriteMetricsAsync("metrics.csv")`. Assert: the reflection-read `_metrics` `.Should().Equal("l1","l2","l3")`.
  - Acceptance: the single named test exists and passes. Disk-safe by construction: `WriteMetricsAsync` never calls `FileIO2`. Satisfies US4.
- [ ] [P3-T18] Add Metrics **A6** `NonBlockingProducer_WhenTokenAlreadyCancelled_BreaksWithoutEnqueuingLine` to `QfcHomeControllerMetricsProducerTests.cs`.
  - Arrange: a controller and a pre-cancelled `CancellationTokenSource`. Act: `await controller.NonBlockingProducer("line", cts.Token)` (reachable directly because of M4). Assert: `_metrics` is empty and no exception escapes.
  - Acceptance: the single named test exists and passes. If `BlockingCollection<T>.TryAdd` does not throw for an already-cancelled token on this framework, route the case through the `MetricsAdder` seam instead and record the substitution in the test's doc comment.
- [ ] [P3-T19] Add Metrics **A7** `NonBlockingProducerBatch_WhenTokenCancelled_ThrowsBeforeEnqueuingAnyLine` to `QfcHomeControllerMetricsProducerTests.cs`.
  - Arrange: a controller, a pre-cancelled token, and the array `["a","b"]`. Act / Assert: `await act.Should().ThrowAsync<OperationCanceledException>()` and `_metrics` is empty.
  - Acceptance: the single named test exists and passes.
- [ ] [P3-T20] Add Metrics **A8** `NonBlockingProducer_WhenTwoConsumersPending_ResetsCounterAndDoesNotThrow` to `QfcHomeControllerMetricsProducerTests.cs`.
  - Arrange: set `_metricsConsumers = 2` by reflection. Act: `await controller.NonBlockingProducer("line", CancellationToken.None)`. Assert: the reflection-read `_metricsConsumers == -1`, and the line is present in `_metrics`.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** in the test's own summary comment naming **#442**; it documents the current 2 → 0 → −1 transition and must **not** assert that the timer fires, because the timer is constructed into a local that is never started.
- [ ] [P3-T21] Add Metrics **B1** `NonBlockingProducer_WhenAddReportsTimeout_RetriesUntilItSucceeds` to `QfcHomeControllerMetricsProducerTests.cs`.
  - Arrange: `controller.MetricsAdder` returns `false` on call 1 and `true` on call 2, recording each invocation. Act: `await controller.NonBlockingProducer("line", CancellationToken.None)`. Assert: exactly 2 invocations, each with `timeoutMs == 20`, and the task completes without awaiting any delay.
  - Acceptance: the single named test exists and passes with zero wall-clock cost.
- [ ] [P3-T22] Add Metrics **B2** `NonBlockingProducer_WhenAddThrowsWithoutCancellation_AwaitsInjectedTwentyMillisecondDelay` to `QfcHomeControllerMetricsProducerTests.cs`.
  - Arrange: a `FakeTimeProvider`; `MetricsAdder` throws `OperationCanceledException` on call 1 with the token **not** cancelled, and returns `true` on call 2. Act: start the task without awaiting. Assert: `task.IsCompleted == false` before `fake.Advance(TimeSpan.FromMilliseconds(20))`, then `await task` completes and the adder was invoked twice.
  - Acceptance: the single named test exists and passes with no wall-clock wait. This test is the reason seam M3 exists — the branch is unreachable through the concrete `BlockingCollection<T>`. It drives the **production method**, so it is additive to, not a duplicate of, the existing `NonBlockingProducer_DelaySeam_HonorsInjectedTwentyMillisecondDelay`, which pins the delay expression in isolation.
- [ ] [P3-T23] Add Metrics **D1** `BuildDurationTexts_WithZeroEmailsLoaded_DoesNotDivideElapsedSeconds` to `QfcHomeControllerMetricsDurationTests.cs`.
  - Act: `BuildDurationTexts(TimeSpan.FromSeconds(30), 0, CultureInfo.InvariantCulture)`. Assert: `duration == 30` and `durationText == "30"`.
  - Acceptance: the single named test exists and passes. Closes the `emailsLoaded > 0` FALSE branch.
- [ ] [P3-T24] Add Metrics **D2** `BuildDurationTexts_WithMultipleEmailsLoaded_DividesElapsedSecondsPerEmail` to `QfcHomeControllerMetricsDurationTests.cs`.
  - Act: `BuildDurationTexts(TimeSpan.FromSeconds(30), 4, CultureInfo.InvariantCulture)`. Assert: `duration == 7.5` and `durationText == "8"`, pinning the `"##0"` rounding.
  - Acceptance: the single named test exists and passes.
- [ ] [P3-T25] Add Metrics **D3** `BuildDurationTexts_WithElapsedOverOneMinute_UsesSecondsComponentNotTotalSeconds` to `QfcHomeControllerMetricsDurationTests.cs`.
  - Act: `BuildDurationTexts(TimeSpan.FromSeconds(90), 1, CultureInfo.InvariantCulture)`. Assert: `duration == 30`.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** in the test's own summary comment naming **#443**; it documents the `TimeSpan.Seconds` truncation and must not assert that the behaviour is correct, and the truncation must not be "fixed" here.
- [ ] [P3-T26] Add Metrics **D4** `BuildDurationTexts_FormatsMinutesWithTwoDecimalPlaces` to `QfcHomeControllerMetricsDurationTests.cs`.
  - Act: `BuildDurationTexts(TimeSpan.FromSeconds(45), 1, CultureInfo.InvariantCulture)`. Assert: `durationMinutesText == "0.75"`.
  - Acceptance: the single named test exists and passes, pinning the `"##0.00"` format.
- [ ] [P3-T27] Add Metrics **D5** `BuildDurationTexts_WithNegativeEmailsLoaded_DoesNotDivide` to `QfcHomeControllerMetricsDurationTests.cs`.
  - Act: `BuildDurationTexts(TimeSpan.FromSeconds(30), -2, CultureInfo.InvariantCulture)`. Assert: `duration == 30`, because the guard is `> 0`.
  - Acceptance: the single named test exists and passes. Boundary/negative case required by AC23.
- [ ] [P3-T28] Retarget the existing vacuous test `GetMoveDiagnostics_NullAppointment_DoesNotThrow` in `QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs` (lines 161-241) so it exercises the behaviour its name describes: seed `MyDocuments` in `SpecialFolders` and assert `GetMoveDiagnostics` is called exactly once with a null `ref` appointment, and update the test's `<summary>` and inline comments to describe what it then actually exercises.
  - Written justification, recorded here per AC16 and required by the plan: as written the test arranges an **empty** `SpecialFolders`, so execution returns at `QfcHomeController.Metrics.cs:38` and never reaches `GetCalendar`, `GetMoveDiagnostics`, or any appointment — its name, summary, and inline comments all describe behaviour it does not exercise. New test A2 ([P3-T12]) now covers the abort path with a real assertion, so leaving the existing test unchanged would leave a partially-redundant and actively misleading assertion surface, which the epic's duplication rule prohibits. Retargeting preserves the coverage the test contributes while making its name true. `QfcHomeControllerMetricsTests.cs` is **not** on the frozen list (D-8), so the modification is permissible; it is nonetheless the only existing test file this child modifies.
  - Acceptance: the test's Arrange seeds `MyDocuments`, its assertion verifies `GetMoveDiagnostics` `Times.Once` with a null `ref` appointment, its summary text matches its behaviour, the test passes, and this justification is mirrored into `<FEATURE>/evidence/other/existing-test-modifications.<TS>.md`.
- [ ] [P3-T29] Measure the physical line count of the four new test files created in this phase plus the modified `QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs`, and append to `<FEATURE>/evidence/qa-gates/test-file-line-counts.<TS>.md`.
  - Acceptance: five counts recorded numerically, each `<= 500`. If any new file exceeds 450, split it along its test-group boundary and record the split in the same artifact before proceeding.
- [ ] [P3-T30] Run F1's per-file coverage harness (command from [P0-T9]) after this phase's changes and record `<FEATURE>/evidence/qa-gates/per-file-coverage-metrics.<TS>.md` with the numeric line rate and branch rate for `QuickFiler/Controllers/QfcHomeController.Metrics.cs`.
  - Acceptance: numeric figures recorded with `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`. Line coverage is `>= 80%` and `>= ` its [P0-T18] baseline; branch rate is recorded and is `>= ` its baseline branch rate (AC4). No indicative research figure may be substituted.

---

### Phase 4 — QfcHomeController.Iteration.cs Coverage

Production file: `QuickFiler/Controllers/QfcHomeController.Iteration.cs` (86 lines). **Zero production edits.** New test files, both under `QuickFiler.Test/Controllers/`: `QfcHomeControllerIterationCoverageTests.cs` and `QfcHomeControllerIterationOrderingTests.cs`. The two-file split is planned up front: the research projects a single file at ~355 lines, and splitting along the group boundary keeps both comfortably clear of 500 while isolating the `TaskCompletionSource`-gated ordering fixture.

This file is the site of the child's single largest process risk. `QuickFiler.Test/Controllers/QfcHomeControllerIterationTests.cs` is **frozen byte-unmodified** and sits directly beside the new files; the natural instinct when adding an iteration test is to append to that existing class. Every task below names its new target file explicitly.

- [ ] [P4-T1] Record the zero-production-edit constraint for this file into `<FEATURE>/evidence/other/iteration-no-production-edit.<TS>.md`, citing the research finding that every dependency this file reaches already resolves at seam rung 1 (`IQfcDatamodel` declares `Complete`, both `DequeueNextItemGroupAsync` overloads, and `DequeueNextItemGroup`; `IQfcQueue` declares `EnqueueAsync`, `CompleteAddingAsync`, and `Dequeue`; `IQfcFormController` declares `ItemsPerIteration`, `Groups`, and both `LoadItems` overloads), plus the `internal` `Globals` setter and `internal CreateCancellationToken()` with the public `TokenSource` getter.
  - Acceptance: the artifact exists and explicitly excludes the four rejected production changes — named constants for the `2000` / `10000` literals, a null guard on the dequeue result, deletion of `Iterate()` / `Iterate2()`, and an `internal Task LastIterationTask` on `Iterate2`.
- [ ] [P4-T2] Create `QuickFiler.Test/Controllers/QfcHomeControllerIterationCoverageTests.cs` with `[TestClass]`, a lean `[TestInitialize]` fixture on the `QfcHomeControllerIssue218Tests` model, and a shared private `ArrangeIteration(...)` builder; add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0. `QfcHomeControllerIterationTests.cs` is not opened for writing at any point.
- [ ] [P4-T3] Create `QuickFiler.Test/Controllers/QfcHomeControllerIterationOrderingTests.cs` with `[TestClass]`, a `[TestInitialize]` fixture, and a shared `TaskCompletionSource`-gating helper; add its `<Compile Include>` entry to `QuickFiler.Test/QuickFiler.Test.csproj`.
  - Acceptance: the file exists with zero `[TestMethod]` members, the csproj entry exists, and the analyzer build exits 0.
- [ ] [P4-T4] Add Iteration **A1** `IterateQueueAsync_WhenTokenAlreadyCancelled_ThrowsBeforeReadingTheDataModel` to `QfcHomeControllerIterationCoverageTests.cs`.
  - Arrange: `controller.CreateCancellationToken(); controller.TokenSource.Cancel();` plus a `Mock<IQfcDatamodel>` and a `Mock<IQfcQueue>` with no `Complete` setup. Act: `Func<Task> act = () => controller.IterateQueueAsync();` Assert: `await act.Should().ThrowAsync<OperationCanceledException>()` **and** `mockDataModel.VerifyGet(m => m.Complete, Times.Never)`.
  - Acceptance: the single named test exists and passes. Pins that the entry guard sits **outside** the `try` and therefore propagates, unlike A2. Closes the throwing arm of the entry `Token.ThrowIfCancellationRequested()` at source line 13 (AC6).
- [ ] [P4-T5] Add Iteration **A2** `IterateQueueAsync_WhenDequeueThrowsOperationCanceled_CompletesWithoutFaulting` to `QfcHomeControllerIterationCoverageTests.cs`.
  - Arrange: `Complete == false`; `DequeueNextItemGroupAsync(It.IsAny<int>(), It.IsAny<int>())` throws `OperationCanceledException`; the token is **not** cancelled; the form-controller mock supplies `ItemsPerIteration` and `Groups`. Act: `await controller.IterateQueueAsync()`. Assert: does not throw; `EnqueueAsync` `Times.Never`; `CompleteAddingAsync` `Times.Never`.
  - Acceptance: the single named test exists and passes. Closes the `catch (OperationCanceledException)` handler at source lines 38-41 (AC6). Documented fallback if `Throws` on the async setup proves awkward: return `Task.FromCanceled<IList<MailItem>>(alreadyCancelledToken)`, which surfaces `TaskCanceledException` at the await and reaches the same handler. Must **not** assert that the empty-batch → `CompleteAddingAsync` routing is correct.
- [ ] [P4-T6] Add Iteration **A3** `IterateQueueAsync_WhenEnqueueFaultsAfterCancellationRequested_SwallowsTheException` to `QfcHomeControllerIterationCoverageTests.cs`.
  - Arrange: `controller.CreateCancellationToken()` (not yet cancelled); `Complete == false`; the dequeue returns two mocked `MailItem`s; `EnqueueAsync` is set up as `.Callback(() => controller.TokenSource.Cancel()).Throws(new InvalidOperationException("com failure"))` so the token flips to cancelled **before** the non-OCE exception is raised inside the `try`. Act / Assert: does not throw, and `EnqueueAsync` `Times.Once` proves the handler was actually entered.
  - Acceptance: the single named test exists and passes. Closes the `Token.IsCancellationRequested == true` swallow at source lines 42-47 (AC6). Documented fallback if Moq's `Callback`-before-`Throws` ordering does not hold in the pinned version: cancel the source before the act and arrange the **dequeue** rather than the enqueue to throw.
- [ ] [P4-T7] Add Iteration **A4** `IterateQueueAsync_WhenEnqueueFaultsWithoutCancellation_RethrowsTheOriginalException` to `QfcHomeControllerIterationCoverageTests.cs`.
  - Arrange: identical to A3 minus the cancel callback; the token is never cancelled. Act / Assert: `await act.Should().ThrowAsync<InvalidOperationException>().WithMessage("com failure")`.
  - Acceptance: the single named test exists and passes. Closes the `throw;` rethrow at source lines 48-51 (AC6) and pins the bare-rethrow type and message preservation. Satisfies US2's fourth outcome.
- [ ] [P4-T8] Add Iteration **B1** `Iterate_WhenGlobalsIsNull_UsesTheSynchronousDequeuePath` to `QfcHomeControllerIterationCoverageTests.cs`.
  - Arrange: `controller.Globals = null`; `DequeueNextItemGroup(It.IsAny<int>())` returns two mocked items; a form-controller mock. Act: `controller.Iterate()`. Assert: `DequeueNextItemGroup` `Times.Once`; `DequeueNextItemGroupAsync(It.IsAny<int>(), It.IsAny<int>())` `Times.Never`; `LoadItems` called once with those two items.
  - Acceptance: the single named test exists and passes. Closes the first null-conditional short-circuit of `Globals?.QfSettings?.HighConfidenceModeEnabled` at source line 60 (AC6).
- [ ] [P4-T9] Add Iteration **B2** `Iterate_WhenQuickFilerSettingsAreAbsent_UsesTheSynchronousDequeuePath` to `QfcHomeControllerIterationCoverageTests.cs`.
  - Arrange: a globals mock with `SetupGet(x => x.QfSettings).Returns((IAppQuickFilerSettings)null)`. Act / Assert: as B1.
  - Acceptance: the single named test exists and passes. Closes the second null-conditional short-circuit at source line 60 (AC6). Distinct arrange and distinct short-circuit point from B1; both are required because the two `?.` operators are independent branch points in the Cobertura branch report.
- [ ] [P4-T10] Add Iteration **C1** `Iterate_ReplacesTheSessionStopwatchWithAFreshRunningInstance` to `QfcHomeControllerIterationOrderingTests.cs`.
  - Arrange: seed `_stopWatch` by reflection with a **stopped** `Stopwatch`; high-confidence disabled; datamodel and form-controller mocks. Act: `controller.Iterate()`. Assert: `controller.StopWatch.Should().NotBeSameAs(seeded)` and `controller.StopWatch.IsRunning.Should().BeTrue()`.
  - Acceptance: the single named test exists and passes.
- [ ] [P4-T11] Add Iteration **C2** `Iterate2_ReplacesTheSessionStopwatchWithAFreshRunningInstance` to `QfcHomeControllerIterationOrderingTests.cs`.
  - Arrange: seed `_stopWatch` as in C1; `Complete == true` so the fire-and-forget completes synchronously; queue and form-controller mocks. Act: `controller.Iterate2()`. Assert: as C1.
  - Acceptance: the single named test exists and passes.
- [ ] [P4-T12] Add Iteration **C3** `SwapStopWatch_PreservesTheOutgoingElapsedTimeAndStartsAFreshInstance` to `QfcHomeControllerIterationOrderingTests.cs`.
  - Arrange: seed `_stopWatch` with a **started** `Stopwatch`. Act: `controller.SwapStopWatch()`. Assert: the reflection-read `_stopWatchMoved` is reference-equal to the seeded instance; `controller.StopWatch` is `NotBeSameAs` the seeded instance **and** `IsRunning == true`.
  - Acceptance: the single named test exists and passes. **Not a duplicate of the frozen `QfcHomeControllerIterationTests.SwapStopWatch_ExecutesCorrectly`**, which asserts only the first of these three post-conditions; this test adds the fresh-instance and running post-conditions, which nothing pins today. Satisfies US5. The frozen file is not modified.
- [ ] [P4-T13] Add Iteration **D1** `Iterate2_WhenBackgroundRefillIsPending_ReturnsWithoutWaitingForTheEnqueue` to `QfcHomeControllerIterationOrderingTests.cs`.
  - Arrange: `dequeueGate = new TaskCompletionSource<IList<MailItem>>()`; `Complete == false`; `DequeueNextItemGroupAsync(It.IsAny<int>(), It.IsAny<int>())` returns `dequeueGate.Task`; `QfcQueue.Dequeue()` returns default (a loose mock yields `(null, null)`, so no `TableLayoutPanel` is constructed); the form-controller mock supplies `ItemsPerIteration` and `Groups`; `EnqueueAsync` returns `Task.CompletedTask`. Act: `controller.Iterate2()`, which returns synchronously. Assert, taken **while the gate is still open**: `LoadItems(It.IsAny<TableLayoutPanel>(), It.IsAny<List<QfcItemGroup>>())` `Times.Once` **and** `EnqueueAsync(...)` `Times.Never`.
  - Acceptance: the single named test exists and passes. Race-free by construction — the continuation is provably not runnable while its antecedent is incomplete — and it consumes zero wall-clock time. No `Task.Yield()`, no scheduler drain, no invocation-count polling.
- [ ] [P4-T14] Add Iteration **D2** `Iterate2_AfterTheBackgroundDequeueCompletes_TheRefillEnqueuesTheItems` to `QfcHomeControllerIterationOrderingTests.cs`.
  - Arrange: as D1, plus `enqueued = new TaskCompletionSource<bool>()` and `EnqueueAsync` set up as `.Callback(() => enqueued.TrySetResult(true)).Returns(Task.CompletedTask)`. Act: `controller.Iterate2(); dequeueGate.SetResult(items); await enqueued.Task;` Assert: `EnqueueAsync(items, groups)` `Times.Once`.
  - Acceptance: the single named test exists and passes. `[Timeout(5000)]` is permitted here **strictly as a hang guard**, with that justification written into the test's doc comment: because the gate is created without `TaskCreationOptions.RunContinuationsAsynchronously`, `SetResult` runs the continuation inline and the passing path consumes zero wall-clock time.
- [ ] [P4-T15] Add Iteration **D3** `Iterate2_WhenTheBackgroundRefillFaults_DoesNotSurfaceTheFaultToTheCaller` to `QfcHomeControllerIterationOrderingTests.cs`.
  - Arrange: as D1 but `EnqueueAsync` is set up as `.Callback(() => faulted.TrySetResult(true)).Throws(new InvalidOperationException("refill failed"))`, with the token never cancelled so the discarded task reaches the rethrow. Act: `System.Action act = () => { controller.Iterate2(); dequeueGate.SetResult(items); };` Assert: `act.Should().NotThrow()` and `faulted.Task.IsCompleted.Should().BeTrue()`.
  - Acceptance: the single named test exists and passes. Label **CHARACTERIZATION** in the test's own summary comment; no GitHub issue is assigned to this finding, so the comment cites `QfcHomeController.Iteration.cs.research.2026-08-07T20-50.md` § 9 LD2 (unobserved-fault discard) and the executor flags the finding to the epic orchestrator for promotion. Safe by verification: no `<ThrowUnobservedTaskExceptions enabled="true"/>` exists anywhere in the repository, and `QuickFiler.Test/app.config` carries only binding redirects, so the finalizer-time unobserved exception cannot destabilize sibling tests.
- [ ] [P4-T16] Re-verify that `QuickFiler.Test/Controllers/QfcHomeControllerIterationTests.cs` is byte-identical to its [P0-T11] SHA-256, and that `git diff --stat -- QuickFiler/Controllers/QfcHomeController.Iteration.cs` is empty; record `<FEATURE>/evidence/regression-testing/iteration-frozen-and-clean.<TS>.md`.
  - Acceptance: the SHA-256 matches the baseline value exactly and the production diff for `QfcHomeController.Iteration.cs` is empty.
- [ ] [P4-T17] Measure the physical line count of both new test files created in this phase and append to `<FEATURE>/evidence/qa-gates/test-file-line-counts.<TS>.md`.
  - Acceptance: two counts recorded numerically, each `<= 500`.
- [ ] [P4-T18] Run F1's per-file coverage harness (command from [P0-T9]) after this phase's changes and record `<FEATURE>/evidence/qa-gates/per-file-coverage-iteration.<TS>.md` with the numeric line rate and branch rate for `QuickFiler/Controllers/QfcHomeController.Iteration.cs`, plus explicit per-path confirmation that the six AC6 paths are now shown covered.
  - AC6 paths: the throwing arm of the entry `Token.ThrowIfCancellationRequested()` (source line 13); the `catch (OperationCanceledException)` handler (38-41); the `Token.IsCancellationRequested == true` swallow (42-47); the `throw;` rethrow (48-51); and both null-conditional short-circuits of `Globals?.QfSettings?.HighConfidenceModeEnabled` in `Iterate()` (line 60).
  - Acceptance: numeric line and branch figures recorded with `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`; line coverage is `>= 80%` and `>= ` its [P0-T18] baseline, branch rate is recorded and `>= ` its baseline (AC5); and all six named paths are individually confirmed covered (AC6). Because this file may already sit above 80% at baseline, the branch-rate improvement and the six named paths — not the headline line rate — are this phase's substantive acceptance signal.

---

### Phase 5 — IQfcHomeController.cs Ledger Classification and Evidence

Production file: `QuickFiler/Controllers/IQfcHomeController.cs` (20 lines). **Read-only. This phase contains no test tasks.** Shape-assertion and reflection-shape tests against this interface are prohibited (D-2): they buy zero coverage because the file emits no IL and test-assembly lines are excluded from measurement by policy; they duplicate a check the compiler already performs more strongly; they have no unit of behavior under test; and they would convert a legitimate future coordinated interface change into a spurious failure in this child's suite.

The full path must be used in every task. A second, uncompiled file named `IQfcHomeController.cs` exists at `QuickFiler/Interfaces/IQfcHomeController.cs`; it declares an unrelated type, survives only in the stale `QuickFiler.csproj.bak`, and must not be edited, deleted, or added to the ledger.

- [ ] [P5-T1] Record F1's ledger row for `QuickFiler/Controllers/IQfcHomeController.cs` **verbatim** into `<FEATURE>/evidence/qa-gates/interface-disposition-iqfchomecontroller.<TS>.md`, together with the three independent evidence lines that support it: every member declaration terminates in `;` with no member body, no `static` member, no `const`, no attribute, and no nested type; the project targets `.NET Framework 4.8.1` (`QuickFiler.csproj:13`), so default interface implementations cannot compile regardless of `LangVersion=preview` (`:14`); and the committed instrumented Cobertura artifact contains no `<class>` element for the file while the concrete `QuickFiler.Interfaces.MailItemActionsAdapter` **is** present as a positive control proving the instrumenter reaches that folder.
  - Acceptance: the artifact contains the verbatim row reading `interface-only / not-measured` plus all three evidence lines. Any other classification halts work; specifically, a `testable` classification is unsatisfiable on a zero-line denominator and must be escalated to the epic orchestrator rather than resolved by inventing coverage work.
- [ ] [P5-T2] Record the numeric output of F1's harness for `QuickFiler/Controllers/IQfcHomeController.cs` — either the zero-line figure or the ledger's chosen zero-line row format from [P0-T8] — into the same artifact.
  - Acceptance: the harness output is recorded with `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`, satisfying AC8's evidence obligation for this file.
- [ ] [P5-T3] Prove the file is unmodified: run `git diff -- QuickFiler/Controllers/IQfcHomeController.cs` and record the empty result in the same artifact.
  - Acceptance: the diff is empty. No member is added to, removed from, renamed in, or re-signatured on this file (AC12).
- [ ] [P5-T4] Run a recorded search over this child's new and modified test files for any shape-assertion or reflection-shape test against `IQfcHomeController` (patterns covering `typeof(IQfcHomeController)`, `GetInterfaces`, `GetMethods`, `GetProperties` applied to the interface type) and record the result in the same artifact with `SearchScope:`, `SearchPatterns:`, and `SearchResult:`.
  - Acceptance: `SearchResult:` is `none`, satisfying the AC8 clause that no interface shape-assertion or reflection-shape test appears anywhere in this child's diff.

---

### Phase 6 — IFilerHomeController.cs Ledger Classification and Evidence

Production file: `QuickFiler/Interfaces/IFilerHomeController.cs` (45 lines). **Read-only. This phase contains no test tasks.** Shape-assertion and reflection-shape tests against this interface are prohibited on the same grounds as Phase 5 (D-2). A test asserting that `EfcHomeController.QuickFileMetrics_WRITE` throws `NotImplementedException` is additionally prohibited because `EfcHomeController.Metrics.cs` is sibling-**F8**-owned.

This is the highest-fan-in file in the child's set: eight wave-1 children touch the contract (F2, F3, F6, F9, F10, F11 as consumers; F7 and F8 as implementers), plus `TaskMaster/Ribbon/RibbonController.cs` outside the epic. An "obvious" one-line edit here has the largest blast radius of any file assigned to F7. The control is that no edit is needed.

- [ ] [P6-T1] Record F1's ledger row for `QuickFiler/Interfaces/IFilerHomeController.cs` **verbatim** into `<FEATURE>/evidence/qa-gates/interface-disposition-ifilerhomecontroller.<TS>.md`, together with the same three independent evidence lines used in [P5-T1] (source-construct exhaustion, the .NET Framework 4.8.1 target precluding default interface implementations, and the committed Cobertura artifact with `MailItemActionsAdapter` as the positive control).
  - Acceptance: the artifact contains the verbatim row reading `interface-only / not-measured` plus all three evidence lines. Any other classification halts work and is escalated to the epic orchestrator.
- [ ] [P6-T2] Record the numeric output of F1's harness for `QuickFiler/Interfaces/IFilerHomeController.cs` — either the zero-line figure or the ledger's chosen zero-line row format from [P0-T8] — into the same artifact.
  - Acceptance: the harness output is recorded with `Timestamp:` / `Command:` / `EXIT_CODE:` / `Output Summary:`, satisfying AC8's evidence obligation for this file.
- [ ] [P6-T3] Prove the file is unmodified and the sibling implementer is untouched: run `git diff -- QuickFiler/Interfaces/IFilerHomeController.cs` and `git diff --name-only -- QuickFiler/Controllers/EfcHomeController*` and record both empty results in the same artifact.
  - Acceptance: both diffs are empty. No member is added, removed, renamed, or re-signatured, and none of the three commented-out members at lines 29, 34, and 40 is uncommented — each would break `EfcHomeController` at compile time with a documented CS0535/CS0738 (AC12).
- [ ] [P6-T4] Run a recorded search over this child's new and modified test files for any shape-assertion or reflection-shape test against `IFilerHomeController`, and for any assertion that an `EfcHomeController` member throws `NotImplementedException`; record the result in the same artifact with `SearchScope:`, `SearchPatterns:`, and `SearchResult:`.
  - Acceptance: `SearchResult:` is `none` for both pattern sets.

---

### Phase 7 — Final C# Toolchain QA Loop and Coverage Verification

Stages [P7-T1] through [P7-T4] are one toolchain pass and must run in exactly that order. **If any stage exits non-zero or changes any file, fix the cause and restart the pass from [P7-T1].** The loop may not be exited while any stage is failing, and no analyzer diagnostic may be suppressed to achieve a pass. Each stage writes its own final-QC artifact; `EXIT_CODE: SKIPPED` is not a valid outcome for any of them.

- [ ] [P7-T1] Run `dotnet tool run csharpier format .` followed by `dotnet tool run csharpier check .` and record `<FEATURE>/evidence/qa-gates/final-csharpier.<TS>.md`.
  - Acceptance: `check` exits 0 with zero unformatted files. If `format` modified any file, restart the pass from this task after recording the modification. `csharpier format` (not `pipe-files`) is the enforcing form for csharpier 1.2.6.
- [ ] [P7-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and record `<FEATURE>/evidence/qa-gates/final-msbuild-analyzers.<TS>.md`.
  - Acceptance: exit code 0, with warning and error counts in `Output Summary:` and no new diagnostic relative to the [P0-T15] baseline. On any failure, fix and restart from [P7-T1].
- [ ] [P7-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and record `<FEATURE>/evidence/qa-gates/final-msbuild-nullable.<TS>.md`.
  - Acceptance: exit code 0, with warning and error counts in `Output Summary:` and no new diagnostic relative to the [P0-T16] baseline. On any failure, fix and restart from [P7-T1].
- [ ] [P7-T4] Run `pwsh -File scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput <FEATURE>\evidence\qa-gates\coverage-final.cobertura.xml` and record `<FEATURE>/evidence/qa-gates/final-vstest-coverage.<TS>.md`.
  - Acceptance: exit code 0, zero test failures, and `Output Summary:` records **numeric** post-change values — total tests, passed, failed, skipped, and the repository-wide `line-rate` and `branch-rate` from the Cobertura root `<coverage>` element as percentages. The Cobertura XML exists at the stated canonical path. On any failure, fix and restart from [P7-T1].
- [ ] [P7-T5] Record the single clean toolchain pass into `<FEATURE>/evidence/qa-gates/final-toolchain-pass.<TS>.md`, naming the four commands in order, their exit codes, and the number of restarts performed before the clean pass.
  - Acceptance: the artifact shows all four stages exiting 0 within one uninterrupted pass, satisfying AC25.
- [ ] [P7-T6] Run F1's per-file coverage harness against the [P7-T4] Cobertura and record `<FEATURE>/evidence/qa-gates/coverage-delta.<TS>.md` as a delta and threshold verification table.
  - Required rows, each with **baseline**, **post-change**, and **delta** figures: `QuickFiler/Controllers/QfcHomeController.cs` (line and branch), `QfcHomeController.Metrics.cs` (line and branch), `QfcHomeController.Iteration.cs` (line and branch), `QfcHomeController.Properties.cs` (line, new file), and the repository-wide line rate.
  - Required new/changed-code figures: the per-file coverage of the newly created `QfcHomeController.Properties.cs` and the coverage of the new members added to existing files (`ShowUserMessage`, `MetricsFileWriter`, `BuildDurationTexts`, `MetricsLineWriter`, `MetricsAdder`, and both widened `NonBlockingProducer` overloads).
  - Acceptance: every value is numeric with no placeholder such as `UNVERIFIED`; the three testable files are each `>= 80%` line and `>= ` their baseline line rate with branch rate recorded and `>= ` baseline (AC3, AC4, AC5); `QfcHomeController.Properties.cs` is `>= 90%` line (AC7); and the repository-wide line rate is `>= ` the [P0-T17] baseline (AC26, a no-regression criterion against the measured baseline, not an absolute floor). If any required value is unavailable, the outcome is remediation-required and must not be reported as PASS.
- [ ] [P7-T7] Re-compute the SHA-256 of all five frozen test files and compare against [P0-T11]; record `<FEATURE>/evidence/qa-gates/frozen-test-files-final.<TS>.md`.
  - Acceptance: all five hashes match their baseline values exactly (AC15).
- [ ] [P7-T8] Run `git diff --name-only` against the merge base recorded in [P0-T10] and record `<FEATURE>/evidence/qa-gates/scope-containment.<TS>.md`.
  - Acceptance: the file list contains only the two seam-bearing production files, `QuickFiler/Controllers/QfcHomeController.Properties.cs`, `QuickFiler/QuickFiler.csproj`, `QuickFiler.Test/QuickFiler.Test.csproj`, the ten new test files, `QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs` (permitted by AC16 via [P3-T28]), and this feature folder's documents and evidence. **No sibling-owned file appears** — specifically none of `IQfcDatamodel`/`QfcDatamodel*`, `IQfcQueue`/`QfcQueue*`/`FilerQueue`, `QfcCollectionController*`, `QfcFormController*`/`QfcExplorerController`, `KeyboardHandler`/`Kbd*`/`Ka*`, `EfcHomeController*`, `coverage.config`, or any shared build property file (AC17). `QuickFiler/QuickFiler.csproj` shows exactly one added line.
- [ ] [P7-T9] Run a recorded search over every new and modified test file in this child for the prohibited determinism constructs and record `<FEATURE>/evidence/qa-gates/determinism-audit.<TS>.md` with `SearchScope:`, `SearchPatterns:`, and `SearchResult:`.
  - Patterns: `Thread.Sleep`, `Task.Delay`, `DateTime.Now`, `DateTime.UtcNow`, `DateTimeOffset.Now`, `new Random(` without a seed, any real file write, any external process, any live Outlook COM construction, `new QfcFormViewer`, `.Show()`, `MessageBox`, `UiThread.Init`, and `SetSynchronizationContext`.
  - Acceptance: `SearchResult:` is `none` for every pattern, and every `[Timeout]` occurrence is listed with the in-test justification proving it is a hang guard on a zero-wall-clock path (AC21).
- [ ] [P7-T10] Verify characterization labelling and record `<FEATURE>/evidence/qa-gates/characterization-labels.<TS>.md`.
  - Acceptance: every test that pins current defective behaviour carries `CHARACTERIZATION` in its own summary comment and names its tracking issue — #442 for Metrics A8 and for TC7-TC11, #443 for Metrics D3, #446 for any test touching the empty-batch routing — or, for Iteration D3 where no issue exists, cites `QfcHomeController.Iteration.cs.research.2026-08-07T20-50.md` § 9 LD2 with the finding flagged to the epic orchestrator for promotion. No new test asserts that the empty-batch → `CompleteAddingAsync` routing is correct, and no fix for #442, #443, #446, or #447 appears in the diff; `Iterate()` and `Iterate2()` remain present in `QfcHomeController.Iteration.cs` and on `IQfcHomeController` (AC19, AC20).
- [ ] [P7-T11] Verify the duplication guards and record `<FEATURE>/evidence/qa-gates/duplication-guard.<TS>.md`.
  - Acceptance: no delivered test duplicates an assertion listed in any research artifact's duplication guard; no test listed as a rejected candidate is present (specifically Iteration R1-R4, and the rejected `QfcHomeController.cs` items — the retired two-argument dequeue assertion, the poll-interval harmonization, the 0→30 band re-assertion, the legacy `Run()` overload assertion, the `HighConfidencePreFilterLoader`-from-`RunAsync` assertion, and the dead `progress?.Report(100)` null arm); and for each new test targeting the same member as an existing test, the distinct post-condition is named — Iteration C3 versus the frozen `SwapStopWatch_ExecutesCorrectly`, Metrics A2 versus the retargeted `GetMoveDiagnostics_NullAppointment_DoesNotThrow`, Metrics B2 versus `NonBlockingProducer_DelaySeam_HonorsInjectedTwentyMillisecondDelay`, and TC17 versus `HighConfidencePreFilterLoader_CanBeOverridden_ForTesting` (AC24).
- [ ] [P7-T12] Record the per-file scenario mapping into `<FEATURE>/evidence/qa-gates/scenario-mapping.<TS>.md`, one section per `testable` file, mapping each delivered test to positive flow, negative flow, boundary condition, error handling, or state transition.
  - Acceptance: each of the three `testable` files shows at least one test in each applicable category, with state transitions covered where the member is stateful (AC23).
- [ ] [P7-T13] Run a recorded test-convention audit over every new and modified test file in this child and record `<FEATURE>/evidence/qa-gates/test-convention-audit.<TS>.md` with `SearchScope:`, `SearchPatterns:`, and `SearchResult:`.
  - Acceptance: for every delivered `[TestMethod]` the audit records that it uses MSTest attributes from `Microsoft.VisualStudio.TestTools.UnitTesting`, uses Moq for its test doubles and FluentAssertions for its assertions (or names the specific assertion shape where FluentAssertions is impractical), follows Arrange–Act–Assert, and carries a `<summary>` comment stating its scenario and expected outcome; and that all ten new test files live under `QuickFiler.Test/Controllers/`, with `QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs` the only pre-existing test file modified (AC22).
- [ ] [P7-T14] Record the final measured line count of every production and test file this child created or modified into `<FEATURE>/evidence/qa-gates/final-file-line-counts.<TS>.md`.
  - Acceptance: every production file in scope, including `QfcHomeController.Properties.cs`, is `<= 500` (AC9); every new or modified test file is `<= 500` (AC10); all values are measured, not estimated.
- [ ] [P7-T15] Check off the acceptance criteria in `<FEATURE>/spec.md` (AC1-AC27 **and the seven `## Definition of Done` items**) and `<FEATURE>/user-story.md` (US1-US13) per `.claude/skills/acceptance-criteria-tracking/SKILL.md`, and record the AC status summary plus an evidence index into `<FEATURE>/evidence/qa-gates/ac-status-summary.<TS>.md`.
  - Acceptance: every AC, every `## Definition of Done` item, and every US is marked with its status and the evidence artifact path that substantiates it; the AC status summary counts include the seven `## Definition of Done` items alongside AC1-AC27 and US1-US13; any adopted conditional item (D7 in [P3-T5], the retarget in [P3-T28]) and any deviation from the ratified seam set is recorded in `spec.md` with its justification; the known unresolved policy debt from D-10 (the `UiThread.Init(false)` global-state dependency and the live-`QfcFormViewer` construction in the two existing `Init*` tests) is recorded explicitly rather than left silent; and the AC status summary is reported to the caller. The three `## Seeded Test Conditions` checkboxes in `spec.md` are planning seeds, not acceptance criteria, and are left untouched.
