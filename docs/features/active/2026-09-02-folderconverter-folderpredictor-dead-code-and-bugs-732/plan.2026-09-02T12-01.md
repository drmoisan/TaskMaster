# folderconverter-folderpredictor-dead-code-and-bugs (Plan)

- **Issue:** #732
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-02T12-01
- **Status:** Draft
- **Version:** 0.1
- **Work Mode:** full-bug (AC source: spec.md only; issue.md's own checklist sections are not the AC source)

**Fail-closed evidence rule:** Every baseline, regression, and final-QC artifact required below must exist with the required fields (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`) before the task it belongs to is checked off. If any required artifact is missing or incomplete, that task and any downstream AC check-off task remain unchecked.

**Evidence accounting rule:** Every evidence-producing task below states the exact evidence artifact path it must write, under the canonical `<FEATURE>/evidence/<kind>/` scheme, where `<FEATURE>` = docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732. No task in this plan writes to any `artifacts/baselines`, `artifacts/qa`, `artifacts/coverage`, or similar non-canonical location; if any upstream instruction ever supplied one, it is superseded by this plan's canonical paths.

## Write Set

The following files are the complete set of paths this change creates, modifies, or deletes. No other file is written by this item. Reproduced verbatim from spec.md's own `## Write Set` section.

- `UtilitiesCS/EmailIntelligence/FolderConverter.cs` (delete)
- `UtilitiesCS.Test/OutlookExtensions/FolderConverter_Tests.cs` (delete)
- `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs` (modify)
- `UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs` (modify)
- `docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/issue.md` (modify -- AC check-off during execution)
- `docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/spec.md` (modify -- this file, status updates during execution)
- `docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/plan.2026-09-02T12-01.md` (modify -- checklist check-off during execution)

## Scope boundary (plain prose, not part of the Write Set)

This plan does not touch UtilitiesCS/Threading/** (owned by a separate concurrently in-flight item covering UiThread/ProgressTracker types), does not touch UtilitiesCS/To Depricate/FileIO2.cs (owned by a separate concurrently in-flight item; the path contains a space), and does not touch the dead caller at ToDoModel/Email Utilities/SortItemsToExistingFolder.cs (itself excluded from the ToDoModel project's build and out of this issue's four findings). It also does not touch the live class at UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs (the compiled, in-use FolderConverter with the same class name in the same namespace as the deleted dead file), TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs, or TaskMaster.Test/AppGlobals/AppFileSystemFolderPathsMatchBestSpecialFolderTests.cs -- all three are read-only citation targets confirmed unchanged by this plan's Final QC phase.

## AC Inventory (re-derived this pass against the current spec.md acceptance-criteria checklist)

spec.md's `## Acceptance Criteria` section (lines 184-193 as read in this planning pass) contains ten checkbox items, not nine. This plan tracks all ten as AC1 through AC10 in checklist order:

- AC1: the dead EmailIntelligence FolderConverter source file no longer exists and no .csproj anywhere references it via Compile Include.
- AC2: the dead OutlookExtensions FolderConverter_Tests test file no longer exists and UtilitiesCS.Test.csproj contains no compile-include entry referencing it.
- AC3: the live OutlookObjects/Folder FolderConverter class and its public members are unchanged and remain compiled and passing under its existing test suite.
- AC4: FolderPredictor.cs line 691 uses the logical `||` operator and guards the branch-path length before indexing its first character.
- AC5: a new regression test invokes CreateFolder with an empty-string parent branch path and asserts no IndexOutOfRangeException, written first per the Bugfix Workflow, RED before the fix, GREEN after.
- AC6: the three pre-existing CreateFolder tests continue to pass unmodified.
- AC7: AppFileSystemFolderPaths.cs's MatchBestSpecialFolder method and its XML doc comment are unchanged; its dedicated test file is unchanged and continues to pass.
- AC8: no production caller of MatchBestSpecialFolder was introduced or discovered; the finding is recorded as confirmed-correct-as-documented.
- AC9: the full C# toolchain (CSharpier format/check, .NET analyzers rebuild, nullable/TreatWarningsAsErrors rebuild, MSTest via vstest.console.exe with coverage) passes clean in a single pass.
- AC10: no file under UtilitiesCS/Threading/** or the FileIO2.cs path under UtilitiesCS/To Depricate is modified by this change.

---

### Phase 0 — Policy Reads, Toolchain Bootstrap, and Baseline Capture

- [ ] [P0-T1] Read, in this exact order, CLAUDE.md, .claude/rules/general-code-change.md, .claude/rules/general-unit-test.md, and .claude/rules/csharp.md, then write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/phase0-instructions-read.md containing `Timestamp:`, a `Policy Order:` line listing the four files in the order read, and one confirmation line per file that it was read in full. Acceptance: the artifact exists on disk and contains all four required fields with all four file paths named.

- [ ] [P0-T2] Record the current git baseline state of this worktree: run `git rev-parse HEAD` and `git status --porcelain` from the worktree root, and write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t2-baseline-git-state.md containing `Timestamp:`, `Command:` (both commands verbatim), `EXIT_CODE:`, and `Output Summary:` recording the resolved commit SHA (call it BASELINE_SHA in every later task of this plan that references it) and the verbatim porcelain output. Acceptance: the artifact exists and BASELINE_SHA is a 40-character hex string.

- [ ] [P0-T3] Bootstrap the repository-pinned .NET SDK by running `pwsh -File scripts/vscode/Install-RepoDotNetSdk.ps1` from the worktree root, then confirm with `dotnet --version` that it resolves without the global.json missing-SDK error. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t3-sdk-bootstrap.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the resolved SDK version string. Acceptance: EXIT_CODE 0 for both commands.

- [ ] [P0-T4] Restore NuGet packages for the whole solution by running `pwsh -File scripts/vscode/Invoke-Restore.ps1 -SolutionPath TaskMaster.sln -Configuration Debug -Platform "Any CPU"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t4-nuget-restore.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE 0 and the packages directory now exists at the worktree root.

- [ ] [P0-T5] Restore the CSharpier 1.2.6 dotnet local tool by running `dotnet tool restore` from the worktree root. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t5-tool-restore.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE 0.

- [ ] [P0-T6] Capture the repo-wide CSharpier formatting baseline (read-only) by running `dotnet tool run csharpier check .` from the worktree root and recording the full verbatim list of files it reports as needing formatting (or confirming an empty list). Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t6-csharpier-baseline.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` containing the verbatim reported-file list (or "no files reported"). This is the merge-base drift snapshot used by the Final QC phase's zero-unexpected-diff gate; no formatting is applied by this task.

- [ ] [P0-T7] From the P0-T6 output, record explicitly in a new evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t7-protected-files-format-baseline.md whether UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs and TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs appear in the P0-T6 reported-file list, with `Timestamp:` and one `Output Summary:` line per file stating "already unformatted at baseline" or "clean at baseline". Acceptance: the artifact names both files by their status explicitly (no third value permitted).

- [ ] [P0-T8] Capture the baseline .NET analyzer rebuild by running `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true /v:normal /fl "/flp:LogFile=coverage\logs\p0-t8-analyzer-baseline.txt;Verbosity=normal"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t8-analyzer-baseline.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the terminal warning/error counts printed by msbuild. The raw log at coverage/logs/p0-t8-analyzer-baseline.txt stays under the gitignored coverage directory (coverage/* is ignored per .gitignore); only the summary goes into the tracked evidence artifact.

- [ ] [P0-T9] Capture the baseline nullable rebuild by running `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true /v:normal /fl "/flp:LogFile=coverage\logs\p0-t9-nullable-baseline.txt;Verbosity=normal"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t9-nullable-baseline.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the terminal warning/error counts. Do not add /p:Nullable=enable to this command (no project in this repo opts into it solution-wide; adding it produced ~195 errors in a prior measurement and diverges from CI).

- [ ] [P0-T10] Capture the baseline scoped test-and-coverage run by running `pwsh -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot UtilitiesCS.Test -Configuration Debug -CoverageOutput docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/coverage-baseline.cobertura.xml *>&1 | Tee-Object -FilePath docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t10-console.log`. The `-SearchRoot UtilitiesCS.Test` scope keeps discovery inside this worktree's own UtilitiesCS.Test project only, avoiding both the single-assembly StrictMode defect in the sibling Invoke-MSTest.ps1 script (this script wraps its discovery result in `@(...)` and does not carry that defect) and cross-worktree contamination from sibling `.claude/worktrees/agent-*` builds. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t10-baseline-test-coverage.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the vstest Passed/Failed/Total counts read from the console log and the Cobertura `line-rate` percentage read from the emitted XML's root `<coverage>` element. Acceptance: EXIT_CODE 0, Failed count 0, and a numeric line-rate percentage recorded (not a placeholder).

- [ ] [P0-T11] Re-verify, by direct read of the current tree, that UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs line 691 still reads exactly `if (olAncestor.EndsWith('\\'.ToString()) | parentBranchPath[0] == '\\')` (bitwise OR, unguarded index) and that line 2 of the same file already contains `using System;`. Record the confirmation in the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p0-t11-defect-line-confirmation.md with `Timestamp:` and `Output Summary:` quoting the exact line-691 text observed. Acceptance: the quoted text contains a single `|` character between the two operands and does not contain `||`.

---

### Phase 1 — Finding 2: Failing regression test for the CreateFolder guard defect (RED)

- [ ] [P1-T1] [expect-fail] Add one new `[TestMethod]` named `CreateFolder_WhenParentBranchPathIsEmpty_DoesNotThrowIndexOutOfRangeException` to UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs, inserted immediately after the existing `CreateFolder_WhenAncestorIsNull_UsesArchiveRootAndCreatesFolder` test method (which currently ends at line 862). The new test body must follow the same Arrange shape as the existing `CreateFolderAsync_WhenParentDoesNotExist_ReturnsNull` test (lines 632-652): construct an archiveRoot folder mock via the file's private `CreateFolder("\\\\ArchiveRoot", new Dictionary<string, OutlookFolder>())` helper (name collision with the production method under test is pre-existing and intentional in this test class), wrap it with `CreateApplication` and `CreateGlobals`, and construct `new TestableFolderPredictor(globals.Object, "FY26")`. The Act/Assert must be `Action act = () => predictor.CreateFolder(string.Empty, "\\\\ArchiveRoot", "C:\\OneDrive"); act.Should().NotThrow();`. Acceptance: `git diff --name-only BASELINE_SHA -- UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs` (BASELINE_SHA recorded by P0-T2) is non-empty, `git status --porcelain -- UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs` reports the file as modified (`M ` prefix, since this is an edit to a pre-existing tracked file, not a new untracked file), and the file contains exactly one occurrence of the literal test method name `CreateFolder_WhenParentBranchPathIsEmpty_DoesNotThrowIndexOutOfRangeException`.

- [ ] [P1-T2] [expect-fail] Record the pre-edit physical line count of UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs as it stood at BASELINE_SHA, using `git show BASELINE_SHA:UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs | Measure-Object -Line` is explicitly NOT to be used (undercounts blank lines); instead run `(git show BASELINE_SHA:UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs) | ForEach-Object { $_ } | Measure-Object` is also insufficient -- use `(git show BASELINE_SHA:UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs).Count` (an array of lines from the git show output, `.Count` on the array, not `Measure-Object -Line`) and the post-edit count via `(Get-Content -LiteralPath UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs).Count`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p1-t2-test-file-line-budget.md with `Timestamp:` and `Output Summary:` recording both counts and their delta. Acceptance: this file already exceeds the repository's 500-line cap at BASELINE_SHA (a pre-existing condition this plan does not remediate, consistent with the Bugfix Workflow's minimal-change directive), so the applicable gate for this file is a budgeted ceiling, not a shrink: the post-edit count must not exceed the BASELINE_SHA count plus 40 lines.

- [ ] [P1-T3] [expect-fail] Build only the test project to confirm the new test compiles cleanly by running `msbuild UtilitiesCS.Test/UtilitiesCS.Test.csproj /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p1-t3-expectfail-build.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE 0.

- [ ] [P1-T4] [expect-fail] Run the new test in isolation and confirm it fails against the pre-fix defect by executing `$vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'; $vstest = & $vswhere -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe' | Select-Object -First 1; & $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~CreateFolder_WhenParentBranchPathIsEmpty_DoesNotThrowIndexOutOfRangeException" /Logger:trx /ResultsDirectory:coverage\trx\p1-t4`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p1-t4-createfolder-empty-parentbranch-fail-before.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the vstest summary line and quoting the failure message. Acceptance: the run reports `Failed: 1` and `Passed: 0` for this single test, and the recorded failure message contains the single-line token `IndexOutOfRangeException`. This is a synchronous, non-async-void assertion path (FluentAssertions' `NotThrow()` invokes the lambda and catches the exception itself, on the calling MSTest thread), so the RED/GREEN pair is deterministic.

---

### Phase 2 — Finding 2: Apply the minimal fix (GREEN)

- [ ] [P2-T1] Edit UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs at line 691 (confirmed by P0-T11): replace the single-line conditional `if (olAncestor.EndsWith('\\'.ToString()) | parentBranchPath[0] == '\\')` with `if (olAncestor.EndsWith("\\", StringComparison.Ordinal) || (parentBranchPath.Length > 0 && parentBranchPath[0] == '\\'))`. No other line in the method (lines 677-725) changes; the else-branch concatenation and both post-conditional statements stay byte-for-byte identical. Acceptance: the file contains the literal substring `parentBranchPath.Length > 0 && parentBranchPath[0] == ` and no longer contains the literal substring `.ToString()) | parentBranchPath[0]` (single-line, non-interpolated token search on the current file content).

- [ ] [P2-T2] Record the post-edit physical line count of UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs via `(Get-Content -LiteralPath UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs).Count` and the BASELINE_SHA count via `(git show BASELINE_SHA:UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs).Count`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/baseline/p2-t2-predictor-file-line-budget.md with `Timestamp:` and `Output Summary:` recording both counts. Acceptance: this file already exceeds the 500-line cap at BASELINE_SHA (pre-existing, not remediated by this plan); the post-edit count must not exceed the BASELINE_SHA count plus 2 lines (the fix is a single-line-to-single-or-double-line rewrite of an existing conditional, with no other line added or removed).

- [ ] [P2-T3] Build only the production project to confirm the fix compiles cleanly by running `msbuild UtilitiesCS/UtilitiesCS.csproj /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU"`, then rebuild the test project by running `msbuild UtilitiesCS.Test/UtilitiesCS.Test.csproj /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p2-t3-postfix-build.md with `Timestamp:`, `Command:` (both commands), `EXIT_CODE:` (both), and `Output Summary:`. Acceptance: EXIT_CODE 0 for both.

- [ ] [P2-T4] Re-run the exact same scoped command as P1-T4 (same TestCaseFilter, a fresh `/ResultsDirectory:coverage\trx\p2-t4`) and confirm the new test now passes. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p2-t4-createfolder-empty-parentbranch-pass-after.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: the run reports `Passed: 1` and `Failed: 0` for this single test, completing the RED-then-GREEN pair required by AC5.

- [ ] [P2-T5] Run the three pre-existing CreateFolder tests by name to confirm the fix preserves their behavior, using `$vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'; $vstest = & $vswhere -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe' | Select-Object -First 1; & $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~CreateFolder_WhenParentBranchStartsWithSeparator_UsesCombinedPathWithoutDoubleSlash|FullyQualifiedName~InjectedDirectory_CreateFolder_WhenPromptSuppliesName_CreatesFolderAndDirectoryPath|FullyQualifiedName~CreateFolder_WhenAncestorIsNull_UsesArchiveRootAndCreatesFolder" /Logger:trx /ResultsDirectory:coverage\trx\p2-t5`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p2-t5-preexisting-createfolder-tests.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: `Passed: 3`, `Failed: 0`.

- [ ] [P2-T6] Check off AC4 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs line 691 uses the logical" to `- [x]`, with no other text change. Acceptance: that single line reads `- [x]` and every other checkbox in the section is unchanged from its prior state.

- [ ] [P2-T7] Check off AC5 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "A new regression test in UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs invokes CreateFolder" to `- [x]`, with no other text change.

- [ ] [P2-T8] Check off AC6 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "The three pre-existing CreateFolder tests in FolderPredictorTests.cs" to `- [x]`, with no other text change.

---

### Phase 3 — Findings 1 and 4: Delete the dead FolderConverter file and its dead test file

- [ ] [P3-T1] Re-confirm, by direct grep of the current tree, that UtilitiesCS/UtilitiesCS.csproj contains zero `<Compile Include>` entries referencing any path under EmailIntelligence/FolderConverter.cs, and that UtilitiesCS.Test/UtilitiesCS.Test.csproj contains zero `<Compile Include>` entries referencing OutlookExtensions/FolderConverter_Tests.cs. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/other/p3-t1-dead-files-precondition.md with `Timestamp:` and `Output Summary:` recording both zero-match results. Acceptance: both greps return zero matches, confirming the deletion is safe (neither file is wired into its project).

- [ ] [P3-T2] Delete UtilitiesCS/EmailIntelligence/FolderConverter.cs via `git rm UtilitiesCS/EmailIntelligence/FolderConverter.cs`. Acceptance: the file no longer exists on disk (`Test-Path` returns false) and `git status --porcelain -- UtilitiesCS/EmailIntelligence/FolderConverter.cs` reports a two-character status field starting with `D`.

- [ ] [P3-T3] Delete UtilitiesCS.Test/OutlookExtensions/FolderConverter_Tests.cs via `git rm UtilitiesCS.Test/OutlookExtensions/FolderConverter_Tests.cs`. Acceptance: the file no longer exists on disk and `git status --porcelain -- UtilitiesCS.Test/OutlookExtensions/FolderConverter_Tests.cs` reports a two-character status field starting with `D`.

- [ ] [P3-T4] Repo-wide grep every `*.csproj` file for the literal strings "EmailIntelligence\FolderConverter.cs" and "OutlookExtensions\FolderConverter_Tests.cs" (backslash form, matching MSBuild `Include` path separator convention) and confirm zero matches for each after the deletions in P3-T2 and P3-T3. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/other/p3-t4-no-csproj-references.md with `Timestamp:`, `Command:`, and `Output Summary:` recording both zero-match results. Acceptance: zero matches for both literal strings across every `*.csproj` file in the repository, satisfying the second clause of AC1 and the second clause of AC2.

- [ ] [P3-T5] Rebuild the whole solution to confirm the two deletions do not break the build by running `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p3-t5-postdeletion-rebuild.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE 0, confirming the live UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs class and every other project in the solution still compile with the two dead files removed.

- [ ] [P3-T6] Grep UtilitiesCS.Test/OutlookObjects/Folder/FolderConverterTests.cs for the literal `[TestMethod]` and record the count N (currently 22 as measured in this planning pass; the task must re-derive N against the current file rather than trust the parenthetical). Then run that test class in isolation using `$vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'; $vstest = & $vswhere -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe' | Select-Object -First 1; & $vstest UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~UtilitiesCS.Test.OutlookObjects.Folder.FolderConverterTests" /Logger:trx /ResultsDirectory:coverage\trx\p3-t6`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/regression-testing/p3-t6-live-folderconverter-suite.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording both the grep-derived N and the vstest Passed/Failed counts. Acceptance: `Passed` equals the grep-derived N and `Failed` equals 0, satisfying AC3's "remain compiled and passing under their existing test suite" clause.

- [ ] [P3-T7] Confirm via `git diff --name-only BASELINE_SHA -- UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs` that the command prints no output, and separately via `git status --porcelain -- UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs` that the command also prints no output (the live file is byte-for-byte unchanged and has no untracked/staged variant). Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/other/p3-t7-live-folderconverter-zero-diff.md with `Timestamp:`, `Command:` (both), and `Output Summary:` recording both outputs. Acceptance: both commands produce empty output, satisfying the "unchanged" clause of AC3.

- [ ] [P3-T8] Check off AC1 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "UtilitiesCS/EmailIntelligence/FolderConverter.cs no longer exists" to `- [x]`, with no other text change.

- [ ] [P3-T9] Check off AC2 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "UtilitiesCS.Test/OutlookExtensions/FolderConverter_Tests.cs no longer exists" to `- [x]`, with no other text change.

- [ ] [P3-T10] Check off AC3 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "The live UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs class and its public members" to `- [x]`, with no other text change.

---

### Phase 4 — Finding 3: Confirm MatchBestSpecialFolder is correct-as-documented (no code change)

- [ ] [P4-T1] Grep the TaskMaster and UtilitiesCS project directories (excluding docs, evidence, and .claude paths) for the literal `MatchBestSpecialFolder` and confirm the only matches are the definition/delegation in TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs (its own instance method at line 77 and the internal static helper at line 97) and the interface declaration in UtilitiesCS/Interfaces/IGlobals/IFileSystemFolderPaths.cs. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/other/p4-t1-matchbestspecialfolder-no-caller-confirmation.md with `Timestamp:`, `Command:`, and `Output Summary:` recording every matched file path. Acceptance: exactly those two files are matched and no other production file appears in the result, satisfying AC8.

- [ ] [P4-T2] Read the XML doc comment on the internal static MatchBestSpecialFolder helper at TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs (currently lines 85-96) and confirm it still states that matching uses ordinal `string.Contains` and that a null/empty collection returns null. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/other/p4-t2-doc-comment-confirmation.md with `Timestamp:` and `Output Summary:` quoting the doc comment's substring-matching sentence verbatim. Acceptance: the quoted sentence explicitly names `string.Contains` as ordinal substring matching. No edit is made to this file by this or any other task in this plan.

- [ ] [P4-T3] Check off AC8 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "No production caller of MatchBestSpecialFolder was introduced" to `- [x]`, with no other text change.

---

### Phase 5 — Final QC: full C# toolchain loop, coverage delta, and scope-boundary gate

Run the four-stage loop in this exact order; if any stage fails or changes a tracked file, restart the loop from stage 1.

- [ ] [P5-T1] Stage 1 (Formatting, apply): run `dotnet tool run csharpier format UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs`, scoped explicitly to only the two files this plan modifies (not `.`), because a repo-wide `csharpier format .` would rewrite any file already unformatted at BASELINE_SHA -- including, if P0-T7 found either one already drifted, UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs or TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs -- which would silently break the zero-diff clauses of AC3 and AC7. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t1-csharpier-format-scoped.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the literal "Formatted N files" (or equivalent unchanged-count) line CSharpier prints on completion. Acceptance: EXIT_CODE 0.

- [ ] [P5-T2] Stage 1 (Formatting, verify, CI parity): run `dotnet tool run csharpier check .` repo-wide, read-only. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t2-csharpier-check-final.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the reported-file list verbatim. Acceptance: either EXIT_CODE 0, OR every file in the reported list also appears in the P0-T6 baseline reported-file list AND neither UtilitiesCS/OutlookObjects/Folder/FolderConverter.cs nor TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs appears in the reported list (both must stay outside it regardless of their P0-T7 baseline status, since this plan makes no edit to either).

- [ ] [P5-T3] Stage 2/3 (Analyzers + nullable, combined final rebuild): run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true /v:normal /fl "/flp:LogFile=coverage\logs\p5-t3-analyzer-final.txt;Verbosity=normal"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t3-analyzer-final.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE 0, the log contains zero occurrences of the literal `Skipping target "CoreCompile"`, and the log contains at least one occurrence of the literal `(Rebuild target(s))` (the plural form MSBuild's normal-verbosity `ProjectStarted`/`Done Building Project` lines always print; the singular `(Rebuild target)` appears only inside a terminal warning/error summary block and must not be substituted).

- [ ] [P5-T4] Stage 3 (nullable/TreatWarningsAsErrors, final rebuild): run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true /v:normal /fl "/flp:LogFile=coverage\logs\p5-t4-nullable-final.txt;Verbosity=normal"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t4-nullable-final.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Acceptance: EXIT_CODE 0, and the log contains at least one occurrence of the literal `(Rebuild target(s))`.

- [ ] [P5-T5] Stage 4 (Testing, full scoped suite with coverage): run `pwsh -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot UtilitiesCS.Test -Configuration Debug -CoverageOutput docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/coverage-final.cobertura.xml *>&1 | Tee-Object -FilePath docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t5-console.log`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t5-final-test-coverage.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the Passed/Failed/Total counts and the final Cobertura `line-rate` percentage. Acceptance: EXIT_CODE 0, Failed count 0, Passed count equals the P0-T10-recorded baseline Passed count plus 1 (the single new regression test added in Phase 1), and the final line-rate percentage is greater than or equal to the P0-T10-recorded baseline line-rate percentage (no coverage regression).

- [ ] [P5-T6] Scope-boundary gate for AC10: run `git diff --name-only BASELINE_SHA -- . ":(exclude)docs" ":(exclude).claude"` and separately `git status --porcelain -- UtilitiesCS/Threading "UtilitiesCS/To Depricate"`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t6-scope-boundary.md with `Timestamp:`, `Command:` (both), and `Output Summary:` recording both outputs verbatim. Acceptance: the first command's output contains no path beginning with UtilitiesCS/Threading/ and no path equal to UtilitiesCS/To Depricate/FileIO2.cs, AND the second command produces empty output, jointly proving no tracked-or-untracked change exists in either excluded area, satisfying AC10.

- [ ] [P5-T7] Confirm via `git diff --name-only BASELINE_SHA -- TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs TaskMaster.Test/AppGlobals/AppFileSystemFolderPathsMatchBestSpecialFolderTests.cs` that the command prints no output, and separately via `git status --porcelain -- TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs TaskMaster.Test/AppGlobals/AppFileSystemFolderPathsMatchBestSpecialFolderTests.cs` that the command also prints no output. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t7-matchbestspecialfolder-zero-diff.md with `Timestamp:`, `Command:` (both), and `Output Summary:` recording both outputs. Acceptance: both commands produce empty output, re-confirming the zero-diff clause of AC7 at the end of the plan (after every other phase has run).

- [ ] [P5-T8] Run the dedicated MatchBestSpecialFolder test class in isolation to re-confirm it still passes unmodified: grep TaskMaster.Test/AppGlobals/AppFileSystemFolderPathsMatchBestSpecialFolderTests.cs for the literal `[TestMethod]` to derive N (currently 9 as measured in this planning pass; re-derive against the current file), then run `$vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'; $vstest = & $vswhere -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe' | Select-Object -First 1; & $vstest TaskMaster.Test\bin\Debug\TaskMaster.Test.dll /Settings:scripts\vscode\TaskMaster.cli.runsettings /InIsolation /TestCaseFilter:"FullyQualifiedName~AppFileSystemFolderPathsMatchBestSpecialFolderTests" /Logger:trx /ResultsDirectory:coverage\trx\p5-t8`. Write the evidence artifact docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/evidence/qa-gates/p5-t8-matchbestspecialfolder-tests.md with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording both the grep-derived N and the vstest Passed/Failed counts. Acceptance: `Passed` equals the grep-derived N and `Failed` equals 0.

- [ ] [P5-T9] Check off AC7 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "TaskMaster/AppGlobals/AppFileSystemFolderPaths.cs's MatchBestSpecialFolder method" to `- [x]`, with no other text change. Acceptance: that single line reads `- [x]`, and only after [P4-T2] (doc-comment confirmation), [P5-T7] (zero-diff confirmation), and [P5-T8] (dedicated test-suite pass) have all recorded their required results.

- [ ] [P5-T10] Check off AC9 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "Full C# toolchain passes clean in a single pass" to `- [x]`, only after P5-T1 through P5-T5 have all recorded EXIT_CODE 0 in the same pass with no restart. Acceptance: that single line reads `- [x]`.

- [ ] [P5-T11] Check off AC10 in spec.md's Acceptance Criteria section: change the `- [ ]` line beginning "No file under UtilitiesCS/Threading/**" to `- [x]`, only after P5-T6 has recorded the required empty results.

---

### Phase 6 — Documentation closeout and commit

- [ ] [P6-T1] Update issue.md's Proposed Fix / Validation Ideas checklist: change the four `- [ ]` lines (the FolderConverter.cs resurrect-or-delete decision, the FolderPredictor.cs:691 operator/guard fix, the MatchBestSpecialFolder semantics decision, and the FolderConverter_Tests.cs wiring/deletion decision) to `- [x]`, changing only the checkbox marker on each of the four lines and no criterion text. Acceptance: all four lines in that checklist read `- [x]`.

- [ ] [P6-T2] Update issue.md's Next Step checklist: change both `- [ ]` lines ("Promote to GitHub issue" and "Move to active fix folder / branch") to `- [x]`, reflecting that both steps already occurred prior to this plan's execution (the issue carries an assigned number and URL, and this plan runs inside the active fix folder). Acceptance: both lines read `- [x]`.

- [ ] [P6-T3] Update spec.md's Status field (currently "Draft" on the metadata line near the top of the file) to "Implemented", and update its Last Updated field to the ISO-8601 date this phase completes. Acceptance: the Status field no longer reads "Draft".

- [ ] [P6-T4] Commit every change made by this plan (the two deletions, the FolderPredictor.cs fix, the FolderPredictorTests.cs new test, and every evidence/doc update under docs/features/active/2026-09-02-folderconverter-folderpredictor-dead-code-and-bugs-732/) with a single commit whose message summarizes the four findings' dispositions. Acceptance: `git status --porcelain` immediately after the commit shows no entry for any of the four Write Set source paths (UtilitiesCS/EmailIntelligence/FolderConverter.cs, UtilitiesCS.Test/OutlookExtensions/FolderConverter_Tests.cs, UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs, UtilitiesCS.Test/OutlookObjects/Folder/FolderPredictorTests.cs).

- [ ] [P6-T5] Report the final Acceptance Criteria Status summary per the acceptance-criteria-tracking skill: source spec.md, total AC items 10, checked-off count, and any remaining unchecked items with their text. Acceptance: all 10 AC items listed in the AC Inventory section above read `- [x]` in spec.md; if any remain `- [ ]`, this task stays unchecked and the gap is reported, not silently closed.
