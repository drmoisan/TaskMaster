# Feature Audit — 2026-09-08-etl-deadline-mechanics-follow-ups (Issue #825)

- Date: 2026-09-10T00-45
- Work mode: `full-bug` (issue.md line 12). **AC source is `spec.md` only.**
- `user-story.md` is correctly absent for a `full-bug` feature. Its absence is **not** a gap and is
  not reported as one.
- Baseline: `96fd3dd86cff542226d192158f1c2f63d5eee926`
- Total acceptance criteria: **35** (AC1–AC35), all checked `[x]` in `spec.md`.

## Evaluation method

The check-off boxes are the **claim**, not the evidence. Each criterion below was re-decided against
one of: the delivered file read directly, the production diff supplied by the invoking agent, a
named evidence artifact whose internal arithmetic I re-performed, or a combination. Where my verdict
rests on a fact I could not derive myself (Bash was withheld for this review — see the policy audit),
the row says so.

Three criteria carry inline `Amended 2026-09-09` paragraphs: **AC6, AC20 and AC35**. Each was
evaluated against its **amended** text, and each row says so. The amendments were made by the
orchestrator during preparation, before the plan reached an executor — recorded in the plan's
Branch A section with the two commit SHAs (13c27214 for AC35, 945659cd for AC6/AC20) — and
`evidence/qa-gates/ac6-ac20-amended-spec-verification.md` records the executor's read-only
verification that it was working against the amended spec. No criterion was amended during
execution. That separation matters: an executor free to rewrite the criterion it is judged against is
not gated by that criterion, and this feature did not do that.

## Verdict tally

| Verdict | Count |
|---|---|
| PASS | 35 |
| PARTIAL | 0 |
| FAIL | 0 |
| UNVERIFIED | 0 |

**Blocking findings: 0.**

Two criteria (AC11, AC22) pass on a **disclosed substitution** of an unsatisfiable or vacuous literal
verification for a discriminating one. Both substitutions are judged sound and are recorded as
PASS-with-deviation rather than PARTIAL; the reasoning is in each row and in section 7 of the code
review.

## Acceptance criteria evaluation

### Item 1 — the 250 ms per-row budget

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC1** — budget still `250 * rowCount`, no change to the expression or literal | **PASS** | Read directly: `OlTableExtensions.Etl.cs` line 94 is `int milliseconds = 250 * rowCount;`. The supplied diff for that file shows only an inserted comment block above it (`@@ -81,6 +81,16 @@`, ten added lines, zero removed) and the unrelated `EtlAsyncOld` deletion. The expression and its numeric literal are untouched. |
| **AC2** — comment records (a) no recorded measurement, (b) none obtainable here, (c) the live-Outlook capture route by name | **PASS** | All three facts present at Etl.cs 84-93, verified by reading the comment. (a) "rests on no recorded measurement". (b) "none is obtainable in this environment", with the reason: no benchmark harness in any project file, and Moq fixtures whose `GetRowCount`, `GetNextRow` and `GetArray` return in microseconds. (c) named explicitly: "LogTableTiming emits rowCount, columnCount and elapsedMs on its \"EtlAsync complete\" payload, and TaskMaster/log4net.config line 4 sets the root logger to level ALL". The criterion's "All three facts must be present" is satisfied. |
| **AC3** — case-sensitive search for `250 * rowCount` returns no hit outside `EtlAsync` | **PASS** | Read of `OlTableExtensions.Etl.cs` confirms exactly one occurrence, at line 94, inside `EtlAsync`. The former second occurrence (inside `EtlAsyncOld`) disappeared with that method's deletion, which the diff shows. |

### Item 2 — the residual 2000 ms window

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC4** — `TimeProvider? timeProvider = null` is the last parameter, after `timeoutSourceFactory`; no caller outside `OlTableExtensions_Tests.cs` and `DfDeedle.cs` edited to keep compiling | **PASS** | `TableAccess.cs` 36-43 read directly: parameter order is `activeExplorer, token, counter, timeoutMs = 2000, timeoutSourceFactory = null, timeProvider = null`. `timeProvider` is last and follows `timeoutSourceFactory`. On the second clause: the committed footprint is the eleven Write Set paths plus this feature's folder (caller's measurement, corroborated by `ac18-commit-language.md` reporting `UnexpectedPaths: 0`), so no other caller file was touched at all. The new test file binds the new signature, but it is a *new* file rather than an existing caller edited to keep compiling. |
| **AC5** — the named test exists in the named file, uses `ArmingBarrierTimeProvider`, and passes | **PASS** | `GetTableInViewAsyncClockTests.cs` line 170 declares `GetTableInViewAsync_InjectedClock_ArmsAcquisitionDeadlineOnInjectedProvider`. It obtains the barrier through `CreateArmingBarrier()` (line 71, `new ArmingBarrierTimeProvider(new FakeTimeProvider())`), passes it as the `timeProvider` argument at line 189, and awaits `barrier.Armed` at line 192. The criterion's mechanism clause holds: `Armed` is signalled by the barrier only after forwarding `CreateTimer`, so the await cannot complete unless the deadline timer was created on the injected provider. Passing status from `phase3-green.md` and the 4919/4919 run in `phase7-green.md`. |
| **AC6** (amended) — DfDeedle.cs passes `timeProvider`; `DfDeedle_COM_Tests.cs` absent from diff; `DfDeedleEtlTimeoutTests.cs` appears only as the bounded timer-ordering update, adding no assertion and removing none; both classes pass | **PASS** — evaluated against the **amended** text | Four clauses, each checked. (1) `DfDeedle.cs` 148-152 reads `await activeExplorer.GetTableInViewAsync(token, 0, timeProvider: timeProvider)`. (2) `DfDeedle_COM_Tests.cs` is not among the eleven Write Set paths and not in the footprint. (3) The `DfDeedleEtlTimeoutTests.cs` change (+48/−13) is bounded exactly as described: `BuildExplorer` gained one leading `onGetTable` gate parameter (line 76), the line-144 test gained a third `ManualResetEventSlim` (`gateAcquire`, line 149) and one further await/re-arm pair (176-178), and the two sibling tests pass an empty lambda. I read the assertions at 192-196 and at 233-236 — none added, none removed. (4) Both classes pass in the 4919/4919 run. The amendment itself is justified: the measured reason (the latch drops a signal when two timers arm inside one await window, so `Advance(250)` would fire nothing and the assertion inside the `try` would hang rather than fail) is recorded in the plan's Branch A section and is a real property of `ArmingBarrierTimeProvider`, and the rejected Branch B alternative is retained so the choice stays reviewable. |
| **AC7** — the retry test passes, with a fail-before record under evidence (kind: regression-testing) | **PASS** | `evidence/regression-testing/ac7-fail-before.md` records `EXIT_CODE: 1`, `ExpectedExitCode: 1`, 0 passed / 1 failed, and the machine-produced failure text "Expected recordedTimeouts[1] to be 750 ... but found 2000 (difference of 1250)". This is a genuine RED-first record, not a claim: the **count** half of the assertion passed (the factory recorded exactly two invocations, proving the retry branch was entered) while only the **value** half failed, on precisely the defect item 2 closes. The artifact is under the correct evidence kind. The test passes post-change per `phase3-green.md` and the full run. |
| **AC8** — availability of `CreateCancellationTokenSource(TimeProvider, TimeSpan)` proven **by a compile**, not by prose or the package XML | **PASS** | `evidence/other/ac8-createcancellationtokensource-proof.md` with the sanitised MSBuild log beside it. A real call site (`_ = TimeProvider.System.CreateCancellationTokenSource(TimeSpan.FromMilliseconds(1));`) was added, the project-scoped rebuild returned `EXIT_CODE: 0` with 0 warnings and 0 errors, and `CS1061Count: 0`. Critically, the artifact does not stop at the zero count: it records the corroborating token `/out:obj\Debug\UtilitiesCS.dll` twice, proving compilation actually ran rather than being skipped, and excludes both confounders that would produce the same CS1061 (`UsingSystemThreadingTasksPresent: true`, `PackagesDirectoryPresentAtP0T3: true`). The settling call site is not present in the delivered file (I read all 452 lines of `TableAccess.cs`), so it was correctly removed after serving its purpose. |
| **AC9** — the `TaskCanceledException` branch propagates `timeoutMs`, `timeoutSourceFactory` and now `timeProvider`; the `TimeoutException` branch contains no numeric literal timeout argument | **PASS** | Read directly. `TableAccess.cs` 99-105: `GetTableInViewAsync(token, counter + 1, timeoutMs, timeoutSourceFactory, timeProvider)`. `TableAccess.cs` 122-128: `GetTableInViewAsync(token, counter + 1, timeoutMs, timeoutSourceFactory, timeProvider)` — the literal `2000` is gone, and the only numeric in the branch is `counter + 1`, which is not a timeout argument. The stale rationale comment that justified the literal was replaced (118-121) rather than left in place. |
| **AC10** — the factory-precedence test passes | **PASS** | `GetTableInViewAsyncClockTests.cs` 223-256. It supplies both a recording factory and a barrier-wrapped fake clock, then asserts `factoryInvocations.Should().Be(1)` **and** `barrier.Armed.IsCompleted.Should().BeFalse(...)`. The second assertion is what makes the test discriminating: it proves the provider created no timer, not merely that the factory ran. Passing per the full run. |

### Item 3 — the `EtlAsync` tuple contract

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC11** — declares `Task<(object[,]? data, Dictionary<string, int> columnInfo)>`, and a search for the null-forgiving return `data!` returns no hit | **PASS** with disclosed substitution | First clause verified directly: `Etl.cs` line 66 declares exactly `public static async Task<(object[,]? data, Dictionary<string, int> columnInfo)> EtlAsync(`. Second clause as literally written is **unsatisfiable** and was not met: `return (data!, columnDictionary);` still occurs once, at line 63, inside the *synchronous* `ETL` method. The delivered discharge is a 2→1 occurrence-count transition plus an anchored diff proving the removed suppression is the one inside `EtlAsync` (`evidence/qa-gates/ac11-etlasync-tuple.md`). I confirm post-change line 141 reads `return (data, columnDictionary);`. **I judge the substitution sound:** the surviving occurrence belongs to a different method with its own separate pre-existing non-null contract, documented at Etl.cs 25-27 and explicitly outside this feature; changing it would be scope creep. The count form is discriminating (it fails if the wrong suppression is removed or none is) where the zero-hit form is unachievable by any in-scope edit. **It is disclosed, not quietly applied** — in the plan's "Other disagreements" item 1, in the P5-T7 check-off text, and in the evidence artifact. |
| **AC12** — still catches and swallows `TimeoutException`, still calls `tokenSource.Cancel()`, throws no new exception type; the `DfDeedle` guard and its message unchanged in the diff | **PASS** | `Etl.cs` 129-135 read directly: `catch (TimeoutException)` → `logger.Error(...)` → `tokenSource.Cancel();`. No `throw`, no new exception type anywhere in the method. `DfDeedle.cs` 186-193: the `if (tableSnapshot.data is null)` guard and its `InvalidOperationException` message are present verbatim; the supplied diff shows no change to those lines — only the *comment* above them changed, from "through a null-forgiving suppression" to "through a nullable tuple element", which is a required truth correction rather than a guard edit. |
| **AC13** — the contract test passes with its body unchanged, including `data.Should().BeNull()` and the `IsCancellationRequested` assertion | **PASS** | `OlTableExtensionsEtlClockTests.cs` diff is +2/−2, which by size can only be the two doc-comment lines. Read directly: lines 144-145 are `data.Should().BeNull();` and `tokenSource.IsCancellationRequested.Should().BeTrue();`, both present, and the surrounding body (barrier construction at 121, `await barrier.Armed` at 139, `barrier.Advance(250)` at 140, the `finally` gate release) is intact. Passing per the full run. |
| **AC14** — the doc comment on that test no longer describes a null-forgiving suppression | **PASS** | `OlTableExtensionsEtlClockTests.cs` 99-103 now reads "a null data array is returned through the tuple's nullable first element". The phrase "null-forgiving suppression" no longer appears. The replacement is accurate rather than merely different: the mechanism genuinely changed. |

### Item 4 — the inert `(int, int)` overloads

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC15** — no `TimeoutAfter` overload with an `int repeatAttempts` parameter, and no `catch (TimeoutException)` inside a `TimeoutAfter` method | **PASS** | Grep of `TimeOutTask.cs` returns exactly three matches for the pattern: `TimeoutAfter` declared at line 835 (`this Task<TResult>, int, TimeProvider? = null`) and line 904 (`this Task, int, TimeProvider? = null`), neither with a `repeatAttempts` parameter, and a single `catch (TimeoutException)` at line 290. I read lines 265-304 to place it: it is inside `RunWithTimeout`, not inside any `TimeoutAfter`. Zero occurrences of the token `repeatAttempts` in the file. Both clauses hold. |
| **AC16** — repository-wide search of C# source for `EtlAsyncOld` returns no hit | **PASS** | Repository-wide grep returns matches only in `.md`, `.trx` and `.txt` files — sibling features' historical test-run records and this feature's own spec and research. **Zero `.cs` matches.** The criterion is scoped to C# source, so it is met, and the surviving historical records are correct to leave untouched per the spec's "Rewriting historical records" non-goal. |
| **AC17** — the two named tests are gone and the remaining `TimeOutTask` tests across all four partial files pass | **PASS** | Grep of `TimeOutTask_Tests.cs` returns no match for either `TimeoutAfter_GenericTask_WithRepeatAttempts_ReturnsResult` or `TimeoutAfter_NonGenericTask_WithRepeatAttempts_CompletesSuccessfully`. The file's numstat is +6/−27, consistent with deleting two tests (≈24 lines) and adding a six-line comment. All four partial files are covered by the whole-assembly run: 4919 total, 4919 passed, 0 failed. |
| **AC18** — no claim, in code comment, commit message or PR body, that the 500-line cap violation is resolved; the reduction is described as a reduction | **PASS** | `evidence/other/file-size-accounting.md` records `LinesBefore: 1011`, `LinesAfter: 966`, `CapLimit: 500`, `CapStillViolated: true`, and states in terms "This is a reduction, not a resolution". `evidence/qa-gates/ac18-commit-language.md` records `SoakLinesInMessage: 0` and `Resolv500LinesInMessage: 0` and quotes the commit's own "That is a reduction of the 500-line cap violation, not a resolution of it; the file remains 466 lines above the cap". I independently swept every source comment added by this feature and every artifact in the feature folder and found **no** contrary claim. I confirmed the head size myself: `TimeOutTask.cs`'s last content line is 966. (Note: `spec.md` still says 968 in two places — a stale pre-execution estimate recorded as a non-blocking finding in the policy audit. It is not a claim of resolution and does not bear on this criterion.) |

### Item 5 — the stale doc comment

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC19** — no `TableEtlInvoker` hit in `DfDeedle.QfcColumns.cs`; the corrected sentence names `DefaultTableEtl` or the `etl` parameter; the CS1769 rationale sentence is retained | **PASS** | Repository-wide grep for `TableEtlInvoker` returns no hit in `DfDeedle.QfcColumns.cs`. The diff shows the single changed line: "interop types cannot be used as generic type arguments across an assembly boundary (CS1769); the same constraint already applies to the `<c>DefaultTableEtl</c>` seam." Both required properties hold — the name is now `DefaultTableEtl`, and the CS1769 rationale clause is retained rather than dropped. The rationale remains *true* of the new referent, which the spec required: `DefaultTableEtl` is declared over `object` for exactly that reason. The historically accurate past-tense mention at `DfDeedleEtlTimeoutTests.cs` line 242 is correctly untouched, per the spec's explicit instruction. |
| **AC20** (amended) — `DfDeedle_COM_Tests.cs` and every file under `docs/features/**` other than this feature's own documents absent from the diff; `DfDeedleEtlTimeoutTests.cs` appears only as the bounded update AC6 permits | **PASS** — evaluated against the **amended** text | The amendment left the `docs/features/**` clause unchanged and it remains the binding one. The committed footprint is the eleven Write Set paths plus this feature's own folder — no promotion record, no potential entry, no epic manifest edit, no sibling feature folder. This rests on the invoking agent's independent measurement, corroborated by `evidence/qa-gates/ac18-commit-language.md` (`ReportedPaths: 59`, `UnexpectedPaths: 0`, `MissingWriteSetPaths: 0`, with the 48 non-source paths accounted for as this feature's own documents) and by `evidence/qa-gates/ac20-docs-boundary.md`. `DfDeedle_COM_Tests.cs` is absent. The `DfDeedleEtlTimeoutTests.cs` bounded-update clause is verified under AC6. |

### Item 6 — `[DoNotParallelize]`

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC21** — attribute absent from `OlTableExtensions_Tests`; class comment no longer asserts a population driving the 2000 ms window and no longer states a soak is pending; states instead that the item 2 change removed the hazard | **PASS** | Read directly. `OlTableExtensions_Tests.cs` 18-24: the comment reads "Four tests in this class call GetTableInViewAsync, not ten as an earlier comment claimed. GetTableInViewAsync_ImmediateSuccess_CallsGetTableOnceAndReturnsSnapshot now supplies a FakeTimeProvider, and it was the only one that armed a real timed CancellationTokenSource, so no wall-clock deadline governs any test here. The DoNotParallelize attribute that guarded against that deadline expiring under thread-pool contention has therefore been removed." Line 23 is `[TestClass]` and line 24 is the class declaration — **no `[DoNotParallelize]`**. All three sub-clauses hold: the corrected population is four not ten, no soak is mentioned, and the item 2 change is named as the reason. |
| **AC22** — the test at line 1646 supplies a `FakeTimeProvider`, so no test in that class arms a real system-clock `CancellationTokenSource`; verified by a search for `new CancellationTokenSource(` with a numeric argument returning no hit | **PASS** with disclosed substitution | The **substance** clause is verified directly: `GetTableInViewAsync_ImmediateSuccess_CallsGetTableOnceAndReturnsSnapshot` at line 1620 has `typeof(TimeProvider)` in its `Type[]` at line 1645 and passes `new FakeTimeProvider()` at line 1652. The **stated verification method** is vacuous, as the plan discloses: my grep returns four occurrences of `new CancellationTokenSource(` in that file (lines 966, 1254, 1300, 1662) and **every one is the no-argument constructor**, which was equally true before any change — the real 2000 ms source is constructed inside `TimeOutTask.cs` line 53 by the default factory, not in the test file. A check that cannot fail proves nothing, so asserting the substance instead is the right call. **Disclosed** in the plan's "Other disagreements" item 2, which retains the zero-hit search as a corroborating guard explicitly labelled already-true. The ordering constraint spec.md Risks imposes was also honoured: AC22 was checked off at P3-T21, before the attribute removal at P7-T2 and before AC21's check-off at P7-T7. |
| **AC23** — `OlTableExtensionsEtlClockTests` and `DfDeedleEtlTimeoutTests` retain their attributes and their existing reason comments | **PASS** | Read directly. `OlTableExtensionsEtlClockTests.cs` 19-22: `[TestClass]`, comment "Not parallelized: this class drives a real Task.Run gate.", `[DoNotParallelize]`. `DfDeedleEtlTimeoutTests.cs` 21-25: `[TestClass]`, comment "Not parallelized: this class drives a real Task.Run gate and shares the DfDeedle logger, exactly as DfDeedleQfcColumnTimeoutTests does.", `[DoNotParallelize]`. Both attributes and both comments survive unchanged; the `DfDeedleEtlTimeoutTests` edit is confined to `BuildExplorer` and the line-144 test body, well below the attribute region. |
| **AC24** — `TimeOutTask_Tests` retains the attribute and a comment immediately above it records the verified reason, naming both wall-clock races | **PASS** | `TimeOutTask_Tests.cs` 9-17: `[TestClass]`, then a six-line comment immediately above `[DoNotParallelize]` at line 16. It names both races explicitly — "One test races a Task.Delay(200) against a TimeoutAfter(10) and asserts the timeout wins; another races a Task.Delay(50) against a TimeoutAfter(0) and asserts the same" — and states the mechanism ("Both depend on the delay not completing first, which class-level parallelism under a saturated thread pool can invert"). I verified both races are real: the `Task.Delay(200)` / `TimeoutAfter(10)` test is at lines 32-43 and the `Task.Delay(50)` / `TimeoutAfter(0)` test at 45-56. The comment is therefore verified, not merely asserted, which is what the criterion demands. It also records the conversion-to-injected-clock alternative as a deferred follow-up rather than pretending the state is ideal. |
| **AC25** — no criterion satisfied by repeated passing runs; the change record contains no soak; AC21's justification is the item 2 change, not observed green runs | **PASS** | `evidence/other/ac21-justification.md` records `RunsObserved: 0` and `Justification: item 2 change`, and devotes a section to why no run count appears. `evidence/regression-testing/phase7-green.md` explicitly disclaims being the justification: "The run is recorded as evidence that the change compiles and the suite is green, not as the justification for the attribute removal". `ac18-commit-language.md` records `SoakLinesInMessage: 0`. I found no soak anywhere in the record. This is the correct discipline and it is unusually well-observed: the temptation to cite a green full-suite run as post-hoc support for the attribute removal was available and was declined in writing. |

### Ownership boundary

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC26** — the `TableAccess.cs` diff shows no added and no removed line containing `Console.WriteLine`; both diagnostics survive byte-identical including leading indentation | **PASS** | I inspected the supplied production diff for `OlTableExtensions.TableAccess.cs` directly: it contains no `+` or `-` content line matching `Console.WriteLine`. Corroborated by reading the delivered file — both statements are present at post-change lines 96 (inside `catch (TaskCanceledException)`, 20 leading spaces) and 115 (inside `catch (TimeoutException)`, 16 leading spaces), matching the pre-change indentation the spec pinned. `evidence/qa-gates/ac26-ac27-boundary.md` records `ConsoleWriteLineDiffLines: 0` and `ConsoleWriteLineOccurrenceCount: 2`, and uses a `^[+-][^+-]` anchor that correctly excludes the diff's own `---`/`+++` headers rather than relying on a naive filter. |
| **AC27** — the `counter` variable and both `catch` blocks still exist | **PASS** | Read directly: `counter` is declared as the third parameter at `TableAccess.cs` line 39 and used at 48, 85, 96, 97, 101, 115, 116 and 124. `catch (TaskCanceledException)` opens at line 88 and `catch (TimeoutException)` at line 113. All three of feature 826's preconditions hold. |
| **AC28** — no file listed as sibling-owned in Non-goals appears in the diff | **PASS** | The Non-goals list names `QfcItemController.FolderHandling.cs`, `QfcHomeController.cs`, `ProgressViewer.cs`, `StoreWrapperController*.cs`, `Breadcrumb*`, `SDIL Reader/**`, `UtilitiesCS.Test/Properties/AssemblyInfo.cs`, `FolderPredictorTests.cs`, `.editorconfig`, `BannedSymbols.txt`, the two `Console.WriteLine` diagnostics, and every `Console.SetOut` restore. The committed footprint contains none of them — it is the eleven Write Set paths plus this feature's folder, per the caller's measurement and `ac18-commit-language.md`'s `UnexpectedPaths: 0`. The `Console.WriteLine` sub-clause is separately proven under AC26. Corroborated by `evidence/qa-gates/ac28-sibling-ownership.md`. |

### Process, build and coverage

| AC | Verdict | Evidence and reasoning |
|---|---|---|
| **AC29** — all new tests in the named file, with a `<Compile Include>` entry, and the project-file diff adds only that entry | **PASS** | All four new `[TestMethod]` declarations are in `GetTableInViewAsyncClockTests.cs`. The csproj carries `<Compile Include="OutlookObjects\Table\GetTableInViewAsyncClockTests.cs" />` at line 549, exactly one occurrence, inserted in alphabetical neighbourhood between `OlTableExtensionsEtlClockTests.cs` and `OlToDoTable_Tests.cs`. The numstat of **+1 added / 0 removed** (caller's measurement, and the discriminating clause D3 substituted for the plan's stale absolute count) proves no line was reordered or reformatted, which is what the fan-in merge with sibling features depends on. |
| **AC30** — `OlTableExtensions_Tests.cs` gains no new `[TestMethod]`; its diff is deletions and in-place edits only | **PASS** | Grep counts 82 `[TestMethod]` declarations in the head file. The numstat is +15/−39, a net deletion of 24 lines, consistent with removing one 32-line test and making in-place edits: the five-line class comment replacement, four `typeof(TimeProvider)` entries, four corresponding argument lines, and one `new FakeTimeProvider()` argument. Fifteen added lines cannot accommodate a new test method plus its attribute, body and assertions alongside those nine required edits, and no new test name appears in the file. |
| **AC31** — the new test file contains no `Thread.Sleep`, `Task.Delay`, `DateTime.Now`, `Stopwatch`, retry loop or timing tolerance | **PASS** | Grep of `GetTableInViewAsyncClockTests.cs` for all four banned tokens returns zero matches. I read all 289 lines: there is no `for`/`while`/`do` loop of any kind, so no retry loop, and no assertion carries a tolerance (`BeCloseTo`, `BeInRange`, `BeApproximately` or a range comparison). Every deadline is either armed on a `FakeTimeProvider` that is never advanced or neutralised by a never-cancelling `CancellationTokenSource` returned from a test-supplied factory. This is the criterion that matters most for a timeout feature and it is cleanly met. |
| **AC32** — a full toolchain pass recorded: `csharpier check` clean, both MSBuild gates passing with `/t:Rebuild`, test run green, and no `Skipping target "CoreCompile"` for any Write Set project | **PASS** | `qc-csharpier-check.md` (1623 files, clean); `qc-build-analyzers.md` and `qc-build-nullable.md` (both `EXIT_CODE: 0`, 0 warnings, 0 errors, both `/t:Rebuild`, both the CI-parity command form); `phase7-green.md` (4919/4919). `evidence/qa-gates/ac32-non-vacuity.md` records `SkippingCoreCompileCount: 0` in both logs — **and**, importantly, `CscInvocationsForWriteSetProjects: 4` in both. The artifact states why the second figure is load-bearing: a build that compiled nothing also emits no skip message, so the zero count is only meaningful alongside positive proof that compilation ran. That is the correct standard and it is met. |
| **AC33** — baseline and post-change coverage both captured; no regression on changed lines; new and changed code at 90 percent or better; the CLAUDE.md § UT2 testable-denominator figure recorded with the deletion-driven delta attributed | **PASS** | Both processed Cobertura files are committed under the feature's evidence tree. **New and changed code: 20/22 = 0.909091**, at or above the 0.90 floor — I re-performed the division and confirm 0.9090909…, which rounds to and exceeds 0.90. **Changed-line no-regression:** Gate D shows no file that this feature did not shrink carries a negative signed `LinesCovered` delta; the only two negatives (Etl.cs −16, TimeOutTask.cs −14) are the two files whose covered code was deliberately deleted, with matching negative `dValid` (−23, −32). **Testable denominator:** 56029/65402 = 0.856686, above the floor, derived mechanically by a package-name filter rather than by per-class judgment, with `ModulesFilteredOut: 0` recorded as the filter's honest zero result and with both exemption mechanisms confirmed to contribute nothing (no `ExcludeFromCodeCoverage` on any in-scope file — which I verified independently by grep — and no first-party module in `coverage.config`'s exclude block). **Deletion attribution:** DeletionSum 55 − AdditionSum 14 = 41, equal to the Gate A `LinesValid` reduction of 41, **residual 0**. I re-performed this arithmetic; it closes exactly. The artifact also correctly declines to use the raw repo-wide rate as a no-regression gate, on the ground that deleting fully covered lines lowers the aggregate by arithmetic — an argument that happens to run *against* the feature's own favourable outcome, since both raw rates actually rose. |
| **AC34** — the implementation matches the invariant sentence and the four-step trace; a reviewer can follow `timeoutMs` from accept point to retry and confirm the caller's value is no longer discarded | **PASS** | I performed this trace myself against the post-change files rather than reading `evidence/other/ac34-value-trace.md` and accepting it. **Accept point:** `TableAccess.cs` 36-43 takes `timeoutMs` with no validation and forwards it to `RunWithTimeout` at 74-81. **Arm point:** `TimeOutTask.cs` 52-54 builds the deadline source from the resolved factory, outside the `try` opened at 61. **Absorption point:** `TimeOutTask.cs` 65-92, with `strict: false` and `maxAttempts: 1` from `TableAccess.cs` 78-79. **Retry:** `TableAccess.cs` 122-128 passes `timeoutMs`. The caller's value is no longer discarded at any point on the path, and the deadline is armed on `timeProvider ?? TimeProvider.System`. The invariant sentence in Proposed Fix therefore holds as delivered. |
| **AC35** (amended) — the reachability observation captured as an evidence artifact under this feature's `evidence/other/`, with the follow-up recorded there as a deferred epic handoff; no promotion record or other `docs/features/**` file written on this branch; no code change acts on the observation | **PASS** — evaluated against the **amended** text | `evidence/other/ac35-reachability-observation.md` exists at the required location and records `PromotionWrittenOnThisBranch: false`. Its `DeferredHandoff` section is addressed to the epic and lists four items to be filed after merge. No file under `docs/features/**` outside this feature's own folder appears in the footprint (see AC20). No code change acts on the observation — the artifact says so and I confirm it: `RunWithTimeout`'s signatures, `strict` semantics and retry behaviour are unchanged in the diff. **I independently re-derived the traced conclusion** from `TimeOutTask.cs` 40-95 and confirm it: on ordinary expiry the linked token cancels, `catch (TaskCanceledException)` at 65 is entered, `token.ThrowIfCancellationRequested()` at 67 does not throw because the caller's token is not cancelled, the internal recursion at 69-79 exhausts, line 82 logs and throws nothing, and line 94 returns `default`. Nothing escapes, so neither `catch` in `GetTableInViewAsync` is entered and both `Console.WriteLine` diagnostics remain unreachable through that path — which is the fact sibling feature 826 depends on. The artifact's adjacent-path caveat is also correct: `TaskCanceledException` derives from `OperationCanceledException`, so a `catch` typed to the derived class cannot receive the base, and the pre-cancelled-token exception does propagate. The amendment itself resolves a genuine contradiction with AC20 (a promotion writes under `docs/features/potential/promoted/`), and it drops the on-branch write rather than the observation, which is the right trade. |

## Baseline-relative assessment

Relative to base `96fd3dd8`, the feature delivers exactly what the six issue items required and
nothing beyond them:

- The change footprint is **exactly** the eleven declared Write Set paths plus this feature's own
  folder. No `.claude/`, no `.github/`, no `docs/features/epics/`, no `.editorconfig`, no
  `BannedSymbols.txt`, no `UtilitiesCS/UtilitiesCS.csproj`.
- Production timing is unchanged for any caller that supplies neither optional parameter.
- No public API was broken. The one widened return-type element propagates correctly under the
  repository's per-file nullable opt-in model.
- Three tests were deleted **with the code they covered**, and the coverage consequence was measured
  and attributed rather than absorbed silently.
- Two pre-existing 500-line cap violations were reduced and neither was claimed resolved.

## Acceptance Criteria Status

```
### Acceptance Criteria Status
- Source: docs/features/active/2026-09-08-etl-deadline-mechanics-follow-ups-825/spec.md
- Total AC items: 35
- Checked off (delivered): 35
- Remaining (unchecked): 0
- Items remaining: none
```

All 35 criteria were already checked `[x]` before this review. Each was independently re-decided
above and each verdict is PASS, so no box state required changing and none was changed. No criterion
text was altered and no criterion was added or removed by this review.

## Outstanding items owed after merge (not blocking)

Recorded so the epic does not lose them. All four are carried in
`evidence/other/ac35-reachability-observation.md`; the fifth is this review's addition.

1. Capture real ETL durations against folder size from a live Outlook session using the recipe now
   recorded in the budget comment, and revisit the 250 ms per-row constant.
2. Reduce `UtilitiesCS/Threading/TimeOutTask.cs` below the 500-line cap (966 lines, 466 over).
3. File the reachability observation concerning the two `catch` blocks in `GetTableInViewAsync` under
   `strict: false` / `maxAttempts: 1`, together with the adjacent null-return-behind-a-non-null-contract
   defect it exposes.
4. Convert the `TimeOutTask_Tests` wall-clock races to an injected clock, which would allow that
   class's `[DoNotParallelize]` to be removed rather than documented.
5. **Added by this review:** `UtilitiesCS.Test/OutlookObjects/Table/OlTableExtensions_Tests.cs` is
   approximately 1822 lines, roughly 1322 over the cap, and no recorded follow-up names it. The
   repository's file-size rule applies to test code identically. Recommend the epic add it.
