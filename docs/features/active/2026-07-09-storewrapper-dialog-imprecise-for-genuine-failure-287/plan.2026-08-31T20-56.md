# storewrapper-dialog-imprecise-for-genuine-failure (Plan)

- **Issue:** #287
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-08-31T21-56
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug
- **Branch:** `bug/storewrapper-dialog-imprecise-for-genuine-failure-287`
- **Spec (authoritative AC source):** `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/spec.md` v2.0
- **Research:** `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/research/readiness-state-semantics.2026-08-31T21-10.md`

**Fail-closed evidence rule:** Every command-bearing task writes an evidence artifact carrying `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. A missing or field-incomplete artifact means the task stays unchecked and the outcome is BLOCKED or INCOMPLETE, never PASS. P5-T19 is the single stated exception and records the reason in its own task text.

**Evidence accounting rule:** Every evidence path below resolves under `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/evidence/` using the canonical kind folders `baseline`, `regression-testing`, `qa-gates`, `issue-updates`, `other`. No artifact is written under `artifacts/`.

## Decisions record (binding on every task below)

- **D1 — Five files change, no more.** `UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs`, `UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs`, `UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs`, `UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs`, `UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs`. No new source or test file. No `.csproj`, `.props`, `.targets`, or `packages.config` edit. Feature-folder documents and evidence artifacts are the only other paths this plan writes.
- **D2 — Selected copy, quoted verbatim from `spec.md:268-276`.** For `ModelUnavailable` the title is `Store Settings Unavailable` and the message is `Store settings are not available. Retry once startup has completed; if the message persists, the store settings failed to load and the application log records the cause.` For `StoresUnavailable` the title is `Store Settings Loading` and the message is `The store list has not finished loading. Please try again shortly.`
- **D3 — Removed literals.** The message literal `Store settings are not available yet` and the stale doc phrase `shows the same warning as the single-store editor` are both removed from `*.cs`. Both are quoted here in plan prose, outside every command span, so a search asserting their post-change absence is exonerated by the plan-quotation condition of `.claude/rules/plan-acceptance-gates.md`.
- **D4 — Created literals.** The plan creates the tokens `Retry once startup has completed`, `The store list has not finished loading.`, `Store Settings Loading`, `BuildUnavailableMessage(readiness.State)`, `BuildUnavailableTitle(readiness.State)`, and `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs:66-72`. Each is quoted here in prose outside every command span for the same reason.
- **D5 — MSBuild command shape.** Both msbuild gates use `/t:Rebuild`, never `/t:Build`: a warm `/t:Build` skips `CoreCompile` on every project and exits 0 without running analyzers, so the gate would be vacuous. Neither gate passes `/p:Nullable=enable`: this repository opts into nullable per file with `#nullable enable`, and forcing the property conscripts every unannotated file and cannot pass.
- **D6 — Shell.** Every msbuild and vstest command is issued as a `pwsh -NoProfile -Command` or `pwsh -NoProfile -File` invocation with the payload in single quotes. A POSIX shell mangles msbuild switches (`/m` becomes `M:/`) and produces MSB1008.
- **D7 — Test runner.** Test runs go through `scripts/vscode/Invoke-MSTest.ps1` or `scripts/vscode/Invoke-MSTestWithCoverage.ps1`. Both supply `/Settings:`, `/InIsolation`, and `/TestCaseFilter:TestCategory!=LiveOutlook` (`scripts/vscode/Invoke-MSTest.ps1:54`). A bare `vstest.console.exe` call omits that filter and starts a live Outlook process. Every run in this plan passes `-SearchRoot .`, which discovers the whole nine-assembly set in one pass. That is a conservative choice rather than a defect workaround, and the asymmetry behind it is recorded so a later reader does not have to re-derive it: `scripts/vscode/Invoke-MSTest.ps1:107-113` does not array-wrap its discovery pipeline, while `scripts/vscode/Invoke-MSTestWithCoverage.ps1:296-302` does, so a narrower `-SearchRoot` matching exactly one `*.Test.dll` would leave `$testAssemblies` a scalar at the `$testAssemblies.Count` reads on `:115` and `:120`. PowerShell 3.0 and later expose `Count` on a scalar, so that expression is expected to evaluate to 1 rather than to throw under the `Set-StrictMode -Version Latest` at `:77`. The scalar case has not been observed on this branch and no task in this plan depends on which way it resolves, because the full-suite form never produces it and is independently required by D8.
- **D8 — Nine assemblies.** `scripts/vscode/Invoke-MSTest.ps1:120` prints the literal line `Discovered 9 test assemblies.` when discovery is correct. A different count indicates a discovery-filter or build defect and must be recorded as a blocker rather than accepted.
- **D9 — Aborts are not verdicts.** An aggregate run reporting `Test host process crashed` together with `Total tests: Unknown` is an environmental abort carrying no verdict. Re-run the affected assembly before judging; do not record an abort as a failure.
- **D10 — Coverage numbers are read from the Cobertura XML, not from stdout.** `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1:459-491` reads `line-rate` and throws only when the percentage is below 80. On a successful run the script prints no percentage at all, so every numeric coverage headline in this plan is derived by reading the root `<coverage>` element's `line-rate`, `branch-rate`, `lines-covered`, and `lines-valid` attributes of the produced Cobertura file. Those four attributes are confirmed present on this repository's Cobertura output at `docs/features/active/2026-08-07-efcviewer-missing-lineage-and-segment-navigation-439/evidence/qa-gates/issue-439-final.normalized.cobertura.xml:2`.
- **D11 — Coverage output paths are gitignored.** `.gitignore:144` ignores `coverage/*`, so the raw Cobertura files and run logs written under `coverage/` never enter the change footprint. The baseline run writes `coverage/baseline.cobertura.xml`; the final run writes `coverage/post-change.cobertura.xml`.
- **D12 — Base anchor.** Every `git diff` in this plan carries the explicit merge-base SHA recorded in P0-T2, referred to below as BASE_SHA. Its expected value on this branch is `2b85134b42872e405602e6064e02dc9cda6c319b`; the recorded value governs. No `git diff` is written without a ref operand, because an unanchored diff compares against the index and passes vacuously once the change is committed.
- **D13 — Diff pathspec carve-outs.** Footprint diffs exclude the feature folder `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/` and the tracked `.claude/` tree. The feature folder is excluded because this plan is required to write documents and evidence into it; `.claude/` is excluded because it is tracked and an agent session may write agent-memory files there. The exclusion is compensated by an explicit assertion that `.claude/rules/` and `.github/instructions/` carry no change at all. Every porcelain-status acceptance condition in this plan is scoped the same way: it tolerates paths under the feature folder and under `.claude/agent-memory/`. No `.gitignore` entry covers `.claude/agent-memory/`, so a memory write by the executing agent would otherwise make a bare prints-nothing condition unsatisfiable for a reason unrelated to this change, and the feature folder necessarily holds uncommitted evidence at the moment most of these conditions are read.
- **D14 — Never run `git update-index`.** No task in this plan may run it for any reason.
- **D15 — Long runs.** A full-suite run takes on the order of twenty minutes. If a direct invocation exceeds the shell timeout, re-issue it through `Start-Process pwsh -Wait` with standard output and standard error redirected to the log path named by the task, then read the log. The recorded `Command:` field must be the command actually issued.
- **D16 — Viewer-construction assertions are scoped to the two edited test files.** `new StoreWrapperViewer` legitimately occurs nine times elsewhere in `UtilitiesCS.Test`, in `StoreWrapperControllerTests.cs`, `StoreWrapperController_Tests.ButtonAndPopulate.cs`, and `StoreWrapperViewerTests.cs`. A repository-wide zero-hit assertion for that token would therefore be unsatisfiable whatever the executor does. The AC9 assertion is scoped to `UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs` and `UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs`, which are the two files this change edits and which contain no such construction today.

## Copy and code the plan mandates

Both new methods are declared on `StoreLaunchReadinessEvaluator` (`UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs:13`), which is `internal static` and reachable from `UtilitiesCS.Test` through `UtilitiesCS/Properties/AssemblyInfo.cs:19`. `net48` with this repository's language version has no `IsExternalInit`, so `init`, `record`, and `record struct` are unavailable; both methods return plain `string`.

    internal static string BuildUnavailableMessage(StoreLaunchReadinessState state)
    {
        return state switch
        {
            StoreLaunchReadinessState.Ready => throw new ArgumentOutOfRangeException(
                nameof(state),
                state,
                "There is no unavailable message for a ready store model."
            ),
            StoreLaunchReadinessState.StoresUnavailable =>
                "The store list has not finished loading. Please try again shortly.",
            _ => "Store settings are not available. Retry once startup has completed; if the message persists, the store settings failed to load and the application log records the cause.",
        };
    }

    internal static string BuildUnavailableTitle(StoreLaunchReadinessState state)
    {
        return state switch
        {
            StoreLaunchReadinessState.Ready => throw new ArgumentOutOfRangeException(
                nameof(state),
                state,
                "There is no unavailable title for a ready store model."
            ),
            StoreLaunchReadinessState.StoresUnavailable => "Store Settings Loading",
            _ => "Store Settings Unavailable",
        };
    }

The discard arm carries both `ModelUnavailable` and any value produced by a cast to an undefined enum member, which is the conservative contract in `spec.md:252-259`. `ArgumentOutOfRangeException` requires `using System;`, which the evaluator file does not yet carry. The message-method declaration line is 87 characters and the title-method declaration line is 85 characters, each at its eight-space indentation, under CSharpier's default 100-column print width, which this repository does not override with a `.csharpierrc` or an `.editorconfig` `max_line_length`, so the declaration stays on one line and the line-oriented search in P5-T1 resolves it.

Both call sites keep their current shape and replace only the two literal arguments:

    MyBox.ShowDialog(
        StoreLaunchReadinessEvaluator.BuildUnavailableMessage(readiness.State),
        StoreLaunchReadinessEvaluator.BuildUnavailableTitle(readiness.State),
        MessageBoxButtons.OK,
        MessageBoxIcon.Warning
    );

Neither replacement line exceeds the CSharpier print width, so each stays on one line.

### Test-name register

New unit tests, added as one region in `UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs`:

1. `BuildUnavailableMessage_WhenModelUnavailable_ReturnsBoundedRetryCopy`
2. `BuildUnavailableMessage_WhenStoresUnavailable_ReturnsStillLoadingCopy`
3. `BuildUnavailableMessage_WhenReady_ThrowsArgumentOutOfRangeException`
4. `BuildUnavailableMessage_WhenStateIsUndefinedCast_ReturnsModelUnavailableCopy`
5. `BuildUnavailableMessage_ForTheTwoNonReadyStates_ReturnsDifferentStrings`
6. `BuildUnavailableTitle_WhenModelUnavailable_ReturnsStoreSettingsUnavailableTitle`
7. `BuildUnavailableTitle_WhenStoresUnavailable_ReturnsStoreSettingsLoadingTitle`
8. `BuildUnavailableTitle_WhenReady_ThrowsArgumentOutOfRangeException`
9. `BuildUnavailableTitle_WhenStateIsUndefinedCast_ReturnsModelUnavailableTitle`

New and extended wiring tests in the same file:

10. `Launch_WhenStoresWrapperIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer` (existing, at `:25`; extended)
11. `Launch_WhenStoresListIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer` (existing, at `:66`; extended)
12. `Launch_ForModelUnavailableAndStoresUnavailable_ShowsDifferentMessages` (new)

New wiring tests in `UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs`:

13. `Launch_WhenStoresWrapperIsNull_ShowsModelUnavailableCopyAndLeavesViewerNull`
14. `Launch_WhenStoresListIsNull_ShowsStoresUnavailableCopyAndLeavesViewerNull`

Tests 10 through 14 are the regression tests for this defect: each asserts the copy the user actually sees, and each fails before the call sites are rewired. Tests 1 through 9 cover new code and have no meaningful pre-existing state.

### Existing tests that must keep passing

Seven tests in `UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs`: `Launch_WhenStoresWrapperIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer`, `Launch_WhenStoresListIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer`, `EvaluateLaunchReadiness_WhenGlobalsIsNull_ReturnsModelUnavailable`, `EvaluateLaunchReadiness_WhenOlIsNull_ReturnsModelUnavailable`, `EvaluateLaunchReadiness_WhenStoresWrapperIsNull_ReturnsModelUnavailable`, `EvaluateLaunchReadiness_WhenStoresListIsNull_ReturnsStoresUnavailable`, `EvaluateLaunchReadiness_WhenModelAndStoresPopulated_ReturnsReadyWithDisplayNames`. The last five are the AC12 set.

Seven tests in `UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs`: `PopulateRows_ProjectsServiceEntriesIntoRows`, `PopulateRows_WhenServiceReturnsEmpty_BindsEmptyListWithoutException`, `Dgv_CellContentClick_OnReenableColumn_InvokesReenableWithRowIdentityOnce`, `Dgv_CellContentClick_OnHeaderOrNonButtonColumn_DoesNothing`, `Dgv_CellContentClick_WhenRowIndexOutOfRange_DoesNotThrow`, `ReenableAsync_OnSuccess_CallsServiceThenRefetchesDisabledStores`, `ReenableAsync_WhenServiceThrows_SurfacesViaMyBoxDoesNotThrowAndStillRefetches`.

These fourteen names are referred to below as the WATCHED SET.

### Test construction constraints

- The `MyBox.DialogInvoker` seam is `AsyncLocal`-backed (`UtilitiesCS/Dialogs/MyBox.cs:30-31`, accessor at `:41-45`) and must be saved before the swap and restored in a `finally` block, matching `StoreWrapperController_Tests.Launch.cs:34-56`, the repeat at `:75-97`, and `DisabledStoresControllerTests.cs:240-263`.
- The capturing invoker reads `viewer.Text` for the title and `viewer.TextMessage.Text` for the message. `UtilitiesCS/Dialogs/MyBox.cs:121-122` assigns both before `DialogInvoker(viewer)` at `:125`, so the invoker observes the final values.
- No new or edited test constructs `StoreWrapperViewer` or `DisabledStoresViewer`, shows a modal dialog, or creates a window handle. `StoreWrapperController_Tests` is `[TestClass]` plus `[DoNotParallelize]` (`StoreWrapperController_Tests.cs:13-14`), not `[STATestClass]`; no apartment attribute changes. `DisabledStoresControllerTests` is `[TestClass]` (`:22`) and already drives `MyBox.ShowDialog` through the same seam at `:240-263`, so the `MyBoxViewer` construction that `MyBox.ShowDialog` performs internally is already tolerated in that class's default apartment.
- On the `Mock<IOlObjects>` set only `Ol` and `StoresWrapper`. Do not force a setup on the `Task`-returning `LoadAsync`.
- The `DisabledStoresController` Launch tests must not use the existing `CreateController()` helper (`DisabledStoresControllerTests.cs:25-41`), because it assigns a mock into `Viewer` and the tests must observe `Viewer` remaining null. Arrange the controller inline and read `Viewer` through the file's existing `GetInternalProperty` helper at `:267-277`.
- The undefined enum value is written as `(StoreLaunchReadinessState)99`.
- No temporary files, no clock, no randomness, no `Thread.Sleep`, no `Task.Delay`.

### Phase 0 — Baseline capture and C# worktree bootstrap

- [ ] [P0-T1] Read, in the order defined by `.claude/skills/policy-compliance-order/SKILL.md`, the files `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/tonality.md`, and `.claude/rules/plan-acceptance-gates.md`; write `evidence/baseline/phase0-instructions-read.md` carrying `Timestamp:`, `Policy Order:`, and the explicit list of files read. Acceptance: the artifact exists and lists all seven paths.
- [ ] [P0-T2] Record the base anchor by running `pwsh -NoProfile -Command 'git rev-parse HEAD; git rev-parse --abbrev-ref HEAD; git merge-base origin/main HEAD'` and write `evidence/baseline/base-anchor.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` recording the branch name, the head SHA, and the merge-base SHA labelled BASE_SHA. Acceptance: the artifact records a 40-character BASE_SHA, the head SHA, and the branch name actually checked out in the execution worktree; every later task that names BASE_SHA uses this recorded value. The branch name is recorded rather than asserted, because this plan is executed in a worktree the orchestrator creates and that worktree may carry an agent branch name. If `git merge-base origin/main HEAD` does not print `2b85134b42872e405602e6064e02dc9cda6c319b`, record the divergence and re-derive the D1 line citations before proceeding, because every line number in this plan was taken against that base.
- [ ] [P0-T3] Provision the repo-local .NET SDK by running `pwsh -NoProfile -File scripts/vscode/Install-RepoDotNetSdk.ps1` (or by mirroring a populated `.dotnet-sdk` tree from the main checkout), then run `pwsh -NoProfile -Command 'dotnet --version; dotnet --list-sdks'` and write `evidence/baseline/dotnet-sdk-bootstrap.md`. `global.json:3` pins `8.0.205` with `paths` `.dotnet-sdk` then the host location (`global.json:6-9`); without the local SDK, `dotnet --version` prints the `global.json:10` error message instead of a version. Acceptance: `dotnet --version` prints a version beginning `8.0.` and `dotnet --list-sdks` lists a path ending `.dotnet-sdk\sdk`.
- [ ] [P0-T4] Restore NuGet packages by running `pwsh -NoProfile -File scripts/vscode/Invoke-Restore.ps1`, then confirm the analyzer package trees exist with `pwsh -NoProfile -Command 'Test-Path packages/Meziantou.Analyzer.3.0.194; Test-Path packages/Roslynator.Analyzers.5.0.0; Test-Path packages/AsyncFixer.2.1.0; Test-Path packages/Microsoft.CodeAnalysis.BannedApiAnalyzers.5.6.0'`, and write `evidence/baseline/nuget-restore.md`. A fresh worktree has no `packages/`, every project's `EnsureNuGetPackageBuildImports` error target fires before compilation, and a missing analyzer path is CS0006, an error rather than a warning. The four versions above are the ones `UtilitiesCS.Test/UtilitiesCS.Test.csproj:960-967` and `UtilitiesCS.Test/packages.config:3,12-17,24-29,168-173` both name. Acceptance: all four `Test-Path` calls print `True`; if any prints `False`, install the named version into `packages/` and re-run before checking this task off.
- [ ] [P0-T5] Run `pwsh -NoProfile -Command 'dotnet tool restore; dotnet tool list --local'` and write `evidence/baseline/dotnet-tool-restore.md`. CSharpier is pinned to 1.2.6 by the repo tool manifest and version 1 requires a subcommand, so only the `dotnet tool run csharpier format` and `dotnet tool run csharpier check` forms run. Acceptance: `EXIT_CODE: 0` and the `dotnet tool list --local` output names `csharpier` at version 1.2.6, matching the pin in `dotnet-tools.json` at the repository root.
- [ ] [P0-T6] Resolve MSBuild by running `pwsh -NoProfile -Command 'msbuild -version'` and write `evidence/baseline/msbuild-resolution.md` recording the printed version and, when `msbuild` is not resolvable on PATH, the absolute MSBuild path located under the Visual Studio installation that the remaining msbuild tasks will use instead. Acceptance: the artifact records an MSBuild version string and names the exact executable the later msbuild tasks invoke.
- [ ] [P0-T7] Confirm the `dotnet-coverage` global tool is available by running `pwsh -NoProfile -Command 'if (-not (Get-Command dotnet-coverage -ErrorAction SilentlyContinue)) { dotnet tool install --global dotnet-coverage }; dotnet-coverage --version'` and write `evidence/baseline/dotnet-coverage-tool.md`. `scripts/vscode/Invoke-MSTestWithCoverage.ps1:292-293` throws before running anything when the tool is absent, so a coverage baseline would produce no number at all. Acceptance: `dotnet-coverage --version` prints a version and `EXIT_CODE: 0`.
- [ ] [P0-T8] Capture the baseline formatter state by running `pwsh -NoProfile -Command 'dotnet tool run csharpier check .'` and write `evidence/baseline/csharpier-check.md` recording the complete stdout verbatim in `Output Summary:` together with `EXIT_CODE:`. The `check` subcommand is read-only and returns a non-zero exit when any file requires formatting, so the exit code discriminates a clean tree from a drifted one without relying on any particular summary wording; the wording of a clean run has not been observed on this branch and must therefore be transcribed rather than asserted. Acceptance: `EXIT_CODE: 0`. If the exit code is non-zero, the artifact must additionally list every drifted path, and P3-T1 then runs scoped to the five files in D1 instead of repo-wide.
- [ ] [P0-T9] Capture the baseline analyzer build by running `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true'` and write `evidence/baseline/analyzer-build.md` recording `EXIT_CODE:` and the final warning-count and error-count lines. Acceptance: `EXIT_CODE: 0` and the recorded error count is 0.
- [ ] [P0-T10] Capture the baseline nullable build by running `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true'` and write `evidence/baseline/nullable-build.md` recording `EXIT_CODE:` and the final warning-count and error-count lines. Acceptance: `EXIT_CODE: 0` and the recorded error count is 0.
- [ ] [P0-T11] Capture the live baseline test run by running `pwsh -NoProfile -Command '& "scripts/vscode/Invoke-MSTest.ps1" -SearchRoot . -Configuration Debug *>&1 | Tee-Object -FilePath "coverage/p0-testrun.log"'` and write `evidence/baseline/full-test-run.md` recording `EXIT_CODE:`, the literal discovery line, the run summary values for total, passed, failed, and skipped, and the complete list of lines whose first token is `Failed`, labelled BASELINE FAILURE SET. Acceptance: the artifact contains the line `Discovered 9 test assemblies.`, records the four summary counts, and records the BASELINE FAILURE SET, which must be empty. `scripts/vscode/Invoke-MSTestWithCoverage.ps1:326` throws before the post-processing at `:333-344` when the collected run exits non-zero, so a non-green suite leaves the Cobertura output raw and un-post-processed and makes the P0-T13 and P3-T5 line-rate values incomparable. If a failure survives one re-run per D9, record it as a pre-existing blocker and stop for orchestrator direction rather than proceeding. A run reporting `Test host process crashed` with an unknown total is an abort per D9: re-run before recording.
- [ ] [P0-T12] Establish the pre-change status of the two owned test classes by searching `coverage/p0-testrun.log` for each of the fourteen WATCHED SET names listed above, and write `evidence/baseline/store-test-classes.md` recording, for each name, whether it appears on a line whose first token is `Failed`. This baseline is mandatory: the research artifact had no shell and its green claim for these classes is documentary only. Acceptance: none of the fourteen WATCHED SET names appears in the BASELINE FAILURE SET recorded by P0-T11, and the artifact records the P0-T11 `Total tests` value as BASELINE TOTAL. If any does, record it as a pre-existing failure and stop for orchestrator direction rather than proceeding.
- [ ] [P0-T13] Capture baseline coverage by running `pwsh -NoProfile -Command '& "scripts/vscode/Invoke-MSTestWithCoverage.ps1" -SearchRoot . -Configuration Debug -CoverageOutput "coverage/baseline.cobertura.xml" *>&1 | Tee-Object -FilePath "coverage/p0-coverage.log"'` and then read the headline numbers with `pwsh -NoProfile -Command '$x = [xml](Get-Content -Raw "coverage/baseline.cobertura.xml"); $c = $x.DocumentElement; "line-rate=" + $c.GetAttribute("line-rate"); "branch-rate=" + $c.GetAttribute("branch-rate"); "lines-covered=" + $c.GetAttribute("lines-covered"); "lines-valid=" + $c.GetAttribute("lines-valid")'`, writing `evidence/baseline/coverage.md`. Per D10 the script prints no percentage on a successful run, so these four attribute values are the only source of the numeric headline. Acceptance: `EXIT_CODE: 0`; the log contains the line `Post-processing coverage XML for Koverage compatibility...` and a line beginning `Done. Coverage artifact:`, which `scripts/vscode/Invoke-MSTestWithCoverage.ps1` prints at `:338` and `:344` only after the collection exited zero, so their presence is what distinguishes a post-processed Cobertura file from the raw file the throw at `:235-237` leaves behind; the failed-test lines are recorded and the failed set is empty; the artifact records four numeric values and `Output Summary:` states the repository-wide line coverage as a percentage computed from `line-rate`; and the run's `Total tests` value is recorded and equals the BASELINE TOTAL recorded by P0-T12. The totals are required to agree because both wrappers build the same vstest argument list (`scripts/vscode/Invoke-MSTest.ps1:54` and `scripts/vscode/Invoke-MSTestWithCoverage.ps1:76` are identical in `/Settings:`, `/InIsolation`, and `/TestCaseFilter:TestCategory!=LiveOutlook`), so a divergence is a discovery or abort defect and makes the P3-T5 total-count control incomparable. If the instrumented run is not green, record it as a pre-existing blocker and stop for orchestrator direction rather than proceeding, because a raw root `line-rate` is not comparable with the post-processed value P3-T5 reads.
- [ ] [P0-T14] Record the pre-change line count of each of the five files in D1 by running `pwsh -NoProfile -Command '"UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs","UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs","UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs","UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs","UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs" | ForEach-Object { $_ + " " + (Get-Content -Path $_).Count }'` and write `evidence/baseline/file-line-counts.md`. Acceptance: the artifact records five path-and-count pairs and each count is below 500.
- [ ] [P0-T15] Record the pre-change literal inventory by running `pwsh -NoProfile -Command 'git grep -n -F "Store settings are not available yet" -- "*.cs"; "EXIT=" + $LASTEXITCODE'` and `pwsh -NoProfile -Command 'git grep -n -F "shows the same warning as the single-store editor" -- "*.cs"; "EXIT=" + $LASTEXITCODE'`, writing `evidence/baseline/literal-inventory.md`. Acceptance: the first search reports exactly two lines, `UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs:123` and `UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs:167`; the second reports exactly one line in `UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs`. This is the tree-verified before-count that AC6 and AC10 are measured against, and it doubles as the positive control proving the search mechanism resolves tracked C# content.

### Phase 1 — Copy-selection methods and failing wiring tests

- [ ] [P1-T1] In `UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs`, add `using System;` and declare `BuildUnavailableMessage` and `BuildUnavailableTitle` exactly as written in the "Copy and code the plan mandates" section above, placed after `Evaluate` inside `StoreLaunchReadinessEvaluator`. Give each method an XML doc summary, a `param` entry, a `returns` entry, and an `exception` entry naming `ArgumentOutOfRangeException`. The doc text must not contain the token `MyBox` or the token `System.Windows.Forms`, and neither method carries `[ExcludeFromCodeCoverage]`. In the same edit, extend the type-level XML doc at `:7-12` with a paragraph recording that `ModelUnavailable` is also the terminal state for the remainder of an Outlook session once the store load has completed through its catch block, citing the location with the exact text `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs:66-72`. Acceptance: the file contains one declaration line for each method, contains the exact text `TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs:66-72`, and contains no occurrence of `MyBox`, `System.Windows.Forms`, or `ExcludeFromCodeCoverage`.
- [ ] [P1-T2] In `UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs`, add a region holding the nine unit tests numbered 1 through 9 in the test-name register, placed between the existing `Launch` region, which ends at `:100`, and the existing `EvaluateLaunchReadiness` region, which begins at `:102`. Each test carries a one-line summary comment and a compact Arrange-Act-Assert body using FluentAssertions. Tests 1, 2, 6, and 7 assert the exact literals in D2; test 4 asserts the `ModelUnavailable` message for `(StoreLaunchReadinessState)99`; test 9 asserts the `ModelUnavailable` title for `(StoreLaunchReadinessState)99`; tests 3 and 8 assert that an `ArgumentOutOfRangeException` is thrown; test 5 asserts the two non-ready messages are not equal. Acceptance: all nine method names in the register appear exactly once each in the file.
- [ ] [P1-T3] In the same file, extend test 10 (`Launch_WhenStoresWrapperIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer`, at `:25`) and test 11 (`Launch_WhenStoresListIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer`, at `:66`) so their replacement invoker captures `viewer.Text` and `viewer.TextMessage.Text` into locals, and add assertions that the captured title and message equal the D2 values for `ModelUnavailable` and `StoresUnavailable` respectively; then add test 12 (`Launch_ForModelUnavailableAndStoresUnavailable_ShowsDifferentMessages`), which drives `Launch()` once per non-ready state through a single capturing invoker and asserts the two captured messages are not equal. Every seam swap keeps the existing save-and-restore-in-`finally` shape and the existing invocation-count and null-viewer assertions. Acceptance: the file contains the name `Launch_ForModelUnavailableAndStoresUnavailable_ShowsDifferentMessages` exactly once, and both existing `Launch` tests contain an assertion on a captured message value.
- [ ] [P1-T4] In `UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs`, add tests 13 and 14 from the register. Each arranges a `Mock<IApplicationGlobals>` and a `Mock<IOlObjects>` setting only `Ol` and `StoresWrapper`, constructs a `DisabledStoresController` from those globals without the `CreateController()` helper, swaps `MyBox.DialogInvoker` inside a `try` with a restoring `finally`, calls `Launch()`, and asserts the captured message and title equal the D2 values for the state under test and that the controller's `Viewer`, read through the existing `GetInternalProperty` helper, is null. Acceptance: both method names appear exactly once each in the file, and neither test constructs a viewer form.
- [ ] [P1-T5] Build the solution with the analyzer gate by running `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true'` and write `evidence/other/build-after-copy-methods.md`. This task sits before the expect-fail run so that the fail-before evidence records a genuine test outcome rather than a compile error. Acceptance: `EXIT_CODE: 0` and the recorded error count is 0.
- [ ] [P1-T6] [expect-fail] Run `pwsh -NoProfile -Command '& "scripts/vscode/Invoke-MSTest.ps1" -SearchRoot . -Configuration Debug *>&1 | Tee-Object -FilePath "coverage/p1-testrun.log"'` and write `evidence/regression-testing/fail-before-wiring-tests.md` carrying `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, and an `Output Summary:` recording the failed-test lines. Acceptance: the failed set consists of exactly the five regression tests numbered 10 through 14 in the register, and nothing else, P0-T11 having recorded an empty BASELINE FAILURE SET; the nine unit tests numbered 1 through 9 do not appear among the failures. This is the reproduction of the defect: both call sites still show the literal message quoted in D3, so the copy assertions and the differing-message assertion cannot hold.

### Phase 2 — Call-site rewiring and stale-doc correction

- [ ] [P2-T1] In `UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs`, replace the two literal arguments at `:123-124` inside `Launch()` with `StoreLaunchReadinessEvaluator.BuildUnavailableMessage(readiness.State)` and `StoreLaunchReadinessEvaluator.BuildUnavailableTitle(readiness.State)`. Leave `[ExcludeFromCodeCoverage]` at `:116`, the gate at `:120`, `MessageBoxButtons.OK` at `:125`, `MessageBoxIcon.Warning` at `:126`, and the early `return` at `:128` untouched. Acceptance: the file contains `BuildUnavailableMessage(readiness.State)` exactly once and contains no occurrence of the message literal quoted in D3.
- [ ] [P2-T2] In `UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs`, apply the identical replacement to the two literal arguments at `:167-168` inside `Launch()`, leaving `[ExcludeFromCodeCoverage]` at `:160`, the evaluator call at `:163`, the gate at `:164`, `MessageBoxButtons.OK` at `:169`, `MessageBoxIcon.Warning` at `:170`, and the early `return` at `:172` untouched. Acceptance: the file contains `BuildUnavailableTitle(readiness.State)` exactly once and contains no occurrence of the message literal quoted in D3.
- [ ] [P2-T3] In the same file, rewrite the XML summary on `Launch` at `:154-159` so it states that a not-ready model produces the state-specific warning built by the two evaluator methods, naming them inside `c` markup rather than `cref` markup to avoid a CS1574 risk under `/p:TreatWarningsAsErrors=true`, and so it no longer contains the phrase quoted in D3. The remainder of the summary, including the statement that the viewer is left null and the WinForms-shell and coverage-exclusion note, is preserved. Acceptance: the file contains no occurrence of the phrase quoted in D3 and the summary names both `BuildUnavailableMessage` and `BuildUnavailableTitle`.
- [ ] [P2-T4] Rebuild with the analyzer gate by running `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true'` and write `evidence/other/build-after-rewiring.md`. Acceptance: `EXIT_CODE: 0` and the recorded error count is 0.
- [ ] [P2-T5] Run `pwsh -NoProfile -Command '& "scripts/vscode/Invoke-MSTest.ps1" -SearchRoot . -Configuration Debug *>&1 | Tee-Object -FilePath "coverage/p2-testrun.log"'` and write `evidence/regression-testing/pass-after-wiring.md` recording `EXIT_CODE:`, the discovery line, the four summary counts, and the failed-test lines. Acceptance: none of the five regression tests numbered 10 through 14 appears among the failures, none of the nine unit tests numbered 1 through 9 appears among the failures, and the failed set is empty.

### Phase 3 — Final C# QC toolchain loop

- [ ] [P3-T1] Run `pwsh -NoProfile -Command 'dotnet tool run csharpier format .'` and write `evidence/qa-gates/final-csharpier-format.md` recording `EXIT_CODE:`, the command's complete stdout, and the output of `pwsh -NoProfile -Command 'git status --porcelain'` taken immediately afterwards. The tree observation, not the exit code, distinguishes a clean run from a repairing one, because a formatter rewrites files and still exits 0. Acceptance: `git status --porcelain` lists only paths among the five files in D1, paths under `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/`, and paths under `.claude/agent-memory/`, which D13 admits. If P0-T8 recorded pre-existing drift outside those paths, run this task with the five D1 paths supplied as explicit arguments instead of the repository root, and record the deviation and its cause in the artifact.
- [ ] [P3-T2] Run `pwsh -NoProfile -Command 'dotnet tool run csharpier check .'` and write `evidence/qa-gates/final-csharpier-check.md` recording `EXIT_CODE:` and the command's complete stdout verbatim. Acceptance: `EXIT_CODE: 0`. If P0-T8 recorded drift outside the five files in D1, run this task instead as `pwsh -NoProfile -Command 'dotnet tool run csharpier check "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs" "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs" "UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs" "UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs"'`, require `EXIT_CODE: 0` from that scoped form, and record the repo-wide result together with the P0-T8 drift list in the artifact as informational. Record the deviation in P5-T18.
- [ ] [P3-T3] Run `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true'` and write `evidence/qa-gates/final-analyzer-build.md` recording `EXIT_CODE:` and the final warning and error counts. Acceptance: `EXIT_CODE: 0` and the recorded error count is 0.
- [ ] [P3-T4] Run `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true'` and write `evidence/qa-gates/final-nullable-build.md` recording `EXIT_CODE:` and the final warning and error counts. Acceptance: `EXIT_CODE: 0` and the recorded error count is 0.
- [ ] [P3-T5] Run the coverage-enabled test step with `pwsh -NoProfile -Command '& "scripts/vscode/Invoke-MSTestWithCoverage.ps1" -SearchRoot . -Configuration Debug -CoverageOutput "coverage/post-change.cobertura.xml" *>&1 | Tee-Object -FilePath "coverage/p3-testrun.log"'`, then read the headline numbers with `pwsh -NoProfile -Command '$x = [xml](Get-Content -Raw "coverage/post-change.cobertura.xml"); $c = $x.DocumentElement; "line-rate=" + $c.GetAttribute("line-rate"); "branch-rate=" + $c.GetAttribute("branch-rate"); "lines-covered=" + $c.GetAttribute("lines-covered"); "lines-valid=" + $c.GetAttribute("lines-valid")'`, and write `evidence/qa-gates/final-coverage-test-run.md` recording `EXIT_CODE:`, the discovery line, the four summary counts, the failed-test lines, and both the baseline and post-change values of all four coverage attributes. Acceptance: `EXIT_CODE: 0`; the failed set is empty; and the post-change root `line-rate` is greater than or equal to the P0-T13 baseline `line-rate` minus 0.0005, which is the run-to-run tolerance this plan allows for a change that adds only covered lines; and the post-change root `branch-rate` is greater than or equal to the P0-T13 baseline `branch-rate` minus 0.0005. Additionally, the run's `Total tests` value equals BASELINE TOTAL plus twelve, which is the number of new `[TestMethod]` declarations this plan adds (register entries 1 through 9, 12, 13, and 14; entries 10 and 11 are extended rather than added). A smaller total means at least one new test was not discovered, and every absence-based acceptance condition in Phase 5 is then unsupported.
- [ ] [P3-T6] Confirm the loop closed in a single clean pass by running `pwsh -NoProfile -Command 'git status --porcelain'` and comparing its output with the porcelain output P3-T1 recorded, then write `evidence/qa-gates/final-qc-loop-summary.md` naming the five gate artifacts from P3-T1 through P3-T5, their exit codes, and both porcelain listings. If any gate in P3-T1 through P3-T5 failed or changed a tracked file, restart the loop at P3-T1 and record each iteration in this artifact. Acceptance: the artifact records one iteration in which formatting, analyzer build, nullable build, and the coverage-enabled test run all completed with `EXIT_CODE: 0`, and the two porcelain listings agree on every path outside `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/` and outside `.claude/agent-memory/`. The comparison is scoped that way because P3-T2 through P3-T5 each write an evidence artifact under the feature folder by design, so a literal no-file-changed reading would be unsatisfiable; what the condition tests is that no tracked source file moved after the formatting pass.

### Phase 4 — Commit and change-footprint capture

- [ ] [P4-T1] Stage and commit the change with `pwsh -NoProfile -Command 'git add -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs" "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs" "UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs" "UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs" "docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287"; git status --porcelain; git commit -m "fix(store): state-specific readiness dialog copy (#287)"'` and write `evidence/other/change-footprint.md` recording the staged path list, `EXIT_CODE:`, and the resulting commit SHA. The pathspec is explicit rather than an all-paths sweep so that no unrelated queued file is committed onto this branch. `evidence/other/change-footprint.md` is necessarily written after the commit, because it records the commit SHA, so the artifact itself is an uncommitted path under the feature folder until P5-T19 commits the feature folder; no acceptance condition in this plan may assert otherwise. Acceptance: the commit succeeds and the staged path list contains only the five files in D1 and paths under the feature folder.
- [ ] [P4-T2] Capture the anchored change footprint with `pwsh -NoProfile -Command 'git status --porcelain; git diff --name-status BASE_SHA..HEAD -- . ":(exclude)docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287" ":(exclude).claude"'`, substituting the BASE_SHA recorded by P0-T2, and append the result to `evidence/other/change-footprint.md`. The porcelain span accompanies the name-listing diff because a name-listing diff enumerates tracked changes only and cannot observe an untracked path. Acceptance: the diff lists exactly five lines, one per file in D1, each with status `M`, and `git status --porcelain` prints no path outside `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/` and outside `.claude/agent-memory/`. The porcelain condition is scoped rather than empty because the artifact this task appends to was written by P4-T1 after that task's commit and is therefore uncommitted when this task runs, per D13.
- [ ] [P4-T3] Confirm no policy file changed by running `pwsh -NoProfile -Command 'git status --porcelain; git diff --name-only BASE_SHA..HEAD -- ".claude/rules" ".github/instructions"'` and write `evidence/other/policy-file-untouched.md`. This assertion compensates for the `.claude` exclusion applied in P4-T2. The porcelain span is the companion required for a name-listing diff and is expected to list the feature-folder artifacts written since P4-T1; it is recorded in the artifact and is not asserted empty. Acceptance: the diff prints no lines.

### Phase 5 — Acceptance-criteria verification and check-off

Each task below verifies one acceptance criterion against the tree and the recorded evidence, writes its own artifact, and then changes that criterion's checkbox from the unchecked form to the checked form in both `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/spec.md` and `docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/issue.md`. The checkbox token is the full bold form, for example `- [x] **AC1**`; the trailing asterisks are load-bearing, because without them `AC1` is a prefix of `AC10`. Where the spec and the issue disagree, the spec governs. P5-T8 is an evidence-only task and sets no checkbox.

- [ ] [P5-T1] Verify AC1 by running `pwsh -NoProfile -Command 'git grep -n -F "internal static string BuildUnavailableMessage" -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"; git grep -n -F "internal static string BuildUnavailableTitle" -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"; git grep -n -F "System.Windows.Forms" -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"; git grep -n -F "MyBox" -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"; git grep -n -F "ExcludeFromCodeCoverage" -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"'`, writing `evidence/qa-gates/ac01-pure-helpers.md`. Acceptance: the first two searches each report exactly one line and the last three each report no lines; then AC1 is checked off in both documents.
- [ ] [P5-T2] Verify AC2 by running `pwsh -NoProfile -Command 'git status --porcelain; git diff --name-only BASE_SHA..HEAD -- "*.csproj" "*.props" "*.targets" "packages.config" "*/packages.config"; git diff --name-status BASE_SHA..HEAD -- "*.cs"'`, writing `evidence/qa-gates/ac02-no-new-files.md`. Acceptance: the project-file diff prints no lines, and the C# diff prints exactly five lines all with status `M` and no line with status `A`; then AC2 is checked off in both documents.
- [ ] [P5-T3] Verify AC3 by confirming, in `coverage/p3-testrun.log` and `evidence/qa-gates/final-coverage-test-run.md`, that `BuildUnavailableMessage_ForTheTwoNonReadyStates_ReturnsDifferentStrings` does not appear among the failed-test lines, and writing `evidence/qa-gates/ac03-messages-differ.md` naming that test. Acceptance: the named test is absent from the failed set of the final run; then AC3 is checked off in both documents.
- [ ] [P5-T4] Verify AC4 by confirming that `BuildUnavailableMessage_WhenReady_ThrowsArgumentOutOfRangeException` and `BuildUnavailableTitle_WhenReady_ThrowsArgumentOutOfRangeException` are both absent from the failed set of the final run, and writing `evidence/qa-gates/ac04-ready-throws.md` naming both tests. Acceptance: both names are absent from the failed set; then AC4 is checked off in both documents.
- [ ] [P5-T5] Verify AC5 by confirming that `BuildUnavailableMessage_WhenStateIsUndefinedCast_ReturnsModelUnavailableCopy` and `BuildUnavailableTitle_WhenStateIsUndefinedCast_ReturnsModelUnavailableTitle` are both absent from the failed set of the final run, and writing `evidence/qa-gates/ac05-undefined-cast.md` naming both tests. Acceptance: both names are absent from the failed set; then AC5 is checked off in both documents.
- [ ] [P5-T6] Verify AC6 by running `pwsh -NoProfile -Command 'git grep -n -F "Store settings are not available yet" -- "*.cs"; "EXIT=" + $LASTEXITCODE'` and, as the live positive control for the same search mechanism, `pwsh -NoProfile -Command 'git grep -n -F "Store Settings Unavailable" -- "*.cs"; "EXIT=" + $LASTEXITCODE; git grep -n -F "Store Settings Unavailable" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"; "EXIT=" + $LASTEXITCODE; git grep -n -F "BuildUnavailableMessage(readiness.State)" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"; git grep -n -F "BuildUnavailableTitle(readiness.State)" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"'`, writing `evidence/qa-gates/ac06-literal-removed.md`. Acceptance: the first search prints no lines and reports `EXIT=1`, down from the two lines recorded by P0-T15; the control search reports `EXIT=0` and at least one line, and that line set includes the occurrence inside `UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs` that P1-T1 creates, proving the mechanism still resolves tracked C# content; the scoped title-literal search prints no lines and reports `EXIT=1`, down from the two lines at `StoreWrapperController.cs:124` and `DisabledStoresController.cs:168` recorded in the tree today; and each of the two evaluator-call searches prints exactly two lines, one per controller file; then AC6 is checked off in both documents. The control bound is one because P1-T1 is the only task that fixes how many times this literal is written: the two pre-change occurrences at `StoreWrapperController.cs:124` and `DisabledStoresController.cs:168` are removed by P2-T1 and P2-T2, and the test-side assertions added by P1-T2 through P1-T4 may legitimately share one expected-value constant instead of repeating the literal. One guaranteed hit is sufficient for the control's only purpose, which is to prove the search resolves tracked C# content rather than to count occurrences.
- [ ] [P5-T7] Verify AC7 by running `pwsh -NoProfile -Command 'git grep -n -F "[ExcludeFromCodeCoverage]" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"; git grep -n -F "readiness.State != StoreLaunchReadinessState.Ready" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"; git grep -n -F "MessageBoxButtons.OK" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"; git grep -n -F "MessageBoxIcon.Warning" -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"'`, writing `evidence/qa-gates/ac07-shells-unchanged.md`. Acceptance: the attribute search reports three lines, of which exactly one is in `DisabledStoresController.cs`; the gate search reports exactly two lines, one per file; the buttons search and the icon search each report three lines, of which exactly two are in `DisabledStoresController.cs`; then AC7 is checked off in both documents.
- [ ] [P5-T8] Record the negative half of the AC7 evidence by running `pwsh -NoProfile -Command 'git status --porcelain; git diff -U0 BASE_SHA..HEAD -- "UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs" "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"'` and appending the complete removed-line list to `evidence/qa-gates/ac07-shells-unchanged.md`. Acceptance: no removed line contains `ExcludeFromCodeCoverage`, `MessageBoxButtons.OK`, `MessageBoxIcon.Warning`, or `readiness.State !=`, and the removed lines are limited to the four dialog literal lines and the rewritten summary lines. This task sets no checkbox.
- [ ] [P5-T9] Verify AC8 by confirming that `Launch_WhenStoresWrapperIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer`, `Launch_WhenStoresListIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer`, and `Launch_ForModelUnavailableAndStoresUnavailable_ShowsDifferentMessages` all appear in the P1-T6 failed set and none appears in the P3-T5 failed set, writing `evidence/qa-gates/ac08-storewrapper-wiring.md`. Acceptance: all three names behave as stated across the two runs; then AC8 is checked off in both documents.
- [ ] [P5-T10] Verify AC9 by confirming that `Launch_WhenStoresWrapperIsNull_ShowsModelUnavailableCopyAndLeavesViewerNull` and `Launch_WhenStoresListIsNull_ShowsStoresUnavailableCopyAndLeavesViewerNull` appear in the P1-T6 failed set and are absent from the P3-T5 failed set, and by running `pwsh -NoProfile -Command 'git grep -n -F "new DisabledStoresViewer" -- "UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs" "UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs"; git grep -n -F "new StoreWrapperViewer" -- "UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs" "UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs"'`, writing `evidence/qa-gates/ac09-disabledstores-wiring.md`. The search is scoped to the two edited test files for the reason recorded in D16. Acceptance: both names behave as stated across the two runs and both viewer-construction searches print no lines; then AC9 is checked off in both documents.
- [ ] [P5-T11] Verify AC10 by running `pwsh -NoProfile -Command 'git grep -n -F "shows the same warning as the single-store editor" -- "*.cs"; "EXIT=" + $LASTEXITCODE'` and `pwsh -NoProfile -Command 'git grep -n -F "BuildUnavailableMessage" -- "UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs"'`, writing `evidence/qa-gates/ac10-stale-doc-corrected.md`. Acceptance: the first search prints no lines and reports `EXIT=1`, down from the one line recorded by P0-T15; the second reports exactly two lines, one in the rewritten summary and one at the call site; then AC10 is checked off in both documents.
- [ ] [P5-T12] Verify AC11 by running `pwsh -NoProfile -Command 'git grep -n -F "TaskMaster/AppGlobals/AppOlObjects.StoreLoading.cs:66-72" -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"'`, writing `evidence/qa-gates/ac11-permanence-documented.md` and quoting the full paragraph the doc now carries. Acceptance: the search reports exactly one line, and the quoted paragraph states that `ModelUnavailable` is also the terminal state after the caught load failure; then AC11 is checked off in both documents.
- [ ] [P5-T13] Verify AC12 by confirming that the five names `EvaluateLaunchReadiness_WhenGlobalsIsNull_ReturnsModelUnavailable`, `EvaluateLaunchReadiness_WhenOlIsNull_ReturnsModelUnavailable`, `EvaluateLaunchReadiness_WhenStoresWrapperIsNull_ReturnsModelUnavailable`, `EvaluateLaunchReadiness_WhenStoresListIsNull_ReturnsStoresUnavailable`, and `EvaluateLaunchReadiness_WhenModelAndStoresPopulated_ReturnsReadyWithDisplayNames` are absent from the failed sets of both the P1-T6 and the P3-T5 runs, and by running `pwsh -NoProfile -Command 'git status --porcelain; git diff -U0 BASE_SHA..HEAD -- "UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs"'` to confirm no removed line falls inside the body of `Evaluate`, writing `evidence/qa-gates/ac12-evaluate-unchanged.md`. Acceptance: none of the five names appears in either failed set, and the diff removes no line from the `Evaluate` body; then AC12 is checked off in both documents.
- [ ] [P5-T14] Verify AC13 by transcribing into `evidence/qa-gates/ac13-toolchain-clean.md` the `EXIT_CODE:` values recorded in `evidence/qa-gates/final-csharpier-check.md`, `evidence/qa-gates/final-analyzer-build.md`, `evidence/qa-gates/final-nullable-build.md`, and `evidence/qa-gates/final-coverage-test-run.md`, together with the discovery line and the failed set of the final run. Acceptance: all four exit codes are 0, the discovery line reads `Discovered 9 test assemblies.`, the final failed set is empty, and the discovery-count control stated in P3-T5 holds; then AC13 is checked off in both documents.
- [ ] [P5-T15] Verify AC14 by running `pwsh -NoProfile -Command '$x = [xml](Get-Content -Raw "coverage/post-change.cobertura.xml"); $cls = @($x.GetElementsByTagName("class") | Where-Object { $_.GetAttribute("name") -eq "UtilitiesCS.OutlookObjects.Store.StoreLaunchReadinessEvaluator" }); "classes=" + $cls.Count; foreach ($c in $cls) { "class-line-rate=" + $c.GetAttribute("line-rate"); foreach ($m in $c.GetElementsByTagName("method")) { "method=" + $m.GetAttribute("name") + " line-rate=" + $m.GetAttribute("line-rate") }; "uncovered-count=" + (@($c.SelectNodes("lines/line") | Where-Object { $_.GetAttribute("hits") -eq "0" }).Count); "uncovered-lines=" + ((@($c.SelectNodes("lines/line") | Where-Object { $_.GetAttribute("hits") -eq "0" }) | ForEach-Object { $_.GetAttribute("number") }) -join ",") }'` and `pwsh -NoProfile -Command 'git status --porcelain; git diff --name-only BASE_SHA..HEAD -- "coverage.config" "TaskMaster.runsettings" "scripts/vscode/TaskMaster.cli.runsettings"'`, writing `evidence/qa-gates/ac14-new-code-coverage.md`. The class, method, and line element shapes this command reads are confirmed present on this repository's Cobertura output at `docs/features/active/2026-08-07-efcviewer-missing-lineage-and-segment-navigation-439/evidence/qa-gates/issue-439-final.normalized.cobertura.xml:123077-123145`, where the class element opens at 123077, the method block's own `lines` span 123080-123110, and the class-level `lines` span 123113-123144. Acceptance: exactly one class element is found; methods named `BuildUnavailableMessage` and `BuildUnavailableTitle` are both present with `line-rate` of at least 0.9; the class `line-rate` is at least 0.9; the printed `uncovered-lines=` value is transcribed into the artifact verbatim whether or not it is empty, together with the printed `uncovered-count=` value; no number in `uncovered-lines=` falls within the line span of `BuildUnavailableMessage` or of `BuildUnavailableTitle` as those spans stand in the post-change file, the two spans being recorded in the artifact alongside the list; and the exclusion-configuration diff prints no lines; then AC14 is checked off in both documents. `SelectNodes("lines/line")` is scoped to the class-level `lines` child rather than to a descendant traversal, because this repository's Cobertura output emits each line both under `methods/method/lines` and under the class-level `lines`, so a descendant traversal double-counts by including the per-method copies. That shape was measured on the artifact this task cites: for the `StoreLaunchReadinessEvaluator` class element a descendant traversal returns twenty-five line elements, the class-level `lines` child holds thirteen, and the single method block holds twelve.
- [ ] [P5-T16] Verify AC15 by running `pwsh -NoProfile -Command '"UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs","UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs","UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs","UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs","UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs" | ForEach-Object { $_ + " " + (Get-Content -Path $_).Count }'` after the final formatting pass, writing `evidence/qa-gates/ac15-file-sizes.md` with the five post-change counts alongside the P0-T14 baseline counts. Acceptance: all five post-change counts are recorded and every one of them is below 500; then AC15 is checked off in both documents.
- [ ] [P5-T17] Verify AC16 by running `pwsh -NoProfile -Command 'git status --porcelain; git diff --name-only BASE_SHA..HEAD -- . ":(exclude)docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287" ":(exclude).claude"'`, writing `evidence/qa-gates/ac16-footprint.md`. The two carve-outs are the ones declared in D13, and the `.claude` carve-out is compensated by `evidence/other/policy-file-untouched.md` from P4-T3. Acceptance: the diff lists exactly the five files in D1 and nothing else; then AC16 is checked off in both documents.
- [ ] [P5-T18] Write `evidence/other/ac-status-summary.md` recording, for each of AC1 through AC16, the criterion identifier, its final state, and the artifact path that supports it, together with the BASE_SHA, the commit SHA from P4-T1, the baseline and post-change repository-wide line coverage percentages, and any deviation recorded by P0-T8, P3-T1, or P3-T2. Acceptance: the summary lists all sixteen criteria with a supporting artifact path for each.
- [ ] [P5-T19] Commit the documentation and evidence updates with `pwsh -NoProfile -Command 'git add -- "docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287"; git commit -m "docs(287): record acceptance verification evidence"; git status --porcelain'`. This is the last task in the plan and the single stated exception to the fail-closed evidence rule at the top of this document: it writes no artifact, because any artifact it wrote would itself be an uncommitted path at the moment its own acceptance condition is read. Acceptance: the commit succeeds and `git status --porcelain` prints no path outside `.claude/agent-memory/`, which D13 admits because no `.gitignore` entry covers that tree.

## Deviations and open items

- The spec's open item at `spec.md:297-300` records that the maintainer has not confirmed whether pointing the user at the application log is the preferred guidance. The plan implements the spec's selected copy. If the maintainer prefers a different support path, only the two literals in D2 and the assertions in tests 1, 6, 10, 11, 13, and 14 change.
- No `.resx` change is made. Every user-facing string in `UtilitiesCS` is a source literal, so a resource table would be unmatched convention.
- The fourth readiness state that would make permanence directly knowable is out of scope and remains recorded under the spec's Rollout and Follow-up section.

PLANNER-INTERNAL-REVIEW: PASS
CITATION-TO-TREE: PASS
AC-TRACEABILITY: PASS
SCOPE-BOUNDARY: PASS
CITATION: UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs | nullable pragma 1; usings 2-3; type XML doc 7-12; class declaration 13; Evaluate XML doc 15-21; Evaluate 22-39; file ends 41
CITATION: UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs | enum StoreLaunchReadinessState 19-24; struct StoreLaunchReadiness 31-65; EvaluateLaunchReadiness 109-112; ExcludeFromCodeCoverage 116; Launch 117-137; gate 120; message literal 123; title literal 124; MessageBoxButtons.OK 125; MessageBoxIcon.Warning 126; return 128; second ExcludeFromCodeCoverage 438; file ends 478
CITATION: UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs | first MyBox dialog 130-135 with MessageBoxButtons.OK 133 and MessageBoxIcon.Warning 134; Launch XML summary 154-159 with the stale phrase on 156; ExcludeFromCodeCoverage 160; Launch 161-178; evaluator call 163; gate 164; message literal 167; title literal 168; MessageBoxButtons.OK 169; MessageBoxIcon.Warning 170; return 172; file ends 180
CITATION: UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs | Launch region 15-100; test 25; seam try/finally 34-56 with invoker assignments 39 and 55; test 66; seam try/finally 75-97 with invoker assignments 80 and 96; EvaluateLaunchReadiness region 102-212; five readiness tests at 110, 127, 146, 168, 189; stub helper region 214-232; file ends 234; no viewer construction anywhere in the file
CITATION: UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.cs | TestClass 13; DoNotParallelize 14; partial class 15
CITATION: UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs | TestClass 22; CreateController 25-41; seam try/finally 240-263 with invoker assignments 244 and 261; GetInternalProperty 267-277; SetInternalProperty 279-289; file ends 291; no viewer construction anywhere in the file
CITATION: UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperControllerTests.cs | new StoreWrapperViewer 36
CITATION: UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.ButtonAndPopulate.cs | new StoreWrapperViewer 177, 287, 299
CITATION: UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperViewerTests.cs | new StoreWrapperViewer 23, 34, 47, 86, 111
CITATION: UtilitiesCS/Dialogs/MyBox.cs | AsyncLocal storage 30-31; DialogInvoker accessor 41-45; viewer overload 112-127 with title 121, message 122, invoker 125; four-argument overload 129-139
CITATION: UtilitiesCS/Properties/AssemblyInfo.cs | InternalsVisibleTo UtilitiesCS.Test at 19
CITATION: scripts/vscode/Invoke-MSTest.ps1 | argument list with Settings, InIsolation and TestCategory filter 54; StrictMode 77; discovery pipeline without array wrapping 107-113; empty-set guard reading Count 115; discovery line reading Count 120; throw on non-zero exit 129-131
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | CoverageOutput parameter 9; vstest argument tail with Settings, InIsolation and TestCategory filter 76; Invoke-DotnetCoverageCollection 172-243 with the non-zero-exit throw 235-237; dotnet-coverage guard 292-294; array-wrapped discovery pipeline 296-302; no-assemblies throw 304-306; discovery line 315; NoExecute early return 322-324; collection call 326-331; post-processing announcement 338; threshold call 341; Set-Content 343; completion line 344
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1 | Assert-CoberturaLineCoverageThreshold 459-491; eighty-percent throw 487-489; no percentage printed on success
CITATION: global.json | sdk version 3; paths 6-9; errorMessage 10
CITATION: .gitignore | coverage output ignored at 144; no entry covers .claude/agent-memory
CITATION: .csharpierignore | evidence exclusion 4; project-file exclusions 12-14
CITATION: coverage.config | ModulePaths excludes 12-22, UtilitiesCS not excluded
CITATION: UtilitiesCS.Test/UtilitiesCS.Test.csproj | Meziantou import 3; TargetFrameworkVersion 17; Analyzer includes 960-967, being Meziantou 960, Roslynator 961-964, AsyncFixer 965, BannedApiAnalyzers 966-967; AdditionalFiles BannedSymbols.txt 968
CITATION: UtilitiesCS.Test/packages.config | AsyncFixer 2.1.0 at 3; Meziantou.Analyzer 3.0.194 at 12-17; Microsoft.CodeAnalysis.BannedApiAnalyzers 5.6.0 at 24-29; Roslynator.Analyzers 5.0.0 at 168-173
CITATION: dotnet-tools.json | isRoot 3; csharpier entry 5-11 with version 1.2.6 at 6 and the csharpier command at 8; this repository carries no .config/dotnet-tools.json, so the root manifest is the pin P0-T5 reads
CITATION: .editorconfig | no max_line_length key is present anywhere in the file, and the repository root carries no .csharpierrc in any extension, so CSharpier's default 100-column print width governs the two new declaration lines
CITATION: .claude/rules/quality-tiers.md | uniform branch-coverage floor of 75% for languages whose tooling measures it at 34
CITATION: docs/features/active/2026-08-07-efcviewer-missing-lineage-and-segment-navigation-439/evidence/qa-gates/issue-439-final.normalized.cobertura.xml | root coverage attributes 2; StoreLaunchReadinessEvaluator class element 123077; methods element 123078; method element 123079; the method's own lines block 123080-123110 holding twelve line elements; the class-level lines block 123113-123144 holding thirteen line elements; class closes 123145, so a descendant traversal over the class element returns twenty-five line elements while its lines child returns thirteen
CITATION: docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/spec.md | design-summary table 207-213 with StoreWrapperController.cs at 478 lines on 210; placement-rationale headroom of 22 lines at 219; risk-note headroom of 22 lines at 437; method contract 252-259; selected copy 268-276, being ModelUnavailable title 270 and message 271 and StoresUnavailable title 275 and message 276; open item 297-300; acceptance criteria 393-433 with AC1 at 393 and AC16 at 433
CITATION: docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/issue.md | Work Mode marker 12; acceptance criteria mirror 82-108
CITATION: docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/research/readiness-state-semantics.2026-08-31T21-10.md | Store Settings Unavailable occurrence inventory 424-448, recording two production C# lines and zero test C# lines
CITATION: .claude/rules/plan-acceptance-gates.md | rule table 31-42; attribution window 52-54; placeholder markers 211
AC-INVENTORY: AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8, AC9, AC10, AC11, AC12, AC13, AC14, AC15, AC16
AC-MAPPING: AC1 | IMPLEMENTATION: P1-T1 | TESTS: P1-T2 | EVIDENCE: evidence/qa-gates/ac01-pure-helpers.md
AC-MAPPING: AC2 | IMPLEMENTATION: D1 scope constraint enforced by P1-T1 through P2-T3 | TESTS: P5-T2 | EVIDENCE: evidence/qa-gates/ac02-no-new-files.md
AC-MAPPING: AC3 | IMPLEMENTATION: P1-T1 | TESTS: BuildUnavailableMessage_ForTheTwoNonReadyStates_ReturnsDifferentStrings | EVIDENCE: evidence/qa-gates/ac03-messages-differ.md
AC-MAPPING: AC4 | IMPLEMENTATION: P1-T1 | TESTS: BuildUnavailableMessage_WhenReady_ThrowsArgumentOutOfRangeException and BuildUnavailableTitle_WhenReady_ThrowsArgumentOutOfRangeException | EVIDENCE: evidence/qa-gates/ac04-ready-throws.md
AC-MAPPING: AC5 | IMPLEMENTATION: P1-T1 | TESTS: BuildUnavailableMessage_WhenStateIsUndefinedCast_ReturnsModelUnavailableCopy and BuildUnavailableTitle_WhenStateIsUndefinedCast_ReturnsModelUnavailableTitle | EVIDENCE: evidence/qa-gates/ac05-undefined-cast.md
AC-MAPPING: AC6 | IMPLEMENTATION: P2-T1 and P2-T2 | TESTS: P5-T6 | EVIDENCE: evidence/qa-gates/ac06-literal-removed.md
AC-MAPPING: AC7 | IMPLEMENTATION: P2-T1 and P2-T2 | TESTS: P5-T7 and P5-T8 | EVIDENCE: evidence/qa-gates/ac07-shells-unchanged.md
AC-MAPPING: AC8 | IMPLEMENTATION: P2-T1 | TESTS: Launch_WhenStoresWrapperIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer, Launch_WhenStoresListIsNull_ShowsUserMessageAndDoesNotThrowOrOpenViewer, Launch_ForModelUnavailableAndStoresUnavailable_ShowsDifferentMessages | EVIDENCE: evidence/qa-gates/ac08-storewrapper-wiring.md
AC-MAPPING: AC9 | IMPLEMENTATION: P2-T2 | TESTS: Launch_WhenStoresWrapperIsNull_ShowsModelUnavailableCopyAndLeavesViewerNull and Launch_WhenStoresListIsNull_ShowsStoresUnavailableCopyAndLeavesViewerNull | EVIDENCE: evidence/qa-gates/ac09-disabledstores-wiring.md
AC-MAPPING: AC10 | IMPLEMENTATION: P2-T3 | TESTS: P5-T11 | EVIDENCE: evidence/qa-gates/ac10-stale-doc-corrected.md
AC-MAPPING: AC11 | IMPLEMENTATION: P1-T1 | TESTS: P5-T12 | EVIDENCE: evidence/qa-gates/ac11-permanence-documented.md
AC-MAPPING: AC12 | IMPLEMENTATION: P1-T1 leaves Evaluate untouched | TESTS: the five EvaluateLaunchReadiness tests | EVIDENCE: evidence/qa-gates/ac12-evaluate-unchanged.md
AC-MAPPING: AC13 | IMPLEMENTATION: P3-T1 through P3-T6 | TESTS: P3-T5 coverage-enabled full-suite run | EVIDENCE: evidence/qa-gates/ac13-toolchain-clean.md
AC-MAPPING: AC14 | IMPLEMENTATION: P1-T1 places the logic outside the coverage-exempt shells | TESTS: the nine unit tests numbered 1 through 9 | EVIDENCE: evidence/qa-gates/ac14-new-code-coverage.md
AC-MAPPING: AC15 | IMPLEMENTATION: P1-T1 places the documentation in the evaluator rather than the controller | TESTS: P5-T16 | EVIDENCE: evidence/qa-gates/ac15-file-sizes.md
AC-MAPPING: AC16 | IMPLEMENTATION: P4-T1 explicit staging pathspec | TESTS: P5-T17 | EVIDENCE: evidence/qa-gates/ac16-footprint.md
UNRESOLVED-GAPS: NONE

SELF-REVIEW: RE-DERIVED THIS PASS
RE-DERIVED: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | Invoke-DotnetCoverageCollection is declared at 172 and throws at 235-237 when the collected run exits non-zero; the call to it is at 326-331 and the Koverage post-processing, the Assert-CoberturaLineCoverageThreshold call at 341, and the Set-Content that persists the processed XML at 343 all sit downstream at 333-344, so a non-green suite leaves the Cobertura output raw and un-post-processed, which is the mechanism the P0-T11, P0-T13, P1-T6, P2-T5, P3-T5, and P5-T14 conditions rest on
RE-DERIVED: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | the two literals the P0-T13 acceptance names in this pass are the Write-Output at 338, which emits Post-processing coverage XML for Koverage compatibility..., and the Write-Output at 344, which emits Done. Coverage artifact: followed by the resolved output path; both sit downstream of the throw at 235-237 reached through the call at 326-331, so neither is printed when the instrumented run exits non-zero, and 344 sits downstream of the threshold assertion at 341 and the Set-Content at 343 as well, which is why P0-T13 requires both lines rather than the first alone
RE-DERIVED: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | sibling region of that citation re-derived: the CoverageOutput parameter default at 9, the vstest argument tail at 76, the dotnet-coverage guard 292-294, the array-wrapped discovery pipeline 296-302, the no-assemblies throw 304-306, the discovery line 315, and the NoExecute early return 322-324 which no task in this plan reaches because no task passes -NoExecute
RE-DERIVED: scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1 | Assert-CoberturaLineCoverageThreshold 459-491 reads only the root line-rate, throws below eighty at 487-489, and emits no percentage on a successful run; it applies no branch-coverage check at all, so the post-change branch-rate condition P3-T5 carries is an assertion this plan makes rather than one the script already enforces
RE-DERIVED: scripts/vscode/Invoke-MSTest.ps1 | discovery pipeline 107-113, empty-set guard 115-117, discovery line 120, NoExecute return 124-126, and the throw on a non-zero vstest exit 129-131, so a P0-T11 run with a non-empty failure set does not exit 0
RE-DERIVED: cross-runner comparability | scripts/vscode/Invoke-MSTest.ps1:54 and scripts/vscode/Invoke-MSTestWithCoverage.ps1:76 build identical inner vstest argument tails, being /Settings:$RunSettingsPath, /InIsolation and /TestCaseFilter:TestCategory!=LiveOutlook, so the Total tests value P0-T11 records, the Total tests value P0-T13 records, and the Total tests value P3-T5 records all count the same discovered set; that identity is what the total-equality clause added to P0-T13 in this pass and the BASELINE TOTAL plus twelve control in P3-T5 both rest on
RE-DERIVED: docs/features/active/2026-08-07-efcviewer-missing-lineage-and-segment-navigation-439/evidence/qa-gates/issue-439-final.normalized.cobertura.xml | for the StoreLaunchReadinessEvaluator class element at 123077 this repository's Cobertura output carries line elements twice: the single method block's lines at 123080-123110 holds twelve, and the class-level lines child at 123113-123144 holds thirteen, the extra one being line 37; the class closes at 123145, so a descendant traversal returns twenty-five and double-counts by including the per-method copies, while SelectNodes("lines/line") from the class element returns the thirteen class-level entries only, which is the scoping the P5-T15 command adopts in this pass
RE-DERIVED: P5-T15 output shape | the command as revised in this pass prints uncovered-count= and uncovered-lines=, so both values the acceptance transcribes are values the command actually emits; the previous form printed a scalar count only, so its demand for an uncovered-line list named output that never existed and its count was inflated by the double-counting recorded above
RE-DERIVED: .claude/rules/quality-tiers.md | the uniform branch-coverage floor of 75% for languages whose coverage tooling measures branch coverage is stated at 34
RE-DERIVED: UtilitiesCS.Test/UtilitiesCS.Test.csproj | the Analyzer includes span 960-967, being Meziantou.Analyzer 960, the four Roslynator entries 961-964, AsyncFixer 965, and the two BannedApiAnalyzers entries 966-967, with AdditionalFiles BannedSymbols.txt at 968; the previous 960-964 citation covered only two of the four package trees the project requires
RE-DERIVED: UtilitiesCS.Test/packages.config | AsyncFixer 2.1.0 at 3, Meziantou.Analyzer 3.0.194 at 12-17, Microsoft.CodeAnalysis.BannedApiAnalyzers 5.6.0 at 24-29, Roslynator.Analyzers 5.0.0 at 168-173
RE-DERIVED: dotnet-tools.json | the tool manifest is the repository-root file, carrying isRoot at 3 and the csharpier entry at 5-11 with version 1.2.6 at 6; the path .config/dotnet-tools.json does not exist in this worktree, so the P0-T5 acceptance names the root manifest
RE-DERIVED: .editorconfig | the file carries no max_line_length key, and the repository root carries no .csharpierrc in any extension, so CSharpier's default 100-column print width governs both new declaration lines
RE-DERIVED: .csharpierignore | evidence exclusion 4, generated-artifact exclusions 5-8, project-file exclusions 12-14; none of the five files in D1 is excluded, so the scoped fallback form of P3-T2 formats and checks all five
RE-DERIVED: UtilitiesCS/OutlookObjects/Store/StoreLaunchReadinessEvaluator.cs | nullable pragma 1, usings System.Collections.Generic 2 and System.Linq 3 with no using System, type doc 7-12, class 13, Evaluate doc 15-21, Evaluate 22-39 with members indented eight spaces, file ends 41; the message declaration line is 79 characters bare and 87 at that indentation and the title declaration line is 77 bare and 85 indented, both under 100, the two differing by the two characters that separate the identifiers BuildUnavailableMessage and BuildUnavailableTitle; the prose closing the copy-and-code section was corrected in this pass, having previously asserted 87 characters for both declarations
RE-DERIVED: UtilitiesCS/Dialogs/MyBox.cs | AsyncLocal storage 30-31, DialogInvoker accessor 41-45, and the viewer overload 112-127 assigning viewer.Text at 121 and viewer.TextMessage.Text at 122 before calling DialogInvoker at 125, so a capturing invoker observes the final title and message, which is what the P1-T3 and P1-T4 assertions read
RE-DERIVED: UtilitiesCS/OutlookObjects/Store/StoreWrapperController.cs | the file is 478 lines, giving the 22 lines of headroom the spec now records; ExcludeFromCodeCoverage 116 and 438, gate 120, message literal 123, title literal 124, MessageBoxButtons.OK 125, MessageBoxIcon.Warning 126
RE-DERIVED: UtilitiesCS/OutlookObjects/Store/DisabledStoresController.cs | MessageBoxButtons.OK 133 and 169, MessageBoxIcon.Warning 134 and 170, stale doc phrase 156, ExcludeFromCodeCoverage 160, gate 164, message literal 167, title literal 168
RE-DERIVED: P5-T6 title-literal figure | Store Settings Unavailable occurs in tracked C# on exactly two lines today, StoreWrapperController.cs:124 and DisabledStoresController.cs:168, both production and both removed by Phase 2, and zero times in test C#, so the scoped zero-hit condition P5-T6 carries is false before the change and true after, while the repository-wide control keeps its one-hit lower bound guaranteed by P1-T1
RE-DERIVED: P5-T7 counts | ExcludeFromCodeCoverage three hits with exactly one in DisabledStoresController.cs, the gate two hits one per file, MessageBoxButtons.OK three hits with two in DisabledStoresController.cs, MessageBoxIcon.Warning three hits with two in DisabledStoresController.cs
RE-DERIVED: UtilitiesCS.Test/OutlookObjects/Store/StoreWrapperController_Tests.Launch.cs | Launch region 15-100, tests at 25 and 66, invoker assignments 39, 55, 80 and 96, EvaluateLaunchReadiness region 102-212, the five readiness tests at 110, 127, 146, 168 and 189, stub-helper region 214-232, closing brace 234, and no viewer construction anywhere in the file
RE-DERIVED: UtilitiesCS.Test/OutlookObjects/Store/DisabledStoresControllerTests.cs | the file is 291 lines; TestClass 22, CreateController 25-41 with the mock assigned into Viewer at 39, invoker assignments 244 and 261, GetInternalProperty 267, SetInternalProperty 279, and no viewer construction anywhere in the file
RE-DERIVED: UtilitiesCS.Test | new StoreWrapperViewer occurs nine times, at StoreWrapperViewerTests.cs 23, 34, 47, 86 and 111, StoreWrapperControllerTests.cs 36, and StoreWrapperController_Tests.ButtonAndPopulate.cs 177, 287 and 299, so a repository-wide zero-hit form would be unsatisfiable and D16 stands; new DisabledStoresViewer occurs zero times anywhere under the test project
RE-DERIVED: test-count control | the register adds twelve new [TestMethod] declarations, being entries 1 through 9, 12, 13 and 14, while entries 10 and 11 are the existing methods at StoreWrapperController_Tests.Launch.cs:25 and :66 that are extended rather than added, which is the figure the P3-T5 discovery-count control uses
RE-DERIVED: docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/spec.md | design-summary table 207-213 recording StoreWrapperController.cs at 478 lines on 210; the corrected headroom of 22 lines at 219 and 437, consistent with the 478-line count measured in the tree; selected copy 268-276; open item 297-300; AC6 at 405-408 requiring that neither Launch contains a dialog message or title literal and that both obtain both strings from the evaluator; acceptance criteria AC1 at 393 through AC16 at 433, unshifted by the headroom correction
RE-DERIVED: docs/features/active/2026-07-09-storewrapper-dialog-imprecise-for-genuine-failure-287/issue.md | Work Mode full-bug at 12, acceptance mirror AC1 at 82 through AC16 at 108, unshifted by the spec correction
