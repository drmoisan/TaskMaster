# breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread (Plan)

- **Issue:** #900
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-17 (date only: the planning session had no shell clock, so a minute-precision value could not be observed; executor-written evidence filenames still use the `yyyy-MM-ddTHH-mm` convention)
- **Status:** Ready for preflight (round 2 revision applied 2026-09-17; the deltas are itemized in `## Planner Self-Review Record (Round 2)`)
- **Version:** 1.1
- **Work Mode:** full-bug (persisted marker `- Work Mode: full-bug` in `issue.md`; `spec.md` is the sole acceptance-criteria source; `user-story.md` is absent by default and not required)
- **Requirements source:** `docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/spec.md`, section `## Acceptance Criteria` (AC1 through AC8)
- **Branch:** `bug/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900`
- **Base:** `origin/main` (branch cut at `91746d2e4776a59ee1db1856c5c490a009c4958b` per `issue.md`; every diff in this plan is anchored to the merge base with `origin/main` computed after a fetch, never to a bare local `main`)
- **Execution session requirement:** this plan's command-bearing tasks (P0-T4 onward; P0-T1 through P0-T3 use only the Read and Grep tools and `git` invocations) require an `atomic-executor` session that either is not worktree-isolated, or has been granted a working `pwsh` tool surface. A worktree-isolated session refuses `pwsh` categorically regardless of invocation shape (`-Command` and `-File` alike, because the isolation guard keys on `pwsh` occupying the command-name position), and the orchestrator is responsible for launching the execution session accordingly. P0-T4 determines the channel and stops the run with `CHANNEL UNAVAILABLE` when neither declared rung works; the executor never modifies hook or permission configuration to obtain a channel.
- **Pre-implementation gate requirement:** the hook `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` classifies every edit of a `.cs` file and every non-exempt `git add`/`git commit` as implementation and denies them unless `artifacts/orchestration/orchestrator-state.json` is seeded and ready (fact 20). P0-T3 records that readiness read-only and stops with `PRE-IMPLEMENTATION GATE NOT SEEDED` when it is absent; seeding the checkpoint is the orchestrator's responsibility before launch, and the executor never creates, edits, or works around that checkpoint or the hook.
- **Task Count:** 46 (Phase 0: 11, Phase 1: 3, Phase 2: 5, Phase 3: 4, Phase 4: 8, Phase 5: 15)

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS. One scoping carve-out: in a task that both commits and records that commit, the artifact's `EXIT_CODE:` field is scoped to the pre-commit observation the task actually runs and names, and the commit's own exit code is reported under a separately named field or in the executor's final message, because an artifact cannot record the exit code of the commit that includes it.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

---

## Constraint Blocks Received

The five constraint blocks supplied with the planning directive were received and are acknowledged for verbatim propagation: `TEST PARALLELISM`, `OBSERVED-FAILING CRITERIA`, `DIFF BASES`, `COMMIT AND SCOPE`, `BASH DISCIPLINE`. This plan issues no further delegation; the orchestrator relays the blocks to `atomic-executor` with the execution delegation. Their operative consequences are encoded below: `scripts/vscode/TaskMaster.cli.runsettings` is read, hashed, and never written; no task adds `[DoNotParallelize]`, `Workers=1`, retries, sleeps, or timeouts; every gate has a recorded before-value on the unfixed tree; every diff is anchored to `MERGE-BASE`, `origin/main..HEAD` or `origin/main...HEAD` (P5-T9 runs the two-dot and three-dot forms and records whether they agree); the plan commits its own work; every command is a `git` invocation or runs inside a `pwsh -NoProfile` process.

## Spec Amendments Applied During Planning (2026-09-17)

Three findings led to four in-place amendments of non-AC sentences in `spec.md` after re-derivation against the tree. The acceptance-criteria text (AC1 through AC8) is unchanged. Each amended site carries the in-place marker `amended 2026-09-17 during planning` (four markers; the spec header names the marker a fifth time); P0-T2 verifies the markers read-only and the executor never edits `spec.md` except to flip an AC checkbox.

1. **Test Strategy item 2, mutation placement.** The earlier wording placed the temporary `ClearViewerDispatcher(scope.Viewer)` call immediately after constructing each `ViewerScope`. The rewritten tests read `scope.Viewer.UiDispatcher.CheckAccess()` inside the worker delegate as their precondition, so that placement nulls `_uiDispatcher` before the precondition reads it and the delegate throws `NullReferenceException` at the precondition. The boundary assertions are then never exercised and the observed failure is `BeOfType` reporting `NullReferenceException`, not the message assertion the spec predicts. The insertion now sits inside the delegate, after the precondition and before the guarded call. The same item's mechanism citation was corrected: for the two-argument `InitializeBreadcrumbPipeline` overload the throw site is `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs:80` (`BreadcrumbUiDispatcher.CaptureCurrent()` evaluated as a `BreadcrumbBridgeCoordinator` constructor argument); the factory at `:74-76` returns the injected operations and does not reach `CaptureCurrent()`. The research artifact (section 5.2) carries the same imprecise citation; it is a research record and is left as written.
2. **Scope & Non-Goals and Rollout & Follow-up, who files the potential entries.** Both sections assigned the filing to the executor. `.claude/skills/feature-promotion-lifecycle/SKILL.md` permits only the MCP route (`mcp__drm-copilot__new_potential_bug_entry`) and the executor has no MCP tool surface. Both sentences now assign the filing to the orchestrator from the handoff record P5-T14 writes.
3. **Repro & Evidence, phase reference.** "Phase 2 of the plan" now reads "the plan's mutation-testing phase (Phase 3)".

## Execution Conventions

- `FEATURE` denotes `docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900`. `FILE` denotes `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`. Both tokens are expanded verbatim by the executor.
- **Evidence paths.** Every artifact is written under `FEATURE/evidence/<kind>/` with `<kind>` one of `baseline`, `regression-testing`, `qa-gates`, `issue-updates`, `other`. Nothing is written to `artifacts/baseline/`, `artifacts/baselines/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/regression-testing/` or `artifacts/post-change/`. `artifacts/csharp/coverage.xml` (written by P5-T8 after the final clean loop iteration) is a git-ignored review-tooling input, not evidence.
- **Artifact filenames.** Each artifact is named `<task-id>-<name>.<TS>.md` where `<TS>` is the executor's write time in `yyyy-MM-ddTHH-mm`, equal to the artifact's own `Timestamp:` field. Later tasks cite an artifact by its task id and name and locate it with the glob `<task-id>-<name>.*.md`, which must match exactly one file. The single exception is `FEATURE/evidence/baseline/phase0-instructions-read.md`, whose name `.claude/skills/atomic-plan-contract/SKILL.md` fixes exactly.
- **Artifact fields.** Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and `Output Summary:`; `ExpectedExitCode:` is added only where a task says so, and each artifact carries at most one expectation. A task that runs only `git` invocations and Read-tool reads (P0-T1, P0-T2, P0-T3) records `CHANNEL: NONE` because no `pwsh` process is involved; every other command-bearing task records the value P0-T4 fixed. `Command:` records the repository-relative form; no artifact records an absolute host path, an account name, or a machine name, and every artifact is checked for those before it is committed (P5-T15 sweeps the feature folder with the run-time-derived account token).
- **Two-rung channel, determined once by P0-T4.** The plan declares two rungs and P0-T4 probes each with a trivial payload before any other command runs. Rung 1 is `pwsh -NoProfile -Command '<payload>'`, with the payload's lines joined by `; ` and wrapped in single quotes so the calling shell performs no interpolation; every payload in this plan is written to contain no single-quote character, and string literals inside payloads are double-quoted. Rung 2 writes the payload, byte for byte and one statement per line, to the single fixed helper path `coverage/plan900-helper.ps1` (rewritten in place for every task; `coverage/*` is git-ignored at `.gitignore:144`; the file is not a deliverable, is registered in no project file, and is asserted by no acceptance condition) and runs `pwsh -NoProfile -File coverage/plan900-helper.ps1`; if a `Write` PreToolUse hook denies that helper path, the same content is written to the session scratchpad (outside the worktree; its absolute path is never recorded) and run from there. Rung 2 is not a fallback from a worktree-isolation refusal of rung 1: the isolation guard keys on `pwsh` in the command-name position, so a session that refuses rung 1 for that reason refuses rung 2 with the same text. P0-T4 records the outcome of each rung and fixes `CHANNEL:` as `COMMAND` (rung 1 worked), `FILE` (rung 1 was refused for a reason other than isolation and rung 2 worked), or `UNAVAILABLE` (both refused), and every later command-bearing task uses the channel P0-T4 fixed and records it in its own `CHANNEL:` field. When P0-T4 records `UNAVAILABLE`, the run stops at P0-T4 (P0-T5 through P0-T11 and every later task are not attempted) and the executor reports `CHANNEL UNAVAILABLE` with the refusal text; a refusal is a session-launch condition for the orchestrator, not a plan defect, and the executor does not modify hook or permission configuration to obtain a channel.
- **Working directory.** Every command runs with the current directory at the worktree root, and every path is repository-relative. Payloads use PowerShell cmdlets (`Get-Content`, `Set-Content`, `Get-FileHash`, `Get-ChildItem`) with `-LiteralPath` and never `[System.IO.File]` with a relative path, because `Set-Location` does not update .NET's `CurrentDirectory`.
- **File hashes.** Every SHA-256 value this plan records or compares (`RUNSETTINGS-HASH:`, `PRE-EDIT-HASH:`, `HASH-BEFORE:`, `HASH-AFTER:`, `FIX-HASH:`, `RUNSETTINGS-HASH-NOW:`, `POST-FORMAT-HASH:`, and the `SHA256 =` line of `CMD-CENSUS`) is the `Hash` property of `Get-FileHash -Algorithm SHA256 -LiteralPath <repository-relative path>`, an uppercase 64-character hexadecimal string; no other hashing method is used, so every comparison is between values produced the same way.
- **Tool resolution inside payloads.** `vswhere.exe` is resolved as `Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"`; `vstest.console.exe` as `& $vswhere -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe" | Select-Object -First 1`; `MSBuild.exe` as `& $vswhere -latest -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1` (the shape `scripts/vscode/Invoke-Restore.ps1:31` uses). The `Command:` field records the CLAUDE.md-canonical `msbuild ...` / `vstest.console.exe ...` form plus the note `resolved through vswhere`.
- **Console counters.** On an all-green run `vstest.console.exe` prints `Test Run Successful.`, `Total tests:`, `Passed:`, `Total time:` and no `Failed:` or `Skipped:` line. Per-test outcomes, counts and failure messages are read from the TRX the run writes (`ResultSummary/Counters` attributes `total`, `executed`, `passed`, `failed`; `UnitTestResult` attributes `testName`, `outcome`; `Output/ErrorInfo/Message`). There is no `skipped` counter; do not report one. TRX files stay under `TestResults/` (`.gitignore:39`) and are never copied into `FEATURE/evidence/`; every run passes an explicit `/ResultsDirectory:` and a quoted `"/Logger:trx;LogFileName=<task-id>.trx"` so no file name carries an account or machine name. When a results directory holds more than one TRX after a re-run, select by `Sort-Object LastWriteTime -Descending | Select-Object -First 1` and record the count found and the selected file's `LastWriteTime`.
- **Token census.** Every occurrence count in this plan is produced by `CMD-CENSUS` (Command Reference), which reads `FILE` once and counts case-sensitive, non-overlapping occurrences with `[regex]::Matches($content, [regex]::Escape($token)).Count`. Counts are occurrences, not lines. `Select-String` is not used for counts because it is case-insensitive by default and counts lines.
- **Refs.** P0-T3 records `MERGE-BASE:` (the 40-character output of `git merge-base HEAD origin/main` after `git fetch origin main`) and `BASE-SHA:` (the output of `git rev-parse HEAD` before any task edits a file). Later command spans write the bare tokens `MERGE-BASE` and `BASE-SHA`; the executor substitutes the recorded 40-character values, because no shell variable survives a task boundary. No command in this plan computes a merge base against, or diffs against, a bare local `main` ref; `origin/main` after a fetch is the only base ref.
- **Inherited paths (rule, not list).** Clause A: every path already changed relative to the merge base before the first task ran, captured mechanically by P0-T3 as `INHERITED-CLAUSE-A:` (the union of `git diff --name-only MERGE-BASE...HEAD` and `git status --porcelain --untracked-files=all`). Clause B: every path under the prefix `.claude/agent-memory/`, which is tracked and is written by agents while this plan executes. Scope gates subtract the captured Clause A set and the Clause B prefix and never subtract `FILE`.
- **Git gates are pathspec-scoped.** Every `git diff` and `git status` gate in this plan carries an explicit pathspec (`QuickFiler`, `QuickFiler.Test`, `FEATURE`, or `FILE`) so that agent-memory writes and sibling-worktree residue cannot fail it. No gate asserts an empty unscoped porcelain output.
- **Commits.** This plan commits three times (P0-T11, P2-T5, P5-T15), each as `git add -- <pathspecs>` followed by a separate `git commit` invocation, one command segment per invocation, never chained. P0-T11 stages only the exempt tree `docs/features/active/...` and commits with a single `-m "<subject>"` string plus the same pathspec operand, which is the form the issue #539 orchestration-bookkeeping exemption models (fact 20): no `-F`, no `$`, backtick, `<` or `>` character anywhere on the line. P2-T5 and P5-T15 stage `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, which is outside every exempt tree, so the exemption cannot apply to them whatever the message form; they use `git commit -F <message-file>` with the message file under `FEATURE/evidence/other/` staged by the same pathspec, and they run only in a session whose pre-implementation checkpoint P0-T3 recorded as ready. A commit message names no gated tool. A PreToolUse refusal of any `git add` or `git commit` span, or of any edit of a `.cs` file (P2-T1, P3-T1, P3-T2, P3-T3, P3-T4, and any repair under P2-T4, P5-T3 or P5-T4), is reported verbatim as `PRE-IMPLEMENTATION GATE BLOCKED` and stops the run; this is a STOP-AND-REPORT condition under the COMMIT AND SCOPE block, not a workaround: the executor does not modify hooks, checkpoints, permission configuration, or another item's state.
- **Stall handling for repository-wide runs.** P0-T10 and P5-T5 take tens of minutes; do not abort them on a short timeout. Each carries `"/Blame:CollectHangDump;TestTimeout=4min;HangDumpType=None"`, so a stalled test is named in a `Sequence_*.xml` file under the run's results directory rather than by a dump. If the testhost idles with no CPU progress across two samples twenty seconds apart, terminate only that run's own `vstest.console` and `testhost` processes, record the `Sequence_*.xml` test name, and re-run the identical command once; an unchanged re-run is not a toolchain-loop restart.

## Write Set (exhaustive; anything outside this set is out of scope)

Tests: `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` (`FILE`): the two `_WorkerThread_ThrowsBoundaryDiagnostic` tests' documentation and bodies (pre-edit lines 199 through 265) and one new private static helper inserted after the `ClearViewerDispatcher` helper (pre-edit line 332). No other line of the file changes. The two mutation tasks in Phase 3 edit this file temporarily and revert it; P2-T1 is the only task that changes its persisted state.

Documentation and evidence: `FEATURE/spec.md` (AC checkbox state only; the three planner amendments were applied before execution), `FEATURE/plan.2026-09-16T23-27.md` (task checkbox state only), `FEATURE/evidence/**`.

Explicitly outside the Write Set: every file under `QuickFiler/` (production), `QuickFiler.Test/QuickFiler.Test.csproj` (the file is already registered at line 96), `scripts/vscode/TaskMaster.cli.runsettings`, `FEATURE/issue.md` (carries no acceptance criteria in full-bug mode; not edited), `FEATURE/research/**`, `.claude/**`, `QuickFiler.Test/Viewers/BreadcrumbPopupBoundaryCoverageTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbUiThreadDispatchTests.cs`.

Local, git-ignored, never staged: `coverage/**` (`.gitignore:144`, with `coverage/.gitkeep` tracked so the directory always exists), `TestResults/**` (`.gitignore:39`), `artifacts/csharp/coverage.xml` (`.gitignore:57`; written once by P5-T8), `packages/**`, `.dotnet-sdk/**`.

Read but never written by any task: `artifacts/orchestration/orchestrator-state.json` (the pre-implementation checkpoint P0-T3 reads; the orchestrator owns it), `.claude/hooks/**`, `.claude/settings.json`.

## Verified Repository Facts

Each fact was re-derived against the assigned worktree on 2026-09-17 during plan authoring; facts 1, 11, 20 and 21 were re-derived again in the round 2 revision (see `## Planner Self-Review Record (Round 2)`).

1. `FILE` is 419 lines (`@(Get-Content -LiteralPath FILE).Count`; the file ends with a newline, and a viewer that shows a 420th empty line is counting the terminator) and declares exactly seven `[TestMethod]` members at lines 39, 89, 129, 168, 204, 237, 280. `InitializeBreadcrumbPipeline_WorkerThread_ThrowsBoundaryDiagnostic` is declared at line 204 (documentation from line 199, `[TestMethod]` at 203, body closes at 229); `ConfigureBreadcrumbDropDown_WorkerThread_ThrowsBoundaryDiagnostic` at line 237 (documentation from 231, `[TestMethod]` at 236, body closes at 265). Line 266 is blank; line 267 opens the next test's documentation.
2. `FILE` contains `Task.Run(` at lines 214, 246, 293 and `.GetAwaiter()` at 217, 253, 296. The third pair belongs to `InitializeBreadcrumbPipeline_NullOwningDispatcher_DoesNotThrow` (line 280), which is out of scope and unchanged.
3. `FILE` pre-edit occurrence counts (positive controls for every count gate; P1-T1 re-records them): `Task.Run(` 3; `.GetAwaiter()` 3; `Throw<InvalidOperationException>(` 2; `error.Message.Contains(` 2; `Contains(` 2; `NotBeOfType<ObjectDisposedException>()` 2; `ClearViewerDispatcher(scope.Viewer);` 1 (line 289); `[TestMethod]` 7; `vacuously` 1 (line 56); `RunOnDedicatedWorkerThread(` 0; `Exception captured = RunOnDedicatedWorkerThread(` 0; `new Thread(` 0; `IsBackground = true` 0; `thread.Join();` 0; `Join(` 0; `Join()` 0; `UiDispatcher.CheckAccess()` 0; `isOwnerThread` 0; `BeOfType<InvalidOperationException>()` 0; `captured.Message.Should().Contain(` 0; `#900` 0; `action();` 0; `Thread.Sleep` 0; `Task.Delay` 0; `[Timeout` 0; `DoNotParallelize` 0. The word `captured` appears twice in prose (lines 277, 368), which is why every gate uses a longer literal.
4. `FILE` lines 1 through 12 import `System`, `System.Collections.Generic`, `System.Drawing`, `System.Reflection`, `System.Threading`, `System.Threading.Tasks`, `System.Windows.Threading`, `FluentAssertions`, `Microsoft.VisualStudio.TestTools.UnitTesting`, `Moq`, `QuickFiler.Viewers`, `UtilitiesCS.OutlookObjects.Folder`. `System.Threading.Thread` and `System.Exception` need no new directive; `System.Threading.Tasks` stays in use by `InertDropDownHost` (`Task<bool>`, `Task.FromResult`) and by line 293. The file carries no `#nullable` directive.
5. `ClearViewerDispatcher` is declared at `FILE` lines 322 through 332 and assigns `null` to the private `_uiDispatcher` field through reflection; line 333 is blank and line 334 opens the `InertDropDownHost` documentation. `ViewerScope` (396-417) constructs `new QuickFiler.ItemViewer()` on the calling thread under a plain `SynchronizationContext`.
6. `QuickFiler/Viewers/ItemViewer.cs` line 20 carries `[ExcludeFromCodeCoverage]` on the `ItemViewer` partial class; line 27 captures `_uiDispatcher = Dispatcher.CurrentDispatcher;`; lines 64 through 68 expose it as the public `UiDispatcher` property.
7. `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs`: `ThrowIfOffUiBoundary` is lines 432 through 447, returns at 435-438 when the owner is null, and throws at 442-445 with the message `"{operation} must be called on the thread that owns this ItemViewer. The calling thread is not the thread the viewer was constructed on."` when `owning.CheckAccess()` is false. The guard is the first statement of `InitializeBreadcrumbPipeline(provider, operations)` (line 51) and of the three-argument `ConfigureBreadcrumbDropDown` (line 233).
8. With the guard escaped (null owner), a first-time two-argument `InitializeBreadcrumbPipeline` call proceeds through `EnsureBreadcrumbLifecycle(() => operations)` (lines 74-76, which returns the injected operations at line 364 and constructs the lifecycle coordinator at 365-378 with argument-null checks only) and then evaluates `BreadcrumbUiDispatcher.CaptureCurrent()` at line 80 as an argument to `new BreadcrumbBridgeCoordinator(...)` (77-81). `BreadcrumbUiDispatcher.CaptureCurrent()` (`QuickFiler/Viewers/BreadcrumbUiDispatcher.cs:44-56`) throws `InvalidOperationException` with the message `Breadcrumb UI components must be constructed on an owning UI synchronization context.` when `SynchronizationContext.Current` is null, which it is on a thread the test creates. The three-argument `ConfigureBreadcrumbDropDown` reaches the same throw through lines 241-243, `EnsureBreadcrumbLifecycle` line 364 (`operationsFactory()` with `BreadcrumbPopupUiOperations.CaptureCurrent`) and `QuickFiler/Viewers/BreadcrumbPopupUiOperations.cs:80-81`. No other member on either path reads `SynchronizationContext.Current` (the only reads under `QuickFiler/Viewers/` are viewer constructors, `BreadcrumbUiDispatcher.cs:47` and `:271`).
9. `scripts/vscode/TaskMaster.cli.runsettings` is nine lines: `<MSTest><Parallelize><Workers>0</Workers><Scope>ClassLevel</Scope></Parallelize></MSTest>`. It configures no logger and no data collector.
10. `QuickFiler.Test/QuickFiler.Test.csproj` registers `Viewers\ItemViewerBreadcrumbThreadAffinityTests.cs` at line 96, defaults `Platform` to `AnyCPU` at line 12, and defines `OutputPath` only under `Debug|AnyCPU` (32-36), `Release|AnyCPU` (41-44), `Debug|x86` (49-51), `Release|x86` (53-55). A project-file build must pass `/p:Platform=AnyCPU`; the solution builds pass `"/p:Platform=Any CPU"`.
11. `dotnet-tools.json` at the repository root pins `csharpier` `1.2.6` (commands `format`, `check`). `global.json` pins SDK `8.0.205` with `rollForward: latestFeature` and `paths: [".dotnet-sdk", "$host$"]`; `scripts/vscode/Install-RepoDotNetSdk.ps1` installs to `.dotnet-sdk` and its success marker is the directory `.dotnet-sdk/sdk/8.0.205` (lines 36, 56, 102-104). `.csharpierignore` (15 lines) excludes `**/evidence/**`, `*.cobertura.xml`, `*.coverage`, `*.coveragexml`, `*.trx`, `*.csproj`, `*.props`, `*.targets`, and no other pattern; `artifacts/csharp/coverage.xml` matches none of them, so a repository-wide `csharpier format .` run after that file exists would process and may rewrite it (CSharpier 1.2.6 formats `*.xml`), which is why P5-T8 writes it only after the final clean loop iteration.
12. `scripts/vscode/Invoke-Restore.ps1` runs `msbuild <sln> /t:Restore /p:Configuration=Debug "/p:Platform=Any CPU" /p:RestorePackagesConfig=true /m` and requires PowerShell 7 (`pwsh`).
13. `scripts/vscode/Invoke-MSTestWithCoverage.ps1` guards its entry point at line 437 (`if ($MyInvocation.InvocationName -ne '.')`), so dot-sourcing exposes `ConvertTo-DerivedCoverageSettingsXml` (97-134), which appends the module exclusion `.*\.Test\.dll$` to `coverage.config`; every `*.Test.dll` is therefore outside the instrumented denominator. `Get-DotnetCoverageArgumentList` (41-95) hardcodes `/TestCaseFilter:TestCategory!=LiveOutlook` with no extension point, which is why the repository-wide runs here issue `dotnet-coverage collect` directly with the same argument shape plus the workstation hang exclusions. `Invoke-DotnetCoverageCollection` throws at 261-263 on a non-zero collector exit, before any post-processing, so the runner cannot be used when a pre-existing failing test exists. `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1` declares `ConvertTo-KoverageCoberturaXml -XmlContent <string> -RepoRoot <string>` at line 407 and dot-sources `Invoke-MSTestWithCoverage.Threshold.ps1` (line 4), which declares `Assert-CoberturaLineCoverageThreshold` (floor 80 percent) and `Assert-CoberturaBranchCoverageThreshold` (floor 75 percent).
14. Four `UtilitiesCS.Test` classes stall a local `vstest` run on this workstation because of a hung shell icon handler (observed 2026-09-04, excluded by the issue #781 plan on 2026-09-05): `HelperClasses.ShellUtilities_Tests`, `HelperClasses.ShellUtilitiesStatic_Tests`, `HelperClasses.SysImageListHelperTests`, `EmailIntelligence.OSBrowser_Tests`. Both repository-wide runs exclude them by `FullyQualifiedName!~` and rely on CI to cover them. `DictionaryExtensions_Tests.TryAddValuesAsync_UpdatesExistingValue` is a known intermittent failure under parallel coverage runs (issue #780).
15. `UtilitiesCS.Test/Threading/UiThreadInitContract_Tests.cs:79-104` declares `internal static class ApartmentThreadRunner` with `RunOnThread(ApartmentState, Action)`: `new Thread(() => { try { action(); } catch (Exception ex) { captured = ex; } })`, `IsBackground = true`, `Start()`, `Join()`, returns the captured exception. It is `internal` to a different test assembly and is the shape the new private helper copies; the target file's own remark (lines 21-25) establishes declaring private helpers locally rather than widening another file's accessibility.
16. `.claude/skills/feature-promotion-lifecycle/SKILL.md` permits potential-entry creation only through `mcp__drm-copilot__new_potential_bug_entry` and states that agent sessions have no approved non-MCP branch for promotion work.
17. `docs/features/archive/2026-07-07-onedrive-writer-timeout-test-determinism-253/evidence/regression-testing/fail-before-exception.2026-07-07T14-05.md` is the dossier precedent: `Timestamp:`, `WhyFailingRunImpossible:`, `## Alternative Proof`, `## Output Summary`.
18. `FEATURE/issue.md` line 4 is `- Work Mode: full-bug`. `FEATURE/spec.md` `## Acceptance Criteria` holds eight lines beginning `- [ ] AC1. ` through `- [ ] AC8. `, each wrapped onto continuation lines; the check-off edit changes only `- [ ] ACn.` to `- [x] ACn.`. `FEATURE/user-story.md` does not exist.
19. `.claude/hooks/validate-planner-output.ps1` requires every task's opening line to carry a slash- or backslash-bearing path token; every task line below does.
20. `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` reads its checkpoint from the fixed path `artifacts/orchestration/orchestrator-state.json` (line 27) and its readiness predicate `Test-OrchestrationReady` (lines 223-251) requires the string properties `issue-num` and `feature-folder` (line 231-232), `route_id` or, when that is absent, `path_selected` (233-236), and a truthy `lifecycle_ready` (238-239), with `feature-folder` beginning `docs/features/active/` (247); an absent file or any missing property denies with the reason at line 442. The issue #539 exemption in `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` admits only `git add` and `git commit` segments (lines 230-235) whose every operand lies under one of the five trees at lines 22-28 (`docs/features/epics/`, `docs/features/parallel/`, `docs/features/active/`, `docs/features/potential/`, `artifacts/orchestration/`), models `-m` and `--message` with a following value token (260-267) and the joined forms `--message=` and `-m<value>` (268-272), denies every other dash-leading option including `-F` (273), requires at least one pathspec operand (283-285), and denies any line containing `$`, backtick, `>` or `<` (line 33, tested at 327-329). The exemption is allow-side only: it can admit an exempt commit, and it never suppresses the gate for a `.cs` edit or a commit that stages `QuickFiler.Test/...`.
21. `artifacts/csharp/coverage.xml` is not excluded by `.csharpierignore` (fact 11), and `git status --porcelain` does not list it because `.gitignore:57` ignores it.

## Decisions

- **D-1 Dedicated thread, private helper named `RunOnDedicatedWorkerThread`.** Adopted per `spec.md` Proposed Fix. Rejected: `TaskCreationOptions.LongRunning` (a scheduler hint, not a contract) and any retry or tolerance (prohibited by the TEST PARALLELISM block). Every local name the executor creates is pinned so count gates cannot drift: helper locals `captured`, `thread`, `error`; test locals `captured`, `isOwnerThread`.
- **D-2 `NotBeOfType<ObjectDisposedException>()` is kept.** `BeOfType<InvalidOperationException>()` is an exact-type check in FluentAssertions 8.10.0 and already excludes the derived `ObjectDisposedException`; the explicit `NotBeOfType` is retained as documentation because AC3 names the exclusion. Its count (2) is an invariant, not a transition gate.
- **D-3 Mutation M1 sits inside the delegate.** Per spec amendment 1: `ClearViewerDispatcher(scope.Viewer);` is inserted after the precondition assertion and before the guarded call. Predicted failing assertion in both tests: `captured.Message.Should().Contain(...)`, because the captured exception is the `CaptureCurrent()` `InvalidOperationException` (fact 8), which passes `BeOfType` and fails the operation-name check. A different failing assertion is a BLOCKED finding, reported, not repaired.
- **D-4 Mutation M2 proves the precondition is exercised.** AC2's clause "so the boundary assertion cannot pass vacuously" is a behavioral claim; the OBSERVED-FAILING block requires it to be seen failing. Inserting `action();` as the first statement of `RunOnDedicatedWorkerThread` runs the delegate inline on the test thread before the dedicated thread starts, so `isOwnerThread` is `true`, `BeFalse` throws `AssertFailedException` out of the helper (outside its `try`), and both tests fail with a message containing `vacuously`. Reverted with the same mechanics as M1.
- **D-5 The fix is committed before the mutations (P2-T5).** The revert checks in Phase 3 are then anchored: `git diff --exit-code HEAD -- FILE` exits 0 and the file's SHA-256 equals `FIX-HASH:`. `git checkout -- FILE` is the authorized recovery if a manual removal leaves a residual diff; it restores the committed fixed state and nothing else, because `FILE` is the only path named.
- **D-6 Repository-wide coverage runs use `dotnet-coverage collect` directly** (fact 13) with the CLI runsettings, `/InIsolation`, the LiveOutlook exclusion, the four hang exclusions (fact 14) and a named TRX. The raw Cobertura (roughly 10 MB) stays under `coverage/`; the artifacts record the six root attributes only. The baseline and final `line-rate` are compared under the two-branch comparability rule (P5-T6) because the cross-assembly merge is order- and parallelism-sensitive. The 80 percent floor is recorded (`FLOOR:`) and is non-blocking: the figure is pre-existing merge-base debt, this change adds no production line to the denominator (fact 6, fact 13), and the issue #424 and #781 precedents ratified that handling for bug-fix plans.
- **D-7 Changed-code coverage is `NOT MEASURABLE` by construction.** No production line changes; `QuickFiler.Test.dll` is excluded by the derived settings (fact 13) and `ItemViewer` is `[ExcludeFromCodeCoverage]` (fact 6). P5-T6 records both class counts and the determination; the substitute behavioral evidence is the pass-after runs plus the two mutation runs.
- **D-8 Analyzer and nullable gates are baseline-relative.** `EXIT_CODE` must equal the Phase 0 value (`ANALYZE-BASELINE-EXIT`, `NULLABLE-BASELINE-EXIT`), the error count must equal the Phase 0 error count, and no diagnostic line may name `ItemViewerBreadcrumbThreadAffinityTests.cs`. `csharpier check .` is absolute (exit 0). Non-vacuity of each rebuild is the echoed compiler line carrying `/out:obj\Debug\QuickFiler.Test.dll` in a normal-verbosity file log.
- **D-9 Follow-ups are handed off, not filed, by the executor** (fact 16, spec amendment 2). P5-T14 writes the two potential-entry bodies verbatim; the orchestrator files them through the MCP tool.
- **D-10 No task edits `scripts/vscode/TaskMaster.cli.runsettings`.** P0-T4 records its SHA-256 as `RUNSETTINGS-HASH:` (with `Get-FileHash -Algorithm SHA256 -LiteralPath`, immediately after the channel probe succeeds, because P0-T3 is deliberately free of `pwsh` so that its stop branches are reachable in a session with no channel); P4-T2, P4-T3 and P5-T9 assert the hash is unchanged.
- **D-11 Channel determination and gate readiness are Phase 0 stop conditions, not Phase 1 discoveries.** P0-T3 (checkpoint readiness, read-only) and P0-T4 (channel probe) each carry an explicit stop branch so that a session launched without a `pwsh` surface or without a seeded checkpoint stops after at most four tasks with a named condition (`PRE-IMPLEMENTATION GATE NOT SEEDED`, `CHANNEL UNAVAILABLE`), rather than after eleven tasks at P1-T1's first payload. Neither branch is repaired by the executor.
- **D-12 The review-input copy is made once, after the loop closes.** `artifacts/csharp/coverage.xml` is written by P5-T8 only when `LOOP: CLEAN PASS` holds, so no `csharpier format .` iteration ever runs after the file exists (fact 21).

## Command Reference

Each block is a payload in the sense of the channel-probe convention: one complete statement per line, no line continuations, no single-quote characters, no comment lines. Uppercase tokens `FILTER`, `TASKID`, `STAGE`, `MERGE-BASE`, `BASE-SHA` are substituted by the executor as each task states.

**CMD-CENSUS** (token census over `FILE`; prints one `TOKEN <token> = <count>` line per token and `LINES = <n>`):

    $path = "QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs"
    $content = Get-Content -LiteralPath $path -Raw -Encoding UTF8
    $tokens = @("Task.Run(", ".GetAwaiter()", "Throw<InvalidOperationException>(", "error.Message.Contains(", "Contains(", "NotBeOfType<ObjectDisposedException>()", "ClearViewerDispatcher(scope.Viewer);", "[TestMethod]", "vacuously", "RunOnDedicatedWorkerThread(", "Exception captured = RunOnDedicatedWorkerThread(", "new Thread(", "IsBackground = true", "thread.Join();", "Join(", "Join()", "UiDispatcher.CheckAccess()", "isOwnerThread", "BeOfType<InvalidOperationException>()", "captured.Message.Should().Contain(", "#900", "action();", "Thread.Sleep", "Task.Delay", "[Timeout", "DoNotParallelize")
    foreach ($t in $tokens) { Write-Output ("TOKEN " + $t + " = " + [regex]::Matches($content, [regex]::Escape($t)).Count) }
    Write-Output ("LINES = " + @(Get-Content -LiteralPath $path).Count)
    Write-Output ("SHA256 = " + (Get-FileHash -Algorithm SHA256 -LiteralPath $path).Hash)

**CMD-BUILD-TEST-PROJECT** (project build after a source edit; `TASKID` is the lower-case task id):

    $vswhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
    $msbuild = & $vswhere -latest -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
    $before = (Get-Item -LiteralPath "QuickFiler.Test\bin\Debug\QuickFiler.Test.dll").LastWriteTimeUtc
    $global:LASTEXITCODE = 0
    & $msbuild "QuickFiler.Test\QuickFiler.Test.csproj" /t:Build /m /p:Configuration=Debug /p:Platform=AnyCPU "/flp:LogFile=coverage\TASKID.msbuild.log;Verbosity=normal"
    Write-Output ("MSBUILD_EXIT_CODE: " + $LASTEXITCODE)
    $log = Get-Content -LiteralPath "coverage\TASKID.msbuild.log" -Raw -Encoding UTF8
    Write-Output ("CSC_OUT_LINES: " + [regex]::Matches($log, [regex]::Escape("/out:obj\Debug\QuickFiler.Test.dll")).Count)
    Write-Output ("ZERO_ERRORS_LINES: " + [regex]::Matches($log, [regex]::Escape(" 0 Error(s)")).Count)
    $after = (Get-Item -LiteralPath "QuickFiler.Test\bin\Debug\QuickFiler.Test.dll").LastWriteTimeUtc
    Write-Output ("DLL_ADVANCED: " + ($after -gt $before))

**CMD-VSTEST-SCOPED** (scoped run; `FILTER` and `TASKID` substituted; the results directory is private to the task):

    $vswhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
    $vstest = & $vswhere -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe" | Select-Object -First 1
    $global:LASTEXITCODE = 0
    & $vstest "QuickFiler.Test\bin\Debug\QuickFiler.Test.dll" "/Settings:scripts\vscode\TaskMaster.cli.runsettings" /InIsolation "/TestCaseFilter:FILTER" "/ResultsDirectory:TestResults\900\TASKID" "/Logger:trx;LogFileName=TASKID.trx"
    Write-Output ("VSTEST_EXIT_CODE: " + $LASTEXITCODE)
    [xml]$trx = Get-Content -LiteralPath "TestResults\900\TASKID\TASKID.trx" -Raw -Encoding UTF8
    $ns = New-Object System.Xml.XmlNamespaceManager($trx.NameTable)
    $ns.AddNamespace("t", "http://microsoft.com/schemas/VisualStudio/TeamTest/2010")
    $counters = $trx.SelectSingleNode("//t:ResultSummary/t:Counters", $ns)
    Write-Output ("COUNTERS total=" + $counters.GetAttribute("total") + " executed=" + $counters.GetAttribute("executed") + " passed=" + $counters.GetAttribute("passed") + " failed=" + $counters.GetAttribute("failed"))
    foreach ($r in @($trx.SelectNodes("//t:UnitTestResult", $ns))) { Write-Output ("RESULT " + $r.GetAttribute("testName") + " = " + $r.GetAttribute("outcome")) }
    foreach ($r in @($trx.SelectNodes("//t:UnitTestResult", $ns))) { if ($r.GetAttribute("outcome") -eq "Failed") { Write-Output ("MESSAGE " + $r.GetAttribute("testName") + " :: " + $r.SelectSingleNode("t:Output/t:ErrorInfo/t:Message", $ns).InnerText) } }

No payload line above embeds a double quote inside a double-quoted string; the failed-result filter is applied in PowerShell rather than in XPath for that reason.

Filters used by this plan: `FILTER-CLASS` is `FullyQualifiedName~ItemViewerBreadcrumbThreadAffinityTests`; `FILTER-PAIR` is `FullyQualifiedName~InitializeBreadcrumbPipeline_WorkerThread_ThrowsBoundaryDiagnostic|FullyQualifiedName~ConfigureBreadcrumbDropDown_WorkerThread_ThrowsBoundaryDiagnostic`. A run whose `total` is not exactly 7 (class) or 2 (pair) is a failure of that task, not a pass: `vstest.console.exe` reports a zero-match filter without a non-zero exit.

**CMD-COVERAGE** (repository-wide test and coverage run; `STAGE` is `baseline` or `final`):

    $repo = (Resolve-Path ".").Path
    . (Join-Path $repo "scripts\vscode\Invoke-MSTestWithCoverage.ps1")
    $canonical = Get-Content -LiteralPath (Join-Path $repo "coverage.config") -Raw -Encoding UTF8
    $derived = ConvertTo-DerivedCoverageSettingsXml -CanonicalSettingsXml $canonical
    $effective = Join-Path $repo "coverage\effective-coverage-900.config"
    Set-Content -LiteralPath $effective -Value $derived -Encoding UTF8 -NoNewline
    $vswhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
    $vstest = & $vswhere -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe" | Select-Object -First 1
    $rootLen = $repo.TrimEnd([char]92).Length
    $asm = @(Get-ChildItem -Path $repo -Recurse -Filter "*.Test.dll" | Where-Object { $_.FullName -like "*\bin\Debug\*" -and $_.FullName -notlike "*\obj\*" -and $_.FullName -notlike "*\ref\*" -and $_.FullName.Substring($rootLen) -notlike "\.claude\*" } | Select-Object -ExpandProperty FullName)
    $filter = "TestCategory!=LiveOutlook&FullyQualifiedName!~HelperClasses.ShellUtilities_Tests&FullyQualifiedName!~HelperClasses.ShellUtilitiesStatic_Tests&FullyQualifiedName!~HelperClasses.SysImageListHelperTests&FullyQualifiedName!~EmailIntelligence.OSBrowser_Tests"
    $output = Join-Path $repo "coverage\STAGE-900.cobertura.xml"
    $settings = Join-Path $repo "scripts\vscode\TaskMaster.cli.runsettings"
    $results = Join-Path $repo "TestResults\900\STAGE"
    $global:LASTEXITCODE = 0
    & dotnet-coverage collect --output $output --output-format cobertura --settings $effective -- $vstest @asm "/Settings:$settings" /InIsolation "/TestCaseFilter:$filter" "/ResultsDirectory:$results" "/Logger:trx;LogFileName=STAGE-900.trx" "/Blame:CollectHangDump;TestTimeout=4min;HangDumpType=None"
    Write-Output ("COLLECT_EXIT_CODE: " + $LASTEXITCODE)
    Write-Output ("ASSEMBLY_COUNT: " + $asm.Count)
    $asm | ForEach-Object { Write-Output ("ASSEMBLY: " + $_.Substring($rootLen)) }

The `ASSEMBLY:` lines print the path relative to the worktree root (the leading backslash is the first character), so no absolute path enters an artifact. The discovery filter uses `-like` wildcards and `[char]92` rather than regex strings ending in a backslash, because a backslash immediately before a closing double quote is re-interpreted by the native command-line parser when the payload travels through `pwsh -NoProfile -Command`; every payload in this plan is free of that sequence. Dot-sourcing the runner sets `Set-StrictMode -Version Latest` and `$ErrorActionPreference = "Stop"` (its lines 271-272); every variable above is assigned before use. The TRX for the run is `TestResults\900\STAGE\STAGE-900.trx`; per-test outcomes are read from it with the same namespace-qualified queries as `CMD-VSTEST-SCOPED`.

**CMD-POSTPROCESS** (post-process a Cobertura document in place and print the headline; `STAGE` substituted):

    $repo = (Resolve-Path ".").Path
    . (Join-Path $repo "scripts\vscode\Invoke-MSTestWithCoverage.Helpers.ps1")
    $path = Join-Path $repo "coverage\STAGE-900.cobertura.xml"
    $raw = Get-Content -LiteralPath $path -Raw -Encoding UTF8
    $processed = ConvertTo-KoverageCoberturaXml -XmlContent $raw -RepoRoot $repo
    Set-Content -LiteralPath $path -Value $processed -Encoding UTF8 -NoNewline
    [xml]$x = $processed
    $c = $x.SelectSingleNode("/coverage")
    Write-Output ("line-rate=" + $c.GetAttribute("line-rate") + " branch-rate=" + $c.GetAttribute("branch-rate") + " lines-covered=" + $c.GetAttribute("lines-covered") + " lines-valid=" + $c.GetAttribute("lines-valid") + " branches-covered=" + $c.GetAttribute("branches-covered") + " branches-valid=" + $c.GetAttribute("branches-valid"))
    $classes = @($x.SelectNodes("//class"))
    Write-Output ("ItemViewerBreadcrumbClassCount=" + @($classes | Where-Object { $_.GetAttribute("filename") -like "*ItemViewer.Breadcrumb.cs" }).Count)
    Write-Output ("ThreadAffinityTestsClassCount=" + @($classes | Where-Object { $_.GetAttribute("filename") -like "*ItemViewerBreadcrumbThreadAffinityTests.cs" }).Count)
    try { Assert-CoberturaLineCoverageThreshold -CoberturaXml $processed; Write-Output "FLOOR: MET" } catch { Write-Output ("FLOOR: NOT MET " + $_.Exception.Message) }

The threshold call is the last statement and is wrapped in `try`/`catch` so a throw is recorded rather than fatal.

## Token Census Expectations

| Token | Pre-edit (fact 3, re-recorded by P1-T1) | After P2-T1 and P2-T2 | Under M1 (P3-T1) | Under M2 (P3-T3) |
| --- | --- | --- | --- | --- |
| `Task.Run(` | 3 | 1 | 1 | 1 |
| `.GetAwaiter()` | 3 | 1 | 1 | 1 |
| `Throw<InvalidOperationException>(` | 2 | 0 | 0 | 0 |
| `error.Message.Contains(` | 2 | 0 | 0 | 0 |
| `Contains(` | 2 | 0 | 0 | 0 |
| `NotBeOfType<ObjectDisposedException>()` | 2 | 2 | 2 | 2 |
| `ClearViewerDispatcher(scope.Viewer);` | 1 | 1 | 3 | 1 |
| `[TestMethod]` | 7 | 7 | 7 | 7 |
| `vacuously` | 1 | 3 | 3 | 3 |
| `RunOnDedicatedWorkerThread(` | 0 | 3 | 3 | 3 |
| `Exception captured = RunOnDedicatedWorkerThread(` | 0 | 2 | 2 | 2 |
| `new Thread(` | 0 | 1 | 1 | 1 |
| `IsBackground = true` | 0 | 1 | 1 | 1 |
| `thread.Join();` | 0 | 1 | 1 | 1 |
| `Join(` equals `Join()` | 0 = 0 | equal | equal | equal |
| `UiDispatcher.CheckAccess()` | 0 | 2 | 2 | 2 |
| `isOwnerThread` | 0 | 4 | 4 | 4 |
| `BeOfType<InvalidOperationException>()` | 0 | 2 | 2 | 2 |
| `captured.Message.Should().Contain(` | 0 | 2 | 2 | 2 |
| `#900` | 0 | 3 | 3 | 3 |
| `action();` | 0 | 1 | 1 | 2 |
| `Thread.Sleep`, `Task.Delay`, `[Timeout`, `DoNotParallelize` | 0 each | 0 each | 0 each | 0 each |
| `LINES` | 419 | at least 421 and at most 500 (expected 490 pre-format: 419 minus the 67 replaced lines 199 through 265, plus 106 test-block lines, plus 32 helper-block lines including its leading blank) | greater than post-fix | greater than post-fix |

The remarks the executor writes must not contain the literals `Task.Run(`, `.GetAwaiter()`, `RunOnDedicatedWorkerThread(` or `UiDispatcher.CheckAccess()`; they refer to those members as `Task.Run`, `GetResult()`, `RunOnDedicatedWorkerThread` and `Dispatcher.CheckAccess()` respectively, so the code counts above are exact.

## Target Source

P2-T1 replaces `FILE` lines 199 through 265 with the following text (pre-format; P2-T2 then formats the file with the pinned CSharpier, which may re-wrap lines but changes no token counted above), and inserts the helper after line 332.

Replacement for lines 199 through 229 (the first test, documentation through closing brace):

    /// <summary>
    /// A genuine cross-thread call must still fail fast with a diagnostic naming the operation,
    /// and must not be an <see cref="ObjectDisposedException"/>.
    /// </summary>
    /// <remarks>
    /// Issue #900: the worker is a dedicated thread created by <c>RunOnDedicatedWorkerThread</c>,
    /// never a <c>Task.Run</c> work item. A work item queued from a thread-pool thread lands on
    /// that thread's local queue, and a blocking wait on it can run the delegate inline on the
    /// constructing thread, in which case <c>Dispatcher.CheckAccess()</c> is true and the guard
    /// never throws. A thread object this test constructs is never the object that constructed
    /// the viewer, so the precondition asserted inside the delegate holds by construction under
    /// any scheduler, including the <c>Workers=0</c> class-level parallel run. The helper's
    /// untimed <c>Thread.Join()</c> is a completion wait for one synchronous call on a dedicated
    /// non-pool thread; unlike the previous blocking <c>GetResult()</c> shape it never parks a
    /// thread-pool slot waiting on another thread-pool slot, so it adds no starvation risk under
    /// parallel execution. <c>BeOfType</c> is an exact-type check, so the derived
    /// <see cref="ObjectDisposedException"/> is excluded by it as well as by the explicit
    /// <c>NotBeOfType</c> that documents the intent.
    /// </remarks>
    [TestMethod]
    public void InitializeBreadcrumbPipeline_WorkerThread_ThrowsBoundaryDiagnostic()
    {
        // Arrange
        using (var scope = new ViewerScope())
        {
            BreadcrumbPopupUiOperations operations = InertOperations();
            var provider = new Mock<IFolderHierarchyProvider>(MockBehavior.Strict);

            // Act
            Exception captured = RunOnDedicatedWorkerThread(() =>
            {
                bool isOwnerThread = scope.Viewer.UiDispatcher.CheckAccess();
                isOwnerThread
                    .Should()
                    .BeFalse(
                        "the dedicated worker thread must not be the thread that constructed "
                            + "the viewer, or the boundary assertion would pass vacuously"
                    );
                scope.Viewer.InitializeBreadcrumbPipeline(provider.Object, operations);
            });

            // Assert
            captured
                .Should()
                .NotBeNull(
                    "a worker thread is not the thread that constructed the viewer, so the "
                        + "guard must throw rather than admit the call"
                );
            captured.Should().BeOfType<InvalidOperationException>();
            captured.Message.Should().Contain("InitializeBreadcrumbPipeline");
            captured.Should().NotBeOfType<ObjectDisposedException>();
        }
    }

Replacement for lines 231 through 265 (the second test):

    /// <summary>
    /// The same cross-thread contract on the three-argument <c>ConfigureBreadcrumbDropDown</c>
    /// overload, whose guard is its first statement and therefore throws before any argument
    /// check or control access.
    /// </summary>
    /// <remarks>
    /// Issue #900: the worker is a dedicated thread created by <c>RunOnDedicatedWorkerThread</c>
    /// rather than a <c>Task.Run</c> work item, for the reason given on
    /// <c>InitializeBreadcrumbPipeline_WorkerThread_ThrowsBoundaryDiagnostic</c>: a pool work
    /// item can be inlined onto the constructing thread, and a thread this test creates cannot.
    /// The precondition inside the delegate proves the call is off the owning thread before the
    /// guarded member runs. The helper's untimed <c>Thread.Join()</c> waits for one synchronous
    /// call on a non-pool thread and parks no thread-pool slot, so it is safe under the
    /// <c>Workers=0</c> class-level parallel run.
    /// </remarks>
    [TestMethod]
    public void ConfigureBreadcrumbDropDown_WorkerThread_ThrowsBoundaryDiagnostic()
    {
        // Arrange
        using (var scope = new ViewerScope())
        {
            var host = new InertDropDownHost();

            // Act
            Exception captured = RunOnDedicatedWorkerThread(() =>
            {
                bool isOwnerThread = scope.Viewer.UiDispatcher.CheckAccess();
                isOwnerThread
                    .Should()
                    .BeFalse(
                        "the dedicated worker thread must not be the thread that constructed "
                            + "the viewer, or the boundary assertion would pass vacuously"
                    );
                scope.Viewer.ConfigureBreadcrumbDropDown(
                    host,
                    () => new Rectangle(0, 0, 10, 10),
                    () => new Rectangle(0, 0, 1920, 1040)
                );
            });

            // Assert
            captured
                .Should()
                .NotBeNull(
                    "a worker thread is not the thread that constructed the viewer, so the "
                        + "guard must throw rather than admit the call"
                );
            captured.Should().BeOfType<InvalidOperationException>();
            captured.Message.Should().Contain("ConfigureBreadcrumbDropDown");
            captured.Should().NotBeOfType<ObjectDisposedException>();
        }
    }

Helper inserted after pre-edit line 332 (the closing brace of `ClearViewerDispatcher`), preceded by one blank line:

    /// <summary>
    /// Runs <paramref name="action"/> on a dedicated background thread, joins it, and returns
    /// the exception it threw, or <see langword="null"/> when it completed normally.
    /// </summary>
    /// <remarks>
    /// Issue #900: a <c>Task.Run</c> work item is not guaranteed to run on a thread other than
    /// the caller's, so it cannot stand in for a different thread in a thread-identity test. A
    /// thread this method constructs is distinct from every live thread by construction. The
    /// untimed <c>Join()</c> is a completion wait on one bounded synchronous call, not a sleep or
    /// a wall-clock wait, and the waiting thread and the waited-for thread are never both
    /// thread-pool workers, so the wait cannot starve the pool under parallel execution.
    /// </remarks>
    private static Exception RunOnDedicatedWorkerThread(Action action)
    {
        Exception captured = null;
        var thread = new Thread(() =>
        {
            try
            {
                action();
            }
            catch (Exception error)
            {
                captured = error;
            }
        });
        thread.IsBackground = true;
        thread.Start();
        thread.Join();
        return captured;
    }

Each block above is shown with its class-member lines (`///`, `[TestMethod]`, `public void`, `private static`) at column 0; the executor writes them at the file's member indentation of eight spaces, with the inner lines indented relative to that, and P2-T2's format pass normalizes any remaining whitespace difference. Whitespace is not a counted token, so the census is unaffected either way.

## Planner Self-Review Record (Round 2)

This section persists the planner's round 2 review so a reader can locate it without the conversation transcript. Round 1 preflight returned `PREFLIGHT: REVISIONS REQUIRED` with three blocking defects (B-1 through B-3) and seven non-blocking defects (N-1 through N-7); all ten deltas were applied in place to this file on 2026-09-17. The five constraint blocks (`TEST PARALLELISM`, `OBSERVED-FAILING CRITERIA`, `DIFF BASES`, `COMMIT AND SCOPE`, `BASH DISCIPLINE`) were received again with the round 2 directive; this plan issues no delegation, and the orchestrator relays them to `atomic-executor`.

Deltas applied and where: B-1 (`LINES` 420 to 419) at fact 1, the Token Census Expectations `LINES` row, and P1-T1; no interior line citation moved. B-2 (both rungs refused under worktree isolation) at the header bullet `Execution session requirement`, the `Two-rung channel, determined once by P0-T4` convention, D-11, and P0-T4 part 1 with its `CHANNEL UNAVAILABLE` stop branch; no third rung was invented. B-3 (pre-implementation gate) at the header bullet `Pre-implementation gate requirement`, fact 20, the `Commits` convention, P0-T3 (checkpoint readiness folded in, read-only, `PRE-IMPLEMENTATION GATE NOT SEEDED` stop branch), P0-T11 (exemption-eligible `-m` form with a pathspec operand, no `-F`), and the `PRE-IMPLEMENTATION GATE BLOCKED` stop sentences on P2-T1, P2-T5 and P5-T15. N-1 at P0-T10 and P5-T5 (`ASSEMBLY:` shape clause). N-2 at P5-T9 (`origin/main..HEAD` versus `origin/main...HEAD`, `TWO-DOT-VS-THREE-DOT-AGREE:`). N-3 at P0-T10 (class counts are recorded observations that select a P5-T6 branch). N-4 at fact 11, fact 21, D-12, P5-T1, P5-T6 and P5-T8 (the copy moves to P5-T8 after `LOOP: CLEAN PASS`). N-5 at the `File hashes` convention and P0-T4 part 2 (`Get-FileHash -Algorithm SHA256 -LiteralPath`). N-6 at P2-T2 and P5-T7 (`FILE SIZE LIMIT EXCEEDED`). N-7 is this section. One placement deviation from the N-5 delta text: the two hash spans moved from P0-T3 to P0-T4 part 2 rather than being named in P0-T3, because `Get-FileHash` is a `pwsh` payload and P0-T3 must stay free of `pwsh` so that its B-3 stop branch, and P0-T4's B-2 stop branch, are reachable in a session with no channel; D-10, P1-T1, P4-T2 and P5-T9 now cite P0-T4 for `RUNSETTINGS-HASH:` and `PRE-EDIT-HASH:`. The task count is unchanged at 46 because the readiness check was folded into P0-T3.

SELF-REVIEW: RE-DERIVED THIS PASS
- `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` | line count 419: the file's last three lines are 417 `        }`, 418 `    }`, 419 `}` followed by the terminating newline; `ViewerScope` still spans 396 through 417 (sibling region of the line-count claim, unchanged).
- Token Census Expectations | post-fix `LINES` expectation 490: the Target Source blocks are 53 lines (first test), 1 blank, 52 lines (second test) and 31 helper lines plus 1 leading blank; 419 minus 67 plus 106 plus 32 equals 490, within the existing `at least 421 and at most 500` gate.
- `.csharpierignore` | lines 1 through 15: the only patterns are `**/evidence/**`, `*.cobertura.xml`, `*.coverage`, `*.coveragexml`, `*.trx`, `*.csproj`, `*.props`, `*.targets`; `artifacts/csharp/coverage.xml` matches none.
- `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` | line 27 (`$script:CheckpointPath = 'artifacts/orchestration/orchestrator-state.json'`), lines 223 through 251 (`Test-OrchestrationReady`: `issue-num`, `feature-folder`, `route_id` else `path_selected`, `lifecycle_ready`, `feature-folder` begins `docs/features/active/`), line 442 (deny reason).
- `.claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1` | lines 22 through 28 (five exempt trees including `docs/features/active/`), line 33 (`$`, backtick, `>`, `<` unresolvable), lines 230 through 235 (`git` then `add` or `commit`), lines 253 through 273 (`-m`, `--message`, `--message=`, `-m<value>` modelled; every other dash-leading token including `-F` returns false at 273), lines 283 through 285 (at least one pathspec operand), lines 327 through 329 (unresolvable-character deny).
- `docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/spec.md` | `- [ ] AC1. ` at line 273, AC2 278, AC3 281, AC4 285, AC5 288, AC6 292, AC7 295, AC8 297, all unchecked; the AC text matches the AC-MAPPING below.
- This plan | sibling lines of every edited site re-read after editing: the `Refs`, `Inherited paths`, `Git gates are pathspec-scoped` and `Stall handling` conventions, D-5 through D-9, P0-T5 through P0-T9, P1-T2, P1-T3, P2-T3, P2-T4, P3-T1 through P3-T4, P4-T1, P4-T3 through P4-T8, P5-T2 through P5-T4, P5-T10 through P5-T14 are unchanged and none cites a value this round moved (the only moved anchors are `RUNSETTINGS-HASH:` and `PRE-EDIT-HASH:`, whose four consumers were updated).

PLANNER-INTERNAL-REVIEW: PASS
CITATION-TO-TREE: PASS
AC-TRACEABILITY: PASS
SCOPE-BOUNDARY: PASS
CITATION: QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs | lines 1-419; `[TestMethod]` at 39, 89, 129, 168, 204, 237, 280; test 1 documentation 199 to close 229; test 2 documentation 231 to close 265; `ClearViewerDispatcher` 322-332; `ViewerScope` 396-417
CITATION: QuickFiler/Viewers/ItemViewer.Breadcrumb.cs | `ThrowIfOffUiBoundary` 432-447; guard at 51 and 233; `CaptureCurrent()` argument at 80; `EnsureBreadcrumbLifecycle` 364-378
CITATION: QuickFiler/Viewers/ItemViewer.cs | `[ExcludeFromCodeCoverage]` at 20; `_uiDispatcher` capture at 27; `UiDispatcher` property 64-68
CITATION: QuickFiler/Viewers/BreadcrumbUiDispatcher.cs | `CaptureCurrent()` 44-56
CITATION: QuickFiler/Viewers/BreadcrumbPopupUiOperations.cs | lines 80-81
CITATION: QuickFiler.Test/QuickFiler.Test.csproj | Compile Include at line 96; Platform default at 12
CITATION: scripts/vscode/TaskMaster.cli.runsettings | nine lines, Workers=0, Scope=ClassLevel
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.ps1 | entry guard 437; `ConvertTo-DerivedCoverageSettingsXml` 97-134; `Get-DotnetCoverageArgumentList` 41-95; throw at 261-263
CITATION: scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1 | `ConvertTo-KoverageCoberturaXml` at 407; dot-source at 4
CITATION: scripts/vscode/Install-RepoDotNetSdk.ps1 | marker lines 36, 56, 102-104
CITATION: scripts/vscode/Invoke-Restore.ps1 | msbuild resolution shape at 31
CITATION: UtilitiesCS.Test/Threading/UiThreadInitContract_Tests.cs | `ApartmentThreadRunner` 79-104
CITATION: .csharpierignore | lines 1-15
CITATION: .gitignore | lines 39, 57, 144
CITATION: .claude/hooks/enforce-orchestration-preimplementation-gate.ps1 | line 27; lines 223-251; line 442
CITATION: .claude/hooks/enforce-orchestration-preimplementation-gate-helpers.ps1 | lines 22-28, 33, 230-235, 253-273, 283-285, 327-329
CITATION: .claude/hooks/validate-planner-output.ps1 | phase-heading regex at 238
CITATION: docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/spec.md | `## Acceptance Criteria`; AC lines 273, 278, 281, 285, 288, 292, 295, 297
CITATION: docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/issue.md | line 4 `- Work Mode: full-bug`
CITATION: docs/features/archive/2026-07-07-onedrive-writer-timeout-test-determinism-253/evidence/regression-testing/fail-before-exception.2026-07-07T14-05.md | dossier precedent
AC-INVENTORY: AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8
AC-MAPPING: AC1 | IMPLEMENTATION: P2-T1 (dedicated-thread helper replaces both Task.Run delegates) | TESTS: P4-T2 pair run | EVIDENCE: p2-t2-csharpier-scoped census (Task.Run( 1, .GetAwaiter() 1, new Thread( 1) and p4-t2-pass-after-scoped; check-off P4-T4
AC-MAPPING: AC2 | IMPLEMENTATION: P2-T1 (isOwnerThread precondition inside each delegate) | TESTS: P3-T3 mutation M2 | EVIDENCE: p3-t3-mutation-inline-precondition (both fail with vacuously) and p2-t2 census (UiDispatcher.CheckAccess() 2); check-off P4-T5
AC-MAPPING: AC3 | IMPLEMENTATION: P2-T1 (BeOfType, Message Contain, NotBeOfType assertions) | TESTS: P3-T1 mutation M1 and P4-T2 | EVIDENCE: p3-t1-mutation-guard-disabled and p4-t2-pass-after-scoped; check-off P4-T6
AC-MAPPING: AC4 | IMPLEMENTATION: P1-T2 dossier | TESTS: P0-T9 original-class baseline run | EVIDENCE: fail-before-exception dossier under evidence/regression-testing; check-off P1-T3
AC-MAPPING: AC5 | IMPLEMENTATION: P3-T1 insertion and P3-T2 revert (plus P3-T3 and P3-T4) | TESTS: P3-T1 and P4-T2 | EVIDENCE: p3-t1-mutation-guard-disabled, p3-t2-mutation-guard-disabled-revert, p3-t4-mutation-inline-precondition-revert, p4-t2-pass-after-scoped; check-off P4-T7
AC-MAPPING: AC6 | IMPLEMENTATION: P2-T1 | TESTS: P4-T2 under the CLI runsettings with RUNSETTINGS-HASH-NOW unchanged | EVIDENCE: p4-t2-pass-after-scoped and p4-t3-pass-after-class; check-off P4-T8
AC-MAPPING: AC7 | IMPLEMENTATION: P5-T9 scope boundary | TESTS: P4-T3 full class run | EVIDENCE: p5-t9-scope-boundary (CHANGED-PATHS exactly the test file) and p5-t15-closure confirming lists; check-off P5-T10
AC-MAPPING: AC8 | IMPLEMENTATION: P5-T1 through P5-T7 loop | TESTS: P5-T5 repository-wide coverage run | EVIDENCE: p5-t8-toolchain-loop-closure (LOOP: CLEAN PASS) and p5-t6-coverage-delta (numeric BASELINE-COVERAGE, FINAL-COVERAGE, COMPARABILITY, CHANGED-CODE COVERAGE); check-off P5-T11
UNRESOLVED-GAPS: NONE
PREFLIGHT: VALIDATION REQUESTED (DIRECTIVE: PREFLIGHT VALIDATION ONLY, round 2 confirming pass by atomic-executor)

---

### Phase 0 — Policy Reads and Baseline Capture

- [x] [P0-T1] Read, in this exact order, the policy and instruction files `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/csharp.md`, `.claude/rules/tonality.md`, `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/skills/acceptance-criteria-tracking/SKILL.md`, `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`, then `FEATURE/spec.md`, `FEATURE/research/2026-09-16T23-50-breadcrumb-thread-affinity-tests-taskrun-distinct-thread-research.md`, and `FEATURE/issue.md`, and write `FEATURE/evidence/baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:` (the ordered list above), and one line per file giving its repository-relative path and line count. The filename carries no timestamp suffix because `.claude/skills/atomic-plan-contract/SKILL.md` names it exactly. Acceptance: the artifact exists at that exact path and lists all twelve files with a line count each.

- [x] [P0-T2] Verify the full-bug preconditions and the planner's spec amendments read-only, recording them in `FEATURE/evidence/baseline/p0-t2-mode-preconditions.<TS>.md`. Acceptance, all six required: `FEATURE/issue.md` contains the exact line `- Work Mode: full-bug`; `FEATURE/spec.md` contains a heading line whose text is exactly `## Acceptance Criteria`; the box-state-independent inventory regex `^- \[[ x]\] AC[1-8]\. ` matches exactly 8 lines of `FEATURE/spec.md` and every one of them begins `- [ ] ` (none is checked before execution starts); `FEATURE/user-story.md` does not exist; the literal `amended 2026-09-17 during planning` occurs exactly 5 times in `FEATURE/spec.md` (four in-place markers plus the header sentence that names the marker) and the literal `ItemViewer.Breadcrumb.cs:80` occurs at least once in it; and the literal `immediately after constructing each` occurs exactly once in `FEATURE/spec.md`, on a line inside the parenthetical that records the superseded wording. Any failure is `MODE PRECONDITION FAILED`: stop and report; the executor does not edit `FEATURE/spec.md` to repair it.

- [x] [P0-T3] Record the working context, the diff anchors, and the pre-implementation gate readiness in `FEATURE/evidence/baseline/p0-t3-worktree-context.<TS>.md`. This task uses only `git` invocations and the Read tool; it runs no `pwsh` payload, so it completes in a session that has no channel. Run, in this order: `git rev-parse --abbrev-ref HEAD`; `git rev-parse HEAD` (recorded as `BASE-SHA:`); `git fetch origin main` (recorded as `FETCH-EXIT:`; a non-zero value is recorded and the task continues with the `origin/main` ref already present); `git merge-base HEAD origin/main` (recorded as `MERGE-BASE:`); `git diff --name-only MERGE-BASE...HEAD` and `git status --porcelain --untracked-files=all`, whose union of paths is recorded under `INHERITED-CLAUSE-A:` (the name-listing diff cannot see untracked paths, which is why the porcelain span is its companion). Record `TOPLEVEL CONTAINS FEATURE:` as `YES` or `NO` by testing that `FEATURE/spec.md` exists beneath the `git rev-parse --show-toplevel` value, and `TOPLEVEL LEAF:` as only that value's final path segment; do not record the value itself. Then read `artifacts/orchestration/orchestrator-state.json` with the Read tool (never `git add`, never edit) and record `CHECKPOINT-EXISTS:` (`YES` or `NO`), and, when it exists, `CHECKPOINT-ISSUE-NUM:` (the `issue-num` value), `CHECKPOINT-FEATURE-FOLDER:` (the `feature-folder` value), `CHECKPOINT-ROUTE:` (the `route_id` value, or the `path_selected` value when `route_id` is absent, or `ABSENT`), and `CHECKPOINT-LIFECYCLE-READY:` (the `lifecycle_ready` value or `ABSENT`); these are the four properties `Test-OrchestrationReady` in `.claude/hooks/enforce-orchestration-preimplementation-gate.ps1` requires (fact 20). Record `PRE-IMPLEMENTATION GATE READY:` as `YES` only when `CHECKPOINT-EXISTS: YES`, `CHECKPOINT-ISSUE-NUM:` is `900`, `CHECKPOINT-FEATURE-FOLDER:` begins `docs/features/active/`, `CHECKPOINT-ROUTE:` is not `ABSENT`, and `CHECKPOINT-LIFECYCLE-READY:` is `true`; otherwise `NO`. The executor never creates, edits, or works around that checkpoint or the hook itself; a `NO` is the orchestrator's launch condition to repair. Acceptance, all seven required: the artifact exists; the abbreviated branch name is recorded verbatim and equals `bug/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` (otherwise record it and report `BRANCH MISMATCH` before Phase 1; do not create or switch branches); `BASE-SHA:` and `MERGE-BASE:` are each a 40-character hexadecimal value; `INHERITED-CLAUSE-A:` is present (it may list paths; it may not list `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, whose presence would mean the Write Set file was already changed on this branch and is reported as `WRITE SET ALREADY DIRTY` before Phase 1); `TOPLEVEL CONTAINS FEATURE:` is `YES`; `PRE-IMPLEMENTATION GATE READY:` is recorded, and when it is `NO` the executor reports `PRE-IMPLEMENTATION GATE NOT SEEDED` with the five `CHECKPOINT-` values and stops at this task (P0-T4 onward are not attempted); and the artifact contains no absolute filesystem path (the `feature-folder` value is repository-relative by the hook's own predicate; if the checkpoint carries any absolute path, record `<repo-root>` in its place).

- [x] [P0-T4] Determine the command channel, then bootstrap and prove the C# toolchain. Part 1, channel determination, written to `FEATURE/evidence/baseline/p0-t4-channel-probe.<TS>.md`: probe rung 1 with the trivial payload `Write-Output "PROBE-OK"` as `pwsh -NoProfile -Command 'Write-Output "PROBE-OK"'`, recording `RUNG-1:` as `OK` when the output line `PROBE-OK` is observed or `REFUSED` with the refusal text verbatim under `RUNG-1-REFUSAL:`; then, whatever rung 1 returned, write the same single line to `coverage/plan900-helper.ps1` and run `pwsh -NoProfile -File coverage/plan900-helper.ps1`, recording `RUNG-2:` and `RUNG-2-REFUSAL:` the same way (the second probe runs even when rung 1 worked, so the artifact documents both rungs in this session). Record exactly one `CHANNEL:` line: `COMMAND` when `RUNG-1: OK`; `FILE` when `RUNG-1: REFUSED` and `RUNG-2: OK`; `UNAVAILABLE` when both are `REFUSED`. Any absolute path or account name inside a refusal text is replaced by `<repo-root>` or `<user>` before it is written (the substitution is the artifact-hygiene rule of P5-T13, applied at write time). When `CHANNEL: UNAVAILABLE`, the executor reports `CHANNEL UNAVAILABLE` with both refusal texts, stops at this task, and attempts neither part 2 nor any later task; it does not modify hook or permission configuration to obtain a channel, and a refusal of both rungs with the same text is the expected signature of a worktree-isolated session (the guard keys on `pwsh` in the command-name position, not on `-Command` versus `-File`). Part 2, toolchain bootstrap, runs only when `CHANNEL:` is `COMMAND` or `FILE`, through that channel, and is written to `FEATURE/evidence/baseline/p0-t4-toolchain-bootstrap.<TS>.md`: first record `RUNSETTINGS-HASH:` as the `Hash` of `Get-FileHash -Algorithm SHA256 -LiteralPath scripts/vscode/TaskMaster.cli.runsettings` and `PRE-EDIT-HASH:` as the `Hash` of `Get-FileHash -Algorithm SHA256 -LiteralPath QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` (both taken before any task edits a file; these are the anchors P1-T1, P4-T2, P4-T3 and P5-T9 compare against); then run `pwsh -NoProfile -File scripts/vscode/Install-RepoDotNetSdk.ps1` (idempotent: it prints an already-installed message when the marker exists), then test that the directory `.dotnet-sdk/sdk/8.0.205` exists (recorded as `SDK-MARKER:` `YES` or `NO`), then `dotnet --version`, then `dotnet tool restore`, then `dotnet tool list --local` (the row for `csharpier` must read version `1.2.6`), then resolve `MSBuild.exe` and `vstest.console.exe` through `vswhere.exe` as the conventions state (recorded as `MSBUILD-RESOLVED:` and `VSTEST-RESOLVED:`, each `YES` or `NO`, plus only the path segment after `Microsoft Visual Studio\`), then `dotnet-coverage --version`. If `dotnet-coverage` is not found, run `dotnet tool install --global dotnet-coverage` and then, in a separate second invocation, `dotnet-coverage --version` again, recording `DOTNET-COVERAGE-RESOLVED: True` only from that second invocation. Acceptance, all ten required: the channel-probe artifact exists with `RUNG-1:`, `RUNG-2:` and exactly one `CHANNEL:` line whose value is `COMMAND` or `FILE` (a value of `UNAVAILABLE` is the stop condition above and the task is left unchecked); `RUNSETTINGS-HASH:` and `PRE-EDIT-HASH:` are each a 64-character hexadecimal value; `SDK-MARKER: YES`; `dotnet --version` exits 0; `dotnet tool restore` exits 0; the `csharpier` row shows `1.2.6`; `MSBUILD-RESOLVED: YES`; `VSTEST-RESOLVED: YES`; `dotnet-coverage --version` exits 0 and its version string is recorded; neither artifact contains an absolute filesystem path.

- [x] [P0-T5] Restore NuGet packages for the solution by running `pwsh -NoProfile -File scripts/vscode/Invoke-Restore.ps1` and write `FEATURE/evidence/baseline/p0-t5-nuget-restore.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording `PACKAGE-DIR-COUNT:` as `@(Get-ChildItem -LiteralPath packages -Directory).Count`. A fresh agent worktree has no `packages/` directory and no `bin\Debug` output, so every later build and test task depends on this step. Acceptance, all three required: `EXIT_CODE: 0`; `PACKAGE-DIR-COUNT:` is at least 1; the directories `packages/FluentAssertions.8.10.0` and `packages/MSTest.TestAdapter.4.4.0` both exist.

- [x] [P0-T6] Capture the baseline formatting state by running `dotnet tool run csharpier check .` from the worktree root and write `FEATURE/evidence/baseline/p0-t6-csharpier-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` quoting the tool's final summary line verbatim (its shape is `Checked N files in Xms.`; the check command never prints a `Formatted` line) and recording `CHECKED-FILES:` as that N. Acceptance, both required: `EXIT_CODE: 0`; `CHECKED-FILES:` is a positive integer. A non-zero exit means pre-existing drift the repository owns, and the later repository-wide `format .` would fold that repair into this branch: stop and report `FORMAT BASELINE NOT CLEAN` with the tool's file list; do not run `format` to repair it.

- [x] [P0-T7] Capture the baseline analyzer state. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true "/flp:LogFile=coverage\p0-t7.msbuild.log;Verbosity=normal"` (MSBuild resolved through vswhere) and write `FEATURE/evidence/baseline/p0-t7-msbuild-analyzers.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (also recorded as `ANALYZE-BASELINE-EXIT:`), `CHANNEL:`, and an `Output Summary:` giving `WARNINGS:` and `ERRORS:` as the integers from the build summary's `Warning(s)` and `Error(s)` lines, `ZERO_ERRORS_LINES:` as the count of the literal ` 0 Error(s)` (leading space included, because `0 Error(s)` is a substring of `10 Error(s)`) in `coverage\p0-t7.msbuild.log`, and `CSC_OUT_LINES:` as the count of the literal `/out:obj\Debug\QuickFiler.Test.dll` in that log. Acceptance, all three required: the artifact records `ANALYZE-BASELINE-EXIT:` and both integer counts; `CSC_OUT_LINES:` is at least 1, which proves the test project was compiled rather than skipped; the log file exists under `coverage/`, which is git-ignored, and is not copied into the feature folder.

- [x] [P0-T8] Capture the baseline nullable and type-check state. Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true "/flp:LogFile=coverage\p0-t8.msbuild.log;Verbosity=normal"` (no `/p:Nullable=enable`, no `/t:Build`) and write `FEATURE/evidence/baseline/p0-t8-msbuild-nullable.<TS>.md` with the same fields as P0-T7, recording `NULLABLE-BASELINE-EXIT:`, `WARNINGS:`, `ERRORS:`, `ZERO_ERRORS_LINES:`, `CSC_OUT_LINES:`, and `TEST-DLL-EXISTS:` as whether `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` exists after the command. Acceptance, all three required: `NULLABLE-BASELINE-EXIT:` and both integer counts are recorded; `CSC_OUT_LINES:` is at least 1; `TEST-DLL-EXISTS: True`, which is the precondition for P0-T9.

- [x] [P0-T9] Capture the baseline scoped run of the original, unfixed test class with `CMD-VSTEST-SCOPED` using `FILTER-CLASS` and `TASKID` `p0-t9`, and write `FEATURE/evidence/baseline/p0-t9-mstest-thread-affinity-class.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (the printed `VSTEST_EXIT_CODE:`), `CHANNEL:`, and an `Output Summary:` recording the `COUNTERS` line, every `RESULT` line, every `MESSAGE` line, and `ORIGINAL-FLAKE-OBSERVED:` as `YES` when either `InitializeBreadcrumbPipeline_WorkerThread_ThrowsBoundaryDiagnostic` or `ConfigureBreadcrumbDropDown_WorkerThread_ThrowsBoundaryDiagnostic` is recorded `Failed`, else `NO`. This is the observation the P1-T2 dossier cites; both outcomes are valid records of the pre-existing state. Write `ExpectedExitCode: 1` only when `failed` is greater than 0; write no expectation line when `failed` is 0. Acceptance, all three required: `total` is exactly 7 (a different value means the filter matched another class or the assembly is stale; that is a failure of this task); every `RESULT` line names one of the seven `[TestMethod]`s in fact 1; `ORIGINAL-FLAKE-OBSERVED:` is present with `YES` or `NO`.

- [x] [P0-T10] Capture the baseline repository-wide test and coverage run with `CMD-COVERAGE` using `STAGE` `baseline`, then `CMD-POSTPROCESS` with `STAGE` `baseline`, and write `FEATURE/evidence/baseline/p0-t10-mstest-coverage.<TS>.md` with `Timestamp:`, `Command:` (both payloads named), `EXIT_CODE:` (the printed `COLLECT_EXIT_CODE:`), `CHANNEL:`, and an `Output Summary:` recording `ASSEMBLY_COUNT:`, every `ASSEMBLY:` line as printed (worktree-relative), the TRX counters `total`, `executed`, `passed`, `failed` read from `TestResults\900\baseline\baseline-900.trx`, `BASELINE-FAILING-SET:` listing the fully-qualified name of every `Failed` result and, on its own line under that heading, `DictionaryExtensions_Tests.TryAddValuesAsync_UpdatesExistingValue KNOWN-FLAKY #780` whether or not it failed, the six root attribute values (`line-rate`, `branch-rate`, `lines-covered`, `lines-valid`, `branches-covered`, `branches-valid`) as `BASELINE-COVERAGE:`, `ItemViewerBreadcrumbClassCount=`, `ThreadAffinityTestsClassCount=`, and `FLOOR:`. Write `ExpectedExitCode:` equal to the observed `COLLECT_EXIT_CODE:` value when it is non-zero and no expectation line when it is 0: this task records the pre-existing state rather than gating on it. Acceptance, all four required: `ASSEMBLY_COUNT:` is at least 1, and every `ASSEMBLY:` line begins with `\` and contains the segment `\bin\Debug\` (the path is printed relative to the worktree root, so the worktree's own location under `.claude\worktrees\` cannot appear in it and is not what this clause checks; the clause fails when the discovery filter admits an `obj\` or `ref\` copy or the root-trimming substring is wrong); `BASELINE-FAILING-SET:` is present with the `KNOWN-FLAKY #780` line under it; all six numeric coverage values are recorded; `FLOOR:` is present, and a `NOT MET` value is reported as `BASELINE COVERAGE FLOOR NOT MET` while execution continues per D-6. Recorded observations, not gated: `ItemViewerBreadcrumbClassCount=` and `ThreadAffinityTestsClassCount=` are transcribed as printed; the pair `0` and `0` is the expected value under fact 6 and fact 13 and selects P5-T6's `NOT MEASURABLE` branch, while any non-zero value is reported to the orchestrator as `INSTRUMENTATION SCOPE CHANGED` and selects P5-T6's `MEASURED` branch; both outcomes complete this task.

- [ ] [P0-T11] Commit the Phase 0 evidence and the feature documents in the exemption-eligible form (fact 20; no message file is used by this task). Run, as two separate invocations, `git add -- docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` and then `git commit -m "docs(900): phase 0 baseline evidence for breadcrumb thread-affinity test fix" -- docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` (a single `-m` string as the only message, one pathspec operand after `--`, one command segment, and no `$`, backtick, `<` or `>` character anywhere on the line; every staged path is under the exempt tree `docs/features/active/`, so the issue #539 exemption admits both segments without the checkpoint being consulted), then `git status --porcelain -- QuickFiler QuickFiler.Test` and `git rev-parse HEAD`. Write `FEATURE/evidence/other/p0-t11-phase0-commit.<TS>.md` recording the commit's exit code under `COMMIT-EXIT:`, the new HEAD under `PHASE0-HEAD:`, and the scoped porcelain output verbatim. Acceptance, all three required: `COMMIT-EXIT: 0`; the scoped porcelain output for `QuickFiler` and `QuickFiler.Test` is empty, proving no source edit preceded Phase 1; `PHASE0-HEAD:` differs from `BASE-SHA:`. A PreToolUse refusal of either git span is reported verbatim as `PRE-IMPLEMENTATION GATE BLOCKED` and stops the run (the executor does not alter hooks, checkpoints, or another item's state). The artifact this task writes is itself uncommitted at the end of the task and is committed by P2-T5.

### Phase 1 — Fail-Before Evidence for the Original Tests

- [ ] [P1-T1] Record the pre-edit token census of `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` by running `CMD-CENSUS` on the unfixed file and write `FEATURE/evidence/regression-testing/p1-t1-token-census-before.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` transcribing every `TOKEN` line, the `LINES` line, and the `SHA256` line. These are the positive controls every later count gate is measured against. Acceptance, all four required: every `TOKEN` value equals the pre-edit column of the Token Census Expectations table (in particular `Task.Run(` 3, `.GetAwaiter()` 3, `RunOnDedicatedWorkerThread(` 0, `UiDispatcher.CheckAccess()` 0, `vacuously` 1, `#900` 0, `action();` 0); `LINES = 419`; the `SHA256` value equals `PRE-EDIT-HASH:` from P0-T4; any mismatch is `TREE DIVERGED FROM PLAN` and is reported before any edit.

- [ ] [P1-T2] Write the fail-before exception dossier `FEATURE/evidence/regression-testing/fail-before-exception.<TS>.md` in the shape of the precedent at `docs/features/archive/2026-07-07-onedrive-writer-timeout-test-determinism-253/evidence/regression-testing/fail-before-exception.2026-07-07T14-05.md`, where `<TS>` equals the artifact's own `Timestamp:` field. Required sections: `Timestamp:`; `WhyFailingRunImpossible:` (one to three sentences: the original tests' failure is a microsecond-scale race inside the runtime's wait-inlining path between the local-queue pop performed by `TaskAwaiter.GetResult()` on a pool thread and a remote worker's steal of the same `Task.Run` work item; a test cannot force the inlined branch without mutating process-global thread-pool state, which is prohibited in committed tests and would perturb every class running in parallel); `## Alternative Proof` (the four-step mechanism chain from research section 2 with its sources, the `Dispatcher.CheckAccess()` object-identity fact from research section 1.3, the MSTest 4.4.0 `DefaultFactoryAsync` fact from research Q6, and the P0-T9 observation quoted by its `ORIGINAL-FLAKE-OBSERVED:` value; then a sentence naming P3-T1 as the task that later demonstrates, deterministically, that the replacement assertions fail when the guard is disabled, without stating that task's result as an observation); `## Output Summary`. The dossier records no figure from a task that has not yet run. Acceptance, all four required: the file exists with all four required sections; its filename timestamp equals its `Timestamp:` field; `WhyFailingRunImpossible:` is present and non-empty; the text contains the literal `wait-inlining` at least once and the literal `P3-T1` at least once.

- [ ] [P1-T3] Check off AC4 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC4. ` to begin `- [x] AC4. `, preserving the criterion text exactly. Evidence: the dossier written by P1-T2. Acceptance: exactly one line of `FEATURE/spec.md` begins `- [x] AC4. `, no line begins `- [ ] AC4. `, and no other AC line changed in this task. If the P1-T2 acceptance did not hold, leave the box unchecked and record `AC4: NOT MET` with the failing condition in `FEATURE/evidence/regression-testing/p1-t3-ac4-checkoff.<TS>.md`; this task completes in either case.

### Phase 2 — Fix

- [ ] [P2-T1] Rewrite the two worker-thread tests and add the helper in `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` exactly as the Target Source section specifies: replace pre-edit lines 199 through 229 and 231 through 265 with the two blocks, and insert the helper block after pre-edit line 332 preceded by one blank line. Change no other line: the using block (lines 1-12), the class remark (16-28), the five sibling tests, `InertOperations`, `ClearViewerDispatcher`, `InertDropDownHost`, `DrainableSynchronizationContext` and `ViewerScope` are byte-identical before and after. The local names are pinned (`captured`, `isOwnerThread`, `thread`, `error`) and the remarks use the member spellings the Token Census Expectations section requires. Then run `CMD-CENSUS` and write `FEATURE/evidence/regression-testing/p2-t1-token-census-after-edit.<TS>.md` transcribing every `TOKEN` line, `LINES` and `SHA256`. Acceptance, all four required: every `TOKEN` value equals the "After P2-T1 and P2-T2" column of the Token Census Expectations table, including `Task.Run(` 1, `.GetAwaiter()` 1, `Throw<InvalidOperationException>(` 0, `Contains(` 0, `RunOnDedicatedWorkerThread(` 3, `Exception captured = RunOnDedicatedWorkerThread(` 2, `new Thread(` 1, `IsBackground = true` 1, `thread.Join();` 1, `UiDispatcher.CheckAccess()` 2, `isOwnerThread` 4, `BeOfType<InvalidOperationException>()` 2, `captured.Message.Should().Contain(` 2, `NotBeOfType<ObjectDisposedException>()` 2, `ClearViewerDispatcher(scope.Viewer);` 1, `[TestMethod]` 7, `#900` 3, `vacuously` 3, `action();` 1, and 0 for each of `Thread.Sleep`, `Task.Delay`, `[Timeout`, `DoNotParallelize`; the count of `Join(` equals the count of `Join()`; `LINES` is at least 421 and at most 500; `git diff -U0 MERGE-BASE -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` (recorded verbatim in the artifact) has hunk headers whose old-side ranges each either lie entirely within pre-edit lines 199 through 265 or are a pure insertion (`,0` old count) whose old-side start is 332 or 333, which together prove that no sibling region changed. A PreToolUse refusal of the edit itself (the pre-implementation gate classifies a `.cs` edit as implementation) is reported verbatim as `PRE-IMPLEMENTATION GATE BLOCKED` and stops the run; the executor does not modify hooks, checkpoints, or another item's state to obtain the edit. This is a STOP-AND-REPORT condition under the COMMIT AND SCOPE block, not a workaround.

- [ ] [P2-T2] Format the edited file with the pinned CSharpier and verify it. Capture the SHA-256 of `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, run `dotnet tool run csharpier format QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, capture the SHA-256 again, then run `dotnet tool run csharpier check QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, then `CMD-CENSUS`. Write `FEATURE/evidence/regression-testing/p2-t2-csharpier-scoped.<TS>.md` with `Timestamp:`, `Command:` (all three), `EXIT_CODE:` (of the `check`), `CHANNEL:`, and an `Output Summary:` recording `HASH-BEFORE:`, `HASH-AFTER:`, `REWRITTEN:` as whether the two hashes differ (the console line `Formatted 1 files in Xms.` is a processed-file count and is not this value), the `check` command's final summary line verbatim, every `TOKEN` line, and `LINES`. Acceptance, all three required: the `check` exits 0; every `TOKEN` value still equals the "After P2-T1 and P2-T2" column (formatting re-wraps lines but every counted token is single-line by construction); `LINES` is at most 500. If `LINES` exceeds 500, stop and report `FILE SIZE LIMIT EXCEEDED` with the value; do not shorten the XML remarks to recover, because the remarks carry the pinned `#900` (3) and `vacuously` (3) census counts and an edit to them can move those counts. `pipe-files` is not a gate and is not used.

- [ ] [P2-T3] Build the test project after the edit with `CMD-BUILD-TEST-PROJECT` using `TASKID` `p2-t3` and write `FEATURE/evidence/regression-testing/p2-t3-build-after-fix.<TS>.md` with `Timestamp:`, `Command:` (`msbuild QuickFiler.Test\QuickFiler.Test.csproj /t:Build /m /p:Configuration=Debug /p:Platform=AnyCPU`, resolved through vswhere; the project-file platform value is `AnyCPU` with no space per fact 10), `EXIT_CODE:` (the printed `MSBUILD_EXIT_CODE:`), `CHANNEL:`, and an `Output Summary:` recording `CSC_OUT_LINES:`, `ZERO_ERRORS_LINES:` and `DLL_ADVANCED:`. Acceptance, all three required: `EXIT_CODE: 0`; `CSC_OUT_LINES:` is at least 1 and `DLL_ADVANCED: True`, which together prove the edited file was compiled into `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll`; `ZERO_ERRORS_LINES:` is at least 1.

- [ ] [P2-T4] Run the two rewritten tests before any mutation with `CMD-VSTEST-SCOPED` using `FILTER-PAIR` and `TASKID` `p2-t4`, and write `FEATURE/evidence/regression-testing/p2-t4-pair-run-before-mutation.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording the `COUNTERS` line and every `RESULT` line. This is a confirming run only; the measured AC6 run is P4-T2, which follows the mutation reverts. Acceptance, all three required: `EXIT_CODE: 0`; `total` is exactly 2 and `passed` is exactly 2; both `RESULT` lines read `Passed`. If either test fails, the fix is wrong: repair `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` within the Target Source contract, re-run P2-T1 through P2-T4, and record each iteration; do not proceed to P2-T5 with a failing pair.

- [ ] [P2-T5] Commit the fixed test file and the Phase 1 and Phase 2 evidence. Write `FEATURE/evidence/other/commit-message-fix.txt` (subject `test(quickfiler): run breadcrumb thread-affinity worker tests on a dedicated thread (#900)`, body stating that `Task.Run` cannot stand in for a distinct thread under wait-inlining and naming the helper), then run `git add -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` and `git commit -F docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/evidence/other/commit-message-fix.txt`, then `git diff --exit-code HEAD -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, then `git show --name-only --format= HEAD`, then the SHA-256 of the test file. Write `FEATURE/evidence/regression-testing/p2-t5-fix-commit.<TS>.md` recording `COMMIT-EXIT:`, `FIX-HEAD:` (`git rev-parse HEAD`), `FIX-HASH:` (the SHA-256, the anchor every Phase 3 revert check compares against), the `git show` path list verbatim, and the exit code of the `git diff --exit-code` span under `EXIT_CODE:`. Acceptance, all four required: `COMMIT-EXIT: 0`; `EXIT_CODE: 0` for the anchored diff, proving the committed state equals the working file; the `git show` list contains `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` and no other path outside `docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/`; `FIX-HASH:` equals the `HASH-AFTER:` value of P2-T2. This commit stages a path outside every exempt tree, so the issue #539 exemption does not apply and the pre-implementation gate consults the checkpoint P0-T3 recorded as ready; a PreToolUse refusal of the `git add` span or the `git commit` span is reported verbatim as `PRE-IMPLEMENTATION GATE BLOCKED` and stops the run, with no modification of hooks, checkpoints, or another item's state. This is a STOP-AND-REPORT condition under the COMMIT AND SCOPE block, not a workaround.

### Phase 3 — Non-Vacuity Mutation Testing

- [ ] [P3-T1] [expect-fail] Mutation M1, guard disabled: insert the line `ClearViewerDispatcher(scope.Viewer);` into `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` inside each of the two rewritten delegates, immediately after the `isOwnerThread` `BeFalse` assertion statement and immediately before the guarded call (`scope.Viewer.InitializeBreadcrumbPipeline(...)` in the first test, `scope.Viewer.ConfigureBreadcrumbDropDown(` in the second), so the precondition still observes the non-null owner and the guarded call takes the null-owner escape at `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs:435-438`. Run `CMD-CENSUS`, then `CMD-BUILD-TEST-PROJECT` with `TASKID` `p3-t1`, then `CMD-VSTEST-SCOPED` with `FILTER-PAIR` and `TASKID` `p3-t1`. Write `FEATURE/evidence/regression-testing/p3-t1-mutation-guard-disabled.<TS>.md` with `Timestamp:`, `Command:` (all three), `EXIT_CODE:` (the `VSTEST_EXIT_CODE:`), `ExpectedExitCode: 1`, `CHANNEL:`, and an `Output Summary:` recording `TOKEN ClearViewerDispatcher(scope.Viewer); = 3`, `MSBUILD_EXIT_CODE:`, the `COUNTERS` line, every `RESULT` line, and every `MESSAGE` line verbatim. Predicted failure (D-3, fact 8): both tests `Failed`, each on the message assertion, with a `MESSAGE` line containing the literal `owning UI synchronization context` (the `CaptureCurrent()` exception text) and the literal `to contain` (FluentAssertions' `Contain` failure phrase). Acceptance, all four required: the census shows `ClearViewerDispatcher(scope.Viewer);` exactly 3; `MSBUILD_EXIT_CODE: 0`; `total` 2, `failed` 2; each of the two `MESSAGE` lines contains both `owning UI synchronization context` and `to contain`, which identifies the failing assertion as `captured.Message.Should().Contain(...)` and proves `BeOfType<InvalidOperationException>()` passed before it. A run in which either test passes, or in which a `MESSAGE` line names `NullReferenceException` or `AssertFailedException`, is `MUTATION PREDICTION MISMATCH`: stop and report the message text; do not adjust the test or the plan.

- [ ] [P3-T2] Revert mutation M1 and verify the revert mechanically. Remove the two inserted `ClearViewerDispatcher(scope.Viewer);` lines from `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` by editing, then run `CMD-CENSUS`, `git diff --exit-code HEAD -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` and `git status --porcelain -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`. Write `FEATURE/evidence/regression-testing/p3-t2-mutation-guard-disabled-revert.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (of the `git diff --exit-code` span), `CHANNEL:`, and an `Output Summary:` recording `TOKEN ClearViewerDispatcher(scope.Viewer); = 1`, the `SHA256` line, and the porcelain output verbatim. Acceptance, all four required: the census shows `ClearViewerDispatcher(scope.Viewer);` exactly 1 and `action();` exactly 1; `EXIT_CODE: 0`; the porcelain span prints nothing; `SHA256` equals `FIX-HASH:` from P2-T5. If any condition fails, the single authorized recovery is `git checkout -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` (which restores exactly the committed fixed state, because that is the only path named), after which the four checks are re-run and recorded again; the task does not complete until all four hold.

- [ ] [P3-T3] [expect-fail] Mutation M2, precondition run inline: insert the line `action();` as the first statement of `RunOnDedicatedWorkerThread` in `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, immediately before `Exception captured = null;`, so the delegate runs on the calling thread before the dedicated thread starts and `isOwnerThread` is `true`. Run `CMD-CENSUS`, then `CMD-BUILD-TEST-PROJECT` with `TASKID` `p3-t3`, then `CMD-VSTEST-SCOPED` with `FILTER-PAIR` and `TASKID` `p3-t3`. Write `FEATURE/evidence/regression-testing/p3-t3-mutation-inline-precondition.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `CHANNEL:`, and an `Output Summary:` recording `TOKEN action(); = 2`, `MSBUILD_EXIT_CODE:`, the `COUNTERS` line, every `RESULT` line, and every `MESSAGE` line verbatim. Predicted failure (D-4): both tests `Failed` at the precondition, with a `MESSAGE` line containing the literal `vacuously` (the reason text of the `BeFalse` assertion); the rest of the FluentAssertions message is recorded verbatim and not gated. Acceptance, all four required: the census shows `action();` exactly 2; `MSBUILD_EXIT_CODE: 0`; `total` 2, `failed` 2; each `MESSAGE` line contains `vacuously`, which proves the `isOwnerThread` precondition is exercised and is what fails when the delegate does not run on a distinct thread. A run in which either test passes is `MUTATION PREDICTION MISMATCH`: stop and report.

- [ ] [P3-T4] Revert mutation M2 and verify the revert mechanically, with the same mechanics as P3-T2: remove the inserted `action();` line from `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, run `CMD-CENSUS`, `git diff --exit-code HEAD -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` and `git status --porcelain -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`, and write `FEATURE/evidence/regression-testing/p3-t4-mutation-inline-precondition-revert.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (of the `git diff --exit-code` span), `CHANNEL:`, and an `Output Summary:` recording `TOKEN action(); = 1`, `TOKEN ClearViewerDispatcher(scope.Viewer); = 1`, the `SHA256` line, and the porcelain output verbatim. Acceptance, all four required: `action();` exactly 1 and `ClearViewerDispatcher(scope.Viewer);` exactly 1; `EXIT_CODE: 0`; the porcelain span prints nothing; `SHA256` equals `FIX-HASH:`. The same `git checkout -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` recovery applies, followed by re-running all four checks. This is the mechanical proof AC5 requires that the temporary insertions are gone before the final pass-after run.

### Phase 4 — Pass-After Verification and Acceptance Check-Off

- [ ] [P4-T1] Rebuild the test project from the reverted source with `CMD-BUILD-TEST-PROJECT` using `TASKID` `p4-t1` and write `FEATURE/evidence/regression-testing/p4-t1-build-after-revert.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording `CSC_OUT_LINES:`, `ZERO_ERRORS_LINES:`, `DLL_ADVANCED:`, and the `LastWriteTimeUtc` of `QuickFiler.Test/bin/Debug/QuickFiler.Test.dll` after the build. `vstest.console.exe` never compiles, so without this rebuild P4-T2 would run the M2 assembly. Acceptance, all three required: `EXIT_CODE: 0`; `CSC_OUT_LINES:` at least 1 and `DLL_ADVANCED: True`; the recorded `LastWriteTimeUtc` is later than the `Timestamp:` of the P3-T4 artifact.

- [ ] [P4-T2] Run the two rewritten tests after the reverts (the measured run for AC6) with `CMD-VSTEST-SCOPED` using `FILTER-PAIR` and `TASKID` `p4-t2`, also recording the SHA-256 of `scripts/vscode/TaskMaster.cli.runsettings`, and write `FEATURE/evidence/regression-testing/p4-t2-pass-after-scoped.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording the `COUNTERS` line, both `RESULT` lines, and `RUNSETTINGS-HASH-NOW:`. Acceptance, all four required: `EXIT_CODE: 0`; `total` 2, `passed` 2, `failed` 0; both `RESULT` lines read `Passed`; `RUNSETTINGS-HASH-NOW:` equals `RUNSETTINGS-HASH:` from P0-T4, proving the run used the unchanged `Workers=0`, `Scope=ClassLevel` settings. Disagreement with the confirming run P2-T4 (a test that passed there and fails here) is `BLOCKED`, reported with both artifacts named.

- [ ] [P4-T3] Run the full `ItemViewerBreadcrumbThreadAffinityTests` class (the measured run for AC7's sibling clause; confirming for AC6) with `CMD-VSTEST-SCOPED` using `FILTER-CLASS` and `TASKID` `p4-t3`, and write `FEATURE/evidence/regression-testing/p4-t3-pass-after-class.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording the `COUNTERS` line, all seven `RESULT` lines, and `RUNSETTINGS-HASH-NOW:`. Acceptance, all four required: `EXIT_CODE: 0`; `total` 7, `passed` 7, `failed` 0; the seven `RESULT` names are exactly the seven `[TestMethod]` names of fact 1; `RUNSETTINGS-HASH-NOW:` equals `RUNSETTINGS-HASH:`.

- [ ] [P4-T4] Check off AC1 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC1. ` to begin `- [x] AC1. `. Evidence: P2-T2's census (`Task.Run(` 1 and `.GetAwaiter()` 1, down from 3 each in P1-T1, with the surviving pair at the out-of-scope line 293 test; `new Thread(` 1; `thread.Join();` 1; `Exception captured = RunOnDedicatedWorkerThread(` 2) and P4-T2. Acceptance: exactly one line begins `- [x] AC1. `, none begins `- [ ] AC1. `, and no other AC line changed in this task; otherwise leave it unchecked and record `AC1: NOT MET` with the failing value in `FEATURE/evidence/regression-testing/p4-t4-ac1-checkoff.<TS>.md`. This task completes in either case.

- [ ] [P4-T5] Check off AC2 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC2. ` to begin `- [x] AC2. `. Evidence: P2-T2's census (`UiDispatcher.CheckAccess()` 2 and `isOwnerThread` 4, both 0 before), which places the precondition inside each delegate before the guarded call, and P3-T3, which observed both tests failing on that precondition with `vacuously` in the message when the delegate ran inline. Acceptance: exactly one line begins `- [x] AC2. `, none begins `- [ ] AC2. `, no other AC line changed; otherwise `AC2: NOT MET` recorded in `FEATURE/evidence/regression-testing/p4-t5-ac2-checkoff.<TS>.md` and the box left unchecked. This task completes in either case.

- [ ] [P4-T6] Check off AC3 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC3. ` to begin `- [x] AC3. `. Evidence: P2-T2's census (`BeOfType<InvalidOperationException>()` 2, `captured.Message.Should().Contain(` 2, `NotBeOfType<ObjectDisposedException>()` 2), P4-T2 (both pass), and P3-T1 (the message assertion observed failing, with the operation name absent from the `CaptureCurrent()` message, when the guard was disabled). Acceptance: exactly one line begins `- [x] AC3. `, none begins `- [ ] AC3. `, no other AC line changed; otherwise `AC3: NOT MET` recorded in `FEATURE/evidence/regression-testing/p4-t6-ac3-checkoff.<TS>.md`. This task completes in either case.

- [ ] [P4-T7] Check off AC5 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC5. ` to begin `- [x] AC5. `. Evidence: P3-T1 (deterministic guard-disabled failing run of both replacement tests, `ExpectedExitCode: 1`, no production edit), P3-T2 and P3-T4 (anchored `git diff --exit-code HEAD` 0, empty scoped porcelain, `SHA256` equal to `FIX-HASH:`), and P4-T2 (final pass-after run after the reverts). Acceptance: exactly one line begins `- [x] AC5. `, none begins `- [ ] AC5. `, no other AC line changed; otherwise `AC5: NOT MET` recorded in `FEATURE/evidence/regression-testing/p4-t7-ac5-checkoff.<TS>.md`. This task completes in either case.

- [ ] [P4-T8] Check off AC6 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC6. ` to begin `- [x] AC6. `. Evidence: P4-T2 (measured: exit 0, 2 of 2 passed under `/Settings:scripts\vscode\TaskMaster.cli.runsettings`, `RUNSETTINGS-HASH-NOW:` unchanged) and P4-T3 (confirming). Acceptance: exactly one line begins `- [x] AC6. `, none begins `- [ ] AC6. `, no other AC line changed; otherwise `AC6: NOT MET` recorded in `FEATURE/evidence/regression-testing/p4-t8-ac6-checkoff.<TS>.md`. This task completes in either case.

### Phase 5 — Final QC Loop, Coverage Delta, and Commit

- [ ] [P5-T1] Run the repository-wide formatter `dotnet tool run csharpier format .` from the worktree root and write `FEATURE/evidence/qa-gates/p5-t1-csharpier-format.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording the tool's `Formatted N files in Xms.` line verbatim (a processed-file count, not a rewrite count), and `FORMAT_CHANGED_TREE:` computed as whether the SHA-256 of the text printed by `git diff MERGE-BASE -- QuickFiler QuickFiler.Test` differs between a capture immediately before and a capture immediately after the command (the anchored patch is compared, not a name list, because the formatter can rewrite a file the plan already changed without changing the name list). Acceptance, both required: `EXIT_CODE: 0`; `FORMAT_CHANGED_TREE:` is recorded as `True` or `False`. When `True`, record which paths changed by `git diff --name-only MERGE-BASE -- QuickFiler QuickFiler.Test` together with `git status --porcelain -- QuickFiler QuickFiler.Test` (both recorded); a changed path that is `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` means P2-T2's scoped format and the repository-wide format disagreed and the loop restarts from this task after re-running `CMD-CENSUS` to confirm the token table still holds; any other changed path was clean at P0-T6 and cannot have been rewritten by a correct pinned formatter, so it is `FORMAT SCOPE BREACH`: stop and report. `artifacts/csharp/coverage.xml` does not exist while this loop runs (P5-T8 writes it only after the final clean iteration, D-12); if a prior aborted session left one behind, a rewrite of that git-ignored file by this command is expected, is outside the pathspec-scoped diff above, and is not a `FORMAT SCOPE BREACH`.

- [ ] [P5-T2] Run `dotnet tool run csharpier check .` from the worktree root and write `FEATURE/evidence/qa-gates/p5-t2-csharpier-check.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` quoting the `Checked N files in Xms.` line verbatim and recording `CHECKED-FILES:` and `CHECKED-DELTA:` as that N minus the P0-T6 `CHECKED-FILES:` value (an observation; the counter is repository-wide and no upper bound is gated). Acceptance, both required: `EXIT_CODE: 0`; `CHECKED-DELTA:` is at least 0, because this plan adds and removes no countable file. A non-zero exit restarts the loop from P5-T1.

- [ ] [P5-T3] Run the analyzer gate `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true "/flp:LogFile=coverage\p5-t3.msbuild.log;Verbosity=normal"` (resolved through vswhere) and write `FEATURE/evidence/qa-gates/p5-t3-msbuild-analyzers.<TS>.md` with the P0-T7 field set plus `FILE-DIAGNOSTIC-LINES:` as the count of lines in `coverage\p5-t3.msbuild.log` that contain both `ItemViewerBreadcrumbThreadAffinityTests.cs` and either `warning ` or `error `. Acceptance, all four required (D-8): `EXIT_CODE:` equals `ANALYZE-BASELINE-EXIT:`; `ERRORS:` equals the P0-T7 `ERRORS:` value; `FILE-DIAGNOSTIC-LINES: 0`; `CSC_OUT_LINES:` at least 1. A diagnostic naming the Write Set file is repaired in that file within the Target Source contract and the loop restarts from P5-T1; any other failure is reported.

- [ ] [P5-T4] Run the nullable gate `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true "/flp:LogFile=coverage\p5-t4.msbuild.log;Verbosity=normal"` (no `/p:Nullable=enable`, no `/t:Build`) and write `FEATURE/evidence/qa-gates/p5-t4-msbuild-nullable.<TS>.md` with the P0-T8 field set plus `FILE-DIAGNOSTIC-LINES:` computed as in P5-T3 over `coverage\p5-t4.msbuild.log`. Acceptance, all four required: `EXIT_CODE:` equals `NULLABLE-BASELINE-EXIT:`; `ERRORS:` equals the P0-T8 `ERRORS:` value; `FILE-DIAGNOSTIC-LINES: 0`; `CSC_OUT_LINES:` at least 1. Same restart rule as P5-T3.

- [ ] [P5-T5] Run the final repository-wide test and coverage pass with `CMD-COVERAGE` using `STAGE` `final` and write `FEATURE/evidence/qa-gates/p5-t5-mstest-coverage.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:` (the `COLLECT_EXIT_CODE:`), `CHANNEL:`, and an `Output Summary:` recording `ASSEMBLY_COUNT:`, every `ASSEMBLY:` line, the TRX counters from `TestResults\900\final\final-900.trx`, `FINAL-FAILING-SET:` (every `Failed` name), and `NEWLY-FAILING:` as the names in `FINAL-FAILING-SET:` that are not in P0-T10's `BASELINE-FAILING-SET:`, or `NONE`. The exit-code expectation is keyed to this run and not to the baseline count: write `ExpectedExitCode: 1` when this run reports at least one failed test and every failed name is in `BASELINE-FAILING-SET:`; write no expectation line when it reports none; write nothing and fail the task when `NEWLY-FAILING:` is not `NONE`. Acceptance, all four required: `ASSEMBLY_COUNT:` equals the P0-T10 value and is at least 1, so the two denominators cover the same assembly set; every `ASSEMBLY:` line begins with `\` and contains the segment `\bin\Debug\` (the same shape clause as P0-T10); `NEWLY-FAILING: NONE`; the seven `ItemViewerBreadcrumbThreadAffinityTests` results in the TRX all read `Passed`.

- [ ] [P5-T6] Post-process the final Cobertura with `CMD-POSTPROCESS` using `STAGE` `final` and write `FEATURE/evidence/qa-gates/p5-t6-coverage-delta.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` carrying `BASELINE-COVERAGE:` (the six P0-T10 values), `FINAL-COVERAGE:` (the six values printed now), the arithmetic difference of each, `ItemViewerBreadcrumbClassCount=`, `ThreadAffinityTestsClassCount=`, `FLOOR:`, exactly one `COMPARABILITY:` line, and exactly one `CHANGED-CODE COVERAGE:` line. `COMPARABILITY: A` when the absolute difference of the two `lines-valid` values is at most 1 percent of the baseline `lines-valid`, in which case the gate is that the final `line-rate` is not more than 0.005 below the baseline `line-rate`; `COMPARABILITY: B` otherwise, in which case the rates were computed over different merged denominators, the comparison is recorded and not gated, and the artifact says so in one sentence. `CHANGED-CODE COVERAGE: NOT MEASURABLE` when both class counts are 0, with the D-7 rationale stated (no production line changed; the test assembly is excluded by the derived settings; `ItemViewer` carries `[ExcludeFromCodeCoverage]` at `QuickFiler/Viewers/ItemViewer.cs:20`) and the substitute behavioral evidence named (P4-T2, P4-T3, P3-T1, P3-T3); `CHANGED-CODE COVERAGE: MEASURED` with the per-file covered and valid line counts when either count is non-zero, reported to the orchestrator as `INSTRUMENTATION SCOPE CHANGED`. This task does not copy the Cobertura document to `artifacts/csharp/coverage.xml`: that copy happens only after the final clean iteration of the P5-T1 through P5-T7 loop, in P5-T8, because `.csharpierignore` does not cover that path (fact 21) and a loop restart from P5-T1 would otherwise run `csharpier format .` over the roughly 10 MB generated file. Acceptance, all three required: all twelve numeric values are recorded; exactly one `COMPARABILITY:` line and, under `A`, the rate gate holds; exactly one `CHANGED-CODE COVERAGE:` line.

- [ ] [P5-T7] Audit the file size and token table after formatting by running `CMD-CENSUS` on `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` and write `FEATURE/evidence/qa-gates/p5-t7-file-size-and-census.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` transcribing every `TOKEN` line, `LINES`, and `SHA256`. This runs after P5-T1 because CSharpier can change line counts. If `LINES` exceeds 500, stop and report `FILE SIZE LIMIT EXCEEDED` with the value rather than shortening the XML remarks, because the remarks carry the pinned `#900` (3) and `vacuously` (3) census counts and shortening them risks moving those counts. Acceptance, all three required: `LINES` at most 500; every `TOKEN` value equals the "After P2-T1 and P2-T2" column, including `ClearViewerDispatcher(scope.Viewer);` 1 and `action();` 1 (no mutation residue); `SHA256` equals `FIX-HASH:` when P5-T1 recorded `FORMAT_CHANGED_TREE: False`, and otherwise the artifact records the new hash under `POST-FORMAT-HASH:` with the reason.

- [ ] [P5-T8] Close the toolchain loop and, only then, write the review-input copy. First determine, for each step P5-T1 through P5-T7 of the most recent iteration, whether it met its own declared expectation (including a declared `ExpectedExitCode: 1` on P5-T5). If any did not, the loop restarts from P5-T1 and this task is re-run after it; nothing is copied. When all seven did, copy the post-processed `coverage/final-900.cobertura.xml` to `artifacts/csharp/coverage.xml` with `Copy-Item -LiteralPath coverage/final-900.cobertura.xml -Destination artifacts/csharp/coverage.xml -Force` (creating `artifacts/csharp/` if absent; a git-ignored review input, a verbatim Cobertura copy per the issue #781 decision; no formatter pass runs after this point, D-12), then write `FEATURE/evidence/qa-gates/p5-t8-toolchain-loop-closure.<TS>.md` with `Timestamp:`, `Command:` (`reconciliation of P5-T1 through P5-T7; Copy-Item to artifacts/csharp/coverage.xml`), `EXIT_CODE: 0`, `CHANNEL:`, and an `Output Summary:` carrying one `EXPECTATION-MET:` line per step P5-T1 through P5-T7, `ITERATIONS:` as the number of times the loop was started from P5-T1, `LOOP: CLEAN PASS`, and `COVERAGE-XML-ROOT-LINE-RATE:` as the `line-rate` attribute of the root `<coverage` element of `artifacts/csharp/coverage.xml` read back after the copy. Acceptance, all three required: the artifact records seven `EXPECTATION-MET:` lines, each `YES`, and `ITERATIONS:`; `LOOP: CLEAN PASS` is present; `artifacts/csharp/coverage.xml` exists, its root element is `<coverage`, and `COVERAGE-XML-ROOT-LINE-RATE:` equals the `FINAL-COVERAGE:` `line-rate` recorded by P5-T6.

- [ ] [P5-T9] Verify the scope boundary (measured run for AC7's no-production-file clause) and write `FEATURE/evidence/qa-gates/p5-t9-scope-boundary.<TS>.md`. Run, in one task: `git diff --name-only MERGE-BASE -- QuickFiler QuickFiler.Test` (working-tree form); `git status --porcelain --untracked-files=all -- QuickFiler QuickFiler.Test`; `git diff --name-only origin/main..HEAD -- QuickFiler QuickFiler.Test`; `git diff --name-only origin/main...HEAD -- QuickFiler QuickFiler.Test`; the SHA-256 of `scripts/vscode/TaskMaster.cli.runsettings` (`Get-FileHash -Algorithm SHA256 -LiteralPath`); and `git diff --exit-code MERGE-BASE -- scripts/vscode/TaskMaster.cli.runsettings QuickFiler.Test/QuickFiler.Test.csproj`. Record the four path lists verbatim, `TWO-DOT-VS-THREE-DOT-AGREE:` as whether the third list (`origin/main..HEAD`, every path that differs between the tip of `origin/main` and `HEAD`) and the fourth list (`origin/main...HEAD`, every path changed on this branch since the merge base) are identical (the DIFF BASES block requires both forms to be run and compared; they agree while `origin/main` is an ancestor of `HEAD`, and a disagreement means `origin/main` advanced past the branch point after P0-T3's fetch, in which case the artifact records both lists and the disagreement is reported to the orchestrator as `BASE ADVANCED`, because the two-dot list then carries paths this branch never touched), `CHANGED-PATHS:` as the union of the first two lists minus `INHERITED-CLAUSE-A:` (P0-T3) minus any path under `.claude/agent-memory/`, and `RUNSETTINGS-HASH-NOW:`. Acceptance, all four required: `CHANGED-PATHS:` is exactly `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` and nothing else, so no path under `QuickFiler/` and no other path under `QuickFiler.Test/` changed; the `git diff --exit-code` span for the runsettings and the project file exits 0; `RUNSETTINGS-HASH-NOW:` equals `RUNSETTINGS-HASH:` (P0-T4); `TWO-DOT-VS-THREE-DOT-AGREE:` is recorded as `True` or `False` and the fourth list (three-dot) is exactly the one test file path.

- [ ] [P5-T10] Check off AC7 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC7. ` to begin `- [x] AC7. `. Evidence: P4-T3 (all seven tests pass, measured) and P5-T9 (`CHANGED-PATHS:` is exactly the test file; measured on the working tree and committed history before the final commit; P5-T15's closure record confirms it on the committed tip). Acceptance: exactly one line begins `- [x] AC7. `, none begins `- [ ] AC7. `, no other AC line changed; otherwise `AC7: NOT MET` recorded in `FEATURE/evidence/qa-gates/p5-t10-ac7-checkoff.<TS>.md`. This task completes in either case.

- [ ] [P5-T11] Check off AC8 in `FEATURE/spec.md` by changing the single line beginning `- [ ] AC8. ` to begin `- [x] AC8. `. Evidence: P5-T8 `LOOP: CLEAN PASS` over P5-T1 through P5-T7 in order, and P5-T6's numeric `BASELINE-COVERAGE:`, `FINAL-COVERAGE:`, `COMPARABILITY:` and `CHANGED-CODE COVERAGE:` lines. Acceptance: exactly one line begins `- [x] AC8. `, none begins `- [ ] AC8. `, no other AC line changed; otherwise `AC8: NOT MET` recorded in `FEATURE/evidence/qa-gates/p5-t11-ac8-checkoff.<TS>.md`. This task completes in either case.

- [ ] [P5-T12] Mirror the issue update to `FEATURE/evidence/issue-updates/issue-900.<TS>.md` with `Timestamp:`, the exact text intended for the GitHub issue (the AC checkbox state of `FEATURE/spec.md` after P5-T11, the branch name, and the one-paragraph summary of the fix), and `PostedAs: body`, `PostedAs: comment`, or a `POSTING BLOCKED` header with the reason (the executor has no GitHub tool surface, so `POSTING BLOCKED` with that reason is the expected form; the orchestrator posts from the mirror). Acceptance: the artifact exists, carries `Timestamp:`, carries the AC state, and carries exactly one of the three posting forms; the filename timestamp equals its `Timestamp:` field.

- [ ] [P5-T13] Sweep the feature folder for host identifiers by running, over every file under `docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` including this plan and `spec.md`, `$t = [regex]::Escape((Split-Path -Leaf $env:USERPROFILE))` followed by `@(Get-ChildItem -Recurse -File -LiteralPath docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900 | Select-String -Pattern "(?i)$t").Count` and `@(Get-ChildItem -Recurse -File -LiteralPath docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900 | Select-String -Pattern "[A-Za-z]:[\\/]Users[\\/]").Count`, and write `FEATURE/evidence/qa-gates/p5-t13-host-path-sweep.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `CHANNEL:`, and an `Output Summary:` recording `ACCOUNT-TOKEN-MATCHES:` and `USERS-PATH-MATCHES:`; the token itself is derived at run time and is never written into the artifact. Acceptance, both required: both counts are 0. A non-zero count is repaired by replacing the offending text with the placeholders `<repo-root>`, `<user-profile>`, `<user>` or `<host>` in the artifact that carries it (never by deleting the artifact) and the sweep is re-run until both counts are 0.

- [ ] [P5-T14] Write the follow-up handoff record `FEATURE/evidence/other/p5-t14-follow-up-handoff.<TS>.md` for the orchestrator, who files the two potential entries through `mcp__drm-copilot__new_potential_bug_entry` (D-9; the executor does not create files under `docs/features/potential/`). The record carries `Timestamp:` and, for each entry, a `short_name`, a one-paragraph summary, exact file and line citations, and the sentence that the entry is not fixed under #900: (a) `short_name` `breadcrumb-dispatcher-tests-assume-taskrun-distinct-thread`, covering `QuickFiler.Test/Viewers/BreadcrumbPopupBoundaryCoverageTests.cs:58-61` (`Dispatcher_OwnerOnlyWorker_ReportsWithoutRunningAction`) and `QuickFiler.Test/Viewers/BreadcrumbUiThreadDispatchTests.cs:298-307`, which assert a cross-thread failure from a `Task.Run` delegate against `BreadcrumbUiDispatcher`'s owner-id check (`QuickFiler/Viewers/BreadcrumbUiDispatcher.cs:40,54,64`) and share the assumption #900 removes, with the research artifact's note that the second uses `await` and is exposed only to genuine idle-thread reuse; (b) `short_name` `breadcrumb-null-owner-test-discrimination-remark-holds-only-when-stolen`, covering the remark at `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs` lines 272-278 (pre-edit numbering; post-edit numbering is recorded from `CMD-CENSUS`-era line counts by locating the literal `the pre-fix guard reads the non-null captured context`) on `InitializeBreadcrumbPipeline_NullOwningDispatcher_DoesNotThrow`, whose discrimination claim holds only in the stolen branch per research Q4. Acceptance, all three required: the record exists; it names both `short_name` values exactly once each; it contains the literal `not fixed under #900` at least twice.

- [ ] [P5-T15] Commit the final state, write the closure record, and leave the source and feature trees clean. Steps, in order: (1) write `FEATURE/evidence/other/commit-message-final.txt` (subject `docs(900): evidence, acceptance check-off and plan state for the thread-affinity test fix`, body listing the artifact kinds, no gated tool named; if the executing session's harness requires attribution trailer lines on commits, append them here) and run `git add -- QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` then `git commit -F docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900/evidence/other/commit-message-final.txt`, recording its exit code as `COMMIT-1-EXIT:`; (2) write `FEATURE/evidence/other/p5-t15-closure.<TS>.md` with `Timestamp:`, `Command:` (the git spans of this task), `HEAD-AFTER-COMMIT:` (`git rev-parse HEAD`), `COMMIT-1-EXIT:`, the confirming AC7 lists `git diff --name-only origin/main...HEAD -- QuickFiler QuickFiler.Test` and `git diff --name-only MERGE-BASE..HEAD -- QuickFiler QuickFiler.Test` (each must list exactly `QuickFiler.Test/Viewers/ItemViewerBreadcrumbThreadAffinityTests.cs`; a `git add` span is the companion of these name-listing diffs), the pointer to P5-T14, and the acceptance-criteria status summary in the exact form `.claude/skills/acceptance-criteria-tracking/SKILL.md` requires (`Source:` `FEATURE/spec.md`, `Total AC items: 8`, `Checked off (delivered): <n>` counted from the `- [x] AC` lines on disk, `Remaining (unchecked): <8 - n>`, `Items remaining:` listing any unchecked criterion text or `none`); (3) tick every remaining unchecked checkbox in `FEATURE/plan.2026-09-16T23-27.md`, including this task's own; (4) run `git add -- docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` then `git commit --amend --no-edit`, recording `COMMIT-2-EXIT:` in the executor's final message (the artifact cannot record it); (5) run `git status --porcelain -- QuickFiler QuickFiler.Test docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` and `git rev-parse HEAD`; (6) append `EXIT_CODE:` (the exit code of the step-5 porcelain span, the observation this artifact's exit-code field is scoped to under the fail-closed evidence rule's carve-out), `POST-AMEND-PORCELAIN:` (`EMPTY` when the span printed nothing, else the lines verbatim) and `FINAL-HEAD:` to the closure record; (7) run `git add -- docs/features/active/breadcrumb-thread-affinity-tests-assume-taskrun-distinct-thread-900` then `git commit --amend --no-edit` once more, and write nothing afterwards. `.claude/agent-memory/` paths are outside every span above by pathspec and are not this task's residual. Acceptance, all five required: `COMMIT-1-EXIT: 0`; both confirming AC7 lists are exactly the one test file path; the status summary reports `Total AC items: 8` and its `Checked off` count equals the number of `- [x] AC` lines in `FEATURE/spec.md`; `POST-AMEND-PORCELAIN: EMPTY`; the executor's final message reports the plan path, `FINAL-HEAD:`, `COMMIT-2-EXIT:`, the status summary, and the P5-T14 follow-up pointer. This task's commits stage `QuickFiler.Test/...`, outside every exempt tree, so the issue #539 exemption does not apply. A PreToolUse refusal at any `git add`, `git commit`, or `git commit --amend` span in this task is `PRE-IMPLEMENTATION GATE BLOCKED`: stop and report the refusal text verbatim; do not alter hooks, checkpoints, permission configuration, or another item's files. This is a STOP-AND-REPORT condition under the COMMIT AND SCOPE block, not a workaround.
