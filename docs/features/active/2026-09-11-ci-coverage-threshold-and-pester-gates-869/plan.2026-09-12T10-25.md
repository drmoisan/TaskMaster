# 2026-09-11-ci-coverage-threshold-and-pester-gates (Plan)

- **Issue:** #869 (closes #561 and #562 on merge)
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-12T10-25
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** full-bug. The sole acceptance-criteria source is the `## Acceptance Criteria` section of `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/spec.md`, which holds 31 criteria.

**Fail-closed evidence rule:** Every evidence-producing task names the artifact path it must write. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or is missing a required field, the verdict is BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Every command-step artifact carries `Timestamp:`, `Command:`, `EXIT_CODE:` and `Output Summary:`. Coverage artifacts additionally carry numeric headline values. For PowerShell runs that means BOTH the JaCoCo LINE figure and Pester's command figure, each labelled. Artifact file names use the fixed suffix `2026-09-12T10-25`; the `Timestamp:` field inside each artifact records the actual capture time.

**Evidence location:** all artifacts are written under `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/` in one of the sub-folders `baseline`, `regression-testing`, `qa-gates`, `issue-updates`, `other`. No artifacts tree location is permitted for evidence.

## Mechanical task counts

Counted by matching the pattern for an unchecked task line at the start of a line.

| Phase | Tasks |
| --- | --- |
| Phase 0 | 17 |
| Phase 1 | 2 |
| Phase 2 | 8 |
| Phase 3 | 2 |
| Phase 4 | 6 |
| Phase 5 | 7 |
| Phase 6 | 10 |
| Phase 7 | 8 |
| Phase 8 | 7 |
| Phase 9 | 33 |
| Phase 10 | 14 |
| **Total** | **114** |

## Declared write set

The delivery creates, modifies or deletes exactly these paths. The set is identical to the `## Write Set` section of the specification.

`.github/workflows/_mstest-coverage.yml`, `.github/workflows/_pester.yml`, `.github/workflows/ci.yml`, `.github/workflows/README.md`, `scripts/vscode/Invoke-MSTestWithCoverage.Threshold.ps1`, `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, `scripts/vscode/Invoke-VSBuild.ps1`, `scripts/vscode/Invoke-Restore.ps1`, `tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1`, `tests/scripts/vscode/Invoke-MSTestWithCoverage.AssemblyDiscovery.Tests.ps1`, `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`, `tests/scripts/vscode/Invoke-MSTestWithCoverage.Merge.Tests.ps1`, `tests/scripts/vscode/Invoke-MSTest.Main.Tests.ps1`, `tests/scripts/vscode/Invoke-VSBuild.Tests.ps1`, `tests/scripts/vscode/Invoke-Restore.Tests.ps1`, `tests/scripts/vscode/TestProcessCleanup.Tests.ps1`, `tests/scripts/vscode/Install-RepoDotNetSdk.Tests.ps1`, `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/issue.md`, `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/spec.md`, `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/user-story.md`, `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/plan.2026-09-12T10-25.md`, `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/research/2026-09-12T11-05-ci-coverage-and-pester-gates-research.md`, `docs/features/potential/promoted/2026-09-11-ci-coverage-threshold-and-pester-gates.md`, `docs/features/potential/2026-09-11-ci-coverage-threshold-and-pester-gates.md`, plus every artifact under the feature folder's evidence directory named by the tasks below.

The second-to-last entry is a deletion the promotion step performed and the preparation commit that established this branch committed, before this plan's anchor tag is created. No task in this plan acts on it, and it does not appear in any diff anchored at the `plan-869-base` tag. It is listed so that this set stays identical to the specification's write set.

Created only if the tracked-fixture contingency of task P6-T7 fires: `tests/scripts/vscode/fixtures/sync-package-references/packages.config` and `tests/scripts/vscode/fixtures/sync-package-references/SyncFixture.Test.csproj`.

## Paths this delivery does not touch

These are named without code formatting on purpose, so that no automated path harvest treats them as part of this delivery's blast radius.

- coverage.config, the repository coverage settings file. It already excludes the third-party modules that would otherwise break instrumentation.
- scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1. It sits at 470 of its 500-line ceiling and must not receive the new assertion. Its existing dot-source of the threshold part already resolves the new function for every caller.
- scripts/vscode/Sync-PackageReferences.ps1, the package-reference sync script.
- scripts/vscode/Invoke-MSTestWithCoverage.FirstParty.ps1, the first-party projection script.
- scripts/vscode/TestProcessCleanup.ps1, scripts/vscode/Install-RepoDotNetSdk.ps1 and scripts/vscode/Invoke-MSTest.ps1. Only their test files change.
- The rules files under the .claude rules directory, and CLAUDE.md. They are push-down-owned from an upstream repository and are overwritten with no templating.
- The main branch ruleset in repository settings. The maintainer performs that edit out of band; no task in this plan covers it.

## Known contention

Another item in the same parallel run rewrites the coverage entry point and its parts to emit projection-only evidence. The two items are scheduled in different cohorts and the scheduler serializes them. No task, note or acceptance condition in this plan asks for coordination with it.

## Superseded prior basis

The 2026-09-03 JaCoCo figures (INSTRUCTION 629 covered of 803, 78.33 percent; LINE 535 covered of 681, 78.56 percent) are the prior basis only. They predate two files now in the tree. Phase 0 re-measures, and the required delta is computed as N equal to the ceiling of 0.80 times T, minus C, where T and C are the total and covered LINE counts from the Phase 0 run. No fixed delta number is carried forward.

---

### Phase 0 — Baseline capture, policy reads and environment discipline

- [ ] [P0-T1] Read, in this order, CLAUDE.md, then .claude/rules/general-code-change.md, then .claude/rules/general-unit-test.md, then .claude/rules/powershell.md, .claude/rules/quality-tiers.md, .claude/rules/ci-workflows.md and .claude/rules/tonality.md, then `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/spec.md`, then `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/issue.md`, then `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/research/2026-09-12T11-05-ci-coverage-and-pester-gates-research.md`, then `.github/workflows/README.md`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/phase0-instructions-read.2026-09-12T10-25.md`. Acceptance: the artifact exists, carries `Timestamp:` and `Policy Order:`, and lists all eleven paths above in the order read.
- [ ] [P0-T2] Write the environment-discipline record to `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/phase0-environment-discipline.2026-09-12T10-25.md`. It must state all eight of these constraints: the Bash allowlist grants only git, gh, pwsh, poetry run and three scripts under the .claude lib directory, and every chained segment is checked independently, so a command that changes directory and then runs something else is denied; use git with the repository-directory switch rather than changing directory, and use the Read, Grep and Glob tools instead of cat, grep, sed or find; the executor runs without worktree isolation because the Bash tool's isolation filter refuses pwsh; a pwsh command payload uses outer single quotes and inner double quotes; no script or test file may exceed 500 lines; the PowerShell test-purity hook denies a write to any test file whose content matches a temporary-file API, a temporary-path environment variable, a network API, a socket API, a process-start call, a sleep call, or a direct mock of git, gh or actionlint, so tests mock the named wrapper seam and never the executable; a Pester helper function is defined inside the setup block and never at file scope, because each test case runs in a child scope; and there is no Python toolchain in this repository, so no poetry run python step is performed. Record, as a ninth constraint, that every commit task in this plan names non-exempt pathspecs and therefore requires the orchestration checkpoint to be present; if the first commit is denied by the pre-implementation gate, halt and report rather than reshaping the pathspec. Record, as a tenth constraint, that a newly created PowerShell file that carries any non-ASCII character must be written with a UTF-8 byte-order mark, because the analyzer reports a new diagnostic otherwise and the per-batch analyzer gate is judged against the baseline count. Before writing the artifact, probe reachability by running `pwsh -NoProfile -Command '"PROBE OK"'` and record the result as an eleventh statement. Authorized halt branch: if the Bash tool refuses the probe, the executor is running under worktree isolation, every later pwsh task in this plan is unreachable, and the executor halts and reports rather than attempting P0-T3. Then open batch 1's change budget by running `pwsh -NoProfile -Command 'Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue | Remove-Item -Force; Write-Output "BATCH BUDGET RESET"'`, and record as a twelfth statement that the output carried the line `BATCH BUDGET RESET` and that a following `pwsh -NoProfile -Command '@(Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue).Count'` printed 0. This opening reset mirrors the four batch-boundary resets later in the plan and is required because batch 1 writes exactly three distinct test files, which is the whole per-batch test allowance, so a single slot carried over from an earlier session in this worktree would make the hook deny one of the writes in P1-T1, P1-T2, P2-T3 or P2-T4. The state directory carries a gitignore entry, but one budget file in it was committed before that entry existed and is therefore tracked, so this deletion does produce an unstaged deletion in the unscoped porcelain output. That path lies outside the four roots .github, scripts, tests and docs, so it affects neither the scoped assertion in P0-T3 nor the four-root assertion in P10-T13 nor the anchored diff clause of P10-T14; P10-T14 restores it before its unscoped porcelain assertion runs. Acceptance: the artifact exists and contains all twelve statements.
- [ ] [P0-T3] Record the pre-change tree state. Run `git -C . rev-parse HEAD`, `git -C . status --porcelain=v1 --untracked-files=all`, and `git -C . tag plan-869-base`. Before creating the anchor tag, run `git -C . status --porcelain=v1 --untracked-files=all` and record its output verbatim as the unscoped anchor-state observation. That unscoped output is recorded and is not a gate, because P10-T14 accepts a residual change under the agent-memory directory and halting on one here would stop the plan on a path no assertion in it covers. Then run `git -C . status --porcelain=v1 --untracked-files=all -- .github scripts tests docs ':!docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence' ':!docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/plan.2026-09-12T10-25.md'` and record its output verbatim. Both exclusions are load-bearing and cover the three writes this plan performs before this task runs: P0-T1 and P0-T2 write two artifacts into the excluded evidence directory, and the executor's completion protocol writes each of those two tasks' check-off into this plan document, which sits at the feature folder root and is therefore not covered by the evidence exclusion. Without both exclusions the command is non-empty by construction at this point and the halt branch below would stop the plan on paths this plan itself creates. The plan document's own state is not lost by being excluded here, because the unscoped observation recorded earlier in this task captures it verbatim. Authorized halt branch: if that scoped output is non-empty, halt and report rather than creating the tag, because every anchored diff and every porcelain assertion later in this plan is taken over those four roots and assumes they are clean at the anchor apart from this delivery's own evidence artifacts and this plan document, and because the deletion under the potential-features directory is performed by the preparation commit rather than by any task in this plan, so a pending deletion at this point would leave P10-T13's porcelain assertion unsatisfiable. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/tree-state.2026-09-12T10-25.md` carrying `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the full 40-character head SHA, and the porcelain output. The tag is the self-anchor every later diff gate in this plan compares against; no fixed upstream ref is used, because a fixed anchor degenerates as other work lands. Acceptance: `git -C . rev-parse plan-869-base` prints the same 40-character SHA the artifact records. If the tag already exists before this task runs, halt and report, because a pre-existing tag means an earlier run of this plan is in progress and re-creating the anchor would silently move every later diff gate.
- [ ] [P0-T4] Record the pre-change line count of every production and test file in the declared write set that already exists, using the Grep tool in count mode over the start-of-line pattern against the two directories scripts/vscode and tests/scripts/vscode. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/file-line-counts.2026-09-12T10-25.md`. Acceptance: the artifact records `scripts/vscode/Invoke-MSTestWithCoverage.Threshold.ps1` at 56, `scripts/vscode/Invoke-MSTestWithCoverage.ps1` at 351, `scripts/vscode/Invoke-VSBuild.ps1` at 167, `scripts/vscode/Invoke-Restore.ps1` at 39, `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1` at 496, `tests/scripts/vscode/Invoke-VSBuild.Tests.ps1` at 86, `tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1` at 15, `tests/scripts/vscode/Invoke-MSTestWithCoverage.AssemblyDiscovery.Tests.ps1` at 99, `tests/scripts/vscode/Invoke-MSTestWithCoverage.Merge.Tests.ps1` at 71, `tests/scripts/vscode/Invoke-MSTest.Main.Tests.ps1` at 144, and `tests/scripts/vscode/Install-RepoDotNetSdk.Tests.ps1` at 28. A mismatch on any one of these eleven values halts the plan and is reported, because the plan's citations describe a different tree.
- [ ] [P0-T5] Bootstrap the worktree toolchain. This worktree is a fresh checkout and none of these steps can be assumed already done. Every command in this task is issued through pwsh, because the Bash allowlist grants no bare dotnet, nuget or msbuild entry and denies each chained segment independently. Run `pwsh -NoProfile -Command 'dotnet tool restore'`, then `pwsh -NoProfile -Command 'nuget restore TaskMaster.sln'`, then `pwsh -NoProfile -Command 'Get-Command dotnet-coverage'`, then `pwsh -NoProfile -Command 'Get-Command msbuild'`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/toolchain-bootstrap.2026-09-12T10-25.md` with one `Command:` and `EXIT_CODE:` pair per invocation. Acceptance: the first two invocations record `EXIT_CODE: 0`; the third records `EXIT_CODE: 0` and the resolved dotnet-coverage command path, either directly or on the re-run its fallback branch prescribes; and the fourth records either `EXIT_CODE: 0` with the resolved msbuild command path, or a non-zero exit code together with the vswhere-resolved absolute msbuild path its fallback branch prescribes. The artifact additionally records the resolved dotnet-coverage version string, obtained by running `pwsh -NoProfile -Command 'dotnet-coverage --version'` and recording the printed version; if that switch is not supported, the version is read instead from the dotnet-coverage row printed by `pwsh -NoProfile -Command 'dotnet tool list --global'`, and the artifact records which of the two sources supplied it. That recorded version string is the literal P7-T1 pins in the workflow file, so no version literal in this plan is executor-chosen. If the third fails, run `pwsh -NoProfile -Command 'dotnet tool install --global dotnet-coverage'` and record that command and its exit code before re-running the lookup. If the fourth fails, resolve msbuild through the vswhere path the production scripts already use, record the resolved absolute path in the artifact, and use that absolute path in place of the bare command name inside the pwsh payload of task P0-T6.
- [ ] [P0-T6] Build the solution so the coverage route has assemblies to discover. Run `pwsh -NoProfile -Command 'msbuild TaskMaster.sln /t:Build /m /p:Configuration=Debug "/p:Platform=Any CPU"'`, because the Bash allowlist grants no bare msbuild entry. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/msbuild-build.2026-09-12T10-25.md`. Acceptance: `EXIT_CODE: 0`, and the `Output Summary:` quotes the msbuild summary line reporting `0 Error(s)` together with the preceding warning-count line, so that a larger error count ending in the same three characters cannot be mistaken for zero.
- [ ] [P0-T7] Capture the baseline C# coverage route. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/csharp-coverage-route.2026-09-12T10-25.md`. Acceptance: the artifact records the exit code. When the exit code is 0, the `Output Summary:` reproduces verbatim the single printed line that begins `First-party coverage:` carrying both the line counts with their percentage and the branch counts with their percentage. When the exit code is non-zero, the line assertion at line 344 of the coverage entry point threw before the report statement at line 345 could print, so no such line exists; the `Output Summary:` instead reproduces the thrown message verbatim. In both cases the artifact records the values of the three document-root attributes `line-rate`, `branch-rate` and `branches-valid` read from the Cobertura document the run wrote at line 342 before either assertion could throw. This baseline task does not demand a zero exit code: the branch assertion does not exist yet, so only the existing line assertion runs, and a non-zero exit here would mean the first-party line figure is already below the 80 floor on the unchanged tree. Authorized halt branch: if the exit code is non-zero, record the thrown message, halt, and report, because wiring the C# gate onto an already-below-floor tree is outside this delivery's scope and would require a separate C# coverage uplift. Second authorized halt branch: if vstest does not terminate within the executor's tool timeout, terminate it, record `EXIT_CODE: 124` together with a `TimeoutObserved: true` line and the names of the test classes that were executing, report the item BLOCKED to the caller, and perform no further task in this plan. Third authorized halt branch: if the written document carries no `branch-rate` attribute, carries `branches-valid="0"`, or carries a `branch-rate` whose parsed value is below 0.75, record that fact, halt and report. The first two cases make the delivery's C# branch assertion fail closed on every run for a reason the delivery does not control. The third means the unchanged tree already sits below the 75 floor this delivery introduces, so the zero exit code P10-T8 requires, which depends on the new branch assertion returning without throwing, could not be reached by any work inside this delivery's scope; that case requires a separate C# branch-coverage uplift and is reported rather than absorbed. The three line numbers 342, 344 and 345 cited above are the pre-change positions in `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, which is the state this task runs against; P2-T2 inserts a line after 344 and shifts the report statement afterwards.
- [ ] [P0-T8] Capture the baseline PowerShell Pester run with coverage. Run `pwsh -NoProfile -Command '$c = New-PesterConfiguration; $c.Run.Path = "tests/scripts/vscode"; $c.Run.PassThru = $true; $c.Output.Verbosity = "Detailed"; $c.CodeCoverage.Enabled = $true; $c.CodeCoverage.Path = "scripts/vscode"; $c.CodeCoverage.OutputFormat = "JaCoCo"; $c.CodeCoverage.OutputPath = "coverage/pester-coverage.xml"; $r = Invoke-Pester -Configuration $c; "PESTER Passed=" + $r.PassedCount + " Failed=" + $r.FailedCount + " Skipped=" + $r.SkippedCount + " Total=" + $r.TotalCount; "PESTER COMMANDPERCENT=" + $r.CodeCoverage.CoveragePercent; "PESTER MODULEVERSION=" + (Get-Module Pester).Version.ToString()'`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/pester-coverage.2026-09-12T10-25.md`. Acceptance: the artifact records the four counts from the line beginning `PESTER Passed=`, the command figure from the line beginning `PESTER COMMANDPERCENT=`, and the module version from the line beginning `PESTER MODULEVERSION=`, which is the Pester version actually in use for this run and is the literal P7-T3 pins in the workflow file, so no version literal in this plan is executor-chosen; and the file coverage/pester-coverage.xml exists. This task deliberately does not assert a zero failure count: Pester does not exit non-zero on a failing test case, so the count is read from the printed line rather than from the exit code. After the run, record the pre-fix side effect by running `git -C . status --porcelain=v1 -- '*.csproj'` and recording its output verbatim. If the output is non-empty, restore those files with `git -C . checkout -- '*.csproj'`, record the restoration, and re-run the status command recording that it now prints nothing. This is the accidental execution the delivery removes; leaving its residue in the tree would falsify the project-file assertions in P3-T1 and P10-T14.
- [ ] [P0-T9] Observe and pin the JaCoCo document shape before any later task asserts over it. Read the file coverage/pester-coverage.xml written by P0-T8 and run `pwsh -NoProfile -Command '[xml]$j = Get-Content coverage/pester-coverage.xml -Raw; $n = @($j.report.counter) | Where-Object { $_.type -eq "LINE" }; "LINE covered=" + $n.covered + " missed=" + $n.missed'`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/jacoco-line-counter-shape.2026-09-12T10-25.md`. Acceptance: the artifact records the selector expression used, records that exactly one counter element matched, and records the two integer attribute values from the printed line beginning `LINE covered=`. If no element matches, the artifact records the actual root element name and the actual location of the report-level LINE counter, together with a corrected selector expression, and every later task that reads the LINE figure uses that corrected expression instead. The recorded selector is the single definition used by every later measurement task and by the workflow gate body. Additionally record the method-entry shape, by printing the `name` attribute of every method element beneath the class entry for the threshold part, and record the name the document uses for statements that sit outside any function. Acceptance addition: the artifact records the selector expression that reaches a named method entry, or, if no method element exists, records the actual per-function granularity the document offers together with the corrected expression that P6-T5 must use instead.
- [ ] [P0-T10] Compute and record the required uplift delta. Using the covered value C and the total T equal to covered plus missed from P0-T9, compute N equal to the ceiling of 0.80 times T, minus C. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/powershell-line-delta.2026-09-12T10-25.md` recording C, T, the baseline LINE percentage to two decimal places, the arithmetic, and N. Acceptance: the artifact states the formula in the form the ceiling of 0.80 times T minus C, shows the substituted integers, and states that the 2026-09-03 figures 535, 681 and 78.56 are cited as the prior basis only and are not used in the computation.
- [ ] [P0-T11] Record the baseline per-file LINE counters that the seam defect affects. From coverage/pester-coverage.xml, read the LINE counter of the class entry for the build script and of the class entry for the package-reference sync script. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/per-file-line-counters.2026-09-12T10-25.md`. Acceptance: the artifact records, for each of the two files, the covered and missed integers and the file name they were read against. These two rows are the comparison basis for the determinism proof in task P6-T6.
- [ ] [P0-T12] Capture the baseline PowerShell analyzer state. Invoke `mcp__drm-copilot__run_poshqc_analyze` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, and separately run `pwsh -NoProfile -Command 'Invoke-ScriptAnalyzer -Path scripts/vscode -Recurse; Invoke-ScriptAnalyzer -Path tests/scripts/vscode -Recurse'`. The paired direct run is load-bearing: the MCP payload carries a count only and no rule name, file path or line, so no per-diagnostic acceptance condition anywhere in this plan can be satisfied from it. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/poshqc-analyze.2026-09-12T10-25.md`. Acceptance: the artifact records the scan folders passed, the count from the MCP excerpt, and one row per diagnostic from the direct run carrying its rule name, file path and line. The artifact also records that the MCP call returns `ok` false whenever the count is non-zero, and that this is the expected baseline shape rather than a tool failure. A non-zero baseline count is recorded, not treated as a failure; later analyzer tasks are judged against this recorded per-diagnostic baseline, not against an absolute zero.
- [ ] [P0-T13] Capture the baseline PowerShell formatter state. Invoke `mcp__drm-copilot__run_poshqc_format` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode. Before the call, run `git -C . status --porcelain=v1 --untracked-files=all -- scripts/vscode tests/scripts/vscode`; after the call, run the identical command again. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/poshqc-format.2026-09-12T10-25.md`. Acceptance: the artifact records both porcelain outputs verbatim and states whether the formatter rewrote any file, derived from the difference between the two outputs rather than from the exit code, because a formatter exits the same way whether or not it rewrote files. If the formatter rewrote a file, the artifact names that file and the rewrite is committed in task P2-T8 with the batch it belongs to.
- [ ] [P0-T14] Capture the baseline workflow lint state. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/dev-tools/run-actionlint.ps1`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/actionlint.2026-09-12T10-25.md`. Acceptance: `EXIT_CODE: 0` and the `Output Summary:` records that actionlint produced no diagnostic lines for the seven workflow YAML files currently present in the workflows directory.
- [ ] [P0-T15] Prove the pre-change absence of the new symbols, so that later existence assertions are not vacuous. Using the Grep tool over the two directories scripts/vscode and tests/scripts/vscode, search for each of the eight identifiers Assert-CoberturaBranchCoverageThreshold, Invoke-VSBuildMain, Invoke-RestoreMain, Get-MSBuildPath, Invoke-SyncPackageReferences, Invoke-MSBuildExe, Get-RestoreMSBuildPath and Invoke-RestoreMSBuildExe. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/pre-change-symbol-absence.2026-09-12T10-25.md`. Acceptance: the artifact records a zero match count for all eight identifiers across both directories, and records that the same search over the feature folder returns non-zero, which confirms the search mechanism itself is working.
- [ ] [P0-T16] Confirm no stray coverage document sits at the repository root, because such a file is matched by neither the repository ignore file nor the formatter ignore file and the formatter processes xml. Using the Glob tool, list the repository-root entry named coverage.xml. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/root-coverage-document-check.2026-09-12T10-25.md`. Acceptance: the artifact records that no repository-root coverage.xml exists, and records the explicit output path coverage/pester-coverage.xml that every Pester invocation in this plan sets so that the Pester default is never relied on. If a root coverage.xml is found, delete it and record the deletion.
- [ ] [P0-T17] Capture the baseline C# formatter state, so the final formatter check in P10-T7 is judged against this tree rather than against an absolute expectation the delivery does not control. Run `pwsh -NoProfile -Command 'dotnet tool run csharpier check .'`, because the Bash allowlist grants no bare dotnet entry. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/baseline/csharpier-check.2026-09-12T10-25.md`. Acceptance: the artifact records `EXIT_CODE:`, the count of files the tool reported as checked, and the full list of any files the tool reported as unformatted. A non-zero baseline exit code is recorded, not treated as a failure of this task; P10-T7 is then judged against the recorded file list rather than against a zero exit code. This command is read-only and rewrites no file, so its exit code together with its printed file list is a sufficient observation.

---

### Phase 1 — Fail-before regression tests for the C# coverage-threshold gate

- [ ] [P1-T1] [expect-fail] Extend `tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1` with a new `Describe` block named `Assert-CoberturaBranchCoverageThreshold` holding exactly seven test cases, and add one further test case to the existing `Assert-CoberturaLineCoverageThreshold` block named `rejects a Cobertura line rate outside the closed unit interval`. The seven branch cases are named: `accepts a branch rate at exactly the floor`, `accepts a branch rate above the floor`, `rejects a branch rate below the floor`, `rejects a missing branch rate`, `rejects a non-numeric branch rate`, `rejects a branch rate outside the closed unit interval`, and `rejects a projection with zero valid branches`. Each case drives the function with a one-line document literal, matching the technique the existing cases in this file already use. The four non-threshold negative cases assert the exact messages `Cobertura branch-rate is missing.`, `Cobertura branch-rate must be numeric.`, `Cobertura branch-rate must be between 0 and 1.` and `Cobertura branch coverage has no valid branches.`; the below-floor case asserts a message matching the token `is below the required 75`. The file must stay under 500 lines. Run `pwsh -NoProfile -Command '$c = New-PesterConfiguration; $c.Run.Path = "tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1"; $c.Run.PassThru = $true; $c.Output.Verbosity = "Detailed"; $r = Invoke-Pester -Configuration $c; "PESTER Passed=" + $r.PassedCount + " Failed=" + $r.FailedCount + " Total=" + $r.TotalCount'` and write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/fail-before-branch-assertion.2026-09-12T10-25.md` with an explicit `Expected Result: RED` field. The artifact records `EXIT_CODE: 0` and states that the exit code is not the red signal, because Pester does not exit non-zero on a failing test case; the red signal is the recorded failed count. Acceptance: the recorded line beginning `PESTER Passed=` reports a failed count of exactly 7, and the failure text names the unresolved command Assert-CoberturaBranchCoverageThreshold. This is the fail-before evidence for the missing branch gate.
- [ ] [P1-T2] [expect-fail] Extend `tests/scripts/vscode/Invoke-MSTestWithCoverage.AssemblyDiscovery.Tests.ps1` with a new `Describe` block named `Invoke-MSTestWithCoverage threshold call-site wiring` holding exactly two test cases, named `invokes the branch assertion once with the post-processed document` and `invokes the line assertion before the branch assertion`. Both drive `Invoke-MSTestWithCoverageMain` with the mock set this file already establishes, mock both threshold functions so the invocation order and the received argument are captured into script-scope variables, and assert that each is invoked exactly once and that the captured argument is the same post-processed string that the mocked conversion returned. The new `Describe` is placed after the existing `Describe` block, at the end of the file, so that the line positions P2-T3 cites in this file are unchanged. Run the same single-file Pester command form as P1-T1 against `tests/scripts/vscode/Invoke-MSTestWithCoverage.AssemblyDiscovery.Tests.ps1` and write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/fail-before-call-site-wiring.2026-09-12T10-25.md` with an explicit `Expected Result: RED` field. Acceptance: the recorded line beginning `PESTER Passed=` reports a failed count of exactly 2, and the failure text names the unresolved command Assert-CoberturaBranchCoverageThreshold. This is the fail-before evidence for the unwired call site.

---

### Phase 2 — Production batch 1: the branch assertion and its call site

This batch changes two production PowerShell files, which is inside the three-file per-batch cap.

- [ ] [P2-T1] Add the function `Assert-CoberturaBranchCoverageThreshold` to `scripts/vscode/Invoke-MSTestWithCoverage.Threshold.ps1`, mirroring the existing line assertion in shape. It takes one mandatory string parameter named CoberturaXml, reads the `branch-rate` attribute of the document root coverage element, rejects a missing value, rejects a non-numeric value using an invariant-culture decimal parse, rejects a value outside the closed interval from 0 to 1, rejects a `branches-valid` attribute whose integer value is not greater than zero, and throws when the percentage is below 75. Its comment-based help states the precondition that the caller passes the post-processed document rather than raw collector output, and states that reaching the end of the function is the success signal. The four non-threshold messages are exactly `Cobertura branch-rate is missing.`, `Cobertura branch-rate must be numeric.`, `Cobertura branch-rate must be between 0 and 1.` and `Cobertura branch coverage has no valid branches.`, and the below-floor message contains the token `is below the required 75`. Acceptance: the file exists, contains the function, and its line count read by the Grep tool count mode is greater than 56 and at most 200.
- [ ] [P2-T2] Add exactly one statement to `scripts/vscode/Invoke-MSTestWithCoverage.ps1` calling `Assert-CoberturaBranchCoverageThreshold` with the post-processed document, placed on the line immediately after the existing call to the line assertion and immediately before the first-party report statement, inside the same function and after the write-back statement. Acceptance: the Grep tool reports exactly one occurrence of the identifier Assert-CoberturaBranchCoverageThreshold in this file, and the line number of that occurrence is exactly one greater than the line number of the single occurrence of the identifier Assert-CoberturaLineCoverageThreshold in the same file, and exactly one less than the line number of the single occurrence of the identifier Get-CoberturaFirstPartyCoverageReport in the same file.
- [ ] [P2-T3] Repair the mocked post-processed document in `tests/scripts/vscode/Invoke-MSTestWithCoverage.AssemblyDiscovery.Tests.ps1` so the new assertion receives a well-formed input. Substitute, at line 48, the literal `<coverage line-rate="0.8"><packages /></coverage>` with the literal `<coverage line-rate="0.8" branch-rate="0.8" branches-valid="10"><packages /></coverage>`. That single setup mock is the only source of the post-processed document for all three pre-existing discovery cases. Acceptance: running the single-file Pester command form against this file reports a failed count of 0 and a passed count of exactly 5, which is the three pre-existing discovery cases plus the two wiring cases added in P1-T2.
- [ ] [P2-T4] Repair the mocked post-processed document literals in `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`. Three test cases in that file drive the entry point through the assertion site with a mocked document that carries no branch rate, so the new assertion turns them red. Substitute, at lines 371 and 404 and in the expectation at line 406, the literal `<coverage line-rate="0.8"><packages /></coverage>` with the literal `<coverage line-rate="0.8" branch-rate="0.8" branches-valid="10"><packages /></coverage>`, in place and on the same three lines. Do not alter the literal at line 417: that case's mocked line rate of 0.5 makes the existing line assertion throw first, so its `Should -Throw` expectation is already satisfied and adding a passing rate there would invert it. The substitution is in place and adds no lines: the file is at 496 of its 500-line ceiling. Acceptance: running the single-file Pester command form against this file reports a failed count of 0, and the Grep tool count of the start-of-line pattern against this file reports exactly 496.
- [ ] [P2-T5] Run the batch-1 formatter step. Invoke `mcp__drm-copilot__run_poshqc_format` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, capturing `git -C . status --porcelain=v1 --untracked-files=all -- scripts/vscode tests/scripts/vscode` immediately before and immediately after. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch1-poshqc-format.2026-09-12T10-25.md`. Acceptance: the artifact records both porcelain outputs and states, from their difference rather than from the exit code, whether any file was rewritten. If a file was rewritten, this phase restarts at P2-T5.
- [ ] [P2-T6] Run the batch-1 analyzer step. Invoke `mcp__drm-copilot__run_poshqc_analyze` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, and separately run the same paired `Invoke-ScriptAnalyzer` command form P0-T12 states. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch1-poshqc-analyze.2026-09-12T10-25.md`. Acceptance: the reported diagnostic count is less than or equal to the count recorded in the P0-T12 baseline artifact, and every diagnostic naming a path inside the declared write set matches a row in the P0-T12 per-diagnostic baseline, and the artifact lists any that does not.
- [ ] [P2-T7] Run the batch-1 test step over the whole PowerShell suite. Invoke `mcp__drm-copilot__run_poshqc_test`, and separately run `pwsh -NoProfile -Command '$c = New-PesterConfiguration; $c.Run.Path = "tests/scripts/vscode"; $c.Run.PassThru = $true; $c.Output.Verbosity = "Detailed"; $r = Invoke-Pester -Configuration $c; "PESTER Passed=" + $r.PassedCount + " Failed=" + $r.FailedCount + " Skipped=" + $r.SkippedCount + " Total=" + $r.TotalCount'`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch1-poshqc-test.2026-09-12T10-25.md`. Acceptance: the line beginning `PESTER Passed=` reports a failed count of 0 and a total count exactly 10 greater than the total count recorded in the P0-T8 baseline artifact, which is the seven branch cases plus the out-of-range line case plus the two wiring cases, with no case removed. The exit code is not used as the pass signal, because Pester does not exit non-zero on a failing test case. After the run, record the pre-fix side effect by running `git -C . status --porcelain=v1 -- '*.csproj'` and recording its output verbatim. If the output is non-empty, restore those files with `git -C . checkout -- '*.csproj'`, record the restoration, and re-run the status command recording that it now prints nothing. This is the accidental execution the delivery removes; leaving its residue in the tree would falsify the project-file assertions in P3-T1 and P10-T14.
- [ ] [P2-T8] Commit batch 1. Run `git -C . add -- scripts/vscode tests/scripts/vscode docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869` and `git -C . commit -m "test(869): add C# branch coverage assertion and wire it at the existing assertion site"`. Add the session's required attribution trailer as a second `-m` argument. Acceptance: `git -C . diff --name-only plan-869-base..HEAD -- scripts/vscode tests/scripts/vscode` lists exactly these five paths, `scripts/vscode/Invoke-MSTestWithCoverage.Threshold.ps1`, `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, `tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1`, `tests/scripts/vscode/Invoke-MSTestWithCoverage.AssemblyDiscovery.Tests.ps1` and `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`, plus any file the P0-T13 formatter step rewrote and this task's commit therefore carries, and `git -C . status --porcelain=v1 --untracked-files=all -- scripts/vscode tests/scripts/vscode` prints nothing. Then close batch 1's change budget by running `pwsh -NoProfile -Command 'Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue | Remove-Item -Force; Write-Output "BATCH BUDGET RESET"'`, which is the reset the batch-budget hook's comment-based help prescribes at a batch boundary. Acceptance addition: the output carries the line `BATCH BUDGET RESET`, and a following `pwsh -NoProfile -Command '@(Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue).Count'` prints 0. The state directory carries a gitignore entry, but one budget file in it was committed before that entry existed and is therefore tracked, so this deletion does produce an unstaged deletion in the unscoped porcelain output. That path lies outside this task's own scoped assertions, which cover only the code roots, and P10-T14 restores it before its unscoped porcelain assertion runs.

---

### Phase 3 — Fail-before regression tests for the unguarded script bodies

- [ ] [P3-T1] [expect-fail] Rewrite `tests/scripts/vscode/Invoke-VSBuild.Tests.ps1` against the seams the next phase introduces. The file must carry no file-level setup block that dot-sources the script; each `Describe` owns its own setup. The first `Describe` is named `Invoke-VSBuild.ps1 entry-point guard` and holds exactly one test case named `defines functions only and performs no work on dot-source`. That case parses the production file with the PowerShell language parser, collects the top-level statements that are neither the parameter block nor a function definition, and asserts that there are exactly three of them: a strict-mode statement, an error-preference assignment, and one conditional statement whose condition text contains the token `InvocationName` and whose body contains exactly one command whose name is `Invoke-VSBuildMain`. The remaining `Describe` blocks retain, unchanged, the three pure-helper `Describe` blocks the current file already holds and all six of their test cases, each block gaining its own setup block that dot-sources the production file, because the file-level setup block is removed, and add a `Describe` named `Invoke-VSBuildMain` driving the extracted function with all three seams mocked across the seven scenarios listed in P4-T1, and add a further `Describe` named `Invoke-VSBuild.ps1 wrapper seams` holding exactly two cases that drive the bodies of the two wrapper seams directly, named `resolves the MSBuild path through the vswhere seam` and `forwards every argument array element as a separate positional argument`, each passing an in-process command name in place of the executable path, in the style of the splatting-seam case that `tests/scripts/vscode/Invoke-MSTest.Main.Tests.ps1` already holds at its line 36. The in-process command name is either a builtin whose parameters the seam's own argument list binds or a helper function defined inside that `Describe`'s setup block; it is not a mock of the seam, because the point of these two cases is to execute the seam bodies. No external process is launched. Run `pwsh -NoProfile -Command '$c = New-PesterConfiguration; $c.Run.Path = "tests/scripts/vscode/Invoke-VSBuild.Tests.ps1"; $c.Run.PassThru = $true; $c.Filter.FullName = "Invoke-VSBuild.ps1 entry-point guard*"; $c.Output.Verbosity = "Detailed"; $r = Invoke-Pester -Configuration $c; "PESTER Passed=" + $r.PassedCount + " Failed=" + $r.FailedCount + " Total=" + $r.TotalCount'` and write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/fail-before-vsbuild-guard.2026-09-12T10-25.md` with an explicit `Expected Result: RED` field. The name filter is load-bearing: it confines the fail-before run to the parser-only case so that no dot-source of the unguarded production file occurs and the package-reference sync script is not executed against the real repository root. Acceptance: the recorded line beginning `PESTER Passed=` reports a total count of 1 and a failed count of 1, and `git -C . status --porcelain=v1 --untracked-files=all -- '*.csproj'` prints nothing, proving the fail-before run wrote no project file.
- [ ] [P3-T2] [expect-fail] Create `tests/scripts/vscode/Invoke-Restore.Tests.ps1` with the same two-part shape. The first `Describe` is named `Invoke-Restore.ps1 entry-point guard` and holds exactly one test case named `defines functions only and performs no work on dot-source`, asserting by the same parser technique that the top-level statements that are neither the parameter block nor a function definition number exactly three and that the conditional statement's body contains exactly one command whose name is `Invoke-RestoreMain`. A second `Describe` named `Invoke-RestoreMain` drives the extracted function under mocked seams across the four scenarios listed in P4-T2. A third `Describe` named `Invoke-Restore.ps1 wrapper seams` holds exactly two cases that drive the bodies of the two wrapper seams directly, named `resolves the MSBuild path through the vswhere seam` and `forwards every argument array element as a separate positional argument`, each passing an in-process command name in place of the executable path, in the style of the splatting-seam case that `tests/scripts/vscode/Invoke-MSTest.Main.Tests.ps1` already holds at its line 36. The in-process command name is either a builtin whose parameters the seam's own argument list binds or a helper function defined inside that `Describe`'s setup block; it is not a mock of the seam, because the point of these two cases is to execute the seam bodies. No external process is launched. Run the same name-filtered Pester command form against this file, filtering on the guard `Describe` name, and write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/fail-before-restore-guard.2026-09-12T10-25.md` with an explicit `Expected Result: RED` field. Acceptance: the recorded line beginning `PESTER Passed=` reports a total count of 1 and a failed count of 1.

---

### Phase 4 — Production batch 2: the build and restore script seams

This batch changes two production PowerShell files, which is inside the three-file per-batch cap. It is a separate batch from Phase 2 because the four production PowerShell files in scope exceed the cap taken together.

- [ ] [P4-T1] Restructure `scripts/vscode/Invoke-VSBuild.ps1`. Move the current top-level body into a new function named `Invoke-VSBuildMain` taking the script's own parameters plus a ScriptRoot parameter defaulting to the script directory. Introduce three named wrapper seams in the same file: `Get-MSBuildPath` taking a VsWherePath parameter and returning the first vswhere match; `Invoke-SyncPackageReferences` taking a SyncScriptPath parameter and a SolutionRoot parameter; and `Invoke-MSBuildExe` taking an MSBuildPath parameter and a string-array parameter that is not named Args, splatted into the executable. The seam name `Invoke-SyncPackageReferences` ends in a plural noun and the plural-noun analyzer rule is active in this repository: the P0-T12 baseline already carries two diagnostics of that rule in this same file, so adding this seam without a suppression emits a third and raises the folder count above the baseline, which no batch analyzer acceptance in this plan can absorb. The function therefore carries a narrowly scoped suppression attribute for the rule named PSUseSingularNouns immediately above its param block, in the same attribute form the repository already uses for two exported functions under the .claude lib directory and for one hook script, with a justification naming the specification decision D3 that fixes the seam name. No other function this delivery adds carries a plural noun, so no other suppression is added. Add the invocation guard so the main function runs only when the script is not dot-sourced. Guard order, guard messages, and the argument list must be unchanged. The seven scenarios the rewritten test drives are: the missing-solution throw, the missing-vswhere throw, the unresolved-MSBuild throw, the branch where the sync script is absent, the branch where the sync script is present and the sync seam is invoked with the resolved repository root, the no-execute path asserting the MSBuild seam is invoked zero times, and the non-zero-exit throw. Each of the two seams `Get-MSBuildPath` and `Invoke-MSBuildExe` must have a body that is reachable with an in-process command name supplied as its path parameter, so that the two direct seam cases added in P3-T1 execute those bodies rather than a mock: neither seam may hard-code its executable, and neither may validate that its path parameter names a file on disk. Acceptance: the Grep tool finds all four identifiers `Invoke-VSBuildMain`, `Get-MSBuildPath`, `Invoke-SyncPackageReferences` and `Invoke-MSBuildExe` in this file, the file's line count read by the Grep tool count mode is greater than 167 and at most 300, and the Grep tool reports exactly one occurrence of the token PSUseSingularNouns in this file.
- [ ] [P4-T2] Restructure `scripts/vscode/Invoke-Restore.ps1` with the same pattern. Move the top-level body into a function named `Invoke-RestoreMain`, reach vswhere through a wrapper seam named `Get-RestoreMSBuildPath` and msbuild through a wrapper seam named `Invoke-RestoreMSBuildExe` whose array parameter is not named Args, and add the invocation guard. The two seam names deliberately differ from the build script's `Get-MSBuildPath` and `Invoke-MSBuildExe`: Pester runs every test container in one runspace, so two same-named functions dot-sourced from two production files shadow each other and the mock registered for one file silently governs the other. Guard order and messages must be unchanged. The four scenarios the new test drives are: the missing-solution throw, the missing-vswhere throw, the unresolved-MSBuild throw, and the non-zero-exit throw. Each of the two seams `Get-RestoreMSBuildPath` and `Invoke-RestoreMSBuildExe` must have a body that is reachable with an in-process command name supplied as its path parameter, so that the two direct seam cases added in P3-T2 execute those bodies rather than a mock: neither seam may hard-code its executable, and neither may validate that its path parameter names a file on disk. Acceptance: the Grep tool finds all three identifiers `Invoke-RestoreMain`, `Get-RestoreMSBuildPath` and `Invoke-RestoreMSBuildExe` in this file, finds none of them in `scripts/vscode/Invoke-VSBuild.ps1`, and the file's line count read by the Grep tool count mode is greater than 39 and at most 200.
- [ ] [P4-T3] Run the batch-2 formatter step. Invoke `mcp__drm-copilot__run_poshqc_format` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, capturing `git -C . status --porcelain=v1 --untracked-files=all -- scripts/vscode tests/scripts/vscode` immediately before and immediately after. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch2-poshqc-format.2026-09-12T10-25.md`. Acceptance: the artifact records both porcelain outputs and states, from their difference rather than from the exit code, whether any file was rewritten. If a file was rewritten, this phase restarts at P4-T3.
- [ ] [P4-T4] Run the batch-2 analyzer step. Invoke `mcp__drm-copilot__run_poshqc_analyze` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, and separately run the same paired `Invoke-ScriptAnalyzer` command form P0-T12 states. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch2-poshqc-analyze.2026-09-12T10-25.md`. Acceptance: the reported diagnostic count is less than or equal to the count recorded in the P0-T12 baseline artifact.
- [ ] [P4-T5] Run the batch-2 test step over the whole PowerShell suite, using the same two invocations and the same directory-scoped Pester command form as P2-T7. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch2-poshqc-test.2026-09-12T10-25.md`. Acceptance: the line beginning `PESTER Passed=` reports a failed count of 0; the artifact records the total count, which every later phase compares against; and the two guard cases named `defines functions only and performs no work on dot-source` both pass, which is the pass-after half of the fail-before evidence recorded in P3-T1 and P3-T2.
- [ ] [P4-T6] Commit batch 2. Run `git -C . add -- scripts/vscode tests/scripts/vscode docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869` and `git -C . commit -m "fix(869): guard the build and restore script bodies behind extracted main functions"`. Add the session's required attribution trailer as a second `-m` argument. Acceptance: `git -C . diff --name-only plan-869-base..HEAD -- scripts/vscode tests/scripts/vscode` additionally lists `scripts/vscode/Invoke-VSBuild.ps1`, `scripts/vscode/Invoke-Restore.ps1`, `tests/scripts/vscode/Invoke-VSBuild.Tests.ps1` and `tests/scripts/vscode/Invoke-Restore.Tests.ps1`, and `git -C . status --porcelain=v1 --untracked-files=all -- scripts/vscode tests/scripts/vscode` prints nothing. Then close batch 2's change budget by running `pwsh -NoProfile -Command 'Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue | Remove-Item -Force; Write-Output "BATCH BUDGET RESET"'`, which is the reset the batch-budget hook's comment-based help prescribes at a batch boundary. Acceptance addition: the output carries the line `BATCH BUDGET RESET`, and a following `pwsh -NoProfile -Command '@(Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue).Count'` prints 0. The state directory carries a gitignore entry, but one budget file in it was committed before that entry existed and is therefore tracked, so this deletion does produce an unstaged deletion in the unscoped porcelain output. That path lies outside this task's own scoped assertions, which cover only the code roots, and P10-T14 restores it before its unscoped porcelain assertion runs.

---

### Phase 5 — Coverage uplift batch 3

This batch changes three test files and no production file, which is inside the three-test-file per-batch cap. Targets are taken in descending estimated value, following the ranking in the research record.

- [ ] [P5-T1] Create `tests/scripts/vscode/TestProcessCleanup.Tests.ps1`, the largest single uplift target. It dot-sources scripts/vscode/TestProcessCleanup.ps1, which defines one function and one alias and has no entry-point body, and drives `Stop-RepoOwnedVSTestProcess` across exactly five test cases named `returns without action when no matching process exists`, `skips a vstest process whose command line does not name the repository root`, `walks the child process tree breadth first from a matching parent`, `returns without action when the process lookup yields nothing`, and `honours the confirmation guard when the operation is not confirmed`. All five mock the CIM query cmdlet, the process lookup cmdlet, the process stop cmdlet and the verbose writer, returning plain object shapes rather than live process objects. No case starts a process, sleeps, touches the network, or uses a temporary file. Any helper the cases share is defined inside the setup block, never at file scope. Acceptance: running the single-file Pester command form against this file reports a passed count of exactly 5 and a failed count of 0, and the file is under 500 lines.
- [ ] [P5-T2] Extend `tests/scripts/vscode/Invoke-MSTestWithCoverage.Merge.Tests.ps1` with exactly four new test cases covering the uncovered edge branches of the class-merge function, named `synthesises a primary class when every group member carries a compiler-generated name`, `creates a methods element when the primary class has none`, `removes a stale condition-coverage attribute when the winning line carries none`, and `replaces the condition elements from the winning line`. Each is driven by a here-string Cobertura fixture in the style the two existing cases in this file already use. No case creates a file. Acceptance: running the single-file Pester command form against this file reports a passed count of exactly 6 and a failed count of 0, and the file is under 500 lines.
- [ ] [P5-T3] Extend `tests/scripts/vscode/Install-RepoDotNetSdk.Tests.ps1` with exactly four new test cases covering the pure path helper and the two early-exit branches, named `resolves the default repo-local install directory from the script directory`, `resolves an explicitly supplied install directory to a full path`, `returns early when the SDK marker already exists and force is not supplied`, and `returns early at the confirmation guard when the operation is not confirmed`. The marker case mocks the path-test cmdlet to report the marker present and mocks the host writer, so it returns at the marker check. The confirmation case mocks the path-test cmdlet to report the marker absent and drives the function with the what-if switch, which is the switch that makes the ShouldProcess call return false so the function returns at the confirmation guard; the confirm-suppressing switch must not be used here, because it makes that call return true and lets control reach the download and archive-extraction region. Neither case reaches the download or archive-extraction region. No case writes a temporary-path literal, a network API, or an archive API, so the test-purity hook accepts the file. Acceptance: running the single-file Pester command form against this file reports a passed count of exactly 6 and a failed count of 0.
- [ ] [P5-T4] Run the batch-3 formatter step, identical in form to P4-T3. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch3-poshqc-format.2026-09-12T10-25.md`. Acceptance: the artifact records both porcelain outputs and states, from their difference rather than from the exit code, whether any file was rewritten. If a file was rewritten, this phase restarts at P5-T4.
- [ ] [P5-T5] Run the batch-3 analyzer step, identical in form to P4-T4: invoke `mcp__drm-copilot__run_poshqc_analyze` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, and separately run the same paired `Invoke-ScriptAnalyzer` command form P0-T12 states. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch3-poshqc-analyze.2026-09-12T10-25.md`. Acceptance: the reported diagnostic count is less than or equal to the count recorded in the P0-T12 baseline artifact.
- [ ] [P5-T6] Run the batch-3 test step over the whole PowerShell suite, identical in form to P4-T5. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch3-poshqc-test.2026-09-12T10-25.md`. Acceptance: the line beginning `PESTER Passed=` reports a failed count of 0 and a total count exactly 13 greater than the total count recorded in the P4-T5 artifact, which is the five process-cleanup cases plus the four merge cases plus the four SDK-install cases.
- [ ] [P5-T7] Commit batch 3. Run `git -C . add -- tests/scripts/vscode docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869` and `git -C . commit -m "test(869): raise scripts/vscode coverage on the process cleanup, merge and SDK install targets"`. Add the session's required attribution trailer as a second `-m` argument. Acceptance: `git -C . diff --name-only plan-869-base..HEAD -- tests/scripts/vscode` additionally lists `tests/scripts/vscode/TestProcessCleanup.Tests.ps1`, `tests/scripts/vscode/Invoke-MSTestWithCoverage.Merge.Tests.ps1` and `tests/scripts/vscode/Install-RepoDotNetSdk.Tests.ps1`, and `git -C . status --porcelain=v1 --untracked-files=all -- tests/scripts/vscode` prints nothing. Then close batch 3's change budget by running `pwsh -NoProfile -Command 'Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue | Remove-Item -Force; Write-Output "BATCH BUDGET RESET"'`, which is the reset the batch-budget hook's comment-based help prescribes at a batch boundary. Acceptance addition: the output carries the line `BATCH BUDGET RESET`, and a following `pwsh -NoProfile -Command '@(Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue).Count'` prints 0. The state directory carries a gitignore entry, but one budget file in it was committed before that entry existed and is therefore tracked, so this deletion does produce an unstaged deletion in the unscoped porcelain output. That path lies outside this task's own scoped assertions, which cover only the code roots, and P10-T14 restores it before its unscoped porcelain assertion runs.

---

### Phase 6 — Coverage uplift batch 4, measurement, contingency and HALT decision

- [ ] [P6-T1] Extend `tests/scripts/vscode/Invoke-MSTest.Main.Tests.ps1` with exactly one new test case named `resolves the vstest console path through the vswhere seam`, driving `Get-VsTestConsolePath` against a command that resolves in-process, passed as the vswhere path, in the style of the splatting-seam case this file already holds at its line 36. The command is either a builtin whose parameters the seam's own argument list binds or a helper function defined inside that `Describe`'s setup block; it is not a mock of the seam, because the point of this case is to execute the seam body. No external process is launched. Acceptance: running the single-file Pester command form against this file reports a failed count of 0 and a passed count of exactly 12, which is the 11 cases the file held before this task plus the one added here, and the file is under 500 lines.
- [ ] [P6-T2] Run the batch-4 formatter step, identical in form to P4-T3. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch4-poshqc-format.2026-09-12T10-25.md`. Acceptance: the artifact records both porcelain outputs and states, from their difference rather than from the exit code, whether any file was rewritten. If a file was rewritten, this phase restarts at P6-T2.
- [ ] [P6-T3] Run the batch-4 analyzer step, identical in form to P4-T4: invoke `mcp__drm-copilot__run_poshqc_analyze` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, and separately run the same paired `Invoke-ScriptAnalyzer` command form P0-T12 states. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch4-poshqc-analyze.2026-09-12T10-25.md`. Acceptance: the reported diagnostic count is less than or equal to the count recorded in the P0-T12 baseline artifact.
- [ ] [P6-T4] Measure the post-uplift PowerShell coverage. Run the same directory-scoped Pester command as P0-T8, then read the report-level LINE counter from coverage/pester-coverage.xml with the selector recorded in the P0-T9 artifact. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/powershell-coverage-postuplift.2026-09-12T10-25.md`. Acceptance: the artifact records the covered and missed integers, the derived LINE percentage to two decimal places labelled as the LINE figure, the value from the line beginning `PESTER COMMANDPERCENT=` labelled as the informational command figure, the four test counts, and the difference between the post-uplift covered count and the baseline covered count recorded in P0-T10, compared against the required delta N from that same artifact. No branch figure is recorded for PowerShell.
- [ ] [P6-T5] Measure the post-uplift coverage of new PowerShell code. From the same JaCoCo document, read the LINE counter of the method entries for `Assert-CoberturaBranchCoverageThreshold`, `Invoke-VSBuildMain`, `Get-MSBuildPath`, `Invoke-SyncPackageReferences`, `Invoke-MSBuildExe`, `Invoke-RestoreMain`, `Get-RestoreMSBuildPath` and `Invoke-RestoreMSBuildExe`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/powershell-new-code-coverage.2026-09-12T10-25.md`. Acceptance: the artifact lists each method name with its covered and missed integers, records the aggregate covered and aggregate total across all listed methods, and records the aggregate percentage. The aggregate percentage must be at least 90. The artifact additionally names, as a separate line, the guard-body invocation statement in each restructured file, which is uncovered by construction because it never runs while the file is dot-sourced, and states how many such statements are in the denominator. The selector used to reach a named method entry is the one recorded in the P0-T9 artifact; if that artifact recorded that the document offers no method element, this task uses the corrected per-function expression P0-T9 recorded instead, and states which of the two it used. Authorized branch: if the aggregate is below 90, the artifact records `Decision: NEW-CODE FLOOR NOT MET` and names every listed method whose covered count is zero together with the reason it is unreachable under the mocked scenarios. Before writing any test file under this branch, close batch 4's change budget by running `pwsh -NoProfile -Command 'Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue | Remove-Item -Force; Write-Output "BATCH BUDGET RESET"'` and record that the output carried the line `BATCH BUDGET RESET`; this reset is required because batch 4 already holds one test file from P6-T1 and this branch can write three more, which exceeds the three-test-file cap and would be denied. The executor then adds one further test case per such method, driving its body against a command that resolves in-process, which is either a builtin whose parameters the seam's own argument list binds or a helper function defined inside that `Describe`'s setup block. After the additions, re-run the P6-T2 formatter step and the P6-T3 analyzer step before re-measuring and re-recording, so batch 4 still carries a full toolchain pass over every file it wrote. If the aggregate remains below 90 after that, the artifact records the residual figure and the item halts under the same three prohibitions P6-T8 states, and no task after this one is performed.
- [ ] [P6-T6] Prove the measurement is deterministic. Read the per-file LINE counter of the package-reference sync script class entry from the JaCoCo document present on disk as this task begins, which is the document the most recent directory-scoped measurement preceding this task wrote, then run the same directory-scoped Pester command again, writing to the same explicit output path, and read the same counter from the document that run writes. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/coverage-determinism.2026-09-12T10-25.md`. Acceptance: the artifact names which measurement wrote the first document, either P6-T4 or the re-measurement performed under the P6-T5 authorized branch, records the covered and missed integers for that file from that document and from this task's own run, states that the two pairs are equal, and contrasts them with the pre-fix pair recorded in P0-T11. It also records that `git -C . status --porcelain=v1 --untracked-files=all -- '*.csproj'` prints nothing after both runs, proving no project file was written by either run.
- [ ] [P6-T7] Apply the tracked-fixture contingency, conditionally. If the LINE percentage recorded in P6-T4 is at or above 80, write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/other/fixture-contingency.2026-09-12T10-25.md` recording `Decision: NOT REQUIRED` together with the measured figure, and change no file. If it is below 80, create `tests/scripts/vscode/fixtures/sync-package-references/packages.config` and `tests/scripts/vscode/fixtures/sync-package-references/SyncFixture.Test.csproj` as tracked, read-only fixtures whose hint paths are either absent or already resolvable, so the sync script's fix count stays zero and its file-write call is never reached, and add one test case to `tests/scripts/vscode/Invoke-VSBuild.Tests.ps1` named `drives the package reference sync script down its zero-fix return path` that invokes the sync seam against the fixture directory. Before writing that test case, close batch 4's change budget with the same reset command and the same `BATCH BUDGET RESET` observation P6-T5's authorized branch states. After the fixture and its driving case are in place, re-run the P6-T2 formatter step and the P6-T3 analyzer step before the re-measurement, so batch 4 still carries a full toolchain pass over every file it wrote. In that case, re-run the directory-scoped Pester measurement of P6-T4 after the fixture and its driving case are in place, and the artifact records `Decision: TAKEN`, the re-measured covered, missed and LINE percentage values, the SHA-256 of both fixture files captured immediately before the suite runs and immediately after, and the statement that the two pairs are equal. Run `pwsh -NoProfile -Command 'dotnet tool run csharpier format tests/scripts/vscode/fixtures'` as part of this task, because CSharpier processes a packages.config file and the formatter ignore file excludes only project, properties and targets files, the evidence tree and the coverage artifact patterns. The artifact records that command and its exit code, and records the porcelain status of the fixture directory before and after the call so a rewrite is observable independently of the exit code. The project file carries the `.Test` suffix so the first-party project allowlist drops it and the C# coverage projection is unaffected. Acceptance: the artifact exists and records exactly one of the two decisions together with the measured figure that produced it, and when the decision is TAKEN it records two equal SHA-256 pairs.
- [ ] [P6-T8] Make the HALT decision. Read the LINE percentage from P6-T4, or from the re-measurement performed under P6-T7 when that contingency was taken. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/halt-decision.2026-09-12T10-25.md`. The artifact records `Verdict: CLEARED` when the figure is at or above 80, and `Verdict: HALT` when it is below. A HALT verdict additionally records the measured figure, the remaining gap in covered lines, every uplift target already taken with its measured gain, whether the fixture contingency was taken, and the reason the remaining uncovered regions cannot be closed without a production change beyond the agreed scope. This task forbids, and the artifact must restate the prohibition verbatim: lowering any floor is prohibited; excluding any production file from coverage measurement is prohibited; and merging the Pester job without its threshold assertion is prohibited. Authorized branch: when the verdict is HALT, no task after this one is performed, and the executor returns the artifact path and the HALT verdict to the caller. Acceptance: the artifact exists, records exactly one verdict, and records the measured figure that produced it.
- [ ] [P6-T9] Run the batch-4 test step over the whole PowerShell suite, identical in form to P4-T5. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/batch4-poshqc-test.2026-09-12T10-25.md`. Acceptance: the line beginning `PESTER Passed=` reports a failed count of 0 and a total count at least 1 greater than the total count recorded in the P5-T6 artifact.
- [ ] [P6-T10] Commit batch 4. Run `git -C . add -- tests/scripts/vscode docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869` and `git -C . commit -m "test(869): close the vstest console path resolver and record the post-uplift coverage basis"`. Add the session's required attribution trailer as a second `-m` argument. Acceptance: `git -C . diff --name-only plan-869-base..HEAD -- tests/scripts/vscode` additionally lists `tests/scripts/vscode/Invoke-MSTest.Main.Tests.ps1`, and `git -C . status --porcelain=v1 --untracked-files=all -- tests/scripts/vscode` prints nothing. Then close batch 4's change budget by running `pwsh -NoProfile -Command 'Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue | Remove-Item -Force; Write-Output "BATCH BUDGET RESET"'`, which is the reset the batch-budget hook's comment-based help prescribes at a batch boundary. This fourth reset is required because batch 4 can hold three distinct test files when the P6-T5 authorized branch fires on the two restructured-script test files, in which case the P8-T4 write of `tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1` would be the fourth and would be denied. The fixture files the P6-T7 contingency creates are not PowerShell and consume no slot. Acceptance addition: the output carries the line `BATCH BUDGET RESET`, and a following `pwsh -NoProfile -Command '@(Get-ChildItem -Path ".claude/state" -Filter "powershell-batch-budget.*.json" -ErrorAction SilentlyContinue).Count'` prints 0. The state directory carries a gitignore entry, but one budget file in it was committed before that entry existed and is therefore tracked, so this deletion does produce an unstaged deletion in the unscoped porcelain output. That path lies outside this task's own scoped assertions, which cover only the code roots, and P10-T14 restores it before its unscoped porcelain assertion runs.

---

### Phase 7 — CI workflow wiring

- [ ] [P7-T1] Rewrite the gate step of `.github/workflows/_mstest-coverage.yml`. Add a dotnet setup step mirroring the version the formatting callee already uses, and a step that installs dotnet-coverage globally at an explicitly pinned version and appends the user-profile dotnet tools directory to the job path file. Replace the inline vstest block with a single pwsh step that invokes `scripts/vscode/Invoke-MSTestWithCoverage.ps1` with the search root set to the repository root and the configuration set to Debug, followed by the repository's established exit-code propagation guard. Retain the checkout, msbuild setup, NuGet setup, package cache, restore and build steps unchanged. The workflow pins the install to the exact version string the P0-T5 artifact recorded, quoted verbatim in the workflow file; the executor invents no version literal, because neither tool in this delivery has an in-repo pin to inherit and a guessed literal would make this task unsatisfiable. The same pinned version is recorded in `.github/workflows/README.md` by P7-T6, so a future bump is a reviewable change. Acceptance: the file contains the literal `scripts/vscode/Invoke-MSTestWithCoverage.ps1`, contains no occurrence of the literal `vstest.console.exe`, contains the literal `dotnet-coverage`, the pinned dotnet-coverage version string appears in the file exactly once, and that version literal and the version literal recorded in the P0-T5 artifact are byte-identical.
- [ ] [P7-T2] Repoint the artifact upload step of `.github/workflows/_mstest-coverage.yml` at the Cobertura document, replacing the two test-result globs with the single path the script writes, and change the no-files behaviour from warn to error. Acceptance: the file contains the literal `if-no-files-found: error` exactly once in this callee, contains the upload path literal `path: coverage/coverage.cobertura.xml`, and contains no occurrence of the literal `if-no-files-found: warn`.
- [ ] [P7-T3] Create `.github/workflows/_pester.yml` as a reusable callee. Its workflow name is the bare gate name `pester`. Its trigger block declares both a workflow-call trigger and a workflow-dispatch trigger. It declares workflow-level read-only contents permission. It declares no concurrency block of its own. Its single job is named `Run Pester suite with coverage`, runs on `windows-latest`, and sets a 10-minute job timeout. Its steps are: checkout at depth 1; install a pinned Pester 5.x from the gallery for the current user and import that exact version; one pwsh gate step; and an artifact upload step. The workflow pins the install to the exact version string the P0-T8 artifact recorded on the line beginning `PESTER MODULEVERSION=`, quoted verbatim in the workflow file; the executor invents no version literal, because neither tool in this delivery has an in-repo pin to inherit and a guessed literal would make this task unsatisfiable. The same pinned version is recorded in `.github/workflows/README.md` by P7-T6, so a future bump is a reviewable change. Acceptance: the file exists; it contains the literals `workflow_call`, `workflow_dispatch`, `contents: read`, `windows-latest`, `timeout-minutes: 10` and `name: Run Pester suite with coverage`; it contains no occurrence of the literal `concurrency`; the pinned Pester version string appears in the file exactly twice, once in the install step and once in the import step, and that version literal and the version literal recorded in the P0-T8 artifact are byte-identical; and the upload step contains the literal `if-no-files-found: error` together with the upload path literal `path: coverage/pester-coverage.xml`.
- [ ] [P7-T4] Author the gate step body inside `.github/workflows/_pester.yml`. It builds a Pester configuration whose run path is the directory tests/scripts/vscode, whose pass-through option is enabled, whose code-coverage path is the directory scripts/vscode, whose coverage output format is JaCoCo, and whose coverage output path is the explicit file coverage/pester-coverage.xml rather than the Pester default. It then prints the passed, failed, skipped and total counts on one line, reads the report-level LINE counter from the written JaCoCo document using the selector recorded in the P0-T9 artifact, prints the measured LINE percentage and, separately labelled as informational, Pester's command percentage, and only then evaluates the gate: it exits 1 when the failed count is greater than zero, exits 1 when the measured LINE percentage is below 80, and otherwise exits 0. The explicit exit is placed after the count-emitting and figure-emitting statements, because the configuration's own exit option would terminate before the figures print and because a pwsh step exits zero unless the script calls exit. No branch figure is read, computed or printed. Every path-valued assignment in the gate step body is written with spaces around its equals sign and a single-quoted value, matching the coverage-output assignment form asserted below. Acceptance: the gate step body contains the literals `Run.Path = 'tests/scripts/vscode'`, `CodeCoverage.Path = 'scripts/vscode'`, `JaCoCo` and `exit 1`, and contains the coverage-output assignment written with spaces around its equals sign as `OutputPath = 'coverage/pester-coverage.xml'`; it contains no occurrence of the literals `Run.Exit`, `BRANCH` or `BranchPercent`; and the first `exit` statement in the step body appears on a later line than the statement that prints the measured LINE percentage.
- [ ] [P7-T5] Add the caller job to `.github/workflows/ci.yml`, keyed `pester`, named `pester`, and referencing the new callee by relative path, matching the shape of the five existing caller jobs, which is the leading-dot relative form the caller convention requires. Acceptance: the file contains the caller key and value written as the literal `uses: ./.github/workflows/_pester.yml`, which names `.github/workflows/_pester.yml` in the caller's required relative spelling, the job key `pester` appears exactly once, and the file still contains no inline steps block.
- [ ] [P7-T6] Update `.github/workflows/README.md`. Change the gate table row for the MSTest callee to describe the script-driven route and the Cobertura artifact; add a table row for the new Pester callee with its runner, gate description and timeout; amend the byte-identical claim so it no longer asserts that the vstest invocation was moved and not edited, and records instead that this delivery deliberately replaced it and thereby changed the gate's pass criterion; record both pinned tool versions; and extend the verbatim required-context list with the new context string `pester / Run Pester suite with coverage`, marked as predicted until the live capture in P8-T5 confirms it. Acceptance: the file contains the literal `pester / Run Pester suite with coverage`, contains the two pinned version strings that P7-T1 and P7-T3 wrote, and a search of that file for the single-line literal `were moved, not edited` returns zero matches, where the same search returned exactly one match before this task ran, at line 49 of that file; both counts are recorded in the P7-T7 artifact.
- [ ] [P7-T7] Lint every changed workflow file. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/dev-tools/run-actionlint.ps1`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/actionlint-postchange.2026-09-12T10-25.md`. Acceptance: `EXIT_CODE: 0`, and the `Output Summary:` states that actionlint produced no diagnostic lines and names all four changed workflow paths `.github/workflows/_mstest-coverage.yml`, `.github/workflows/_pester.yml`, `.github/workflows/ci.yml` and `.github/workflows/README.md`, noting that the markdown file is not linted by actionlint and is verified by the content assertions in P7-T6 instead. The artifact additionally records the before count of 1 and the after count of 0 for the single-line literal `were moved, not edited` in `.github/workflows/README.md`, which is the observation that makes the P7-T6 removal falsifiable.
- [ ] [P7-T8] Commit the workflow batch. Run `git -C . add -- .github/workflows docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869` and `git -C . commit -m "ci(869): enforce the C# coverage floors and add the Pester gate"`. Add the session's required attribution trailer as a second `-m` argument. Acceptance: `git -C . diff --name-only plan-869-base..HEAD -- .github/workflows` lists exactly `.github/workflows/README.md`, `.github/workflows/_mstest-coverage.yml`, `.github/workflows/_pester.yml` and `.github/workflows/ci.yml`, and `git -C . status --porcelain=v1 --untracked-files=all -- .github/workflows` prints nothing.

---

### Phase 8 — Negative-path proofs and context capture

- [ ] [P8-T1] Prove the C# branch gate fails closed on a below-floor projection. Run a pwsh command that dot-sources scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1, calls `Assert-CoberturaBranchCoverageThreshold` with a one-line document whose branch rate is below the floor and whose valid-branch count is positive, and lets the terminating error propagate. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/csharp-branch-gate-negative.2026-09-12T10-25.md` with `ExpectedExitCode: 1`. Acceptance: the recorded exit code is 1, the recorded error text contains the token `is below the required 75`, and the artifact additionally quotes the exit-code propagation guard line from `.github/workflows/_mstest-coverage.yml` that carries that non-zero exit into the job result.
- [ ] [P8-T2] Prove the C# gate fails closed when its input has nothing to measure. Run a pwsh command that calls `Assert-CoberturaBranchCoverageThreshold` with a one-line document whose valid-branch count is zero and whose branch rate is above the floor, and lets the terminating error propagate. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/csharp-zero-branch-guard.2026-09-12T10-25.md` with `ExpectedExitCode: 1`. Acceptance: the recorded exit code is 1, the recorded error text is exactly `Cobertura branch coverage has no valid branches.`, and the artifact quotes the `if-no-files-found: error` setting from both upload steps as the complementary proof that an absent coverage document also fails the job rather than warning.
- [ ] [P8-T3] Prove the Pester gate goes red when the suite covers less of scripts/vscode. Run the gate body exactly as `.github/workflows/_pester.yml` runs it, with one change: the run path is set to the single file tests/scripts/vscode/Invoke-MSTestWithCoverage.PackageRate.Tests.ps1 while the coverage path stays the whole directory scripts/vscode. This reduces the covered set to one small script's worth of lines and is deterministic, whereas deleting one arbitrary test file crosses the floor only if the final margin happens to be smaller than that file's contribution. The tree is not modified. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/pester-gate-negative-coverage.2026-09-12T10-25.md` with `ExpectedExitCode: 1`. Acceptance: the recorded exit code is 1, the recorded LINE percentage is below 80, the recorded failed count is 0 so that the non-zero exit is attributable to the coverage floor and not to a test failure, and `git -C . status --porcelain=v1 --untracked-files=all -- tests/scripts/vscode scripts/vscode` prints nothing. The artifact additionally records the tolerance margin computed from the P6-T4 figures as the covered count minus the ceiling of 0.80 times the total, which is the number of covered lines the suite may lose before the gate turns red.
- [ ] [P8-T4] Prove the Pester gate goes red on a failing test case. With the tree committed and clean, append one deliberately failing test case to `tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1`, run the gate body exactly as `.github/workflows/_pester.yml` runs it, then restore the file with `git -C . checkout -- tests/scripts/vscode/Invoke-MSTestWithCoverage.Threshold.Tests.ps1`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/regression-testing/pester-gate-negative-failure.2026-09-12T10-25.md` with `ExpectedExitCode: 1`. Acceptance: the recorded exit code is 1, the recorded failed count is 1, and `git -C . status --porcelain=v1 --untracked-files=all -- tests/scripts/vscode` prints nothing after the restore. This proves the explicit exit statement is reached and is not masked by the configuration default under which a pwsh step would otherwise exit zero.
- [ ] [P8-T5] Capture the actual check-run context names. Run the check-runs query that `.github/workflows/README.md` prescribes against the pull request head SHA, reading the names field. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/other/check-run-contexts.2026-09-12T10-25.md`. Acceptance: the artifact records the exact command, the head SHA it was run against, and the full list of returned names, and states whether the returned list contains the string the plan predicted. Authorized branch: if no pull request run against the head SHA exists at execution time, the artifact instead records `Status: PENDING LIVE RUN`, the exact command to run once the run completes, and the predicted string labelled as predicted and not confirmed; in that case the corresponding acceptance criterion stays unchecked with that reason recorded.
- [ ] [P8-T6] Record the accepted behaviour changes that arrive with adopting the local script in CI. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/other/accepted-behaviour-changes.2026-09-12T10-25.md`. Acceptance: the artifact records that the runsettings file the script passes declares class-level test scope with one worker per core, that this is accepted for the first run and that any new failure is treated as a finding rather than a flake, and that the trx logger is lost because the script's argument builder does not pass it, with the remedy being the upload repointing performed in P7-T2. It quotes the two runsettings element values it relies on from the file scripts/vscode/TaskMaster.cli.runsettings.
- [ ] [P8-T7] Record the absence of any coverage exclusion and of any PowerShell branch figure. Using the Grep tool, search the four changed workflow files and the four changed production PowerShell files for the tokens `ExcludeFromCodeCoverage`, `CodeCoverage.ExcludeTests`, `BranchPercent` and `BRANCH`, and read the repository coverage settings file without modifying it to confirm it holds no entry matching a production source path. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/other/no-exclusions-no-branch-figure.2026-09-12T10-25.md`. Acceptance: the artifact records a zero match count for all four tokens across the eight files searched, and records the full exclude list read from the coverage settings file together with the statement that every entry names a third-party module and none names a first-party production source path. The artifact also records that the C# branch assertion is deliberately the only branch assertion in the delivery and that no PowerShell branch assertion, threshold or reported figure exists. The searches are case-sensitive, which is the Grep tool's default; the artifact records that the uppercase token `BRANCH` is searched deliberately, because the lowercase Cobertura attribute names that the C# branch assertion legitimately reads must not be counted as a PowerShell branch figure.

---

### Phase 9 — Documentation and acceptance-criteria check-off

Throughout this plan, AC-NN denotes the NNth checkbox in the `## Acceptance Criteria` section of the specification, counted from the top. The section holds 31 checkboxes and no other checkbox appears in it.

- [ ] [P9-T1] Update `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/spec.md` to record the measured Phase 0 basis alongside the prior basis, the required delta N, and the HALT verdict. Acceptance: the specification's Repro and Evidence section carries a row labelled as the 2026-09-12 re-measurement with the covered, missed, total and percentage values from the P0-T9 and P0-T10 artifacts, and the prior-basis table remains, labelled as the prior basis.
- [ ] [P9-T2] Update `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/issue.md` with the delivered outcome and the superseded context count. Acceptance: the file records that the delivery adds exactly one new required check-run context and that the issue's original expectation of two is superseded, with the reason that the C# assertion changes no job name.
- [ ] [P9-T3] Update `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/user-story.md` to describe the two enforced gates from the maintainer's point of view. Acceptance: the file names both gates and states the three enforced figures 80 for C# line, 75 for C# branch and 80 for PowerShell line.
- [ ] [P9-T4] Update `docs/features/potential/promoted/2026-09-11-ci-coverage-threshold-and-pester-gates.md` to record the delivery outcome and link the plan. Acceptance: the file records the issue number 869 and the plan path `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/plan.2026-09-12T10-25.md`.
- [ ] [P9-T5] Mark acceptance criterion AC-01 complete in `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/spec.md`, citing the P7-T1 assertions and the P0-T7 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T6] Mark acceptance criterion AC-02 complete, citing the P7-T1 assertions and the P0-T7 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T7] Mark acceptance criterion AC-03 complete, citing the P2-T1 assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T8] Mark acceptance criterion AC-04 complete, citing the P2-T2 line-ordering assertion and the two wiring cases from P1-T2 and P2-T3. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T9] Mark acceptance criterion AC-05 complete, citing the seven branch cases from P1-T1 and their pass counts in P2-T7. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T10] Mark acceptance criterion AC-06 complete, citing the P8-T1 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T11] Mark acceptance criterion AC-07 complete, citing the P8-T2 artifact and the P7-T2 upload assertion. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T12] Mark acceptance criterion AC-08 complete, citing the P7-T3 assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T13] Mark acceptance criterion AC-09 complete, citing the P7-T5 assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T14] Mark acceptance criterion AC-10 complete, citing the P7-T4 and P7-T3 assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T15] Mark acceptance criterion AC-11 complete, citing the P7-T4 assertions and the P8-T3 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T16] Mark acceptance criterion AC-12 complete, citing the P7-T4 exit-ordering assertion and the P8-T4 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T17] Mark acceptance criterion AC-13 complete, citing the P4-T1 assertions and the P3-T1 scenario list. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T18] Mark acceptance criterion AC-14 complete, citing the P3-T1 fail-before artifact and the P4-T5 pass-after assertion. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T19] Mark acceptance criterion AC-15 complete, citing the P6-T6 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T20] Mark acceptance criterion AC-16 complete, citing the P4-T2 assertions and the P3-T2 fail-before artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T21] Mark acceptance criterion AC-17 complete, citing the P0-T9 and P0-T10 artifacts. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T22] Mark acceptance criterion AC-18 complete, citing the P6-T4 artifact as the measured basis, and recording that P10-T4 re-asserts the same floor on the final tree. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T23] Mark acceptance criterion AC-19 complete, citing the P6-T8 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T24] Mark acceptance criterion AC-20 complete, citing the P8-T7 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T25] Mark acceptance criterion AC-21 complete, citing the P8-T7 artifact and the P7-T4 token assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T26] Mark acceptance criterion AC-22 complete, citing the P7-T6 assertions and the P9-T2 issue update. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T27] Mark acceptance criterion AC-23 complete or record it unchecked with the pending reason, citing the P8-T5 artifact. Acceptance: exactly one criterion checkbox is evaluated in this task, and when it stays unchecked the criterion line is left byte-identical, and the pending reason is recorded in the P8-T5 artifact and in this plan's checklist entry for this task, not in the specification's criterion text.
- [ ] [P9-T28] Mark acceptance criterion AC-24 complete, citing the P7-T6 assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T29] Mark acceptance criterion AC-25 complete, citing the P8-T6 artifact and the P7-T2 assertions. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T30] Mark acceptance criterion AC-26 complete, citing the specification's own settled-decision D10 and the P8-T7 confirmation that no rules file changed. Acceptance: exactly one criterion checkbox changes state in this task, and `git -C . diff --name-only plan-869-base..HEAD -- .claude/rules` prints nothing.
- [ ] [P9-T31] Mark acceptance criterion AC-27 complete, citing the four per-batch toolchain artifact sets from Phases 2, 4, 5 and 6. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T32] Mark acceptance criterion AC-28 complete, citing the P6-T5 artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P9-T33] Mark acceptance criterion AC-29 complete, citing the P0-T2 environment-discipline record, the fact that the purity hook accepted every test write, and the P6-T7 fixture decision. Acceptance: exactly one criterion checkbox changes state in this task.

Acceptance criteria AC-30 and AC-31 are not checked off in this phase. Their evidence is written by Phase 10, and AC-31 is additionally unsatisfiable before the Phase 10 formatter runs, because that formatter can still change a file's line count. They are checked off in P10-T11 and P10-T12.

---

### Phase 10 — Final QC loop

The loop is format, then lint, then type check where applicable, then test. If any step fails or rewrites a file, the loop restarts at P10-T1. No step in this phase may be recorded as skipped.

- [ ] [P10-T1] Final PowerShell formatter step. Invoke `mcp__drm-copilot__run_poshqc_format` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, capturing `git -C . status --porcelain=v1 --untracked-files=all -- scripts/vscode tests/scripts/vscode` immediately before and immediately after. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-poshqc-format.2026-09-12T10-25.md`. Acceptance: the two porcelain outputs recorded in the artifact are identical, which is the observation that proves no file was rewritten; the exit code alone is not sufficient because a formatter exits the same way in both cases. If they differ, restart the loop at P10-T1.
- [ ] [P10-T2] Final PowerShell analyzer step. Invoke `mcp__drm-copilot__run_poshqc_analyze` with the scan folders set explicitly to scripts/vscode and tests/scripts/vscode, and separately run the same paired `Invoke-ScriptAnalyzer` command form P0-T12 states, because the per-diagnostic comparison below cannot be read from the MCP payload. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-poshqc-analyze.2026-09-12T10-25.md`. Acceptance: the reported diagnostic count is less than or equal to the count recorded in the P0-T12 baseline artifact, and every diagnostic naming a path inside the declared write set is byte-identical, by rule name and file, to a row already present in the P0-T12 per-diagnostic baseline. The baseline carries four such rows, three in `scripts/vscode/Invoke-VSBuild.ps1` and one in `scripts/vscode/Invoke-Restore.ps1`; removing a pre-existing diagnostic is permitted, introducing a new one is not.
- [ ] [P10-T3] Record that PowerShell has no type-check stage, per the PowerShell rules file, and proceed to testing. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-typecheck-not-applicable.2026-09-12T10-25.md`. Acceptance: the artifact states that type checking is not applicable to PowerShell and cites the rules file section that says so, and states that no C# source file, properties file or targets file is in the declared write set, and that the only project file the write set can contain is the contingency fixture named by P6-T7, which is not a member of the solution and therefore cannot change the analyzer or nullable build result, so those two gates cannot change their result and are not re-run.
- [ ] [P10-T4] Final PowerShell test step with coverage. Invoke `mcp__drm-copilot__run_poshqc_test`, and separately run the directory-scoped Pester command with coverage as in P0-T8, then read the report-level LINE counter with the selector recorded in P0-T9. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-pester-coverage.2026-09-12T10-25.md`. Acceptance: the line beginning `PESTER Passed=` reports a failed count of 0, and the artifact records the skipped count without requiring it to be zero, because a pre-existing skipped case is outside this delivery's scope; the artifact records the LINE covered, missed and percentage values labelled as the LINE figure, and the value from the line beginning `PESTER COMMANDPERCENT=` labelled as the informational command figure; and the LINE percentage is at or above 80. No branch figure is recorded.
- [ ] [P10-T5] Final workflow lint. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/dev-tools/run-actionlint.ps1`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-actionlint.2026-09-12T10-25.md`. Acceptance: `EXIT_CODE: 0` and the `Output Summary:` states that actionlint produced no diagnostic lines across the eight workflow YAML files now present, which is the seven recorded in the P0-T14 baseline plus `.github/workflows/_pester.yml`.
- [ ] [P10-T6] Confirm no stray repository-root coverage document was left behind by any Pester run, because such a file is matched by neither the repository ignore file nor the formatter ignore file and the formatter processes xml. Using the Glob tool, list the repository-root entry named coverage.xml. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-root-coverage-document-check.2026-09-12T10-25.md`. Acceptance: the artifact records that no repository-root coverage.xml exists. If one is found, delete it and restart the loop at P10-T1.
- [ ] [P10-T7] Final C# formatter verification. Run `pwsh -NoProfile -Command 'dotnet tool run csharpier check .'`, because the Bash allowlist grants no bare dotnet entry. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-csharpier-check.2026-09-12T10-25.md`. Acceptance: the `Output Summary:` records the count of files the tool reported as checked and the full list of any files it reported as unformatted, and that list is identical to the list recorded in the P0-T17 baseline artifact, so the delivery introduced no new formatting difference. This step is read-only and rewrites no file, so its exit code together with its printed file list is a sufficient observation. If the list gained an entry, fix that file and restart the loop at P10-T1.
- [ ] [P10-T8] Final C# coverage route, proving both assertions pass on the changed tree. Run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-csharp-coverage-route.2026-09-12T10-25.md`. Acceptance: `EXIT_CODE: 0`, and the `Output Summary:` reproduces verbatim the single printed line that begins `First-party coverage:` and carries both the line counts with their percentage and the branch counts with their percentage, records the three document-root attribute values `line-rate`, `branch-rate` and `branches-valid` from the file the run wrote, and states that reaching a zero exit code proves both the line assertion and the branch assertion returned without throwing. Authorized halt branch: the same timeout branch stated in P0-T7 applies, so if vstest does not terminate within the executor's tool timeout, terminate it, record `EXIT_CODE: 124` together with a `TimeoutObserved: true` line and the names of the test classes that were executing, report the item BLOCKED to the caller, and perform no further task in this plan.
- [ ] [P10-T9] Record the coverage comparison. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/coverage-comparison.2026-09-12T10-25.md`. Acceptance: the artifact records, for PowerShell, the baseline LINE covered, missed and percentage from P0-T9, the final values from P10-T4, and the difference; for new PowerShell code, the aggregate percentage from P6-T5 against the 90 floor; and for C#, the baseline first-party line and branch figures from P0-T7 and the final figures from P10-T8, with the statement that neither figure regressed.
- [ ] [P10-T10] Audit file sizes after the final format pass. Using the Grep tool in count mode over the start-of-line pattern against the directories scripts/vscode, tests/scripts/vscode and .github/workflows, record every file's line count. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/file-size-audit.2026-09-12T10-25.md`. Acceptance: the artifact lists a count for every file in those three directories, every count is at most 500, and the artifact explicitly names the count for `tests/scripts/vscode/Invoke-MSTest.RunSettings.Tests.ps1`, which must be at most 500 and was 496 before the change.
- [ ] [P10-T11] Mark acceptance criterion AC-30 complete, citing the P7-T7 artifact, the P10-T5 final actionlint artifact, and the P10-T1, P10-T2, P10-T3 and P10-T4 clean-pass artifacts. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P10-T12] Mark acceptance criterion AC-31 complete, citing the P10-T10 file-size audit artifact. Acceptance: exactly one criterion checkbox changes state in this task.
- [ ] [P10-T13] Commit the documentation and evidence. Run `git -C . add -- docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869 docs/features/potential/promoted/2026-09-11-ci-coverage-threshold-and-pester-gates.md` and `git -C . commit -m "docs(869): record the coverage gate evidence and check off the acceptance criteria"`. Add the session's required attribution trailer as a second `-m` argument. Acceptance: `git -C . status --porcelain=v1 --untracked-files=all -- .github scripts tests docs` prints nothing.
- [ ] [P10-T14] Verify the final diff equals the declared write set. First restore the one tracked file that the five batch-budget resets delete, by running `git -C . checkout -- .claude/state/powershell-batch-budget.default.json`, so that the unscoped porcelain output below is not made non-empty by a path this plan resets rather than delivers; the restore is safe because every PowerShell file write in this plan has already completed and no later task consults the budget. Then run `git -C . diff --name-status plan-869-base..HEAD -- .github scripts tests docs` and `git -C . status --porcelain=v1 --untracked-files=all`. Write `docs/features/active/2026-09-11-ci-coverage-threshold-and-pester-gates-869/evidence/qa-gates/final-write-set-verification.2026-09-12T10-25.md`. Acceptance: every path in the name-status output appears in the declared write set of this plan, the porcelain output names no path outside the feature folder and the agent-memory directory, and the artifact states explicitly that no path under .claude/rules appears in the diff, that no C# source file, properties file or targets file appears in the diff, and that the only project file that can appear is the contingency fixture named by P6-T7, which is not a member of the solution and therefore cannot change the analyzer or nullable build result.
