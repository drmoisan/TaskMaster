# Remediation Plan — Issue #644, Cycle 2 (CR-6 + CR-2, class-scoped sweep)

- **Issue:** #644
- **Branch:** `bug/qfc-unregister-navigation-count-mismatch-orphan-644`
- **Head at cycle entry:** `85a1939f92f64ebada4e71d19cc095dc2e8e8a26`
- **Source of scope:** `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/remediation-inputs.2026-08-30T02-08.md`, citing `code-review.2026-08-30T01-46.md` findings CR-6 (new) and CR-2 (stands).
- **Entry condition:** the cycle-1 exit reaudit returned zero blocking findings, so no cycle is mandated. This cycle is opened by orchestrator election for consistency and closure: the reaudit's first class-scoped sweep for the CR-1 defect class found two further instances of it in the identical file that cycle 1 corrected, and shipping them would make cycle 1's premise arbitrary.
- **Evidence timestamp for every artifact this plan produces:** `2026-08-30T02-08`.
- **Evidence location (non-overridable):** every artifact is written under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/` in a subfolder named for the artifact kind (`remediation-baseline/`, `other/`, `qa-gates/`), per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Writing evidence under `artifacts/` in any form is a policy violation. No delegation instruction in this cycle named a non-canonical evidence path, so there is no override to record.
- **Evidence schema:** every artifact named below carries `Timestamp:`, `Command:` (or, for a read-only document review, a statement that no shell command was executed), `EXIT_CODE:`, and `Output Summary:`, in addition to the specific fields each task's acceptance names.

## Hard scope limits (restated for a fresh executor)

- **Comment and string-literal text only.** No assertion, no `.Should()` chain, no test name, no attribute, and no executable line changes anywhere in this cycle.
- **The second assertion message at line 222** (`UnregisterNavigation_AfterRegisteringAtOneDigitAndGrowingToTen_RemovesTheOneDigitKeys`) **and the `<summary>` block at lines 189-196** (the same test) were corrected in cycle 1 and are correct. They are not touched, and `[P0-T2]`/`[P3-T1]` gate that they are unchanged.
- **The second assertion message at lines 183-186** of the two-digit test (`"issue #644 replaced the count-bounded removal loop with a ledger that replays the recorded registration set verbatim, so no key survives unregistration"`) already correctly describes #644 and is not touched. `[P0-T2]`/`[P3-T1]` gate that it is unchanged.
- **The second paragraph of the two-digit test's `<summary>` block, lines 147-151,** already describes #644 correctly and is out of scope; only line 145 in the first paragraph is corrected.
- Each replacement is kept close in length to the text it replaces so CSharpier's line-breaking decision for the enclosing construct is unchanged. Both replacement literals and their measured character counts are quoted verbatim in `[P1-T1]` and `[P1-T2]` below.
- The file is 226 lines with 3 `[TestMethod]` attributes and must remain so.
- **AC-16 is not re-opened.** No coverage comparison, no re-run of the repository-wide Cobertura post-processor, and no edit to `spec.md` anywhere in this plan.
- **No acceptance criterion changes state in this cycle.** Re-derived at cycle entry against `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/spec.md`: `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[x\]').Count` is `21`, `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[ \]').Count` is `5`, and `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '\[[ xX]\]').Count` is `26` — the two figures sum to the total, so no indented or upper-case-`X` checkbox is missed by the first two patterns. Both figures (`21`, `5`) must be identical at cycle exit; `[P3-T1]` re-measures them.
- **No production file is touched.** `QuickFiler/Controllers/QfcCollectionController.cs` and every other production path are out of scope.
- **`plan.2026-08-29T07-42.md`** (the approved plan of record), **`remediation-plan.2026-08-29T23-23.md`** (the cycle-1 remediation plan), and **every prior audit or evidence artifact** — including `code-review.2026-08-29T23-06.md`, `feature-audit.2026-08-29T23-06.md`, `policy-audit.2026-08-29T23-06.md`, `code-review.2026-08-30T01-46.md`, `feature-audit.2026-08-30T01-46.md`, `policy-audit.2026-08-30T01-46.md`, `remediation-inputs.2026-08-29T23-23.md`, `remediation-inputs.2026-08-30T02-08.md`, `issue.md`, and every existing file under `evidence/` — are not modified by this cycle. This is enforced structurally: `[P3-T1]`'s containment check requires the anchored name-status diff, scoped to `QuickFiler.Test` and this feature folder (the only two pathspecs this cycle's commit stages), to show exactly one `M` line — every file in this paragraph lives under one of those two pathspecs, so an edit to any of them would be caught there without needing a per-file check. That mechanism covers tracked files only. The four prior artifacts that are untracked at cycle entry — `code-review.2026-08-30T01-46.md`, `feature-audit.2026-08-30T01-46.md`, `policy-audit.2026-08-30T01-46.md`, and `remediation-inputs.2026-08-30T02-08.md` — cannot appear in an anchored name-status diff at all, so their invariance is enforced separately by the SHA-256 capture in `[P0-T4]` and its re-check in `[P3-T1]` clause 8.
- **Three untracked reaudit artifacts already exist in the feature folder at cycle entry** — `policy-audit.2026-08-30T01-46.md`, `code-review.2026-08-30T01-46.md`, and `feature-audit.2026-08-30T01-46.md` — plus this cycle's own `remediation-inputs.2026-08-30T02-08.md` and this plan file itself. Four of the five — the three `2026-08-30T01-46` reaudit artifacts and `remediation-inputs.2026-08-30T02-08.md` — are not edited by any task below and are carried into the final commit unmodified, which is what `[P0-T4]`'s and `[P3-T1]`'s four-file SHA-256 hash check verifies. The fifth, this plan file, is the one artifact in this set that is edited: every task check-off flips a `- [ ]` to `- [x]` in this file, and `[P3-T3]` explicitly flips its own checkbox before staging so the commit captures the fully checked-off plan.
- **Divergence from the cycle input's stated exit figure.** The cycle input's exit condition names two surviving sweep hits. That figure assumed line 145 would be reworded out of the pattern's match set. This plan declines that rewording as gate evasion (see `[P1-T2]`) and instead corrects line 145 in place to a past-tense attributed form that remains in the match set as a third legitimate hit. The correct post-edit figure is therefore three: `QfcCollectionControllerNavigationLedgerTests.cs` line 305, `QfcCollectionController.cs` line 2372, and `QfcCollectionControllerNavigationDigitsTests.cs` line 145. The input file is a cycle-entry record and is not edited; this paragraph is the divergence record.

## Change footprint this plan produces

- `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` — modified, two single-line text-only edits (line 145 and line 179 in the same test method), no executable change.
- `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/remediation-plan.2026-08-30T02-08.md` — this file.
- New evidence artifacts under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/{remediation-baseline,other,qa-gates}/`.
- Every other file already present in the feature folder at cycle entry is carried into the final commit unmodified because `[P3-T3]` stages the whole feature folder; none of it is edited by any task below.

### Phase 0 — Policy reads and remediation baseline capture

- [x] [P0-T1] Read, in the `policy-compliance-order` sequence, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/csharp.md`. Also read `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/remediation-inputs.2026-08-30T02-08.md` in full, the CR-6 and CR-2 sections of `code-review.2026-08-30T01-46.md`, `.claude/skills/atomic-plan-contract/SKILL.md`, and `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Write `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t1-instructions-read.2026-08-30T02-08.md`. Acceptance: that file exists and contains a `Timestamp:` field, a `Policy Order:` field naming the four rule files in the order above, and an explicit list of every document read.

- [x] [P0-T2] Capture the current (pre-edit) state of both target lines and of every sibling region that must not move. Run, from the repository root:
  - `Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs | Select-Object -Skip 144 -First 1` and record the single returned line verbatim (line 145, 57 characters of comment content following the `/// ` prefix, through the terminal period).
  - `Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs | Select-Object -Skip 178 -First 1` and record the single returned line verbatim (line 179, the `.BeEmpty(...)` because-message, 72 characters between its quotes).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'recorded width and removes').Count` — expected `1` (line 145's stale fragment).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '#472 fix, it replayed the recorded width').Count` — expected `0` (line 145's corrected fragment does not exist yet).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'registration width is replayed').Count` — expected `1` (line 179's stale fragment).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'each recorded key verbatim').Count` — expected `0` (line 179's corrected fragment does not exist yet).
  - `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` — expected `226`.
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '[TestMethod]').Count` — expected `3`.
  - Sibling-region tokens, each expected `1` and each re-measured unchanged in `[P3-T1]`: `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'modelling the unbracketed').Count` (line 141, item 2's untouched first-paragraph sibling); `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'The single residual').Count` (line 147, item 2's untouched second-paragraph opening); `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the tenth key was never visited whatever the digit width').Count` (line 149, item 2's untouched second paragraph); `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'StartsWith("0", StringComparison.Ordinal)').Count` (line 176, item 1's untouched `.Where` clause); `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'issue #644 replaced the count-bounded removal loop with a ledger that replays').Count` (line 184, the two-digit test's already-correct second assertion); `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the added tenth group is irrelevant to').Count` (line 194, the one-digit test's cycle-1-corrected summary); `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'regardless of group count').Count` (line 222, the one-digit test's cycle-1-corrected assertion).

  Acceptance: `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t2-target-lines-baseline.2026-08-30T02-08.md` exists and records all fifteen commands, their `EXIT_CODE:`, and the measured values, including the two verbatim captured lines. Any measured value that differs from the expected value above is recorded as a discrepancy and `[P1-T1]`'s and `[P1-T2]`'s acceptance are each evaluated against the measured baseline rather than the expected one.

- [x] [P0-T3] Capture the current (pre-edit) class-scoped sweep count for the defect class this cycle closes, repository-wide, with no path exclusion of any kind. This plan file is categorically outside the file set the sweep can match, because the sweep's `--include=*.cs` restricts it to C# source files and this plan is a `.md` file; no manual exclusion is applied or needed. Run:
  - `@(Get-ChildItem -Recurse -File -Include *.cs -Path . | Select-String -Pattern 'recorded (registration )?width|loop bound').Count` — expected `4`.
  - `Get-ChildItem -Recurse -File -Include *.cs -Path . | Select-String -Pattern 'recorded (registration )?width|loop bound' | Select-Object -ExpandProperty Path` paired with the line numbers, to confirm the four hits are exactly: `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationLedgerTests.cs` line 305 (legitimate, past tense — "before the ledger the loop bound dereferenced that null field"), `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs` line 145 (this cycle's item 2, `CR-2`) — corrected in place to past tense and thereafter tolerated as a legitimate past-tense hit; it remains in the match set by design, `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs` line 179 (this cycle's item 1, `CR-6`) — to be removed from the match set, and `QuickFiler\Controllers\QfcCollectionController.cs` line 2372 (legitimate, past tense — "the loop bound stayed Count"). This was independently re-derived by the planner against the current tree in this authoring pass and matches the remediation-inputs' stated entry baseline of four.

  Acceptance: `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t3-class-sweep-baseline.2026-08-30T02-08.md` exists and records both commands, their `EXIT_CODE:`, the measured count `4`, and the four-line listing with file and line number for each hit, classified exactly as above (two legitimate past-tense hits, line 145 corrected in place and thereafter tolerated, line 179 to be removed from the match set).

- [x] [P0-T4] Capture the containment and acceptance-criteria baseline that `[P3-T1]` re-checks after this cycle's edits. Run, all scoped to the two paths this cycle's commit will stage (not repository-wide — the repository carries pre-existing, unrelated drift in `.claude/agent-memory` and elsewhere that is out of scope for this cycle and is not part of either comparison):
  - `git diff --name-status 85a1939f92f64ebada4e71d19cc095dc2e8e8a26 -- QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644` — expected empty, confirming no tracked file under either path carries an uncommitted change at cycle entry (the branch head is `85a1939f92f64ebada4e71d19cc095dc2e8e8a26` itself).
  - `git status --porcelain -- QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644`, its required companion, expected to show only `??` entries for the pre-existing untracked cycle artifacts (this plan file, `remediation-inputs.2026-08-30T02-08.md`, and the three `2026-08-30T01-46` reaudit artifacts) and no `M`, `A`, or `D` entry.
  - `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[x\]').Count` — expected `21`.
  - `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[ \]').Count` — expected `5`.
  - `git diff --name-only 85a1939f92f64ebada4e71d19cc095dc2e8e8a26 -- QuickFiler/Controllers/QfcCollectionController.cs`, paired with its companion `git status --porcelain -- QuickFiler/Controllers/QfcCollectionController.cs` — both expected empty. Neither `QuickFiler/Controllers/` nor any other production directory is covered by the two pathspecs the other commands above scope to, so this pair is the only check in this plan that directly observes the production file; it is repeated in `[P3-T1]` after the edits.
  - `Get-FileHash -Algorithm SHA256 -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\code-review.2026-08-30T01-46.md, docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\feature-audit.2026-08-30T01-46.md, docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\policy-audit.2026-08-30T01-46.md, docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\remediation-inputs.2026-08-30T02-08.md | Select-Object Path, Hash` — record all four hashes. These four artifacts are untracked at cycle entry, so no anchored git diff can observe a modification to them; this hash set is the only mechanism that can.

  Acceptance: `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t4-invariance-baseline.2026-08-30T02-08.md` exists and records all seven commands, their `EXIT_CODE:`, the empty diffs, the porcelain listings, the two integer counts, and the four recorded SHA-256 hashes.

### Phase 1 — The two edits and the closing class-scoped sweep

- [x] [P1-T1] Correct CR-6 in `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs`, line 179 — the first `.BeEmpty(...)` because-message in `UnregisterNavigation_AfterRegisteringAtTwoDigitsAndShrinkingToNine_RemovesTheTwoDigitKeys`. "The recorded registration width" names `_registeredDigits`, which this branch's fix deleted (`git grep _registeredDigits` returns zero occurrences repository-wide); the message states, as the reason the assertion holds, a mechanism that no longer exists.

  Replace the line-179 literal, currently reading exactly (72 characters between the quotes):

  `"the recorded registration width is replayed, so the '0'-prefixed keys go"`

  with exactly (78 characters between the quotes — the text `code-review.2026-08-30T01-46.md`'s CR-6 finding suggests verbatim):

  `"the ledger replays each recorded key verbatim, so no '0'-prefixed key survives"`

  The replacement is 6 characters longer than the text it replaces (72 to 78, an 8.3% increase). This is a string-literal argument to a chained `.Should().BeEmpty(...)` call that CSharpier has already broken onto its own line because the whole chain does not fit on one line regardless of the argument's length, so a 6-character difference does not change CSharpier's line-breaking decision for the enclosing call; `[P2-T1]`'s before/after SHA-256 check on this file independently confirms no further rewrite occurs. Measured: the replacement line is exactly 100 columns (20 indent + 2 quotes + 78 content), against a CSharpier default print width of 100 with no repository override (no `.csharpierrc` or `csharpier.json` exists; `dotnet-tools.json` pins 1.2.6). This was verified empirically rather than inferred: the edit was applied to an out-of-tree copy and `dotnet tool run csharpier format` left it byte-identical, `csharpier check` returned exit 0, and the file remained 226 lines with line 179 unwrapped. Line 184 of the same already-formatted file is likewise exactly 100 columns and passes the repository-wide `csharpier check .`, so 100 is inside the limit. Change nothing else on this line and no other line: the `.Should()` chain, `.BeEmpty(`, the test name, and every attribute stay exactly as they are.

  Confirm before editing that the replacement's key phrase does not collide with any other pinned token in this plan: `each recorded key verbatim` does not appear in the line-145 replacement (`[P1-T2]`, below) and is not a substring of the line-222 text corrected in cycle 1 (`"the ledger replays each key verbatim, so every key is removed regardless of group count"` — that text reads "each key verbatim", three words, not "each recorded key verbatim", four words, so the two do not overlap as tokens).

  Acceptance, all five clauses must hold. Measured before this edit (per `[P0-T2]`): clause 1's count is `1`; clause 2's count is `0`; clause 3's count is `226`; clause 4's `[TestMethod]` count is `3`.
  1. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'registration width is replayed').Count` returns `0`.
  2. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'each recorded key verbatim').Count` returns `1`.
  3. `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` returns `226` (unchanged from `[P0-T2]`).
  4. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '[TestMethod]').Count` returns `3` (unchanged from `[P0-T2]`), and `git diff 85a1939f92f64ebada4e71d19cc095dc2e8e8a26 -- QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` shows no added or removed line containing the token `Should()`, `[TestMethod]`, or `public void`.
  5. The seven sibling tokens measured in `[P0-T2]` (`modelling the unbracketed`, `The single residual`, `the tenth key was never visited whatever the digit width`, `StartsWith("0", StringComparison.Ordinal)`, `issue #644 replaced the count-bounded removal loop with a ledger that replays`, `the added tenth group is irrelevant to`, `regardless of group count`) each still return `1`.

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t1-cr6-edit.2026-08-30T02-08.md`.

- [x] [P1-T2] Correct CR-2 in the same file, line 145 — inside the `<summary>` block of the same test (`UnregisterNavigation_AfterRegisteringAtTwoDigitsAndShrinkingToNine_RemovesTheTwoDigitKeys`, lines 139-152). Line 144 ends `... After the` and line 145 continues the sentence; the sentence is a correct historical statement about the #472 fix, but "After the fix" does not name which fix, inside a block that also describes #644 in its second paragraph (lines 147-151, untouched by this task), so a reader reaches the ambiguous sentence before the paragraph that disambiguates it.

  Replace the line-145 text, currently reading exactly (57 characters, the comment content following `/// `):

  `fix it replays the recorded width and removes "01".."09".`

  with exactly (64 characters):

  `#472 fix, it replayed the recorded width and removed "01".."09".`

  Combined with the unedited line 144, the sentence reads: "... After the #472 fix, it replayed the recorded width and removed "01".."09"." — naming #472 and using the past tense throughout. `code-review.2026-08-30T01-46.md`'s CR-2 finding suggests a longer rewrite that also restates the #644 mechanism a second time; that restatement is not used here because the second paragraph (lines 147-151) already states it and the remediation inputs bar rewriting that paragraph. The phrase `recorded width` is deliberately retained. The class-scoped sweep pattern is a proxy for the defect class "text naming the deleted mechanism as current behavior"; it already tolerates two hits (`QuickFiler/Controllers/QfcCollectionController.cs` line 2372 and `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationLedgerTests.cs` line 305) because both are past tense. Once line 145 is past tense and attributed to #472 it belongs to that same tolerated category, so the correct outcome is to enumerate it as a third tolerated hit rather than to reword the line out of the pattern's match set. Rewording to escape the pattern would remove the line from every future run of that detector without the defect being what changed. The post-edit sweep therefore returns `3`, not `2`.

  Change no other line: the two spaces of leading indentation before `///` and the `///` marker itself are unchanged, and the second paragraph (lines 147-151) is not rewritten.

  Acceptance, all four clauses must hold. Measured before this edit (per `[P0-T2]`): clause 1's count is `1`; clause 2's count is `0`; clause 3's count is `226` (unchanged by `[P1-T1]`, a same-length-class single-line replacement).
  1. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'recorded width and removes').Count` returns `0`.
  2. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '#472 fix, it replayed the recorded width').Count` returns `1`.
  3. `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` returns `226`.
  4. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'The single residual').Count` (the untouched second paragraph's opening, line 147) and `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'modelling the unbracketed').Count` (the untouched first-paragraph sibling sentence, line 141) each still return `1`, confirming both regions are unchanged by this edit.

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t2-cr2-edit.2026-08-30T02-08.md`.

- [x] [P1-T3] Run the closing class-scoped sweep and confirm exactly the three tolerated past-tense hits remain, repository-wide, `--include=*.cs`, no path exclusion of any kind (this plan file is categorically outside the `*.cs` file set by construction, not by exclusion — the same reasoning as `[P0-T3]`). Run:
  - `@(Get-ChildItem -Recurse -File -Include *.cs -Path . | Select-String -Pattern 'recorded (registration )?width|loop bound').Count` — must return `3`, down from the `4` recorded in `[P0-T3]`.
  - `Get-ChildItem -Recurse -File -Include *.cs -Path . | Select-String -Pattern 'recorded (registration )?width|loop bound' | Select-Object -ExpandProperty Path` paired with line numbers, confirming the three survivors are exactly `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationLedgerTests.cs` line 305, `QuickFiler\Controllers\QfcCollectionController.cs` line 2372, and `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs` line 145, and that line 179 of the digits file no longer appears.
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '#472 fix, it replayed the recorded width').Count` must return `1`, confirming the one surviving hit in that file is the past-tense attributed form and not the original text.

  This gate is falsifiable across the correction: immediately before `[P1-T1]` and `[P1-T2]` run, the count is 4, and it must read 3 only once both edits are in place. Acceptance: the sweep returns `3`, the three-line listing matches the three named survivors exactly with line 179 absent, and the `#472 fix, it replayed the recorded width` count returns `1`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t3-class-sweep-final.2026-08-30T02-08.md`.

### Phase 2 — Full C# toolchain loop

`[P1-T1]` and `[P1-T2]` each edit the same `.cs` file, so the full toolchain runs in the mandated order: format, lint/analyze, type-check, test. If any step fails or rewrites a tracked file, restart the loop from `[P2-T1]`.

- [x] [P2-T1] Run `dotnet tool run csharpier format .` at the repository root. This is a write-mode command: it exits 0 both when it rewrites files and when it changes nothing, so the exit code alone is not the observation. Capture `git status --porcelain -- . ':!.claude/agent-memory'` immediately before and immediately after the command, and record the literal console line the command prints on this run (of the shape `Formatted <N> files in <M>ms.`, with the actual `<N>` and `<M>` substituted from the real output — a documentation placeholder for the task description, not a search token). The porcelain scope is repository-wide, not scoped to `QuickFiler.Test`, because `csharpier format .` is file-based and processes `*.cs`, `*.xml`, and `packages.config` across the whole tree; a rewrite outside the test project would be invisible to a narrower observation. The `.claude/agent-memory` exclusion is the only exclusion and excludes no C# source; it is required because that path carries unrelated modifications authored by other agents that would otherwise make the comparison non-deterministic. Capture `(Get-FileHash -Algorithm SHA256 -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Hash` immediately before and immediately after the command and require the two hashes to be identical — the porcelain comparison alone cannot detect a further rewrite of a file that `[P1-T1]`/`[P1-T2]` already left in `M` status, since a rewrite would not change that status letter. Acceptance: `EXIT_CODE:` is `0`; the before/after repository-wide porcelain listings are identical (not empty — the `[P1-T1]`/`[P1-T2]` edits and the untracked cycle artifacts are present in both); and the before/after SHA-256 hashes of the digits test file are identical. If any file is rewritten or either hash differs, restart the loop from this task. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t1-csharpier-format.2026-08-30T02-08.md`.

- [x] [P2-T2] Run `dotnet tool run csharpier check .` at the repository root (read-only verify form). Acceptance: `EXIT_CODE:` is `0`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t2-csharpier-check.2026-08-30T02-08.md`.

- [x] [P2-T3] Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Use `/t:Rebuild`, not `/t:Build`: a warm `/t:Build` skips `CoreCompile` and runs no analyzers. When recording tool output, write no absolute host path, account name, or machine name; substitute generic placeholders, since msbuild echoes full project paths in its per-project output and the worktree root is itself an absolute path under the user profile. Acceptance: `EXIT_CODE:` is `0`; record the warning and error counts from msbuild's final summary block (`0 Error(s)` expected). Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t3-analyzer-build.2026-08-30T02-08.md`.

- [x] [P2-T4] Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`. Do not add `/p:Nullable=enable` and do not substitute `/t:Build`. When recording tool output, write no absolute host path, account name, or machine name; substitute generic placeholders. Acceptance: `EXIT_CODE:` is `0`; record the warning and error counts (`0 Error(s)` expected). Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t4-nullable-build.2026-08-30T02-08.md`.

- [x] [P2-T5] Run the full test gate for the touched assembly. Resolve the runner with `& "C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe"` — this fixed system location under `Program Files (x86)` is identical on every Windows host, carries no account name and no machine name, and is written verbatim rather than redacted for that reason; the resolved runner path itself, which varies by installed Visual Studio edition, is recorded as a generic placeholder. Then run that executable against `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with `/EnableCodeCoverage /InIsolation /Logger:trx /ResultsDirectory:coverage\trx\remediation-2026-08-30T02-08-p2-t5 /TestCaseFilter:"TestCategory!=LiveOutlook"`. When recording tool output, write no absolute host path, account name, or machine name; the default `vstest.console.exe` TRX filename embeds both and must be cited in redacted form, and the working directory is recorded as a generic repository-root placeholder. Acceptance, both clauses must hold: (1) the TRX `Counters` element records `failed="0"`; (2) the TRX records `UnregisterNavigation_AfterRegisteringAtTwoDigitsAndShrinkingToNine_RemovesTheTwoDigitKeys` — the test carrying both corrected regions — with outcome `Passed`. If the run stalls, sample the runner process's CPU time twice about 20 seconds apart to distinguish a hang from a slow run. On a confirmed hang, terminate only this task's own `vstest.console.exe` and `testhost` chain — never a shared MSBuild or `VBCSCompiler` worker — and re-run the identical command. A retry with no intervening file change is not a toolchain-loop restart and does not require returning to `[P2-T1]`; prove it by recording the SHA-256 of `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs` before and after the retry and confirming they are identical. This retry branch is explicitly authorized by this task text. Record the number of attempts and the outcome of each in this task's artifact. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t5-vstest-final.2026-08-30T02-08.md`.

### Phase 3 — Containment check, final host-identity sweep, and commit

- [x] [P3-T1] Re-verify containment and every invariant this cycle must preserve. Run `git diff --name-status 85a1939f92f64ebada4e71d19cc095dc2e8e8a26 -- QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644`, paired with its required companion `git status --porcelain -- QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644`; the production-file pair from `[P0-T4]` (`git diff --name-only 85a1939f92f64ebada4e71d19cc095dc2e8e8a26 -- QuickFiler/Controllers/QfcCollectionController.cs` and `git status --porcelain -- QuickFiler/Controllers/QfcCollectionController.cs`); the two `spec.md` checkbox commands from `[P0-T4]`; the four-file `Get-FileHash -Algorithm SHA256` command from `[P0-T4]`; and the seven sibling-region token commands from `[P0-T2]`.

  The anchored name-status diff enumerates tracked changes only, so it is structurally blind to the untracked cycle artifacts (this plan, `remediation-inputs.2026-08-30T02-08.md`, and the three `2026-08-30T01-46` reaudit files); the porcelain companion is what observes those as `??` entries.

  Acceptance, all eight clauses must hold:
  1. The name-status output (`QuickFiler.Test` and this feature folder) contains exactly one line, `M	QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs`.
  2. The porcelain companion shows no `M`, `A`, or `D` entry for any path other than that one file, and every `??` entry it lists is under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/`. No `??` entry under `QuickFiler.Test` is permitted: this cycle creates no file there, and `[P3-T3]`'s pathspec would otherwise commit one.
  3. The porcelain companion contains at least one `??` entry covering this cycle's new evidence artifacts under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/`.
  4. The production-file diff and its porcelain companion are both still empty, confirming `QuickFiler/Controllers/QfcCollectionController.cs` carries no change.
  5. `spec.md`'s checked-checkbox count is still `21` and its unchecked count is still `5`, identical to `[P0-T4]`. Report the AC Status Summary **read-only** in this task's evidence: AC-16 stands as PARTIAL, unchecked, referred and reported, unchanged by this cycle. Change nothing in `spec.md`.
  6. All seven sibling-region tokens from `[P0-T2]` (`modelling the unbracketed`, `The single residual`, `the tenth key was never visited whatever the digit width`, `StartsWith("0", StringComparison.Ordinal)`, `issue #644 replaced the count-bounded removal loop with a ledger that replays`, `the added tenth group is irrelevant to`, `regardless of group count`) still each return `1`.
  7. `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` returns `226` and `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '[TestMethod]').Count` returns `3`.
  8. The same four-file `Get-FileHash -Algorithm SHA256` command from `[P0-T4]` returns hashes identical to those recorded there, confirming the three `2026-08-30T01-46` reaudit artifacts and `remediation-inputs.2026-08-30T02-08.md` are carried into the commit unmodified.

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p3-t1-containment.2026-08-30T02-08.md`.

- [x] [P3-T2] Run a final host-identity sweep over the feature folder, no path exclusion of any kind, before the commit. This guards against a host path or account token being echoed into an evidence artifact by any Phase 2 command's captured output. Run both:
  - `@(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count` — must return `0`.
  - `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); @(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern "(?i)$t").Count` — must return `0`.

  If either pattern returns nonzero, redact the offending artifact's host-identifying content in place with a generic placeholder and repeat this task from the beginning; do not proceed to `[P3-T3]`. This repeat branch is explicitly authorized by this task text. Do not reproduce the raw absolute-path text or the account token in this task's own evidence artifact. Acceptance: both counts return `0`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p3-t2-host-identity-sweep.2026-08-30T02-08.md`.

- [x] [P3-T3] Commit the whole change, code and evidence together. First flip this task's own checkbox in this plan file from `- [ ]` to `- [x]`. Before staging and after flipping this task's checkbox, re-run both host-identity commands from `[P3-T2]` unchanged. Both must return `0`. This pass is the one that covers `[P3-T2]`'s own artifact and this plan file's flipped checkbox, neither of which existed in their final form when `[P3-T2]` ran. `[P3-T3]` writes no artifact, so this closing pass creates nothing further to sweep. If either count is nonzero, redact the offending artifact, re-run `[P3-T1]` in full, and repeat this task from its start. Then stage with `git add QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644`, then make a single `git commit` whose message contains the literal substring `#644` and states that the change corrects two stale documentation/assertion-message regions in `QfcCollectionControllerNavigationDigitsTests.cs` (CR-6 at line 179 and CR-2 at line 145) that named the deleted `_registeredDigits`-width mechanism as the reason for their assertions, and closes the class-scoped sweep for that defect pattern, without placing any GitHub closing keyword (`close`, `closes`, `closed`, `fix`, `fixes`, `fixed`, `resolve`, `resolves`, `resolved`) immediately before `#644`. Flipping this checkbox before staging is required for the first acceptance clause below to be reachable: this plan file lives inside the folder the commit covers, so a checkbox flipped after the commit re-dirties the tree the commit asserts is clean. This commit also carries the five untracked cycle artifacts already present in the feature folder at cycle entry (this plan, `remediation-inputs.2026-08-30T02-08.md`, and the three `2026-08-30T01-46` reaudit artifacts) as new files, since they were never committed and this task's pathspec covers the whole feature folder. Never use `git add -A` or `git add .`; the two-pathspec form above excludes `.claude/agent-memory/` and `docs/features/potential/` by construction, since neither path is named.

  Acceptance, three clauses must hold:
  1. `git status --porcelain -- QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644` produces zero output lines.
  2. `git log -1 --format=%B | Select-String -SimpleMatch -Pattern '#644'` returns at least one match.
  3. `((git log -1 --format=%B) -join ' ') | Select-String -Pattern '(?i)(clos(e|es|ed)|fix(es|ed)?|resolv(e|es|ed))\s+#644'` returns zero matches. Joining the lines first is required: `git log -1 --format=%B` yields one array element per line, so a per-line search cannot see a keyword and `#644` separated by a newline.

  This task deliberately writes no evidence artifact of its own: every artifact this plan produces lives inside the folder this commit stages, so an artifact written after the commit would re-dirty the tree that the first acceptance clause asserts is clean. PR authoring and CI monitoring are orchestrator steps outside this plan and no task above performs them.

## Self-review

`SELF-REVIEW: RE-DERIVED THIS PASS`

Citations re-derived directly against the current tree in this revision pass (revision round applying eleven preflight deltas; every citation below was re-read in this pass, not carried forward from the authoring round):

- `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` line 179 — re-read verbatim: `"the recorded registration width is replayed, so the '0'-prefixed keys go"` (72 characters between the quotes).
- `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` line 145 — re-read verbatim: `        /// fix it replays the recorded width and removes "01".."09".` — 69 columns total, of which the `        /// ` prefix is 12, leaving 57 characters of comment content through the terminal period.
- `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` lines 135-194 and 194-226 — re-read to confirm the sibling regions that must not move. Re-derived line numbers: `modelling the unbracketed` line 141; `The single residual` line 147 (sole occurrence in the file); `the tenth key was never visited whatever the digit width` line **149** (the plan previously cited 148; corrected in this pass); `StartsWith("0", StringComparison.Ordinal)` line 176; `issue #644 replaced the count-bounded removal loop with a ledger that replays` line 184; `the added tenth group is irrelevant to` line 194; `regardless of group count` line 222. Both target lines sit in the two-digit test method, not the one-digit test.
- `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` total line count and `[TestMethod]` count — re-derived in this pass: three `[TestMethod]` attributes at lines 66, 153, and 197, and 226 substantive lines (the 227th row is the trailing-newline artifact of the file viewer, not a 227th line).
- Class-scoped sweep — re-ran `recorded (registration )?width|loop bound` (case-insensitive) over `*.cs` in the current tree in this pass: exactly four hits, at `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationLedgerTests.cs:305`, `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs:145`, `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs:179`, and `QuickFiler\Controllers\QfcCollectionController.cs:2372`.
- Post-edit sweep arithmetic — re-derived in this pass: the `[P1-T2]` replacement text `#472 fix, it replayed the recorded width and removed "01".."09".` still contains the substring `recorded width`, so line 145 remains a match and the post-edit count is `3`, not `2`. The `[P1-T1]` replacement text `"the ledger replays each recorded key verbatim, so no '0'-prefixed key survives"` contains neither `recorded width`, `recorded registration width`, nor `loop bound`, so line 179 leaves the match set.
- New pinned token `#472 fix, it replayed the recorded width` — measured `0` occurrences in the file in this pass, so the `[P0-T2]` expected baseline of `0` and the `[P1-T2]`/`[P1-T3]` post-edit expectation of `1` are both falsifiable. The retained clause-1 token `recorded width and removes` measured `1` in this pass and does not survive the replacement, whose text reads `removed`, not `removes`.
- Character counts re-derived in this pass: the `[P1-T2]` replacement literal `#472 fix, it replayed the recorded width and removed "01".."09".` is 64 characters, which at the 12-character `        /// ` prefix yields a 76-column line. The `[P1-T1]` replacement literal is 78 characters between the quotes, which at 20 columns of indentation plus the two quotes yields exactly 100 columns.
- CSharpier configuration — re-derived in this pass: no `.csharpierrc` and no `csharpier.json` exists anywhere in the tree, and `dotnet-tools.json` pins csharpier `1.2.6`, so the default print width of 100 applies unmodified. Line 184 of the same already-formatted file is itself exactly 100 columns.
- Replacement-literal and sibling-token collision check re-run in this pass against all seven sibling tokens: no collision found. `each recorded key verbatim` is a four-word phrase not present in the cycle-1-corrected line 222 text, which reads only `each key verbatim`; the line-145 replacement contains none of the seven sibling tokens.
- `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/spec.md` checkbox counts — re-ran the three patterns in this pass: `21` checked, `5` unchecked, `26` total, and the two figures sum to the total.
- `code-review.2026-08-30T01-46.md` — its CR-6 suggested wording is adopted verbatim for line 179. Its CR-2 suggested wording is diverged from for line 145 only on the ground that it duplicates the #644 restatement already present in the untouched second paragraph (lines 147-151); the retention of the `recorded width` fragment is deliberate and is not a reason for divergence, because rewording the line out of the detector's match set would be gate evasion. See the `[P1-T2]` rationale and the divergence record in "Hard scope limits".
- `85a1939f92f64ebada4e71d19cc095dc2e8e8a26` (branch head) and `fa2ddefacf2c08abe18f3e3250d77da804534637` (merge base) — read from `policy-audit.2026-08-30T01-46.md` and `code-review.2026-08-30T01-46.md`, consistent with the branch/head values supplied for this cycle.
- Feature-folder file listing — re-enumerated in this pass; confirmed `code-review.2026-08-30T01-46.md`, `feature-audit.2026-08-30T01-46.md`, `policy-audit.2026-08-30T01-46.md`, and `remediation-inputs.2026-08-30T02-08.md` all exist, and that together with this plan file they are the five artifacts the porcelain companion reports as `??` under the feature folder.
- Cross-reference consistency — re-derived in this pass: `[P0-T2]` carries fifteen commands (two `Get-Content` captures, four target-fragment counts, one line count, one `[TestMethod]` count, seven sibling tokens); `[P0-T4]` carries seven commands; `[P3-T1]` carries eight acceptance clauses; the post-edit sweep figure is `3` in every place it appears.
