# Remediation Plan — Issue #644, Cycle 1 (CR-1 + PA-7)

- **Issue:** #644
- **Branch:** `bug/qfc-unregister-navigation-count-mismatch-orphan-644`
- **Head at cycle entry:** `a2c69aead286ad0ec6c7087f1bd8c46d39d0d472`
- **Source of scope:** `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/remediation-inputs.2026-08-29T23-23.md`, citing `code-review.2026-08-29T23-06.md` finding CR-1 and `policy-audit.2026-08-29T23-06.md` finding PA-7.
- **Entry condition:** the branch already passed review with zero blocking findings. This cycle is an elective quality pass over two non-blocking findings the reviewer recommended fixing before merge, not a blocking-gate failure.
- **Evidence timestamp for every artifact this plan produces:** `2026-08-29T23-23`.
- **Evidence location (non-overridable):** every artifact is written under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/` in a subfolder named for the artifact kind (`remediation-baseline/`, `other/`, `qa-gates/`), per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Writing evidence under `artifacts/` in any form is a policy violation.
- **Evidence schema:** every artifact named below carries `Timestamp:`, `Command:` (or, for a read-only document review, a statement that no shell command was executed), `EXIT_CODE:`, and `Output Summary:`, in addition to the specific fields each task's acceptance names.
- **PA-7 sweep patterns (no exclusion):** every "zero remaining instances" search below runs at least **two** patterns over its target — **three** in `[P0-T3]` and `[P3-T3]` — all with **no path exclusion of any kind**, including no exclusion for this plan file:
  - **Shape pattern** — the regular expression `[A-Za-z]:[\\/]Users[\\/]`, a Windows drive-letter-plus-`Users` shape that carries no account name.
  - **Account-token pattern** — `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); Select-String -Pattern "(?i)$t"`, which derives the account token at run time from the profile folder name and therefore never spells it in this plan.

  A third pattern, the **machine-name pattern** — `$m=[regex]::Escape($env:COMPUTERNAME); Select-String -Pattern "(?i)$m"`, which derives the machine name at run time and therefore never spells it in this plan — runs in `[P0-T3]` (baseline) and `[P3-T3]` (final containment sweep) only, not in `[P1-T5]`. `[P3-T3]`'s own rationale below and this plan's `[P2-T3]`, `[P2-T4]`, and `[P2-T5]` sanitisation clauses all name the machine name as a risk (msbuild and the default `vstest.console.exe` TRX filename both echo it), but neither the shape pattern nor the account-token pattern can detect a machine name or the account's mail local-part, so a sweep running only the original two patterns could report `0` while a machine name was still present. Measured at cycle entry by inspecting every file in the feature folder that references a host or TRX identifier — `evidence/regression-testing/p1-t4-expect-fail...md`, `p2-t5-ledger-green...md`, `p3-t7-reconciled-green...md`, `evidence/qa-gates/p4-t5-vstest-final...md`, `evidence/baseline/p0-t11-vstest-baseline...md`, and `policy-audit.2026-08-29T23-06.md` line 487 — every one of them already writes the placeholder `<HOST>` rather than a real machine-name string, so the machine-name pattern's measured baseline is `0`. This pattern is intentionally omitted from `[P1-T5]`: `[P1-T5]` verifies that the two edited-and-redacted counts fall from their nonzero baselines (`2` and `3`) to `0`, and a pattern whose baseline is already `0` cannot demonstrate a fix — a clause already true before the edit and still true after it verifies nothing, the same defect class D1 corrects for the `- **Verification:**` bullet-prefix token. `[P3-T3]` is the correct place for it because that task is a final invariant-preservation sweep, not a before/after remediation check, and a steady `0` there is exactly what it is designed to confirm.

  The shape pattern alone is insufficient, which is why the account-token pattern is mandatory rather than supplementary. `[\\/]` matches exactly one separator character, so a Windows absolute path written with **doubled** backslashes as regex escapes presents a second separator where the pattern expects the literal `Users`, and the match fails; and a bare account token carries no drive-letter prefix at all, so the shape pattern cannot see it under any spelling. `policy-audit.2026-08-29T23-06.md` line 483 carries both of those forms, which is why it requires its own remediation task rather than being covered by the shape sweep.

  This plan never quotes any leaked line's literal text: every task below that needs to identify a leaked line does so by naming its file and line number and describing the content generically (for example, a Windows absolute path naming the account and an agent-worktree identifier), so this plan file itself carries zero instances under any of the three patterns and needs no exclusion from its own sweep. The shape pattern is self-exempt from matching its own literal text by construction, not by exclusion: in that pattern's own printed form, the character immediately before its `:` is the class-closing `]`, which is outside `[A-Za-z]`, so quoting the pattern in this plan's prose or command arguments does not make the sweep match itself. The account-token and machine-name patterns are each self-exempt because neither ever contains the token it matches, only the expression that derives it. Re-derived at cycle entry: the shape pattern matches `2` lines across the feature folder, the account-token pattern matches `3`, and the machine-name pattern matches `0`. `[P0-T3]` measures all three directly; `[P1-T5]` re-measures the shape and account-token patterns only, for the reason stated above; `[P3-T3]` re-measures all three.

## Hard scope limits (restated for a fresh executor)

- **AC-16 stays unchecked.** No coverage comparison, no re-run of the repository-wide Cobertura post-processor (`scripts/vscode/Invoke-MSTestWithCoverage.ps1`), and no edit to `spec.md` anywhere in this plan. A comment-and-string-literal-only edit in a test file cannot change instrumented production line counts, and re-running an instrument whose measured noise exceeds the disputed delta produces a third number without resolving anything.
- **No acceptance criterion changes state in this cycle.** This is stated as a measurable invariant rather than an intention. Re-derived at cycle entry against `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/spec.md`: `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[x\]').Count` is `21` and `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[ \]').Count` is `5`. Those two figures sum to the `26` checkbox lines that `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '\[[ xX]\]').Count` reports, so the file contains no indented checkbox and no upper-case-`X` checkbox that the first two patterns would miss. Both figures must be identical at cycle exit; `[P3-T2]` re-measures them and fails if either has moved. The executor reports the AC Status Summary **read-only** and checks, unchecks, adds, removes, or rewords no acceptance criterion anywhere in this cycle.
- **No evidence artifact under `evidence/` is edited**, except the new artifacts this plan itself creates under `evidence/remediation-baseline/`, `evidence/other/`, and `evidence/qa-gates/`. `evidence/qa-gates/p4-t6-coverage-final.2026-08-29T08-15.md` in particular must remain byte-for-byte unmodified; this is verified by hash comparison in `[P0-T4]` and `[P3-T2]`.
- **No production file is touched.** `QuickFiler/Controllers/QfcCollectionController.cs` and every other production path are out of scope.
- **`plan.2026-08-29T07-42.md` (the approved plan of record) is not modified.** It is complete with all 58 tasks checked; `[P0-T4]` and `[P3-T2]` verify this count is unchanged before and after this cycle.

## Change footprint this plan produces

- `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` — modified, documentation-comment and assertion because-message text only, no executable change (CR-1, two stale regions in the same test).
- `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/research/research.2026-08-29T07-55.md` — modified, one line redacted (PA-7, instance 1).
- `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/policy-audit.2026-08-29T23-06.md` — modified, two lines redacted (PA-7, instance 2 on line 482 and instance 3 on line 483). This file is untracked at cycle entry and is staged for the first time by the commit task, so both lines enter `main` with this pull request; that is why line 483 is in scope rather than treated as a historical record.
- `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/remediation-plan.2026-08-29T23-23.md` — this file.
- New evidence artifacts under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/{remediation-baseline,other,qa-gates}/`.
- Every other file already present in the feature folder at cycle entry (`remediation-inputs.2026-08-29T23-23.md`, `code-review.2026-08-29T23-06.md`, `feature-audit.2026-08-29T23-06.md`, `issue.md`, `spec.md`, `plan.2026-08-29T07-42.md`, prior evidence) is carried into the final commit unmodified because `[P3-T4]` stages the whole feature folder; none of it is edited by any task below.

### Phase 0 — Policy reads and remediation baseline capture

- [x] [P0-T1] Read, in the `policy-compliance-order` sequence, `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, and `.claude/rules/csharp.md`. Also read `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/remediation-inputs.2026-08-29T23-23.md` in full, the CR-1 section of `code-review.2026-08-29T23-06.md`, the PA-7 section of `policy-audit.2026-08-29T23-06.md`, `.claude/skills/atomic-plan-contract/SKILL.md`, and `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Write `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t1-instructions-read.2026-08-29T23-23.md`. Acceptance: that file exists and contains a `Timestamp:` field, a `Policy Order:` field naming the four rule files in the order above, and an explicit list of every document read.

- [x] [P0-T2] Capture the current (pre-edit) state of both CR-1 stale regions in the same test method: the `<summary>` block `[P1-T1]` corrects, and the `.BeEmpty(...)` because-message `[P1-T6]` corrects. Run, from the repository root:
  - `Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs | Select-Object -Skip 188 -First 8` and record the eight returned lines verbatim (the `<summary>` block on `UnregisterNavigation_AfterRegisteringAtOneDigitAndGrowingToTen_RemovesTheOneDigitKeys`, lines 189-196).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the loop bound has grown to ten').Count` — expected `1` (the stale sentence, present once, confined to this block).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the nine recorded keys').Count` — expected `0` (the corrected phrase does not exist yet). `the ledger replays` is not used as the pinned token here: `[P1-T6]`'s replacement literal also begins with that phrase, so a token shared between the two Phase 1 edits cannot be pinned to `1` in `[P1-T1]`'s acceptance without going stale once `[P1-T6]` runs.
  - `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` — expected `226`.
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '[TestMethod]').Count` — expected `3`.
  - `Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs | Select-Object -Skip 221 -First 1` and record the single returned line verbatim (the `.BeEmpty` because-message at line 222, inside the same test method, 86 characters between its quotes).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'grown loop bound reaches').Count` — expected `1` (the stale fragment at line 222, present once).
  - `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'regardless of group count').Count` — expected `0` (the corrected fragment does not exist yet).

  Acceptance: `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t2-cr1-baseline.2026-08-29T23-23.md` exists and records all eight commands, their `EXIT_CODE:`, and the measured values, including the verbatim eight-line `<summary>` block and the verbatim line-222 string capture. Any measured value that differs from the expected value above is recorded as a discrepancy and `[P1-T1]`'s and `[P1-T6]`'s acceptance are each evaluated against the measured baseline rather than the expected one.

- [x] [P0-T3] Capture the current (pre-edit) PA-7 counts across the whole feature folder under **all three** sweep patterns, with no path exclusion of any kind — this plan file is included in the sweep. Run all six commands:
  - `@(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count` — expected `2` — and separately list the matching files and line numbers.
  - `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); @(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern "(?i)$t").Count` — expected `3` — and separately list the matching files and line numbers. The account-token count exceeds the shape count because `policy-audit.2026-08-29T23-06.md` line 483 writes the path with doubled backslashes and also carries bare account tokens, neither of which the shape pattern can match.
  - `$m=[regex]::Escape($env:COMPUTERNAME); @(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern "(?i)$m").Count` — expected `0` — the machine-name pattern; every file in the feature folder that references a TRX identifier already writes the `<HOST>` placeholder rather than a real machine-name string, so no remediation is required for this pattern in this cycle.
  - `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\remediation-plan.2026-08-29T23-23.md -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count` — expected `0` — confirming this plan file, which quotes the shape pattern's own literal text in several places, produces zero self-matches: the character immediately before the `:` in `[A-Za-z]:[\\/]Users[\\/]` is the class-closing `]`, outside `[A-Za-z]`, so the pattern is self-exempt by construction.
  - `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); @(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\remediation-plan.2026-08-29T23-23.md -Pattern "(?i)$t").Count` — expected `0` — confirming this plan file never spells the account token, because it derives it at run time instead.
  - `$m=[regex]::Escape($env:COMPUTERNAME); @(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\remediation-plan.2026-08-29T23-23.md -Pattern "(?i)$m").Count` — expected `0` — confirming this plan file never spells the machine name, because it derives it at run time instead.

  Do not reproduce the raw absolute-path text, the account token, the machine name, or any bare account spelling in this task's own evidence artifact; cite the three remaining locations by file and line number only, describing the content generically: `research/research.2026-08-29T07-55.md` line 5 and `policy-audit.2026-08-29T23-06.md` line 482, each a Windows absolute path naming the account and an agent-worktree identifier, and `policy-audit.2026-08-29T23-06.md` line 483, a quoted search pattern naming a doubled-backslash absolute path, a bare account name, and the account's mail local-part. Acceptance: `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t3-pa7-baseline.2026-08-29T23-23.md` exists and records all six commands, their `EXIT_CODE:`, and the six measured counts (`2`, `3`, `0`, `0`, `0`, `0`). If any of the three whole-folder counts differs from its expected value, record `REMEDIATION-REQUIRED` and report it to the orchestrator before starting Phase 1; this reporting branch is explicitly authorized by this task text.

- [x] [P0-T4] Capture the three invariance baselines that `[P3-T2]` re-checks after this cycle's edits. Run:
  - `(Get-FileHash -Algorithm SHA256 -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\evidence\qa-gates\p4-t6-coverage-final.2026-08-29T08-15.md).Hash` and record the printed hash verbatim.
  - `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\plan.2026-08-29T07-42.md -Pattern '^- \[x\] \[P\d+-T\d+\]').Count` — expected `58`.
  - `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\plan.2026-08-29T07-42.md -Pattern '^- \[ \] \[P\d+-T\d+\]').Count` — expected `0`.
  - `git diff a2c69aead286ad0ec6c7087f1bd8c46d39d0d472 --name-only -- QuickFiler/Controllers/QfcCollectionController.cs` — expected empty output, confirming the production file carries no uncommitted change at cycle entry, paired with its companion `git status --porcelain -- QuickFiler/Controllers/QfcCollectionController.cs`, also expected empty.

  Acceptance: `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/remediation-baseline/p0-t4-invariance-baseline.2026-08-29T23-23.md` exists and records all five commands, their `EXIT_CODE:`, the SHA-256 hash string, and the two integer counts.

### Phase 1 — The remediation edits and the PA-7 sweep

- [x] [P1-T1] Correct the stale CR-1 documentation block in `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs`. Replace the eight-line block (lines 189-196) reading exactly:

  ```
          /// <summary>
          /// Issue #472, mirror direction. A nine-item page registers keys "1".."9" at width 1. A group
          /// is then added without an intervening unregister, so the live <c>Digits</c> getter now
          /// computes width 2. Before the fix <c>UnregisterNavigation</c> removed the never-registered
          /// "01".."10" and left all nine single-digit keys orphaned. After the fix it replays the
          /// recorded width 1 and, because the loop bound has grown to ten, removes every registered
          /// key.
          /// </summary>
  ```

  with exactly:

  ```
          /// <summary>
          /// Issue #472, mirror direction. A nine-item page registers keys "1".."9" at width 1. A group
          /// is then added without an intervening unregister, so the live <c>Digits</c> getter now
          /// computes width 2. Before the fix <c>UnregisterNavigation</c> removed the never-registered
          /// "01".."10" and left all nine single-digit keys orphaned. After the fix the ledger replays
          /// the nine recorded keys "1".."9" verbatim, so the added tenth group is irrelevant to
          /// unregistration.
          /// </summary>
  ```

  Change no other line in the file: no assertion, no test name, no attribute, and no executable line. The sibling `Issue #472` summary at lines 139-152 (which already names the #644 ledger correctly and contains the CR-2 sentence, out of scope for this cycle) is untouched.

  The fenced replacement blocks above carry two spaces of Markdown indentation, which must be stripped before applying: the replacement begins at column 9 for the C# block (the `///` lines are indented ten spaces in the fence as written; stripping the two-space Markdown indent leaves eight spaces before `///`, i.e. column 9). This convention recurs at `[P1-T2]` and `[P1-T3]`, whose fenced blocks are Markdown bullet lines and so begin at column 1 once the same two-space indent is stripped. `[P1-T4]` and `[P1-T6]` are not fenced: each replaces its target with a single-line inline-code literal (backtick-quoted, not triple-backtick), so there is no nested block whose column could be misplaced, and their two-space plan-prose indentation is ordinary list continuation that is not part of the literal.

  Confirmed: the mandated replacement block above places the phrase `the nine recorded keys` exactly once, on the single source line reading `/// the nine recorded keys "1".."9" verbatim, so the added tenth group is irrelevant to`, inside the eight-line block this task replaces (the region `[P1-T6]` does not touch, since `[P1-T6]` edits only line 222). Re-derived against the current tracked file: `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the nine recorded keys').Count` is `0` before this task runs (the phrase does not exist yet) and must be `1` after. No adjustment to the eight `///` lines was needed.

  Acceptance, all four clauses must hold. Measured before this edit: clause 1's count is `1`; clause 2's count is `0`; clause 3's count is `226`; clause 4's `[TestMethod]` count is `3`.
  1. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the loop bound has grown to ten').Count` returns `0`.
  2. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'the nine recorded keys').Count` returns `1`. The token `the ledger replays` is not usable for this clause: `[P1-T6]`'s replacement literal begins with that same phrase, so a clause pinned to `1` describes a value that stops being true before Phase 1 ends.
  3. `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` returns `226` (unchanged from `[P0-T2]`).
  4. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern '[TestMethod]').Count` returns `3` (unchanged from `[P0-T2]`), and `git diff a2c69aead286ad0ec6c7087f1bd8c46d39d0d472 -- QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` shows no added or removed line containing the token `Should()`, `[TestMethod]`, or `public void`.

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t1-cr1-edit.2026-08-29T23-23.md`.

- [x] [P1-T2] Redact PA-7 instance 1 in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/research/research.2026-08-29T07-55.md`, line 5. Line 5 is a Markdown bullet of the form `` - Worktree: `<value>` `` where `<value>` is a Windows absolute path naming the account and an agent-worktree identifier (drive letter, `Users`, the account name, the repository path, and `.claude/worktrees/<agent-worktree-id>`, joined with forward slashes). Replace the entire line with exactly:

  ```
  - Worktree: `<repo-root>/.claude/worktrees/<agent-worktree>`
  ```

  Change no other line in the file.

  Acceptance, both clauses must hold:
  1. `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\research\research.2026-08-29T07-55.md -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count` returns `0`.
  2. `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\research\research.2026-08-29T07-55.md -SimpleMatch -Pattern '- Worktree:').Count` returns `1` (the line still exists, redacted rather than deleted).

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t2-pa7-research-edit.2026-08-29T23-23.md`.

- [x] [P1-T3] Redact PA-7 instance 2 in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/policy-audit.2026-08-29T23-06.md`, line 482 (the `- **Content:**` citation inside the PA-7 finding). Line 482 is a Markdown bullet of the form `` - **Content:** `- Worktree: \`<value>\`` `` where `<value>` is the same Windows absolute path described in `[P1-T2]` — drive letter, `Users`, the account name, the repository path, and `.claude/worktrees/<agent-worktree-id>`, joined with forward slashes. Replace the entire line with exactly:

  ```
  - **Content:** `- Worktree: \`<repo-root>/.claude/worktrees/<agent-worktree>\``
  ```

  Change no other line in this task: the `Location:`, `Standing:`, `Why non-blocking:`, and `Recommendation:` lines of the PA-7 finding (lines 480-481 and 484-491) are untouched here, and no finding, verdict, or measured figure anywhere else in the document is altered. The `Verification:` line at 483 is remediated by `[P1-T4]`, not by this task. This task replaces exactly one line with exactly one line, so every following line keeps its current number and `[P1-T4]`'s citation of line 483 remains correct after this task runs.

  Acceptance, both clauses must hold. Clause 1 is satisfiable after this task alone even though line 483 still carries an account token at that point, because line 483 writes its path with doubled backslashes and the shape pattern's `[\\/]` matches only a single separator character, so line 483 has never been a shape-pattern match:
  1. `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\policy-audit.2026-08-29T23-06.md -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count` returns `0`.
  2. `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\policy-audit.2026-08-29T23-06.md -SimpleMatch -Pattern '- **Content:**').Count` returns `1` (the line still exists, redacted rather than deleted).

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t3-pa7-policy-audit-edit.2026-08-29T23-23.md`.

- [x] [P1-T4] Redact PA-7 instance 3 in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/policy-audit.2026-08-29T23-06.md`, line 483. Line 483 is the `- **Verification:**` line of the PA-7 finding; it is a two-line bullet continuing onto line 484. It quotes a `grep -nE` pattern whose alternation names, in order, a Windows absolute path built with doubled-backslash separators and containing the account name, the bare account name, and the account's mail local-part. Replace line 483 with exactly:

  `- **Verification:** a case-insensitive search for the account name and the account's mail local-part returns that`

  and leave line 484 in place unchanged. Line 484 reads `single line; the same scan over the rest of the feature folder returns nothing.`; joined with the corrected line 483 above, the bullet reads as a single grammatical sentence and retains both of the measured figures it already records — the single matched line in the research artifact, and the empty result over the rest of the feature folder. Change no other line. This alters wording only: the finding, its verdict, its `Location:` citation, and both measured figures are preserved, satisfying the inputs' constraint not to alter any finding, verdict, or measured figure.

  Acceptance, all three clauses must hold. Measured before this edit: clause 1's count is `1` (the account token is present at line 483 before redaction); clause 2's count is `0` (the corrected wording does not exist yet); clause 3's count is `3` (lines 385, 395, and 483 all carry the bare `- **Verification:**` bullet prefix before this edit).
  1. `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); @(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\policy-audit.2026-08-29T23-06.md -Pattern "(?i)$t").Count` returns `0`.
  2. `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\policy-audit.2026-08-29T23-06.md -SimpleMatch -Pattern '- **Verification:** a case-insensitive search').Count` returns `1` — the line still exists, redacted rather than deleted, and carries the exact wording this task mandates. The bare bullet prefix `- **Verification:**` cannot be used as the token: it is the bullet prefix of three findings in this document (lines 385, 395, and this task's own line), so a count over it returns `3` both before and after this edit and the clause could never fail.
  3. `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\policy-audit.2026-08-29T23-06.md -SimpleMatch -Pattern '- **Verification:**').Count` returns `3`, unchanged, confirming no `Verification:` bullet anywhere in the document was deleted.

  Do not reproduce the raw absolute-path text, the account token, or the account's mail local-part in this task's own evidence artifact. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t4-pa7-verification-line-edit.2026-08-29T23-23.md`.

- [x] [P1-T5] Sweep the whole feature folder and confirm zero remaining PA-7 instances under **both** sweep patterns, each run with **no path exclusion of any kind** — this plan file is included in the sweep. Run both:
  - `@(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count` — must return `0`, down from the `2` recorded in `[P0-T3]`.
  - `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); @(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern "(?i)$t").Count` — must return `0`, down from the `3` recorded in `[P0-T3]`.

  Both patterns are required because the shape pattern alone is structurally incapable of seeing the third instance: `[\\/]` matches exactly one separator character, so the shape pattern cannot match a path written with doubled backslashes, and it carries a mandatory drive-letter prefix, so it cannot see a bare account token at all. A sweep running only the shape pattern would have returned `0` while an account token was still present on line 483.

  This gate is falsifiable across the correction. The `2` shape-pattern instances remain open until `[P1-T2]` and `[P1-T3]` run, and the third account-token instance remains open until `[P1-T4]` runs, so immediately before those tasks execute the two patterns read `2` and `3` — both nonzero, both false — and both must read `0` once the three edits complete. Do not reproduce the raw absolute-path text or the account token in this task's own evidence artifact. Acceptance: both counts return `0`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t5-pa7-sweep.2026-08-29T23-23.md`.

- [x] [P1-T6] Correct the second stale CR-1 region in the same test method, `QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs`, line 222 — the `.BeEmpty(...)` because-message inside `UnregisterNavigation_AfterRegisteringAtOneDigitAndGrowingToTen_RemovesTheOneDigitKeys`. This is the identical defect `[P1-T1]` corrects, in the identical test, differing only in that it sits in a string literal rather than an XML comment: the current text names both mechanisms this cycle's fix deleted (the recorded width and the grown loop bound), so leaving it stale would close this cycle having fixed the documentation block while the assertion message three lines below still asserts the deleted mechanism as the reason the assertion holds.

  Replace the line-222 literal, currently reading exactly (86 characters between the quotes):

  `"the recorded width 1 is replayed and the grown loop bound reaches every registered key"`

  with exactly (87 characters between the quotes):

  `"the ledger replays each key verbatim, so every key is removed regardless of group count"`

  The replacement is one character longer than the text it replaces, so CSharpier's line-breaking decision for the enclosing `.BeEmpty( … )` call is unchanged and the surrounding lines do not reflow. Change nothing else: the `.Should()` chain, `.BeEmpty(`, the test name, and every attribute stay exactly as they are. Only the string literal's contents change, and only on this one line.

  Acceptance, all three clauses must hold:
  1. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'grown loop bound reaches').Count` returns `0` (down from `1` in `[P0-T2]`).
  2. `@(Select-String -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs -SimpleMatch -Pattern 'regardless of group count').Count` returns `1` (up from `0` in `[P0-T2]`).
  3. `(Get-Content QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Count` returns `226` (unchanged from `[P0-T2]` and from `[P1-T1]`'s clause 3): this is a one-line-for-one-line replacement, so the file's 226-line invariant holds unaffected by this task.

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/other/p1-t6-cr1-line222-edit.2026-08-29T23-23.md`.

### Phase 2 — Full C# toolchain loop

`[P1-T1]` and `[P1-T6]` each edit the same `.cs` file, so the full toolchain runs in the mandated order: format, lint/analyze, type-check, test. If any step fails or rewrites a tracked file, restart the loop from `[P2-T1]`.

- [x] [P2-T1] Run `dotnet tool run csharpier format .` at the repository root. This is a write-mode command: it exits 0 both when it rewrites files and when it changes nothing, so the exit code alone is not the observation. Capture `git status --porcelain -- . ':!.claude/agent-memory'` immediately before and immediately after the command, and record the literal console line the command prints on this run (of the shape `Formatted <N> files in <M>ms.`, with the actual `<N>` and `<M>` substituted from the real output — this is a documentation placeholder for the task description, not a search token). The porcelain scope is repository-wide, not scoped to `QuickFiler.Test`, because the command's blast radius is repository-wide: `csharpier format .` is file-based and processes `*.cs`, `*.xml`, and `packages.config` across the whole tree, so a rewrite outside the test project would be invisible to a narrower observation and the following `csharpier check .` would then pass over an already-normalised tree. The `.claude/agent-memory` exclusion is required because that path carries unrelated modifications authored by other agents, which would otherwise make the before/after comparison non-deterministic; it is the only exclusion, and it excludes no C# source. The porcelain comparison alone cannot detect a rewrite of `QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs`: `[P1-T1]` and `[P1-T6]` have already left that file in ` M` (modified) status before this task runs, so a further CSharpier rewrite of the same file — for example, normalising a mixed-line-ending file back to uniform line endings after an edit introduced an LF into an otherwise-CRLF file — changes the file's content without changing its porcelain status letter, and the before/after porcelain listings stay identical across that rewrite. Capture `(Get-FileHash -Algorithm SHA256 -Path QuickFiler.Test\Controllers\QfcCollectionControllerNavigationDigitsTests.cs).Hash` immediately before and immediately after the `csharpier format .` command and require the two hashes to be identical; if they differ, CSharpier rewrote the file and the loop restarts from this task. Acceptance: `EXIT_CODE:` is `0`; the before/after repository-wide `git status --porcelain -- . ':!.claude/agent-memory'` listings are identical, so no file anywhere in the tree changes state across the command; and the before/after SHA-256 hashes of the digits test file are identical. The listing is not expected to be empty: the `[P1-T1]` and `[P1-T6]` edits are already present in both the before and the after listing, and identity between the two is what the acceptance asserts. If any file is rewritten or either hash differs, restart the loop from this task. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t1-csharpier-format.2026-08-29T23-23.md`.

- [x] [P2-T2] Run `dotnet tool run csharpier check .` at the repository root (read-only verify form). Acceptance: `EXIT_CODE:` is `0`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t2-csharpier-check.2026-08-29T23-23.md`.

- [x] [P2-T3] Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Use `/t:Rebuild`, not `/t:Build`: a warm `/t:Build` skips `CoreCompile` and runs no analyzers. When recording tool output in this task's evidence artifact, write no absolute host path, account name, or machine name. Substitute a generic repository-root placeholder, a generic account placeholder, and a generic host placeholder, following the convention already used by `evidence/qa-gates/p4-t5-vstest-final.2026-08-29T08-15.md`. This is load-bearing here because the worktree root is itself an absolute path under the user profile and msbuild echoes full project paths in its per-project output. Acceptance: `EXIT_CODE:` is `0`; record the warning and error counts msbuild prints in its final summary block (`0 Error(s)` expected). Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t3-analyzer-build.2026-08-29T23-23.md`.

- [x] [P2-T4] Run `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`. Do not add `/p:Nullable=enable` and do not substitute `/t:Build`. When recording tool output in this task's evidence artifact, write no absolute host path, account name, or machine name. Substitute a generic repository-root placeholder, a generic account placeholder, and a generic host placeholder, following the convention already used by `evidence/qa-gates/p4-t5-vstest-final.2026-08-29T08-15.md`. This is load-bearing here because the worktree root is itself an absolute path under the user profile and msbuild echoes full project paths in its per-project output. Acceptance: `EXIT_CODE:` is `0`; record the warning and error counts (`0 Error(s)` expected) and an explicit statement that no `CS0414` diagnostic appears. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t4-nullable-build.2026-08-29T23-23.md`.

- [x] [P2-T5] Run the full test gate for the touched assembly. Resolve the runner with `& "C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe"`, then run that executable against `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with `/EnableCodeCoverage /InIsolation /Logger:trx /ResultsDirectory:coverage\trx\remediation-p2-t5 /TestCaseFilter:"TestCategory!=LiveOutlook"`. The `vswhere.exe` path above is a fixed system location under `Program Files (x86)`, not a host-identifying path: it is identical on every Windows host, carries no account name and no machine name, and trips no PA-7 sweep pattern, so it is deliberately written verbatim rather than redacted; the resolved runner path returned by this command and recorded in evidence remains a placeholder because that returned path can vary by installed Visual Studio edition and version. When recording tool output in this task's evidence artifact, write no absolute host path, account name, or machine name. Substitute a generic repository-root placeholder, a generic account placeholder, and a generic host placeholder, following the convention already used by `evidence/qa-gates/p4-t5-vstest-final.2026-08-29T08-15.md`. The default `vstest.console.exe` TRX filename embeds both the account and the machine name and must be cited in redacted form; record the resolved runner path as a generic placeholder rather than verbatim, and record the working directory as a generic repository-root placeholder. Acceptance, both clauses must hold: (1) the TRX `Counters` element records `failed="0"`; (2) the TRX records `UnregisterNavigation_AfterRegisteringAtOneDigitAndGrowingToTen_RemovesTheOneDigitKeys` — the test carrying the corrected CR-1 documentation and the corrected assertion because-message — with outcome `Passed`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p2-t5-vstest-final.2026-08-29T23-23.md`.

### Phase 3 — Containment check and final commit

- [x] [P3-T1] Verify the change is contained to the expected set. Run `git diff --name-status a2c69aead286ad0ec6c7087f1bd8c46d39d0d472 -- . ':!.claude/agent-memory'` and, as its required companion, `git status --porcelain -- . ':!.claude/agent-memory'`.

  The two commands are complementary and each alone is wrong in one state. The anchored name-status diff enumerates tracked changes only, so it is structurally blind to untracked files: `policy-audit.2026-08-29T23-06.md` is untracked at cycle entry and is therefore **correctly absent** from the name-status output even though `[P1-T3]` and `[P1-T4]` both edited it, and its absence is not evidence that those edits did not happen. The porcelain companion is what observes it. Conversely, porcelain goes empty once the change is committed, which is why the anchored diff carries the tracked-file assertion.

  Acceptance, all four clauses must hold:
  1. Filtering the name-status output to lines beginning with `M` returns exactly two lines, `M	QuickFiler.Test/Controllers/QfcCollectionControllerNavigationDigitsTests.cs` and `M	docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/research/research.2026-08-29T07-55.md`.
  2. The porcelain companion shows no ` M ` entry for `QuickFiler/Controllers/QfcCollectionController.cs` and none for `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/plan.2026-08-29T07-42.md`.
  3. The porcelain companion contains a line whose status field is `??` and whose path is exactly `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/policy-audit.2026-08-29T23-06.md`, positively confirming that the file edited by `[P1-T3]` and `[P1-T4]` is present in the working tree as a new untracked artifact and will be staged by `[P3-T4]`.
  4. The porcelain companion contains at least one `??` entry covering this cycle's new evidence artifacts under `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/`. Accept either listing form: git collapses a wholly-untracked directory into a single entry whose path ends in `/`, so `evidence/remediation-baseline/` is expected as one collapsed entry, while `evidence/other/` and `evidence/qa-gates/` already contain tracked artifacts from the prior cycle and therefore list this cycle's new files individually. Record which form was observed.

  Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p3-t1-containment.2026-08-29T23-23.md`.

- [x] [P3-T2] Re-check the three invariance baselines from `[P0-T4]`, plus the acceptance-criteria invariant stated in the hard scope limits above. Run the same five `[P0-T4]` commands again, and additionally run `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[x\]').Count` and `@(Select-String -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644\spec.md -Pattern '^- \[ \]').Count`. Acceptance, all four clauses must hold: (1) the SHA-256 hash of `evidence/qa-gates/p4-t6-coverage-final.2026-08-29T08-15.md` is identical to the `[P0-T4]` value; (2) `plan.2026-08-29T07-42.md` still shows `58` checked `[P#-T#]` tasks and `0` unchecked; (3) `git diff a2c69aead286ad0ec6c7087f1bd8c46d39d0d472 --name-only -- QuickFiler/Controllers/QfcCollectionController.cs` and its companion `git status --porcelain -- QuickFiler/Controllers/QfcCollectionController.cs` are both still empty; (4) `spec.md` still reports exactly `21` checked and `5` unchecked checkbox lines, identical to the cycle-entry figures re-derived in the hard scope limits above, confirming that no acceptance criterion changed state in this cycle. Report the AC Status Summary in this task's evidence artifact **read-only**: state the AC-16 disposition as it already stands (PARTIAL, unchecked, referred and reported) and change nothing in `spec.md`. Recorded in `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p3-t2-invariance-final.2026-08-29T23-23.md`.

- [x] [P3-T3] Run the final PA-7 sweep over the state that `[P3-T4]` will commit. This task exists because `[P1-T5]` did not run at the end of Phase 1 — `[P1-T6]` follows it — and `[P1-T6]`, Phase 2, and Phase 3 together write eight further artifacts into the very folder `[P1-T5]` swept (`[P1-T6]`, `[P2-T1]`, `[P2-T2]`, `[P2-T3]`, `[P2-T4]`, `[P2-T5]`, `[P3-T1]`, and `[P3-T2]`), so `[P1-T5]` cannot fail for any leak those artifacts introduce. `[P1-T6]`'s own artifact carries no host path or account token — it records a plain test-string edit — so it does not retroactively invalidate `[P1-T5]`'s zero counts; it is listed here only so the artifact count in this paragraph stays accurate. The remaining seven artifacts are the highest-risk ones in the plan: the worktree root is itself an absolute path under the user profile, msbuild echoes full project paths, and the default `vstest.console.exe` TRX filename embeds both the account and the machine name.

  Run all three patterns — the two from `[P1-T5]` plus the machine-name pattern that `[P1-T5]` deliberately omits — over the whole feature folder with no path exclusion of any kind:
  - `@(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern '[A-Za-z]:[\\/]Users[\\/]').Count`
  - `$t=[regex]::Escape((Split-Path -Leaf $env:USERPROFILE)); @(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern "(?i)$t").Count`
  - `$m=[regex]::Escape($env:COMPUTERNAME); @(Get-ChildItem -Recurse -File -Path docs\features\active\2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644 | Select-String -Pattern "(?i)$m").Count` — measured baseline `0` at cycle entry (see the front-matter PA-7 sweep-patterns note); this run confirms the seven artifacts written by `[P1-T6]` through `[P3-T2]` did not introduce a machine name.

  Write `docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644/evidence/qa-gates/p3-t3-pa7-final-sweep.2026-08-29T23-23.md` recording all three commands, their `EXIT_CODE:`, and the counts from the first run, then **re-run all three patterns a second time with that artifact present on disk** and record the second triple of counts in the same artifact. Do not reproduce the raw absolute-path text, the account token, or the machine name in this artifact.

  Acceptance: the **second** run of all three patterns returns `0`. The second run is required because this task's own artifact lands inside the folder it sweeps, so a single run cannot observe it. If any pattern returns nonzero on either run, redact the offending artifact and repeat this task from the beginning; do not proceed to `[P3-T4]`. This repeat branch is explicitly authorized by this task text.

  Nothing that can carry a host path, an account name, or a machine name lands after this task's second sweep run. Two single-character checkbox flips happen between that run and the commit — this task's own checkbox, flipped from `- [ ]` to `- [x]` when this task completes, and `[P3-T4]`'s own checkbox, flipped the same way when it runs — and neither can introduce any of the three patterns. `[P3-T4]` writes no evidence artifact of its own.

- [x] [P3-T4] Commit the whole change, code and evidence together. First flip this task's own checkbox in this plan file from `- [ ]` to `- [x]`, then stage with `git add QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644`, then make a single `git commit` whose message contains the literal substring `#644` and states that the change corrects the two stale CR-1 test-documentation regions (the `<summary>` block and the assertion because-message) and redacts the three PA-7 host-identity instances (two absolute host paths and one quoted search pattern carrying bare account tokens), without placing any GitHub closing keyword (`close`, `closes`, `closed`, `fix`, `fixes`, `fixed`, `resolve`, `resolves`, `resolved`) immediately before `#644`. Flipping this checkbox before staging is required for the first acceptance clause below to be reachable: this plan file lives inside the folder the commit covers, so a checkbox flipped after the commit re-dirties the tree the commit asserts is clean. Never use `git add -A` or `git add .`; the two-pathspec form above excludes `.claude/agent-memory/` and `docs/features/potential/` by construction, since neither path is named.

  Acceptance, three clauses must hold:
  1. `git status --porcelain -- QuickFiler.Test docs/features/active/2026-08-27-qfc-unregister-navigation-count-mismatch-orphan-644` produces zero output lines.
  2. `git log -1 --format=%B | Select-String -SimpleMatch -Pattern '#644'` returns at least one match.
  3. `git log -1 --format=%B | Select-String -Pattern '(?i)(clos(e|es|ed)|fix(es|ed)?|resolv(e|es|ed))\s+#644'` returns zero matches.

  This task deliberately writes no evidence artifact of its own, for two reasons that reinforce each other. First, every artifact this plan produces lives inside the folder this commit stages, so an artifact written after the commit would re-dirty the tree that the first acceptance clause asserts is clean. Second, it is the reason nothing lands in the feature folder after `[P3-T3]`'s final PA-7 sweep: because this task creates no file, the state `[P3-T3]` observed on its second run is the state this commit stages, apart from two single-character checkbox flips — `[P3-T3]`'s own checkbox, flipped when that task completed, and this task's own checkbox, flipped immediately above. PR authoring and CI monitoring are orchestrator steps outside this plan and no task above performs them.
