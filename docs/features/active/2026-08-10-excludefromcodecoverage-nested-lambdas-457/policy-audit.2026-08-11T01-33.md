# Policy Audit — bug/excludefromcodecoverage-nested-lambdas-457

- Feature: `docs/features/active/2026-08-10-excludefromcodecoverage-nested-lambdas-457`
- Issue: #457 (epic `build-ci-coverage-gate-fidelity`, wave 1)
- Branch: `bug/excludefromcodecoverage-nested-lambdas-457`
- Base branch: `epic/build-ci-coverage-gate-fidelity-integration`
- Merge base: `1c221399a72d9102c357e4d5164f5f0bb5c7fd2e` (recomputed by the reviewer with `git merge-base`; matches the caller-supplied value)
- Head: `0105e71ccf6ce62ea6e7cad28d50ded8a92e2896` (working tree clean)
- Work mode: `full-bug` (persisted marker in `issue.md`; `spec.md` is the sole AC source)
- Reviewer: feature-review agent
- Timestamp: 2026-08-11T01-33

## 1. Audit Scope

The audit scope is the full branch diff `1c221399..0105e71c`: 57 files. Production surface verified by the reviewer against `git diff --numstat`:

- `scripts/vscode/Invoke-MSTestWithCoverage.ClosureFilter.ps1` — new, 389 lines (`wc -l`)
- `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1` — +2/-0 (one dot-source line, one call site)
- `tests/scripts/vscode/Invoke-MSTestWithCoverage.ClosureFilter.Tests.ps1` — new, 443 lines
- `tests/scripts/vscode/Invoke-MSTestWithCoverage.Helpers.Tests.ps1` — +22/-0 (regression case 6)

The remaining 53 files are feature documents (checkbox-only toggles in `spec.md` and `plan.2026-08-10T14-08.md`, verified by diff filtering), 46 evidence artifacts under `<FEATURE>/evidence/`, and 3 follow-up potential entries under `docs/features/potential/`.

PR context artifacts were absent at review start and were regenerated by the reviewer at `artifacts/pr_context.summary.txt` and `artifacts/pr_context.appendix.txt` from `git diff --numstat` / `git diff` (the `collect_pr_context` MCP tool is not available in this repository).

## Rejected Scope Narrowing

None detected. The caller directive requested the full feature-vs-base audit and its stated production surface matches the actual branch diff exactly. The plan trailer contains no scope-narrowing directives.

## 2. Policy Compliance Verdicts

| # | Policy area | Verdict | Evidence |
|---|---|---|---|
| 1 | General Code Change Policy — file size ceiling (500 lines) | PASS | `wc -l`: ClosureFilter.ps1 = 389; Helpers.ps1 = 457; ClosureFilter.Tests.ps1 = 443; Helpers.Tests.ps1 = 490. All four under 500. (Helpers.Tests.ps1 at 490 is near the ceiling; recorded as a non-blocking observation in the code review.) |
| 2 | General Code Change Policy — design principles, separation of concerns | PASS | The filter is a pure XML-to-XML transform in a dedicated file; I/O remains in the existing pipeline. Reviewer read the full 389-line module. |
| 3 | Bugfix Workflow — failing regression test first | PASS | `evidence/regression-testing/expect-fail-run.2026-08-11T01-08.md` records the pre-fix failing run (cases fail on missing commands; case 6 fails with exempt closure lines still present in the merged class). `evidence/regression-testing/pass-after-run.2026-08-11T01-30.md` records the post-fix pass. |
| 4 | Bugfix Workflow — minimal targeted fix | PASS | Helpers.ps1 diff is exactly +2/-0; no opportunistic refactor; deeper design residuals deferred to three follow-up potential entries. |
| 5 | Toolchain loop (format -> analyze -> test, restart on failure) | PASS | `evidence/qa-gates/toolchain-loop.2026-08-11T01-48.md`: 2 iterations; iteration 1 failed at analyze (`PSUseBOMForUnicodeEncodedFile`, fixed at source); iteration 2 clean pass in order with no file changed. Exit codes recorded per step. |
| 6 | Lint baseline — independently reproduced | PASS | Reviewer ran `Invoke-ScriptAnalyzer` over all four changed files: exactly 1 diagnostic, the pre-existing `PSUseSingularNouns` on `Get-CoberturaLineConditionCoverageParts` (Helpers.ps1:141). Zero diagnostics on the new module and both test files. |
| 7 | General Unit Test Policy — independence, isolation, determinism, no temporary files | PASS | All fixtures are inline here-strings; reviewer grep confirmed zero occurrences of `TestDrive`, `New-Item`, `Set-Content`, `Out-File`, `Get-Date`, `Get-Random`, `Start-Process`, or web calls in the new/changed test code and production module. Tests independently re-run by the reviewer: 31 passed, 0 failed. |
| 8 | Test structure — AAA, documented intent, substantive assertions | PASS | Every test carries a scenario comment naming its regression case. XPath predicates and `Should -Contain`/`-Not -Contain` comparisons correctly use unescaped `<`/`>` against parsed attribute values (e.g., ClosureFilter.Tests.ps1:140-143); no assertion compares an escaped entity against a parsed value. |
| 9 | PowerShell standards — `[CmdletBinding()]`, approved verbs, strict mode | PASS | All five new functions are advanced functions with approved verbs; `Set-StrictMode -Version Latest` at module top; `GetAttribute` used deliberately for strict-mode-safe attribute access. |
| 10 | Tonality policy in authored documents | PASS | Evidence and potential entries reviewed are factual and neutral. |

## 3. Load-Bearing Invariants (substantively adjudicated)

| Invariant | Verdict | How verified |
|---|---|---|
| Ordering: filter runs after `//class[@filename]` normalization and before `Merge-CoberturaClassesByFilename` | PASS | (a) Static: call site at Helpers.ps1:427, immediately after the normalization loop and before the merge call at line 428. (b) Probe: reviewer executed a scratch script applying merge-then-filter to a case-6-shaped fixture; the exempt line 406 survived and `lines-valid` stayed inflated, proving a post-merge filter is a silent no-op. (c) Regression case 6 (Helpers.Tests.ps1) drives `ConvertTo-KoverageCoberturaXml` end-to-end and asserts the merged rollup is `'10,11'` with `lines-valid '2'`; under the wrong ordering the rollup would read `'10,11,406,409'` with `lines-valid` 4, so the test fails if the call is moved. |
| Fail-safe retention (no over-exclusion) | PASS | Code paths audited: an unresolved declaring member is always retained (ClosureFilter.ps1:296-300); a closure class with zero `<method>` nodes is skipped (278-280); non-closure classes are never mutated (273-275); class removal requires at least one dropped method, and every drop requires a successfully derived member absent from the presence set. Regression cases 4 (`.ctor` retained) and 9 (`MoveNext`/`.ctor` derive `$null`) pin the behavior. |
| Async guard (`d__` state machines admitted to presence set) | PASS | Presence source 2 regex `<(?<m>[^<>]+)>d__\d+$` (ClosureFilter.ps1:202), end-anchored so a closure-nested state machine cannot admit a spurious member. Regression cases 3 and 7 pin both directions. |
| Purity and idempotence | PASS | Full module read: no file read, no process invocation, no clock, no randomness, no network. Idempotence pinned non-vacuously by regression case 10 (first pass changes the document, second pass does not), with all output streams asserted empty. |
| Test fixture purity | PASS | All fixtures inline here-strings; no on-disk fixtures, no committed `.cs` sources; `evidence/regression-testing/fixture-purity-audit.2026-08-11T01-10.md` corroborates. |

## 4. Coverage Verification (PowerShell — the only language with changed files)

Changed-language enumeration from the branch diff: PowerShell only (4 × `.ps1`). No `.cs`, `.py`, `.ts` files changed on this branch.

| Row | Measured | Verdict |
|---|---|---|
| PowerShell coverage, new file `scripts/vscode/Invoke-MSTestWithCoverage.ClosureFilter.ps1`: 100.00% line (93/93), from committed direct-Pester JaCoCo `evidence/qa-gates/pester-coverage.final-qc.iter2.2026-08-11T01-46.xml`, re-parsed by the reviewer | 100.00% >= 90% new-code floor and >= 85% uniform floor | PASS |
| PowerShell coverage, modified file `scripts/vscode/Invoke-MSTestWithCoverage.Helpers.ps1`: 90.69% line (185/204) vs baseline 90.59% (183/202) from `evidence/baseline/pester-coverage.2026-08-11T00-20.xml`; both added lines (2 and 427) covered (`ci=2`, `ci=1`); the 19 missed lines are identical pre-existing debt untouched by this diff — no regression on changed lines | 90.69% >= 85%, no changed-line regression | PASS |
| PowerShell coverage, changed-file scope overall (both files combined): 93.60% line (278/297) from the same committed JaCoCo evidence | 93.60% >= 85% | PASS |
| PowerShell repo-wide coverage from the bundled artifact `artifacts/pester/powershell-coverage.xml`: reads 0.00% (0 covered / 16075 missed) — an invalid capture; this is open issue #536, a known defect in the bundled `run_poshqc_test` coverage writer, and contradicts the valid direct-Pester captures above | 0.00% < 85% floor as read from that artifact | FAIL — dispositioned non-blocking: the figure is a tooling capture defect (#536), pre-existing on the base branch and unrelated to this diff; the authoritative committed direct-Pester evidence above passes every floor |
| PowerShell branch coverage: the configured tooling (Pester 5.6.1) emits no BRANCH counter in its JaCoCo output, so the 75% branch floor cannot be evaluated for PowerShell; recorded as a measured tooling gap and handed to issue #494 in `evidence/qa-gates/threshold-assessment.2026-08-11T02-00.md` | no branch counter emitted | FAIL by strict floor application — dispositioned non-blocking: measurement gap of the repository's configured tooling, pre-existing, documented, and formally handed to #494 (epic wave 2) which owns threshold reconciliation |

Overall PowerShell coverage verdict for this branch diff, adjudicated from the committed direct-Pester JaCoCo evidence: PASS.

Repository-wide C# figures (recorded for completeness; C# has no changed files on this branch): post-change line-rate 0.855355 (53375/62401) and branch-rate 0.790134 (12541/15872), read verbatim from `evidence/qa-gates/coverage-final-extract.2026-08-11T01-56.md`; both clear the 85%/75% floors. The reviewer did not regenerate `artifacts/csharp/coverage.xml` (no C# in the diff; figures are cited from committed evidence per the review directive).

## 5. Scope Prohibitions (each a blocking finding if violated)

| Prohibition | Verdict | Evidence |
|---|---|---|
| No coverage threshold re-tuned, lowered, raised, or relaxed anywhere | PASS | Diff file list contains no `quality-tiers.yml`, no rules file, no CI config, no runsettings; the only production changes are the two PowerShell pipeline files, neither of which encodes a threshold. `evidence/qa-gates/threshold-assessment.2026-08-11T02-00.md` records the reconciliation handoff to #494 without changing any figure. The iteration-1 84.07% per-module shortfall was remedied by adding tests, not by adjusting a threshold. |
| `CLAUDE.md` and `.claude/rules/**` untouched | PASS | Neither path appears in `git diff --numstat 1c221399..HEAD`; corroborated by the executor's recorded `git status --porcelain -uall -- CLAUDE.md .claude/rules` returning empty. |
| No C# source, `coverage.config`, `TaskMaster.runsettings`, or `scripts/vscode/TaskMaster.cli.runsettings` modified | PASS | No `.cs`, `.config`, or `.runsettings` file appears in the branch diff. |
| Evidence only under `<FEATURE>/evidence/<kind>/` | PASS | See section 6. |

## 6. Evidence Location Compliance

The reviewer scanned the full branch diff for files written under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/evidence/`, or `artifacts/coverage/`: zero occurrences. All 46 evidence files in the diff live under `docs/features/active/2026-08-10-excludefromcodecoverage-nested-lambdas-457/evidence/{baseline, regression-testing, qa-gates, other}/`, all of which are canonical kinds per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. `scripts/dev_tools/validate_evidence_locations.py` does not exist in this repository, so the scan was performed manually against the numstat file list. No violations.

## 7. Known Pre-Existing Conditions (dispositions; none is a finding against this branch)

1. Two failing C# tests (`*ThroughThePumpHost*`, window-handle `InvalidOperationException`) — identical counts before and after (6435/6433/2); tracked as open issue #511. This branch changes no C#.
2. The bundled `artifacts/pester/powershell-coverage.xml` zero-capture — open issue #536; see the honest FAIL row in section 4.
3. `PSUseSingularNouns` on `Get-CoberturaLineConditionCoverageParts` (Helpers.ps1:141) — pre-existing; independently reproduced by the reviewer as the sole analyzer diagnostic; renaming would exceed the two edits fixed by spec AC 13.
4. The `CLAUDE.md` `/p:Nullable=enable` gate — known-defective (issue #522); deliberately omitted by the plan; no C# changed here in any case.
5. Pester 5.6.1 emits no branch counter — see the dispositioned row in section 4 and the #494 handoff.

## 8. Findings Summary

- Blocking findings (FAIL or blocking-PARTIAL): **0**
- Non-blocking dispositions: 2 (section 4: bundled-artifact zero-capture #536; PowerShell branch-coverage measurement gap handed to #494)
- Non-blocking observations: recorded in `code-review.2026-08-11T01-33.md` (rollup-rebuild divergence from the merge path; Helpers.Tests.ps1 nearing the 500-line ceiling; `SupportsShouldProcess` note)

No remediation-inputs artifact is produced: no finding requires remediation.
