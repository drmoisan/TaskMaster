# breadcrumb-router-navigation-defects (Atomic Plan)

- **Issue:** #498 (also closes #440 and #499; #439 is INHERITED as fixed on `main` by PR #605 and is not re-implemented here)
- **Parent:** epic `quickfiler-bug-family` (integration branch `epic/quickfiler-bug-family-integration`)
- **Owner:** drmoisan
- **Last Updated:** 2026-08-25T10-05
- **Status:** Ready for preflight
- **Version:** 2.0 — re-scoped against `spec.md` version 1.1 after PR #605 landed the independent #439 fix
- **Work Mode:** full-bug — `spec.md` is the authoritative acceptance-criteria source (AC-1 through AC-31 and decisions D1 through D9). `user-story.md` is absent by design and is not required.

**Fail-closed evidence rule:** every baseline artifact, regression-testing artifact, QA-gate artifact, and coverage-comparison artifact named below must exist with its required fields before the corresponding checkbox may be ticked. A missing or field-incomplete artifact makes the outcome BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** every command-bearing task states its artifact path. Each artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. Baseline and final-QC test artifacts record numeric coverage values in `Output Summary:`, not placeholders.

---

## Re-Scope Notice (read first)

Version 1.0 of this plan was authored at HEAD `988e819b` and cleared preflight. A separate workstream then fixed issue **#439** independently and merged it to `main` as pull request **#605** (feature commit `c39db103`). `spec.md` was re-scoped to version 1.1 to reconcile with that landed work, and this plan is version 2.0 of the same file, brought into agreement with that spec.

What changed in this plan relative to version 1.0:

- **Deleted** the six tasks that would have re-implemented landed work — the two decision-D6 hazard tests, their RED/fix/GREEN cycle, and the Efc lineage-rendering test. AC-10, AC-12 and AC-13 are RETIRED as inherited-and-verified and are already checked in `spec.md`. Version 1.0's decision-D6b task would have re-derived `SelectedFolderPath` from a retained presented-text map; `SelectRow` already derives it from `row.FilingTarget`, so that task was a defect.
- **Reframed** the provider suffix-match work, the D7 rung-verification task and the Qfc lineage task as the **#440 Qfc prerequisite** they now are, rather than as #439 work. Nothing in decision D7 applies to the Efc surface any more.
- **Added** a dedicated first phase performing the MANDATORY decision-D8 partial-class split of `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`, which stands at 596 lines — 96 over the limit — before this feature adds anything.
- **Rewrote** #440's Efc half to CONSUME the landed active-segment seams on `BreadcrumbRow` (decision D9) rather than add a second selected-node index.
- **Re-cited** every line number against the current worktree.

## Execution Context (read before Phase 0)

This plan was authored in a preparation worktree and is executed later in a DIFFERENT worktree by the epic orchestrator. Every path in this plan is repo-relative. Resolve the workspace root at execution time with `git rev-parse --show-toplevel` and treat every relative path below as relative to that value. Do not substitute any absolute path recorded during planning.

Throughout this plan, `FF` denotes the repo-relative feature folder `docs/features/active/breadcrumb-router-navigation-defects-498`. All task text writes the full repo-relative path; `FF` is used only in this prose.

## Citation Basis (how to read the line numbers in this plan)

Every `file:line` citation below was verified against the current worktree contents, which include pull request #605. Eight files were rewritten by that pull request and their version 1.0 citations were stale:

`QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`, `QuickFiler/Controllers/EfcFormController.cs`, `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs`, `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbRowStateTests.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbDocumentAssets.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbHtmlRenderer.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs`.

**Line-number stability across the Phase 1 split.** The Phase 1 partial-class split relocates a CONTIGUOUS TAIL of `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` — the twelve private members occupying `:410-594`. Every member at or above line 409 keeps its current line number after the split. Tasks in Phases 2 through 6 therefore cite pre-split line numbers for members at or above `:409` and cite the twelve relocated members by NAME and by their new file only.

## File Ownership (binding; never violated)

**OWNED — writable by this plan:**

- `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`
- `QuickFiler/Controllers/KeyboardHandler.cs`
- `QuickFiler/Resources/FolderBreadcrumb.html`
- `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs` (owned but NOT written — decision D5; it is a pre-existing 983-line violation and this plan does not touch it)
- `UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyProvider.cs`
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs`
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs`
- `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs`
- new partial-class siblings of the owned `.cs` files (decision D8), specifically `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs` created by `P1-T2` and `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs` created by `P2-T1`
- the following six existing test files (this explicit enumeration is the sole, authoritative statement of the owned test files):
  - `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs`
  - `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterTests.cs`
  - `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderHierarchyProviderTests.cs`
  - `UtilitiesCS.Test/OutlookObjects/Folder/FolderBreadcrumbBridgeRouterTests.cs`
  - `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbRowStateTests.cs`
  - `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelTests.cs`
- `QuickFiler/QuickFiler.csproj`, `QuickFiler.Test/QuickFiler.Test.csproj`, `UtilitiesCS/UtilitiesCS.csproj` and `UtilitiesCS.Test/UtilitiesCS.Test.csproj`, for `Compile Include` entries of NEW files only, each placed IMMEDIATELY ADJACENT to the existing entry for the file it splits
- everything under `docs/features/active/breadcrumb-router-navigation-defects-498/`

**MUST NOT WRITE:**

- `QuickFiler/Controllers/KbdActions.cs` (feature 444). This is a DIFFERENT file from the owned `QuickFiler/Controllers/KeyboardHandler.cs`, which this plan does own.
- `QuickFiler/Controllers/EfcFormController.cs` (feature 464). Pull request #605 also wrote this file; it is still NOT owned here, and it remains a pre-existing 1084-line violation this feature neither inherits nor worsens.
- `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs` (sibling epic child 501). This is the sole construction site of `FolderBreadcrumbBridgeRouter`, which is why the archive-root route used for the Efc surface is unavailable to the Qfc surface (decision D5).
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs` (written by the landed #439 workstream)
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbDocumentAssets.cs` (written by the landed #439 workstream)
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbHtmlRenderer.cs` (written by the landed #439 workstream)
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs`
- `UtilitiesCS/OutlookObjects/Folder/IFolderHierarchyProvider.cs`
- `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionSession.cs`
- `QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs` (decision D1: must pass UNMODIFIED)
- `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` — the landed #439 regression file. **Read it before authoring any regression test so that no assertion is duplicated.** It is read-only for this feature and must be confirmed still passing, never extended.

Read-only access to every forbidden file is permitted and expected. Writing is not.

## File-Size Constraint (decision D8)

Line counts re-measured against the current worktree at spec version 1.1. The research section Q6c figures used by version 1.0 of this plan are stale and are not used.

| File | Lines now | Version 1.0 figure | Status |
|---|---:|---:|---|
| `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` | 596 | 450 | **OVER the limit already.** Mandatory split, Phase 1. |
| `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` | 531 | did not exist | **PRE-EXISTING violation.** Not owned, not written, not worsened. |
| `QuickFiler/Controllers/EfcFormController.cs` | 1084 | 1086 | **PRE-EXISTING violation.** Not owned, not written, not worsened. |
| `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs` | 983 | 983 | **PRE-EXISTING violation.** Owned but not written (decision D5). |
| `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs` | 485 | 485 | 15 lines of headroom; split pre-authorized. |
| `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs` | 457 | 457 | 43 lines of headroom; split pre-authorized. |
| `QuickFiler/Resources/FolderBreadcrumb.html` | 489 | 489 | 11 lines of headroom; **cannot be split**. Hard constraint. |
| `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs` | 462 | 446 | 38 lines of headroom; split in `P2-T1`. |
| `QuickFiler/Controllers/KeyboardHandler.cs` | 414 | 414 | No change expected. |
| `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs` | 361 | 265 | 139 lines of headroom. |
| `UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyProvider.cs` | 98 | 98 | No risk. |

The three PRE-EXISTING violations are recorded so that a reviewer can distinguish them from anything this feature causes. This plan neither repairs nor worsens them, and no acceptance criterion of this feature asserts anything about their line counts.

Partial-class splits are pre-authorized for the owned `.cs` files. `FolderBreadcrumb.html` cannot be split. A NEW `.cs` file requires a `Compile Include` entry placed IMMEDIATELY ADJACENT to the existing entry for the file it splits, because the item groups are shared with concurrently-executing sibling epic children and a misplaced entry causes a rebase conflict (RISK-5). The item groups are already NOT strictly alphabetical: pull request #605 placed the `BreadcrumbBridgeRouterIssue439Tests.cs` entry at `QuickFiler.Test/QuickFiler.Test.csproj:60`, after the `BreadcrumbBridgeRouterTests.cs` entry at `:59`. Do not re-sort. The six existing owned test files already carry `Compile Include` entries and need no project-file edit.

## C# Toolchain (exact commands, exact order)

1. `dotnet tool run csharpier format .` — verify with `dotnet tool run csharpier check .`
2. `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`
3. `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`
4. `vstest.console.exe <test-assembly-paths> /EnableCodeCoverage`

Constraints the executor must not get wrong:

- `/t:Rebuild` is mandatory. `/t:Build` skips `CoreCompile` on a warm tree, so the analyzer and nullable gates return exit 0 having compiled nothing.
- Do NOT add `/p:Nullable=enable`. It is absent from CI deliberately and conscripts files that never adopted the `#nullable enable` pragma.
- `dotnet tool restore` must run once in the execution worktree before the first csharpier invocation. The manifest is at the repository root (`dotnet-tools.json`, CSharpier pinned to 1.2.6). CSharpier 1.2.6 requires a subcommand; the bare-path form does not run.
- msbuild and vstest MUST be invoked through `pwsh -NoProfile -Command` with explicitly resolved executable paths. A bare bash invocation mangles MSBuild switches and MSBuild fails with MSB1008.
- Quote every `pwsh -NoProfile -Command` payload with OUTER SINGLE quotes and use inner double quotes for literals. A double-quoted payload is expanded by the parent shell before pwsh sees it.
- If any step fails or changes files, restart the loop from step 1.

**Step 1 scoping exception (mandatory).** The MUTATING pass `dotnet tool run csharpier format .` is replaced by a pass scoped to the file list this plan writes. Sibling epic children execute concurrently against the same integration branch, and a repo-wide mutating format would place unrelated files in this feature's diff, breaking AC-30. The READ-ONLY repo-wide verification `dotnet tool run csharpier check .` is run unchanged, exactly as CI does.

**Evidence artifacts are exempt from formatting.** `.csharpierignore` already excludes `**/evidence/**`, `*.trx`, and `*.cobertura.xml`, so writing evidence under the feature folder cannot break the format gate.

## Baseline-Comparison Rule for Whole-Solution Gates (binding)

Sibling epic children execute concurrently against the same integration branch and three pre-existing 500-line violations exist. This plan therefore NEVER asserts an absolute diagnostic count, an absolute test count, or a zero-skip condition over a file or assembly it does not own. Every whole-solution gate is expressed as a comparison against the corresponding Phase 0 baseline artifact:

- **Formatting.** `EXIT_CODE: 0` is required. If and only if the `P0-T11` baseline artifact recorded a non-zero exit code for the identical command, the gate degrades to: no file on this plan's written-file list appears in the unformatted list, and the unformatted list is a subset of the `P0-T11` baseline list. The degradation is recorded explicitly in the task artifact with a pointer to `P0-T11`.
- **Analyzer and nullable gates.** `EXIT_CODE: 0` is required. If and only if the corresponding baseline artifact (`P0-T12` or `P0-T13`) recorded a non-zero exit code for the identical command, the gate degrades to: the total warning and error counts are at or below the baseline counts, and no reported diagnostic names any file on this plan's written-file list. The degradation is recorded explicitly with a pointer to the baseline artifact.
- **Full-suite test run.** The set of FAILING test identifiers must be a subset of the failing set recorded in the `P0-T14` baseline artifact (`BASELINE_FAILURE_SET`), and no failing identifier may belong to any test class this plan writes. Skipped counts are never asserted.

## Command Recipes

Scoped test run (used by every RED and GREEN task). `FILTER` and `RESULTS` are substituted per task; `ASSEMBLY` is `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` or `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`:

```
pwsh -NoProfile -Command '& "scripts\vscode\Invoke-VSBuild.ps1" -Target Build; $vsw = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"; $vs = & $vsw -latest -products * -find "Common7\IDE\Extensions\TestPlatform\vstest.console.exe" | Select-Object -First 1; & $vs "ASSEMBLY" /InIsolation "/TestCaseFilter:FILTER" "/Logger:trx;LogFileName=results.trx" "/ResultsDirectory:RESULTS"; "EXIT_CODE: $LASTEXITCODE"'
```

Every scoped run writes to its OWN results directory so that an earlier `[expect-fail]` run's TRX cannot be mistaken for a later run's. Each task names its directory explicitly.

Rebuild recipe (analyzer and nullable gates):

```
pwsh -NoProfile -Command '$vsw = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"; $mb = & $vsw -latest -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1; & $mb "TaskMaster.sln" /t:Rebuild /m "/p:Configuration=Debug" "/p:Platform=Any CPU" "/p:EnableNETAnalyzers=true" "/p:EnforceCodeStyleInBuild=true"; "EXIT_CODE: $LASTEXITCODE"'
```

Coverage recipe (full suite, Cobertura output; expect a long run, allow it to complete):

```
pwsh -NoProfile -Command '& "scripts\vscode\Invoke-MSTestWithCoverage.ps1" -SearchRoot . -Configuration Debug -CoverageOutput "coverage\coverage.cobertura.xml"; "EXIT_CODE: $LASTEXITCODE"'
```

`-SearchRoot .` must always be supplied explicitly. The raw Cobertura lands in the gitignored `coverage/` directory, so every coverage task copies it into the feature evidence folder.

## Sequencing

Fixed by the spec "Design summary" as re-scoped at version 1.1:

mandatory decision-D8 partial split (Phase 1) -> `#498` (Phase 2) -> `#499` (Phase 3) -> Qfc ancestor-chain prerequisite, decisions D5 and D7 verification (Phase 4) -> decision-D7 ladder for the Qfc filing target (Phase 5) -> `#440` on both surfaces (Phase 6).

The split is MANDATORY and FIRST because `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` is already 596 lines and Phases 2, 3 and 6 all add to it. The split commit is a pure mechanical relocation with no behavior change, so reverting a later fix never requires reverting it.

Per the CLAUDE.md Bugfix Workflow each of the three live defects gets, in order: (a) a failing regression test FIRST tagged `[expect-fail]` with a regression-testing evidence artifact proving it RED, (b) the minimal targeted fix, (c) the same test proving GREEN. The Qfc chain-resolution prerequisite carries the same RED-first obligation (AC-27).

**#439 is not implemented by this plan.** Its lineage resolution, its Efc mouse gesture and its separator glyph all landed on `main` under pull request #605. AC-10, AC-12 and AC-13 are retired as inherited-and-verified and are already checked in `spec.md`. No task below re-implements any of them, and `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` is read-only.

**Decision D9 choice, made here and recorded by `P6-T20`.** Right's descent transition — moving selection back toward the leaf — is implemented by **descend by child activation**: after Right has expanded the active segment's children, a subsequent Right selects a child through the landed `BreadcrumbRow.GetActiveChild(int)` (`BreadcrumbRow.cs:175-188`) and the router's `SelectHierarchyPath`, which is exactly how the landed mouse gesture descends (`BreadcrumbBridgeRouter.cs:429-441`). This adds NO member to `BreadcrumbRow` and does not weaken `ActivateSegment`'s guard, whose leaf-index refusal at `BreadcrumbRow.cs:156` is what makes a descent through `ActivateSegment` impossible. The alternative permitted mechanism — a new owned transition on `BreadcrumbRow` — is rejected as the larger change for no additional capability.

**Conditional branches.** Only the following tasks are permitted a conditional branch, and each states its condition in its own task text: `P5-T4` and `P5-T5` (the decision-D7 ladder rungs, gated by the rung recorded in `P4-T1`), and `P8-T2`, `P8-T3`, `P8-T4` and `P8-T5` (the baseline-comparison degradation described under "Baseline-Comparison Rule for Whole-Solution Gates"). Every other task in this plan is unconditional and must execute.

**Out of scope for this plan** (a later epic phase owns them): PR creation, PR body authoring, CI monitoring, feature-review or audit artifacts, merging, rebasing, and branch operations beyond recording the baseline commit.

---

### Phase 0 — Baseline Capture and Toolchain Bootstrap

- [ ] [P0-T1] Read `CLAUDE.md` at the repository root in full, with attention to the Bugfix Workflow section and the C# Toolchain section. Acceptance: the file has been read end to end and the four toolchain commands plus the `/t:Rebuild` and no-`/p:Nullable=enable` constraints are quoted into the Phase 0 read artifact drafted in `P0-T9`.
- [ ] [P0-T2] Read `.claude/rules/general-code-change.md` in full. Acceptance: the 500-line file-size limit and the mandatory toolchain-loop restart rule are quoted into the Phase 0 read artifact drafted in `P0-T9`.
- [ ] [P0-T3] Read `.claude/rules/general-unit-test.md` in full. Acceptance: the coverage thresholds and the Coverage Exclusion Policy are quoted into the Phase 0 read artifact drafted in `P0-T9`.
- [ ] [P0-T4] Read `.claude/rules/csharp.md` in full. Acceptance: the C#-specific rules that layer on top of the general code-change policy are summarised into the Phase 0 read artifact drafted in `P0-T9`, including any toolchain or test-framework constraint that differs from or extends CLAUDE.md.
- [ ] [P0-T5] Read `.claude/rules/tonality.md` in full. Acceptance: the professional-tone requirement is quoted into the Phase 0 read artifact drafted in `P0-T9`.
- [ ] [P0-T6] Read `.claude/rules/plan-acceptance-gates.md` in full. Acceptance: the G1 through G6 rule table is summarised into the Phase 0 read artifact drafted in `P0-T9`.
- [ ] [P0-T7] Read the feature inputs in this order: `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md` (version 1.1, the authority), `docs/features/active/breadcrumb-router-navigation-defects-498/issue.md`, and `docs/features/active/breadcrumb-router-navigation-defects-498/research/2026-08-24T09-50-breadcrumb-router-navigation-defects.md`. The research artifact is a dated record of HEAD `988e819b` and is SUPERSEDED in the sections listed in the spec's "Research sections superseded by the landed work" table; where the two disagree the spec governs. Acceptance: all 31 acceptance criteria identifiers AC-1 through AC-31 and all nine decision identifiers D1 through D9 are listed in the Phase 0 read artifact drafted in `P0-T9`, and that artifact records which of AC-10, AC-12 and AC-13 are already checked in the spec as retired inherited-and-verified criteria.
- [ ] [P0-T8] Read `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` in full (531 lines, seven test methods, registered at `QuickFiler.Test/QuickFiler.Test.csproj:60`) and record the name and the asserted behavior of each of its seven test methods, so that no regression test authored later in this plan duplicates an assertion it already makes. Also read the landed test method `LeafExpand_UsesBoundActiveSegmentKeyWithoutResolvingAgain` at `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs:377-410`. This file is MUST-NOT-WRITE. Acceptance: the Phase 0 read artifact drafted in `P0-T9` lists all seven method names with one line each describing what each asserts, plus the behavior pinned by `LeafExpand_UsesBoundActiveSegmentKeyWithoutResolvingAgain`.
- [ ] [P0-T9] Write the policy-read artifact to `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/phase0-instructions-read.md`. Acceptance: the file exists and contains a `Timestamp:` field, a `Policy Order:` field naming the reading order used, and an explicit list of every file read in `P0-T1` through `P0-T8`.
- [ ] [P0-T10] Resolve the workspace root and record the branch and commit baseline. Command: `pwsh -NoProfile -Command 'git rev-parse --show-toplevel; git rev-parse --abbrev-ref HEAD; git rev-parse HEAD; git status --porcelain --untracked-files=all; "EXIT_CODE: $LASTEXITCODE"'`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t10-branch-commit.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the resolved workspace root, the branch name, the full 40-character HEAD commit sha, and the porcelain status output verbatim.
- [ ] [P0-T11] Bootstrap the execution worktree toolchain. Command: `pwsh -NoProfile -Command 'dotnet tool restore; & "scripts\vscode\Invoke-Restore.ps1" -Configuration Debug; "EXIT_CODE: $LASTEXITCODE"'`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t11-restore-bootstrap.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` confirming CSharpier 1.2.6 was restored and the NuGet restore completed; `EXIT_CODE:` is `0`.
- [ ] [P0-T12] Capture the baseline formatting gate. Command: `pwsh -NoProfile -Command 'dotnet tool run csharpier check .; "EXIT_CODE: $LASTEXITCODE"'`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t12-csharpier-check.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the observed exit code and, when the exit code is non-zero, the complete list of files CSharpier reports as unformatted. That list is the pre-existing set consumed by the "Baseline-Comparison Rule" and must be reported to the orchestrator; this plan does not reformat any file on it that is outside the owned set. When the observed exit code is non-zero, declare `ExpectedExitCode:` with that integer in the artifact so it normalizes correctly in downstream PR-body collection.
- [ ] [P0-T13] Capture the baseline analyzer gate using the Rebuild recipe above. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t13-analyzer-rebuild.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the observed exit code and the total warning and error counts reported by MSBuild. These counts are the baseline consumed by the "Baseline-Comparison Rule". When the observed exit code is non-zero, declare `ExpectedExitCode:` with that integer in the artifact.
- [ ] [P0-T14] Capture the baseline nullable and type-check gate. Command: the Rebuild recipe with the two analyzer properties replaced by `"/p:TreatWarningsAsErrors=true"`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t14-nullable-rebuild.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the observed exit code and the total error count. The command must not contain `/p:Nullable=enable`. When the observed exit code is non-zero, declare `ExpectedExitCode:` with that integer in the artifact.
- [ ] [P0-T15] Capture the baseline full-suite test and coverage run using the Coverage recipe above, then copy `coverage/coverage.cobertura.xml` to `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t15-coverage.cobertura.xml`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t15-coverage-test.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the total, passed, failed and skipped test counts; the fully qualified identifier of EVERY failing test under the heading `BASELINE_FAILURE_SET` (the heading is written even when the set is empty); and the NUMERIC repository-wide line rate read from the `line-rate` attribute of the root coverage element of the copied Cobertura file, expressed as a percentage to two decimal places. Every value must be MEASURED in this execution worktree; no figure inherited from version 1.0 of this plan or from any other feature folder is acceptable, because pull request #605 changed the coverage denominator by removing an `[ExcludeFromCodeCoverage]` attribute from `QuickFiler/Controllers/EfcFormController.cs` (1084 lines). Placeholder values are not acceptable. When the observed exit code is non-zero, declare `ExpectedExitCode:` with that integer in the artifact.
- [ ] [P0-T16] Record the ownership and file-size baseline. For each of the eight owned production files and the six owned test files listed in "File Ownership", record its current line count; for each of the ten forbidden files, record its current line count and its git object id. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t16-ownership-and-line-counts.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` containing one row per file with its line count measured with `(Get-Content -LiteralPath $path).Count`, and the measured counts are compared against the "File-Size Constraint" table above, with any divergence from that table reported explicitly. The artifact must state whether `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` is over 500 lines at baseline.

### Phase 1 — Mandatory Partial-Class Split of BreadcrumbBridgeRouter.cs (decision D8)

- [ ] [P1-T1] Capture the PRE-SPLIT pass count for the three router test classes, so that the post-split comparison in `P1-T3` is non-vacuous. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~BreadcrumbBridgeRouter`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/trx/p1-t1`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p1-t1-router-tests-pre-split.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the total, passed and failed counts read from the TRX at `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/trx/p1-t1/results.trx`, plus the fully qualified identifier of every failing test. Those three numbers are the comparison basis for `P1-T3` and are not asserted against any absolute value here.
- [ ] [P1-T2] Perform the mandatory decision-D8 partial-class split of `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`. Change the type declaration at `:19` from a sealed class to a sealed PARTIAL class, and RELOCATE, unchanged in body, the contiguous tail of twelve private members occupying `:410-594` — `ActivateSegment(BreadcrumbRow, int)`, `ActivateChild`, `FetchChainAsync`, `SelectRow`, `SelectHierarchyPath`, `ToArchiveRelativePath`, `PostRowRender`, `PostOutbound`, `DeliverDocument`, `FindRow`, `IndexOf` and `FindSelectable` — into a NEW file `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs` declaring the same sealed partial class in namespace `QuickFiler.Controllers`, carrying `#nullable enable` on line 1 and only the `using` directives its own members need. This is a PURE MECHANICAL RELOCATION: no member body, signature, accessibility or ordering within the moved set may change, and no member outside the moved set may move. Add `<Compile Include="Controllers\BreadcrumbBridgeRouter.Selection.cs" />` to `QuickFiler/QuickFiler.csproj` on the line IMMEDIATELY AFTER the existing `<Compile Include="Controllers\BreadcrumbBridgeRouter.cs" />` entry at `:290`; do not re-sort the item group. Acceptance: `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs` exists; `QuickFiler/QuickFiler.csproj` contains the literal `Controllers\BreadcrumbBridgeRouter.Selection.cs` on the line immediately following the line containing the literal `Controllers\BreadcrumbBridgeRouter.cs`; `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` measures at or under 460 lines and `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs` measures at or under 220 lines, both with `(Get-Content -LiteralPath $path).Count`; and the analyzer Rebuild recipe returns `EXIT_CODE: 0`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p1-t2-router-partial-split.md`.
- [ ] [P1-T3] Prove the split changed no behavior. Re-run the same three router test classes. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~BreadcrumbBridgeRouter`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p1-t3`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p1-t3-router-tests-post-split.md`. Acceptance: the TRX at `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p1-t3/results.trx` records a total count and a passed count each EQUAL to the corresponding value recorded in the `P1-T1` artifact, and a failing-test identifier set equal to the set recorded in `P1-T1`. Any divergence means the relocation was not mechanical and the task fails.
- [ ] [P1-T4] Confirm the split left the pre-relocation line numbering intact for every member at or above `:409`, which the rest of this plan cites. Read `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` and verify that `ProcessInboundAsync` still opens at `:229`, its `SegmentDoubleClick` arm still occupies `:241-247`, `OnHostMessageReceived` still occupies `:266-277`, `HandleArrowKeyAsync` still occupies `:304-339`, `ExpandLeafAsync` still occupies `:364-408`, `BindRowsAsync` still ends with `_selectedRowId = null;` at `:136`, and `SelectFirstRow` still opens at `:192`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p1-t4-citation-stability.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving one row per checked member with its observed opening line and whether it matches the expected value; if any row diverges, the artifact records the corrected line and the executor uses the corrected value for the remainder of the plan.

### Phase 2 — Defect #498: Out-of-Range segmentIndex Escapes the async void Boundary

- [ ] [P2-T1] Create the partial sibling `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs` declaring `public partial class BreadcrumbBridgeRouterQueueTests` in namespace `QuickFiler.Test.Controllers` with no `[TestClass]` attribute of its own, change the existing declaration at `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs:23` to `public partial class BreadcrumbBridgeRouterQueueTests`, and add `<Compile Include="Controllers\BreadcrumbBridgeRouterQueueTests.Part2.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` on the line IMMEDIATELY AFTER the existing `Controllers\BreadcrumbBridgeRouterQueueTests.cs` entry at `:58`; do not re-sort the item group. Rationale: the existing file is 462 of 500 lines (38 lines of headroom) and receives six new regression test methods across Phases 2 and 3. Acceptance: `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs` remains at or under 500 lines, `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs` exists, `QuickFiler.Test/QuickFiler.Test.csproj` contains the literal `Controllers\BreadcrumbBridgeRouterQueueTests.Part2.cs` on the line immediately following the line containing the literal `Controllers\BreadcrumbBridgeRouterQueueTests.cs`, and the analyzer Rebuild recipe returns `EXIT_CODE: 0`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p2-t1-queue-tests-partial-split.md`.
- [ ] [P2-T2] Add exactly two regression test methods to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs`, named verbatim `SegmentDoubleClick_IndexAboveRange_ViaHostEvent_DoesNotThrowAndLeavesStateUnchanged` and `SegmentDoubleClick_NegativeIndex_ViaHostEvent_DoesNotThrowAndLeavesStateUnchanged`. Each reuses the existing `[TestInitialize] Setup()` arrange block at `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs:34-74`, calls the `Bind()` helper at `:86-96` to produce the two-segment `row-0` built from the `Segment` helper at `:76-84`, captures `int postedBefore = _posted.Count;`, then raises the host event through the async-void seam `_host.Raise(h => h.MessageReceived += null, _host.Object, ...)` following the pattern at `:193-205`, with a `segmentDoubleClick` payload carrying `segmentIndex` of 99 and of -1 respectively, and asserts with FluentAssertions that the raise does not throw and that `_posted.Count` equals `postedBefore`. Outbound assertions stay raw-JSON substring assertions; `QuickFiler.Test` carries no Newtonsoft reference. Neither method may duplicate an assertion made by the read-only `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` as catalogued in `P0-T8`. Acceptance: both method names are present in the new file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P2-T3] [expect-fail] Run the two #498 regression tests and record them RED. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~SegmentDoubleClick_IndexAboveRange_ViaHostEvent|FullyQualifiedName~SegmentDoubleClick_NegativeIndex_ViaHostEvent`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p2-t3`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p2-t3-498-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p2-t3/results.trx` records total 2, passed 0, failed 2, and the artifact's `Output Summary:` names `System.ArgumentOutOfRangeException` as the observed failure cause for both. Satisfies AC-25 fail-before.
- [ ] [P2-T4] Apply the minimal #498 fix in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`: in the `SegmentDoubleClick` arm of `ProcessInboundAsync` at `:241-247`, replace the null-forgiving dereference `message.SegmentIndex!.Value` at `:242` with a `HasValue` check, and add a range guard rejecting any index outside `[0, segments.Count - 1]` before `row.CollapseAfter` is called, logging the rejected index at `Error` through the existing `log4net` pattern used at `:235` and `:336`, and returning without a transition and without a render post. Do not modify `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs`. Do not widen the `catch (BreadcrumbMessageException)` in `OnHostMessageReceived` at `:266-277`. Do not modify the landed `SegmentActivate` arm at `:248-250` or the landed `RenderedChildActivate` arm at `:251-253`; both call members that validate internally and return `false` or `null` rather than throwing. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P2-T5] Re-run the two #498 regression tests and record them GREEN. Same recipe and FILTER as `P2-T3`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p2-t5`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p2-t5-498-green.md`. Acceptance: the TRX at that results directory records total 2, passed 2, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-1 and AC-25 pass-after.
- [ ] [P2-T6] Add the #498 control and short-circuit tests to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs`, named verbatim `SegmentDoubleClick_ValidIndex_ViaHostEvent_CollapsesRowAndPostsRender` and `SegmentDoubleClick_BannerRow_ViaHostEvent_ShortCircuitsBeforeRangeCheck`. The second exercises the banner short-circuit at `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs:202-205`, which returns `false` before the range check. Run both. Scoped test run recipe, FILTER `FullyQualifiedName~SegmentDoubleClick_ValidIndex_ViaHostEvent|FullyQualifiedName~SegmentDoubleClick_BannerRow_ViaHostEvent`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p2-t6`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p2-t6-498-controls.md`. Acceptance: the TRX records total 2, passed 2, failed 0. Satisfies the AC-3 valid-index clause.
- [ ] [P2-T7] Verify the #498 invariants that must survive. Read `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs` and confirm `CollapseAfter` at `:200-229`, including its documented XML contract at `:190-199` and the `ArgumentOutOfRangeException` throw at `:207-214`, is byte-identical to its state recorded in `P0-T16`; read `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` and confirm `OnHostMessageReceived` at `:266-277` still has exactly one catch clause and that clause is `catch (BreadcrumbMessageException)`; confirm the new `Error` log site exists in the `SegmentDoubleClick` arm. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p2-t7-498-invariants.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` stating for each of the three checks whether it holds, with the file and line range inspected. Satisfies AC-2 and the AC-3 no-broad-catch clause.

### Phase 3 — Defect #499: Stale SelectedFolderPath After Re-Bind

- [ ] [P3-T1] Add exactly two regression test methods to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs`, named verbatim `BindRowsAsync_AfterSelection_ClearsSelectedFolderPath` and `BindRowsAsync_AfterSelection_RaisesSelectedFolderPathChangedWithNull`. Both reuse the existing `Setup()` arrange block, call the `Bind()` helper, then `Inbound("{\"type\":\"rowSelected\",\"rowId\":\"row-0\"}")` using the existing `Inbound` helper at `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs:98-101` whose call idiom appears at `:111` and `:189`, then call `Bind()` a SECOND time. The `Bind()` helper is re-entrant; note that the "double-`Bind` pattern" cited by version 1.0 of this plan no longer exists in the file, so the second bind must be introduced by these tests themselves. The first test asserts `_router.SelectedFolderPath.Should().BeNull()`. The second subscribes before the second bind with the idiom `string observed = "sentinel"; _router.SelectedFolderPathChanged += (s, path) => observed = path;` and asserts `observed.Should().BeNull()`. Acceptance: both method names are present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P3-T2] [expect-fail] Run the two #499 regression tests and record them RED. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~BindRowsAsync_AfterSelection_ClearsSelectedFolderPath|FullyQualifiedName~BindRowsAsync_AfterSelection_RaisesSelectedFolderPathChangedWithNull`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p3-t2`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p3-t2-499-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 2, passed 0, failed 2. Satisfies AC-26 fail-before.
- [ ] [P3-T3] Apply the minimal #499 fix in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`: in the internal `BindRowsAsync` overload at `:92-138`, alongside the existing `_selectedRowId = null;` at `:136`, set the `SelectedFolderPath` property declared at `:59` to `null`, and raise `SelectedFolderPathChanged` (declared at `:62`) with `(this, null)` ONLY when the previous value was non-null. Do not call `SelectFirstRow` (`:192-199`) from `BindRowsAsync`. Do not change the landed `ToHierarchyPath` call at `:119` or the landed `AttachSegmentKeys` call at `:135`. Do not change `DeliverDocument`, `SelectRow` or `SelectHierarchyPath`, all three of which were relocated to `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs` by `P1-T2`; in particular `SelectRow`'s derivation of `SelectedFolderPath` from `row.FilingTarget` landed with pull request #605 and is PRESERVED unchanged. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P3-T4] Re-run the two #499 regression tests and record them GREEN. Same recipe and FILTER as `P3-T2`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p3-t4`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p3-t4-499-green.md`. Acceptance: the TRX records total 2, passed 2, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-26 pass-after.
- [ ] [P3-T5] Add the two #499 write-site preservation tests to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs`, named verbatim `SelectRow_StillAssignsFilingTargetToSelectedFolderPath` and `SelectHierarchyPath_StillAssignsArchiveRelativePathToSelectedFolderPath`, confirming that both existing `SelectedFolderPath` write sites still assign the value they assign today. Run both. Scoped test run recipe, FILTER `FullyQualifiedName~SelectRow_StillAssignsFilingTargetToSelectedFolderPath|FullyQualifiedName~SelectHierarchyPath_StillAssignsArchiveRelativePathToSelectedFolderPath`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p3-t5`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p3-t5-499-write-sites.md`. Acceptance: the TRX records total 2, passed 2, failed 0. Satisfies the AC-4 write-site clause.
- [ ] [P3-T6] Add the two #499 no-side-effect tests to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs`, named verbatim `BindRowsAsync_WithNoPriorSelection_RaisesNoSelectedFolderPathChangedEvent` and `BindRowsAsync_DoesNotAutoSelectAnyRow`, and run them. Scoped test run recipe, FILTER `FullyQualifiedName~BindRowsAsync_WithNoPriorSelection_RaisesNoSelectedFolderPathChangedEvent|FullyQualifiedName~BindRowsAsync_DoesNotAutoSelectAnyRow`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p3-t6`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p3-t6-499-preservation.md`. Acceptance: the TRX records total 2, passed 2, failed 0. Satisfies AC-5 and AC-6.
- [ ] [P3-T7] Confirm the pre-existing queue tests are unaffected. Run the existing test method `MalformedInboundJson_ThrowsCodecExceptionWithoutCorruptingState` at `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs:175-191` and the existing test method `MalformedInboundJson_ViaHostEvent_IsContainedAtTheBoundary` at `:194-205`, neither of which may be edited. Scoped test run recipe, FILTER `FullyQualifiedName~MalformedInboundJson_ThrowsCodecExceptionWithoutCorruptingState|FullyQualifiedName~MalformedInboundJson_ViaHostEvent_IsContainedAtTheBoundary`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p3-t7`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p3-t7-499-no-regression.md`. Acceptance: the TRX records total 2, passed 2, failed 0, and neither method body was edited.

### Phase 4 — Qfc Ancestor-Chain Prerequisite for #440 (decisions D5 and D7)

This phase is NOT #439 work. #439's Efc lineage resolution landed on `main` under pull request #605 and is inherited, not re-implemented. The Efc router supplies a full hierarchy path via `ToHierarchyPath` (`QuickFiler/Controllers/BreadcrumbBridgeRouter.cs:140-163`), which the exact first pass of `ResolveLeafKeyAsync` already resolves. The Qfc router still passes the archive-relative stem `row.Score.Value.FolderPath` (`UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs:49`) to `ResolveLeafKeyAsync` (`:53`) and therefore still renders leaf-only. This phase closes that residual gap because #440's Qfc half needs a resolved multi-segment chain to have a parent to navigate to; on a one-segment row both Left and Right are permanent no-ops.

- [ ] [P4-T1] Perform the decision-D7 read-only verification and RECORD which rung applies. Read `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs` `CreateFallbackRow` at `:245-255` and `ReplaceRowsPreservingSession` at `:474`, and read `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs` `BreadcrumbStateRow` at `:14` including all of its constructors and its `Chain` property. Determine whether the presented archive-relative stem can be preserved into the value that `BreadcrumbSelectionMap.GetSelectedFolder` reads at `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs:109` WITHOUT writing `BreadcrumbSelectionMap.cs`. The in-repo precedent for the shape is the landed `BreadcrumbRow.FilingTarget` (`UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs:88`) — an immutable per-row filing target held independently of the resolved chain — but it exists on the Efc row type, so whether the Qfc `BreadcrumbStateRow` can carry an equivalent is exactly what this read must settle. Write nothing to any production file in this task. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p4-t1-d7-rung-verification.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that quotes the constructor signatures of `BreadcrumbStateRow`, quotes the body of `CreateFallbackRow`, and ends with a line reading exactly one of `D7 RUNG SELECTED: 1`, `D7 RUNG SELECTED: 2`, or `D7 RUNG SELECTED: 3`, followed by the reasoning that selects it. This recorded value is the sole input to the branch conditions in `P5-T4` and `P5-T5`.
- [ ] [P4-T2] Add one regression test method to `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderHierarchyProviderTests.cs`, named verbatim `ResolveLeafKeyAsync_ArchiveRelativeStem_ResolvesToUniqueSuffixMatchNode`. It builds a real `OutlookFolderHierarchyProvider` over the existing `ServiceReturning` mock helper at `:231-280`, constructs snapshot nodes with REALISTIC full paths (the leaf node's `FolderPath` is a store-qualified path ending in the two segments `Projects` then `Alpha` under an `Archive` root, not a display name), calls `ResolveLeafKeyAsync` with the archive-relative stem, and asserts with FluentAssertions that the returned key is not null and its `FolderPath` equals the leaf node's full path. Do not reuse the existing `Node` helper's `displayName`-as-`relativePath` shorthand at `:275` for this test; pass a realistic relative path. Acceptance: the method name is present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P4-T3] [expect-fail] Run the Qfc prerequisite provider regression test and record it RED. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~ResolveLeafKeyAsync_ArchiveRelativeStem_ResolvesToUniqueSuffixMatchNode`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p4-t3`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p4-t3-qfc-prereq-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 1, passed 0, failed 1, and the artifact's `Output Summary:` states that the observed return value was null. Satisfies AC-27 fail-before.
- [ ] [P4-T4] Apply the minimal decision-D5 provider fix in `UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyProvider.cs` `ResolveLeafKeyAsync` (signature at `:52-55`, body at `:56-71`): keep the existing exact `OrdinalIgnoreCase` equality at `:66-68` as the FIRST pass, UNCHANGED, then add a second pass that enumerates `snapshot.NodesByKey.Values` and accepts a node whose `FolderPath` ends with a directory separator followed by the requested path, compared `OrdinalIgnoreCase`, and accepts the match ONLY when exactly one node qualifies. The uniqueness requirement is the safety property. On zero or multiple candidates, log at `Error` through the existing `log4net` pattern and return null, preserving today's single-segment rendering. Leave the early return for empty or whitespace input at `:57-60` unchanged. The second pass must be unreachable whenever the first pass succeeds, so that the change is a strict no-op for the Efc surface. Do not add any member to `UtilitiesCS/OutlookObjects/Folder/IFolderHierarchyProvider.cs`. Do not write `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs`. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P4-T5] Re-run the Qfc prerequisite provider regression test and record it GREEN. Same recipe and FILTER as `P4-T3`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p4-t5`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p4-t5-qfc-prereq-green.md`. Acceptance: the TRX records total 1, passed 1, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-8 and AC-27 pass-after.
- [ ] [P4-T6] Add the two decision-D5 boundary tests to `UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderHierarchyProviderTests.cs`, named verbatim `ResolveLeafKeyAsync_EfcFullHierarchyPath_ResolvesByExactFirstPassWithoutSuffixFallback` and `ResolveLeafKeyAsync_AmbiguousStemWithDecoyNode_ReturnsNullAndLogsError`. The first supplies the value shape produced by the landed `BreadcrumbBridgeRouter.ToHierarchyPath` (`QuickFiler/Controllers/BreadcrumbBridgeRouter.cs:140-163`) — a full store-qualified Outlook path — and asserts it resolves and that the suffix fallback is never reached, so the decision-D5 change is proved a strict no-op for the Efc surface. The second constructs a decoy node under an `Inbox` root alongside the node under the `Archive` root, both ending in the same two segments, and asserts the method returns null so the row keeps today's single-segment fallback rendering. Run both. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~ResolveLeafKeyAsync_EfcFullHierarchyPath_ResolvesByExactFirstPassWithoutSuffixFallback|FullyQualifiedName~ResolveLeafKeyAsync_AmbiguousStemWithDecoyNode_ReturnsNullAndLogsError`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p4-t6`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p4-t6-qfc-prereq-boundaries.md`. Acceptance: the TRX records total 2, passed 2, failed 0. Satisfies AC-7 and AC-9.
- [ ] [P4-T7] Add the Qfc lineage resolution test to `UtilitiesCS.Test/OutlookObjects/Folder/FolderBreadcrumbBridgeRouterTests.cs`, named verbatim `SetSuggestionsAsync_StrictProvider_ResolvesMultiSegmentChain`, using the existing strict `ProviderMock` pattern at `:51-70` with `MockBehavior.Strict` and per-path setups so that resolving the wrong path form throws rather than silently succeeding, and asserting the resulting row's chain has more than one segment in root-to-leaf order. Run it. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~SetSuggestionsAsync_StrictProvider_ResolvesMultiSegmentChain`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p4-t7`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p4-t7-qfc-lineage.md`. Acceptance: the TRX records total 1, passed 1, failed 0. Satisfies AC-11.

### Phase 5 — Decision D7 Ladder: Qfc Filing-Target Preservation

Re-scoped with the spec at version 1.1: this ladder applies to the **Qfc surface only**. The Efc analogue of this hazard was resolved on `main` by the landed immutable `BreadcrumbRow.FilingTarget` (`UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs:88`) and the landed `SelectRow` derivation from it. Nothing in decision D7 applies to the Efc surface any more.

- [ ] [P5-T1] Add the Qfc filing-target regression test to `UtilitiesCS.Test/OutlookObjects/Folder/FolderBreadcrumbBridgeRouterTests.cs`, named verbatim `SelectedFolder_ChainResolvedToFullPath_RemainsPresentedStem`. It arranges a strict provider that resolves the presented archive-relative stem to a chain whose leaf segment carries the full store-qualified path, selects that row through the router, and asserts with FluentAssertions that the Qfc selected-folder value equals the presented stem. Acceptance: the method name is present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P5-T2] [expect-fail] Run the Qfc filing-target regression test and record it RED. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~SelectedFolder_ChainResolvedToFullPath_RemainsPresentedStem`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p5-t2`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p5-t2-d7-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 1, passed 0, failed 1, and the artifact's `Output Summary:` states the observed value was the store-qualified path.
- [ ] [P5-T3] RUNG 1 (PREFERRED). Preserve the presented stem through the OWNED files `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs` (`CreateFallbackRow` at `:245-255`, `ReplaceRowsPreservingSession` at `:474`) and `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs` (`BreadcrumbStateRow` at `:14`), following the shape of the landed `BreadcrumbRow.FilingTarget` precedent — an immutable per-row filing target held independently of the resolved chain — so that the value read at `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs:109` is the presented stem and `BreadcrumbSelectionMap.cs` needs no change. Then re-run the test. Scoped test run recipe, same FILTER as `P5-T2`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p5-t3`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p5-t3-d7-rung1-green.md`. Acceptance: the TRX records total 1, passed 1, failed 0, `EXIT_CODE:` is `0`, and `git status --porcelain --untracked-files=all -- UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs` produces no output. Satisfies the AC-14 rung-1 criterion.
- [ ] [P5-T4] RUNG 2 (FALLBACK). CONDITIONAL: this task applies ONLY IF the artifact written by `P4-T1` records the line `D7 RUNG SELECTED: 2`, meaning rung 1 is not achievable in owned files. If the recorded line is `D7 RUNG SELECTED: 1` this task is recorded as NOT APPLICABLE with a pointer to the `P4-T1` artifact and no code is written. When it applies: change `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs` so the Qfc router does NOT consume the newly-resolved chain for the filing-target path, explicitly preserving today's Qfc filing behavior; replace the test added in `P5-T1` with one asserting Qfc filing behavior is unchanged from the pre-change baseline; and record the deliberate limitation in the RISK-1 section of `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p5-t4-d7-rung2.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that either states NOT APPLICABLE with the `P4-T1` citation, or records the replacement test passing and the spec limitation text added. Satisfies the AC-14 rung-2 criterion when it applies.
- [ ] [P5-T5] RUNG 3 (HALT). CONDITIONAL: this task applies ONLY IF the artifact written by `P4-T1` records the line `D7 RUNG SELECTED: 3`, meaning neither rung 1 nor rung 2 is achievable. If the recorded line is `D7 RUNG SELECTED: 1` or `D7 RUNG SELECTED: 2` this task is recorded as NOT APPLICABLE with a pointer to the `P4-T1` artifact and execution continues. When it applies: STOP execution of this plan, write no further production code, write nothing to `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs`, and report a BLOCKING cross-feature dependency on `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs:109` to the epic orchestrator. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p5-t5-d7-rung3-halt.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` that either states NOT APPLICABLE with the `P4-T1` citation, or records the halt, the blocking-dependency citation, and confirmation via `git status --porcelain --untracked-files=all -- UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs` producing no output. Satisfies the AC-14 rung-3 criterion when it applies.
- [ ] [P5-T6] Record the rung actually taken in `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md`. Replace the RISK-1 placeholder so that the phrase `to be recorded during execution` no longer appears anywhere in that file, and state the rung number, the read-only evidence that selected it, and the path of the `P4-T1` artifact. The literal to be removed appears verbatim in the spec's RISK-1 entry today as `to be recorded during execution`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p5-t6-d7-rung-recorded.md`. Acceptance: a fixed-string search of `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md` for the literal `to be recorded during execution` returns zero matches, and the RISK-1 section names the rung and cites the `P4-T1` artifact path. Satisfies the AC-14 recording clause.

### Phase 6 — Defect #440: Left and Right Tree Navigation on Both Surfaces

Per decision D9, #440's Efc half CONSUMES the active-segment seams that landed with pull request #605 on `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs` — `ActiveSegmentIndex` (`:94`), `ActiveSegment` (`:97-98`), `ActiveSegmentKey` (`:101-105`), `SetSegmentKey` (`:131-144`), `ActivateSegment` (`:151-172`), `GetActiveChild` (`:175-188`). No second selected-node index is added. No `ResolveLeafKeyAsync` call is reintroduced on the expansion path: the landed `ExpandLeafAsync` (`QuickFiler/Controllers/BreadcrumbBridgeRouter.cs:364-408`) reads `row.ActiveSegmentKey` at `:375` and makes a SINGLE `GetImmediateSubfoldersAsync` call at `:384-385`, and the landed test `LeafExpand_UsesBoundActiveSegmentKeyWithoutResolvingAgain` (`QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs:377-410`) pins that. `ActivateSegment`'s guard is not weakened: it refuses a non-suggestion row, a negative index, the LEAF index (`:156`), an index with no attached key, and the already-active index, and it resets `_leafChildren` and `IsLeafExpanded` on success (`:169-170`).

- [ ] [P6-T1] Add the two Efc Left-transition tests to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterTests.cs`, named verbatim `HandleArrowKey_LeftOnMultiSegmentRow_ActivatesParentSegment` and `HandleArrowKey_RepeatedLeft_WalksToRootThenFallsThroughToExistingBehavior`. Both drive the router through an `arrowKey` inbound message with key `Left` on a row whose resolved chain has more than one segment, using the existing `SetupProviderChain` helper at `:77-106` and the existing raw-string assertion shape at `:133-135`. The first asserts `ActiveSegmentIndex` decreases by exactly one. The second asserts repeated Left presses walk toward the root until `ActivateSegment` refuses, at which point the decision-D1 fall-through to the existing `row.LeftArrow()` behavior runs. Neither test may duplicate an assertion made by the read-only `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` as catalogued in `P0-T8`. Acceptance: both method names are present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P6-T2] [expect-fail] Run the two Efc Left-transition tests and record them RED. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~HandleArrowKey_LeftOnMultiSegmentRow_ActivatesParentSegment|FullyQualifiedName~HandleArrowKey_RepeatedLeft_WalksToRootThenFallsThroughToExistingBehavior`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t2`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t2-440-efc-left-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 2, passed 0, failed 2. Satisfies AC-28 fail-before for the Efc surface.
- [ ] [P6-T3] Implement the Efc Left transition in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` `HandleArrowKeyAsync`, whose `Left` arm is at `:322-328`. Attempt the tree transition FIRST by calling the landed `row.ActivateSegment(int)` with the index one below `row.ActiveSegmentIndex`, and post a row render when it returns true; when it returns false — including at the root, on a non-suggestion row, and where no key is attached — fall through UNCHANGED to the existing `row.LeftArrow()` behavior, and from there to the existing third-priority behavior. Do NOT add a selected-node index to `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs`; the concept already exists as `ActiveSegmentIndex` (`:94`). Do NOT weaken `ActivateSegment`'s guard at `BreadcrumbRow.cs:153-166`, in particular the leaf-index refusal at `:156`. Do NOT change the existing `default:` branch and its `log.Error` at `:335-337`. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P6-T4] Re-run the two Efc Left-transition tests and record them GREEN. Same recipe and FILTER as `P6-T2`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t4`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t4-440-efc-left-green.md`. Acceptance: the TRX records total 2, passed 2, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-15 and AC-28 pass-after for the Efc surface.
- [ ] [P6-T5] Add the two Efc Right-transition tests to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterTests.cs`, named verbatim `HandleArrowKey_RightOnActivatedParent_ExpandsViaSingleImmediateSubfolderCall` and `HandleArrowKey_RightAfterExpansion_DescendsByChildActivation`. The first asserts that Right on an activated non-leaf segment expands it through exactly ONE `GetImmediateSubfoldersAsync` call keyed on `row.ActiveSegmentKey`, verified with `Moq.Times.Once()`, and that `ResolveLeafKeyAsync` is NOT called during the expansion, verified with `Moq.Times.Never()`. The second asserts that a subsequent Right selects a child through the landed `GetActiveChild` and `SelectHierarchyPath` path, which is the decision-D9 descent mechanism chosen by this plan. Acceptance: both method names are present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P6-T6] [expect-fail] Run the two Efc Right-transition tests and record them RED. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~HandleArrowKey_RightOnActivatedParent_ExpandsViaSingleImmediateSubfolderCall|FullyQualifiedName~HandleArrowKey_RightAfterExpansion_DescendsByChildActivation`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t6`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t6-440-efc-right-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 2, passed 0, failed 2.
- [ ] [P6-T7] Implement the Efc Right transition in `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` `HandleArrowKeyAsync`, whose `Right` arm is at `:308-321`. Attempt the tree transition FIRST: when the active segment is not yet expanded, expand it through the LANDED `ExpandLeafAsync` (`:364-408`) with no change to its single-call shape; when it is already expanded, descend by child activation through the landed `GetActiveChild` and `SelectHierarchyPath` path used by `ActivateChild` (`:429-441`), which was relocated to `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs` by `P1-T2`. Fall through UNCHANGED to the existing `row.ReExpand()` and leaf-expansion behavior when no tree transition is available, and from there to the existing third-priority behavior. Add no member to `UtilitiesCS/OutlookObjects/Folder/IFolderHierarchyProvider.cs`. Do not add a `ResolveLeafKeyAsync` call anywhere on the expansion path. Retain the landed `catch (OperationCanceledException)` at `:394-399` and the landed broad `catch (Exception ex)` at `:400-407` in `ExpandLeafAsync` unchanged. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P6-T8] Re-run the two Efc Right-transition tests and record them GREEN. Same recipe and FILTER as `P6-T6`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t8`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t8-440-efc-right-green.md`. Acceptance: the TRX records total 2, passed 2, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-16.
- [ ] [P6-T9] Add the Qfc state-model regression tests to `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelTests.cs`, named verbatim `LeftArrow_QfcMultiSegmentRow_SelectsParentNode` and `RightArrow_QfcSelectedParentNode_ExpandsIntoChildren`. Do not modify `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelSelectorTests.cs` or `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelSequenceTests.cs`. Acceptance: both method names are present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P6-T10] [expect-fail] Run the two Qfc state-model tests and record them RED. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~LeftArrow_QfcMultiSegmentRow_SelectsParentNode|FullyQualifiedName~RightArrow_QfcSelectedParentNode_ExpandsIntoChildren`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t10`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t10-440-qfc-model-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 2, passed 0, failed 2. Satisfies AC-28 fail-before for the Qfc surface.
- [ ] [P6-T11] Implement the Qfc transitions in `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs` `RightArrow` at `:424-437` and `LeftArrow` at `:443-455`, plus the selected-node state they require on `BreadcrumbStateRow` at `:14`. Attempt the tree transition FIRST and fall through to the existing behavior second. `BreadcrumbStateRow.Chain` holds `FolderBreadcrumbSegment`, which carries a `Key` (used at `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs:416`), so no re-resolution by path is required on this surface either. Do not write `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionSession.cs`. If the file exceeds 500 lines, split it into a partial sibling with a `Compile Include` entry placed immediately adjacent to the existing entry. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P6-T12] Re-run the two Qfc state-model tests and record them GREEN. Same recipe and FILTER as `P6-T10`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t12`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t12-440-qfc-model-green.md`. Acceptance: the TRX records total 2, passed 2, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-28 pass-after for the Qfc surface.
- [ ] [P6-T13] Add the Qfc router regression tests to `UtilitiesCS.Test/OutlookObjects/Folder/FolderBreadcrumbBridgeRouterTests.cs`, named verbatim `ArrowAsync_QfcLeftOnMultiSegmentRow_RoutesParentSelectTransition` and `ArrowAsync_QfcRightOnSelectedParentNode_RoutesChildExpansion`. Acceptance: both method names are present in the file and the analyzer Rebuild recipe returns `EXIT_CODE: 0`.
- [ ] [P6-T14] [expect-fail] Run the two Qfc router tests and record them RED. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~ArrowAsync_QfcLeftOnMultiSegmentRow_RoutesParentSelectTransition|FullyQualifiedName~ArrowAsync_QfcRightOnSelectedParentNode_RoutesChildExpansion`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t14`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t14-440-qfc-router-red.md` with `ExpectedExitCode: 1`. Acceptance: the TRX at that results directory records total 2, passed 0, failed 2.
- [ ] [P6-T15] Implement the Qfc routing change in `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs` `ArrowAsync` at `:378-406`, attempting the new tree transitions first and preserving the existing `UnhandledArrowMessage` emission at `:387-393` unchanged as the fall-through. The file is 485 lines; if it exceeds 500 lines, split it into a partial sibling following the in-repo precedent `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.SearchPresentation.cs` with its entry at `UtilitiesCS/UtilitiesCS.csproj:629-630`, placing the new `Compile Include` immediately adjacent to the existing entry. Acceptance: the analyzer Rebuild recipe and the nullable Rebuild recipe both return `EXIT_CODE: 0`.
- [ ] [P6-T16] Re-run the two Qfc router tests and record them GREEN. Same recipe and FILTER as `P6-T14`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t16`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t16-440-qfc-router-green.md`. Acceptance: the TRX records total 2, passed 2, failed 0, and `EXIT_CODE:` is `0`. Satisfies AC-17.
- [ ] [P6-T17] Reconcile `QuickFiler/Resources/FolderBreadcrumb.html` `onArrow` at `:395-404` and its keydown wiring at `:420-426` with the Qfc routing implemented in `P6-T15`, so that Left and Right on a selected row reach the C# router. The `arrowKey` and `unhandledArrow` message SHAPES must be unchanged. The file is 489 lines and CANNOT be split, so the change has 11 lines of headroom, which is a hard constraint. Then run the entire asset-contract test class, which is MUST-NOT-WRITE. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~FolderBreadcrumbAssetContractTests`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t17`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t17-440-html.md`. Acceptance: the TRX records failed 0, `QuickFiler/Resources/FolderBreadcrumb.html` measures at or under 500 lines, and `git status --porcelain --untracked-files=all -- QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs` produces no output.
- [ ] [P6-T18] Add the decision-D1 handling-order tests, named verbatim `ArrowKey_SingleSegmentRow_TakesPreExistingCollapsePath` in `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterTests.cs` and `ArrowKey_QfcSingleSegmentRow_TakesPreExistingCollapsePath` in `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelTests.cs`, each asserting that a row whose resolved chain has exactly one segment still takes the pre-existing expand and collapse path and, where none applies, the pre-existing unhandled fall-through. Run both. Scoped test run recipe once per assembly: FILTER `FullyQualifiedName~ArrowKey_SingleSegmentRow_TakesPreExistingCollapsePath` for `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t18-quickfiler-test`, and FILTER `FullyQualifiedName~ArrowKey_QfcSingleSegmentRow_TakesPreExistingCollapsePath` for `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll` with RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t18-utilities-test`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t18-d1-handling-order.md`. Acceptance: both TRX files record failed 0 and combined passed 2. Satisfies AC-18.
- [ ] [P6-T19] Add the decision-D2 boundary preservation tests to `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterTests.cs`, named verbatim `Boundary_EfcLeftAtRootAndRightOnChildlessNode_RemainSilentNoOps` and `Boundary_QfcUnhandledArrow_StillReachesBreadcrumbArrowFallThrough`. The first exercises the Efc boundaries the spec re-cites after pull request #605: the `LeftArrow` root refusal at `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs:304-308`, the arrow routing at `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs:304-339`, and the childless-node early return in `ExpandLeafAsync` at `:366-370`, which now tests the ACTIVE segment rather than the leaf. The second asserts at the `BreadcrumbArrowFallThrough` call site (`QuickFiler/Controllers/QfcItemController.ViewerSetup.cs:190`) by mocking `IQfcKeyboardHandler.BreadcrumbArrowFallThrough`, following the precedent at `QuickFiler.Test/Controllers/QfcItemControllerBreadcrumbDropDownTests.cs:156-168`, which drives the call site through `HarnessController` and `QfcItemControllerTestSupport.SetField`; both harness types live in `QuickFiler.Test`, so this test MUST reside in `QuickFiler.Test` and is not reachable from `UtilitiesCS.Test`. It must never invoke the modal `MyBox.ShowDialog` (`QuickFiler/Controllers/KeyboardHandler.cs:304-309`), which has no injectable seam and would block. Run both. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~Boundary_EfcLeftAtRootAndRightOnChildlessNode_RemainSilentNoOps|FullyQualifiedName~Boundary_QfcUnhandledArrow_StillReachesBreadcrumbArrowFallThrough`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t19`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/p6-t19-d2-boundaries.md`. Acceptance: the TRX at `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/regression-testing/trx/p6-t19/results.trx` records total 2, passed 2, failed 0. Satisfies AC-23 and AC-24.
- [ ] [P6-T20] Record the decision-D9 descent mechanism in the D9 section of `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md`, naming the mechanism this plan chose and the reason, and citing `P6-T7` as the implementing task and `P6-T5` as the test that pins it. The exact line to add is `D9 DESCENT MECHANISM SELECTED: descend by child activation` followed by the rationale that `ActivateSegment` refuses the leaf index (`UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs:156`) and therefore cannot express a downward transition, and that the child-activation route adds no member to `BreadcrumbRow`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p6-t20-d9-descent-recorded.md`. Acceptance: a fixed-string search of `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md` for the literal `D9 DESCENT MECHANISM SELECTED: descend by child activation` returns at least one match, and the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` quoting the added text. Satisfies the AC-16 descent-mechanism recording clause.

### Phase 7 — Ownership, File-Size, and Scope Verification

- [ ] [P7-T1] Sweep every owned `.cs` file written by this plan for the 500-line limit. Measure each with `(Get-Content -LiteralPath $path).Count`; where a file exceeds 500 lines, move whole members into a NEW partial-class sibling named after the file it splits, and add its `Compile Include` entry IMMEDIATELY ADJACENT to the existing entry for the file being split, in `QuickFiler/QuickFiler.csproj`, `QuickFiler.Test/QuickFiler.Test.csproj`, `UtilitiesCS/UtilitiesCS.csproj`, or `UtilitiesCS.Test/UtilitiesCS.Test.csproj` as applicable. The in-repo precedent is `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.SearchPresentation.cs` with its entry at `UtilitiesCS/UtilitiesCS.csproj:629-630`. Do not split `QuickFiler/Resources/FolderBreadcrumb.html`. This is a RESIDUAL sweep: the mandatory split of `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` already happened in `P1-T2` and must not be redone here. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/other/p7-t1-partial-splits.md`. Acceptance: the analyzer Rebuild recipe returns `EXIT_CODE: 0` and the artifact lists every file measured with its post-task line count and every new partial sibling with the project file and insertion point of its `Compile Include`.
- [ ] [P7-T2] Verify the file-size criterion. Measure the line count of every file written by this plan, including `QuickFiler/Resources/FolderBreadcrumb.html` and every partial sibling created in `P1-T2`, `P2-T1` and `P7-T1`, and compare the at-risk owned files against the re-measured decision-D8 figures recorded in this plan's "File-Size Constraint" table: `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs` 596 before the `P1-T2` split, `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs` 485, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs` 457, `QuickFiler/Resources/FolderBreadcrumb.html` 489, `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs` 462, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs` 361, `UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyProvider.cs` 98. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t2-file-size.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving one row per written file with its line count, every row is at or under 500, and the artifact separately records the three PRE-EXISTING violations — `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs` 983, `QuickFiler/Controllers/EfcFormController.cs` 1084, `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` 531 — as unowned, unwritten and unworsened, with their measured counts equal to their `P0-T16` baseline counts. Satisfies AC-31.
- [ ] [P7-T3] Verify the ownership criterion. Command: `pwsh -NoProfile -Command 'git diff --name-only; git status --porcelain --untracked-files=all; "EXIT_CODE: $LASTEXITCODE"'`. Scope the review to production and test source: paths under `QuickFiler/`, `QuickFiler.Test/`, `UtilitiesCS/`, `UtilitiesCS.Test/`, and any `.csproj`. Paths under `docs/features/active/breadcrumb-router-navigation-defects-498/` are this feature's own artifacts and are expected; paths under `.claude/` are excluded from this gate. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t3-ownership-diff.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` listing every changed and untracked path in scope, and none of them is `QuickFiler/Controllers/KbdActions.cs`, `QuickFiler/Controllers/EfcFormController.cs`, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowBuilder.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbDocumentAssets.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbHtmlRenderer.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionMap.cs`, `UtilitiesCS/OutlookObjects/Folder/IFolderHierarchyProvider.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionSession.cs`, `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs`, `QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs`, or `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs`, and every remaining in-scope path appears in the OWNED list of this plan. Satisfies AC-30.
- [ ] [P7-T4] Verify the decision-D1 message-shape criterion. Run the complete `FolderBreadcrumbAssetContractTests` class, including the test method `LeftAndRightBreadcrumbMessages_RemainSupported` at `QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs:359-367`. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~FolderBreadcrumbAssetContractTests`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/trx/p7-t4`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t4-ac19-message-shapes.md`. Acceptance: the TRX records failed 0, and `git status --porcelain --untracked-files=all -- QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs` produces no output. Satisfies AC-19.
- [ ] [P7-T5] Verify the decision-D1 selector-session criterion. Run the complete `BreadcrumbStateModelSelectorTests` class. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~BreadcrumbStateModelSelectorTests`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/trx/p7-t5`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t5-ac20-selector-session.md`. Acceptance: the TRX records failed 0, and `git status --porcelain --untracked-files=all -- UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectionSession.cs UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelSelectorTests.cs` produces no output. Satisfies AC-20.
- [ ] [P7-T6] Verify the #400 residual selector contract. Run the complete `BreadcrumbStateModelSequenceTests` class, which carries the Up, Down, Enter and Escape selector sequences of #400 AC-5 through AC-8. Scoped test run recipe with ASSEMBLY `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll`, FILTER `FullyQualifiedName~BreadcrumbStateModelSequenceTests`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/trx/p7-t6`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t6-ac22-400-residual.md`. Acceptance: the TRX records failed 0, and `git status --porcelain --untracked-files=all -- UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelSequenceTests.cs` produces no output. Satisfies AC-22.
- [ ] [P7-T7] Verify the decision-D1 supersession record. Confirm `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md` still carries the reviewer-findable section whose heading contains the literal `AC-9 supersession record`, and that its disposition table names the retracted clause and the three preserved clauses. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t7-ac21-supersession-record.md`. Acceptance: a fixed-string search of that spec file for the literal `AC-9 supersession record` returns at least one match, and the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` quoting the four table rows. Satisfies AC-21.
- [ ] [P7-T8] Verify that the inherited #439 regression coverage still passes and was not edited. Run the complete `BreadcrumbBridgeRouterIssue439Tests` class. Scoped test run recipe with ASSEMBLY `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll`, FILTER `FullyQualifiedName~BreadcrumbBridgeRouterIssue439Tests`, RESULTS `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/trx/p7-t8`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p7-t8-inherited-439-coverage.md`. Acceptance: the TRX records failed 0 and a passed count equal to the count of `[TestMethod]` members catalogued in the `P0-T8` artifact, and `git status --porcelain --untracked-files=all -- QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` produces no output. This is the evidence that the retired AC-10, AC-12 and AC-13 remain satisfied by the inherited work after this feature's changes.

### Phase 8 — Final QA Loop, Coverage Delta, and Acceptance-Criteria Check-Off

- [ ] [P8-T1] Toolchain step 1, mutating pass, scoped to the exact files this plan writes. Command: `pwsh -NoProfile -Command 'dotnet tool run csharpier format QuickFiler/Controllers/BreadcrumbBridgeRouter.cs QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyProvider.cs UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.cs QuickFiler.Test/Controllers/BreadcrumbBridgeRouterQueueTests.Part2.cs QuickFiler.Test/Controllers/BreadcrumbBridgeRouterTests.cs UtilitiesCS.Test/OutlookObjects/Folder/OutlookFolderHierarchyProviderTests.cs UtilitiesCS.Test/OutlookObjects/Folder/FolderBreadcrumbBridgeRouterTests.cs UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbRowStateTests.cs UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelTests.cs; "EXIT_CODE: $LASTEXITCODE"'`, extended with the path of every additional new partial-class sibling created in `P6-T11`, `P6-T15` or `P7-T1`. The target list deliberately names individual files and NO directory: a directory-scoped mutating format would rewrite MUST-NOT-WRITE files — `QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs` (violating the clean `git status` gates in `P6-T17` and `P7-T4`), `QuickFiler.Test/Controllers/BreadcrumbBridgeRouterIssue439Tests.cs` (`P7-T8`), `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelSelectorTests.cs` (`P7-T5`, AC-20), and `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbStateModelSequenceTests.cs` (`P7-T6`, AC-22) — and would place sibling epic children's files in this feature's diff, breaking AC-30. `QuickFiler/Controllers/KeyboardHandler.cs` and `UtilitiesCS/OutlookObjects/Folder/FolderPredictor.cs` are excluded because no task in this plan writes them; `QuickFiler/Resources/FolderBreadcrumb.html` is excluded because it is not a CSharpier input. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t1-csharpier-format.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the exit code and the number of files CSharpier REWROTE, determined by comparing the SHA-256 of each listed file before and after the command rather than by reading CSharpier's own processed-file count. `EXIT_CODE:` is `0`.
- [ ] [P8-T2] Toolchain step 1, read-only verification at CI parity. Command: `pwsh -NoProfile -Command 'dotnet tool run csharpier check .; "EXIT_CODE: $LASTEXITCODE"'`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t2-csharpier-check.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the exit code and the list of any unformatted files, and `EXIT_CODE:` is `0`. CONDITIONAL degradation, permitted ONLY IF the `P0-T12` baseline artifact recorded a non-zero exit code for the identical command: the gate instead requires that no file on the `P8-T1` target list appears in the unformatted list and that the unformatted list is a subset of the `P0-T12` baseline list, and the artifact records the degradation with a pointer to `P0-T12` and declares `ExpectedExitCode:` with the baseline integer.
- [ ] [P8-T3] Toolchain step 2, analyzer gate, using the Rebuild recipe. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t3-analyzer-rebuild.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the warning and error counts, `EXIT_CODE:` is `0`, and the command contains `/t:Rebuild` and does not contain `/t:Build`. CONDITIONAL degradation, permitted ONLY IF the `P0-T13` baseline artifact recorded a non-zero exit code for the identical command: the gate instead requires that the warning and error counts are at or below the `P0-T13` counts and that no reported diagnostic names any file on the `P8-T1` target list, and the artifact records the degradation with a pointer to `P0-T13` and declares `ExpectedExitCode:` with the baseline integer.
- [ ] [P8-T4] Toolchain step 3, nullable and type-check gate. Command: the Rebuild recipe with the two analyzer properties replaced by `"/p:TreatWarningsAsErrors=true"`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t4-nullable-rebuild.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the error count, `EXIT_CODE:` is `0`, and the command contains neither `/p:Nullable=enable` nor `/t:Build`. CONDITIONAL degradation, permitted ONLY IF the `P0-T14` baseline artifact recorded a non-zero exit code for the identical command: the gate instead requires that the error count is at or below the `P0-T14` count and that no reported diagnostic names any file on the `P8-T1` target list, and the artifact records the degradation with a pointer to `P0-T14` and declares `ExpectedExitCode:` with the baseline integer.
- [ ] [P8-T5] Toolchain step 4, full-suite test with coverage, using the Coverage recipe, then copy `coverage/coverage.cobertura.xml` to `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t5-coverage.cobertura.xml`. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t5-coverage-test.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` giving the total, passed and failed counts; the fully qualified identifier of every failing test; and the NUMERIC repository-wide line rate read from the `line-rate` attribute of the root coverage element of the copied Cobertura file, expressed as a percentage to two decimal places. The failing-test identifier set must be a SUBSET of the `BASELINE_FAILURE_SET` recorded in the `P0-T15` artifact, and no failing identifier may belong to `BreadcrumbBridgeRouterQueueTests`, `BreadcrumbBridgeRouterTests`, `BreadcrumbBridgeRouterIssue439Tests`, `OutlookFolderHierarchyProviderTests`, `FolderBreadcrumbBridgeRouterTests`, `BreadcrumbRowStateTests`, `BreadcrumbStateModelTests`, `BreadcrumbStateModelSelectorTests`, `BreadcrumbStateModelSequenceTests` or `FolderBreadcrumbAssetContractTests`. Skipped counts are recorded but not asserted. Placeholder coverage values are not acceptable. When the observed exit code is non-zero because the baseline failure set is non-empty, declare `ExpectedExitCode:` with that integer.
- [ ] [P8-T6] Confirm the toolchain completed in ONE clean pass. Compare the `P8-T1` rewritten-file count and the results of `P8-T2` through `P8-T5`. If `P8-T1` rewrote any file, or if any of `P8-T2` through `P8-T5` failed its acceptance including any permitted degradation, the loop restarts from `P8-T1` and every artifact from `P8-T1` through `P8-T5` is rewritten for the new pass. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t6-clean-pass.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, an `Output Summary:` naming the pass number, the four toolchain outcomes, and which if any of them used a permitted baseline-comparison degradation with its baseline artifact pointer, and states that the final pass rewrote zero files. Satisfies AC-29.
- [ ] [P8-T7] Report the coverage delta. Read the baseline Cobertura at `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/baseline/p0-t15-coverage.cobertura.xml` and the post-change Cobertura at `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t5-coverage.cobertura.xml`. Both figures must come from those two MEASURED artifacts; no figure inherited from version 1.0 of this plan is acceptable, because pull request #605 changed the coverage denominator by removing an `[ExcludeFromCodeCoverage]` attribute from the unowned `QuickFiler/Controllers/EfcFormController.cs`. For the changed-line figure, take the added and modified lines from `git diff HEAD --unified=0` restricted to the owned production files, plus every executable line of any new partial sibling, and intersect them with the post-change per-line hit counts, AGGREGATING every Cobertura class element that shares the same `filename` value so that compiler-generated async state machines and lambda closure classes are counted in the same denominator as their source file. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/qa-gates/p8-t7-coverage-delta.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` stating three numeric values as percentages to two decimal places — baseline repository-wide line coverage, post-change repository-wide line coverage, and changed-line coverage — plus a per-file changed-line figure for `QuickFiler/Controllers/BreadcrumbBridgeRouter.cs`, `QuickFiler/Controllers/BreadcrumbBridgeRouter.Selection.cs`, `UtilitiesCS/OutlookObjects/Folder/OutlookFolderHierarchyProvider.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRow.cs`, `UtilitiesCS/OutlookObjects/Folder/BreadcrumbStateModel.cs`, and `UtilitiesCS/OutlookObjects/Folder/FolderBreadcrumbBridgeRouter.cs`. The task passes when post-change repository-wide line coverage is at or above the `P0-T15` baseline value and every per-file changed-line figure is at or above 90.00 percent; otherwise the executor adds tests and re-runs `P8-T5` and this task. The coverage-denominator change caused by pull request #605 is an inherited condition recorded in the artifact and is not this feature's obligation to remedy.
- [ ] [P8-T8] Walk the acceptance criteria in `docs/features/active/breadcrumb-router-navigation-defects-498/spec.md` from AC-1 through AC-31 in order and check off only those with verified evidence on disk, per the acceptance-criteria-tracking skill. Each newly satisfied criterion is checked in the spec file itself by changing its leading marker from unchecked to checked; an unmet criterion is left unchecked with a one-line reason. AC-10, AC-12 and AC-13 are ALREADY checked as retired inherited-and-verified criteria delivered by pull request #605; they must be left checked, must not be re-verified as this feature's work, and their evidence pointer is the `P7-T8` artifact. Artifact: `docs/features/active/breadcrumb-router-navigation-defects-498/evidence/issue-updates/p8-t8-acceptance-criteria.md`. Acceptance: the artifact records `Timestamp:`, `Command:`, `EXIT_CODE:`, and an `Output Summary:` containing a 31-row table with one row per criterion identifier AC-1 through AC-31, each row naming the criterion's disposition — `SATISFIED`, `RETIRED-INHERITED`, or `UNMET` with a reason — and the repo-relative path of the single evidence artifact that establishes it, and the checkbox state of every criterion in the spec file matches its row.

---

## Acceptance-Criteria Coverage Map

| Criterion | Disposition | Tasks |
|---|---|---|
| AC-1 | This feature | P2-T4, P2-T5 |
| AC-2 | This feature | P2-T7 |
| AC-3 | This feature | P2-T6, P2-T7 |
| AC-4 | This feature | P3-T3, P3-T4, P3-T5 |
| AC-5 | This feature | P3-T3, P3-T6 |
| AC-6 | This feature | P3-T6 |
| AC-7 | This feature (#440 Qfc prerequisite) | P4-T6 |
| AC-8 | This feature (#440 Qfc prerequisite) | P4-T4, P4-T5 |
| AC-9 | This feature (#440 Qfc prerequisite) | P4-T6 |
| AC-10 | **RETIRED — inherited from PR #605, already checked in spec.md** | none; confirmed still passing by P7-T8 |
| AC-11 | This feature (#440 Qfc prerequisite) | P4-T7 |
| AC-12 | **RETIRED — inherited from PR #605, already checked in spec.md** | none; confirmed still passing by P7-T8 |
| AC-13 | **RETIRED — inherited from PR #605, already checked in spec.md** | none; confirmed still passing by P7-T8 |
| AC-14 | This feature (#440 Qfc prerequisite, decision D7) | P4-T1, P5-T3, P5-T4, P5-T5, P5-T6 |
| AC-15 | This feature | P6-T3, P6-T4 |
| AC-16 | This feature | P6-T7, P6-T8, P6-T20 |
| AC-17 | This feature | P6-T11, P6-T12, P6-T15, P6-T16 |
| AC-18 | This feature | P6-T18 |
| AC-19 | This feature | P6-T17, P7-T4 |
| AC-20 | This feature | P7-T5 |
| AC-21 | This feature | P7-T7 |
| AC-22 | This feature | P7-T6 |
| AC-23 | This feature | P6-T19 |
| AC-24 | This feature | P6-T19 |
| AC-25 | This feature | P2-T3, P2-T5 |
| AC-26 | This feature | P3-T2, P3-T4 |
| AC-27 | This feature (#440 Qfc prerequisite) | P4-T3, P4-T5 |
| AC-28 | This feature | P6-T2, P6-T4, P6-T10, P6-T12 |
| AC-29 | This feature | P8-T1 through P8-T6 |
| AC-30 | This feature | P7-T3 |
| AC-31 | This feature | P1-T2, P7-T1, P7-T2 |

Three criteria — AC-10, AC-12 and AC-13 — carry no task in this plan. They were delivered by pull request #605, are recorded in `spec.md` as INHERITED-AND-VERIFIED, and are already checked there. No task in this plan re-implements them; `P7-T8` only confirms that the inherited regression coverage still passes after this feature's changes and that its test file is absent from the diff.
