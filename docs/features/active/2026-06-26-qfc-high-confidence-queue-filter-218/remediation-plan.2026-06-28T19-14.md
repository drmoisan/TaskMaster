# Remediation Plan (Cycle 2, Rebased Tree): qfc-high-confidence-queue-filter (Issue #218)

Authoritative remediation inputs: `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/remediation-inputs.2026-06-28T19-14.md` (read especially the "Ground-Truth Update (2026-06-28T19-34)" section).

Cycle provenance: Cycle 1 (`remediation-plan.2026-06-26T20-58.md`) cleared preflight and was blocked at `[P1-T4]` because reducing the touched production files below the 500-line limit required extraction beyond the original queue-admission seam. The user approved an expanded extraction scope. This plan opens cycle 2.

REVISION NOTE (2026-06-28T19-34): The maintainer rebased the branch onto current `main` (merge-base now `1b8536b6`, includes the #219 deterministic-test fix) and committed `2637e4c1 refactor(qfc): split oversized controllers to meet 500-line limit`, which already performed most of Finding 1 on the production side. The original Phase 1 / Phase 2 production-extraction tasks are therefore obsolete and are replaced by verification-only tasks. The only remaining production-side structural work is completing the test split. This plan was re-planned against the new ground truth; no production re-extraction is included.

## Verified ground truth (2026-06-28, rebased tree)

Production splits — DONE by `2637e4c1`, VERIFY ONLY (do NOT re-extract):
- `QuickFiler/Controllers/QfcDatamodel.cs` (432), `QfcDatamodel.FrameBuilding.cs` (154), `QfcDatamodel.QueueProcessing.cs` (146).
- `QuickFiler/Controllers/QfcHomeController.cs` (454), `QfcHomeController.Iteration.cs` (82), `QfcHomeController.Metrics.cs` (226).
- `QuickFiler/Controllers/EmailSorter.cs` (85), `QfcRemainingQueueAdmission.cs` (58).
- All wired into `QuickFiler/QuickFiler.csproj` (confirmed includes at lines 299-308).

Test split — INCOMPLETE (the remaining production-side structural work):
- `QuickFiler.Test/Controllers/QfcHomeControllerTests.cs` is still 1370 lines with all 30 active `[TestMethod]`s (33 `[TestMethod]` lines total; 3 are commented `// [TestMethod]`). It was NOT trimmed.
- The four split files exist on disk but are NOT wired into `QuickFiler.Test/QuickFiler.Test.csproj` (which currently includes only `QfcHomeControllerTests.cs` and `QfcHomeControllerIssue218Tests.cs` at lines 71-72), so they are not compiled:
  - `QfcHomeControllerRunAsyncTests.cs` (448, 6 tests): `Run_ExecutesCorrectly`, `RunAsync_ExecutesCorrectly`, `HighConfidencePreFilterLoader_CanBeOverridden_ForTesting`, `RunAsync_HighConfidenceDisabled_DoesNotPreFilterUsesPlainOverload`, `RunAsync_HighConfidenceDisabled_UsesPlainOverloadOnly`, `Worker_RunWorkerCompleted_HandlesCompletionCorrectly`.
  - `QfcHomeControllerIterationTests.cs` (352, 6 tests): `IterateQueueAsync_DataModelComplete`, `IterateQueueAsync_QueueEmpty`, `IterateQueueAsync_Queue2`, `Iterate_ExecutesCorrectly`, `Iterate2_ExecutesCorrectly`, `SwapStopWatch_ExecutesCorrectly`.
  - `QfcHomeControllerMetricsTests.cs` (241, 2 tests): `QuickFileMetrics_WRITE_WhenGetCalendarReturnsNull_DoesNotThrow`, `GetMoveDiagnostics_NullAppointment_DoesNotThrow`.
  - `QfcHomeControllerPropertyTests.cs` (345, 13 tests): `Cleanup_ExecutesCorrectly`, and `*_PropertyWorksCorrectly` for `Loaded`, `ExplorerController`, `FormController`, `KeyboardHandler`, `DataModel`, `FilerQueue`, `UiScheduler`, `StopWatch`, `TokenSource`, `Token`, `WorkerComplete`, `UiSyncContext`.
- Residual tests that must REMAIN in `QfcHomeControllerTests.cs` after trim: `Constructor_InitializesCorrectly`, `Init_InitializesCorrectly`, `InitAsync_InitializesCorrectly` (3 active), plus the commented-out tests and the `QfcFormViewerDerived` helper (defined at line ~270, not referenced by any split file — confirm its required home after trim).
- Test-method count anchors: 27 moved + 3 residual = 30 active in `QfcHomeControllerTests.cs` pre-trim. Currently-compiled QfcHomeController suite active `[TestMethod]` count = 30 (Tests) + 2 (Issue218) = 32. End-state compiled active count must equal 32 (Tests 3 + RunAsync 6 + Iteration 6 + Metrics 2 + Property 13 + Issue218 2).
- Merge-base = `main` = `1b8536b6`.

Hard constraints (from inputs): no behavior change beyond mechanical, behavior-preserving completion of the split; preserve every existing test name and assertion; if a split copy has diverged from its original, the original (compiled) version is canonical and the split file must be corrected to match before trimming; do not modify policy files (`CLAUDE.md`, `.claude/rules/**`, `.editorconfig`, `.globalconfig`, coverage config); do not weaken issue #218 acceptance criteria; do not introduce temporary files in tests; do not raise repository-wide coverage to the raw 80% figure with out-of-scope tests.

Toolchain order (from `CLAUDE.md` / `.claude/rules/csharp.md`): CSharpier check -> analyzer build -> nullable build -> MSTest coverage. Restart the loop from CSharpier if any step changes files or fails.

Note on `[ExcludeFromCodeCoverage]`: `QfcDatamodel` carries `[ExcludeFromCodeCoverage]` and is COM-host-bound; the `partial class QfcDatamodel` files inherit this disposition and remain outside the testable coverage denominator. The home-controller partials inherit the home controller's existing coverage disposition.

Out of scope for this plan: feature-review reaudit (the orchestrator runs that separately at cycle exit).

---

### Phase 0 — Remediation Baseline (Cycle 2, Rebased Tree)

- [x] [P0-T1] Read the required policy and review inputs in order: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/skills/policy-compliance-order/SKILL.md`, `.claude/skills/atomic-plan-contract/SKILL.md`, `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`, `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/remediation-inputs.2026-06-28T19-14.md`, `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/policy-audit.2026-06-26T20-58.md`, `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/code-review.2026-06-26T20-58.md`, `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/feature-audit.2026-06-26T20-58.md`, and `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/issue.md`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/phase0-instructions-read-cycle2-218.md` with `Timestamp:`, `Policy Order:`, the explicit list of files read, and `Output Summary:`.
- [x] [P0-T2] Capture the merge-base by running `git merge-base HEAD main` and `git rev-parse main`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/merge-base-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the resolved merge-base SHA, and an explicit assertion that it equals the anchor `1b8536b6` (if it differs, record the actual SHA and use it as the changed-line denominator source for the remainder of the plan).
- [x] [P0-T3] Capture the changed-file line-count baseline over the six production split files and all QfcHomeController test files by running a deterministic line count (command: `$files=@('QuickFiler/Controllers/QfcDatamodel.cs','QuickFiler/Controllers/QfcDatamodel.FrameBuilding.cs','QuickFiler/Controllers/QfcDatamodel.QueueProcessing.cs','QuickFiler/Controllers/EmailSorter.cs','QuickFiler/Controllers/QfcHomeController.cs','QuickFiler/Controllers/QfcHomeController.Iteration.cs','QuickFiler/Controllers/QfcHomeController.Metrics.cs','QuickFiler/Controllers/QfcRemainingQueueAdmission.cs','QuickFiler.Test/Controllers/QfcHomeControllerTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerRunAsyncTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerIterationTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerPropertyTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerIssue218Tests.cs'); foreach($f in $files){[pscustomobject]@{File=$f;Lines=(Get-Content -LiteralPath $f).Count}} | Format-Table -AutoSize`); write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/changed-file-line-counts-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and an explicit PASS/FAIL per file against the 500-line limit (anchors: six production files all <=500; `QfcHomeControllerTests.cs` expected FAIL at ~1370 until the test split completes).
- [x] [P0-T4] Capture the pre-trim test-method baseline by deterministically counting active `[TestMethod]` attributes (command: `Select-String -Path 'QuickFiler.Test/Controllers/QfcHomeControllerTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerRunAsyncTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerIterationTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerMetricsTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerPropertyTests.cs','QuickFiler.Test/Controllers/QfcHomeControllerIssue218Tests.cs' -Pattern '^\s*\[TestMethod\]'`); write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/testmethod-count-baseline-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the per-file active `[TestMethod]` count, and the anchors: `QfcHomeControllerTests.cs` = 30 active (3 commented excluded), currently-compiled QfcHomeController suite active count = 32 (Tests 30 + Issue218 2), and the four split files contribute 6+6+2+13 = 27 (currently uncompiled).
- [x] [P0-T5] Capture the CSharpier baseline by running `dotnet tool run csharpier -- check .`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/remediation-baseline-csharpier-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P0-T6] Capture the analyzer-build baseline by running `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/remediation-baseline-analyzer-build-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P0-T7] Capture the nullable-build baseline by running `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/remediation-baseline-nullable-build-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P0-T8] Capture the repository-wide coverage baseline by parsing the merge-base baseline Cobertura `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/baseline/coverage-baseline-218.cobertura.xml` for the root `line-rate`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/coverage-shortfall-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the exact baseline repo-wide line-coverage percentage, the numeric gap to the raw 80% threshold, a note that this baseline Cobertura is the authoritative no-regression reference, and a note that disposition of the repo-wide shortfall is an authority-scoped exception (Finding 3).
- [x] [P0-T9] Capture the banned-API baseline by running a deterministic scan for `DateTime.Now`, `DateTime.UtcNow`, `Random.Shared`, `Thread.Sleep`, and `Task.Delay` over the eight touched production files (command: `Select-String -Path 'QuickFiler/Controllers/QfcDatamodel.cs','QuickFiler/Controllers/QfcDatamodel.FrameBuilding.cs','QuickFiler/Controllers/QfcDatamodel.QueueProcessing.cs','QuickFiler/Controllers/QfcHomeController.cs','QuickFiler/Controllers/QfcHomeController.Iteration.cs','QuickFiler/Controllers/QfcHomeController.Metrics.cs','QuickFiler/Controllers/EmailSorter.cs','QuickFiler/Controllers/QfcRemainingQueueAdmission.cs' -Pattern 'DateTime\.Now','DateTime\.UtcNow','Random\.Shared','Thread\.Sleep','Task\.Delay'`); write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/remediation-baseline/banned-api-baseline-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and the exact file/line of each match (or `none`).

---

### Phase 1 — Production Partial-Split Verification (Finding 1, Verification-Only — NO Re-Extraction)

- [x] [P1-T1] Verify each of the six production split files is 500 lines or fewer using the line counts captured in P0-T3; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/production-split-line-counts-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and an explicit PASS/FAIL per file against the 500-line limit for `QfcDatamodel.cs` (~432), `QfcDatamodel.FrameBuilding.cs` (~154), `QfcDatamodel.QueueProcessing.cs` (~146), `EmailSorter.cs` (~85), `QfcHomeController.cs` (~454), `QfcHomeController.Iteration.cs` (~82), and `QfcHomeController.Metrics.cs` (~226). If any production file exceeds 500, record it as a deferred finding (do NOT re-extract in this cycle) and stop for caller guidance.
- [x] [P1-T2] Verify the production partials are wired into `QuickFiler/QuickFiler.csproj` by confirming explicit `<Compile Include>` items exist for `Controllers\EmailSorter.cs`, `Controllers\QfcDatamodel.cs`, `Controllers\QfcDatamodel.FrameBuilding.cs`, `Controllers\QfcDatamodel.QueueProcessing.cs`, `Controllers\QfcHomeController.cs`, `Controllers\QfcHomeController.Metrics.cs`, and `Controllers\QfcHomeController.Iteration.cs` (command: `Select-String -Path 'QuickFiler/QuickFiler.csproj' -Pattern 'EmailSorter\.cs','QfcDatamodel(\.\w+)?\.cs','QfcHomeController(\.\w+)?\.cs'`); write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/production-split-csproj-wiring-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and an explicit PASS/FAIL that all seven includes are present.
- [x] [P1-T3] Verify the production partial splits compile and preserve the public surface by running `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/production-split-build-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (expected `EXIT_CODE: 0`). This task is verification-only; do not modify any production file. If the build fails, record the failure as a deferred finding and stop for caller guidance rather than re-extracting.

---

### Phase 2 — Complete the Test Split (Finding 1, Tests)

- [x] [P2-T1] Verify the four split files faithfully reproduce the 27 moved tests (names AND assertions) versus the originals in `QuickFiler.Test/Controllers/QfcHomeControllerTests.cs` BEFORE any deletion: for each of the 6 RunAsync, 6 Iteration, 2 Metrics, and 13 Property tests, compare the method name and the full method body (assertions, arrange/act, attributes) between the split file and the original. Write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/test-split-equivalence-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, a per-test EQUIVALENT/DIVERGED line for all 27 tests, and the explicit list of the 27 expected names. If any split copy has DIVERGED from its original, fix the split file so it matches the original (compiled) version exactly — the original is canonical — and re-record the comparison until all 27 are EQUIVALENT. Do not trim any original test in this task.
- [x] [P2-T2] Wire the four split files into `QuickFiler.Test/QuickFiler.Test.csproj` by adding explicit `<Compile Include="Controllers\QfcHomeControllerRunAsyncTests.cs" />`, `<Compile Include="Controllers\QfcHomeControllerIterationTests.cs" />`, `<Compile Include="Controllers\QfcHomeControllerMetricsTests.cs" />`, and `<Compile Include="Controllers\QfcHomeControllerPropertyTests.cs" />` to the existing Controllers `<Compile Include>` group (legacy `packages.config` project has no glob; new files must be wired explicitly); verify the four includes are present via `Select-String -Path 'QuickFiler.Test/QuickFiler.Test.csproj' -Pattern 'QfcHomeController(RunAsync|Iteration|Metrics|Property)Tests\.cs'`.
- [x] [P2-T3] Trim the 27 moved tests from `QuickFiler.Test/Controllers/QfcHomeControllerTests.cs`, leaving exactly the residual 3 active tests (`Constructor_InitializesCorrectly`, `Init_InitializesCorrectly`, `InitAsync_InitializesCorrectly`), the existing commented-out tests, the `[TestClass]`/`Setup` scaffolding required by the residual tests, and the `QfcFormViewerDerived` helper. Determine the required home of `QfcFormViewerDerived` (defined at line ~270, not referenced by any split file): if any compiled test (residual or split) references it, keep it where it is referenced; if it is referenced only by trimmed tests, keep it in `QfcHomeControllerTests.cs` for the residual scaffolding or remove it only if provably unreferenced across the compiled suite. Do not rename, weaken, or alter the residual tests' assertions. Record the disposition of `QfcFormViewerDerived` inline in the verification evidence written by P2-T4.
- [x] [P2-T4] Verify the completed test split by running a deterministic line count over `QfcHomeControllerTests.cs`, `QfcHomeControllerRunAsyncTests.cs`, `QfcHomeControllerIterationTests.cs`, `QfcHomeControllerMetricsTests.cs`, `QfcHomeControllerPropertyTests.cs`, and `QfcHomeControllerIssue218Tests.cs`; a deterministic active `[TestMethod]` count over the same files; and a duplicate-name check across the compiled suite (command: `Select-String -Path 'QuickFiler.Test/Controllers/QfcHomeController*Tests.cs' -Pattern 'public\s+(async\s+Task|void)\s+\w+\s*\(' | ... group by method name`). Write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/test-split-verification-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, an explicit PASS/FAIL per file that every `QfcHomeController*Tests.cs` file is <=500 lines, an explicit assertion of ZERO duplicate `[TestMethod]` definitions across the compiled suite, an explicit assertion that the total compiled active `[TestMethod]` count equals the anchor 32 (Tests 3 + RunAsync 6 + Iteration 6 + Metrics 2 + Property 13 + Issue218 2), and the recorded `QfcFormViewerDerived` disposition from P2-T3. If any file still exceeds 500, if any duplicate exists, or if the count does not equal 32, fix and rerun this task before checking off.
- [x] [P2-T5] Verify the completed test split compiles by running `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/test-split-build-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (expected `EXIT_CODE: 0`). If the build fails, fix the wiring or residual scaffolding (no assertion weakening) and rerun before checking off.

---

### Phase 3 — Banned-API Sweep (Touched Production Files)

- [x] [P3-T1] Run a banned-API sweep over the eight touched production files (`QuickFiler/Controllers/QfcDatamodel.cs`, `QfcDatamodel.FrameBuilding.cs`, `QfcDatamodel.QueueProcessing.cs`, `QfcHomeController.cs`, `QfcHomeController.Iteration.cs`, `QfcHomeController.Metrics.cs`, `EmailSorter.cs`, `QfcRemainingQueueAdmission.cs`) for `DateTime.Now`, `DateTime.UtcNow`, `Random.Shared`, `Thread.Sleep`, and `Task.Delay` (same command form as P0-T9); for each match, either replace it with the repository injectable clock/delay seam without changing behavior, or, if it cannot be removed within behavior-preserving scope, record it as a precise deferred finding for a follow-up cycle. Write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/banned-api-sweep-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, each match with file/line, the disposition (removed-with-seam or deferred-finding), and an explicit statement that no banned API is silently retained. If any production file is modified to remove a banned API, rerun `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true` and record the result in the same evidence file before checking off.

---

### Phase 4 — Issue #218 Changed-Line Coverage Gap and Minimal Tests (Finding 2)

- [x] [P4-T1] Map the issue #218 production diff against the merge-base (`1b8536b6` from P0-T2) to the most recent available Cobertura `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/coverage-final-218.cobertura.xml` to identify any uncovered admission or initial-load behavior line (e.g., `TryQueueRemainingMailItemAsync`, `LoadRemainingEmailsToQueueAsync`, `InitEmailQueueAsync`, and the `QfcRemainingQueueAdmission` seam); write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/issue218-coverage-gap-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and the explicit list of uncovered issue #218 admission/initial-load lines (or `none uncovered`).
- [x] [P4-T2] For each uncovered issue #218 admission/initial-load line listed in `issue218-coverage-gap-cycle2-218.md`, add the smallest deterministic MSTest (MSTest + Moq + FluentAssertions, Arrange-Act-Assert, no live Outlook COM, no temporary files) that covers that line, placing it in the appropriate `QuickFiler.Test/Controllers/` test file and wiring any new test file into `QuickFiler.Test/QuickFiler.Test.csproj` with an explicit `<Compile Include>` item; if P4-T1 recorded `none uncovered`, record in the same gap evidence file that no test was required and check off without adding tests. Do not add tests that target out-of-scope code solely to raise repo-wide coverage.

---

### Phase 5 — Final C# QA Loop and Findings 2 & 3 Disposition

- [x] [P5-T1] Run `dotnet tool run csharpier -- check .` and write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-csharpier-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; if the check fails, run `dotnet tool run csharpier -- format .`, record the formatter result in the same file, restart Phase 5 at P5-T1, and check off only after a subsequent `dotnet tool run csharpier -- check .` exits `0` with evidence recorded.
- [x] [P5-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-analyzer-build-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; if the command fails or changes files, fix and restart Phase 5 at P5-T1.
- [x] [P5-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-nullable-build-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`; if the command fails or changes files, fix and restart Phase 5 at P5-T1.
- [x] [P5-T4] Run C# MSTest coverage with `& 'C:\Program Files\Microsoft Visual Studio\18\Community\Common7\IDE\CommonExtensions\Microsoft\TestWindow\vstest.console.exe' QuickFiler.Test\bin\Debug\QuickFiler.Test.dll UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll /EnableCodeCoverage /InIsolation /ResultsDirectory:TestResults\issue218-remediation-cycle2-final`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-mstest-coverage-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, total tests, pass/fail counts, the coverage attachment path, and the numeric repo-wide coverage headline; if the command fails, fix tests (no assertion weakening) and restart Phase 5 at P5-T1.
- [x] [P5-T5] Convert the P5-T4 coverage attachment to Cobertura at `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-coverage-cycle2-218.cobertura.xml`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-coverage-conversion-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`.
- [x] [P5-T6] Compare the merge-base baseline `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/baseline/coverage-baseline-218.cobertura.xml` against `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-coverage-cycle2-218.cobertura.xml`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/coverage-comparison-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, baseline repo-wide coverage, post-change repo-wide coverage, the delta, an explicit no-regression PASS/FAIL on changed lines, and an explicit positive-or-equal-delta PASS/FAIL.
- [x] [P5-T7] Regenerate the changed-production-line coverage (Finding 2) by mapping the merge-base (`1b8536b6`) production diff (`git diff 1b8536b6 -- QuickFiler/Controllers/*.cs`) to the line hits in `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/final-coverage-cycle2-218.cobertura.xml` across the post-split file set; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/changed-line-coverage-final-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the changed-production-line total, the covered changed-line total, the explicit numeric changed-line coverage percentage, and an explicit PASS/FAIL against the 90%-for-new/changed-code expectation.
- [x] [P5-T8] Write the authority-scoped repository-wide coverage exception (Finding 3) to `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/repo-wide-coverage-exception-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, the exact repo-wide figure re-derived from `final-coverage-cycle2-218.cobertura.xml`, the numeric gap to the raw 80% threshold, the `CLAUDE.md` testable-denominator exemption basis (VSTO add-in lifecycle, WinForms/Designer, Outlook Interop event-handler classes excluded from the floor), confirmation that change-scope gates pass (no regression per P5-T6, positive/equal delta per P5-T6, issue #218 changed-line coverage per P5-T7), and an explicit statement that repo-wide uplift to the raw 80% figure is out of scope for issue #218 and requires maintainer ratification via `feature/csharp-coverage-uplift`; do not modify any policy file.
- [x] [P5-T9] Re-run the focused issue #218 tests with `& 'C:\Program Files\Microsoft Visual Studio\18\Community\Common7\IDE\CommonExtensions\Microsoft\TestWindow\vstest.console.exe' QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation /TestCaseFilter:"FullyQualifiedName~QfcDatamodelTests|FullyQualifiedName~RunAsync_HighConfidenceEnabled_DoesNotPreFilterInitialGuiBatch|FullyQualifiedName~RunAsync_HighConfidence_LoadsInitialBatchWithoutPreFilter"`; write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/regression-testing/focused-pass-after-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and the pass/fail count confirming the issue #218 behavior is preserved after the test split.
- [x] [P5-T10] Run a final deterministic line-count check over all changed C# files (the eight production files and the six `QfcHomeController*Tests.cs` files from P0-T3); write `docs/features/active/2026-06-26-qfc-high-confidence-queue-filter-218/evidence/qa-gates/line-counts-final-cycle2-218.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, and a consolidated PASS/FAIL confirming every changed C# file is 500 lines or fewer.
