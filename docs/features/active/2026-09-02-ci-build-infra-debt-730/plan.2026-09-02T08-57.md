# 2026-09-02-ci-build-infra-debt-730 (Plan)

- **Issue:** #730
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-09-02T23-15
- **Status:** Draft
- **Version:** 0.5
- **Work Mode:** full-bug
- **AC Source:** `docs/features/active/2026-09-02-ci-build-infra-debt-730/spec.md` § `## Acceptance Criteria`

## Framing

- **AC count correction.** The delegation prompt for this plan stated `spec.md`'s `## Acceptance Criteria` section has 7 items. Direct re-read of `spec.md` in this pass shows the section spans lines 167–175 (heading at 167; 8 bulleted items, `AC1`–`AC8`, at lines 168–175). This plan and its AC-traceability section are built against the observed 8-item inventory, not the stated 7, per the Planner Adversarial Self-Review mandate to re-derive against current tree state rather than carry forward an uncited count.
- **No `[expect-fail]` regression-test phase.** Per CLAUDE.md's Bugfix Workflow and `spec.md`'s Repro & Evidence section, both findings are static configuration inspections with no executable failing state to capture (Finding 1: a cache fallback whose risk was investigated and refuted, not a functional defect; Finding 2: a missing suppression property, not a code defect). There is no application code path to red/green, so no synthetic failing test is authored. Phase 0's baseline msbuild rebuilds serve as the deterministic "before" state that Phase 2's rebuilds are diffed against, in place of a fail-before test.
- **No coverage-denominator impact.** Neither fix adds, removes, or modifies any `.cs` application source line. There is no new/changed-code coverage target and no repository-wide coverage delta to report; the Coverage Evidence Contract in `atomic-plan-contract` does not apply to this plan.
- **Evidence path.** All evidence in this plan resolves under `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/<kind>/`, per the Non-Overridable Evidence Path Clause. No `artifacts/baseline*`, `artifacts/qa*`, or `artifacts/coverage*` path is used anywhere in this plan.
- **Scope boundary carried from the orchestrator's directive:** no edit under the Claude runtime tree at path .claude (and everything beneath it), the Codex mirror tree at path .codex (and everything beneath it), the dot-agents tree at path .agents (and everything beneath it), or the two published configuration files at paths config/blast-radius.json and config/orchestration-routing.json; no coverage threshold/Pester job/coverage gate change; no edit to the workflow file at path .github/workflows/ci.yml; no edit to any .csproj, packages.config, or Directory.Build.targets file; no `System.Reactive` `PackageReference` migration.
- **Warning-count measurement method (round 4 correction).** Direct inspection of `evidence/baseline/msbuild-analyzers-pre.log` and its `msbuild-nullable-pre.log` sibling (both already on disk from the stopped executor run) confirms MSBuild's file logger at `verbosity=normal` prints every `System.Reactive.PackagesConfigCheck.targets` warning twice: once inline as each project builds (5 occurrences: analyzers-pre lines 7665, 8764, 10028, 10725, 11842; nullable-pre lines 7669, 8727, 10716, 10877, 11994), and again in the end-of-build per-target summary that immediately follows the single `Build succeeded.` line each log contains exactly once (analyzers-pre line 11846; nullable-pre line 11998) (analyzers-pre summary duplicates at 11851, 11856, 11861, 11866, 11871; nullable-pre summary duplicates at 12003, 12008, 12013, 12018, 12023) — 10 total matches per log for a naive whole-log `-SimpleMatch` count, confirmed by direct count. Both logs' own `5 Warning(s)` / `0 Error(s)` summary lines (analyzers-pre line 11873–11874; nullable-pre line 12025–12026) are, by contrast, printed exactly once and already report the correct, undoubled total. This plan adopts **option (a)**: `RxCheck_*` is derived by counting only the token's occurrences strictly before the log's single `Build succeeded.` line (the inline occurrences), and `W_*`/`E_*` are derived by parsing MSBuild's own once-emitted `^\s*(\d+) Warning\(s\)` / `^\s*(\d+) Error\(s\)` summary line rather than a whole-log token count. This choice is adopted over option (b) because it makes `W_*_pre` itself correct (5, not 10) rather than requiring every downstream delta assertion to be restated in doubled form, and because the `Build succeeded.` marker and the `N Warning(s)`/`N Error(s)` summary lines are MSBuild's own single-emission, format-stable output rather than an artifact of this repository's specific warning text, making the method more robust against reformatting of the `System.Reactive.PackagesConfigCheck.targets` message text itself.
- **Round 5 corrections.** (1) P2-T2's diff was rescoped to the same four Phase-1 pathspecs P2-T1 uses, because an unscoped `git diff --cached origin/main --name-status` also lists the four feature-folder documents (`issue.md`, this plan, `research/research.2026-09-02T09-15.md`, `spec.md`) already committed ahead of `origin/main`, producing 8 lines rather than 4 — re-confirmed this pass by direct inspection: those four documents are present and already tracked in the current tree (consistent with P0-T5's existing note that they predate this execution session), while `Directory.Build.props` and the three workflow edits are not yet present (P0-T2 confirms `Directory.Build.props` absence; P1-T1–P1-T4 remain unchecked), so the 4-vs-8 discrepancy is a property of the unscoped ref comparison, not of current working-tree state. (2) A new task P2-T10 was inserted after P2-T9 (confirmed, by re-reading this plan in this pass, to be the last task in Phase 2 before this insertion) and before P3-T9 (confirmed, by re-reading Phase 3, to be the commit task) to sanitize the absolute worktree-root path prefix out of all four raw MSBuild log files before they are committed — direct inspection of `evidence/baseline/msbuild-analyzers-pre.log` and `evidence/baseline/msbuild-nullable-pre.log` in this pass confirms both already carry 7765 case-insensitive occurrences of the operator account token, and the two post-change logs (`evidence/qa-gates/msbuild-analyzers-post.log`, `evidence/qa-gates/msbuild-nullable-post.log`) do not yet exist on disk (confirmed absent in this pass) because P2-T6/P2-T7 have not yet run; P2-T10 therefore runs after both P2-T6 and P2-T7 have created them and strictly before P3-T9 commits them. Per this repository's own no-absolute-host-path rule, P2-T10's substitution and verification steps derive the worktree-root path and the account token at run time (`git rev-parse --show-toplevel`, `Split-Path -Leaf $env:USERPROFILE`) rather than spelling either literal in this plan, so the plan and its evidence stay self-exempt from the rule they enforce. (3) P0-T3's example de-duplication script was corrected from `[0..($cut-1)]` to `[0..($cut-2)]` so the 0-based slice excludes the `Build succeeded.` line, matching its own "strictly before" prose (this was a documentation/code-consistency fix only; it does not change any recorded numeric value, since the `Build succeeded.` line never contains the `System.Reactive.PackagesConfigCheck.targets` token in either baseline log). (4) P3-T9 now carries an explicit closing note that its own subsequent checkbox-completion edit to this plan file is expected, accepted checklist-metadata residue, not an unclean delivery.
- **Round 8 correction (ancestor `.editorconfig` path leak).** [P2-T10]'s existing worktree-root substitution correctly reduced worktree-root-prefixed occurrences to 0 in all four MSBuild log files (before/after line counts stayed identical at 11878/11878, 12030/12030, 11906/11906, 11742/11742 across the stopped executor run — unaffected by this round's change and not re-derived). However, every `csc.exe` compiler-invocation line in all four logs also embeds a second, distinct absolute path via an `/analyzerconfig:` argument pointing at the main-checkout `.editorconfig` — an ancestor of the worktree root, not itself prefixed by the worktree-root string, so the existing substitution never matches it. Direct re-inspection of `evidence/baseline/msbuild-analyzers-pre.log:1139` in this pass confirms the leak on the current on-disk (partially-sanitized) file: the line carries two `/analyzerconfig:` arguments back to back — the first is the raw, unsanitized main-checkout-root path (this planning document does not quote that raw path, per the same no-absolute-host-path rule the round-5 note above already applies to this file), the second is `<repo-root>\.editorconfig`, the already-sanitized worktree-rooted duplicate reference MSBuild also emits for the same file. This produced 144 residual account-token matches across the four logs (36 `csc.exe` invocations per log × 4 logs), which the executor's residual-verification step correctly caught and refused to paper over with a false `residual count: 0` artifact. [P2-T10] is extended, not replaced, with a second, strictly-later substitution step that derives the main-checkout root at run time via `git rev-parse --path-format=absolute --git-common-dir` (the shared `.git` directory's parent) rather than a hardcoded ascend-N-levels convention — chosen because it is correct regardless of how deeply the worktree is nested under the checkout root, whereas an ascend-N-levels shortcut silently breaks if that nesting depth ever changes — and maps it to a distinct placeholder, `<main-checkout-root>`, kept distinct from `<repo-root>` because the two roots are visibly different and appear juxtaposed on the same log line (line 1139 above): collapsing both to `<repo-root>` would make the sanitized log falsely assert the worktree root and its ancestor checkout are the same path, destroying the log's own record that MSBuild emits two distinct `/analyzerconfig:` arguments for the same file. The main-checkout-root substitution MUST run strictly after the worktree-root substitution has completed for all four files, because the main-checkout-root string is itself a prefix of the worktree-root string; running it first would truncate-match and corrupt every worktree-root occurrence before the worktree-root substitution could run against clean content. This round changes only [P2-T10]'s task text; the before/after line-count invariant, the residual-verification final check, and the account-token derivation are all still governed by the same reasoning already established in the round-5 note above and remain unaffected, since the addition is a second pure substring substitution, not a line-structural change — none of P0-T3, P0-T4, P2-T6, or P2-T7's line-number or count citations are touched by this round.

---

### Phase 0 — Policy Reads & Pre-Change Baseline Capture

- [x] [P0-T1] Read, in the order defined by `policy-compliance-order`, the five policy files applicable to this change — CLAUDE.md, the general code-change rule at path .claude/rules/general-code-change.md, the general unit-test rule at path .claude/rules/general-unit-test.md, the C# rule at path .claude/rules/csharp.md (in scope because CLAUDE.md's C# Code Change Policy explicitly extends to `*.props` files, and Phase 1 creates `Directory.Build.props`), and the CI-workflows rule at path .claude/rules/ci-workflows.md (the rule scoped to the .github/workflows directory tree, in scope because Phase 1 edits three workflow files) — then write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:` (the five files listed in the order read), and an explicit itemized list of the five file paths. Acceptance: the artifact file exists and lists all five paths verbatim.

- [x] [P0-T2] Confirm the pre-change absence of `Directory.Build.props` at the repository root: run `Test-Path Directory.Build.props` and record the result in `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/directory-build-props-absence.<timestamp>.md` with `Timestamp:`, `Command: Test-Path Directory.Build.props`, `EXIT_CODE: 0`, and `Output Summary:` recording the literal boolean printed. Acceptance: the artifact records the literal value `False`, establishing that Phase 0's baseline rebuilds (P0-T3, P0-T4) run before the file exists.

- [x] [P0-T3] Execute, before any Phase 1 task has run, `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true /flp:logfile=docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-analyzers-pre.log;verbosity=normal` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-analyzers-pre.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording three numeric values read from the log file using the de-duplicating method fixed by the Framing section's "Warning-count measurement method" note (option (a), chosen because a whole-log `-SimpleMatch` count double-counts: MSBuild's file logger at `verbosity=normal` prints every `System.Reactive.PackagesConfigCheck.targets` warning once inline and again in the end-of-build summary): (a) `RxCheck_analyzers_pre` = the count of lines matching the literal token `System.Reactive.PackagesConfigCheck.targets` found strictly before the line matching the literal token `Build succeeded.` (the log contains exactly one such line; e.g. `$content = Get-Content <logfile>; $cut = ($content | Select-String -Pattern 'Build succeeded.' -SimpleMatch).LineNumber[0]; ($content[0..($cut-2)] | Select-String -Pattern 'System.Reactive.PackagesConfigCheck.targets' -SimpleMatch).Count` — the 0-based slice upper bound is `$cut-2`, not `$cut-1`, because `$cut` is the 1-based line number of the `Build succeeded.` line itself and the slice must exclude that line to match the "strictly before" semantics stated above), (b) `W_analyzers_pre` = the integer captured from MSBuild's own once-emitted end-of-build line matching `^\s*(\d+) Warning\(s\)` (via `(Select-String -Path <logfile> -Pattern '^\s*(\d+) Warning\(s\)').Matches.Groups[1].Value`, not a whole-log token count), and (c) `E_analyzers_pre` = the integer captured the same way from the line matching `^\s*(\d+) Error\(s\)`. Acceptance: the artifact records all three values with `RxCheck_analyzers_pre` equal to exactly 5 (one inline occurrence per affected project: QuickFiler, TaskMaster, ToDoModel, UtilitiesCS, and UtilitiesCS.Test, per research §2.1's confirmed exhaustive derivation), `W_analyzers_pre` equal to exactly 5 (this clean baseline build carries no warnings other than the five System.Reactive suppressions), and `E_analyzers_pre` equal to 0.

- [x] [P0-T4] Execute, before any Phase 1 task has run, `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true /flp:logfile=docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-nullable-pre.log;verbosity=normal` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-nullable-pre.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording `RxCheck_nullable_pre`, `W_nullable_pre`, and `E_nullable_pre` using the same de-duplicating method defined in P0-T3 (option (a): `RxCheck_nullable_pre` counted strictly before this log's single `Build succeeded.` line; `W_nullable_pre` and `E_nullable_pre` parsed from this log's own once-emitted `^\s*(\d+) Warning\(s\)` / `^\s*(\d+) Error\(s\)` summary line), read from this command's log file. Acceptance: the artifact records all three values with `RxCheck_nullable_pre` equal to exactly 5, `W_nullable_pre` equal to exactly 5, and `E_nullable_pre` equal to 0 (the System.Reactive.PackagesConfigCheck diagnostic is an MSBuild target-emitted `<Warning>`, not a compiler `CSxxxx`/`BCxxxx` diagnostic, so `/p:TreatWarningsAsErrors=true` — which is consumed by the Csc/Vbc compiler tasks, not by arbitrary target `<Warning>` calls — does not promote it to an error; this is consistent with `spec.md`'s Impact/Severity statement that neither finding causes a build failure today).

- [x] [P0-T5] Record the pre-change git baseline: run `git rev-parse HEAD` and `git status --porcelain` on branch `bug/ci-build-infra-debt-730`, and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/git-state-pre.<timestamp>.md` with `Timestamp:`, the recorded HEAD SHA, and the `git status --porcelain` output. `issue.md`, `spec.md`, and `research/research.2026-09-02T09-15.md` are already tracked at HEAD — committed by a prior preparation-mode commit (`docs(ci-build-infra-debt): prepare issue #730 feature folder and atomic plan`) that predates this or any execution session — so none of the three appears in porcelain output; only this plan file (modified by this task's own preceding P0-T1/P0-T2 checkbox edits) and the evidence subdirectory (created and partially populated by P0-T1 and P0-T2, both of which run before this task) are unclean at the point this task executes. Acceptance: the artifact records a 40-character SHA and confirms `git status --porcelain` output contains exactly two lines — ` M docs/features/active/2026-09-02-ci-build-infra-debt-730/plan.2026-09-02T08-57.md` and `?? docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/` — and no other entries, before Phase 1 begins.

---

### Phase 1 — Implementation (Comment-Only Workflow Edits + New `Directory.Build.props`)

- [x] [P1-T1] In `.github/workflows/_build-analyzers.yml`, insert the following 16 comment lines immediately above the `restore-keys:` line (currently line 40, re-derived directly against the current tree in this planning pass), at the same 12-space indentation as the `restore-keys:` block-scalar value line (one level deeper than the sibling `key:`/`restore-keys:` lines, which sit at 10 spaces), with no other change to the file:
  ```
            # The bare-prefix fallback below is safe against stale package
            # versions: `nuget restore` (next step) always runs unconditionally
            # and is idempotent per package for packages.config-style restores —
            # each package is materialized under a version-qualified directory
            # (packages/{id}.{version}/, matching every HintPath in this repo's
            # .csproj files). A fallback cache populated under an older
            # packages.config hash can therefore only ever contribute either
            # (a) version-folders that still match the current packages.config
            # (a legitimate, desired reuse) or (b) inert orphaned version-
            # folders for packages no longer referenced by any HintPath. Either
            # way, `nuget restore` fetches exactly the delta implied by the
            # current packages.config from the network before the build step
            # runs, so a fallback hit can never cause the build to compile
            # against a package version other than the one packages.config
            # names. See docs/features/active/2026-09-02-ci-build-infra-debt-730/
            # research/ for the full analysis (issue #730).
  ```
  Acceptance: the file's `key:`, `path:`, `uses:` (`actions/cache@v4`), the `restore-keys:` key and its value (`nuget-${{ runner.os }}-`), and the subsequent `Restore solution` step are byte-identical to before; the only change is these 16 inserted comment lines.

- [x] [P1-T2] In `.github/workflows/_build-nullable.yml`, insert the identical 16 comment lines quoted in P1-T1 immediately above the `restore-keys:` line (currently line 40, re-derived directly against the current tree in this planning pass), at the same 12-space indentation as the `restore-keys:` block-scalar value line (one level deeper than the sibling `key:`/`restore-keys:` lines, which sit at 10 spaces), with no other change to the file. Acceptance: same as P1-T1, applied to `.github/workflows/_build-nullable.yml`.

- [x] [P1-T3] In `.github/workflows/_mstest-coverage.yml`, insert the identical 16 comment lines quoted in P1-T1 immediately above the `restore-keys:` line (currently line 40, re-derived directly against the current tree in this planning pass), at the same 12-space indentation as the `restore-keys:` block-scalar value line (one level deeper than the sibling `key:`/`restore-keys:` lines, which sit at 10 spaces), with no other change to the file. Acceptance: same as P1-T1, applied to `.github/workflows/_mstest-coverage.yml`.

- [x] [P1-T4] Create a new file `Directory.Build.props` at the repository root (confirmed absent pre-change in P0-T2) with exactly this content:
  ```xml
  <Project>
    <!--
      System.Reactive 7.0.0+ refuses to build cleanly against packages.config
      projects (see System.Reactive.PackagesConfigCheck.targets) and instead
      emits an "unsupported scenario" warning on every build of every project
      that references it. This repository intentionally keeps its legacy
      non-SDK VSTO / .NET Framework 4.8.1 projects on packages.config (see
      .claude/rules/csharp.md) rather than migrating to PackageReference, so
      the warning is accepted here as a known, deliberate trade-off rather than
      fixed by migration. RxUseUnsupportedPackagesConfig=true is the package's
      own documented suppression switch for this exact scenario. See issue #730
      and docs/features/active/2026-09-02-ci-build-infra-debt-730/ for the
      accepted-trade-off rationale.
    -->
    <PropertyGroup>
      <RxUseUnsupportedPackagesConfig>true</RxUseUnsupportedPackagesConfig>
    </PropertyGroup>
  </Project>
  ```
  Acceptance: the file exists at the repository root and its content is byte-identical to the block quoted above.

---

### Phase 2 — Verification & Final QC

- [x] [P2-T1] Stage exactly the four changed files: `git add Directory.Build.props .github/workflows/_build-analyzers.yml .github/workflows/_build-nullable.yml .github/workflows/_mstest-coverage.yml`, then run `git status --porcelain -- Directory.Build.props .github/workflows/_build-analyzers.yml .github/workflows/_build-nullable.yml .github/workflows/_mstest-coverage.yml` (scoped to just these four pathspecs, so the plan file's own checkbox-edit modification and the evidence directory recorded in P0-T5 fall outside this command's view) and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/git-stage-scope.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the full porcelain output. Acceptance: the scoped porcelain output lists exactly these four staged paths — `A  Directory.Build.props`, `M  .github/workflows/_build-analyzers.yml`, `M  .github/workflows/_build-nullable.yml`, `M  .github/workflows/_mstest-coverage.yml` — and no other entries.

- [x] [P2-T2] Run `git diff --cached origin/main --name-status -- Directory.Build.props .github/workflows/_build-analyzers.yml .github/workflows/_build-nullable.yml .github/workflows/_mstest-coverage.yml` (anchored to the `origin/main` ref and scoped to these same four Phase-1 pathspecs used by P2-T1, rather than an unscoped diff against `origin/main` — an unscoped diff would additionally list the four feature-folder documents (`issue.md`, this plan file, `research/research.2026-09-02T09-15.md`, `spec.md`) that are already committed ahead of `origin/main` by a prior preparation-mode commit, producing 8 lines instead of 4; paired with the `git status --porcelain` companion captured in P2-T1) and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/git-scope-boundary.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the full name-status output and an explicit statement of zero occurrences of each of these substrings in that output: .csproj, packages.config, Directory.Build.targets, .claude/, .codex/, .agents/, config/blast-radius.json, config/orchestration-routing.json. Acceptance: the artifact records the scoped name-status output matching the four-line set from P2-T1 exactly (no fifth or further line) and confirms none of the listed substrings appear.

- [x] [P2-T3] Run `git diff --cached origin/main -- .github/workflows/_build-analyzers.yml` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/diff-build-analyzers.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the count of added lines (`+`-prefixed, excluding the `+++` file-header line) and the count of removed lines (`-`-prefixed, excluding the `---` file-header line), plus confirmation each added line matches one of the 16 comment lines quoted in P1-T1. Acceptance: the artifact records exactly 16 added lines and 0 removed lines.

- [x] [P2-T4] Run `git diff --cached origin/main -- .github/workflows/_build-nullable.yml` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/diff-build-nullable.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the count of added lines (`+`-prefixed, excluding the `+++` file-header line) and the count of removed lines (`-`-prefixed, excluding the `---` file-header line), plus confirmation each added line matches one of the 16 comment lines quoted in P1-T1. Acceptance: the artifact records exactly 16 added lines and 0 removed lines.

- [x] [P2-T5] Run `git diff --cached origin/main -- .github/workflows/_mstest-coverage.yml` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/diff-mstest-coverage.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the count of added lines (`+`-prefixed, excluding the `+++` file-header line) and the count of removed lines (`-`-prefixed, excluding the `---` file-header line), plus confirmation each added line matches one of the 16 comment lines quoted in P1-T1. Acceptance: the artifact records exactly 16 added lines and 0 removed lines.

- [x] [P2-T6] After Phase 1 is complete, execute `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true /flp:logfile=docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-analyzers-post.log;verbosity=normal` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-analyzers-post.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording, using the same de-duplicating method defined in P0-T3 (option (a): `RxCheck_*` counted strictly before this log's `Build succeeded.` line; `W_*`/`E_*` parsed from this log's own `^\s*(\d+) Warning\(s\)` / `^\s*(\d+) Error\(s\)` summary line, not a whole-log token count): (a) `RxCheck_analyzers_post`, (b) `W_analyzers_post`, and (c) `E_analyzers_post`, read from this command's log file. Acceptance: the artifact records `RxCheck_analyzers_post == 0` (down from the `RxCheck_analyzers_pre == 5` baseline recorded in P0-T3), `W_analyzers_post == W_analyzers_pre - 5` (baseline `W_analyzers_pre == 5` from P0-T3, so `W_analyzers_post` is expected to equal 0), and `E_analyzers_post == E_analyzers_pre` (both baseline and post-change error counts recorded, expected 0 in both).

- [x] [P2-T7] After Phase 1 is complete, execute `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true /flp:logfile=docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-nullable-post.log;verbosity=normal` and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-nullable-post.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording `RxCheck_nullable_post`, `W_nullable_post`, and `E_nullable_post` using the same de-duplicating method defined in P0-T3 (option (a): `RxCheck_*` counted strictly before this log's `Build succeeded.` line; `W_*`/`E_*` parsed from this log's own `^\s*(\d+) Warning\(s\)` / `^\s*(\d+) Error\(s\)` summary line, not a whole-log token count), read from this command's log file. Acceptance: the artifact records `RxCheck_nullable_post == 0` (down from the `RxCheck_nullable_pre == 5` baseline recorded in P0-T4), `W_nullable_post == W_nullable_pre - 5` (baseline `W_nullable_pre == 5` from P0-T4, so `W_nullable_post` is expected to equal 0), and `E_nullable_post == E_nullable_pre` (both expected 0).

- [x] [P2-T8] Run `dotnet tool run csharpier check .` (read-only verify command; not a write-mode command, so no G7 rewrite-literal is required) and write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/csharpier-check.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the literal console output observed at run time. Acceptance: `EXIT_CODE: 0` is recorded. (Per the .csharpierignore file, `*.props` and `*.yml` are outside CSharpier's checked-extension set — the .csharpierignore file excludes `*.props` explicitly and CSharpier does not process `.yml` at all — so the new `Directory.Build.props` and the three comment-only workflow edits are not expected to introduce any formatting drift; this task executes the command and records its actual exit code rather than assuming the result.)

- [x] [P2-T9] Locate `vstest.console.exe` via `vswhere.exe` using the same resolution the repository's own `.github/workflows/_mstest-coverage.yml:60-67` uses (`Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'`, then `-latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe'`), locate UtilitiesCS.Test.dll and QuickFiler.Test.dll under their respective bin\Debug\ output directories using the same filter `.github/workflows/_mstest-coverage.yml:70-76` uses (path matches bin\Debug\, excludes obj\ and ref\), then run `& $vstestPath UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /EnableCodeCoverage /InIsolation /Logger:trx /TestCaseFilter:"TestCategory!=LiveOutlook"`, then write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/vstest-regression.<timestamp>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` recording the literal Passed/Failed/Total counts printed in the console output. Acceptance: `EXIT_CODE: 0` and `Failed: 0` are both recorded, confirming the Rx-dependent MSTest suites pass unchanged after the `Directory.Build.props` addition.

- [x] [P2-T10] Sanitize BOTH absolute host-path prefixes out of all four raw MSBuild log files before any of them is committed — `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-analyzers-pre.log`, `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-nullable-pre.log`, `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-analyzers-post.log`, `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-nullable-post.log` (the first two exist after P0-T3/P0-T4; the last two exist after P2-T6/P2-T7, both of which have already run by this point in the phase) — because MSBuild's normal-verbosity logger prints the full absolute path to `TaskMaster.sln` and to every project file on effectively every diagnostic line via the worktree-rooted path, AND because every `csc.exe` compiler-invocation line in all four logs additionally embeds a second, distinct absolute path via an `/analyzerconfig:` argument pointing at the main-checkout `.editorconfig` — an ancestor of the worktree root, not itself prefixed by the worktree-root string, so a worktree-root-only substitution never matches it (confirmed by direct inspection of `evidence/baseline/msbuild-analyzers-pre.log:1139`, whose `csc.exe` invocation reads `...analyzerconfig:<main-checkout-root literal>\.editorconfig /analyzerconfig:<worktree-root literal>\.editorconfig...` on one line — the raw ancestor path immediately followed by the worktree-rooted duplicate reference MSBuild also emits for the same file). Per this repository's own no-absolute-host-path rule, no committed artifact may carry an operator account name or host path in any form. Do not hardcode the worktree-root path, the main-checkout-root path, or the operator account name as a literal string in this task or in any artifact it writes; derive all three at run time so the plan and its evidence stay self-exempt from the rule they enforce.

  Step 1 (worktree-root substitution — must run first): `$repoRoot = (git rev-parse --show-toplevel).Trim() -replace '/','\'; $repoRootFwd = $repoRoot -replace '\\','/'; $userToken = [regex]::Escape((Split-Path -Leaf $env:USERPROFILE))`. For each of the four files, in place: `$c = Get-Content -Raw -LiteralPath <file>; $c = $c -ireplace [regex]::Escape("$repoRoot\"), '<repo-root>\'; $c = $c -ireplace [regex]::Escape("$repoRootFwd/"), '<repo-root>/'; Set-Content -LiteralPath <file> -Value $c -NoNewline`.

  Step 2 (main-checkout-root substitution — must run strictly after Step 1 has completed for all four files, against the already-worktree-sanitized content): derive the main-checkout root without assuming a fixed ascend-N-levels nesting depth — `$gitCommonDir = (git -C $repoRoot rev-parse --path-format=absolute --git-common-dir).Trim() -replace '/','\'; $mainCheckoutRoot = Split-Path -Parent $gitCommonDir; $mainCheckoutRootFwd = $mainCheckoutRoot -replace '\\','/'` (`--git-common-dir` resolves, from any linked worktree, to the single `.git` directory shared with the main checkout; its parent is exactly the ancestor directory that owns the leaking `.editorconfig`; this is preferred over a hardcoded ascend-levels shortcut because it remains correct regardless of how deeply the worktree is nested under the checkout root). For each of the same four files, in place: `$c = Get-Content -Raw -LiteralPath <file>; $c = $c -ireplace [regex]::Escape("$mainCheckoutRoot\"), '<main-checkout-root>\'; $c = $c -ireplace [regex]::Escape("$mainCheckoutRootFwd/"), '<main-checkout-root>/'; Set-Content -LiteralPath <file> -Value $c -NoNewline`. Step 2 MUST NOT run before Step 1: the main-checkout-root string is itself a prefix of the worktree-root string, so applying it first would truncate-match and corrupt every worktree-root occurrence before Step 1 could run against clean content. Use the distinct placeholder `<main-checkout-root>` rather than reusing `<repo-root>`, because the two roots are visibly different and appear juxtaposed on the same log line (the line-1139 citation above): collapsing both to `<repo-root>` would make the sanitized log falsely assert the worktree root and its ancestor checkout are the same path, destroying the log's own record that MSBuild emits two distinct `/analyzerconfig:` arguments for the same file.

  Both steps are pure substring substitutions performed within each existing line — neither step inserts, deletes, reorders, or merges any line in any of the four files, so every line-number citation already recorded against these files in P0-T3, P0-T4, P2-T6, P2-T7, and this plan's own Framing section "Warning-count measurement method" bullet remains valid unchanged after this task runs; verify this invariant by recording each file's `(Get-Content -LiteralPath <file>).Count` immediately before Step 1 and again immediately after Step 2 completes, confirming the two counts are identical for all four files.

  After both substitutions, verify zero residual leakage with a single case-insensitive sweep for the run-time-derived account token across all absolute-path forms now present in the logs (the sweep is agnostic to which root the token was embedded in, so one sweep still covers both the worktree-root and main-checkout-root leaks): `@(Get-ChildItem -LiteralPath <the four files> | Select-String -Pattern "(?i)$userToken").Count` must equal 0 across all four files.

  Write `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/log-sanitization.<timestamp>.md` with `Timestamp:`, `Command:` (the Step 1 and Step 2 substitution commands and the verification commands actually run), `EXIT_CODE:`, and `Output Summary:` recording (a) the before/after line-count-identical confirmation for all four files, and (b) the residual-match count (must read 0) — describe the two substituted tokens only by class (the worktree-root path prefix and the main-checkout-root path prefix, each in both backslash and forward-slash forms) and do not quote either raw path or the operator account name as a literal "before" value anywhere in this artifact.

  Acceptance: the artifact records `EXIT_CODE: 0`, records all four before/after line counts as identical pairs, and records the residual-match count as exactly 0 for all four files, with no raw absolute path (worktree-root or main-checkout-root form) or account-name literal appearing anywhere in the artifact's own prose.

---

### Phase 3 — AC Traceability, Acceptance-Criteria Check-off, and Commit

- [x] [P3-T1] In `docs/features/active/2026-09-02-ci-build-infra-debt-730/spec.md`, check off AC1 (line 168, the comment-block-present criterion) from `- [ ]` to `- [x]`, citing evidence: implementation P1-T1/P1-T2/P1-T3 and verification P2-T3/P2-T4/P2-T5 (16-line comment-only diff confirmed in all three workflow files). Acceptance: the AC1 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T2] In `spec.md`, check off AC2 (line 169, the `Directory.Build.props` existence/content criterion) from `- [ ]` to `- [x]`, citing evidence: P1-T4 (file creation, content byte-identical to the quoted block). Acceptance: the AC2 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T3] In `spec.md`, check off AC3 (line 170, the zero-warnings-post-rebuild criterion) from `- [ ]` to `- [x]`, citing evidence: baseline P0-T3/P0-T4 (`RxCheck_*_pre == 5`) and post-change P2-T6/P2-T7 (`RxCheck_*_post == 0`, `W_*_post == W_*_pre - 5`, `E_*_post == E_*_pre`). Acceptance: the AC3 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T4] In `spec.md`, check off AC4 (line 171, the no-.csproj/packages.config/Directory.Build.targets/application-source-modified criterion) from `- [ ]` to `- [x]`, citing evidence: P2-T1/P2-T2 (exactly four changed files, none matching the excluded path substrings). Acceptance: the AC4 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T5] In `spec.md`, check off AC5 (line 172, the no-coverage-threshold/Pester-job/coverage-gate-change criterion) from `- [ ]` to `- [x]`, citing evidence: P2-T2's exhaustive four-file change enumeration (no coverage/Pester/gate-related file present) together with the literal diff content quoted in P1-T1 through P1-T4 (comment text and MSBuild property only, no coverage/threshold token present). Acceptance: the AC5 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T6] In `spec.md`, check off AC6 (line 173, the Rx-dependent MSTest-suites-pass criterion) from `- [ ]` to `- [x]`, citing evidence: P2-T9 (`EXIT_CODE: 0`, `Failed: 0` for UtilitiesCS.Test and QuickFiler.Test). Acceptance: the AC6 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T7] In `spec.md`, check off AC7 (line 174, the full-toolchain-pass criterion) from `- [ ]` to `- [x]`, citing evidence: P2-T8 (`csharpier check`, `EXIT_CODE: 0`), P2-T6/P2-T7 (both `msbuild /t:Rebuild` commands, `EXIT_CODE: 0`), and P2-T9 (`vstest.console.exe` regression re-run, `EXIT_CODE: 0`). Acceptance: the AC7 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task.

- [x] [P3-T8] In `spec.md`, check off AC8 (line 175, the evidence-capture-location criterion) from `- [ ]` to `- [x]`, citing evidence: all baseline artifacts under `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/` (P0-T1 through P0-T5) and all final-QC artifacts under `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/` (P2-T6 through P2-T9). Acceptance: the AC8 checkbox in `spec.md` reads `- [x]` and no other line in the Acceptance Criteria section is altered by this task; confirm via `Get-ChildItem` that both `evidence/baseline/` and `evidence/qa-gates/` directories are non-empty.

- [ ] [P3-T9] Stage and commit all changes and evidence artifacts: run `git add Directory.Build.props .github/workflows/_build-analyzers.yml .github/workflows/_build-nullable.yml .github/workflows/_mstest-coverage.yml docs/features/active/2026-09-02-ci-build-infra-debt-730/spec.md docs/features/active/2026-09-02-ci-build-infra-debt-730/issue.md docs/features/active/2026-09-02-ci-build-infra-debt-730/research/research.2026-09-02T09-15.md docs/features/active/2026-09-02-ci-build-infra-debt-730/plan.2026-09-02T08-57.md` (the four files from Phase 1; `spec.md`, modified by Phase 3's own AC check-off edits; `issue.md` and `research/research.2026-09-02T09-15.md`, both already tracked and unmodified since the prior preparation-mode commit, so re-adding them here is a harmless no-op; and this plan file itself, modified by this and earlier rounds' checkbox edits), then run `git add -f docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/` (force-add is required because the repository root `.gitignore:84`'s blanket `*.log` rule would otherwise silently exclude the four MSBuild log artifacts `msbuild-analyzers-pre.log`, `msbuild-nullable-pre.log`, `msbuild-analyzers-post.log`, and `msbuild-nullable-post.log` from a plain `git add`; by this point in the plan all four have already been sanitized and residual-match-verified by P2-T10, so no raw absolute-path or account-name content is being committed here), then run `git status --porcelain --ignored=no` before committing and confirm each of these four literal paths appears with an `A  ` status: `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-analyzers-pre.log`, `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/baseline/msbuild-nullable-pre.log`, `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-analyzers-post.log`, and `docs/features/active/2026-09-02-ci-build-infra-debt-730/evidence/qa-gates/msbuild-nullable-post.log`, then commit, then run `git status --porcelain`. Acceptance: the pre-commit `git status --porcelain --ignored=no` check confirms all four named `.log` files staged with `A  ` status, and the post-commit `git status --porcelain` output is empty (clean worktree). Note: the executor's own subsequent edit marking this task's checkbox `[x]` in this plan file necessarily happens after this task's clean-tree verification and therefore leaves exactly one residual porcelain line (`M` on this plan file); this is expected checklist-metadata residue, not an unclean delivery, and no further task reconciles it.
