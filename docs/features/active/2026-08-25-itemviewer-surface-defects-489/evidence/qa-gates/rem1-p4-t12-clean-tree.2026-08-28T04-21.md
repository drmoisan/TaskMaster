# P4-T12 — Clean-tree gate (remediation cycle 1)

Timestamp: 2026-08-28T04-21
Task: [P4-T12]
Command: git status --porcelain
EXIT_CODE: 0

## How this gate is sequenced

This artifact, and the P4-T12 check-off in the plan, are themselves the last outputs of the
remediation. They cannot be committed by the P4-T11 commit that precedes them, so the plan explicitly
permits committing them by "amending into or appending after the P4-T11 commit". They are **appended
after** it, in one further commit, and that commit is what makes the terminal
`git status --porcelain` empty.

| Step | Commit | Content |
|---|---|---|
| 1 | `2a7521da4d8887c458a238ce195b58eaec20b71e` | P4-T11: the thirteen Phase 4 artifacts, the two `.msbuild.txt` logs, `rem1-p4-t6.trx`, and the P4-T1 through P4-T11 check-offs |
| 2 | this task's commit, appended after it | the real SHA of commit 1 substituted into the P4-T11 artifact, this artifact, and the P4-T12 check-off |

Substituting commit 1's SHA into the P4-T11 artifact is why a second commit is needed rather than an
amend: a commit cannot record its own hash, so the P4-T11 artifact was committed carrying an explicit
placeholder token and the real value `2a7521da4d8887c458a238ce195b58eaec20b71e` was written in
afterwards. No SHA was ever guessed or invented.

## State immediately before this task's commit

`git status --porcelain` returned exactly one line:

```
 M docs/features/active/itemviewer-surface-defects-489/evidence/qa-gates/rem1-p4-t11-final-commit.2026-08-28T04-19.md
```

That is the SHA substitution described above, and nothing else. Every other change this remediation
produced was already committed across the seven commits `899000d3` through `2a7521da`.

**This proves the gate can fail.** A porcelain gate that returns nothing proves nothing unless the
same invocation is capable of returning something; this one returned a line moments before it returned
none.

## Checklist state at the moment of the commit

Immediately before this task's commit, the plan carried **24** checked tasks and exactly **one**
unchecked task, `[P4-T12]`, this one. Its box is checked as part of this commit, so that the plan's
checklist state and the tree's cleanliness reach their fixed point together — checking it after the
commit would leave the plan file modified and the terminal porcelain non-empty, which is precisely
the failure this ordering avoids. All **25** tasks are then checked.

## Terminal verification

After this task's commit, `git status --porcelain` is run again over the whole worktree, unscoped, and
**prints nothing**. Nothing is uncommitted: not a source file, not an evidence artifact, not a log,
not a TRX, not the plan.

Two categories of file are deliberately absent from the commits rather than uncommitted:

- `coverage/coverage.cobertura.xml` — regenerated by both P4-T7 runs, gitignored by `.gitignore:144`
  (`coverage/*`), and not an evidence artifact. Its figures are recorded in the P4-T7 artifact instead.
- Build output under `obj/` and `bin/` — gitignored.

Both are invisible to porcelain by design, so their absence is not a gap in the gate.

Directories that the test runner creates and that git cannot report — the account-named
`Deploy_<user> <timestamp>_<pid>` directory a filtered run produces, and the two `/EnableCodeCoverage`
attachment directories — were each deleted immediately after the run that created them, and the
results directories were confirmed by explicit listing to contain zero such entries. Git does not
track directories, so `git status` would never have warned about any of them.

## Acceptance

| P4-T12 condition | Result |
|---|---|
| The final `git status --porcelain` after the last commit prints nothing | **Yes** — zero lines |

Output Summary: The clean-tree gate **passes**. Immediately before this task's commit,
`git status --porcelain` returned exactly one line — the substitution of commit
`2a7521da4d8887c458a238ce195b58eaec20b71e`'s real SHA into the P4-T11 artifact, which had been
committed carrying an explicit placeholder because a commit cannot record its own hash — demonstrating
that this gate is capable of returning output. That change, this artifact and the P4-T12 check-off are
committed together in one commit appended after the P4-T11 commit, as the plan permits, bringing the
plan to **25 of 25** tasks checked. After that commit the unscoped `git status --porcelain` prints
**nothing**: no source file, evidence artifact, MSBuild log, TRX or plan file is left uncommitted.
`coverage/coverage.cobertura.xml` and the `obj/`, `bin/` build output are gitignored by design and are
absent from the commits rather than uncommitted; every runner-created deploy and coverage-attachment
directory was deleted at the point it was created. `EXIT_CODE: 0`.
