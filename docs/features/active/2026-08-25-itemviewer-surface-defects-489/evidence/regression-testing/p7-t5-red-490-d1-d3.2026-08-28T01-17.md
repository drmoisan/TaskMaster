# P7-T5 — [expect-fail] RED for issue #490 D1 and D3, interface-surface half

Timestamp: 2026-08-28T01-17
Command: vstest.console.exe QuickFiler.Test\bin\Debug\QuickFiler.Test.dll /InIsolation "/TestCaseFilter:FullyQualifiedName~IItemViewer_DeclaresAddFolderItemsAndNotSetFolderItems|FullyQualifiedName~IItemViewer_FocusSubjectReturnsBool" "/Logger:trx;LogFileName=p7-t5.trx" /ResultsDirectory:<temp-results-dir>
EXIT_CODE: 1
ExpectedExitCode: 1

## Acceptance

`Total tests: 2`, `Failed: 2`, 0 passed, 0 skipped, in 1.21 seconds. Both tests fail, which is the
expected outcome for this task: the interface surface they assert has not been changed yet. P8-T1
and P8-T4 make them green.

TRX: `evidence/regression-testing/p7-t5.trx` (sanitised; 2 `UnitTestResult` elements, matching the
total above; the file parses under a strict XML reader after redaction).

## Failure messages — each fails for the intended reason

`IItemViewer_DeclaresAddFolderItemsAndNotSetFolderItems` [131 ms], both clauses of its
`AssertionScope` failing:

```
Expected added not to be <null> because the additive folder-list member must be named for what it does.
Expected renamed to be <null> because the misleading SetFolderItems name must not survive the rename, but found IItemViewer.SetFolderItems.
```

The second clause names the member that must disappear, `IItemViewer.SetFolderItems`, and the first
records that `AddFolderItems` does not yet exist on the interface. This is the #490 D1 rename RED.

`IItemViewer_FocusSubjectReturnsBool` [1 ms]:

```
Expected type to be System.Boolean because a caller cannot observe a failed subject focus through a void member, but found System.Void.
```

That names the current `void FocusSubject();` return type directly. This is the runtime half of the
#490 D3 RED; the compile-time half is P7-T7 and P7-T8.

## Results-directory substitution

PlanPrintedResultsDirectory: docs\features\active\itemviewer-surface-defects-489\evidence\regression-testing

The run was pointed at a directory under the system temp tree instead, and only the sanitised TRX
was copied back to the canonical evidence path. `vstest.console.exe` writes a deployment directory
named `Deploy_<account> <timestamp>` alongside the TRX, and one was created by this run. That
directory name embeds the account name in a path, which the evidence rules forbid anywhere in the
evidence tree, and git does not track directories, so such a directory is invisible to
`git status --porcelain` once created. Running outside the repository and copying in only the
sanitised TRX satisfies both the plan's artifact-existence acceptance and the no-host-paths rule.

Output Summary: **RED confirmed.** Exactly 2 tests ran, 2 failed, 0 passed, 0 skipped;
`EXIT_CODE: 1` matches `ExpectedExitCode: 1`, which `vstest.console.exe` sets whenever any executed
test fails. `IItemViewer_DeclaresAddFolderItemsAndNotSetFolderItems` fails naming
`IItemViewer.SetFolderItems` as still present and `AddFolderItems` as absent; `IItemViewer_FocusSubjectReturnsBool`
fails with `found System.Void`. Both are the intended pre-fix failures for #490 D1 and D3.
