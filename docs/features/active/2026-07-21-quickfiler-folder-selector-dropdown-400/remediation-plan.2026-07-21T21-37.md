# QuickFiler Folder Selector Drop-Down (#400) — Atomic Remediation Plan

- **Feature folder:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400`
- **Requirements source:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/remediation-inputs.2026-07-21T21-37.md`
- **Authoritative specification:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/spec.md`
- **Issue source:** `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/issue.md`
- **Work mode:** `full-bug`
- **Issue:** `#400`
- **Promotion type:** `bug`
- **User story:** `NONE` (optional and absent for `full-bug`)
- **Research artifacts:** `NONE` (the authoritative remediation inputs and fresh review artifacts contain the required source and external-contract evidence)
- **Execution route:** existing large-path orchestration, split into bounded typed C# engineer batches
- **Per-batch budget:** at most three production C# source files and three test C# source files; legacy project files are limited to the exact adjacent `Compile` entries named by a task
- **Status:** executor candidate; execution is prohibited until the external atomic-executor preflight returns the exact signal `PREFLIGHT: ALL CLEAR` and the MCP plan validator succeeds

## Planning handoff receipt

- `agent_name: atomic-planner`
- `skill_source: .codex/agents/atomic-planner.toml`
- `handoff_type: fresh role handoff from completed static-audit turn`
- `handoff_scope: planning only; no production, test, configuration, evidence, or acceptance-criteria edit was performed while authoring this plan`
- `continuity_rule: this exact remediation-plan path is the only plan target for this remediation cycle`

## Fixed execution rules

- Execute tasks in task-ID order. Do not replan, create a sibling remediation plan, or widen issue #400 beyond the breadcrumb selector and the minimum shared dispatcher/readiness/lifecycle seams required by the seven confirmed findings.
- Use only automated deterministic verification. Do not use live Outlook, a live WebView/UI, screenshots, user interaction, manual QA, manual bootstrap, wall-clock sleeps, temporary files, network access, or externally launched acceptance processes.
- Preserve all current worktree changes and evidence. Do not reset, discard, rewrite, or silently overwrite pre-existing work. New evidence receives a current UTC `yyyy-MM-ddTHH-mm` suffix and is stored only under this feature's `evidence/baseline`, `evidence/regression-testing`, or `evidence/qa-gates` directories.
- Every evidence artifact, including read-only inventories and analyses, must contain `Timestamp:`, a fully expanded exact `Command:` or exact deterministic inspection command, a numeric `EXIT_CODE:`, and an `Output Summary:`. Each baseline and final-QA command receives its own evidence artifact. Coverage artifacts must record numeric repository, changed/new-line, type, and measurable-member values where required.
- A task tagged `[expect-fail]` passes only when the named newly added regression is discovered and fails for the intended pre-fix assertion. A build failure, missing test, unrelated failure, crash, timeout, or zero-test run does not satisfy failure-first evidence.
- Each implementation batch is delegated to the typed C# engineer with the exact source/test scope stated in the task. If a batch needs a fourth production C# source file or fourth test C# source file, stop and request an in-place plan revision before editing that additional file.
- New focused C# files are intentional where existing production files are near the 500-line limit. Every new C# file must be at most 500 lines, reach at least 90% measurable line/member coverage, and have exactly one adjacent legacy-project `Compile` include. Every modified production/test C# file and the modified HTML resource must remain at most 500 lines. Do not add runtime behavior to generated `QuickFiler/Viewers/ItemViewer.Designer.cs`.
- Do not add packages, persisted settings, class-level coverage exclusions, test filters that omit first-party assemblies, or threshold/coverage-config changes. Do not weaken `coverage.config`, repository coverage `>=80%`, new/changed measurable selector type/member coverage `>=90%`, or changed-line no-regression requirements.
- Keep `FolderRow.Score`, `PercentageFormatter`, exact folder output paths, `GetSelectedFolder`, `SetFolderSelectedItem`, `FolderContains`, issue #398 atomic replacement, existing Left/Right behavior, popup ownership/placement/readiness, focus, themes, accessibility, and public `IItemViewer` signatures intact.
- Do not hold the router synchronization lock across `await`, provider work, UI/WebView dispatch, event publication, logging, or host callbacks. Pure/provider work may run off-thread; every production request into `CoreWebView2` and every UI-owned host callback must cross the owning UI dispatcher.
- If any batch-local formatter changes a file or any analyzer, nullable, or focused-test command fails, correct only within that batch's approved scope and restart that batch's verification sequence from its formatter task with a fresh evidence suffix.
- If any final-QA command changes a file or fails, invalidate the current final-pass evidence set and restart Phase 8 from P8-T1. `EXIT_CODE: SKIPPED` is never a passing result for a planned command.
- Leave a checklist item unchecked whenever required evidence is absent, incomplete, contradictory, stale for current HEAD/worktree state, or reports a nonzero exit code, except for a correctly satisfied `[expect-fail]` task.

## Finding-to-task and acceptance mapping

| Finding | Required failure-first tasks | Implementation tasks | Primary ACs |
|---|---|---|---|
| Unique logical row identities for duplicate suggestion/recent paths | P1-T1 through P1-T5 | P2-T1 through P2-T20 | AC-1, AC-5, AC-6, AC-7, AC-10, AC-13 |
| WebView2 UI-thread boundary and observable dispatch failure | P1-T6 through P1-T10 | P3-T1 through P3-T9 | AC-10, AC-12, AC-14, AC-15, AC-19 |
| Correlated readiness for the persistent collapsed surface | P1-T6 through P1-T10 | P4-T1 through P4-T15 | AC-1, AC-2, AC-12, AC-13, AC-14, AC-15 |
| One synchronization owner for selector/model mutation | P1-T11 through P1-T15 | P2-T8 through P2-T14 | AC-5, AC-6, AC-7, AC-11, AC-12 |
| Stale suggestion-upgrade cancellation/suppression | P1-T11 through P1-T15 | P5-T1 through P5-T8 | AC-11, AC-12, AC-14, AC-15 |
| Pending native open cancelable through `Close` | P1-T11 through P1-T15 | P5-T9 through P5-T16 | AC-8, AC-14, AC-15 |
| Expanded subfolder activation is an explicit selector commit | P1-T16 through P1-T20 | P6-T1 through P6-T16 | AC-7, AC-8, AC-9, AC-19 |

## Approved batch ledger

| Batch | Production C# source budget | Test C# source budget | Adjacent non-source/project scope |
|---|---:|---:|---|
| Failure-first A — duplicate identities | 0 | 3 | `UtilitiesCS.Test.csproj`, `QuickFiler.Test.csproj` exact new includes |
| Failure-first B — UI dispatch/collapsed readiness | 0 | 3 | `QuickFiler.Test.csproj` exact new includes |
| Failure-first C — selection/lifecycle/pending close | 0 | 3 | `UtilitiesCS.Test.csproj`, `QuickFiler.Test.csproj` exact new includes |
| Failure-first D — subfolder commit | 0 | 3 | `UtilitiesCS.Test.csproj`, `QuickFiler.Test.csproj` exact new includes |
| Identity core | 3 | 3 | `UtilitiesCS.csproj` exact `BreadcrumbRowIdentity.cs` include |
| Router-owned selection | 3 | 3 | no new project entry |
| Identity surface | 1 | 3 | `FolderBreadcrumb.html` is the only non-C# production resource |
| UI dispatcher | 3 | 3 | `QuickFiler.csproj` exact `BreadcrumbUiDispatcher.cs` include |
| Collapsed ready-surface core | 2 | 3 | `QuickFiler.csproj` exact `BreadcrumbCollapsedSurfaceController.cs` include |
| Collapsed integration | 3 | 3 | no new project entry |
| Coordinator upgrade lifetime | 3 | 3 | `QuickFiler.csproj` exact `BreadcrumbCoordinatorUpgradeLifetime.cs` include |
| Pending-open cancellation | 3 | 3 | `QuickFiler.csproj` exact `BreadcrumbDropDownOpenLifetime.cs` include |
| Subfolder selector core | 3 | 3 | no new project entry |
| Subfolder surface integration | 1 | 3 | `FolderBreadcrumb.html` is the only non-C# production resource |

### Phase 0 — Policy, requirements, AC reset, and remediation baseline

- [x] [P0-T1] Read, in order, `AGENTS.md` standing instructions, the cross-language code-change section in `AGENTS.md`, the cross-language unit-test section in `AGENTS.md`, and `.agents/skills/csharp/SKILL.md`; then write `phase0-instructions-read` evidence under `docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/evidence/baseline/` with a current UTC suffix, `Timestamp:`, `Policy Order:`, the exact files/sections read, and the other required evidence fields.
- [x] [P0-T2] Read completely `issue.md`, `spec.md`, `remediation-inputs.2026-07-21T21-37.md`, `code-review.2026-07-21T21-27.md`, `policy-audit.2026-07-21T21-27.md`, `feature-audit.2026-07-21T21-27.md`, `plan.2026-07-21T10-41.md`, `remediation-plan.2026-07-21T18-19.md`, `artifacts/pr_context.summary.txt`, and `artifacts/pr_context.appendix.txt`; write `remediation-requirements-map` baseline evidence that confirms `full-bug`, records all seven findings and all 19 ACs, maps them to this plan's tasks, records `user-story: NONE`, and fails closed if `spec.md` is absent or the branch/base context cannot be reconciled.
- [x] [P0-T3] Before any test or production implementation, edit only the checkbox markers in `spec.md` so AC-1, AC-5, AC-6, AC-7, AC-8, AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16, and AC-19 change from checked to unchecked while AC-2, AC-3, AC-4, AC-9, AC-17, and AC-18 remain checked; preserve every AC word and order exactly, and write `ac-remediation-reset` baseline evidence that records the 6-pass/13-open inventory, exact wording hashes before/after, and declares `evidence/qa-gates/spec-checkbox-reconciliation.2026-07-21T21-23.md` superseded and invalid as a current 19-PASS reconciliation source.
- [x] [P0-T4] Run `git status --short` and write a dedicated `git-status-baseline` evidence artifact containing the exact output; preserve and classify every pre-existing tracked/untracked change rather than cleaning or discarding it.
- [x] [P0-T5] Run `git rev-parse HEAD` and write a dedicated `head-baseline` evidence artifact containing the exact commit.
- [x] [P0-T6] Run `git merge-base HEAD origin/main` and write a dedicated `merge-base-baseline` evidence artifact; reconcile the result with the fresh review's recorded merge base and document any base advancement without importing base-only changes into feature scope.
- [x] [P0-T7] Run `git diff --check` and write a dedicated `diff-check-baseline` evidence artifact; record any pre-existing whitespace defect as baseline debt and do not fix unrelated files.
- [x] [P0-T8] Run `csharpier check .` as the non-mutating formatting baseline and write one dedicated `csharpier-remediation-baseline` evidence artifact. A formatter delta is baseline debt, not authorization to modify code during Phase 0.
- [x] [P0-T9] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write one dedicated `analyzers-remediation-baseline` evidence artifact with error/warning counts and the complete command result.
- [x] [P0-T10] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true` and write one dedicated `nullable-remediation-baseline` evidence artifact with compiler/nullable diagnostic counts.
- [x] [P0-T11] Compute a current UTC-minute suffix and run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/evidence/baseline/coverage-remediation-baseline.` followed by the concrete suffix and `.cobertura.xml`; write a separate `mstest-coverage-remediation-baseline` evidence artifact with the fully expanded command, all discovered assembly/test totals, zero-test detection, skip/failure counts, and numeric repository line coverage.
- [x] [P0-T12] Parse the Phase 0 Cobertura file and the current reviewed coverage evidence to write `coverage-accounting-remediation-baseline` evidence under `evidence/baseline/` with numeric repository coverage, merge-base changed-line coverage, current-remediation changed/new selector-line coverage, per-selector-type coverage, and every measurable changed/new member; enumerate bounded direct WebView2/WinForms adapter surfaces separately and treat any unavailable required number as remediation required.
- [x] [P0-T13] Write `scope-project-and-hash-baseline` evidence under `evidence/baseline/` containing line counts and whole-file SHA-256 hashes for every production/test file named in this plan, exact breadcrumb-related `Compile` inventories from all four legacy projects, hashes of `coverage.config` and package/configuration files in the current diff, and confirmation that the five planned new production helpers and nine planned new focused test files are absent before their authorized batches.

### Phase 1 — Deterministic failure-first regressions for all seven findings

- [x] [P1-T1] Delegate failure-first test batch A to the typed C# engineer with zero production files and exactly these three test sources: add `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbDuplicateIdentityTests.cs`, add `QuickFiler.Test/Viewers/BreadcrumbDuplicateIdentityIntegrationTests.cs`, and extend `QuickFiler.Test/Viewers/FolderBreadcrumbAssetContractTests.cs`; add exactly one adjacent legacy-project include for each new file. Cover duplicate suggestion/recent output paths across scored fallback, resolved upgrade, closed movement, open pending movement, exact activation, collapsed readback/probability, and exactly one active/`aria-selected=true` option.
- [x] [P1-T2] Run CSharpier format only on the three batch-A test sources and write a dedicated `duplicate-identity-tests-csharpier` artifact under `evidence/regression-testing/`; if formatting changes them, retain formatter output and rerun this task before continuing.
- [x] [P1-T3] Run the analyzer-enabled `msbuild` command from P0-T9 after batch A and write `duplicate-identity-tests-analyzers` evidence; a compile/analyzer failure is not expected-failure evidence.
- [x] [P1-T4] Run the nullable warnings-as-errors `msbuild` command from P0-T10 after batch A and write `duplicate-identity-tests-nullable` evidence; a compiler/nullable failure is not expected-failure evidence.
- [x] [P1-T5] [expect-fail] Resolve `vstest.console.exe` through the repository-standard Visual Studio discovery path and run `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll` plus `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbDuplicateIdentityTests|FullyQualifiedName~BreadcrumbDuplicateIdentityIntegrationTests|FullyQualifiedName~FolderBreadcrumbAssetContractTests"`; require one or more named duplicate-identity assertions to fail for first-match/duplicate-active behavior, and write `duplicate-identity-fail-before` evidence with every discovered named result.
- [x] [P1-T6] Delegate failure-first test batch B with zero production files and exactly these three QuickFiler test sources: add `QuickFiler.Test/Viewers/BreadcrumbUiThreadDispatchTests.cs`, add `QuickFiler.Test/Viewers/BreadcrumbCollapsedSurfaceReadinessTests.cs`, and extend `QuickFiler.Test/Controllers/QfcItemControllerBreadcrumbDropDownTests.cs`; add exactly one adjacent `QuickFiler.Test.csproj` include per new file. Cover worker-thread provider completion, UI dispatcher scheduling of every post/callback, observable dispatch failure, correlated collapsed navigation pending/unrelated/success/failure paths, reset/disposal, and exactly-once cached render/selector/theme replay.
- [x] [P1-T7] Run CSharpier format only on the three batch-B test sources and write `ui-readiness-tests-csharpier` evidence under `evidence/regression-testing/`; retain formatter output and rerun this task if it changes a file.
- [x] [P1-T8] Run the analyzer-enabled `msbuild` command from P0-T9 after batch B and write `ui-readiness-tests-analyzers` evidence; build failure is not expected-failure proof.
- [x] [P1-T9] Run the nullable warnings-as-errors `msbuild` command from P0-T10 after batch B and write `ui-readiness-tests-nullable` evidence.
- [x] [P1-T10] [expect-fail] Run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbUiThreadDispatchTests|FullyQualifiedName~BreadcrumbCollapsedSurfaceReadinessTests|FullyQualifiedName~QfcItemControllerBreadcrumbDropDownTests"`; require at least one named fail-before assertion for each independent defect—worker-thread delivery bypasses the UI dispatcher, and cached collapsed state/theme is replayed before the correlated document is ready—and write `ui-readiness-fail-before` evidence.
- [x] [P1-T11] Delegate failure-first test batch C with zero production files and exactly these three test sources: add `UtilitiesCS.Test/OutlookObjects/Folder/FolderBreadcrumbRouterSelectionConcurrencyTests.cs`, add `QuickFiler.Test/Viewers/BreadcrumbCoordinatorLifecycleTests.cs`, and add `QuickFiler.Test/Viewers/BreadcrumbPendingOpenCloseTests.cs`; add exactly one adjacent legacy-project include per new file. Use controlled `TaskCompletionSource` interleavings for closed move, open pending move, activation, and direct item selection after upgrade start; overlapping/current/stale suggestion upgrades; clear/reset/reuse/disposal with late success/failure; and `Close` while factory/readiness work is pending, repeated close, reopen, toggle, Escape, and automatic close.
- [x] [P1-T12] Run CSharpier format only on the three batch-C test sources and write `concurrency-lifecycle-tests-csharpier` evidence under `evidence/regression-testing/`; rerun after formatter changes.
- [x] [P1-T13] Run the analyzer-enabled `msbuild` command from P0-T9 after batch C and write `concurrency-lifecycle-tests-analyzers` evidence.
- [x] [P1-T14] Run the nullable warnings-as-errors `msbuild` command from P0-T10 after batch C and write `concurrency-lifecycle-tests-nullable` evidence.
- [x] [P1-T15] [expect-fail] Run both test assemblies through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~FolderBreadcrumbRouterSelectionConcurrencyTests|FullyQualifiedName~BreadcrumbCoordinatorLifecycleTests|FullyQualifiedName~BreadcrumbPendingOpenCloseTests"`; require named assertion failures for post-start selection loss, stale post/callback, and pending-open show/focus after close, and write `selection-lifecycle-fail-before` evidence.
- [x] [P1-T16] Delegate failure-first test batch D with zero production files and exactly these three test sources: add `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbSubfolderSelectorSessionTests.cs`, add `QuickFiler.Test/Viewers/BreadcrumbSubfolderActivationTests.cs`, and extend `UtilitiesCS.Test/OutlookObjects/Folder/BreadcrumbSelectorMessagesTests.cs`; add exactly one adjacent legacy-project include per new file. Cover open-selector subfolder activation followed independently by Enter, Escape, and native automatic close; durable full-path readback; one `SelectionChanged`; one explicit close/focus return; no rollback; and invalid row/subfolder identities/indexes according to the existing bridge contract.
- [x] [P1-T17] Run CSharpier format only on the three batch-D test sources and write `subfolder-commit-tests-csharpier` evidence under `evidence/regression-testing/`; rerun after formatter changes.
- [x] [P1-T18] Run the analyzer-enabled `msbuild` command from P0-T9 after batch D and write `subfolder-commit-tests-analyzers` evidence.
- [x] [P1-T19] Run the nullable warnings-as-errors `msbuild` command from P0-T10 after batch D and write `subfolder-commit-tests-nullable` evidence.
- [x] [P1-T20] [expect-fail] Run both test assemblies through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbSubfolderSelectorSessionTests|FullyQualifiedName~BreadcrumbSubfolderActivationTests|FullyQualifiedName~BreadcrumbSelectorMessagesTests"`; require named failures proving subfolder activation does not durably commit/close the open session, and write `subfolder-commit-fail-before` evidence.
- [x] [P1-T21] Write `failure-first-policy-and-scope` evidence under `evidence/regression-testing/` proving every new/modified test is MSTest with FluentAssertions and focused Moq/fakes, follows Arrange-Act-Assert, is independent and deterministic, contains no sleep/temp file/network/Outlook/live WebView/display/user interaction/external process, remains at most 500 lines, has exactly one project include when new, and leaves every P0-T13 production hash unchanged.

### Phase 2 — Unique row identities and router-owned selector synchronization

- [x] [P2-T1] Delegate identity-core batch A to the typed C# engineer with exactly three production sources—add `UtilitiesCS/OutlookObjects/Folder/BreadcrumbRowIdentity.cs`, modify `BreadcrumbStateModel.cs`, and modify `FolderBreadcrumbBridgeRouter.cs`—and no more than these three tests: `BreadcrumbDuplicateIdentityTests.cs`, `BreadcrumbStateModelSelectorTests.cs`, and `FolderBreadcrumbBridgeRouterEdgeTests.cs`; add exactly one adjacent `UtilitiesCS.csproj` include for the new helper.
- [x] [P2-T2] Implement a deterministic source-qualified logical-row identity that is unique per selectable row occurrence even when output paths are equal, distinguishes suggestion from recent/search/plain origins, remains stable from scored fallback through resolved-chain replacement, and is never used as the folder output value; keep exact path/probability behavior in `GetSelectedFolder`, `SetFolderSelectedItem`, `FolderContains`, and projection.
- [x] [P2-T3] Make model construction reject or deterministically disambiguate any accidental duplicate logical identity while preserving both valid duplicate-path rows, their independent score/kind/order, and their selectable status; do not deduplicate a recent/search row merely because its path matches a suggestion.
- [x] [P2-T4] Run CSharpier format on the three batch-A production sources and three approved tests and write `identity-core-csharpier` evidence under `evidence/qa-gates/`; restart this batch verification if formatting changes a file.
- [x] [P2-T5] Run the analyzer-enabled `msbuild` command from P0-T9 and write `identity-core-analyzers` evidence; fix only inside batch-A scope and restart at P2-T4 on failure/change.
- [x] [P2-T6] Run the nullable warnings-as-errors `msbuild` command from P0-T10 and write `identity-core-nullable` evidence; restart at P2-T4 on failure/change.
- [x] [P2-T7] Run the resolved VSTest path against `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll` with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbDuplicateIdentityTests|FullyQualifiedName~BreadcrumbStateModelSelectorTests|FullyQualifiedName~FolderBreadcrumbBridgeRouterEdgeTests"`; require all named identity/fallback/resolution tests to pass and write `identity-core-pass-after` evidence.
- [x] [P2-T8] Delegate router-owned-selection batch B with exactly three production sources—`FolderBreadcrumbBridgeRouter.cs`, `BreadcrumbSelectionSession.cs`, and `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`—and no more than these three tests: `FolderBreadcrumbRouterSelectionConcurrencyTests.cs`, `BreadcrumbSelectionSessionTests.cs`, and `BreadcrumbSelectorCoordinatorTests.cs`.
- [x] [P2-T9] Move open/move/activate/commit/cancel/direct-item transitions behind router methods so the router is the sole owner of model/session mutation under its synchronization and generation contract; external callers receive immutable/read-only state or render snapshots and cannot mutate the public model directly.
- [x] [P2-T10] Make atomic upgrade replacement reconcile the latest selection/session mutation at swap time rather than a pre-await captured identity, so closed movement, pending movement, activation, and `SelectItem` occurring after an upgrade starts win over the replacement; stale generations remain unable to replace state, and no external work executes while the router lock is held.
- [x] [P2-T11] Run CSharpier format on the three batch-B production sources and three approved tests and write `router-selection-csharpier` evidence; restart batch verification if it changes a file.
- [x] [P2-T12] Run the analyzer-enabled `msbuild` command and write `router-selection-analyzers` evidence; restart at P2-T11 on failure/change.
- [x] [P2-T13] Run the nullable warnings-as-errors `msbuild` command and write `router-selection-nullable` evidence; restart at P2-T11 on failure/change.
- [x] [P2-T14] Run both test assemblies through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~FolderBreadcrumbRouterSelectionConcurrencyTests|FullyQualifiedName~BreadcrumbSelectionSessionTests|FullyQualifiedName~BreadcrumbSelectorCoordinatorTests"`; require all controlled in-flight and selector-session tests to pass and write `router-selection-pass-after` evidence.
- [x] [P2-T15] Delegate identity-surface batch C with exactly one production C# source, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, plus `QuickFiler/Resources/FolderBreadcrumb.html`, and no more than these three tests: `BreadcrumbDuplicateIdentityIntegrationTests.cs`, `FolderBreadcrumbAssetContractTests.cs`, and `BreadcrumbSelectorCoordinatorTests.cs`.
- [x] [P2-T16] Route row movement and activation by the unique logical identity while keeping the exact output folder path separate; ensure the collapsed state renders the actually committed logical row and probability, and ensure the expanded DOM computes one active option and one `aria-selected=true` option even when folder paths repeat.
- [x] [P2-T17] Run CSharpier format on the batch-C C# source/tests and write `identity-surface-csharpier` evidence; verify the HTML file remains at most 500 lines and contains no formatter-independent whitespace defect.
- [x] [P2-T18] Run the analyzer-enabled `msbuild` command and write `identity-surface-analyzers` evidence; restart at P2-T17 on failure/change.
- [x] [P2-T19] Run the nullable warnings-as-errors `msbuild` command and write `identity-surface-nullable` evidence; restart at P2-T17 on failure/change.
- [x] [P2-T20] Run both test assemblies through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbDuplicateIdentityTests|FullyQualifiedName~BreadcrumbDuplicateIdentityIntegrationTests|FullyQualifiedName~FolderBreadcrumbAssetContractTests|FullyQualifiedName~FolderBreadcrumbRouterSelectionConcurrencyTests"`; require every duplicate-path fallback/upgrade/navigation/activation/collapsed/accessibility assertion to pass and write `duplicate-identity-complete-pass` evidence.

### Phase 3 — UI-thread dispatch for WebView2 and host callbacks

- [x] [P3-T1] Delegate the UI-dispatcher batch with exactly three production sources—add `QuickFiler/Viewers/BreadcrumbUiDispatcher.cs`, modify `WebView2Messenger.cs`, and modify `BreadcrumbBridgeCoordinator.cs`—and no more than these three tests: `BreadcrumbUiThreadDispatchTests.cs`, `BreadcrumbSelectorCoordinatorTests.cs`, and `BreadcrumbBridgeCoordinatorProbabilityTests.cs`; add exactly one adjacent `QuickFiler.csproj` include for the new dispatcher.
- [x] [P3-T2] Implement an injectable dispatcher that captures the owning UI synchronization boundary at production construction, executes inline only when already on that boundary, schedules otherwise without a blocking wait, and routes scheduling/action failure to an observable error sink that tests can assert and production can log through the existing boundary.
- [x] [P3-T3] Require `WebView2Messenger` to perform every `CoreWebView2` request, including `PostWebMessageAsJson`, only inside the dispatcher; require coordinator provider continuations to marshal all outbound render/selector/theme posts and UI-owned callbacks before delivery while allowing pure/provider work off-thread.
- [x] [P3-T4] Bound the inbound `async void` message-event path with explicit exception observation so invalid/current dispatch failure is reported without escaping as an unhandled exception; stale work may be suppressed only when proven stale by the later lifecycle generation contract.
- [x] [P3-T5] Run CSharpier format on the three production sources and three approved tests and write `ui-dispatch-csharpier` evidence; restart this phase verification if it changes a file.
- [x] [P3-T6] Run the analyzer-enabled `msbuild` command and write `ui-dispatch-analyzers` evidence; restart at P3-T5 on failure/change.
- [x] [P3-T7] Run the nullable warnings-as-errors `msbuild` command and write `ui-dispatch-nullable` evidence; restart at P3-T5 on failure/change.
- [x] [P3-T8] Run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbUiThreadDispatchTests|FullyQualifiedName~BreadcrumbSelectorCoordinatorTests|FullyQualifiedName~BreadcrumbBridgeCoordinatorProbabilityTests"`; require worker completions to schedule every production post/callback through the fake UI boundary and dispatch failures to be observed exactly once, then write `ui-dispatch-pass-after` evidence.
- [x] [P3-T9] Write `webview-thread-boundary-audit` evidence under `evidence/qa-gates/` from a scoped source search and call-chain inspection proving every issue-#400 `CoreWebView2` request is inside the dispatcher/direct-adapter boundary, no `ConfigureAwait(false)` continuation directly posts or raises a UI-owned callback, the new file has one project include, and all batch files are at most 500 lines.

### Phase 4 — Correlated readiness for the persistent collapsed surface

- [x] [P4-T1] Delegate collapsed-ready-surface core batch A with exactly two production sources—add `QuickFiler/Viewers/BreadcrumbCollapsedSurfaceController.cs` and modify `BreadcrumbWebViewSurfaceFactory.cs`—and no more than these three tests: `BreadcrumbCollapsedSurfaceReadinessTests.cs`, `BreadcrumbDropDownReadinessTests.cs`, and `BreadcrumbUiThreadDispatchTests.cs`; add exactly one adjacent `QuickFiler.csproj` include for the new controller.
- [x] [P4-T2] Generalize the existing popup-ready navigation seam so popup and collapsed surfaces share correlation by the exact `NavigationStarting.NavigationId`/`NavigationCompleted.NavigationId`, success/failure translation, pre-navigation handler registration, disposal cancellation, and handler detachment on every synchronous/async success, failure, cancellation, reset, and disposal path.
- [x] [P4-T3] Make `BreadcrumbCollapsedSurfaceController` own one collapsed navigation generation and cancellation lifetime, reject unrelated or late completion, publish a ready messenger only after the exact target document succeeds, and dispose/detach partial or stale work without a hub attachment, replay, host callback, or unobserved fault.
- [x] [P4-T4] Run CSharpier format on the two production sources and three approved tests and write `collapsed-ready-core-csharpier` evidence; restart on formatter change.
- [x] [P4-T5] Run the analyzer-enabled `msbuild` command and write `collapsed-ready-core-analyzers` evidence; restart at P4-T4 on failure/change.
- [x] [P4-T6] Run the nullable warnings-as-errors `msbuild` command and write `collapsed-ready-core-nullable` evidence; restart at P4-T4 on failure/change.
- [x] [P4-T7] Run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbCollapsedSurfaceReadinessTests|FullyQualifiedName~BreadcrumbDropDownReadinessTests|FullyQualifiedName~BreadcrumbUiThreadDispatchTests"`; require pending/unrelated/success/failure/reset/disposal correlation tests to pass and write `collapsed-ready-core-pass-after` evidence.
- [x] [P4-T8] Delegate collapsed integration batch B with exactly three production sources—`QuickFiler/Viewers/ItemViewer.Breadcrumb.cs`, `QuickFiler/Controllers/QfcItemController.ViewerSetup.cs`, and `QuickFiler/Viewers/BreadcrumbMessengerHub.cs`—and no more than these three tests: `BreadcrumbCollapsedSurfaceReadinessTests.cs`, `QfcItemControllerBreadcrumbDropDownTests.cs`, and `BreadcrumbMessengerHubTests.cs`.
- [x] [P4-T9] Change controller/viewer attachment flow to await the collapsed controller's exact ready result before attaching to the hub; keep suggestions and theme cached while pending, replay the current render/selector/theme exactly once after success, and ensure failure/reset/disposal leaves no messenger, inbound subscription, partial attachment, or stale callback.
- [x] [P4-T10] Preserve popup readiness and hub mode separation while making attach/replay idempotent for one collapsed surface and one popup surface; an unrelated navigation event, a repeated controller setup call, or pooled viewer reuse must not add a second inbound subscription or duplicate cached replay.
- [x] [P4-T11] Run CSharpier format on the three batch-B production sources and three approved tests and write `collapsed-ready-integration-csharpier` evidence; restart on formatter change.
- [ ] [P4-T12] Run the analyzer-enabled `msbuild` command and write `collapsed-ready-integration-analyzers` evidence; restart at P4-T11 on failure/change.
- [ ] [P4-T13] Run the nullable warnings-as-errors `msbuild` command and write `collapsed-ready-integration-nullable` evidence; restart at P4-T11 on failure/change.
- [ ] [P4-T14] Run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbCollapsedSurfaceReadinessTests|FullyQualifiedName~QfcItemControllerBreadcrumbDropDownTests|FullyQualifiedName~BreadcrumbMessengerHubTests|FullyQualifiedName~BreadcrumbDropDownReadinessTests"`; require exact-once cached state/theme replay, correlated readiness, failure cleanup, and popup-preservation tests to pass and write `collapsed-ready-integration-pass-after` evidence.
- [ ] [P4-T15] Write `collapsed-ready-scope-and-adapter-audit` evidence under `evidence/qa-gates/` proving both ready surfaces use one correlation contract, direct WebView2 code remains method-bounded and separately enumerated, the new controller has one project include, no class-level exclusion was added, and every modified/new file is at most 500 lines.

### Phase 5 — Coordinator lifecycle invalidation and cancelable pending native open

- [ ] [P5-T1] Delegate coordinator-upgrade-lifetime batch A with exactly three production sources—add `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs`, modify `BreadcrumbBridgeCoordinator.cs`, and modify `ItemViewer.Breadcrumb.cs`—and no more than these three tests: `BreadcrumbCoordinatorLifecycleTests.cs`, `BreadcrumbCollapsedSurfaceReadinessTests.cs`, and `BreadcrumbMessengerHubTests.cs`; add exactly one adjacent `QuickFiler.csproj` include for the new lifetime helper.
- [ ] [P5-T2] Give every suggestion population a coordinator-owned monotonic generation and cancellation source; a later population, clear, reset, pooled reuse, or disposal must invalidate all earlier work, and each completion must prove it is current before posting render/selector state, raising callbacks, or updating current error/state.
- [ ] [P5-T3] Make coordinator disposal idempotently cancel outstanding work and unsubscribe its exact inbound messenger handler; ItemViewer reset/reuse/disposal must invoke the correct reset/dispose lifetime so late success/failure cannot reach a disposed hub or reused selector, while one current population produces exactly one update per ready surface.
- [ ] [P5-T4] Run CSharpier format on the three batch-A production sources and three approved tests and write `coordinator-lifetime-csharpier` evidence; restart on formatter change.
- [ ] [P5-T5] Run the analyzer-enabled `msbuild` command and write `coordinator-lifetime-analyzers` evidence; restart at P5-T4 on failure/change.
- [ ] [P5-T6] Run the nullable warnings-as-errors `msbuild` command and write `coordinator-lifetime-nullable` evidence; restart at P5-T4 on failure/change.
- [ ] [P5-T7] Run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbCoordinatorLifecycleTests|FullyQualifiedName~BreadcrumbCollapsedSurfaceReadinessTests|FullyQualifiedName~BreadcrumbMessengerHubTests"`; require overlapping-upgrade, clear/reset/reuse/disposal late success/failure, one-current-update, and no-post-after-disposal tests to pass and write `coordinator-lifetime-pass-after` evidence.
- [ ] [P5-T8] Write `coordinator-lifetime-scope-audit` evidence under `evidence/qa-gates/` proving `CancellationToken.None` is absent from the suggestion-upgrade route, all current/stale checks precede dispatch/callback/error mutation, the helper has one project include, the hub is never called by stale work, and all batch files are at most 500 lines.
- [ ] [P5-T9] Delegate pending-open-cancellation batch B with exactly three production sources—add `QuickFiler/Viewers/BreadcrumbDropDownOpenLifetime.cs`, modify `BreadcrumbDropDownHost.cs`, and modify `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs`—and no more than these three tests: `BreadcrumbPendingOpenCloseTests.cs`, `BreadcrumbDropDownLifecycleConcurrencyTests.cs`, and `BreadcrumbDropDownLifecycleTests.cs`; add exactly one adjacent `QuickFiler.csproj` include for the new lifetime helper.
- [ ] [P5-T10] Move the pending-open generation/cancellation state into the focused helper so `Close(Uncommitted)` invalidates an in-flight factory or readiness wait even while `_isOpen` is false, all callers sharing that `OpenAsync` complete deterministically with `false`, and canceled work cannot later attach, replay, show, focus, publish, or invoke a close callback; update ItemViewer's close/toggle and selector-open-state-changed paths to call `Close(Uncommitted)` while host initialization is pending even though `IsOpen` is false.
- [ ] [P5-T11] Use the host close result in ItemViewer to preserve exactly-once selection rollback, anchor-focus return, and close callback for toggle/Escape/outside/automatic close without duplicating session cancellation when pending work is invalidated; keep repeated close idempotent, allow one fresh open after canceled close, retain a fully created reusable surface only when no canceled/stale action can attach/show/focus/callback, and dispose partial/stale resources exactly once.
- [ ] [P5-T12] Keep `BreadcrumbDropDownHost.cs` at most 500 lines by the planned extraction rather than by suppressing or deleting tested behavior; keep the helper at most 500 lines and preserve existing Reset/Dispose/reentrant-callback safeguards and public interface/constructor contracts.
- [ ] [P5-T13] Run CSharpier format on the two batch-B production sources and three approved tests and write `pending-open-close-csharpier` evidence; restart on formatter change.
- [ ] [P5-T14] Run the analyzer-enabled `msbuild` command and write `pending-open-close-analyzers` evidence; restart at P5-T13 on failure/change.
- [ ] [P5-T15] Run the nullable warnings-as-errors `msbuild` command and write `pending-open-close-nullable` evidence; restart at P5-T13 on failure/change.
- [ ] [P5-T16] Run `QuickFiler.Test\bin\Debug\QuickFiler.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbPendingOpenCloseTests|FullyQualifiedName~BreadcrumbDropDownLifecycleConcurrencyTests|FullyQualifiedName~BreadcrumbDropDownLifecycleTests"`; require pending-factory/readiness close, repeated close, reopen, toggle/Escape/automatic-close, cleanup, rollback, and focus tests to pass and write `pending-open-close-pass-after` evidence.

### Phase 6 — Explicit durable commit for expanded subfolder activation

- [ ] [P6-T1] Delegate subfolder-selector-core batch A with exactly three production sources—`UtilitiesCS/OutlookObjects/Folder/BreadcrumbSelectorMessages.cs`, `FolderBreadcrumbBridgeRouter.cs`, and `BreadcrumbSelectionSession.cs`—and no more than these three tests: `BreadcrumbSubfolderSelectorSessionTests.cs`, `BreadcrumbSelectorMessagesTests.cs`, and `FolderBreadcrumbBridgeRouterEdgeTests.cs`.
- [ ] [P6-T2] Add one explicit subfolder selector-activation message containing unique row identity and subfolder index, with deterministic serialization/parsing and existing-contract handling for missing identity, unknown row, negative/out-of-range index, and non-subfolder rows; preserve the legacy full output path and existing Left/Right message contracts.
- [ ] [P6-T3] Add one router-owned atomic transition that validates the row/subfolder, selects the exact subfolder, commits and closes the current selector session with explicit-commit semantics, and returns a durable render/readback result; a later Enter, Escape, cancel, or native close must be a no-op for that completed session and cannot restore the opening parent/row.
- [ ] [P6-T4] Run CSharpier format on the three batch-A production sources and three approved tests and write `subfolder-core-csharpier` evidence; restart on formatter change.
- [ ] [P6-T5] Run the analyzer-enabled `msbuild` command and write `subfolder-core-analyzers` evidence; restart at P6-T4 on failure/change.
- [ ] [P6-T6] Run the nullable warnings-as-errors `msbuild` command and write `subfolder-core-nullable` evidence; restart at P6-T4 on failure/change.
- [ ] [P6-T7] Run `UtilitiesCS.Test\bin\Debug\UtilitiesCS.Test.dll` through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbSubfolderSelectorSessionTests|FullyQualifiedName~BreadcrumbSelectorMessagesTests|FullyQualifiedName~FolderBreadcrumbBridgeRouterEdgeTests"`; require durable subfolder commit and invalid-input tests to pass and write `subfolder-core-pass-after` evidence.
- [ ] [P6-T8] Delegate subfolder-surface batch B with exactly one production C# source, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, plus `QuickFiler/Resources/FolderBreadcrumb.html`, and no more than these three tests: `BreadcrumbSubfolderActivationTests.cs`, `FolderBreadcrumbAssetContractTests.cs`, and `BreadcrumbSelectorCoordinatorTests.cs`.
- [ ] [P6-T9] Make expanded accessible/mouse subfolder activation emit the explicit selector message; have the coordinator invoke the router transition, publish `SelectionChanged` exactly once for the full subfolder path, broadcast durable collapsed state, request exactly one explicit-commit close/focus return, and ignore later Enter/Escape/native-close completion for that ended session.
- [ ] [P6-T10] Preserve legacy subfolder output mapping and Left/Right expand/collapse/fall-through; invalid subfolder messages must leave selection/session/open state deterministic and must not publish, close, focus, or throw beyond the existing logged parser boundary.
- [ ] [P6-T11] Run CSharpier format on the batch-B C# source/tests and write `subfolder-surface-csharpier` evidence; verify the HTML resource remains at most 500 lines and has no whitespace error.
- [ ] [P6-T12] Run the analyzer-enabled `msbuild` command and write `subfolder-surface-analyzers` evidence; restart at P6-T11 on failure/change.
- [ ] [P6-T13] Run the nullable warnings-as-errors `msbuild` command and write `subfolder-surface-nullable` evidence; restart at P6-T11 on failure/change.
- [ ] [P6-T14] Run both test assemblies through the resolved VSTest path with `/InIsolation /TestCaseFilter:"FullyQualifiedName~BreadcrumbSubfolderSelectorSessionTests|FullyQualifiedName~BreadcrumbSubfolderActivationTests|FullyQualifiedName~BreadcrumbSelectorMessagesTests|FullyQualifiedName~FolderBreadcrumbAssetContractTests|FullyQualifiedName~BreadcrumbSelectorCoordinatorTests"`; require activation followed separately by Enter, Escape, and automatic close to preserve one durable subfolder selection/event/close/focus and write `subfolder-composition-pass-after` evidence.
- [ ] [P6-T15] Run the existing Left/Right, selector-key, path readback, probability, issue #398 in-flight, and invalid-message test classes with an exact VSTest filter and write `preserved-breadcrumb-contracts` evidence; do not accept a source-token-only assertion as proof of runtime state transition.
- [ ] [P6-T16] Write `subfolder-scope-and-delivery-audit` evidence under `evidence/qa-gates/` proving one new message shape, one router-owned commit transition, one inbound coordinator route, one event/close/focus outcome, unchanged full-path output semantics, no duplicate legacy route, and at-most-500-line compliance for every batch file.

### Phase 7 — Complete remediation regression and structural gates

- [ ] [P7-T1] Run both test assemblies with one exact filter covering all nine new focused test classes—`BreadcrumbDuplicateIdentityTests`, `BreadcrumbDuplicateIdentityIntegrationTests`, `BreadcrumbUiThreadDispatchTests`, `BreadcrumbCollapsedSurfaceReadinessTests`, `FolderBreadcrumbRouterSelectionConcurrencyTests`, `BreadcrumbCoordinatorLifecycleTests`, `BreadcrumbPendingOpenCloseTests`, `BreadcrumbSubfolderSelectorSessionTests`, and `BreadcrumbSubfolderActivationTests`—and write `seven-findings-complete-pass-after` evidence with every discovered test name/count and zero failures/skips.
- [ ] [P7-T2] Run all 19 pre-remediation issue-related test classes listed in `policy-audit.2026-07-21T21-27.md` plus the nine new classes through exact VSTest filters and write `issue-400-focused-regression` evidence; require zero failures, zero skips, and no removed/renamed pre-existing test without an explicit one-to-one replacement recorded in the artifact.
- [ ] [P7-T3] Run all compiled `FolderBreadcrumbAssetContractTests` and write `html-accessibility-theme-contracts` evidence proving collapsed/expanded modes, hidden overflow, one toggle, one active/selected option for duplicate paths, selector/subfolder messages, Left/Right compatibility, focus hooks, and light/dark theme hooks.
- [ ] [P7-T4] Compare the current source/project inventory with P0-T13 and write `scope-project-file-size-integrity` evidence: all new production/test C# files have exactly one adjacent `Compile` include, all modified/new C#/HTML files are at most 500 lines, `ItemViewer.Designer.cs` is untouched, public `IItemViewer` signatures are unchanged, and no package/configuration/coverage-filter/threshold/persisted-setting change exists.
- [ ] [P7-T5] Write `failure-first-to-pass-after-trace` evidence under `evidence/regression-testing/` mapping each intended named failure from P1-T5, P1-T10, P1-T15, and P1-T20 to its current passing test and implementation task; missing, unrelated, or still-failing mappings block final QA.
- [ ] [P7-T6] Run `git diff --check` and write `remediation-diff-check` evidence under `evidence/qa-gates/`; correct only issue-#400 whitespace defects and rerun the applicable batch verification before marking this task complete.

### Phase 8 — Ordered final C# QA and numeric coverage

- [ ] [P8-T1] Start a fresh final-pass run identity and run `csharpier format .`; write one dedicated `final-remediation-csharpier` artifact under `evidence/qa-gates/`. Require exit zero and no subsequent formatter delta; if files change, retain only valid formatter output within approved scope, invalidate this final-pass set, and restart at P8-T1 with a new suffix.
- [ ] [P8-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; write one dedicated `final-remediation-analyzers` artifact. On any error or file change, fix the root cause without suppression, invalidate the final-pass set, and restart at P8-T1.
- [ ] [P8-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; write one dedicated `final-remediation-nullable` artifact. On any compiler/nullable error or file change, invalidate the final-pass set and restart at P8-T1.
- [ ] [P8-T4] Compute a current UTC-minute suffix and run `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vscode/Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput docs/features/active/2026-07-21-quickfiler-folder-selector-dropdown-400/evidence/qa-gates/coverage-final-remediation.` followed by the concrete suffix and `.cobertura.xml`; write one dedicated `final-remediation-mstest-coverage` artifact with all assembly/test counts, zero failures/skips, and numeric repository line coverage. On any failure, skip, zero-test result, file change, or incomplete coverage file, invalidate the final-pass set and restart at P8-T1.
- [ ] [P8-T5] Write `final-remediation-pass-integrity` evidence proving P8-T1 through P8-T4 are one uninterrupted clean format → analyzer → nullable → coverage sequence for the same source state and run identity, every command artifact has complete required fields, and no superseded prior-final-pass artifact is cited as current proof.
- [ ] [P8-T6] Recompute coverage against the P0 remediation baseline and the live P0 merge base using complete source-line/member accounting; write `coverage-remediation-delta` evidence with numeric baseline/final repository coverage, changed/new-line coverage, per-type coverage, and every measurable new/changed selector member. Require repository coverage `>=80%`, no changed-line regression, and every measurable new/changed selector type/member `>=90%`; unavailable numbers or threshold failures are remediation required, never a pass or skip.
- [ ] [P8-T7] Write `nonnumeric-adapter-remediation-accounting` evidence enumerating every direct WebView2/WinForms or unavoidable navigation-readiness method that cannot receive numeric unit coverage, the exact bounded source lines and exclusion attributes, and deterministic injected tests for every branch; require no class-level exclusion, no host-neutral selector/lifecycle logic in excluded code, and no weakened filter/threshold/configuration.
- [ ] [P8-T8] Run `git diff --check` again and write `final-remediation-diff-integrity` evidence proving zero whitespace errors, exact planned source/test/project/resource scope, at-most-500-line compliance, one include per new C# file, unchanged package/configuration/coverage policy, no manual/temp artifacts, and no unexplained change outside issue #400.

### Phase 9 — Acceptance reconciliation, validators, and fresh independent review

- [ ] [P9-T1] Reevaluate all 19 `spec.md` acceptance criteria against the current P7/P8 evidence and write an AC-by-AC table in a new `acceptance-verification-remediation` artifact under `evidence/qa-gates/`; every PASS row must cite direct current evidence and commands, and missing/contradictory evidence is FAIL.
- [ ] [P9-T2] Edit only checkbox markers in `spec.md`: recheck each of the 13 criteria reset by P0-T3 only when its P9-T1 row is PASS, leave every failed/unverified criterion unchecked, preserve all wording/order exactly, and write `spec-checkbox-reconciliation-remediation` evidence with the 19-item result and wording hashes.
- [ ] [P9-T3] Run `mcp__drm-copilot__validate_orchestration_artifacts` for this exact executed plan and every required full-bug feature artifact using the live workspace root; write `artifact-validation-remediation` evidence with exact inputs/results, and leave this task unchecked on any warning, schema error, missing artifact, checklist/evidence contradiction, or nonzero result.
- [ ] [P9-T4] Delegate a fresh independent full feature review through the repository feature-review route using live `origin/main`, current HEAD/worktree, `spec.md`, this plan, `remediation-inputs.2026-07-21T21-37.md`, all current evidence, and the complete merge-base feature diff; require new UTC-timestamped `policy-audit`, `code-review`, and `feature-audit` artifacts and prohibit reuse of the 2026-07-21T21-27 conclusions as current approval.
- [ ] [P9-T5] Require the fresh independent policy/code/feature review to report no Blocker/Major remediation finding, a feature verdict of `PASS`, all 19 ACs supported by current evidence, and no coverage/policy/scope defect; if any audit returns remediation required, preserve its findings, uncheck affected criteria, leave this and later tasks unchecked, and begin a new same-feature remediation cycle rather than claiming completion.
- [ ] [P9-T6] After the independent PASS, rerun `mcp__drm-copilot__validate_orchestration_artifacts` for the plan and required full-bug artifacts and append a new successful invocation/result to `artifact-validation-remediation` evidence; do not accept a human-readable audit summary as a substitute for validator success.
- [ ] [P9-T7] Run `git status --short`, reconcile every remaining path with the executed plan/evidence/review outputs, and record `remediation-end-state` evidence under `evidence/qa-gates/`; mark remediation execution complete only when all plan tasks are evidence-consistent, all 19 criteria are checked and independently supported, validators pass, final QA remains current for the exact source state, and no unexplained worktree change remains.

## Mandatory external planner preflight contract

- The planner must hand this exact path to the existing atomic-executor with the exact directive line `DIRECTIVE: PREFLIGHT VALIDATION ONLY`.
- The preflight must verify the P0-T3 acceptance reset and prior-P6-T2 invalidation as an execution prerequisite, all seven failure-first paths, every 3/3 batch boundary, canonical evidence locations, ordered final QA/coverage, AC reconciliation, validators, and fresh independent review.
- The only valid final preflight signals are `PREFLIGHT: ALL CLEAR` and `PREFLIGHT: REVISIONS REQUIRED`.
- A revision-required response must contain a precise delta for this same plan path. The planner must apply the delta in place and repeat the same atomic-executor preflight until the exact final sentinel is `PREFLIGHT: ALL CLEAR`.
- If the atomic-executor handoff cannot complete, planning is blocked and this plan must not be self-approved.
- After `PREFLIGHT: ALL CLEAR`, the planner must run `mcp__drm-copilot__validate_orchestration_artifacts` with `artifact_type: "plan"`, this exact artifact path, and the current workspace root. Execution remains prohibited if the validator does not succeed.
