# taskvisualization-secondary-testability — Atomic Implementation Plan (Issue #298)

- **Issue:** #298
- **Parent epic:** winforms-testability-refactor (#295)
- **Owner:** drmoisan
- **Last Updated:** 2026-07-09T16-07
- **Status:** Ready for Preflight
- **Version:** 1.0
- **Work Mode:** full-feature
- **Depends on (HARD):** #297 (taskvisualization-core-testability-refactor). Execution occurs in a worktree branched from `epic/winforms-testability-refactor-integration` AFTER #297 merges. All paths below are repo-root-relative on purpose; do not resolve them against this design workspace.

## Sources of Truth

- Requirements/AC: `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/issue.md` (`## Acceptance Criteria`).
- Authoritative spec: `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/spec.md`.
- Design research: `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/research/research-findings.2026-07-09T15-31Z.md`.
- Epic Shared Design Pattern: `docs/features/epics/winforms-testability-refactor/epic.md`.
- Upstream sibling seams (reuse, do not duplicate): `docs/features/active/2026-07-09-taskvisualization-core-testability-refactor-297/spec.md`.

## Evidence Location Determination

- Canonical evidence scheme is `<FEATURE>/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md` (non-overridable).
- `<FEATURE>` = `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298`.
- `EVIDENCE_LOCATION_OVERRIDE_REJECTED: artifacts/csharp/ (as the coverage-evidence narrative location) replaced with docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/baseline/ (baseline coverage) and docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/ (post-change coverage).` The caller-supplied `artifacts/csharp/` is not a canonical `evidence/<kind>/` location; all coverage EVIDENCE-of-record narratives (with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:` and numeric coverage) are written to the canonical folders below.
- The raw machine-readable `artifacts/csharp/coverage.xml` is produced ONLY as a feature-review tooling input (feature-review consumes coverage XML at that path); it is not the evidence-of-record. Its numeric values are mirrored into the canonical evidence narratives. `artifacts/csharp/` is not among the forbidden `artifacts/` evidence sub-paths and is not used as an `evidence/<kind>/` substitute.
- Evidence folders used by this plan:
  - Baseline: `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/baseline/`
  - QA gates: `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/`
  - Regression/verification: `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/regression-testing/`
  - Other (assumptions, exemption inventory, file-size): `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/`

## Scope Lock (files this plan may create or modify)

New production files (each requires an explicit `<Compile Include>` in the legacy packages.config `TaskVisualization/TaskVisualization.csproj`; the project uses explicit includes, no glob):

- CREATE `TaskVisualization/IEditFilterViewer.cs` (+ `<Compile Include="IEditFilterViewer.cs" />`)
- CREATE `TaskVisualization/IManageFiltersViewer.cs` (+ `<Compile Include="IManageFiltersViewer.cs" />`)
- CREATE `TaskVisualization/ManageFiltersController.cs` (+ `<Compile Include="ManageFiltersController.cs" />`)
- CREATE `TaskVisualization/FlagCalculations.cs` (+ `<Compile Include="FlagCalculations.cs" />`)

Modified production files:

- MODIFY `TaskVisualization/EditFilterController.cs`
- MODIFY `TaskVisualization/EditFilterViewer.cs`
- MODIFY `TaskVisualization/ManageFilters.cs`
- MODIFY `TaskVisualization/FlagTasks.cs`
- MODIFY `TaskVisualization/AutoCreateProject.cs`
- MODIFY `TaskVisualization/AutoAssignContext.cs`
- MODIFY `TaskVisualization/AutoAssignPeople.cs`
- MODIFY `TaskVisualization/FlagChangeTrainingQueue.cs` (only if a determinism/`InternalsVisibleTo` accessor gap is found; no behavior change)
- MODIFY `TaskVisualization/FlagChangeItem.cs` (only if required to expose the POCO for tests; no behavior change)
- MODIFY `TaskVisualization/TaskVisualization.csproj` (each new `<Compile Include>` above)

New test files (each requires an explicit `<Compile Include>` in `TaskVisualization.Test/TaskVisualization.Test.csproj`, also legacy packages.config explicit-include):

- CREATE `TaskVisualization.Test/FlagCalculationsTests.cs`
- CREATE `TaskVisualization.Test/FlagChangeItemTests.cs`
- CREATE `TaskVisualization.Test/FlagChangeGroupTests.cs`
- CREATE `TaskVisualization.Test/FlagChangeTrainingQueueTests.cs`
- CREATE `TaskVisualization.Test/AutoCreateProjectTests.cs`
- CREATE `TaskVisualization.Test/AutoAssignContextTests.cs`
- CREATE `TaskVisualization.Test/AutoAssignPeopleTests.cs`
- CREATE `TaskVisualization.Test/EditFilterControllerTests.cs`
- CREATE `TaskVisualization.Test/ManageFiltersControllerTests.cs`
- MODIFY `TaskVisualization.Test/TaskVisualization.Test.csproj` (each new `<Compile Include>` above)

Out of scope (MUST NOT edit): `TaskVisualization/TaskController.cs`, `TaskVisualization/TaskViewer.cs`, `TaskVisualization/TaskViewer.Designer.cs` (all #297-owned), `TaskVisualization/FlagChangeGroup.cs` structural change (only `TryEnqueue` is tested; its four `[ExcludeFromCodeCoverage]` COM members from #197 stay), `TaskVisualization/EditFilterViewer.designer.cs` (Designer-generated; MUST NOT be hand-split), `TaskVisualization/ManageFilters.Designer.cs`, `QuickFiler/**`, `UtilitiesCS/Interfaces/IToDo/**` (`IFlagChange*`).

## Invariants (verified by tasks; never violated)

1. `FlagTasks(IApplicationGlobals, IList, bool, IntPtr, string)` constructor shape and the `FlagTasks` concrete type name are unchanged (QuickFiler factory seam `Func<IApplicationGlobals, List<MailItem>, bool, IntPtr, FlagTasks>` in `QuickFiler/Controllers/QfcItemController.Initialization.cs`). Coverage is achieved by extracting pure statics into `TaskVisualization/FlagCalculations.cs`, never by seaming the constructor.
2. `EfcFormController`'s three-call `ManageFilters` surface (`new ManageFilters()`, `.LoadFilters(_globals)`, `.Show()`) at `QuickFiler/Controllers/EfcFormController.cs` is preserved; `EfcFormController.cs` is not edited. `LoadFilters` delegates internally to `ManageFiltersController`.
3. Observable UI behavior of every touched form/helper is preserved (including `BtnDelete_Click`'s current no-side-effect behavior in `TaskVisualization/ManageFilters.cs`).
4. No new production dependencies. `UtilitiesCS/Interfaces/IToDo/*` `IFlagChange*` interfaces unchanged.
5. Every touched production and test file stays `<= 500` lines. `TaskVisualization/EditFilterViewer.designer.cs` (503 lines, Designer-generated) is exempt by the form partial's class-level `[ExcludeFromCodeCoverage]` and MUST NOT be hand-split.

## Coverage Exemption Inventory (for maintainer ratification)

Every `[ExcludeFromCodeCoverage]` this plan RETAINS, ADDS, or REMOVES is enumerated here with per-site justification. Testable seams are never exempt. Phase 9 writes this inventory to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/` for ratification.

REMOVED (previously #197 class-level; now measured):
- `TaskVisualization/EditFilterController.cs` class-level `[ExcludeFromCodeCoverage]` — removed; controller retargets to `IEditFilterViewer` + viewer-factory + `_tagSelector` seams so its logic is measured.
- `TaskVisualization/FlagTasks.cs` class-level `[ExcludeFromCodeCoverage]` — removed; extracted statics move to `TaskVisualization/FlagCalculations.cs`; remaining exemptions are method-level only (below).
- `TaskVisualization/AutoCreateProject.cs` class-level `[ExcludeFromCodeCoverage]` — removed; host-neutral members measured; live-Interop members carry method-level exemptions (below).
- `TaskVisualization/AutoAssignContext.cs` / `TaskVisualization/AutoAssignPeople.cs` class-level `[ExcludeFromCodeCoverage]` — removed; host-neutral branches measured; classifier-engine invocation lines carry method-level exemptions (below).

RETAINED (irreducible UI/COM wiring; justified):
- `TaskVisualization/EditFilterViewer.cs` + `TaskVisualization/EditFilterViewer.designer.cs` — class-level on the form partial (covers the designer partial). Justification: `Form`-derived + Designer-generated control construction; interface pass-throughs read/write live `Control.Text`. Ratified WinForms/COM policy.
- `TaskVisualization/ManageFilters.cs` + `TaskVisualization/ManageFilters.Designer.cs` — class-level on the form partial (covers the designer partial). Justification: `Form`-derived; `FastObjectListView`/`OLVColumn` pass-throughs bind live BrightIdeasSoftware controls.
- `TaskVisualization/FlagChangeGroup.cs` — retain #197 method-level exemptions on the four Outlook-bound members (ctor, `ProcessGroupAsync`, `TryProcessFlagItemAsync`, `ProcessFlagItemAsync`). `TryEnqueue` stays measured (never exempt).
- `TaskVisualization/FlagTasks.cs` — method-level on genuinely Outlook-bound members only: ctor, `Run`, `GetSelection`, `InitializeToDoList`, `PopulateUdf`, `GetUserInputFlagsToAdjust`. Justification: `ActiveExplorer()`/`Selection`/`MessageBox.Show`/`new TaskViewer()`/`new TaskController()` live-host dependencies with no cheap seam.
- `TaskVisualization/AutoCreateProject.cs` — method-level on `CreateProjectTaskItem`, `GetTaskItems`, `ToHelper`, `AutoFindAsync`, and the MAPI `CreateCategory` line inside `AddColorCategory`. Justification: `Ol.App.Session.GetDefaultFolder`, `MailItemHelper.FromMailItemAsync`, MAPI `CreateCategory` live-Interop. Host-neutral members (`FilterList`, `GetNextProjectID`, `TryAutoExtractProgram`, `StripPrefix`, the `NotImplementedException` throwers) are NOT exempt.
- `TaskVisualization/AutoAssignContext.cs` / `TaskVisualization/AutoAssignPeople.cs` — method-level on `AutoFindAsync`/`ToHelper` classifier-engine invocation lines and the `MailItemHelper`-constructing branches. `FilterList`, the `NotImplementedException` throwers, and the `null`/unknown-type early-return `[]` branches are NOT exempt.

NEVER exempt (must be measured): `TaskVisualization/FlagCalculations.cs`, `TaskVisualization/ManageFiltersController.cs`, retargeted `TaskVisualization/EditFilterController.cs` logic, `TaskVisualization/FlagChangeGroup.cs` `TryEnqueue`, `TaskVisualization/FlagChangeTrainingQueue.cs` logic, `TaskVisualization/FlagChangeItem.cs`.

Interface-only files (`TaskVisualization/IEditFilterViewer.cs`, `TaskVisualization/IManageFiltersViewer.cs`) carry no `[ExcludeFromCodeCoverage]`; they are legitimately 0% executable and excluded from measurement per the interface-only clarification in `.claude/rules/general-unit-test.md`.

---

### Phase 0 — Policy Reading, Assumption Re-Verification, and Baseline Capture

- [ ] [P0-T1] Read the policy set in required order and record a policy-read evidence artifact at `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/phase0-instructions-read.md` listing, with `Timestamp:` and `Policy Order:`, each file read: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`. Acceptance: artifact exists and enumerates all five files in order.
- [ ] [P0-T2] Re-verify assumption 1 (ITaskViewer shape) against the post-#297 head: confirm `TaskVisualization/TaskController.cs` `formInstance` parameter type and whether `ITaskViewer` exists; record findings and the chosen fallback (reuse `ITaskViewer` viewer construction vs self-contained seam) in `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/assumption-verification.md`. Acceptance: assumption 1 marked PASS or FAIL-with-fallback in the artifact; if FAIL and no fallback is viable, halt (see P0-T9).
- [ ] [P0-T3] Re-verify assumption 2 (dialog seam): grep the post-#297 tree for a reusable dialog seam (`TaskVisualization/ITagPromptService.cs` or equivalent) that #298 reuses for the Tag/flag-selection dialogs; if present, record its type/shape; if absent, record the fallback of narrow per-call delegates (`_tagSelector`, `_flagSelector`). Append the result to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/assumption-verification.md`. Acceptance: assumption 2 recorded with the concrete seam decision.
- [ ] [P0-T4] Re-verify assumption 3 (Interop adapter): determine whether a reusable folder/items/`MailItemHelper` adapter exists post-#297; record reuse-or-local-delegate decision for `AutoCreateProject`/`AutoAssign*`/`FlagTasks` boundaries in `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/assumption-verification.md`. Acceptance: assumption 3 recorded with the concrete adapter/delegate decision.
- [ ] [P0-T5] Re-verify assumption 4 (FlagChange stability): confirm `TaskVisualization/FlagChangeGroup.cs` ctor `(IApplicationGlobals, MailItem)`, `TaskVisualization/FlagChangeTrainingQueue.cs` `Init()`, and `UtilitiesCS/Interfaces/IToDo/*` `IFlagChange*` are unchanged by #297; confirm `[assembly: InternalsVisibleTo("TaskVisualization.Test")]` remains present in `TaskVisualization/FlagTasks.cs`. Append to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/assumption-verification.md`. Acceptance: assumption 4 marked PASS or FAIL-with-required-changes.
- [ ] [P0-T6] Re-verify assumption 5 (csproj cleanliness) plus the #293 watch item: confirm `TaskVisualization/TaskVisualization.csproj` and `TaskVisualization.Test/TaskVisualization.Test.csproj` build clean at the post-#297 head before any `<Compile Include>` additions, and confirm the `TagViewer`/`TagController` call sites in `TaskVisualization/EditFilterController.cs` and `TaskVisualization/FlagTasks.cs` still compile against #293's `Tags` project (compile as long as `TagViewer : ITagViewer`). Append to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/assumption-verification.md`. Acceptance: assumption 5 + #293 watch item marked PASS or FAIL-with-required-changes.
- [ ] [P0-T7] Re-verify assumption 6 (exemption state): confirm #197's `[ExcludeFromCodeCoverage]` annotations are still present on the in-scope files (`TaskVisualization/EditFilterController.cs`, `TaskVisualization/FlagTasks.cs`, `TaskVisualization/AutoCreateProject.cs`, `TaskVisualization/AutoAssignContext.cs`, `TaskVisualization/AutoAssignPeople.cs`, and `TaskVisualization/FlagChangeGroup.cs` method-level) so the plan's removal/narrowing steps apply as written; append to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/assumption-verification.md`. Acceptance: assumption 6 recorded; deltas from the plan's exemption inventory noted.
- [ ] [P0-T8] Scan every in-scope production and test file for banned APIs (`Thread.Sleep`, `Task.Delay`, `DateTime.Now`/`UtcNow` in code under test, real timer waits) per `.claude/rules/csharp.md`; record findings and any required in-plan remediation at `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/banned-api-scan.md`. Acceptance: artifact lists each touched file with `NONE` or an enumerated remediation folded into the relevant phase task.
- [ ] [P0-T9] Assumption gate: if any of P0-T2..P0-T7 is FAIL with no viable documented fallback, HALT and write a required-changes report to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/required-changes-halt.md` (naming the failed assumption, the blocking condition, and the upstream change needed) instead of proceeding to Phase 1. Acceptance: either all assumptions resolved PASS/fallback (proceed) OR the halt report exists and execution stops.
- [ ] [P0-T10] Capture format baseline: run `csharpier .` in check mode and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/baseline/csharpier-baseline.md` with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:`. Acceptance: artifact present with all four fields.
- [ ] [P0-T11] Capture analyzer baseline: run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/baseline/msbuild-analyzers-baseline.md` with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:` (warning/error counts). Acceptance: artifact present with all four fields.
- [ ] [P0-T12] Capture nullable/type baseline: run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/baseline/msbuild-nullable-baseline.md` with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:`. Acceptance: artifact present with all four fields.
- [ ] [P0-T13] Capture test + coverage baseline: run `vstest.console.exe TaskVisualization.Test/bin/Debug/TaskVisualization.Test.dll /EnableCodeCoverage`, convert the result to `artifacts/csharp/coverage.xml` (feature-review tooling input), and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/baseline/coverage-baseline.md` with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:` including the numeric post-#297 `TaskVisualization` project line-coverage percent (the denominator #298 must lift to `>= 80%`). Acceptance: artifact records a numeric baseline percent (not a placeholder).

### Phase 1 — Viewer Interfaces and csproj Registration

- [ ] [P1-T1] Create `TaskVisualization/IEditFilterViewer.cs`: `public interface IEditFilterViewer : UtilitiesCS.Interfaces.IWinForm.IForm` with the five text properties (`ContextSelectionText`, `PeopleSelectionText`, `ProjectSelectionText`, `TopicSelectionText`, `FilterNameText`), `string Text { get; set; }`, `void Show()`, `void Hide()`, `void Dispose()`, the seven click events (`ContextSelectionClick`, `PeopleSelectionClick`, `ProjectSelectionClick`, `TopicSelectionClick`, `FoldersSelectedClick`, `OkClick`, `CancelClick`), and `void ResetTips()`; reference only `EventHandler`/`string` (no WinForms control types). Add `<Compile Include="IEditFilterViewer.cs" />` to `TaskVisualization/TaskVisualization.csproj`. Acceptance: file exists, is wired in the csproj, and the toolchain (P-QA order below) is green.
- [ ] [P1-T2] Create `TaskVisualization/IManageFiltersViewer.cs`: `public interface IManageFiltersViewer : UtilitiesCS.Interfaces.IWinForm.IForm` with `FilterEntry SelectedFilter { get; }`, `void SetFilters(IEnumerable<FilterEntry> filters)`, `void RebuildList()`, `void Show()`, and the three click events (`AddFilterClick`, `EditFilterClick`, `DeleteClick`); reference only `FilterEntry`/`IEnumerable<FilterEntry>`/`EventHandler` (no WinForms control types). Add `<Compile Include="IManageFiltersViewer.cs" />` to `TaskVisualization/TaskVisualization.csproj`. Acceptance: file exists, is wired in the csproj, and the toolchain is green.

### Phase 2 — EditFilter Viewer and Controller Retarget

- [ ] [P2-T1] Modify `TaskVisualization/EditFilterViewer.cs` to declare `: IEditFilterViewer` and implement the interface as thin pass-throughs (text properties bind `ContextSelection.Text`/`PeopleSelection.Text`/`ProjectSelection.Text`/`TopicSelection.Text`/`FilterName.Text`; the seven `...Click` events add/remove on the concrete `Button`/`Label` `.Click`; `ResetTips()` performs the existing "toggle all tips Off" that `Initialize()` did via `GetTips()`), keeping the form partial's class-level `[ExcludeFromCodeCoverage]`. Acceptance: `EditFilterViewer` compiles as `IEditFilterViewer`, file `<= 500` lines, no behavior change, toolchain green.
- [ ] [P2-T2] Modify `TaskVisualization/EditFilterController.cs`: change field `_viewer` from `EditFilterViewer` to `IEditFilterViewer`; replace the seven `_viewer.X.Click += Handler` wirings in `RegisterEventHandlers()` with `_viewer.XClick += Handler`; replace control `.Text` reads/writes with the new string properties; add a viewer-factory seam `Func<IEditFilterViewer> _viewerFactory` defaulting to `() => new EditFilterViewer()`; add a Tag-dialog seam `Func<SortedDictionary<string,bool>, (bool cancelled, string selection)> _tagSelector` for `SelectItems` (reusing #297's dialog seam if P0-T3 found one, else a local delegate) and drop the `System.Windows.Forms.Label` parameter in favor of an `Action<string>`/text-property setter; add an explicit `internal` constructor overload (or internal settable seam fields) that accepts `Func<IEditFilterViewer> viewerFactory` and the `_tagSelector` delegate so `TaskVisualization.Test` (reaching the type via `InternalsVisibleTo`) can inject both seams, keeping the existing public constructors' behavior unchanged (public paths default `_viewerFactory` to `() => new EditFilterViewer()` and `_tagSelector` to the real Tag-dialog delegate); retarget `InitializeFactory()` to return `IEditFilterViewer` so `return _viewer;` type-matches the retargeted `_viewer` field, and make `DeleteFilterDialog()` operate on `IEditFilterViewer` (both `Text` — declared on `IEditFilterViewer` — and `ShowDialog()` — inherited from `IForm` — are available via the interface, so the `.Text` set and `.ShowDialog()` call compile); verify no caller of `InitializeFactory()` requires the concrete `EditFilterViewer` return type, and if one exists, specify and apply the exact caller-side adjustment within this task; call `_viewer.ResetTips()` in place of the `GetTips()` toggle; remove the class-level `[ExcludeFromCodeCoverage]` and apply method-level exemptions only where genuinely UI-bound. Acceptance: controller depends only on `IEditFilterViewer`, exposes an internal seam constructor/fields accepting `viewerFactory` + `_tagSelector` with public-constructor behavior unchanged, constructs its viewer via the factory seam, `InitializeFactory()` returns `IEditFilterViewer` and `DeleteFilterDialog()` compiles against `IEditFilterViewer`, file `<= 500` lines, toolchain green.

### Phase 3 — ManageFilters Logic Extraction

- [ ] [P3-T1] Create `TaskVisualization/ManageFiltersController.cs`: host-neutral `public class ManageFiltersController` depending on `IManageFiltersViewer` + `IApplicationGlobals` + a seam `Func<IApplicationGlobals, FilterEntry, EditFilterController> _editFilterFactory`; implement `LoadFilters()` (`viewer.SetFilters(globals.AF.Filters)`), `EditSelected()` (read `viewer.SelectedFilter`, construct via seam), `AddFilter()` (construct via seam with `EditFilterCallback`, then `viewer.RebuildList()`), `EditFilterCallback(EditFilterController, FilterEntry)` (`globals.AF.Filters.Add`/`Serialize` then `viewer.RebuildList()`), and `DeleteSelected()` (read `viewer.SelectedFilter`; no deletion side effect — behavior preserved). Add `<Compile Include="ManageFiltersController.cs" />` to `TaskVisualization/TaskVisualization.csproj`. Acceptance: file exists, wired in csproj, references no WinForms control types, `<= 500` lines, toolchain green.
- [ ] [P3-T2] Modify `TaskVisualization/ManageFilters.cs` to declare `: IManageFiltersViewer`, implement the interface members as pass-throughs to `FiltersOlv` (`SelectedFilter`, `SetFilters`, `RebuildList`, `Show`, the three click events), hold a `ManageFiltersController`, and make `LoadFilters(IApplicationGlobals)` delegate to `ManageFiltersController` while preserving the public `LoadFilters`/`Show` surface (invariant 2). Keep the form partial's class-level `[ExcludeFromCodeCoverage]`. Acceptance: `EfcFormController`'s three-call surface still compiles unchanged, file `<= 500` lines, toolchain green.

### Phase 4 — FlagTasks Statics Extraction and Dialog Seam

- [ ] [P4-T1] Create `TaskVisualization/FlagCalculations.cs`: extract the pure statics `GetFlagsToSet(int)`, `ConvertFlagStringsToEnum(List<string>)`, and `GetSymbolsDictionary()` verbatim from `TaskVisualization/FlagTasks.cs` into a host-neutral static class, with `GetFlagsToSet`'s `count > 1` branch delegating to an injected `Func<SortedDictionary<string,bool>, List<string>> flagSelector` parameter (no direct dialog). Add `<Compile Include="FlagCalculations.cs" />` to `TaskVisualization/TaskVisualization.csproj`. Acceptance: file exists, wired in csproj, no WinForms/Interop `using`, `<= 500` lines, toolchain green.
- [ ] [P4-T2] Modify `TaskVisualization/FlagTasks.cs`: replace the in-file static bodies with calls to `TaskVisualization/FlagCalculations.cs`; add a flag-selection dialog seam `Func<SortedDictionary<string,bool>, List<string>> _flagSelector` (default = current `GetUserInputFlagsToAdjust` body) passed into `GetFlagsToSet`; remove the class-level `[ExcludeFromCodeCoverage]` and apply method-level exemptions only to the Outlook-bound members (ctor, `Run`, `GetSelection`, `InitializeToDoList`, `PopulateUdf`, `GetUserInputFlagsToAdjust`); PRESERVE the `FlagTasks(IApplicationGlobals, IList, bool, IntPtr, string)` constructor and `Run()` exactly (invariant 1) and keep `[assembly: InternalsVisibleTo("TaskVisualization.Test")]`. Acceptance: constructor/type name unchanged, statics delegate to `FlagCalculations`, file `<= 500` lines, toolchain green.

### Phase 5 — AutoCreateProject and AutoAssign Interop Seams

- [ ] [P5-T1] Modify `TaskVisualization/AutoCreateProject.cs`: add optional seam constructor parameters with safe production defaults — `Func<IEnumerable<string>, string> _chooseProgram` (program chooser), `Func<IPrefix, string, Category> _createCategory` (MAPI category creation), `Func<Items> _getTaskItems` (task-item folder) — keeping the single-arg `AutoCreateProject(IApplicationGlobals)` form valid for callers `TaskMaster/Ribbon/TryFunctionalityInConstruction.cs` and `TaskVisualization/FlagTasks.cs`; remove the class-level `[ExcludeFromCodeCoverage]` and apply method-level exemptions only to `CreateProjectTaskItem`/`GetTaskItems`/`ToHelper`/`AutoFindAsync` and the MAPI `CreateCategory` line in `AddColorCategory`. Acceptance: single-arg ctor still compiles for both callers, host-neutral members measured, file `<= 500` lines, toolchain green.
- [ ] [P5-T2] Modify `TaskVisualization/AutoAssignContext.cs`: add a `Func<object, Task<MailItemHelper>> _toHelper` seam (default = current `ToHelper` body); remove the class-level `[ExcludeFromCodeCoverage]` and apply method-level exemptions only to the classifier-engine invocation in `AutoFindAsync`/`ToHelper`; keep `FilterList`, the `NotImplementedException` throwers measured. Acceptance: `FlagTasks.cs` caller compiles unchanged, host-neutral branches measured, file `<= 500` lines, toolchain green.
- [ ] [P5-T3] Modify `TaskVisualization/AutoAssignPeople.cs`: add a SYNCHRONOUS `Func<object, MailItemHelper> _toHelper` seam whose safe production default reproduces the current inline `new MailItemHelper(...)` construction, and route the two inline helper-construction branches inside the synchronous `AutoFind(object)` through `_toHelper` (do NOT introduce a `ToHelper` method — none exists on this type). Leave `AutoFindAsync` as the existing `Task.Run(() => AutoFind(...))` wrapper; it is influenced by the seam only transitively through `AutoFind`, so make no `_toHelper` claim about `AutoFindAsync`. Remove the class-level `[ExcludeFromCodeCoverage]` and apply method-level exemptions only to the classifier-engine invocation lines in `AutoFind`; keep `FilterList` and the `null`/unknown-type early-return `[]` branches measured. Acceptance: `internal` ctor compiles unchanged for `FlagTasks.cs`, `AutoFind` constructs its helper via the synchronous `_toHelper` seam, host-neutral branches measured, file `<= 500` lines, toolchain green.

### Phase 6 — Pure-Unit Tests: Flag Helpers

- [ ] [P6-T1] Create `TaskVisualization.Test/FlagCalculationsTests.cs` (MSTest + FluentAssertions): `GetSymbolsDictionary` excludes `All`/`None` and returns sorted keys; `ConvertFlagStringsToEnum` maps empty -> `All`, valid strings -> bit-or, invalid ignored; `GetFlagsToSet(1)` -> `All`; `GetFlagsToSet(>1)` with an injected `flagSelector` stub -> parsed enum. Add `<Compile Include="FlagCalculationsTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: tests wired, deterministic (no form/popup/sleep/temp file), toolchain green.
- [ ] [P6-T2] Create `TaskVisualization.Test/FlagChangeItemTests.cs`: POCO round-trip; `UntrainFlags`/`TrainFlags` default non-null empty. Add `<Compile Include="FlagChangeItemTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: tests wired, `TaskVisualization/FlagChangeItem.cs` reaches near-100% line coverage, toolchain green.
- [ ] [P6-T3] Create `TaskVisualization.Test/FlagChangeGroupTests.cs`: test `TryEnqueue` via a test subclass overriding the `virtual` `Item`/`Globals` (no live `MailItem`) — no difference -> returns false and nothing enqueued; additions/removals -> returns true and one `FlagChangeItem` with correct `TrainFlags`/`UntrainFlags` from the pure `CompareTo`. Add `<Compile Include="FlagChangeGroupTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: `TaskVisualization/FlagChangeGroup.cs` `TryEnqueue` measured and covered, no COM member constructed, toolchain green.
- [ ] [P6-T4] Create `TaskVisualization.Test/FlagChangeTrainingQueueTests.cs`: `Init()` returns self and sets `ConsumerTimer`; `Enqueue` with `Options=Immediate` + mocked `IFlagChangeGroup` returning `Task.CompletedTask` -> `ProcessGroupAsync` invoked, queue drained, guard reset; `Enqueue` with `Options=Timed` -> item present in `Queue` (via `InternalsVisibleTo`) and timer requested. The 500ms `TimedAsyncTask` is never awaited; no `Thread.Sleep`/`Task.Delay`/wall-clock wait. Add `<Compile Include="FlagChangeTrainingQueueTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: `TaskVisualization/FlagChangeTrainingQueue.cs` logic covered deterministically, toolchain green.

### Phase 7 — Pure-Unit Tests: AutoCreate and AutoAssign

- [ ] [P7-T1] Create `TaskVisualization.Test/AutoCreateProjectTests.cs` (reuse `TaskVisualization.Test/MoqOlToDo.cs`): `GetNextProjectID` (mocked `ProjInfo`/`IDList` -> seed selection + next-id); `TryAutoExtractProgram` (substring match true/false, longest-first ordering); `StripPrefix` (present/absent/empty); `FilterList`; `AddChoicesToDict`/`AutoFind` -> `NotImplementedException`; where reachable, `AddColorCategory` host-neutral branches via the `_chooseProgram`/`_createCategory`/`_getTaskItems` seams with stubs. Add `<Compile Include="AutoCreateProjectTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: `AutoCreateProject` host-neutral members `>= 90%`, no live Interop, toolchain green.
- [ ] [P7-T2] Create `TaskVisualization.Test/AutoAssignContextTests.cs`: `FilterList`; `AddChoicesToDict`/`AddColorCategory`/`AutoFind` -> `NotImplementedException`; `AutoFindAsync` with a stubbed `_toHelper` returning null -> `[]`. Add `<Compile Include="AutoAssignContextTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: host-neutral members measured and covered, no live Interop, toolchain green.
- [ ] [P7-T3] Create `TaskVisualization.Test/AutoAssignPeopleTests.cs`: `FilterList`; `AutoFind(null)` -> `[]`; `AutoFind(unknownType)` -> `[]`; plus a case that injects a stubbed synchronous `_toHelper` returning a controlled `MailItemHelper` and asserts `AutoFind` returns a result determined by that stub (proving the seam actually changes the output through `AutoFind`). Add `<Compile Include="AutoAssignPeopleTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: host-neutral branches measured and covered, the `_toHelper` seam is exercised through `AutoFind`, no live Interop, toolchain green.

### Phase 8 — Mocked-Interface Controller Tests

- [ ] [P8-T1] Create `TaskVisualization.Test/EditFilterControllerTests.cs` using `Mock<IEditFilterViewer>` via the injected viewer factory + `MoqOlToDo` globals: `RegisterEventHandlers` wiring (raise each `...Click`, assert handler runs); `BtnOk_Click` (sets `_filterEntry.Name` from `FilterNameText`, invokes callback when set, calls `Hide()`/`Dispose()`); `BtnCancel_Click` (null callback -> `Close()` + `RevertToCopy`; callback set -> no close); `Initialize`/`InitializeFactory` (selection texts set from `_filterEntry.Flags.*`, `ResetTips()` called); `SelectItems` with injected `_tagSelector` (`(false,"X;Y")` -> target text written; `(true,_)` -> no write). No live form/popup. Add `<Compile Include="EditFilterControllerTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: `EditFilterController` logic measured `>= 90%` on new/changed lines, deterministic, toolchain green.
- [ ] [P8-T2] Create `TaskVisualization.Test/ManageFiltersControllerTests.cs` using `Mock<IManageFiltersViewer>` + mocked globals: `LoadFilters` -> `SetFilters(globals.AF.Filters)`; `AddFilter` -> `_editFilterFactory` seam invoked with the callback + `RebuildList`; `EditFilterCallback` -> `AF.Filters.Add`/`Serialize` + `RebuildList`; `EditSelected`/`DeleteSelected` -> read `SelectedFilter` (behavior preserved). No live form/popup. Add `<Compile Include="ManageFiltersControllerTests.cs" />` to `TaskVisualization.Test/TaskVisualization.Test.csproj`. Acceptance: `ManageFiltersController` `>= 90%`, deterministic, toolchain green.

### Phase 9 — File-Size, Exemption Inventory, and Coverage Verification

- [ ] [P9-T1] Verify every touched production and test file is `<= 500` lines (all files under Scope Lock), and confirm `TaskVisualization/EditFilterViewer.designer.cs` (503, Designer-generated) was NOT hand-split; write results to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/file-size-check.md` with per-file line counts. Acceptance: artifact shows all in-scope files `<= 500` (designer carve-out explicitly noted), no violation.
- [ ] [P9-T2] Finalize the coverage-exemption inventory (REMOVED/RETAINED/NEVER-exempt per this plan's inventory section, reconciled against P0-T7 findings) and write it for maintainer ratification to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/other/exemption-inventory.md`, with per-site justification and a reducibility note confirming no coverable logic is hidden behind any exemption. Acceptance: artifact enumerates every `[ExcludeFromCodeCoverage]` site with justification; no testable seam is exempt.
- [ ] [P9-T3] Run `vstest.console.exe TaskVisualization.Test/bin/Debug/TaskVisualization.Test.dll /EnableCodeCoverage`, regenerate `artifacts/csharp/coverage.xml`, and write a coverage-delta artifact to `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/coverage-delta.md` reporting baseline percent (from P0-T13), post-change `TaskVisualization` project line-coverage percent, and new-class percentages (`FlagCalculations`, `ManageFiltersController`, retargeted `EditFilterController` new lines). Acceptance: project line coverage `>= 80%` overall AND each new class `>= 90%`; if either threshold is unmet, mark REMEDIATION-REQUIRED (not PASS).

### Phase 10 — Final QA Loop and Acceptance Reconciliation

- [ ] [P10-T1] Format: run `csharpier .` and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/final-csharpier.md` with `Timestamp:`/`Command:`/`EXIT_CODE:`/`Output Summary:`. If files change, restart the loop from this task. Acceptance: EXIT_CODE 0 and no files reformatted in the final pass.
- [ ] [P10-T2] Lint/analyzers: run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/final-msbuild-analyzers.md` with the four schema fields. If it fails or changes files, restart from P10-T1. Acceptance: EXIT_CODE 0, zero analyzer errors.
- [ ] [P10-T3] Type/nullable: run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/final-msbuild-nullable.md` with the four schema fields. If it fails or changes files, restart from P10-T1. Acceptance: EXIT_CODE 0, no nullable warnings-as-errors.
- [ ] [P10-T4] Test + coverage: run `vstest.console.exe TaskVisualization.Test/bin/Debug/TaskVisualization.Test.dll /EnableCodeCoverage`, refresh `artifacts/csharp/coverage.xml`, and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/final-vstest-coverage.md` with the four schema fields and the numeric `TaskVisualization` project line-coverage percent in `Output Summary:`. If it fails or changes files, restart from P10-T1. Acceptance: all tests pass and project line coverage `>= 80%`.
- [ ] [P10-T5] Reconcile acceptance criteria: verify each `issue.md` `## Acceptance Criteria` item against evidence and write `docs/features/active/2026-07-09-taskvisualization-secondary-testability-298/evidence/qa-gates/acceptance-reconciliation.md` mapping every AC to its proving artifact (interfaces derive from `IForm` and forms implement them; `EditFilterController` depends on `IEditFilterViewer`; `ManageFilters` logic testable via `IManageFiltersViewer`; helper seams at Interop boundaries; no file `> 500` lines; no test constructs a live form/popup; project `>= 80%`; full toolchain green with no regression). Acceptance: all seven AC marked satisfied with a cited artifact, or the report marks the unmet AC REMEDIATION-REQUIRED.

## Preflight Self-Check (planner)

Structural self-check performed against the atomic-plan-contract and the local `.claude/hooks/validate-planner-output.ps1` gate:

- Canonical phase headings `### Phase N — <Title>` present for Phases 0–10 (em-dash per `atomic-plan-contract` skill and repo convention; the local hook's hyphen-only regex is a documented divergence that did not fatally block prior em-dash planner runs, and every committed repo plan uses the em-dash).
- Phase 0 includes a policy-read task (P0-T1) and baseline tasks (P0-T10..P0-T13).
- Task IDs are sequential `[P#-T#]` within each phase; every task contains at least one explicit `/`-bearing path token.
- Final phase (Phase 10) contains format/lint/type/test/coverage QA work.
- Coverage contract satisfied: numeric baseline (P0-T13), post-change + new-code coverage (P9-T3), and final coverage (P10-T4); thresholds `>= 80%` project / `>= 90%` new classes with a REMEDIATION-REQUIRED fail path.
- Evidence paths resolve to canonical `<FEATURE>/evidence/<kind>/`; the non-canonical `artifacts/csharp/` coverage-evidence location was rejected and substituted (see Evidence Location Determination); `artifacts/csharp/coverage.xml` is retained only as a feature-review tooling input, not evidence-of-record.
- The MCP `validate_orchestration_artifacts` plan validator is NOT available in this checkout; it was NOT RUN. The operative gate is the structural self-check above plus an atomic-executor `DIRECTIVE: PREFLIGHT VALIDATION ONLY` pass performed by the caller/orchestrator; nested worker delegation is not claimed from within planner execution.
