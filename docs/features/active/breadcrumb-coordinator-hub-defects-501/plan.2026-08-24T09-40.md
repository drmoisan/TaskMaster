# breadcrumb-coordinator-hub-defects (Plan)

- **Issue:** #501 (also closes #462, #500, #502)
- **Parent (optional):** epic `quickfiler-bug-family`
- **Owner:** drmoisan
- **Last Updated:** 2026-08-24T09-40
- **Status:** Ready for preflight
- **Version:** 1.0
- **Work Mode:** `full-bug` (marker `- Work Mode: full-bug` in `issue.md`). `spec.md` is the authoritative
  acceptance-criteria source; `user-story.md` is deliberately absent and must stay absent.
- **Requirements source:** `docs/features/active/breadcrumb-coordinator-hub-defects-501/spec.md`
  (AC-01 through AC-32, invariant families I-462.x / I-500.x / I-501.x / I-502.x, scope rulings SR-1
  through SR-5, cross-cutting NFR).
- **Research source:** `docs/features/active/breadcrumb-coordinator-hub-defects-501/research/2026-08-24T09-12-breadcrumb-ordering-invariants-research.md`
  (verified against HEAD `988e819b`).

**Fail-closed evidence rule:** every baseline command step, every regression run, and every final-QC
command step writes its own artifact. If any required baseline artifact, regression artifact, QA
artifact, or coverage-comparison artifact is missing or lacks a required field, the outcome is
BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** each evidence-producing task below names its artifact path. Do not mark
an evidence-backed task complete without the artifact on disk.

---

## Conventions used by every task below

- **`WS`** — the workspace root: the agent worktree checked out for this feature. Every command runs
  with `WS` as the current directory. Every path in this plan is relative to `WS` unless stated
  otherwise. No absolute host path (user profile, account name, machine name) may be written into any
  artifact; record `WS` as the literal token `WS` when a path must be referenced.
- **`FF`** — `docs/features/active/breadcrumb-coordinator-hub-defects-501`. Every evidence path in
  this plan resolves to `FF/evidence/<kind>/` where `<kind>` is one of `baseline`,
  `regression-testing`, `qa-gates`, `issue-updates`, `other`. Evidence paths under `artifacts/` are
  forbidden and must never be used.
- **`<TS>`** — the ISO-8601 `yyyy-MM-ddTHH-mm` timestamp at which the task ran. Artifact filenames
  embed it.
- **Artifact schema.** Every command-step artifact records, at minimum, four fields:
  `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. An `[expect-fail]` artifact additionally
  records `ExpectedExitCode: 1` and the failing assertion text. Baseline and final-QC test artifacts
  record NUMERIC coverage values; the literal `UNVERIFIED` is not an acceptable value anywhere.
- **Shell.** PowerShell 7 (`pwsh -NoProfile`). Single-quote command payloads; double any literal
  quote inside them.
- **Line count.** Every "line count is at or below N" acceptance in this plan means the physical
  line count reported by `(Get-Content -LiteralPath <path>).Count`, including blank lines.

#### Toolchain command reference (from `CLAUDE.md`; do not alter)

1. **Format:** `dotnet tool run csharpier format .` (verify read-only: `dotnet tool run csharpier check .`)
2. **Analyze:** `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`
3. **Type-check:** `msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true`
4. **Test:** `vstest.console.exe <test-assembly-paths> /EnableCodeCoverage`

Two omissions are deliberate and must not be "restored": do NOT add `/p:Nullable=enable`, and do NOT
use `/t:Build`. A warm `/t:Build` returns exit 0 with `CoreCompile` skipped on every project, so the
analyzer and nullable gates would be vacuous.

`msbuild` and `vstest.console.exe` are not on `PATH` under `pwsh -NoProfile`. Phase 0 resolves both
through `vswhere.exe` and records the absolute paths; every later task uses the recorded path with
the call operator `&`.

#### Scoped test-run template

Regression and verification runs use the resolved `vstest.console.exe` path from P0-T5 with an
explicit assembly path and an explicit results directory, so each run deposits its own TRX:

```text
& $vstest 'QuickFiler.Test\bin\Debug\QuickFiler.Test.dll' /InIsolation
    '/Settings:scripts\vscode\TaskMaster.cli.runsettings'
    '/TestCaseFilter:FullyQualifiedName~TESTNAME'
    '/Logger:trx;LogFileName=pN-tM.trx'
    '/ResultsDirectory:docs/features/active/breadcrumb-coordinator-hub-defects-501/evidence/regression-testing/trx/pN-tM'
```

A shared results directory makes "which TRX belongs to which task" ambiguous once several
`[expect-fail]` runs deposit into it, so each run gets its own `pN-tM` segment. `LogFileName=` is
mandatory: the vstest default TRX name embeds the account and host name.

#### Design rulings made by this plan (recorded, with evidence)

- **PD-1 — `RunSynchronous` settles the lease on its own skip path.** AC-19 requires the lease-leak
  regression test to live in `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`,
  to compile against HEAD, to be RED there, and to be green after the fix. That test calls
  `lifetime.RunSynchronous(lease, action)` directly, with no coordinator in the picture, so it can
  only turn green if `RunSynchronous` itself settles the skipped lease. `RunSynchronous` therefore
  calls `Abandon(lease)` when `TryRunCurrent` returns `false`. Both call sites additionally branch on
  `false` exactly as `spec.md` → `## Proposed Fix` → `#502` specifies. The duplicate `Abandon` is
  harmless and is verified idempotent by construction:
  `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs:266-271` returns early once
  `CancellationStarted` is set, and the disposal predicates at `:246` and `:285` are both guarded by
  `!lease.SourceDisposed`.
- **PD-2 — AC-11's logging half is verified by a source assertion, not by a runtime log assertion.**
  `QuickFiler.Test/QuickFiler.Test.csproj` carries no `log4net` reference (verified: zero `log4net`
  matches in that project file, and zero `log4net` matches in any `QuickFiler.Test/**/*.cs`), so a
  `MemoryAppender` test of the kind used in `TaskMaster.Test/AppGlobals/AppEventsTests.Helpers.cs:228-247`
  would not compile there. Adding a `<Reference>` is outside this feature's authorized project-file
  budget of exactly one `<Compile Include>` line per project file (spec `## Scope & Non-Goals`). The
  reflective alternative (resolving `log4net` types by name at run time) is rejected as brittle and
  as ~30 lines against 86 lines of headroom. AC-11's second half — `PostJson` does not propagate the
  surface throw — is asserted by a real test. Recorded as a plan-level verification-route decision;
  it changes no spec requirement.

---

### Phase 0 — Baseline capture, policy reads, and toolchain bootstrap

This worktree has no `.dotnet-sdk`, no `.config/dotnet-tools.json`, and no `packages/` tree, and the
16 first-party `.csproj` files pin analyzer DLL paths (`Meziantou.Analyzer.3.0.156`,
`Roslynator.Analyzers.4.16.0`) that differ from the `packages.config` versions (`3.0.174`,
`4.16.1`). A missing `<Analyzer Include>` path is `error CS0006`, not a warning, so every later
`EXIT_CODE: 0` build acceptance is unreachable until P0-T6 through P0-T8 complete.

- [x] [P0-T1] Read the policy documents in the order defined by `.claude/skills/policy-compliance-order/SKILL.md`: `CLAUDE.md`, then `.claude/rules/general-code-change.md`, then `.claude/rules/general-unit-test.md`, then `.claude/rules/csharp.md`, then `.claude/rules/plan-acceptance-gates.md`, then `.claude/skills/atomic-plan-contract/SKILL.md`, then `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Write `FF/evidence/baseline/phase0-instructions-read.<TS>.md` containing `Timestamp:`, `Policy Order:` and the explicit list of the seven file paths read. **Acceptance:** the artifact exists and lists all seven paths in the order above.
- [x] [P0-T2] Read `FF/spec.md` in full and `FF/research/2026-08-24T09-12-breadcrumb-ordering-invariants-research.md` in full, then write `FF/evidence/baseline/ac-inventory.<TS>.md` enumerating acceptance criteria AC-01 through AC-32 with the spec line number of each. **Acceptance:** the artifact lists exactly 32 rows, `AC-01` through `AC-32`, each with a line number.
- [x] [P0-T3] Run `git rev-parse HEAD` and the gating scoped `git status --porcelain -- QuickFiler/ QuickFiler.Test/`, and record both in `FF/evidence/baseline/repo-state.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Additionally record, as explicitly NON-GATING context in the same artifact, (a) the feature-folder-scoped `git status --porcelain -- docs/features/active/breadcrumb-coordinator-hub-defects-501/` output verbatim and (b) the unscoped `git status --porcelain` output verbatim; both are expected to be non-empty and neither is part of the acceptance condition. The feature folder is a write target of P0-T1 and P0-T2, which run before this task, so a zero-line acceptance over that pathspec is unsatisfiable by construction — the production/test source tree is the only pathspec that can legitimately be clean at this point. The unscoped capture is likewise expected non-empty (`.claude/agent-memory/**` is tracked in this worktree and is written by agents during planning and execution; the plan file itself may be uncommitted at HEAD during preflight), matching the same fix at P9-T4/P9-T6. **Acceptance:** the artifact records a 40-character HEAD sha and reports that `git status --porcelain -- QuickFiler/ QuickFiler.Test/` produced zero output lines. That sha is `BASELINE_SHA` for every later diff gate.
- [x] [P0-T4] Resolve MSBuild by running `& 'C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe' -latest -products * -requires Microsoft.Component.MSBuild -find 'MSBuild\**\Bin\MSBuild.exe'` and record the single resolved absolute path on a line beginning `MSBUILD:` in `FF/evidence/baseline/toolchain-resolution-msbuild.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` and one `MSBUILD:` line whose path ends `Bin\MSBuild.exe`.
- [x] [P0-T5] Resolve the test runner by running `& 'C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe' -latest -products * -find 'Common7\IDE\Extensions\TestPlatform\vstest.console.exe'` and record the resolved absolute path on a line beginning `VSTEST:` in `FF/evidence/baseline/toolchain-resolution-vstest.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` and one `VSTEST:` line whose path ends `vstest.console.exe`.
- [x] [P0-T6] Provision the repo-local SDK by running `pwsh -NoProfile -File .\scripts\vscode\Install-RepoDotNetSdk.ps1` from `WS`, then run `dotnet --version` and `dotnet --list-sdks`. Record all three in `FF/evidence/baseline/bootstrap-dotnet-sdk.<TS>.md`. `global.json` pins `sdk.version 8.0.205` with `paths: [".dotnet-sdk", "$host$"]`, so without this step every `dotnet` command prints the `global.json` `errorMessage` instead of a version. **Acceptance:** the artifact records `dotnet --version` printing `8.0.205` and `dotnet --list-sdks` containing a path ending `.dotnet-sdk\sdk`.
- [x] [P0-T7] Restore NuGet packages by running `pwsh -NoProfile -File .\scripts\vscode\Invoke-Restore.ps1 -SolutionPath TaskMaster.sln -Configuration Debug` and record the run in `FF/evidence/baseline/bootstrap-nuget-restore.<TS>.md`. **Acceptance:** the artifact records `EXIT_CODE: 0` and the directory `packages/` exists under `WS`.
- [x] [P0-T8] Back-fill the two skewed analyzer versions the `<Analyzer Include>` items name but `packages.config` does not pin, by running `nuget install Meziantou.Analyzer -Version 3.0.156 -OutputDirectory packages` and `nuget install Roslynator.Analyzers -Version 4.16.0 -OutputDirectory packages` (or by copying those two folders from the primary checkout). Record both in `FF/evidence/baseline/bootstrap-analyzer-backfill.<TS>.md`. **Acceptance:** the artifact records that both `packages/Meziantou.Analyzer.3.0.156/analyzers/dotnet/roslyn5.0/cs/Meziantou.Analyzer.dll` and `packages/Roslynator.Analyzers.4.16.0/analyzers/dotnet/roslyn4.7/cs/Roslynator.CSharp.Analyzers.dll` exist.
- [x] [P0-T9] Resolve the CSharpier invocation: run `dotnet tool restore` from `WS` and record its exit code, then run `dotnet tool run csharpier --version` and confirm it reports `1.2.6` (the version pinned by the root `dotnet-tools.json` manifest). A global-tool fallback is NOT an authorized outcome: `CLAUDE.md` § C#1.1 forbids invoking a globally installed `csharpier`, so if the manifest invocation fails, the remedy is to relocate the manifest to a location `dotnet tool restore` reads (e.g., move `dotnet-tools.json` to `.config/dotnet-tools.json`) and re-run `dotnet tool restore` — never to fall back to a global install. Write `FF/evidence/baseline/toolchain-resolution-csharpier.<TS>.md` with the four schema fields, the resolved version, and exactly one line beginning `CSHARPIER_INVOCATION:` whose value is exactly `dotnet tool run csharpier`. **Acceptance:** the artifact records exactly one `CSHARPIER_INVOCATION:` line whose value is exactly `dotnet tool run csharpier`, and a `--version` run for that invocation records `EXIT_CODE: 0` and reports `1.2.6`.
- [x] [P0-T10] Probe the coverage collector by running `dotnet-coverage --version` and record it in `FF/evidence/baseline/toolchain-resolution-dotnet-coverage.<TS>.md` with the four schema fields. `scripts/vscode/Invoke-MSTestWithCoverage.ps1` throws when `dotnet-coverage` is absent from `PATH`, which would make every coverage acceptance unreachable. **Acceptance:** the artifact records `EXIT_CODE: 0` and a non-empty version string.
- [x] [P0-T11] Capture the formatter baseline by running `dotnet tool run csharpier check .` from `WS` and write `FF/evidence/baseline/csharpier-check.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE:` and, in `Output Summary:`, the count of files reported as needing formatting. If that count is greater than zero, the artifact must also list the offending paths, because Phase 7 runs `csharpier format .` repository-wide and the Phase 9 scope-lock gate admits only this feature's own `.cs` and `.csproj` paths.
- [x] [P0-T12] Capture the analyzer baseline by running `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` using the path recorded in P0-T4, and write `FF/evidence/baseline/msbuild-analyzers.<TS>.md` with the four schema fields plus the error and warning counts. **Acceptance:** the artifact records `EXIT_CODE: 0` and an error count of 0.
- [x] [P0-T13] Capture the nullable/type-check baseline by running `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true` using the path recorded in P0-T4, and write `FF/evidence/baseline/msbuild-nullable.<TS>.md` with the four schema fields plus the error count. The command must contain `/t:Rebuild` and must not contain `/p:Nullable=enable`. **Acceptance:** the artifact records `EXIT_CODE: 0`, an error count of 0, and its `Command:` line contains `/t:Rebuild` and does not contain `/p:Nullable=enable`.
- [x] [P0-T14] Capture the full-suite test and coverage baseline by launching `pwsh -NoProfile -File .\scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput coverage\baseline.cobertura.xml` with `Start-Process -PassThru -RedirectStandardOutput -RedirectStandardError` to a log under `FF/evidence/baseline/`, recording the PID, polling the log, and taking `EXIT_CODE:` from the process object's `ExitCode`. Copy the produced Cobertura file to `FF/evidence/baseline/baseline.cobertura.<TS>.xml` and write `FF/evidence/baseline/test-coverage.<TS>.md` with the four schema fields. This is a baseline capture, not a gate: a non-zero exit code is recorded, not remediated. **Acceptance:** the artifact records the observed `EXIT_CODE:`, total/passed/failed/skipped test counts, a `BASELINE_FAILURE_SET` section explicitly enumerating the fully qualified name of every failing test (an empty set is a valid value and must be recorded as an explicitly empty set), and NUMERIC repository `line-rate` and `branch-rate` values read from the Cobertura root element expressed as percentages to two decimal places. The placeholder `UNVERIFIED` is not acceptable.
- [x] [P0-T15] From `FF/evidence/baseline/baseline.cobertura.<TS>.xml`, compute the per-file baseline line-rate and branch-rate for each of `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs`, `QuickFiler/Viewers/BreadcrumbMessengerHub.cs`, and `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, aggregating every `<class>` element that shares the same `filename` attribute (async state machines and closures are emitted as separate `<class>` elements and must not be reported separately). Write `FF/evidence/baseline/owned-file-coverage.<TS>.md`. **Acceptance:** the artifact records four rows, one per file path above, each with a numeric line-rate and a numeric branch-rate.
- [x] [P0-T16] Record the current line count of every file this plan may touch — `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs`, `QuickFiler/Viewers/BreadcrumbMessengerHub.cs`, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, `QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs`, `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs` — using `(Get-Content -LiteralPath <path>).Count`, into `FF/evidence/baseline/line-counts.<TS>.md`. Do not use the `Get-Content -LiteralPath <path> | Measure-Object -Line` form: `Measure-Object -Line` drops blank lines and undercounts (measured 436 against 487 actual physical lines on `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`). **Acceptance:** the artifact records eight rows with numeric counts, and every count is at or below 500.
- [x] [P0-T17] Establish the pre-change green status of the three tests that must pass unmodified by running the scoped test template with `/TestCaseFilter:FullyQualifiedName~PendingToggleClose_HostOwnershipSuppressesFallbackAndRepeatedClose|FullyQualifiedName~SelectorStateTransitions_RequestOpenThenCloseOnlyWhenRequired|FullyQualifiedName~Attach_ReplayFailureRollsBackSubscriptionAndAllowsRetry` and results directory `FF/evidence/baseline/trx/p0-t17`. Write `FF/evidence/baseline/must-pass-tests.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` and exactly 3 passed, 0 failed, 0 skipped.
- [x] [P0-T18] Write `FF/evidence/other/design-rulings.<TS>.md` recording plan rulings PD-1 and PD-2 verbatim from this plan's `Design rulings made by this plan` subsection, each with the file:line evidence cited there. **Acceptance:** the artifact contains one section headed `PD-1` and one headed `PD-2`, and each cites at least one `file:line` reference.

---

### Phase 1 — #462 failing-first regression and the two-flag close fix

Defect #462 is fully independent of the other three: `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`
shares no member, field, or call path with them. The design is spec SR-4 / research §6.1 option D:
split `_closePending` into `_closeInFlight` and `_closeCompleted`. The two new identifiers this phase
introduces, quoted verbatim so later search assertions are exonerated, are `_closeInFlight` and
`_closeCompleted`.

- [x] [P1-T1] [expect-fail] Add the regression test `RequestOpen_AfterSuccessfulCloseAndHostReopen_ReachesHostOpenAsync` to `QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs`, driving I-462.2 with `CoordinatorHarness`, `ControlledHost.SetOpen(true)`, and `CapturingSynchronizationContext.DrainUntil`, and asserting `harness.Host.Requests` has 2 entries and the returned task's result is `true`. Add only new lines; do not edit or delete any existing line in that file. **Acceptance:** `git diff --numstat -- QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs` reports a deleted-line count of 0 and an added-line count greater than 0, and the file's line count is at or below 500.
- [x] [P1-T2] [expect-fail] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~RequestOpen_AfterSuccessfulCloseAndHostReopen_ReachesHostOpenAsync`, `/Logger:trx;LogFileName=p1-t2.trx`, results directory `FF/evidence/regression-testing/trx/p1-t2`, and write `FF/evidence/regression-testing/red-462-reopen.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`, and the verbatim assertion-failure text. **Acceptance:** the artifact records exactly 1 test run, 1 failed, 0 passed, and the failure message shows the observed `Requests` count is 1.
- [x] [P1-T3] Add the guard test `CloseCore_RepeatedCloseWithoutReopen_ClosesHostExactlyOnce` to `QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs`, asserting I-462.3 (two consecutive `SetDroppedDown(false)` drives yield exactly one `harness.Host.CloseReasons` entry). This test is green on HEAD by design; it is the standing guard that rules out research §6.1 option A. **Acceptance:** `git diff --numstat` for that file still reports a deleted-line count of 0, and the file's line count is at or below 500.
- [x] [P1-T4] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~CloseCore_RepeatedCloseWithoutReopen_ClosesHostExactlyOnce`, `/Logger:trx;LogFileName=p1-t4.trx`, results directory `FF/evidence/regression-testing/trx/p1-t4`, and write `FF/evidence/regression-testing/green-462-repeated-close-prefix.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 1 passed, 0 failed — proving the guard is meaningful before the fix rather than an artefact of it.
- [x] [P1-T5] Implement the two-flag fix in `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`: replace the `private bool _closePending;` field at `:28` with `private bool _closeInFlight;` and `private bool _closeCompleted;`, each carrying an XML doc comment stating its distinct meaning; rewrite `CloseCore` (`:283-313`) to the order `if (_released) return false;` then `if (_closeInFlight) return true;` then `if (_closeCompleted) return true;` then latch `_closeInFlight`, and clear `_closeInFlight` in a `finally` around the `_host.Close(reason)` call at `:296` so it reads `false` on the success, not-closed, throw and released exits; set `_closeCompleted` on the successful-close path at `:303-308`; change `RequestOpen`'s guard at `:93` to `if (_closeInFlight && _host.IsOpen)` and clear `_closeCompleted` at `:95`; clear `_closeCompleted` in `Invalidate` at `:329`; remove the now-unused `ClearClosePending` helper at `:315-319`. Do not add the `&& !_host.IsOpen` refinement — SR-4 rejects it because it would read `_host.IsOpen` under `_sync`. **Acceptance:** `git grep -F -n '_closePending' -- QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs` returns no matching lines, and `git grep -F -n '_closeInFlight' -- QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs` returns at least four matching lines.
- [x] [P1-T6] Build with `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`, then run the scoped test template with `/TestCaseFilter:FullyQualifiedName~RequestOpen_AfterSuccessfulCloseAndHostReopen_ReachesHostOpenAsync|FullyQualifiedName~CloseCore_RepeatedCloseWithoutReopen_ClosesHostExactlyOnce`, `/Logger:trx;LogFileName=p1-t6.trx`, results directory `FF/evidence/regression-testing/trx/p1-t6`, and write `FF/evidence/regression-testing/green-462.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 2 passed, 0 failed, 0 skipped.
- [x] [P1-T7] Run the whole drop-down coordinator regression surface with the scoped test template and `/TestCaseFilter:FullyQualifiedName~BreadcrumbDropDownOpenCoordinatorTests|FullyQualifiedName~BreadcrumbPendingOpenCloseTests|FullyQualifiedName~BreadcrumbDropDownLifecycleTests|FullyQualifiedName~BreadcrumbDropDownIntegrationTests`, `/Logger:trx;LogFileName=p1-t7.trx`, results directory `FF/evidence/regression-testing/trx/p1-t7`, and write `FF/evidence/regression-testing/green-462-suite.<TS>.md` with the four schema fields plus the pass/fail/skip counts. **Acceptance:** the artifact records `EXIT_CODE: 0`, 0 failed, 0 skipped, and a passed count greater than 20.
- [x] [P1-T8] Verify that neither named must-pass drop-down test was edited: `git diff --numstat BASELINE_SHA -- QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.cs` (the single-commit form, which compares `BASELINE_SHA` against the working tree; the two-dot `BASELINE_SHA..HEAD` form is prohibited here because the plan's first commit is P9-T4, so at this point `HEAD == BASELINE_SHA` and the two-dot form prints nothing whatever the working tree holds — P6-T8 uses the same single-commit form) reports 0 added and 0 deleted lines, and `git diff --numstat BASELINE_SHA -- QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs` reports 0 deleted lines. Record both in `FF/evidence/qa-gates/unmodified-tests-462.<TS>.md`. **Acceptance:** the artifact records both numstat results and both satisfy the stated bounds.

---

### Phase 2 — SR-1 partial split of BreadcrumbBridgeCoordinator

`QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs` is at 487 of a hard 500-line cap and the #502
call-site change adds 13 to 17 lines, so the split must land before the Phase 4 edit. SR-1 is
APPROVED and settled; SR-2 (reusing `BreadcrumbBridgeCoordinator.Search.cs`) is REJECTED. The class is
already `public sealed partial` at `:25` and the repository already made this exact split for this
exact file for this exact reason (`BreadcrumbBridgeCoordinator.Search.cs:10-13`).

- [x] [P2-T1] Create `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs` as a new partial part opening with `#nullable enable`, the usings the moved members require, `namespace QuickFiler.Viewers`, a doc comment naming the 500-line ceiling as its reason, and `public sealed partial class BreadcrumbBridgeCoordinator`; move into it, verbatim and with no behavioural edit, `SetSuggestions` (`BreadcrumbBridgeCoordinator.cs:99-115`), the `SuggestionsUpgrade` property (`:117-118`), `PopulateSuggestionsAsync` (`:120-128`) and `AddItems` (`:130-147`), deleting those members from `BreadcrumbBridgeCoordinator.cs` in the same task so the type never declares them twice. Then run `git add -N QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs` before the acceptance check runs: `git grep` searches only tracked files, and `git add -N` adds the path to the index without staging content, making the new file visible to `git grep`. **Acceptance:** `git grep -F -c 'public Task SuggestionsUpgrade' -- QuickFiler/Viewers/` reports exactly one file with exactly one match, and that file is `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`.
- [x] [P2-T2] Add exactly one line `    <Compile Include="Viewers\BreadcrumbBridgeCoordinator.Suggestions.cs" />` to `QuickFiler/QuickFiler.csproj` immediately after line 392 (`<Compile Include="Viewers\BreadcrumbBridgeCoordinator.Search.cs" />`), making no other edit to that file. **Acceptance:** `git diff --numstat -- QuickFiler/QuickFiler.csproj` reports exactly 1 added line and 0 deleted lines.
- [x] [P2-T3] Prove the move compiles by running `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and writing `FF/evidence/qa-gates/build-after-partial-split.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` and an error count of 0.
- [x] [P2-T4] Prove the move is behaviour-neutral by running the scoped test template with `/TestCaseFilter:FullyQualifiedName~BreadcrumbBridgeCoordinatorTests|FullyQualifiedName~BreadcrumbBridgeCoordinatorProbabilityTests|FullyQualifiedName~BreadcrumbCoordinatorLifecycleTests|FullyQualifiedName~BreadcrumbSelectorCoordinatorTests`, `/Logger:trx;LogFileName=p2-t4.trx`, results directory `FF/evidence/regression-testing/trx/p2-t4`, and writing `FF/evidence/regression-testing/pure-move-proof.<TS>.md` with the four schema fields plus counts. **Acceptance:** the artifact records `EXIT_CODE: 0`, 0 failed, 0 skipped, and a passed count greater than 30.
- [x] [P2-T5] Record post-split line counts for `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs` and `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`, measured with `(Get-Content -LiteralPath <path>).Count`, in `FF/evidence/qa-gates/line-counts-after-split.<TS>.md`. Do not use `Get-Content -LiteralPath <path> | Measure-Object -Line`, which drops blank lines and undercounts (436 reported against 487 actual physical lines on the pre-split primary file). **Acceptance:** the artifact records the primary file at or below 445 lines and the new part at or below 100 lines.

---

### Phase 3 — #500 lifetime half: TryRunCurrent lock scope

Research §6.2 option A is mandated. The cross-cutting NFR binds this phase: `TryRunCurrent`'s `bool`
must continue to mean "the action was invoked at entry-time currency" — the currency verdict taken at
entry, never a verdict recomputed after the action returns. Option B (folding a post-action re-check
into the return value) is rejected because it would make Phase 4's `false` branch fire after the
guarded lambda has already assigned `SuggestionsUpgrade`, turning the #502 remedy into a fresh
instance of the #502 defect.

- [x] [P3-T1] [expect-fail] Add `TryRunCurrent_GuardedActionRunsWithoutHoldingLifetimeSync` to `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`, reflecting the private `_sync` field of `BreadcrumbCoordinatorUpgradeLifetime` with the same `BindingFlags.Instance | BindingFlags.NonPublic` the file already uses at `:98`, and recording `Monitor.IsEntered(sync)` from inside the guarded action, asserting it is `false` (I-500.1). The probe is exact on a single thread; use no second thread, no timer, and no wait. **Acceptance:** the test method exists in that file, the file's line count is at or below 500, and `git diff --numstat` for that file reports a deleted-line count of 0.
- [x] [P3-T2] [expect-fail] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~TryRunCurrent_GuardedActionRunsWithoutHoldingLifetimeSync`, `/Logger:trx;LogFileName=p3-t2.trx`, results directory `FF/evidence/regression-testing/trx/p3-t2`, and write `FF/evidence/regression-testing/red-500-lifetime-lock.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`, and the verbatim failure text. **Acceptance:** the artifact records 1 run, 1 failed, 0 passed, and the failure text shows the observed value was `True`.
- [x] [P3-T3] Add `TryRunCurrent_ReentrantInvalidateStillReportsEntryTimeInvocation` to the same file, asserting I-500.3: an action that calls `lifetime.Invalidate()` re-entrantly still yields `TryRunCurrent == true`, and `lifetime.IsCurrent(lease)` is `false` immediately afterwards. This test is green on HEAD by design; it is the standing regression guard for the cross-cutting NFR (AC-28). **Acceptance:** the test method exists in that file, and `git diff --numstat` for that file still reports a deleted-line count of 0.
- [x] [P3-T4] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~TryRunCurrent_ReentrantInvalidateStillReportsEntryTimeInvocation`, `/Logger:trx;LogFileName=p3-t4.trx`, results directory `FF/evidence/regression-testing/trx/p3-t4`, and write `FF/evidence/regression-testing/green-500-nfr-guard-prefix.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 1 passed, 0 failed, establishing that the guard already holds before the fix and therefore detects a regression rather than the fix itself.
- [x] [P3-T5] Restructure `TryRunCurrent` in `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs:133-148` to capture the currency verdict under `_sync`, release the lock, then invoke the action and return the captured verdict: keep the `ArgumentNullException` guard, then `bool current; lock (_sync) { current = IsGenerationCurrentCore(lease) && !lease.Token.IsCancellationRequested; } if (!current) { return false; } action(); return true;`. Add an XML comment on the method stating that the `bool` is the entry-time currency verdict and must never be recomputed after the action returns, and recording the documented consequence that two threads could now both pass the check (not reachable on current wiring: every guarded action runs on the captured `BreadcrumbUiDispatcher` boundary). Change nothing else in the file in this task. **Acceptance:** the body of `TryRunCurrent` contains no invocation of `action` lexically inside a `lock` block, and `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` records `EXIT_CODE: 0` in `FF/evidence/qa-gates/build-after-tryruncurrent.<TS>.md`.
- [x] [P3-T6] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~TryRunCurrent_GuardedActionRunsWithoutHoldingLifetimeSync|FullyQualifiedName~TryRunCurrent_ReentrantInvalidateStillReportsEntryTimeInvocation`, `/Logger:trx;LogFileName=p3-t6.trx`, results directory `FF/evidence/regression-testing/trx/p3-t6`, and write `FF/evidence/regression-testing/green-500-lifetime.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 2 passed, 0 failed, 0 skipped.
- [x] [P3-T7] Run the surrounding regression surface with the scoped test template and `/TestCaseFilter:FullyQualifiedName~BreadcrumbCoordinatorUpgradeLifetimeTests|FullyQualifiedName~BreadcrumbCoordinatorLifecycleTests|FullyQualifiedName~BreadcrumbBridgeCoordinatorTests|FullyQualifiedName~BreadcrumbSelectorCoordinatorTests`, `/Logger:trx;LogFileName=p3-t7.trx`, results directory `FF/evidence/regression-testing/trx/p3-t7`, and write `FF/evidence/regression-testing/green-500-lifetime-suite.<TS>.md` with the four schema fields plus counts. Confirm `RunSynchronous_FailureAbandonsLinkedLeaseAndReportsCancellationFailure` is among the passing tests. **Acceptance:** the artifact records `EXIT_CODE: 0`, 0 failed, 0 skipped, and names `RunSynchronous_FailureAbandonsLinkedLeaseAndReportsCancellationFailure` as passed.

---

### Phase 4 — #502 and the companion lease leak

The companion lease-leak assertion is authored FIRST because it is the only #502 assertion that
compiles against HEAD and is RED there; the I-502.1 and I-502.2 assertions both require a signature
or seam that does not exist on HEAD, and a compile error is not a test failure. Ruling PD-1 applies:
`RunSynchronous` calls `Abandon(lease)` on its own skip path so the lifetime-level test turns green,
and both call sites additionally branch on `false` as `spec.md` specifies.

- [x] [P4-T1] [expect-fail] Add `RunSynchronous_SupersededLeaseSettlesAndDisposesItsSource` to `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`: construct the lifetime, call `BeginPopulation()`, call `Invalidate()`, call `RunSynchronous(lease, () => ran = true)` as a statement (valid against HEAD's `void` return), then assert `ran` is `false`, `lease.Settled` is `true`, and `lease.SourceDisposed` is `true` (I-502.3, AC-15). Use no second thread and no wait. **Acceptance:** the test method exists in that file, the file's line count is at or below 500, and `git diff --numstat` for that file reports a deleted-line count of 0.
- [x] [P4-T2] [expect-fail] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~RunSynchronous_SupersededLeaseSettlesAndDisposesItsSource`, `/Logger:trx;LogFileName=p4-t2.trx`, results directory `FF/evidence/regression-testing/trx/p4-t2`, and write `FF/evidence/regression-testing/red-502-lease-leak.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`, and the verbatim failure text. **Acceptance:** the artifact records 1 run, 1 failed, 0 passed, and the failure text names `Settled` with an observed value of `False`.
- [x] [P4-T3] Change `RunSynchronous` in `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs:111-122` to `internal bool RunSynchronous(BreadcrumbUpgradeLease lease, Action operation)`, returning `TryRunCurrent(lease, operation)` and, when that returns `false`, calling `Abandon(lease)` before returning `false`. Preserve `catch { Abandon(lease); throw; }` verbatim. Add an XML `<returns>` comment stating that `true` means the guarded action was invoked at entry-time currency and `false` means it was skipped and the lease has been settled. **Acceptance:** `git grep -F -n 'internal bool RunSynchronous' -- QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs` returns exactly one matching line, and the analyzer build records `EXIT_CODE: 0` in `FF/evidence/qa-gates/build-after-runsynchronous.<TS>.md`.
- [x] [P4-T4] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~RunSynchronous_SupersededLeaseSettlesAndDisposesItsSource`, `/Logger:trx;LogFileName=p4-t4.trx`, results directory `FF/evidence/regression-testing/trx/p4-t4`, and write `FF/evidence/regression-testing/green-502-lease-leak.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 1 passed, 0 failed.
- [x] [P4-T5] In `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`, split `SetSuggestions` into the public entry point (null guard, `BeginPopulation`, then a call to the new core) and `internal void SetSuggestionsCore(IReadOnlyList<FolderRow> rows, BreadcrumbUpgradeLease lease)` carrying the current body; inside the core, consume the `bool` from `_upgradeLifetime.RunSynchronous(...)` and, when it is `false`, set `SuggestionsUpgrade = Task.CompletedTask;` and call `_upgradeLifetime.Abandon(lease);` (SR-5, AC-13). Use `Task.CompletedTask`, not `Task.FromCanceled`: eleven existing tests call `SuggestionsUpgrade.GetAwaiter().GetResult()`, which would throw on a cancelled task, and a completed task matches the property's declared initial value. This acceptance depends on P2-T1 having already run `git add -N` on `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`; if the index entry is missing, re-run `git add -N QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs` before the check. **Acceptance:** `git grep -F -n 'internal void SetSuggestionsCore' -- QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs` returns exactly one matching line, and that file's line count is at or below 120.
- [x] [P4-T6] In the same file, make `AddItems` consume the `bool` from `_upgradeLifetime.RunSynchronous(...)` and, when it is `false`, call `_upgradeLifetime.Abandon(lease);`, with an XML comment on `AddItems` documenting that a superseded `AddItems` exposes no handle to replace, so the skip is deliberate and observable only through the settled lease (I-502.4, AC-14). **Acceptance:** the analyzer build records `EXIT_CODE: 0` in `FF/evidence/qa-gates/build-after-additems.<TS>.md`, and `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs` is at or below 120 lines.
- [x] [P4-T7] Add `RunSynchronous_SupersededLeaseReportsSkipToCaller` to `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`, asserting I-502.1 in both directions: a superseded lease yields `false` and the action did not run; a current lease yields `true` and the action ran. **Acceptance:** the test method exists in that file, the file's line count is at or below 500, and `git diff --numstat` for that file reports a deleted-line count of 0.
- [x] [P4-T8] Create `QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs` with a `[TestClass]` in namespace `QuickFiler.Test.Viewers` containing `SetSuggestionsCore_SupersededLeaseReplacesStaleSuggestionsUpgrade`: reflect `_upgradeLifetime` out of the coordinator (precedent: `QuickFiler.Test/Viewers/BreadcrumbCoordinatorLifecycleTests.cs:370-377`); FIRST make `SuggestionsUpgrade` a genuinely pending (not-yet-completed) task — construct the coordinator over a `Mock<IFolderHierarchyProvider>(MockBehavior.Strict)` whose hierarchy-resolution call is gated on an uncompleted `TaskCompletionSource<FolderTreeNodeKey>` (the exact arrangement at `QuickFiler.Test/Viewers/BreadcrumbCoordinatorLifecycleTests.cs:340-346`, using that file's `Configure(provider, path, gate.Task, key)` pattern), then call the coordinator's population entry point `SetSuggestions(rows)` so `SuggestionsUpgrade` becomes a pending population task — and only THEN capture that pending task as the incomplete `SuggestionsUpgrade` handle. This ordering is load-bearing: the property's initial value is `Task.CompletedTask`, a process-wide singleton, so a handle captured before this arrangement would be unconditionally reference-equal to any later `Task.CompletedTask` assignment and the inequality assertion could never pass. Then call `BeginPopulation()` then `Invalidate()`, invoke `SetSuggestionsCore` with the dead lease, and assert the post-call `SuggestionsUpgrade` is not reference-equal to the captured task and is already completed (I-502.2, AC-13). The gating `TaskCompletionSource` is deliberately never completed, so the test remains deterministic with no second thread and no wait. `[assembly: InternalsVisibleTo("QuickFiler.Test")]` at `QuickFiler/Properties/AssemblyInfo.cs:5` makes the `internal` call direct. Use MSTest, Moq and FluentAssertions; no second thread, no wait, no temporary file. Before the acceptance check runs, run `git add -N QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs` so the new file has an index entry and the P6-T2 determinism scan (which uses `git grep`, tracked files only) can see it. **Acceptance:** the file exists, its line count is at or below 200, and the test's arrange section contains an assertion that the captured handle's `IsCompleted` is `false` BEFORE the act step, so the later reference-inequality assertion is not trivially satisfiable against the `Task.CompletedTask` singleton.
- [x] [P4-T9] Add exactly one line `    <Compile Include="Viewers\BreadcrumbBridgeCoordinatorSupersessionTests.cs" />` to `QuickFiler.Test/QuickFiler.Test.csproj` immediately after line 60 (`<Compile Include="Viewers\BreadcrumbBridgeCoordinatorTests.cs" />`), adjacent to the sibling test for the same production type; that item group is not alphabetically ordered, so adjacency — not alphabetical position — is the operative rule. Make no other edit to that file. **Acceptance:** `git diff --numstat -- QuickFiler.Test/QuickFiler.Test.csproj` reports exactly 1 added line and 0 deleted lines.
- [x] [P4-T10] Build with the analyzer command, then run the scoped test template with `/TestCaseFilter:FullyQualifiedName~RunSynchronous_SupersededLeaseReportsSkipToCaller|FullyQualifiedName~SetSuggestionsCore_SupersededLeaseReplacesStaleSuggestionsUpgrade|FullyQualifiedName~RunSynchronous_SupersededLeaseSettlesAndDisposesItsSource`, `/Logger:trx;LogFileName=p4-t10.trx`, results directory `FF/evidence/regression-testing/trx/p4-t10`, and write `FF/evidence/regression-testing/green-502.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 3 passed, 0 failed, 0 skipped.
- [x] [P4-T11] Run the coordinator regression surface with the scoped test template and `/TestCaseFilter:FullyQualifiedName~BreadcrumbBridgeCoordinatorTests|FullyQualifiedName~BreadcrumbBridgeCoordinatorProbabilityTests|FullyQualifiedName~BreadcrumbCoordinatorLifecycleTests|FullyQualifiedName~BreadcrumbCoordinatorUpgradeLifetimeTests|FullyQualifiedName~BreadcrumbSubfolderActivationTests`, `/Logger:trx;LogFileName=p4-t11.trx`, results directory `FF/evidence/regression-testing/trx/p4-t11`, and write `FF/evidence/regression-testing/green-502-suite.<TS>.md` with the four schema fields plus counts. **Acceptance:** the artifact records `EXIT_CODE: 0`, 0 failed, 0 skipped, and a passed count greater than 40.

---

### Phase 5 — #500 hub half and #501 as one PostJson rewrite

These are ONE edit carrying two acceptance criteria families, not two tasks against the same method:
containing the throw inside the existing `lock` would leave I-500.2 unsatisfied, and narrowing the
lock without containment would leave I-501.1 unsatisfied. The starvation assertion must be
ORDER-INDEPENDENT: `Dictionary<TKey,TValue>.Values` enumeration order is not contractual, so the test
attaches two surfaces that BOTH increment an attempt counter before throwing and asserts the total is
2. The log literal this phase introduces, quoted verbatim here so the later search assertion is
exonerated, is `Breadcrumb surface delivery failed.`

- [x] [P5-T1] [expect-fail] Add a `private sealed class CountingThrowingMessenger : IWebViewMessenger, IDisposable` fake and the consolidated test `PostJson_SurfaceFailureDoesNotStarveOtherSurfacesOrFalsifyReplayCache` to `QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs`: attach two `CountingThrowingMessenger` surfaces that each increment a shared attempt counter BEFORE throwing, plus one recording surface; call `hub.PostJson(render)` once; assert the total attempt count is exactly 2 (I-501.1, AC-08), that the recording surface's `Posted` collection contains the payload (I-501.2, AC-09), that `hub.PostJson` did not throw (SR-3, AC-11 containment half), and that a freshly attached recording surface replays the same payload (I-501.3, AC-10). Add only new lines. **Acceptance:** the test method and the fake class both exist in that file, `git diff --numstat` for that file reports a deleted-line count of 0, and the file's line count is at or below 500.
- [x] [P5-T2] [expect-fail] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~PostJson_SurfaceFailureDoesNotStarveOtherSurfacesOrFalsifyReplayCache`, `/Logger:trx;LogFileName=p5-t2.trx`, results directory `FF/evidence/regression-testing/trx/p5-t2`, and write `FF/evidence/regression-testing/red-501-starvation.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`, and the verbatim failure text. **Acceptance:** the artifact records 1 run, 1 failed, 0 passed, and the failure text shows either an observed attempt count of 1 or the propagated surface exception.
- [x] [P5-T3] [expect-fail] Add `PostJson_SurfaceInvocationRunsAfterHubLockIsReleased` to `QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs` — the file that owns the `Monitor.IsEntered` template at `:145-192` and has 66 lines of headroom — reflecting `BreadcrumbMessengerHub._sync` with `BindingFlags.Instance | BindingFlags.NonPublic`, attaching a `Mock<IWebViewMessenger>` whose `PostJson` callback records `Monitor.IsEntered(hubSync)`, and asserting the recorded value is `false` (I-500.2, AC-05). **Acceptance:** the test method exists in that file, `git diff --numstat` for that file reports a deleted-line count of 0, and the file's line count is at or below 500.
- [x] [P5-T4] [expect-fail] Add `PostJson_ReentrantAttachFromSurfaceDoesNotThrowCollectionModified` to the same file: a `Mock<IWebViewMessenger>` whose `PostJson` callback calls `hub.Attach(other.Object, mode)` re-entrantly; assert `hub.PostJson` does not throw `InvalidOperationException` and that the re-entrant attach took effect (I-500.4, AC-07). **Acceptance:** the test method exists in that file, `git diff --numstat` for that file reports a deleted-line count of 0, and the file's line count is at or below 500.
- [x] [P5-T5] [expect-fail] Run the scoped test template with `/TestCaseFilter:FullyQualifiedName~PostJson_SurfaceInvocationRunsAfterHubLockIsReleased|FullyQualifiedName~PostJson_ReentrantAttachFromSurfaceDoesNotThrowCollectionModified`, `/Logger:trx;LogFileName=p5-t5.trx`, results directory `FF/evidence/regression-testing/trx/p5-t5`, and write `FF/evidence/regression-testing/red-500-hub.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `ExpectedExitCode: 1`, `Output Summary:`, and both verbatim failure texts. **Acceptance:** the artifact records 2 run, 2 failed, 0 passed, one failure showing the lock probe observed `True` and the other showing `InvalidOperationException`.
- [x] [P5-T6] Rewrite `PostJson` in `QuickFiler/Viewers/BreadcrumbMessengerHub.cs:119-136` as ONE edit: keep the `ArgumentNullException` guard; under `lock (_sync)` call `ThrowIfDisposed()`, compute `type`, call `CacheState(type, json)` — the cache write stays inside the lock and before the broadcast — and snapshot `_attachments.Values` into a local array; release the lock; iterate the snapshot outside the lock, wrapping each `PostToSurface` call in its own `try`/`catch (Exception exception)` that logs through the file's existing `log4net` logger with the message `Breadcrumb surface delivery failed.` on the pattern already at `:261-273` (`SafeUnsubscribe`) and continues to the next attachment. `PostJson` must not propagate a surface throw (SR-3). Preserve the `ArgumentNullException` on a null `json` and the `ObjectDisposedException` from `ThrowIfDisposed()`. If the file would exceed 500 lines, extract the broadcast loop into a small private method rather than adding another file. **Acceptance:** `git grep -F -n 'Breadcrumb surface delivery failed.' -- QuickFiler/Viewers/BreadcrumbMessengerHub.cs` returns exactly one matching line, and the file's line count is at or below 490.
- [x] [P5-T7] Build with the analyzer command, then run the scoped test template with `/TestCaseFilter:FullyQualifiedName~PostJson_SurfaceFailureDoesNotStarveOtherSurfacesOrFalsifyReplayCache|FullyQualifiedName~PostJson_SurfaceInvocationRunsAfterHubLockIsReleased|FullyQualifiedName~PostJson_ReentrantAttachFromSurfaceDoesNotThrowCollectionModified`, `/Logger:trx;LogFileName=p5-t7.trx`, results directory `FF/evidence/regression-testing/trx/p5-t7`, and write `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` with 3 passed, 0 failed, 0 skipped.
- [x] [P5-T8] Verify the diagnosability half of I-501.4 by source inspection under ruling PD-2: run `git grep -F -n 'Breadcrumb surface delivery failed.' -- QuickFiler/Viewers/BreadcrumbMessengerHub.cs` and confirm the matched line sits inside the per-surface `catch` block of the broadcast loop, and that the surrounding statement calls `log4net.LogManager.GetLogger`. Write `FF/evidence/qa-gates/logging-verification-501.<TS>.md` recording the command, the matched line with its line number, the enclosing method name, and a restatement of PD-2's justification. **Acceptance:** the artifact records exactly one matched line, names `PostJson` (or the extracted broadcast helper) as the enclosing method, and records why the assertion is source-level rather than a runtime appender assertion: `QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs` stands at 492 of the 500-line budget (AC-25), leaving no room for a `MemoryAppender` fixture, and AC-24 forbids a third new test file. **PLAN CORRECTION (recorded 2026-08-27, resumed run):** the original wording of this acceptance required citing `QuickFiler.Test/QuickFiler.Test.csproj` as carrying no `log4net` reference. That premise (ruling PD-2) is FALSE of the tree — the reference exists at `QuickFiler.Test/QuickFiler.Test.csproj:209-210`. The false premise is replaced here by the true file-budget reason. The two verified conjuncts are unchanged, and no verification is weakened: AC-11's non-propagation half remains proven by a red-to-green runtime test.
- [x] [P5-T9] Run the hub regression surface with the scoped test template and `/TestCaseFilter:FullyQualifiedName~BreadcrumbMessengerHubTests|FullyQualifiedName~BreadcrumbMessengerHubCoverageTests|FullyQualifiedName~BreadcrumbSelectorCoordinatorTests|FullyQualifiedName~BreadcrumbDuplicateIdentityIntegrationTests`, `/Logger:trx;LogFileName=p5-t9.trx`, results directory `FF/evidence/regression-testing/trx/p5-t9`, and write `FF/evidence/regression-testing/green-501-suite.<TS>.md` with the four schema fields plus counts, confirming `Attach_ReplayFailureRollsBackSubscriptionAndAllowsRetry` is among the passing tests. **Acceptance:** the artifact records `EXIT_CODE: 0`, 0 failed, 0 skipped, and names `Attach_ReplayFailureRollsBackSubscriptionAndAllowsRetry` as passed.
- [x] [P5-T10] Record post-change line counts for `QuickFiler/Viewers/BreadcrumbMessengerHub.cs`, `QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs` and `QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs` in `FF/evidence/qa-gates/line-counts-after-hub.<TS>.md`. **Acceptance:** the artifact records three numeric counts, each at or below 500.

---

### Phase 6 — Structure, ownership, determinism, and budget audits

- [x] [P6-T1] Audit the 500-line cap over every added and modified `.cs` file — `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs`, `QuickFiler/Viewers/BreadcrumbMessengerHub.cs`, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`, `QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs`, `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs` — with `(Get-Content -LiteralPath <path>).Count`, into `FF/evidence/qa-gates/line-count-audit.<TS>.md`. Do not use the `Get-Content -LiteralPath <path> | Measure-Object -Line` form: `Measure-Object -Line` drops blank lines and undercounts (measured 436 against 487 actual physical lines on `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`). This task is the pre-format leg of the AC-25 audit; P7-T8 is the post-format leg — it re-runs the same audit after the Phase 7 formatting pass completes, and its artifact is the artifact of record for AC-25's "after the change" condition. **Acceptance:** the artifact records ten rows, every count is at or below 500, and the artifact states the instrument used as `(Get-Content -LiteralPath <path>).Count` (AC-25).
- [x] [P6-T2] Scan every test file this plan added or modified for banned determinism constructs by running `git grep -n -E 'Thread\.Sleep|Task\.Delay|DateTime\.Now|DateTime\.UtcNow|new Thread\(|Path\.GetTempFileName|Path\.GetTempPath' -- QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs` and write `FF/evidence/qa-gates/determinism-scan.<TS>.md` with the command and its complete output. **Acceptance:** the artifact records zero matched lines; it additionally states for each of the ten new test methods this plan adds (P1-T1, P1-T3, P3-T1, P3-T3, P4-T1, P4-T7, P4-T8, P5-T1, P5-T3, P5-T4) which ordering mechanism it uses (injected delegate, reflected `Monitor.IsEntered` probe, or explicit synchronization-context drain); and it additionally records, for each of the five scanned paths, that `git ls-files -- <path>` returns that path — proving none of the five was silently skipped by `git grep` as untracked (AC-27).
- [x] [P6-T3] Confirm no sibling-owned file was written by running `git status --porcelain -- QuickFiler/Viewers/WebView2Messenger.cs QuickFiler/Viewers/WebView2BreadcrumbHost.cs QuickFiler/Viewers/BreadcrumbItemViewerLifecycleCoordinator.cs QuickFiler/Viewers/BreadcrumbPopupUiOperations.cs QuickFiler/Viewers/BreadcrumbDropDownHost.cs QuickFiler/Viewers/ItemViewer.Breadcrumb.cs` and write `FF/evidence/qa-gates/ownership-precommit.<TS>.md` with the command and its output. **Acceptance:** the artifact records zero output lines from that command (AC-26, pre-commit leg; the post-commit leg is P9-T5).
- [x] [P6-T4] Confirm the project-file budget by running `git diff --numstat -- QuickFiler/QuickFiler.csproj QuickFiler.Test/QuickFiler.Test.csproj` and write `FF/evidence/qa-gates/project-file-budget.<TS>.md` with the command and its output. **Acceptance:** the artifact records exactly two rows, each with 1 added line and 0 deleted lines, and no other project file appears.
- [x] [P6-T5] Confirm the three must-pass existing tests were never edited by running `git diff --numstat -- QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.cs QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs` and write `FF/evidence/qa-gates/unmodified-tests-audit.<TS>.md`. **Acceptance:** the artifact records 0 added and 0 deleted lines for `BreadcrumbDropDownOpenCoordinatorTests.cs`, and a deleted-line count of 0 for each of the other two files, proving every change to them was additive (AC-20, AC-21, AC-22).
- [x] [P6-T6] Confirm the #462 field split is complete by running `git grep -F -n '_closePending' -- QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs` and `git grep -F -n '_closeCompleted' -- QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`, and write `FF/evidence/qa-gates/closepending-split.<TS>.md` with both commands and both outputs. The search is scoped to that single file because the token also appears in this feature's own documents and in tracked agent-memory files, where a repository-wide zero-hit assertion would be unsatisfiable by construction. **Acceptance:** the artifact records zero matched lines for the first command and at least three matched lines for the second (AC-01).
- [x] [P6-T7] Confirm the cross-cutting NFR by reading `TryRunCurrent` in `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs` and recording in `FF/evidence/qa-gates/nfr-entry-time-verdict.<TS>.md` the full method body, the statement that returns the value, and an explicit statement that no currency evaluation occurs after `action()` is invoked. **Acceptance:** the artifact quotes the method body, and that body contains no call to `IsGenerationCurrentCore`, `IsCurrent`, or `IsCancellationRequested` lexically after the `action()` invocation (AC-28).
- [x] [P6-T8] Confirm no third new test file was added by running `git status --porcelain -- QuickFiler.Test/` and `git diff --name-only --diff-filter=A BASELINE_SHA -- QuickFiler.Test/`, and write `FF/evidence/qa-gates/new-test-file-budget.<TS>.md` with both commands and outputs. **Acceptance:** the artifact shows exactly one added file under `QuickFiler.Test/`, and it is `QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs` (AC-24).

---

### Phase 7 — Final QA loop

Run the four toolchain steps in order: format, then analyze, then type-check, then test. **If any step
fails, or if any step changes any file, restart the loop from step 1** and re-run every step; the
phase is complete only when all four pass in one uninterrupted pass with no file change after the
formatting step.

The full-suite coverage run takes roughly twenty minutes across nine assemblies. Launch it with
`Start-Process -PassThru` plus `-RedirectStandardOutput` and `-RedirectStandardError` to a log under
`FF/evidence/qa-gates/`, record the PID, poll the log with short waits, and take `EXIT_CODE:` from the
process object's `ExitCode`. Before any retry, kill the whole process tree (the pwsh runner, every
`testhost`, `vstest.console` and `dotnet-coverage`). The coverage runner may be re-run at most twice,
and only on the recorded `PumpTimeoutMs` expiry signature; every other failure restarts the toolchain
loop from step 1.

- [x] [P7-T1] Run the formatting step `dotnet tool run csharpier format .` from `WS` and write `FF/evidence/qa-gates/final-csharpier-format.<TS>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the count of files the formatter rewrote, computed as the number of files whose SHA-256 changed across the run rather than from the tool's own processed-file count. **Acceptance:** the artifact records `EXIT_CODE: 0` and a rewritten-file count computed from before/after SHA-256 comparison (AC-29, first half).
- [x] [P7-T2] Run the read-only verification `dotnet tool run csharpier check .` and write `FF/evidence/qa-gates/final-csharpier-check.<TS>.md` with the four schema fields. **Acceptance:** the artifact records `EXIT_CODE: 0` and reports zero files needing formatting (AC-29, second half).
- [x] [P7-T3] Run `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write `FF/evidence/qa-gates/final-msbuild-analyzers.<TS>.md` with the four schema fields plus error and warning counts. **Acceptance:** the artifact records `EXIT_CODE: 0` and an analyzer error count of 0 (AC-30).
- [x] [P7-T4] Run `& $msbuild TaskMaster.sln /t:Rebuild /m /p:Configuration=Debug "/p:Platform=Any CPU" /p:TreatWarningsAsErrors=true` and write `FF/evidence/qa-gates/final-msbuild-nullable.<TS>.md` with the four schema fields plus the error count. **Acceptance:** the artifact records `EXIT_CODE: 0`, an error count of 0, and its `Command:` line contains `/t:Rebuild` and does not contain `/p:Nullable=enable` (AC-31).
- [x] [P7-T5] Run the full-suite coverage step by launching `pwsh -NoProfile -File .\scripts\vscode\Invoke-MSTestWithCoverage.ps1 -SearchRoot . -Configuration Debug -CoverageOutput coverage\postchange.cobertura.xml` under the detached mechanic described in this phase's preamble; copy the produced Cobertura file to `FF/evidence/qa-gates/postchange.cobertura.<TS>.xml` and write `FF/evidence/qa-gates/final-test-coverage.<TS>.md` with the four schema fields. **Acceptance:** the artifact records the observed `EXIT_CODE:`; 0 failed and 0 skipped WITHIN the `QuickFiler.Test` assembly specifically; NUMERIC repository `line-rate` and `branch-rate` values expressed as percentages to two decimal places; and, for every OTHER discovered `*.Test.dll`, a failing-test set that is a SUBSET of the `BASELINE_FAILURE_SET` recorded by P0-T14 — that is, no NEW failures were introduced outside `QuickFiler.Test`. Skipped tests outside `QuickFiler.Test` are recorded but do not gate: the repository carries five pre-existing ACTIVE `[Ignore]` attributes on `[TestMethod]`s in a sibling-owned assembly, at `UtilitiesCS.Test/InputBox_Test.cs:11`, `UtilitiesCS.Test/ResourceTests.cs:17`, `UtilitiesCS.Test/ResourceTests.cs:25`, `UtilitiesCS.Test/ResourceTests.cs:108`, and `UtilitiesCS.Test/YesNoToAll_Test.cs:10`, so a repo-wide run always reports skipped >= 5; those tests are pre-existing, sibling-owned, and out of scope per AC-32's ownership boundary, and the P9-T5 scope lock forbids editing those files to remove the `[Ignore]` attributes (AC-32, first half).
- [x] [P7-T6] Compute the coverage delta by comparing `FF/evidence/baseline/baseline.cobertura.<TS>.xml` with `FF/evidence/qa-gates/postchange.cobertura.<TS>.xml`, and write `FF/evidence/qa-gates/coverage-delta.<TS>.md` reporting (a) baseline repository line-rate and branch-rate, (b) post-change repository line-rate and branch-rate, (c) per-file baseline and post-change line-rate for each of the three owned production files that are NOT part of the SR-1 split — `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs`, `QuickFiler/Viewers/BreadcrumbMessengerHub.cs` — plus one COMBINED row for the SR-1 split pair, whose baseline is the `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs` row from P0-T15 and whose post-change value is (covered lines of `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs` plus covered lines of `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`) divided by (total coverable lines of both files combined), aggregating every `<class>` element that shares a `filename` attribute. The split pair is combined because P2-T1 relocates lines between the two files, so a per-file comparison across the split has no common denominator, and the new file has no baseline row by construction. And (d) new/changed-code coverage: for each line added or modified in the five owned production files according to `git diff BASELINE_SHA -- <owned paths>`, whether the post-change Cobertura reports `hits` greater than 0. **Acceptance:** the artifact records all four sections with numeric values, the repository line-rate delta is at or above 0.00 percentage points, each of the four per-file deltas (three single files plus the combined split pair) is at or above -0.50 percentage points (recorded as measurement noise with both raw rates cited when negative), and new/changed-line coverage is at or above 90.00 percent (AC-32, second half).
- [x] [P7-T7] Write `FF/evidence/qa-gates/toolchain-pass-attestation.<TS>.md` recording, for the final uninterrupted pass, the four commands in order with their timestamps and exit codes, the number of loop restarts that occurred, and an explicit statement that no file changed after P7-T1 in the final pass. **Acceptance:** the artifact lists exactly four command rows in the order format, analyze, type-check, test, each with `EXIT_CODE: 0`, and states the restart count as a number.
- [x] [P7-T8] Re-run the 500-line audit AFTER the Phase 7 formatting pass completes. This is the audit deferred from P6-T1 (its post-format leg) and the artifact of record for AC-25's "after the change" condition. Audit the same ten files P6-T1 audits — `QuickFiler/Viewers/BreadcrumbDropDownOpenCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbCoordinatorUpgradeLifetime.cs`, `QuickFiler/Viewers/BreadcrumbMessengerHub.cs`, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs`, `QuickFiler.Test/Viewers/BreadcrumbDropDownOpenCoordinatorTests.Part2.cs`, `QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbMessengerHubTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbSelectorCoordinatorTests.cs`, `QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs` — using the instrument `(Get-Content -LiteralPath <path>).Count`, writing `FF/evidence/qa-gates/line-count-audit-postformat.<TS>.md`. If any file exceeds 500 lines, remediate only within the authorized scope — for a production file, extract members into the already-authorized `QuickFiler/Viewers/BreadcrumbBridgeCoordinator.Suggestions.cs` part or into a private helper method within the same file (no new `<Compile Include>` line); for a test file, relocate whole test methods into another already-owned test file that has headroom (`QuickFiler.Test/Viewers/BreadcrumbCoordinatorUpgradeLifetimeTests.cs` or `QuickFiler.Test/Viewers/BreadcrumbBridgeCoordinatorSupersessionTests.cs`), never a new file. Adding a further `<Compile Include>` line or a further new file is prohibited by SR-1, AC-23, AC-24, and the P9-T5 owned list. If no in-scope remedy exists, halt and escalate rather than add a file or project entry. After any relocation, re-run P6-T1, P6-T4, P6-T5, and P6-T8 (since their recorded evidence would otherwise be stale), then restart the Phase 7 toolchain loop from P7-T1. **Acceptance:** the artifact records one row per audited file (ten rows), every count is at or below 500, it names the instrument used as `(Get-Content -LiteralPath <path>).Count`, and it states it was produced after the final Phase 7 formatting run (AC-25, post-format leg).

---

### Phase 8 — Acceptance-criteria check-off

Each check-off task below flips exactly one checkbox in `FF/spec.md` → `## Acceptance Criteria` from
`- [ ]` to `- [x]` and changes nothing else on that line: the criterion text is preserved
byte-for-byte apart from the marker, per `.claude/skills/acceptance-criteria-tracking/SKILL.md`
Check-Off Protocol rules 3 and 5. Evidence pointers are NOT appended to `FF/spec.md`. Instead, each
task records its evidence pointer in its own task output AND appends a row to
`FF/evidence/issue-updates/ac-status-summary.<TS>.md` (created by P8-T1). No task in this phase flips
more than one checkbox.

- [x] [P8-T1] Create `FF/evidence/issue-updates/ac-status-summary.<TS>.md` containing a `Timestamp:` field and an empty two-column Markdown table whose header columns are `AC ID` and `Evidence artifact path(s)`, so that later Phase 8 tasks append rows rather than create the file. **Acceptance:** the file exists, carries a `Timestamp:` line, and contains a table header row with the columns `AC ID` and `Evidence artifact path(s)` and zero data rows.
- [x] [P8-T2] Check off AC-01: flip `- [ ] AC-01` to `- [x] AC-01` in `FF/spec.md`, record the evidence pointer `FF/evidence/qa-gates/closepending-split.<TS>.md` in this task's output, and append a row for AC-01 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-01 line in `FF/spec.md` reads `- [x] AC-01` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-01 citing `FF/evidence/qa-gates/closepending-split.<TS>.md`.
- [x] [P8-T3] Check off AC-02: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-462.<TS>.md` in this task's output, and append a row for AC-02 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-02 line in `FF/spec.md` reads `- [x] AC-02` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-02 citing `FF/evidence/regression-testing/green-462.<TS>.md`.
- [x] [P8-T4] Check off AC-03: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-462-suite.<TS>.md` in this task's output, and append a row for AC-03 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-03 line in `FF/spec.md` reads `- [x] AC-03` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-03 citing `FF/evidence/regression-testing/green-462-suite.<TS>.md`.
- [x] [P8-T5] Check off AC-04: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-lifetime.<TS>.md` in this task's output, and append a row for AC-04 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-04 line in `FF/spec.md` reads `- [x] AC-04` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-04 citing `FF/evidence/regression-testing/green-500-lifetime.<TS>.md`.
- [x] [P8-T6] Check off AC-05: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` in this task's output, and append a row for AC-05 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-05 line in `FF/spec.md` reads `- [x] AC-05` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-05 citing `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md`.
- [x] [P8-T7] Check off AC-06: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-lifetime.<TS>.md` in this task's output, and append a row for AC-06 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-06 line in `FF/spec.md` reads `- [x] AC-06` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-06 citing `FF/evidence/regression-testing/green-500-lifetime.<TS>.md`.
- [x] [P8-T8] Check off AC-07: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` in this task's output, and append a row for AC-07 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-07 line in `FF/spec.md` reads `- [x] AC-07` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-07 citing `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md`.
- [x] [P8-T9] Check off AC-08: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` in this task's output, and append a row for AC-08 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-08 line in `FF/spec.md` reads `- [x] AC-08` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-08 citing `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md`.
- [x] [P8-T10] Check off AC-09: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` in this task's output, and append a row for AC-09 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-09 line in `FF/spec.md` reads `- [x] AC-09` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-09 citing `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md`.
- [x] [P8-T11] Check off AC-10: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` in this task's output, and append a row for AC-10 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-10 line in `FF/spec.md` reads `- [x] AC-10` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-10 citing `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md`.
- [x] [P8-T12] Check off AC-11: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/logging-verification-501.<TS>.md` (logging half) and `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` (non-propagation half) in this task's output, and append a row for AC-11 citing both paths and noting ruling PD-2 to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-11 line in `FF/spec.md` reads `- [x] AC-11` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-11 citing both artifact paths and noting PD-2.
- [x] [P8-T13] Check off AC-12: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-502.<TS>.md` in this task's output, and append a row for AC-12 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-12 line in `FF/spec.md` reads `- [x] AC-12` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-12 citing `FF/evidence/regression-testing/green-502.<TS>.md`.
- [x] [P8-T14] Check off AC-13: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-502.<TS>.md` in this task's output, and append a row for AC-13 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-13 line in `FF/spec.md` reads `- [x] AC-13` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-13 citing `FF/evidence/regression-testing/green-502.<TS>.md`.
- [x] [P8-T15] Check off AC-14: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/build-after-additems.<TS>.md` and `FF/evidence/regression-testing/green-502-suite.<TS>.md` in this task's output, and append a row for AC-14 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-14 line in `FF/spec.md` reads `- [x] AC-14` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-14 citing both artifact paths.
- [x] [P8-T16] Check off AC-15: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/regression-testing/green-502-lease-leak.<TS>.md` in this task's output, and append a row for AC-15 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-15 line in `FF/spec.md` reads `- [x] AC-15` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-15 citing `FF/evidence/regression-testing/green-502-lease-leak.<TS>.md`.
- [x] [P8-T17] Check off AC-16: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/regression-testing/red-462-reopen.<TS>.md` and `FF/evidence/regression-testing/green-462.<TS>.md` in this task's output, and append a row for AC-16 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-16 line in `FF/spec.md` reads `- [x] AC-16` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-16 citing both artifact paths.
- [x] [P8-T18] Check off AC-17: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/regression-testing/red-500-lifetime-lock.<TS>.md` and `FF/evidence/regression-testing/green-500-lifetime.<TS>.md` in this task's output, and append a row for AC-17 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-17 line in `FF/spec.md` reads `- [x] AC-17` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-17 citing both artifact paths.
- [x] [P8-T19] Check off AC-18: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/regression-testing/red-501-starvation.<TS>.md` and `FF/evidence/regression-testing/green-500-hub-and-501.<TS>.md` in this task's output, and append a row for AC-18 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-18 line in `FF/spec.md` reads `- [x] AC-18` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-18 citing both artifact paths.
- [x] [P8-T20] Check off AC-19: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/regression-testing/red-502-lease-leak.<TS>.md` and `FF/evidence/regression-testing/green-502-lease-leak.<TS>.md` in this task's output, and append a row for AC-19 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-19 line in `FF/spec.md` reads `- [x] AC-19` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-19 citing both artifact paths.
- [x] [P8-T21] Check off AC-20: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/unmodified-tests-audit.<TS>.md` and `FF/evidence/regression-testing/green-462-suite.<TS>.md` in this task's output, and append a row for AC-20 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-20 line in `FF/spec.md` reads `- [x] AC-20` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-20 citing both artifact paths.
- [x] [P8-T22] Check off AC-21: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/unmodified-tests-audit.<TS>.md` and `FF/evidence/regression-testing/green-462-suite.<TS>.md` in this task's output, and append a row for AC-21 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-21 line in `FF/spec.md` reads `- [x] AC-21` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-21 citing both artifact paths.
- [x] [P8-T23] Check off AC-22: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/unmodified-tests-audit.<TS>.md` and `FF/evidence/regression-testing/green-501-suite.<TS>.md` in this task's output, and append a row for AC-22 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-22 line in `FF/spec.md` reads `- [x] AC-22` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-22 citing both artifact paths.
- [x] [P8-T24] Check off AC-23: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/line-counts-after-split.<TS>.md` and `FF/evidence/qa-gates/project-file-budget.<TS>.md` in this task's output, and append a row for AC-23 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-23 line in `FF/spec.md` reads `- [x] AC-23` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-23 citing both artifact paths.
- [x] [P8-T25] Check off AC-24: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/new-test-file-budget.<TS>.md` and `FF/evidence/qa-gates/project-file-budget.<TS>.md` in this task's output, and append a row for AC-24 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-24 line in `FF/spec.md` reads `- [x] AC-24` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-24 citing both artifact paths.
- [x] [P8-T26] Check off AC-25: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/line-count-audit.<TS>.md` (P6-T1, pre-format leg) and `FF/evidence/qa-gates/line-count-audit-postformat.<TS>.md` (P7-T8, post-format leg, artifact of record for AC-25's "after the change" condition) in this task's output, and append a row for AC-25 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-25 line in `FF/spec.md` reads `- [x] AC-25` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-25 citing both `FF/evidence/qa-gates/line-count-audit.<TS>.md` and `FF/evidence/qa-gates/line-count-audit-postformat.<TS>.md`.
- [x] [P8-T27] Check off AC-26: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/ownership-precommit.<TS>.md` and `FF/evidence/qa-gates/scope-lock.<TS>.md` in this task's output, and append a row for AC-26 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-26 line in `FF/spec.md` reads `- [x] AC-26` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-26 citing both artifact paths.
- [x] [P8-T28] Check off AC-27: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/qa-gates/determinism-scan.<TS>.md` in this task's output, and append a row for AC-27 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-27 line in `FF/spec.md` reads `- [x] AC-27` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-27 citing `FF/evidence/qa-gates/determinism-scan.<TS>.md`.
- [x] [P8-T29] Check off AC-28: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/nfr-entry-time-verdict.<TS>.md` and `FF/evidence/regression-testing/green-500-lifetime.<TS>.md` in this task's output, and append a row for AC-28 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-28 line in `FF/spec.md` reads `- [x] AC-28` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-28 citing both artifact paths.
- [x] [P8-T30] Check off AC-29: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/final-csharpier-format.<TS>.md` and `FF/evidence/qa-gates/final-csharpier-check.<TS>.md` in this task's output, and append a row for AC-29 citing both paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-29 line in `FF/spec.md` reads `- [x] AC-29` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-29 citing both artifact paths.
- [x] [P8-T31] Check off AC-30: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/qa-gates/final-msbuild-analyzers.<TS>.md` in this task's output, and append a row for AC-30 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-30 line in `FF/spec.md` reads `- [x] AC-30` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-30 citing `FF/evidence/qa-gates/final-msbuild-analyzers.<TS>.md`.
- [x] [P8-T32] Check off AC-31: flip the marker in `FF/spec.md`, record the evidence pointer `FF/evidence/qa-gates/final-msbuild-nullable.<TS>.md` in this task's output, and append a row for AC-31 citing that path to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-31 line in `FF/spec.md` reads `- [x] AC-31` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-31 citing `FF/evidence/qa-gates/final-msbuild-nullable.<TS>.md`.
- [x] [P8-T33] Check off AC-32: flip the marker in `FF/spec.md`, record the evidence pointers `FF/evidence/qa-gates/final-test-coverage.<TS>.md`, `FF/evidence/qa-gates/coverage-delta.<TS>.md` and `FF/evidence/qa-gates/toolchain-pass-attestation.<TS>.md` in this task's output, and append a row for AC-32 citing all three paths to `FF/evidence/issue-updates/ac-status-summary.<TS>.md`. **Acceptance:** the AC-32 line in `FF/spec.md` reads `- [x] AC-32` with its criterion text byte-identical to the pre-edit text apart from the `[ ]`→`[x]` marker, and `FF/evidence/issue-updates/ac-status-summary.<TS>.md` carries a row for AC-32 citing all three artifact paths.
- [x] [P8-T34] Reconcile the whole acceptance-criteria block: count the `- [x]` entries under `FF/spec.md` → `## Acceptance Criteria`, cross-check them against the rows of `FF/evidence/issue-updates/ac-status-summary.<TS>.md`, run `git diff -- docs/features/active/breadcrumb-coordinator-hub-defects-501/spec.md`, and write `FF/evidence/qa-gates/ac-reconciliation.<TS>.md` listing all 32 identifiers with their state and the artifact(s) the summary table cites for each, plus the recorded spec diff. **Acceptance:** the artifact lists 32 rows, every row is `[x]`, every row names at least one artifact path that exists on disk, zero `- [ ]` entries remain in that spec section, and the recorded `git diff -- docs/features/active/breadcrumb-coordinator-hub-defects-501/spec.md` output shows only `[ ]`→`[x]` marker changes plus (when already applied) the plan's own P9-T1 metadata edit, with no other content change to the `## Acceptance Criteria` block.

---

### Phase 9 — Follow-ups, commit, and handoff

- [x] [P9-T1] Update `FF/spec.md` metadata: set `Status` to `Implemented` and `Last Updated` to the run timestamp, and append a short `## Implementation Notes` subsection recording rulings PD-1 and PD-2 and the SR-4 known limitation as shipped. **Acceptance:** the `Status` line reads `Implemented` and the file contains an `## Implementation Notes` heading.
- [x] [P9-T2] File the follow-up issue the spec's `## Rollout & Follow-up` section requires for research §6.2 option C (a non-re-entrant upgrade-lifetime guard), explicitly out of scope here, using `gh issue create` with a title and body that cite this feature folder and research §6.2 option C; mirror the posted text to `FF/evidence/issue-updates/followup-nonreentrant-guard.<TS>.md` with `Timestamp:`, the exact text, `PostedAs: body`, and the issue URL. **Acceptance:** the mirror artifact exists, carries a `https://github.com/drmoisan/TaskMaster/issues/` URL, and records `PostedAs: body`.
- [x] [P9-T3] File the follow-up issue recording the SR-4 known limitation (`_closeCompleted` stays `true` when the host is reopened by a path that reaches neither `RequestOpen` nor `Invalidate`) against the host paths owned by sibling feature 488, using `gh issue create`; mirror the posted text to `FF/evidence/issue-updates/followup-sr4-residual.<TS>.md` with the same fields. **Acceptance:** the mirror artifact exists, carries a `https://github.com/drmoisan/TaskMaster/issues/` URL, and records `PostedAs: body`.
- [ ] [P9-T4] Commit every production change, test change, project-file line and evidence artifact with explicit pathspecs covering `QuickFiler/`, `QuickFiler.Test/` and `docs/features/active/breadcrumb-coordinator-hub-defects-501/`, using a message that references `#501`, `#462`, `#500` and `#502`. **Acceptance:** `git status --porcelain -- QuickFiler/ QuickFiler.Test/ docs/features/active/breadcrumb-coordinator-hub-defects-501/` produces zero output lines, and `git rev-parse HEAD` reports a sha different from `BASELINE_SHA`. The pathspec on the status check is mandatory: `.claude/agent-memory/**` is tracked in this worktree and is written during execution, so an unscoped `git status --porcelain` would report files this plan never authorizes committing.
- [ ] [P9-T5] Run the post-commit scope-lock gate: `git diff --name-only BASELINE_SHA..HEAD` and `git log --format=%s BASELINE_SHA..HEAD`, writing both to `FF/evidence/qa-gates/scope-lock.<TS>.md`. **Acceptance:** the artifact records that the name-only list contains none of `QuickFiler/Viewers/WebView2Messenger.cs`, `QuickFiler/Viewers/WebView2BreadcrumbHost.cs`, `QuickFiler/Viewers/BreadcrumbItemViewerLifecycleCoordinator.cs`, `QuickFiler/Viewers/BreadcrumbPopupUiOperations.cs`, `QuickFiler/Viewers/BreadcrumbDropDownHost.cs`, `QuickFiler/Viewers/ItemViewer.Breadcrumb.cs`; that every entry ending `.cs` or `.csproj` appears on this feature's owned list (the five owned production files, the five test files, and the two project files); and that at least one commit subject references `#501` (AC-26).
- [ ] [P9-T6] Write the review-handoff index `FF/evidence/other/handoff-index.<TS>.md` listing every evidence artifact this plan produced with its path, kind, and the acceptance criteria it supports, plus the post-commit HEAD sha from P9-T4, then commit it with an explicit pathspec under `docs/features/active/breadcrumb-coordinator-hub-defects-501/`. **Acceptance:** the artifact exists, lists at least 40 artifact paths that each resolve to an existing file, and `git status --porcelain -- QuickFiler/ QuickFiler.Test/ docs/features/active/breadcrumb-coordinator-hub-defects-501/` again produces zero output lines (same mandatory pathspec as P9-T4, for the same tracked-agent-memory reason).
