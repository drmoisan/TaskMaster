# triage-multiselect-only-first (Plan)

- **Issue:** #183
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-06-10T09-13
- **Status:** Draft
- **Version:** 0.2
- **Work Mode:** minor-audit
- **Branch:** bug/triage-multiselect-only-first-183

**Requirements source (sole):** `docs/features/active/2026-06-10-triage-multiselect-only-first-183/issue.md`, section `## Acceptance Criteria` (AC1–AC5). No `spec.md` / `user-story.md` / `research.md` is required or consulted for this minor-audit plan. If `spec.md` or `user-story.md` is unexpectedly present in this folder, execution/audit MUST fail closed.

**Fail-closed evidence rule:** This plan includes explicit baseline artifact tasks, final-QC artifact tasks, and a coverage-comparison task for the C# language scope. If any required baseline artifact, final-QC artifact, or coverage-comparison artifact is missing or has incomplete fields, the audit verdict MUST be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Each evidence-producing task records its canonical artifact path. Do not mark an evidence-backed task complete without the artifact present on disk with required fields (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`).

**Canonical evidence root (non-overridable):** `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/`. Any non-canonical evidence location (for example `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`) is a policy violation and is rejected.

**Production-file budget (small path):** exactly 1 production file (`UtilitiesCS\EmailIntelligence\ClassifierGroups\Triage\Triage_OlLogic.cs`) and 1 test file (`UtilitiesCS.Test\EmailIntelligence\ClassifierGroups\Triage\Triage_OlLogicTests.cs`). No changes to `RibbonViewer.cs`, `Triage.cs`, or any other production file.

**C# toolchain order (from `.claude/rules/csharp.md`, run in this exact order):**
1. `dotnet tool run csharpier .`
2. `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`
3. `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`
4. `vstest.console.exe <test-assembly-paths> /EnableCodeCoverage`

---

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read policy files in required order and record a Phase 0 read-evidence artifact at `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/baseline/phase0-instructions-read.md`. Required fields: `Timestamp:`, `Policy Order:`, and an explicit list of files read. Files to read in order: `CLAUDE.md`; `.claude/rules/general-code-change.md`; `.claude/rules/general-unit-test.md`; `.claude/rules/csharp.md`; `docs/features/active/2026-06-10-triage-multiselect-only-first-183/issue.md`.
- [x] [P0-T2] Record branch/commit baseline (output of `git rev-parse --abbrev-ref HEAD` and `git rev-parse HEAD`) into `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/baseline/branch-commit.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (branch name and commit SHA).
- [x] [P0-T3] Run baseline formatter check `dotnet tool run csharpier .` and write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/baseline/csharpier.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (files changed count or "no changes").
- [x] [P0-T4] Run baseline analyzer build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true` and write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/baseline/analyzer-build.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (build pass/fail, warning/error counts).
- [x] [P0-T5] Run baseline nullable/type-check build `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true` and write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/baseline/nullable-build.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (build pass/fail).
- [x] [P0-T6] Run baseline tests with coverage `vstest.console.exe <UtilitiesCS.Test assembly path> /EnableCodeCoverage` and write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/baseline/tests-coverage.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric coverage headline values: repository-wide line coverage percent and the baseline line coverage of `Triage_OlLogic.cs` (specifically `TrainSelectionAsync`). Confirm the four existing same-conversation/training tests in `Triage_OlLogicTests.cs` currently pass.

---

### Phase 1 — Constrained Small-Path Implementation

This phase is the constrained small-path development block for issue #183. It contains a regression-test-first step (expect-fail), the minimal decoupling fix, and the pass-after confirmation. No production file other than `Triage_OlLogic.cs` may be modified. Maps to AC1–AC4.

- [x] [P1-T1] [expect-fail] Add a deterministic MSTest regression test `TrainSelectionAsync_WhenSelectionContainsTwoMailItemsWithSameConversationId_WritesTriageUdfToEveryItem` to `UtilitiesCS.Test\EmailIntelligence\ClassifierGroups\Triage\Triage_OlLogicTests.cs`. Follow the existing same-conversation test setup pattern (two `Mock<MailItem>(MockBehavior.Loose)` items; each with a loose `Attachments` mock whose `IEnumerable.GetEnumerator` returns an empty sequence; full `globals -> Ol -> App -> ActiveExplorer -> Selection` chain; `mockOlObjects.Setup(o => o.EmailPrefixToStrip).Returns("")`). Both mocks leave `ConversationID` unstubbed so they return the same default value and group together under `.GroupBy(m => m.ConversationID)`. Observation seam (required, because `SetUdf` is an extension method and CANNOT be verified directly via Moq): the per-item write is `helper.Item.SetUdf("Triage", "A")`, which binds to the `MailItem` overload in `UtilitiesCS\OutlookObjects\Fields\UserDefinedFields.cs` and, on a `MailItem` mock, exercises only `MailItem.UserProperties` (`.Find`/`.Add`) and `MailItem.Save()`. For EACH of the two `MailItem` mocks, stub the `UserProperties` chain so the write path completes and reaches `Save()`: set up `mockMailItemN.Setup(m => m.UserProperties)` to return a `Mock<UserProperties>(MockBehavior.Loose)` whose `Find(It.IsAny<string>(), It.IsAny<object>())` returns a `Mock<UserProperty>(MockBehavior.Loose).Object` (so the null-branch and the assignable `property.Value = value` both succeed), and set up `mockMailItemN.Setup(m => m.Save())`. Assert the per-item UDF write reached BOTH items by verifying the interceptable `MailItem.Save()` member on each mock: `mockMailItem1.Verify(m => m.Save(), Times.Once)` and `mockMailItem2.Verify(m => m.Save(), Times.Once)`. Add a code comment stating that `Save()` is the chosen observable proxy for the swallowed `SetUdf` extension write. Also assert `_triage.ClassifierGroup.TotalEmailCount.Should().Be(emailCountBefore + 1)` to confirm training still dedups by `ConversationID`. Maps to AC1, AC2, AC3. (If, on inspection of the actual `MailItem`/`UserProperties`/`UserProperty` interop signatures, `Save()` cannot be set up as a verifiable member, substitute the nearest interceptable COM-interface member actually invoked by the `MailItem` `SetUdf` overload — for example `UserProperties.Add` verified `Times.Once` per item — and document the chosen seam in the test comment; the assertion MUST distinguish per-item writes for BOTH items, and MUST NOT attempt to `Verify` the `SetUdf` extension method itself.)
- [x] [P1-T2] [expect-fail] Run only the new regression test via `vstest.console.exe <UtilitiesCS.Test assembly path> /Tests:TrainSelectionAsync_WhenSelectionContainsTwoMailItemsWithSameConversationId_WritesTriageUdfToEveryItem` and confirm it FAILS against the unmodified `Triage_OlLogic.cs` (the second item's UDF write is suppressed by the dedup). Write the failing-run artifact to `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/regression-testing/fail-before.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:` (non-zero), `Output Summary:` (assertion failure message confirming the second item was not triaged). Maps to AC3. The expected fail-before signal is that the second item's observable write member (`mockMailItem2.Verify(m => m.Save(), Times.Once)`, or the substituted interceptable member from P1-T1) is never invoked because the pre-fix dedup drops the second item; record that assertion failure message in the artifact.
- [x] [P1-T3] Apply the minimal decoupling fix in `UtilitiesCS\EmailIntelligence\ClassifierGroups\Triage\Triage_OlLogic.cs`, method `TrainSelectionAsync` only. Iterate the full (non-deduplicated) `MailItem` sequence and call `Parent.TestActionAsync(helper, triageId, token)` (the per-item `Triage` UDF write) for EVERY selected item, while gating `Parent.TrainAsync(...)` so it runs at most once per distinct `ConversationID` (preserve issue #137 training-dedup). Keep the change confined to this method; do not alter `FilterView`, `UnTrainSelectionAsync`, `RibbonViewer.cs`, or `Triage.cs`. Preserve the trailing `Parent.ClassifierGroup.Serialize();` call. Maps to AC1, AC2, AC4.
- [x] [P1-T4] Run the full new and existing `Triage_OlLogicTests` set via `vstest.console.exe <UtilitiesCS.Test assembly path>` and confirm: the new regression test now PASSES, and all four pre-existing tests still pass unchanged — `TrainSelectionAsync_WhenSelectionContainsTwoMailItemsWithSameConversationId_TrainsOnlyOneItem_TotalEmailCountIncrementsOnce`, `..._MatchEmailCountIncrementsOnce`, `TrainSelectionAsync_WhenSelectionContainsMailItem_TrainsClassifierWithExpectedLabel`, `TrainSelectionAsync_WhenSelectionIsNull_SkipsWithoutThrowingOrTraining`. Write the pass-after artifact to `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/regression-testing/pass-after.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:` (zero), `Output Summary:` (passed test count, explicit confirmation the four prior tests passed). Maps to AC2, AC3.

---

### Phase 2 — Final QC Loop

Run the full C# toolchain in order. If any step changes files or fails, fix and restart from P2-T1. All four command tasks are unconditional; `SKIPPED` is not a valid passing outcome.

- [x] [P2-T1] Run `dotnet tool run csharpier .`. Write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/qa-gates/csharpier.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (must report no files changed in the clean pass). If files changed, restart from P2-T1.
- [x] [P2-T2] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`. Write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/qa-gates/analyzer-build.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (build pass, zero new analyzer errors). On failure, fix and restart from P2-T1.
- [x] [P2-T3] Run `msbuild TaskMaster.sln /t:Build /p:Configuration=Debug /p:Platform="Any CPU" /p:Nullable=enable /p:TreatWarningsAsErrors=true`. Write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/qa-gates/nullable-build.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (build pass, zero nullable/warning-as-error failures). On failure, fix and restart from P2-T1.
- [x] [P2-T4] Run `vstest.console.exe <UtilitiesCS.Test assembly path> /EnableCodeCoverage`. Write artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/qa-gates/tests-coverage.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change coverage: repository-wide line coverage percent and the post-change line coverage of `Triage_OlLogic.cs` / `TrainSelectionAsync`. All tests must pass. Maps to AC5. On failure, fix and restart from P2-T1.
- [x] [P2-T5] Write a coverage-comparison/threshold artifact `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/qa-gates/coverage-comparison.2026-06-10T09-13.md` with fields `Timestamp:`, `Command:` (reference to P0-T6 and P2-T4 artifacts), `EXIT_CODE:`, `Output Summary:` reporting: baseline coverage (from P0-T6), post-change coverage (from P2-T4), and changed/new-method coverage for `TrainSelectionAsync`. Verify repository-wide line coverage remains >= 80%, the changed method targets >= 90%, and changed-line coverage does not regress versus baseline. If any threshold fails, the outcome is remediation-required (not PASS). Maps to AC5.
- [x] [P2-T6] Update `docs/features/active/2026-06-10-triage-multiselect-only-first-183/issue.md` `## Acceptance Criteria` checkboxes (AC1–AC5) to reflect verified outcomes, and mirror the update per issue-update conventions to `docs/features/active/2026-06-10-triage-multiselect-only-first-183/evidence/issue-updates/issue-183.2026-06-10T09-13.md` with `Timestamp:`, the exact text, and `PostedAs:`. Confirm `git status` is clean (all evidence committed).
