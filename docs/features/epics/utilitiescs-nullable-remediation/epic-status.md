# Epic Status: utilitiescs-nullable-remediation

> Generated projection of `artifacts/orchestration/epic-orchestrator-state.json`. Do not hand-edit;
> regenerated by epic-orchestrator at kickoff, each `merge_status` transition, each wave transition,
> and final integration-PR completion.

> ## ▶ ALL 13 CHILDREN MERGED — CAPSTONE #376 MERGED — FINAL integration→main PR STAGE
>
> **Wave-2 capstone #376 (utilitiescs-nullable-ci-capstone) durably MERGED 2026-07-20T05:51:50Z** as
> PR [#389](https://github.com/drmoisan/TaskMaster/pull/389), merge commit
> `9dd6ee7b5b5ed00a6e7eaf3df809a8beaf4b721a` (docs follow-up `f451ee1e`). Verified via `gh pr view 389`
> state MERGED + `git merge-base --is-ancestor` against the integration branch. It revised the CI
> nullable gate to per-file `#nullable` pragma enforcement under `/t:Rebuild /p:TreatWarningsAsErrors=true`
> (dropping the global `/p:Nullable=enable`), remediated **both** integrated-tree build debts (SVGControl
> CS0649 + the cross-child CS86xx fan-in, resolved as 296 CS86xx + 28 CS0618 + 2 CS0168 across 62
> UtilitiesCS files plus escalation layers in ToDoModel/TaskVisualization/QuickFiler/TaskMaster/test
> projects, all behavior-neutral), reached solution-wide gate EXIT 0, 5702/5702 tests, coverage
> 83.89/76.37 (no regression), passed a genuine-enforcement probe (opted-in defect fails, non-opted-in
> does not; both reverted), and recorded a green `workflow_dispatch` run `29719565487` on the real
> `windows-latest` runner (satisfying `modified-workflow-needs-green-run`). `blocking_count == 0`;
> AC1–AC7 checked.
>
> **All 13 children (6 wave-0 + 6 wave-1 + 1 wave-2 capstone) are now MERGED.** Wave 0, wave 1, and
> wave 2 are all COMPLETE.
>
> **PR #361 supersession + main sync.** The integration branch was based on `20d163ac` (PR #361's head,
> the original CI-gate-repair commit); the capstone further revised `ci.yml`, so the integration→main PR
> **supersedes PR #361**. `origin/main` advanced to `bd435724` (PR #362, worktree-memory salvage) during
> the epic; a **sync merge of `origin/main` into the integration branch** was performed this stage
> (agent-memory index union conflicts resolved), producing integration tip `a242acdb`, which now merges
> cleanly into `main` (`git merge-tree` exit 0). PR #361 will be closed with a comment referencing the
> epic integration PR rather than merged separately.
>
> **Final stage in progress.** epic-orchestrator drives the final integration→`main` PR:
> `collect_pr_context` (with refresh verification per the carried #374 MCP anomaly), pr-author body +
> SHA-256 receipt (in-thread, since `pr-author` is not a registered agent type this session), the S9
> CI-green procedure recorded under `epic_merge_pr`, then `gh pr merge --merge` once green. This PR runs
> REAL CI on `main` (including the now-genuine nullable gate).

- Integration branch: `epic/utilitiescs-nullable-remediation-integration`
- Integration base: `20d163ac` (PR #361 head; original CI-gate-repair commit — superseded by this epic PR)
- Integration tip: `a242acdb` (main-sync merge over `9dd6ee7b`/`f451ee1e`, the #376 capstone merge)
- origin/main tip at final-PR stage: `bd435724` (PR #362), now merged into the integration branch
- model_budget.fable_policy: `disabled`
- Current wave: 2 (wave 0, wave 1, and wave 2 all COMPLETE; all 13 children merged).
- Wave-barrier policy: `edge-satisfaction`. All dependency edges durably merged.
- Concurrency: **0 children running.** All 13 children merged.
- **Only remaining work**: the final integration→`main` PR (open, real CI, merge on green) and closing
  the superseded PR #361.
- Last updated: 2026-07-20T06:15:00Z

## Waves

| Wave | Status | Features |
| --- | --- | --- |
| 0 | complete | all six merged: 363 (#379), 364 (#382), 366 (#380), 367 (#378), 368 (#377), 369 (#381) |
| 1 | complete | all six merged: 365 (#388), 370 (#384), 371 (#387), 372 (#386), 374 (#385), 375 (#383) |
| 2 | complete | 376 MERGED (#389, 9dd6ee7b; docs follow-up f451ee1e) |

## Features

| issue | feature_folder | wave | complexity/model | merge_status | PR | merge_commit | worktree_created | pr_opened | merge_confirmed | worktree_removed |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 363 | utilitiescs-nullable-extensions | 0 | C3/opus | worktree_removed | [#379](https://github.com/drmoisan/TaskMaster/pull/379) | df2235bc | 2026-07-19T12:42:24Z | — | 2026-07-19T14:22:31Z | 2026-07-19T14:22:31Z |
| 364 | utilitiescs-nullable-helperclasses | 0 | C3/opus | worktree_removed | [#382](https://github.com/drmoisan/TaskMaster/pull/382) | dffadd5a | 2026-07-19T12:42:24Z | 2026-07-19T14:33:53Z | 2026-07-19T14:44:00Z | 2026-07-19T14:44:00Z |
| 366 | utilitiescs-nullable-reusabletypes | 0 | C3/opus | worktree_removed (MERGED; four-file cross-child waiver FINAL; [P9-T3] solution-wide-zero deviation documented, carried to #376) | [#380](https://github.com/drmoisan/TaskMaster/pull/380) | bfcdb394 | 2026-07-19T12:42:24Z | 2026-07-19T14:20:34Z | 2026-07-20T03:00:00Z | 2026-07-20T03:00:00Z |
| 367 | utilitiescs-nullable-newtonsofthelpers | 0 | C3/opus | merged | [#378](https://github.com/drmoisan/TaskMaster/pull/378) | 52c1d7cf | 2026-07-19T12:42:24Z | 2026-07-19T14:03:52Z | 2026-07-19T14:12:00Z | deferred (worktree framework-locked) |
| 368 | utilitiescs-nullable-svgcontrol | 0 | C2/sonnet | merged | [#377](https://github.com/drmoisan/TaskMaster/pull/377) | 8b95d4a1 | 2026-07-19T12:42:24Z | 2026-07-19T13:57:48Z | 2026-07-19T14:02:08Z | deferred (worktree framework-locked) |
| 369 | utilitiescs-nullable-threading | 0 | C3/opus | merged | [#381](https://github.com/drmoisan/TaskMaster/pull/381) | e19cd910 | 2026-07-19T12:42:24Z | 2026-07-19T14:26:47Z | 2026-07-19T14:36:00Z | deferred (unlocked, but uncommitted files) |
| 365 | utilitiescs-nullable-outlook-folder-store | 1 | C2/sonnet | worktree_removed | [#388](https://github.com/drmoisan/TaskMaster/pull/388) | 3dfe3390 | 2026-07-19T14:44:00Z | — | 2026-07-19T17:07:45Z | 2026-07-19T17:20:00Z (named wt; secondary agent wt still locked) |
| 370 | utilitiescs-nullable-email-parsing | 1 | C2/sonnet | merged | [#384](https://github.com/drmoisan/TaskMaster/pull/384) | 1c7e2dc3 | 2026-07-19T14:22:31Z | — | 2026-07-19T16:40:00Z | deferred (worktree framework-locked) |
| 371 | utilitiescs-nullable-outlook-mailitem-item | 1 | C2/sonnet | merged | [#387](https://github.com/drmoisan/TaskMaster/pull/387) | f6425131 | 2026-07-19T14:44:00Z | — | 2026-07-19T17:05:00Z | deferred (worktree framework-locked) |
| 372 | utilitiescs-nullable-email-classifier | 1 | C3/opus | worktree_removed | [#386](https://github.com/drmoisan/TaskMaster/pull/386) | 0b000511 | 2026-07-19T14:22:31Z | — | 2026-07-19T17:08:21Z | 2026-07-19T17:20:00Z |
| 374 | utilitiescs-nullable-dialogs-misc | 1 | C2/sonnet | merged | [#385](https://github.com/drmoisan/TaskMaster/pull/385) | aa154796 | 2026-07-19T14:44:00Z | — | 2026-07-19T16:48:00Z | deferred (worktree framework-locked) |
| 375 | utilitiescs-nullable-residuals | 1 | C3/opus | worktree_removed | [#383](https://github.com/drmoisan/TaskMaster/pull/383) | 27cf64fe | 2026-07-19T14:44:00Z | — | 2026-07-19T16:35:00Z | 2026-07-19T16:35:00Z |
| 376 | utilitiescs-nullable-ci-capstone | 2 | C2/sonnet | merged | [#389](https://github.com/drmoisan/TaskMaster/pull/389) | 9dd6ee7b | 2026-07-20T03:00:00Z | 2026-07-20T05:00:00Z | 2026-07-20T06:15:00Z | deferred (worktree framework-locked pid 34848) |

## Capstone #376 carried inputs (execution-time gates)

The capstone #376 (utilitiescs-nullable-ci-capstone), launched 2026-07-20T03:00:00Z, must clear the
following before the revised CI gate can pass on `main`. All seven were enumerated in its delegation.

- **BUILD DEBT 1 — SVGControl CS0649**: a full-solution `TaskMaster.sln`
  `/t:Rebuild /p:TreatWarningsAsErrors=true` build exits 1 on the pre-existing SVGControl CS0649
  (`SvgImageSelector.cs`) EVEN at a base including #368's merge (#368 opted the file into `#nullable`
  but did not eliminate the unassigned-field warning). The capstone must fix, scope, or otherwise
  account for it. Empirical finding from #375 (falsifies the earlier "resolved by PR #377" reading).
- **BUILD DEBT 2 — cross-child nullable fan-in debt (~148 CS86xx, high priority)**: cross-child
  annotation propagation on the integrated tree emits NEW CS86xx in already-opted-in files owned by
  other children (EmailIntelligence / NewtonsoftHelpers etc.). Measured at ~148 sibling-owned CS86xx
  at #366 completion — this **supersedes** the earlier 76-count estimate (from the #372 completion
  report) and the interim ~140 estimate (from #366's `[P9-T3]` operative-gate ruling). No scope-locked
  child could fix these; the capstone MUST run a full integrated-tree pragma gate
  (`/t:Rebuild /p:TreatWarningsAsErrors=true`) and remediate to zero.
- **Revise/supersede PR #361 CI gate**: drop the global `/p:Nullable=enable` and rely on per-file
  `#nullable enable` pragmas under `/t:Rebuild /p:TreatWarningsAsErrors=true`, genuinely enforcing the
  gate (verify with an expect-fail probe per the plan). `ci.yml` is the ONLY workflow file the capstone
  may touch; deliberately-failing nested `pwsh` steps must follow `.claude/rules/ci-workflows.md`.
- **`.claude/rules/csharp.md` conflict**: flagged for the maintainer; no rules edits.
- **Optional project-level `<Nullable>` flip**: out of default scope.
- **Consolidated maintainer inventory**: `PeopleScoDictionaryNewBackup.cs` (dead duplicate),
  `MSDemoConv.cs`, `To Depricate/*`, `MailResolution_ToRemove`, and the >500-line flagged files
  (`PrettyPrint.cs`, `FilePathHelper.cs`, `TimeOutTask.cs`, plus residuals-scope oversize files) —
  recorded as a maintainer-facing flag.
- **PeopleScoDictionaryNew.cs `#nullable disable` island**: now #366's dictionary bases carry the
  `where TKey : notnull` constraint, evaluate removing #375's scoped island as part of BUILD DEBT 2
  remediation.

## MCP anomaly carried for the final integration→main PR (from the #374 child)

`collect_pr_context` reported success WITHOUT refreshing the `pr_context` file, so the #374 child's
pr-author fell back to a hand-authored summary. When epic-orchestrator refreshes context via
`collect_pr_context` for the final integration→main PR, verify the `pr_context` artifact was actually
refreshed rather than trusting the success return.

## Wave-0 completions and review dispositions (for final epic audit)

- **#363 utilitiescs-nullable-extensions — MERGED** via PR
  [#379](https://github.com/drmoisan/TaskMaster/pull/379), merge commit
  `df2235bc1716ddf18891ff01f3f283f6da6168b9`, merged 2026-07-19T14:17:52Z. 23 files opted in via
  per-file `#nullable enable` + 2 verify-only; zero CS86xx; AC1–AC5 checked in all three AC sources;
  5702/5702 tests; no coverage regression. Worktree removed (unlocked, non-force, exit 0).
- **#364 utilitiescs-nullable-helperclasses — MERGED** via PR
  [#382](https://github.com/drmoisan/TaskMaster/pull/382), merge commit
  `dffadd5a102884dd811ed5731477de18417594f1`, merged 2026-07-19T14:36:39Z. 42 of 43 files opted in
  (`DvgForm.Designer.cs` left oblivious per the Designer rule); zero CS86xx (verified twice);
  4511/4511 tests; coverage flat. `PrettyPrint.cs`/`FilePathHelper.cs` >500-line breaches flagged, not
  fixed. Worktree removed (exit 0).
- **#366 utilitiescs-nullable-reusabletypes — MERGED** via PR
  [#380](https://github.com/drmoisan/TaskMaster/pull/380), merge commit
  `bfcdb394b19a12ca2345bf740a4efffe0ad6e330` (the CURRENT integration tip), merged
  2026-07-20T02:38:54Z, merged head `120365b7`. Durable verification: `gh pr view 380` state MERGED,
  `git merge-base --is-ancestor bfcdb394 origin/epic/...-integration` == YES; bfcdb394 is the tip.
  - Maintainer RATIFIED the `[P6-T2]` `where TKey : notnull` contract change in-session
    2026-07-19T22:14:30Z. The constraint applies to the three truly generic bases
    (`ConcurrentObservableDictionary`, `ScoDictionaryNew`, `ScDictionary`); `ScoDictionaryStatic` is a
    non-generic static class and is untouched; `ScBag` is unconstrained.
  - **Cross-child cascade resolved across four escalations (Option A → A''), cascade proven CLOSED at
    four NewtonsoftHelpers consumers** by assembly-wide enumeration: `WrapperScoDictionary.cs`,
    `ScoDictionaryConverter.cs`, `WrapperScDictionary.cs`, `ScDictionaryConverter.cs` (one
    `where TKey : notnull` line each). `WrapperPeopleScoDictionaryNew.cs` and `PeopleScoConverter.cs`
    verified SAFE; no fifth consumer. Options B (withdraw) and C (defer) rejected in every escalation.
    (Recorded under `features[#366].cross_child_conflict_p6t3*`.)
  - Isolated-cluster operative gate: 0 CS86xx / 0 CS8714 incl. the four waiver lines; 5702/5702 tests;
    coverage 83.88 line / 76.35 branch, no changed-line regression; AC1–AC6 checked; feature-review
    `blocking_count == 0`.
  - `[P9-T3]` solution-wide-zero deviation (~148 sibling-owned cross-child CS86xx) documented per the
    operative-gate ruling, not treated as a #366 failure; carried to capstone #376 (BUILD DEBT 2).
  - Operational notes for audit: MCP plan validator em-dash defect worked around via executor textual
    preflight; session-root checkpoint backup-swap-restore used around `gh pr edit`/`merge` with step9
    pending→passed phasing.
  - Worktree removal COMPLETED this resume: `agent-aac70fb1e66a2e16a` was UNLOCKED (framework lock
    released after the final resume agent `aa9338b8` tore down) and clean; non-force
    `git worktree remove` succeeded (exit 0). Gated removal permitted because the #366 record was set
    to `merged` first.
- **#368 utilitiescs-nullable-svgcontrol — MERGED** via PR
  [#377](https://github.com/drmoisan/TaskMaster/pull/377), merge commit `8b95d4a17b95e60bc6...`. All 47
  tasks / 6 phases; 6/6 ACs; zero solution-wide CS86xx under the per-file pragma gate. #368 opted
  `SvgImageSelector.cs` into `#nullable` but did NOT eliminate the pre-existing CS0649 (see BUILD DEBT
  1). Worktree removal deferred: `agent-a56dcba40416f18d6` framework-locked (pid 34848; retried this
  resume, still locked).
- **#367 utilitiescs-nullable-newtonsofthelpers — MERGED** via PR
  [#378](https://github.com/drmoisan/TaskMaster/pull/378), merge commit
  `52c1d7cff2e9942a39cc07a748c845bde95a8ad2`, mergedAt 2026-07-19T14:04:42Z. All 19 files opted in;
  zero CS86xx; 4511 tests; coverage 93.71%→93.81%; 16 ACs checked. Note: #367's `WrapperScoDictionary`,
  `ScoDictionaryConverter`, `WrapperScDictionary`, `ScDictionaryConverter` are the four consumers that
  #366's ratified constraint propagated into under the FINAL cross-child waiver. Worktree removal
  deferred: `agent-a8743eb3002f83770` framework-locked (pid 34848; retried this resume, still locked).
- **#369 utilitiescs-nullable-threading — MERGED** via PR
  [#381](https://github.com/drmoisan/TaskMaster/pull/381), merge commit
  `e19cd9101a2fc713256fc16f1839026b2552bbff`, merged 2026-07-19T14:28:37Z. 16/16 ACs; all 25 files
  opted in; zero CS86xx; coverage 81.85%→81.93%. `TimeOutTask.cs` 976-line breach flag-only.
  Worktree removal deferred: `agent-a01d6eefe1f9bff5a` is UNLOCKED but a non-force
  `git worktree remove` refuses (exit 128) on uncommitted content (modified/untracked
  `.claude/agent-memory` + untracked coverage cobertura XMLs); retried this resume, still refused.

## Wave-1 completions and review dispositions (for final epic audit)

- **#375 utilitiescs-nullable-residuals — MERGED** via PR
  [#383](https://github.com/drmoisan/TaskMaster/pull/383), merge commit
  `27cf64fe2b126431d8d6aa2ac6e57b678f230471`, merged 2026-07-19T16:22:56Z. 37 files opted in; isolated
  pragma gate EXIT 0, zero CS86xx; AC1–AC8; 4511/4511 tests. Raised capstone inputs: CS0649 status
  correction, PeopleScoDictionaryNew.cs `#nullable disable` island removal candidate, three >500-line
  files flagged. Worktree removed (exit 0).
- **#370 utilitiescs-nullable-email-parsing — MERGED** via PR
  [#384](https://github.com/drmoisan/TaskMaster/pull/384), merge commit
  `1c7e2dc3d8b5488b5a3091f71bc37d4f99a90b9e`, merged 2026-07-19T16:27:22Z. 24 files opted in; zero
  CS86xx; 6/6 ACs; 5702/5702 tests. Worktree removal deferred: `agent-a3211bcc5c56f78c6`
  framework-locked (pid 34848; retried this resume, still locked).
- **#374 utilitiescs-nullable-dialogs-misc — MERGED** via PR
  [#385](https://github.com/drmoisan/TaskMaster/pull/385), merge commit
  `aa154796aaa9d8a339ab54787fbeb8745820670c`, merged 2026-07-19T16:38:01Z. 12 opted in + 2 verify-only;
  zero CS86xx; AC1–AC6; 5702/5702 tests; 93.10% changed-line coverage. Carried the `collect_pr_context`
  MCP anomaly (see above). Worktree removal deferred: `agent-a7e55c484f5d14b45` framework-locked (pid
  34848; retried this resume, still locked).
- **#371 utilitiescs-nullable-outlook-mailitem-item — MERGED** via PR
  [#387](https://github.com/drmoisan/TaskMaster/pull/387), merge commit
  `f6425131f839a513ea563a780892093979c534fb`, merged 2026-07-19T17:01:30Z. 30 files opted in; zero
  CS86xx; 17+6 ACs; 4511/4511 tests. Head branch prefix `bug/` (harmless). Worktree removal deferred:
  `agent-ae0ac7a610e9df2cd` framework-locked (pid 34848; retried this resume, still locked).
- **#365 utilitiescs-nullable-outlook-folder-store — MERGED** via PR
  [#388](https://github.com/drmoisan/TaskMaster/pull/388), merge commit
  `3dfe3390a892c709fe0d7e5ff1de9eed3f65128b`, merged 2026-07-19T17:07:45Z. 63 remediated + 18
  verify-only + 2 Designer-excluded; zero nullable diagnostics under the final (widened CS8xxx) gate; a
  review-driven CS8766 fixed (commit `95b289c5`); AC1–AC7; 4511/4511 tests; changed-line coverage
  96.97%. NAMED feature worktree removed (exit 0); secondary agent worktree `agent-ac400c3748b22c1a2`
  framework-locked (pid 34848; retried this resume, still locked).
- **#372 utilitiescs-nullable-email-classifier — MERGED** via PR
  [#386](https://github.com/drmoisan/TaskMaster/pull/386), merge commit
  `0b000511ff30528f2a51fda55faee1ef84280596`, merged 2026-07-19T17:08:21Z. 36 files opted in; 40 tasks
  / 10 commits `d7ac0cc6..76bc0f7f`; AC1–AC5; annotation-only. Raised the cross-child fan-in finding
  (originally 76 CS86xx; now measured ~148 at #366 completion — carried to capstone #376 as BUILD DEBT
  2). Worktree removed (exit 0).

## Worktree-cleanup residuals (optional; each child's merge_status already satisfies completion)

Retried the non-force gated `git worktree remove` for every prior deferral this resume (all still deferred):

- **Still framework-locked (pid 34848 — this session's own pid, releasing only at session teardown)**:
  #376 `agent-a990f1a3b96eb6fae` (capstone), #367 `agent-a8743eb3002f83770`, #368
  `agent-a56dcba40416f18d6`, #370 `agent-a3211bcc5c56f78c6`, #371 `agent-ae0ac7a610e9df2cd`, #374
  `agent-a7e55c484f5d14b45`, and the #365-secondary `agent-ac400c3748b22c1a2`.
- **Unlocked but uncommitted files**: #369 `agent-a01d6eefe1f9bff5a`.

Each listed child's `merge_status` is already `merged` or `worktree_removed`, so these deferrals do NOT
gate epic completion. Reclaim only on framework lock release (session/agent teardown) or user force
authorization; never force autonomously mid-flight.

## Epic Merge PR

**OPEN — CI running (not yet merged).** The final integration→`main` PR is
[**#390**](https://github.com/drmoisan/TaskMaster/pull/390), head
`epic/utilitiescs-nullable-remediation-integration` @ `a242acdb`, base `main`. GitHub reports
`mergeable: MERGEABLE` (the `origin/main` sync merge removed all conflicts); `mergeStateStatus: BLOCKED`
only because required checks are pending.

- PR body hand-authored in-thread (pr-author skill; `pr-author` is not a registered agent type this
  session) at `artifacts/pr_body_390.md` with SHA-256 receipt `artifacts/pr_body_390.receipt.json`.
  `collect_pr_context` refreshed the context file but resolved HEAD to the session branch (carried
  #374-style MCP anomaly), so the body was authored from the real `origin/main..a242acdb` diff/log.
- REAL CI is running on `main`: run
  [`29720787207`](https://github.com/drmoisan/TaskMaster/actions/runs/29720787207) — checks "Format,
  build, analyze, and test" and "actionlint" — including the now-genuine nullable gate on the
  `windows-latest` runner. A background watcher is waiting for it to conclude.
- Merge is gated by `enforce-epic-merge-gate.ps1` on `epic_merge_pr.ci_gate.conclusion == "success"`
  with `pr_number == 390`. **Not merged while CI is pending.** If CI concludes green in-session, the PR
  is merged via `gh pr merge 390 --merge` and `epic_merge_pr.merge_commit_sha` is recorded; if CI is
  still pending at report time, the epic resumes on its conclusion.
- PR #361 (original CI-gate-repair, head `20d163ac`) is **superseded** by this epic PR and will be
  closed with a comment referencing #390 rather than merged separately (done at final merge).
