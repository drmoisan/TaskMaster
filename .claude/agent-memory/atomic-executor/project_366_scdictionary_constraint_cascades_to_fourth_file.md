---
name: 366-scdictionary-constraint-cascades-to-fourth-file
description: Constraining ScDictionary/WrapperScDictionary with notnull cascades CS8714 into a FOURTH #367 NewtonsoftHelpers file (ScDictionaryConverter.cs, no 'o'); RESOLVED via epic-authorized four-file A'' waiver (commit 685a7a24)
metadata:
  type: project
---

Applying the ratified `where TKey : notnull` constraint to `ScDictionary<TKey,TValue>` and `WrapperScDictionary<TDerived,TKey,TValue>` (#366 Batch 8, [P8-T2]) cascades exactly 4 CS8714 errors into a fourth cross-child #367-owned NewtonsoftHelpers consumer: `UtilitiesCS/NewtonsoftHelpers/ScDictionaryConverter.cs` — at (15,40) referencing `ScDictionary<TKey,TValue>` in its `where TDerived : ScDictionary<TKey,TValue>` clause, and at (30,85)/(31,50)/(43,61) referencing `WrapperScDictionary<TDerived,TKey,TValue>`.

**Why:** `ScDictionaryConverter.cs` (no 'o') is a DISTINCT file from the epic-authorized `ScoDictionaryConverter.cs` (with 'o'). The epic waiver authorized exactly THREE #367 files: WrapperScoDictionary.cs, ScoDictionaryConverter.cs, WrapperScDictionary.cs. ScDictionaryConverter.cs is a genuine FOURTH file. At HEAD (2afaddb9) there are 0 CS8714; the two authorized ScDictionary/WrapperScDictionary edits introduce exactly +4, all in this fourth file. (The ~140 CS86xx flood in the whole-assembly build — SubjectMapEncoder, PeopleScoDictionaryNew, FolderScorer, BayesianPerformanceMeasurement, EmailDataMiner — is PRE-EXISTING at HEAD, identical with/without the edits, and out of scope; do not attribute it to the constraint work.)

**How to apply:** Executor MUST STOP and re-escalate to the epic layer per [P8-T2] ("if a FOURTH cross-child consumer surfaces, STOP") — do NOT widen the waiver to a fourth file on executor authority. The fix, once the epic authorizes a four-file waiver, is one `where TKey : notnull` line on `public class ScDictionaryConverter<TDerived, TKey, TValue> : JsonConverter<TDerived>` (line 15). Related: [[project_366_notnull_cascades_beyond_wrapperscodictionary]], [[project_366_batch7_tnullable_return_cs8766]].

**RESOLVED (2026-07-19, commit 685a7a24):** The epic ratified Option A'' (four-file cross-child waiver, dossier authorized 2026-07-20T01:43:00Z). All THREE deferred constraint lines were applied — `ScDictionary` base + `WrapperScDictionary.cs` + `ScDictionaryConverter.cs` — clearing the +4 CS8714; the closed four-consumer set (WrapperScoDictionary, ScoDictionaryConverter, WrapperScDictionary, ScDictionaryConverter) held with NO fifth consumer. Isolated-cluster gate: 0 CS86xx / 0 CS8714. Tests 5702/5702. Also note for future #366-family work: the branch is an INTEGRATED tree — the sibling #367 (`fix(367)` commit c9284b30) already `#nullable enable`'d the whole NewtonsoftHelpers folder, so a merge-base-with-main diff over-reports NewtonsoftHelpers churn; attribute #366 scope by `feat(366)` commit authorship, not by the raw diff. And the solution-wide `/t:Rebuild` P9-T3 gate legitimately exits 1 (~148 cross-child CS86xx in EmailIntelligence/OutlookObjects sibling files + 2 vendored SVGControl CS0649) — the operative #366 gate is the isolated UtilitiesCS.csproj cluster build, not the solution-wide count (that is the #376 capstone's obligation).
