---
name: 489-review-residuals
description: "#489 itemviewer-surface-defects: cycle-1 reaudit (2026-08-28T04-35) GO/0 blocking — RC-1 PicturesChanged leak cured in-branch (RED-first, 17/17); commit-granularity plan-vs-instruction conflict adjudicated via intent; post-489 baseline 85.1617/79.2335"
metadata:
  type: project
---

Epic child `bug/itemviewer-surface-defects-489` (quickfiler-bug-family wave 2, closes #486/#487/#489/#490).
Cycle 0 (HEAD 74d02ad2): NO-GO, 1 Blocking (RC-1). Remediation cycle 1 ran 25/25 tasks; reaudit at
HEAD 923cd3ce (artifacts committed `a3039548`, timestamp 2026-08-28T04-35): **GO, blocking_count 0**,
62/62 spec ACs PASS (criterion count preserved; the #486 handoff criterion was strengthened in place).

**RC-1 closure evidence chain worth reusing:** (a) source re-count 17 `+=` / 17 `-=` (an 18th textual
`+=` match is a commented-out line — always exclude comments when counting operators); (b) RED TRX
committed in the SAME commit as the test (71e363ea) with the fix in the NEXT commit (6d6a0ee1) — commit
granularity makes RED-first provable from history; (c) the RED TRX's Moq performed-invocation ledger
(17 add_*, 16 remove_*, no remove_PicturesChanged) shows the defect at runtime; (d) reviewer live rerun
of the filtered pair at HEAD binaries (2/2 pass); (e) changed line hits=1 in the runner's gitignored
canonical Cobertura left on disk — its mtime matched the recorded gate run and its figures matched
bit-for-bit, a cheap independent corroboration without emitting anything under artifacts/.

**Commit-granularity adjudication (deviation 4):** plan said "a single commit"; caller's standing
instruction said commit-per-phase; executor followed the instruction and disclosed the divergence in
the artifact with a cumulative path-to-commit attribution. Judged on intent (all required paths
committed, traceable, nothing outside scope) = non-blocking. Residual: remediation plans should encode
the caller's commit-granularity convention so plan text and instruction cannot diverge.

**Spec-amendment-by-reference pattern:** the scope-discipline AC ("diff confined to the members named
in § Sibling-collision resolution") stayed true because the remediation amended the referenced
disposition row rather than the criterion — when an AC delegates to a table, amend the table.

**Deliberate stale sibling test name accepted (RCV-1, Info):** `..._DetachesAllSixteenIntentSubscriptions`
left unrenamed against 17 detachments — assertions all still true (16 individual VerifyRemoves, no
total pinned, no VerifyNoOtherCalls), passes unmodified, staleness documented in spec + handoff
addendum; renaming a merged sibling's stable node ID is churn.

Other reusable: detached-run exit codes are unobservable → foreground re-run is the judged run and does
NOT consume a below-floor jitter allowance; `git grep -c` counts lines so an artifact restating its own
machine-checkable token in prose fails an exactly-one gate (rephrase prose, keep the field).

Non-blocking residuals owed at fan-in / post-merge (unchanged): O1–O8, E1–E4, reframed O3 (FocusSearch
silent no-op caller guard, 444-owned), #489 D4 residual, #490 D5, deferred #490 D1 clear-insertion —
all owe MCP promotion to real issues; BreadcrumbDropDownIntegrationTests.cs exactly 500/500
(EventWiringTests.cs 499, MailActionsTests.cs 498, FolderHandlingTests.cs 498); PA-1 85/75-vs-80/90
wording conflict; E1 stale `<Analyzer Include>` HintPaths on origin/main (cold checkout CS0006).
Post-489 figures (post-processed shape): line 0.851617 (54420/63902), branch 0.792335 (12942/16334).

Hook handling (repeat of cycle 0, per [[review-worktree-differs-from-session-cwd-mirror-artifacts]] and
the #476 precedent): caller forbade session-cwd mirroring and .git/info/exclude edits; artifacts written
+ committed in the caller worktree only; hook simulation exit 0 from the worktree cwd, path-existence
FAIL from the session cwd — accepted, termination relies on the worktree run. pr_context regenerated by
hand at the new HEAD (stale head 74d02ad2) in the hook's `- path (+N/-N)` bullet format.

Related: [[484-review-residuals]], [[464-review-residuals]], [[501-review-residuals]],
[[476-review-residuals]], [[verify-zero-own-effect-coverage-noise-491]]
