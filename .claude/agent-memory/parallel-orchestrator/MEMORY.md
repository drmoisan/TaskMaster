# Parallel Orchestrator Memory Index

- [Parallel run execution playbook](project_parallel_run_execution_playbook.md) — kickoff lives on the plan-home branch; bare `python` IS on PATH; the mutation engine lives only in drm-copilot; close keys on `in_flight` alone
- [Pre-implementation gate scope](project_preimplementation_gate_scope.md) — parallel runs need no synthetic orchestrator-state.json; .json/.js writes blocked everywhere, .md allowed; git commit needs a pathspec operand
- [Free item branches by detaching](feedback_free_item_branches_by_detaching.md) — stale planner worktrees hold the item branches; detach HEAD, never `git worktree remove` (both removal gates fail closed)
- [Issue merge/removal commands bare](feedback_issue_merge_and_removal_commands_bare.md) — the merge gate reads the first digit run in the whole command, so a `cd` path with digits becomes the PR number; also unlock a finished child's worktree before removing
- [Defer the /parallel-add checkpoint write](feedback_defer_the_checkpoint_write_until_admission.md) — a `proposed` item is unrepresentable (invariants 9 + 13); recompute after prep — the verdict flips BOTH ways, and a deferred recolor moves unstarted items and may put the candidate FIRST
- [Reconcile the derived radius against the branch diff](feedback_reconcile_derived_radius_against_branch_diff.md) — agent-memory commits escape plan-text derivation; add exact paths never a glob, and NEVER re-normalize the widened radius (that filter strips them straight back out)
- [Verify delivery before preparing an admission](feedback_verify_delivery_before_preparing_an_admission.md) — grep main, read guard sites, settle residual scope from the feature's spec.md AC table; delivered/non-reproducing/no-diff all reject
- [Resume a dead preparation child, don't restart it](feedback_resume_a_dead_preparation_child_dont_restart_it.md) — diagnose from branch + dirty tree + the PLAN ITSELF; the fourth rung (prepared, checkpoint write lost) is now the FIRST hypothesis — 3 in a row, no child delegated
- [The left-open backlog is now CLEARED](project_left_open_issue_backlog_cleared.md) — READ FIRST: all 11 issues the six family files below call OPEN were verified CLOSED on 2026-09-01; treat their status claims as historical only
- [468-family delivery record](project_qfc_collection_468_family_shipped_issues_left_open.md) — all 7 delivered on main; #474 verified no-residual, so adding any is a no-op (status claims superseded)
- [bugs-635-440 planned run is obsolete](project_bugs_635_440_planned_run_is_obsolete.md) — never executed; #635/#440 shipped via standalone PRs #688/#689 and are CLOSED, so its kickoff artifact is stale
- [464-family delivery record](project_efc_464_family_shipped_issues_left_open.md) — EIGHT issues incl. #460; the merge-commit body names them all, so one bare-number hit settles it (status claims superseded)
- [476-family delivery record](project_webview2_host_476_family_shipped_issues_left_open.md) — #458/#477 delivered under the #476 WebView2 host feature, 37/37 ACs checked (status claims superseded)
- [501-family delivery record](project_breadcrumb_coordinator_501_family_shipped_issues_left_open.md) — the fix-commit subject names no issue AND no slug, so only the bare-number grep finds it (status claims superseded)
- [498-family delivery record](project_breadcrumb_router_498_family_shipped_issues_left_open.md) — 31/31 ACs checked; the subject names `(#499)` outright yet the issue stayed open at the time (status claims superseded)
- [446-family delivery record](project_quickfiler_bug_family_446_shipped_issues_left_open.md) — closing keywords WERE used, but PR #625 targeted the epic integration branch, so they never fired (status claims superseded)
- [Derive counts exhaustively before approving](feedback_derive_counts_exhaustively_before_approving.md) — no count enters an approved AC from a single-pass grep; search the symbol family, cross-check independently, scope counting tools to the named section
- [Self-review before preflight round one](feedback_self_review_before_preflight_round_one.md) — round count above 1 is a process defect; the dominant cause is scoping citations to files the plan NAMES, not the transitive set its edits force
- [Children share one orchestrator-state file](project_children_share_one_orchestrator_state_file.md) — a finishing child's late write lands in the next child's live checkpoint; verify identity fields by substring, stay read-only mid-run
- [Don't repair a concurrent add's partial item](feedback_do_not_repair_a_concurrent_adds_partial_item.md) — a stalled /parallel-add leaves an `admitted` item with no cohort; leave it, report it, keep re-reading before writes
- [PR-context bundle has three coupled defects](project_pr_context_bundle_autoclose_list_is_unsafe.md) — invents issue numbers, falsely reports gh missing, and WORST: classes .cs as docs so C# coverage enforcement silently never runs
- [PR-creation gate + receipt mechanics](project_pr_creation_readiness_gate_and_receipt_mechanics.md) — local_execution_overrides must be empty with no documented drain (escalate to the user); use `cp -p` or the pr-author receipt goes stale
- [Keyed issue_num in delegation prompts](feedback_keyed_issue_num_in_delegation_prompts.md) — "Issue number:" with a space misses the keyed regex; the gate then grabs a bare `#NNN` from prose and denies on the wrong item
- [Confirm CI by conclusion, not watch exit code](feedback_confirm_ci_by_conclusion_not_watch_exit_code.md) — `gh run watch --exit-status` returns 0 on a CANCELLED run; check the conclusion against the FINAL head, and land late commits before CI starts
- [orchestrator-state.json is tracked on main](project_orchestrator_state_json_tracked_on_main.md) — .gitignore can't untrack it, so every child dirties a tracked file and breaches its own footprint clause; the parallel checkpoint is unaffected
- [Defer dirty-worktree cleanup, never force](feedback_defer_dirty_worktree_cleanup_never_force.md) — a merged item's worktree often holds unrelated build repair; also the delivery worktree may not be the tracked one, and that one no gate can remove
- [Blast-radius PowerShell calling convention](reference_blast_radius_powershell_calling_convention.md) — absolute Import-Module, `-DateKind String`, read the `conflict` key; four traps, incl. a `detail` string that names the wrong pair
- [PowerShell checkpoint-write traps](reference_powershell_checkpoint_write_traps.md) — three: `[ordered]` integer keys index POSITIONALLY; a 1-element pipeline result collapses so `[0]` yields a CHARACTER; long heredocs die regardless of backticks
- [mandate_reads omits scripts/vscode/**](project_mandate_reads_omits_scripts_vscode.md) — it's a derivation-time citation filter; every C# plan cites the coverage script, so every C# pair contends and the surface goes serial
- [Raising max_concurrency is a no-op on a complete graph](feedback_raising_max_concurrency_is_a_noop_when_graph_is_complete.md) — set it on the checkpoint never the manifest; count missing pairs first, and never buy lanes by shrinking the radius
- [Absence gates don't prove validity](feedback_absence_gates_do_not_prove_validity.md) — a redaction rule mandated angle brackets into XML attributes, corrupting 6 files while the gate passed on token-absence alone; check the replacement is legal where the producer writes it
- [BASH DISCIPLINE block in every child prompt](feedback_bash_discipline_block_in_every_child_prompt.md) — the permission engine splits on `&&`/`;`/`|` and needs EVERY segment allowed, so one grep/sed/cd segment prompts the whole line; allow list is only git/poetry/pwsh + 3 scripts
- [Never mix gh UTC with local timestamps](feedback_never_mix_gh_utc_with_local_timestamps.md) — a verbatim `mergedAt` fabricates a barrier violation that never happened; the passing case is just as wrong, so validation success proves nothing
- [Quiescence is not a liveness test](feedback_quiescence_is_not_a_liveness_test.md) — an idle worktree cannot distinguish a finished child from one paused between delegations; relaunching on it put two orchestrators on one worktree
- [Reuse existing item worktrees](feedback_reuse_existing_item_worktrees.md) — operator wants the planner-era worktrees reused: omit `isolation`, make the absolute path a binding first-line directive, and ff-merge inside the worktree
- [Never cd before a file operand in delegation prompts](feedback_never_cd_before_a_file_operand_in_delegation_prompts.md) — a cd prefix makes the operand statically unresolvable and any Read() deny rule then forces a permission prompt
