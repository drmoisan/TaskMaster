---
name: project-424-quickfiler-deadline-plan-seams
description: "#424 plan decisions: 12s DefaultFirstBatchDeadline const, Timeout.InfiniteTimeSpan sentinel, liveness-flag clear in Worker_DoWork finally (not inside LoadRemaining), Part2 partial gate-test file, QfcScanProgressBandMapper new module, fail-before split real-test vs dossier"
metadata:
  type: project
---

Planning seams fixed for issue #424 (QuickFiler High Confidence queue-init stall), plan at `docs/features/active/2026-08-06-quickfiler-high-confidence-queue-init-stall-424/plan.2026-08-06T21-17.md`:

- **Deadline:** `internal static readonly TimeSpan DefaultFirstBatchDeadline = TimeSpan.FromSeconds(12)` on the gate (spec range 10–15 s); sentinel `Timeout.InfiniteTimeSpan`; guard rejects other non-positive values. Not a `QfSettings` member (AC 10).
- **Liveness flag clear location deviation:** spec design-summary says clear in a `finally` at the end of `LoadRemainingEmailsToQueueAsync`, but the spec's own Test Strategy drives flag transitions via the `RemainingEmailLoader` seam — a clear inside the real loader is unobservable when tests substitute the seam. Plan clears in `Worker_DoWork`'s `finally` wrapping `await RemainingEmailLoader(...)`; AC 7's binding wording ("false only after the loader completes, cleared in a finally") is satisfied identically. Deviation recorded in the plan Decisions Record item 3.
- **Fail-before feasibility split:** a fail-before regression test must compile against the pre-fix surface. Deadline and liveness-honesty changes admit real compiling failing tests (assert bounded scan count / keeps-polling using only existing ctor + seams); the progress-callback and interface-overload changes cannot (seam absent pre-change → compile error, not a failing run) and carry `fail-before-exception.*.md` dossiers per evidence conventions. Design the real failing test to terminate deterministically pre-fix (finite 50-candidate source, sourceActive false after exhaustion) — an unbounded-scan repro must not hang.
- **File-size posture (verified counts 2026-08-06):** QfcDatamodel.cs 479 and QfcHomeController.cs 477 vs 500 limit → flag field declared in QfcDatamodel.QueueProcessing.cs (139); mapping logic in new `QuickFiler/Controllers/QfcScanProgressBandMapper.cs` (new module, >=90% gate); fallbacks pre-decided: relocate `RunAsync` to `QfcHomeController.Run.cs` partial / move `ScoreRemainingQueueMailItemAsync` into QueueProcessing partial. Gate tests (300 lines) extended via `partial` + `QfcStreamingDequeueConfidenceGateTests.Part2.cs`; both QuickFiler csprojs are legacy → every new .cs needs `<Compile Include>`.
- **O1 adopted:** 1000→200 ms only at `QfcHomeController.cs:294`; `Iteration.cs:23` exact-arg pin `(8, 2000)` untouched. Existing 2-arg `DequeueNextItemGroupAsync` delegates to the new 4-arg overload with the default deadline, so post-UI dequeues also become bounded — spec-authorized.

**Preflight revision 1 outcomes (nine findings on a structurally-valid plan):**
- `.Part2.cs` partial test files must NOT repeat `[TestClass]` (`AllowMultiple = false`, CS0579); attribute stays on the base file only (cf. `BreadcrumbDropDownOpenCoordinatorTests.cs` vs its Part2).
- Rewiring `WaitForQueue` (`QueueProcessing.cs:132`) to the flag breaks `WaitForQueue_WhenWorkerBusyAndQueueShort_AwaitsInjectedTwoHundredMsDelay` (`QfcDatamodelTests.cs:281-309`, drives via `SetPrivateField(worker, "isRunning", true)`); a swap of a consumed signal needs a companion task updating every test that drives the old signal.
- `ProgressTracker.Report` is `(double, string)` (`ProgressTracker.cs:121`); method-group conversion to `Action<int, string>` is CS0123 — the mapper ctor takes `Action<double, string>`.
- Existing gate cancellation test cancels only pre-first-take; AC "cancellation during scan / during empty-poll wait" needed two new tests (park on `TimeProvider.Delay`; cancel inside score loader hitting the post-score check), run under both deadline configs.
- `BackgroundWorker.isRunning` clears asynchronously post-`WorkerThreadStart` — "IsBusy already false" is a race; the fail-before liveness test must await a bounded state signal (pattern `QfcInitEmailQueueZeroBatchTests.cs:97-108,139-144`). "No wall-clock waits" was softened to "no fixed sleeps; bounded TCS waits only".
- Executor demanded a `P3-T5a` insertion; validator requires digit-only sequential IDs → inserted as P3-T6 with downstream renumber (see [[plan-validator-task-id-sequential-constraint]]).
- Baseline porcelain was not empty (agent-memory + feature folder); P0 git-baseline acceptance now allows exactly those path prefixes and blocks on any production/test/csproj entry.
- Canonical scoped vstest form pinned in a Decisions-Record item: [[reference-vstest-scoped-run-command]]; csharpier 1.2.6 needs `format`/`check` subcommands.
- Confirmed by executor: `InternalsVisibleTo("QuickFiler.Test")` exists (internal mapper directly constructible); single `IQfcDatamodel` implementer and all 18 mocks loose-behavior, so interface expansion is safe.

**Mid-execution delta v1.2 (blocked at P5-T1):**
- `QfcHomeControllerIssue218Tests.cs` was misclassified as dormant by both spec and plan (grouped with #171/#169 suites). Its two tests `Setup`/`Verify` the two-arg `DequeueNextItemGroupAsync` on a **loose** mock, so moving the call site to the four-arg overload produced zero-invocation `MockException`s at run time — invisible to the strict-mock/compile-break grep. Lesson: when a plan migrates a call site to a new overload, grep ALL test files for `Setup`/`Verify` of the **old overload shape** (loose mocks hide the break), and never classify a test file as "dormant/unchanged" without reading what its tests actually assert. A byte-unmodified + must-pass requirement on a file that pins the changed call is self-contradictory.
- Coverage reality: repo baseline is 70.19% line / 58.30% branch — below the 80% floor before any change. Orchestrator-ratified handling (plan Decisions item 13): the 80% floor applies to the testable denominator after the CLAUDE.md § UT2 COM/VSTO exemptions; the raw repo-wide figure is pre-existing debt, **reported non-blocking**; binding gates are changed-line no-regression + new/changed modules/methods >= 90%. Do not gate a bug-fix plan on the raw repo-wide figure.
- Executor-observed growth: Part2 gate tests landed at 584 lines, QfcDatamodelTests at 529 — the P5-T2 pre-decided relocations (Part3 partial; QfcDatamodelLivenessTests with duplicated helpers) both fire. The 458-503 projection for QfcDatamodelTests under-estimated by ~30 lines; bias projections upward.
- Mid-execution insertions: preserve executed tasks' checked state and text verbatim, change only IDs when renumbering (here P4-T7→T8, T8→T9), and move traceability cross-references with them.

Related: [[literal-call-clauses-block-file-size-tightening]], [[project_400_partial_class_headroom_placement]], [[reference-invoke-mstest-with-coverage-script]], [[project-coverage-threshold-conflict-claude-md-vs-general-unit-test]], [[reference-vstest-scoped-run-command]].
