---
name: project-446-quickfiler-bug-family-plan-seams
description: "#446 family plan (quickfiler-bug-family-446) R1+R2 preflight seams: folder slug vs descriptive slug, seam-before-COM-test ordering, AC28/AC18 conflict via unchecked box + REMEDIATION-REQUIRED, reconciliation branches propagated to all downstream consumers, gh-issue-create denied by the promotion hook, line-count-changing spec edits invalidate plan pointers"
metadata:
  type: project
---

Preflight R1 seams for `docs/features/active/quickfiler-bug-family-446/plan.2026-08-24T09-37.md` (issues #426/#427-A/#446/#448).

- The active feature folder is `quickfiler-bug-family-446`; the plan's descriptive slug is `quickfiler-queue-datamodel-defects` (H1 title, commit scopes). Never use the descriptive slug in a path.
- Ordering: the `ScoringServiceFactory` seam task must precede any test exercising `DequeueNextItemGroupAsync` with high-confidence mode on, or the test path reaches live Outlook COM via `new FolderScoringService()` (UT4 violation). Now `[P1-T5]` (seam) before `[P1-T6]` (test).
- AC28 names three TYPES but sibling-owned partial files make a literal whole-type >=90% gate conflict with AC18. Resolution: blocking gate on the three changed FILES (`[P5-T7]`), whole-type held to no-regression; `[P5-T17]` leaves the AC28 box unchecked and records `REMEDIATION-REQUIRED: AC28 whole-type reading conflicts with AC18` when any whole-type figure is below 90.00. Terminal gate accepts checked-with-evidence OR unchecked-with-recorded-gap. Do not edit spec.md.
- Known load-flaky tests exist in the coverage-enabled/Workers-0 configuration (UtilitiesCS.Test timing tests, QuickFiler WinFormsPumpHost tests), so `[P5-T5]` and `[P5-T2]` carry baseline-identical reconciliation branches keyed to `[P0-T9]`/`[P0-T12]`; `[P0-T12]` must record failed-test NAMES for that comparison to be satisfiable.
- `Invoke-MSTestWithCoverage.ps1` prints only `Discovered N test assemblies.` (a count, not a list); tasks needing the list must reproduce it via the discovery prelude.
- `QfcItemController.FolderHandlingTests.cs` is 498 lines (compliant), not a 500-cap violator.
- R2 preflight lesson: adding a reconciliation branch to a gate task ([P5-T2]/[P5-T5]) is incomplete until EVERY downstream consumer is branched too — the coverage re-run ([P5-T6]), the clean-pass aggregator ([P5-T9]), the AC check-off ([P5-T16], unchecked box + `REMEDIATION-REQUIRED: AC27 ...` line), AND the phase preamble's restart rule (a step completing on its reconciliation branch is not a failure, else the loop never terminates). Same shape as [[thread-granted-discharges-through-consumers]].
- R2 also caught a residual unscoped `git status --porcelain` in the terminal commit task despite the plan's own scoped-gates convention paragraph — sweep TERMINAL commit tasks specifically when asserting "no unscoped git gate" ([[agent-memory-is-tracked-scope-git-gates]], [[terminal-phase-planner-traps]]).
- spec.md:966-967 stale pointers (old folder slug) were corrected via an extension to [P0-T2] rather than a new task, to avoid renumbering; document-pointer fixes to spec.md are permitted, AC text is not. Do NOT "helpfully" fix those pointers during a plan revision — [P0-T2] asserts a zero-match grep against them at execution time, so pre-fixing makes its acceptance vacuous.
- 2026-08-25 re-base onto PR #610 (closed #608), which landed on main after R2 cleared and touched all three gate files. Net line shifts in `QfcStreamingDequeueConfidenceGate.cs`: +2 from 42, +6 from 109; file 171 -> 177. Exits moved `:98/:113/:122/:148` -> `:100/:119/:128/:154`; deadline guard `:112-116`, log `:118`; accept decision `:138-141` -> `:144-147`; 8-param ctor `:55-64` -> `:57-66`; `DequeueAsync` sig `:87-91` -> `:89-93`. Gate test-method total 21 -> 23. UNCHANGED and re-verified: every citation into `QfcStreamingDequeueConfidenceGateTests.cs` (#610's hunk was appended at `@@ -369,5 +369,56 @@`, so nothing before :369 moved), `Part2.cs:36-69`, `QfcDatamodel.QueueProcessing.cs:128`, and the `GetConstructor` count of 4. Recorded as plan decision D-Plan-9. See [[absolute-counts-in-shared-files-go-stale]] for why the 21 -> 23 clauses became relative rather than absolute.

- R2-on-the-rebase (2026-08-25) findings, two of them SELF-INFLICTED by R1's own fixes: (a) re-pinning a provenance sentence to branch HEAD must not sweep a clause whose SUBJECT is the research document — the research was never re-verified, so its line numbers are stale and the spec must say so explicitly rather than claim re-verification on its behalf; (b) any spec edit that changes LINE COUNT invalidates every `spec.md:<n>` pointer the plan holds — apply the line-count-changing edit FIRST, then re-derive the pointer with a fresh `git grep -n` and never trust a number carried in from the finding report.
- `gh issue create` / `gh issue new` are DENIED by the registered PreToolUse hook `.claude/hooks/enforce-promotion-mcp-only.ps1` (`PROMOTION_MCP_ONLY_BLOCKED`). A follow-up-issue task must route through `mcp__drm-copilot__new_potential_entry` then `mcp__drm-copilot__potential_to_issue`, and its acceptance cannot be `EXIT_CODE: 0` — an MCP-tool task has no exit code, so the plan's fail-closed evidence rule needs a `Tool:`/`Result:` artifact variant. Same family as [[feedback_promote_latent_defects_to_issues]].
- Inserting a caveat clause AFTER a semicolon that terminated a list orphans the following clause's verb and drags it inside the caveat's `if`. Parenthesize the caveat and give the trailing clause its own explicit verb ("retarget, unconditionally and in every case, ...").
- An acceptance that says "verified by reading the post-change test body" goes vacuous when an EARLIER task extracts that body into a helper. Name the helper as an alternative read target and add a second conjunct asserting the substantive change.
- `.claude/rules/csharp.md` belongs in the Phase 0 read list for any C# plan (policy-compliance-order item 4). It records `Task.Delay` as banned under RS0030 and states a repo-wide >=80% floor; a plan that demotes the repo-wide figure to record-and-report must pre-resolve that as a settled scope decision, or CLAUDE.md's halt-on-conflicting-instructions rule fires mid-execution.

**Why:** these were the B/H findings of the atomic-executor preflight rounds 1 and 2 on 2026-08-24 and round 2 on the #610 rebase on 2026-08-25; a later remediation cycle on this feature will re-hit the same seams.
**How to apply:** when revising or extending this feature's plan, preserve the seam-before-test ordering, the two-scope coverage split, and the reconciliation branches; see also [[enumerate-condition-outcomes-before-case-list]] and [[coverage-gate-clr-invoked-private-members]].
